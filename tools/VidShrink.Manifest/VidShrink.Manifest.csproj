<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <IsPackable>false</IsPackable>
    <AssemblyName>vidshrink-manifest</AssemblyName>
    <RootNamespace>VidShrink.Manifest</RootNamespace>
  </PropertyGroup>

  <!-- No ProjectReference on purpose: this is a CI helper, not part of the application.
       Nothing that ships to a user references it. -->

</Project>
