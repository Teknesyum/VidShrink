# Kapalı Denetim — .claude/kapali.md, satır 126-270

## Sayılar

- Toplam `- [x]` satırı: **139**
- Commit hash içeren satır: **73** (hepsi `git merge-base --is-ancestor <hash> main` ile ATA — tek istisna yok)
- Salt "ajan: ... — sonucu aktarıldı/aktarılacak" biçiminde, doğrulanacak somut iddiası olmayan satır: **25** (YOKLANAMAZ — kod/dosya/hash yok)
- Somut kod/test/belge iddiası taşıyan ve tek tek Grep+Read ile doğrulanan satır: **~40** (aşağıdaki liste), hepsi DOĞRULANDI
- ŞÜPHELİ (yanlış bulunan): **0**
- Düşük güvenle doğrulanan (eşleşme buldum ama iddianın birebir sözcüğü — değişken adı gibi — tutmuyor): **1**

## Doğrulanan başlıca iddialar (örnek, tükenmeyen liste)

- Tüm 73 commit hash `main`in atası — `git merge-base --is-ancestor` ile tek tek kontrol edildi, hepsi ANCESTOR.
- `OrtakOdakTests.AyniYoldaSahipDegisinceOlayYayiliyor` DENETIM notuyla kaldırılmış, yerine `OrtakOdakTests.OluOdakYuzeyiGeriGelmiyor` geldi — `tests/VidShrink.Tests/OrtakOdakTests.cs:344` doğrulandı, eski ad artık yalnız AGENTS.md'de tarihsel not olarak var.
- `KaydediciAyarYalitimTests.KapiKapandiktanSonraBekleyenYazmaDosyayiKirletmiyor` — `tests/VidShrink.Tests/KaydediciAyarYalitimTests.cs:86` var.
- `GelismisAyarGidisDonusTests.cs` mevcut ve AGENTS.md açıklaması iddiayla (IndexOutOfRangeException, CmbAdvTune) örtüşüyor.
- `docs/olcumler/kaydedici-ayar-yalitimi.md` — satır 91-95 VIDSHRINK_LIBMPV şartı, satır 115-117 "toplam 171" doğrulandı.
- `RecorderArguments.cs`: `PixelFormatsFor` (satır 525), `Vp9Profiles = {"0","1","2","3"}` (satır 362), `KnownColorRanges` içinde `mpeg`/`jpeg` (satır 353), `MaxKeyframeSeconds=60` (satır 288) — hepsi kaynakta.
- `AudioCaptureArguments.cs:142,150`: `MinGainDb=-60`, `MaxGainDb=30` — kaynakta (not: `StreamMapping.cs`'de AYRI bir `MinGainDb=-20/MaxGainDb=20` de var, farklı bağlam, karışıklık yok).
- `RecorderView.Serit.cs:65` `OnSnapshot` → `SnapshotAsync` çağırıyor — doğrulandı.
- `error.bad-range` — `CliRequest.cs:190,328` üretiyor, `CliTests.cs:243` ve `KesBolumKareTests.cs:102` okuyor (borç kapanış notu "pim kodda zaten kapalı" doğru).
- `TrimWindow.Of` kendisiyle karşılaştırmıyor — `KucultmeAraligiTests.cs:269-273`, sabit beklenen değerlerle (`10.0`, `600.0`, `590.0`) karşılaştırıyor; "zaten kapalı" notu doğru.
- `main.unit.score-suffix` / `score-value` / `main.advanced.mode.crf` 42 dilde pimli — `BiciminTests.cs:586-588,758-777` doğrulandı.
- `ClickArbiter.Source`, `PlayerInputMap.cs:265` sınıfı var; `Fare.cs` → gerçek dosya adı `PlayerView.Fare.cs:42-47`, Aero Snap cümlesi birebir orada.
- `AltyaziKanit.Kapat` → ortak `KanitKapanisi.Kapat` — `tests/VidShrink.Tests/KanitKapanisi.cs:23-49`, joker eşleme mantığı satır 36-38'de "eski AltyaziKanit.Kapat" notuyla birebir.
- `PendingCount` kaynaktan silinmiş — yalnız üçüncü taraf `System.Private.CoreLib.dll` ikili dosyalarında eşleşiyor, proje kaynağında yok.
- `tools/libmpv/libmpv-2.dll` var (114.8M) ve `.gitignore`'da.
- HandBrake içe aktarma iletisi/özellik uyuşmazlığı (satır ~130 civarı, tarihsiz kapanış): `MainWindow.OnAyar.cs:328-333` artık `PresetFileError.HandBrakeFile` yakalayıp `HandBrakePresetImport.TranslateFile` çağırıyor — özellik gerçekten açılmış (commit `0666989b`, main'in atası). İddia doğru ama satırda kapanış hash'i YOK; kanıt yalnız koda bakılarak bulundu.

## Düşük güvenle doğrulanan / not

| Satır (yaklaşık) | İddia | Bulgu | Neden |
|---|---|---|---|
| "Borc: Assert.True(uzak >= 5)..." (2026-09-18, t0/pim-borclari) | Mutasyon sınır değeri (5) ölçünün kendi beklediği değerle örtüşüyor, HB pimi | `FfmpegArgumentsTests.cs:640`: `Assert.True(g / fps >= 5.0, ...)` — konu ve sayı (5) örtüşüyor ama değişken adı `uzak` değil `g`; `KucultmeAraligiTests.cs:108-109`'da `uzak` adlı değişken var ama HB/mutasyon bağlamı yok. Birebir satırı bulamadım. | Yanlış olduğunu göstermedim (kod tarafında "zaten kapalı" tutarlı bir aday var), ama tam eşleşmeyi doğrulayamadım — SUSPICIOUS değil, YOKLANAMAZ. |

## Genel değerlendirme

Bu dilimde denetlenen tüm commit hash'leri (73/73) main'in atası; örneklenen ~40 somut kod/test/belge iddiasının hepsi kaynakta doğrulandı (dosya, satır, sınıf/yöntem adı, sabit değer birebir eşleşti). Hiçbir satırda "iddia" ile "kod" arasında çelişki bulunmadı — 0 ŞÜPHELİ. 25 satır ("ajan: ... sonucu aktarıldı") doğası gereği yoklanamaz (ne dosya ne hash veriyor). 1 satır (Assert.True(uzak>=5) HB pimi) düşük güvenle doğrulandı — muhtemel aday bulundu ama değişken adı tutmadığı için tam teyit edilemedi.
