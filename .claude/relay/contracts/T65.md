---
id: T65
title: Büyük harf geçidi birimleri bozuyor, sekme değişince kırpılma ölçüsü ölü
role: builder
model: opus
depends: []
status: active
round: 1
audit: —
auditor_id: —
diff: —
verification: —
owns: [src/VidShrink.App/LanguageCatalog.cs, tests/VidShrink.Tests/CasingTests.cs, tests/VidShrink.Tests/WindowLayoutTests.cs]
side_effects: []
---

## Amaç

T64 mühürlenirken iki borç devredildi. İkisi de T64'ün `owns`'ı dışındaydı, ajan
doğru davranıp dokunmadı. İkisi de **panelden geniş** — uygulamanın tamamını
ilgilendiriyor.

## İş 1 — büyük harf geçidi birimi ve tanımlayıcıyı bozuyor

`LanguageCatalog.Title` (`src/VidShrink.App/LanguageCatalog.cs:82`) ekrana giden her
cümleyi başlık biçimine çeviriyor. Bu, cümleler için doğru; **birimler ve kodlayıcı
adları için yanlış**:

- `ms` → `Ms`. SI'da `Ms` megasaniyedir. Yeni başarım panelinde bütçe dolduğunda
  ekranda "(21000 Ms Of 20000 Ms)" çıkıyor.
- `libx264` → `Libx264`, `h264_nvenc` → benzeri. Bunlar ffmpeg kodlayıcı adları,
  kullanıcı bunları kaydedicisinin ayarlarında arayacak; yazımı değişirse bulamaz.

T64'ün ajanı bunu **kaçınarak** çözdü: sayı bloğunu `Run`'lardan kurup geçitten
kaçırdı (`MainWindow.axaml.cs:2087-2091`). Kaçınma çalışıyor ama sebep yerinde
duruyor ve bir dahaki cümleyi yazan yine tökezleyecek.

**Yapılacak:** geçidin kendisi bu iki sınıfı tanısın. Ölçü birimleri (`ms`, `MB`,
`kB`, `fps`, `Hz`, `dB` ve dosyada geçen ötekiler) ve kodlayıcı/kodek tanımlayıcıları
(`libx264`, `libx265`, `h264_nvenc`, `hevc_nvenc`, `h264_qsv`, `h264_amf`, `aac`,
`opus` ve dosyada geçen ötekiler) **yazıldığı gibi kalsın.**

Listeyi uydurma — kaynakta gerçekten geçenleri bul, `rg` ile tara. Bulduğun listeyi
tek yerde topla; iki ayrı yerde tutulan liste bir süre sonra ayrışır.

**Dikkat:** `LanguageCatalog` sözlükleri `Title(...)` çıktısıyla **anahtarlanıyor**
(`:311`, `:324`, `:361-368`). Geçidin davranışını değiştirmek bu anahtarları kaydırır
ve çeviriler sessizce düşer. Bunu ölç: değiştirdiğin geçidin ardından İngilizce ve
Türkçe sözlüklerin **her girdisi hâlâ bulunabiliyor mu**.

### Kabul kriteri

**K1 — birim ve tanımlayıcı bozulmuyor.**
CHECK: `ms`, `MB`, `fps`, `libx264`, `h264_nvenc` içeren örnek cümleler geçitten
geçirilir, bu parçalar yazıldığı gibi çıkar; cümlenin geri kalanı başlık biçiminde
kalır.

**K2 — sözlükler düşmedi.**
CHECK: `EnglishSource` ve Türkçe karşılıklarının **her** girdisi geçit değişikliğinden
sonra hâlâ çözülüyor; ölçü sözlük üzerinde döner, örnek seçmez.

**K3 — kural tek yerde.**
CHECK: birim/tanımlayıcı listesi tek bir bildirimde; kaynakta ikinci bir kopya yok.

## İş 2 — sekme değişince kırpılma ölçüsü boşa dönüyor

`tests/VidShrink.Tests/WindowLayoutTests.cs:594-629`,
`NoTextIsClippedAtTheSmallestSize` her sekme için `tabs.SelectedIndex = index` yapıp
`LayOutAt(...)` çağırıyor. T64'ün ajanının ölçtüğüne göre **seçim değiştikten sonra
yeni sekmenin içeriği ölçülmeden kalıyor**: `Bounds` 0×0 çıkıyor, çünkü
`window.UpdateLayout()` onu sıfır `ClientSize` ile ölçüp temiz işaretliyor. Sonuç:
`block.Bounds.Width > 0` süzgeci bütün blokları eliyor ve ölçü **ilk sekme dışında
hiçbir şeyi denetlemiyor.**

Bu, sessizce ölmüş bir güvenlik ağı — üstelik uygulamanın bütün sekmelerini
kapsıyordu.

**Önce doğrula, sonra düzelt.** İddia T64 ajanınındır; kendi gözünle ölç. Doğruysa
ölçüyü çalışır hale getir (T64'ün ajanı kendi ölçüsünde pencereyi hiç sürmeyen ikinci
bir geçişle aştı, `PerformanceCheckUiTests.cs:111-119` — oraya bak, ama kendi
çözümünü seç). Yanlışsa **düzeltme yapma**, neyi ölçtüğünü yaz.

### Kabul kriteri

**K4 — iddia ölçüldü.**
CHECK: düzeltmeden önceki halde kaç sekmenin kaç bloğunun ölçüldüğü sayılır ve ham
dosyaya yazılır; düzeltmeden sonra aynı sayım tekrarlanır.

**K5 — ölçü artık gerçekten koruyor.**
CHECK: bir sekmeye kasten taşan bir metin konulduğunda ölçü **kırmızıya döner**.
Bunu geçici olarak yap ve geri al; kanıtı ham dosyaya yaz. Kırmızıya dönmüyorsa
düzeltme çalışmıyor demektir.

**K6 — Gelişmiş sekmesi artık kapsanıyor.**
CHECK: en küçük boyutta koşulan ölçü, T64'ün eklediği başarım panelinin bloklarını
da görüyor; görülen blok sayısı sıfır değil.

**K7 — süit yeşil.**
CHECK: `dotnet build -c Release` 0 hata 0 uyarı; sade `dotnet test -c Release`
tamamı yeşil ve taban 761 geçti / 0 kaldı / 16 atlandı / 777 toplam'ın altına düşmez.

## Sınırlar

- `src/VidShrink.Core/**` ve `src/VidShrink.Ffmpeg/**` kapalı.
- `MainWindow.axaml` ve `MainWindow.axaml.cs` kapalı. T64'ün `Run` kaçışına dokunma —
  geçit düzelince gereksizleşse bile kaldırmak ayrı bir sözleşmenin işi.
- Renk ve ölçü yalnız `Themes/Theme.axaml` belirteçlerinden.

## Sayı yasağı — T63'ten devreden kural

Docstring'de ve kod açıklamasında **ölçüm sonucu bildiren sayı yasak.** Sayı yalnız
ham günlükte ve raporda yaşar. Kodun ne yaptığını tanımlayan sayı (sabitler, biçimler)
serbest. Ham günlüğe **satır numarasıyla atıf yapma** — günlük eklemeli, bugün doğru
olan atıf yarın başka şeyi gösterir.

Satır içi `//` kod yorumu yazma; anlatılacak şey docstring'e girer.

## Kayıt noktası ve çıktı

Ham çıktı `.calisma/t65/` altına. Dalını `worktree` yalıtımında aç.

Teslimde: commit hash'i, `dotnet build` ve sade `dotnet test` ham sayıları, yedi
kriterin karşısına ölçü adı ve dosya:satır. **Beyandan önce `git diff` oku** — bu
projede dört tur, "kaldırdım" deyip kaldırmamaktan açıldı.
