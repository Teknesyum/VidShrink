---
id: T109
title: Sahne esigi icerikten turetilsin, sabit kalmasin
role: builder
model: opus
depends: [T105]
status: submitted
round: 1
verify: [dotnet test -c Release --filter "SceneMapTests"]
owns: [src/VidShrink.Core/SceneMap.cs, src/VidShrink.Ffmpeg/SceneDetector.cs, tests/VidShrink.Tests/SceneMapTests.cs, docs/olcumler/dinamik-esik.md]
side_effects: []
---

## Bağlam

Ürünün farkı burada duruyor. Kullanıcının kendi cümlesiyle: sabit pencere gibi
**statik** bir yöntem yerine içerikten türeyen **dinamik** bir işleyiş. T105
bunun ilk somut kanıtını ölçtü ve kanıt tek eşiğin aleyhine çıktı.

T105 üç pencerede eşik eğrisi çizdi. En iyi eşik pencereden pencereye kayıyor:

| Pencere | İçerik | En iyi eşik |
|---|---|---|
| P1 (144,117–333,300] | karışık, 28 kesim | 0,105–0,110 |
| P2 (333,300–519,666] | durgun, 7 kesim | ≤0,08 |
| P3 (600,000–789,000] | hareketli, 0 kesim | ≥0,115 |

**Durgun ve hareketli ters yöne çekiyor**, aradaki açı ~0,035. Seçilen 0,105
üç pencerenin birleşiminde F2 tepesi — yani hiçbirinde en iyi değil, üçünün
ortalamasında en az kötü. Sabit tek eşiğin tanımı budur.

T105 doğru olanı yaptı: ölçmeden oynatmadı, seçtiğini savundu, kaymayı yazdı.
Bu sözleşme kaymanın kendisini alıp **eşiği içerikten türetir.**

## T105 muhurlendi — devraldigin durum (T0, 2026-09-02)

T105 denetimi GECTI verdi ve `main`e birlesti. Baslangic noktan `main`:
`SceneMap.DefaultThreshold = 0.105`, `SceneDetector.BaseThreshold = 0.05`,
`DefaultMinSceneSeconds = 1.0`. Denetci sekiz kriterin sekizini de bagimsiz
dogruladi; 16 esik satirinin 16'si ham CSV'den yeniden hesaplandi. Yani
yukaridaki tablo saglam zemin.

Denetimin biraktigi ve **seni ilgilendiren** iki borc:

- **`tools/sahne-yer-gercegi/sahneler.csv` eski esigin haritasi** — 24 satir,
  0,2 ile uretilmis. Yeni esigin sahne listesi yalniz `.calisma/` altinda kaldi,
  yani gitignore'lu. Ona bakan bir sonraki okuyucu **eski haritayi** gorur.
  `tools/` senin `owns`'inda degil; **kendin uret, kaynagi belgene yaz**, ve
  eski CSV'nin bayat oldugunu rapora bildir — tasimayi ben yaptiririm.
- **Belgedeki egri tablosunda P2'nin uretilen/yanlis pozitif sutunu yok** —
  `sahne-haritasi.md:56-73` P2 icin yalniz "yakalanan/7" veriyor; P2 FP = 0
  iddiasi duz cumlede. Senin ureteceğin tabloda **her pencere icin ucu de**
  (uretilen / yakalanan / yanlis pozitif) sutun olsun.

Ayrica: **T98 tur 2 ayni anda kosuyor** ve `SceneMapThresholdOfRecord`'u
0,105'e tasiyip boleni o esikte yeniden olcuyor. Esigi turetilir yaparsan o
sabit yine ayrisir. Bu **beklenen** — dosya senin degil, rapora yaz, ben T98
tarafinda kapatirim. T98'i bekleme, olcumune basla.

## Neden bu iş önemli

Haritanın iki müşterisi var ve ikisi de eşiğe bakıyor: T98 anahtar kare
aralığını, T104 ölçüm penceresini haritadan türetiyor. Harita yanlış sahne
sayısı verince ikisi de yanlış yerden başlıyor. Bugün durgun içerikte harita
kesim kaçırıyor, hareketli içerikte uydurma kesim üretiyor — ve ikisine de
aynı sayıyla gidiyoruz.

Ayrıca T105 iki yerde T101'i yanılttı: "yanlış pozitif sıfır" yalnız 0,2 için
doğruymuş (0,05'te üç pencerede 46 yanlış kesim), ve `DefaultMinSceneSeconds`
payı sıfır değilmiş. **Sabit sayının etrafına örülen sonuçlar sabit değişince
düşüyor.** Türetilen bir eşikte bu kırılganlık azalır.

## Kabul kriteri

1. **Türetme girdisi ölçüyle seçilir.** Eşiği neyden türeteceğin bir tasarım
   kararı değil, ölçüm sonucudur. En az **üç aday** sınanır — öneri: kesim
   skorlarının dağılım istatistiği (medyan/çeyrekler arası açıklık),
   T101/T105'in karmaşıklık ölçüsü, ve kare arası hareket miktarı. Her aday
   için: o girdiden türeyen eşik, üç pencerenin her birinde F2'yi sabit
   0,105'e göre ne yapıyor?
   CHECK: aday × pencere ızgarası, hücrede F2.
   **Hiçbiri sabiti yenemiyorsa bu da sonuçtur** — "ölçüldü, sabit eşik daha
   iyi" yaz ve türetmeyi koyma. Ürünün dinamiklik ilkesi ölçünün önüne
   geçmez.

2. **Üç pencere yetmez.** T105'in üç penceresi kalibrasyon kümesidir; onlara
   uyduracağın kural onlarda iyi çıkacaktır. En az **iki yeni pencere** elle
   işaretlenir — biri durgun, biri hareketli — ve kural **orada** sınanır.
   İşaretleme yöntemi T105'inkiyle aynı olsun (tara → çiftler → gözle ayrım);
   yer gerçeği listesi belgeye gömülür.
   CHECK: iki yeni pencerede F2, sabit 0,105 ile yan yana.

3. **Yanlış pozitif ayrı sayılır.** T101 "yanlış pozitif sıfır" dedi ve
   yanıldı çünkü tek eşikte ölçtü. Türeyen eşik pencereye göre aşağı
   inebiliyorsa yanlış pozitif riski de pencereye göre değişir. **Sayılır,
   varsayılmaz.**
   CHECK: pencere başına yakalanan / kaçan / yanlış pozitif.

4. **Takas asimetrisi korunur.** Kaçan kesim bir sahnenin bit bütçesini
   komşusuyla karıştırır ve hatayı sahne boyunca taşır; yanlış kesim fazladan
   bir anahtar kare üretir, bedeli yereldir. T105 bu yüzden F2 (β=2) seçti.
   Aynı ölçütü kullan; başka bir ölçüt seçeceksen **neden** olduğunu yaz.

5. **Türeyen eşiğin sınırları yazılır.** Kural bir aralığa hapsedilmeli —
   üst ve alt sınır ölçüyle konur, keyfi değil. Sınırın dışına çıkan bir
   içerik gelirse ne oluyor? Bu davranışın ölçüsü olsun.

6. **Müşteriye ne değiştiği bildirilir.** Kaynağın tamamı için sahne sayısı,
   ortalama ve **medyan** sahne uzunluğu; sabit 0,105 ile yan yana. T105
   ölçtü ki dağılım sağa çarpık ve **aralık seçen taraf medyanı kullanmalı** —
   bu cümle belgede kalsın. T98 aralığını, T104 penceresini bu sayılardan
   türetiyor.
   CHECK: eski/yeni iki sütun, medyan dahil.

7. **`DefaultMinSceneSeconds` yeniden yargılanır.** T105 ölçtü: 1,0'ın payı
   sıfır değil — P2'de 334,000 her eşikte kaçıyor çünkü 333,300'de kesim var;
   0,5'e çekince P2 6/7 → 7/7, F2 0,899 → 0,922. T105 sabiti değiştirmedi
   çünkü bedeli T98'in anahtar kare aralığında ve T104'ün penceresinde ve
   ikisi de ölçülmedi. **O bedeli bu turda ölç** ya da "hâlâ ölçülmedi" diye
   taşı; sessizce bırakma.

8. **Kod ile onu anlatan cümle aynı şeyi söyler.** Eşik artık sabit değilse
   `SceneMap.cs`'te ve belgede sabit eşiğe atıfta bulunan her cümle aynı
   commit'te değişir. Değişmeyen sabit için "ölçüldü, değişmedi" yazılır.

9. **Mutasyon kanıtı.** Türetme kuralının her parçası — girdi, katsayı,
   kıskaç uçları — iki yönde bozulunca ölçü kırılmalı. İki sabiti
   karşılaştıran ölçü yazma, `Skip` yazma, ffmpeg yokluğunda sessiz erken
   dönüş yazma. ffmpeg gerektiren ölçü `[FfmpegFact]`.
   T105 `BaseThreshold` pimini tek yönlüden `(0,036 – 0,057]` kıskacına
   geçirdi; türeyen eşiğin pimi de çift yönlü olsun.

## Sınırlar

- `owns` dışına yazma. `PlanCalculator.cs` T107'nin, `FfmpegArguments.cs`
  T108'in, `ComplexityProbe.cs` T103'ün — hepsini **okuyabilirsin**.
- **Düzeneği `tools/sahne-yer-gercegi/` altına koyamazsın** — `owns` dışı ve
  kapı engelliyor. T105 bunu yaşadı. Alternatif aynı: yer gerçeği listeleri ve
  tam ffmpeg komutları `docs/olcumler/dinamik-esik.md` içine gömülür ki
  denetçi doğrulayabilsin. `.calisma/` gitignore'lu ve worktree-yerel,
  oraya koyduğunu denetçi göremez.
- **T98'in tel tuzağına dikkat.** `FfmpegArguments.cs`'te
  `SceneMapThresholdOfRecord` sabiti var ve `SceneMap.DefaultThreshold`'dan
  ayrışırsa T98'in ölçüsü kırmızıya döner. Bu **kasıtlı**. Eşiği türetilir
  yaparsan o sabitin dayandığı düzeltme yeniden ölçülmeli — dosya senin
  değil, **rapora yaz**, ben T108/T98 tarafında kapatırım.
- `.calisma/kaynak/` ortak, dokunma.
- Makine paylaşımlı. Kodlama komutlarında iş parçacığını **sabitle**
  (`-threads N`, x265'te `pools`). Süre sayısına "makine paylaşımlıydı"
  damgası bas; kalite ve boyut sayısına basma.
- Tam süiti koşturma; yalnız `SceneMapTests`.
- Kod yorumu yazma.

## Kayıt noktası

Her kabul kriteri bittiğinde commit. Dal: `T109-dinamik-esik`.

## Çıktı

- `docs/olcumler/dinamik-esik.md`: aday × pencere ızgarası, iki yeni pencerenin
  yer gerçeği, yanlış pozitif sayımı, türeyen eşiğin kıskacı, eski/yeni sahne
  dağılımı.
- Ölçüye dayanan türetme kuralı — ya da "ölçüldü, sabit eşik daha iyi".
- `DefaultMinSceneSeconds` hakkında ölçü ya da açıkça "hâlâ ölçülmedi".
- Kendi dalın, itilmiş. `main`e sen birleştirme.

## Kayıt noktası

Bitti. Dal `T109-dinamik-esik` itildi (`origin/T109-dinamik-esik`), worktree
`.claude/worktrees/T109`. Üç commit: `e0595c8` (kod + pimler), `bc67a38`
(ölçüm sayfası), `1b952af` (ızgarada eşit aday sayısı düzeltmesi).
Son hash `1b952af`. `dotnet test -c Release --filter "SceneMapTests"` 26/26
yeşil, atlanan 0; `tools/ci-gibi-kos.sh` 1026 geçti / 0 kaldı / 101 atlandı /
1127 toplam.

## Rapor (T109, tur 1)

- **Karar: türetme kondu.** Kural `θ = clamp(0,08 + 2,09·p90(±40 sn), 0,05, 0,15)`.
  Altı pencerede birleşik F2 0,972; sabit 0,105 ile 0,895. Kalibrasyon P1–P3
  (0,977), sınama elle işaretlenen P4–P6 (0,962). Seçim yalnız kalibrasyonda
  yapıldı, önceden ilan edilmiş eşitlik bozmayla.
- **`SceneDetector.BaseThreshold` 0,05 → 0,012.** p90 girdisi 0,05 tabanında altı
  pencerenin beşinde sıfıra çöküyor. Ölçülen çalışma aralığı 0,010–0,015,
  0,018'de kırılıyor. Süre farkı ölçülemedi (makine paylaşımlıydı), günlük
  83 KB → 1,65 MB.
- **`ScanArgs`'ta biçim kusuru bulundu ve düzeltildi.** `gte(scene,{taban:0.###})`
  0,0005'in altındaki tabanı sessizce `0`a kırpıyordu; `gte(scene,0)` her kareyi
  aday yapıyor. 0,05 tabanıyla görünmezdi. `0.#####` oldu, pime bağlandı.
- **T98 tel tuzağı ayrıştı — beklenen.** `FfmpegArguments.SceneMapThresholdOfRecord`
  artık kararı veren sayı değil: üretim yolu türeyen kuralı kullanıyor ve türeyen
  haritada tek eşik yok (`SceneMap.Threshold` = `NaN`, kural `SceneMap.Rule`
  alanında). Dosya bende değil, T98/T108 tarafında kapatılmalı.
- **`SceneMap.DefaultThreshold` adı yanıltıcı kaldı.** Artık varsayılan değil;
  `FixedThreshold = 0,105` eklendi ve `DefaultThreshold` ona takma ad oldu.
  Silinemedi çünkü `tests/VidShrink.Tests/QualityMeterTests.cs:338` kullanıyor ve
  o dosya `owns` dışında. Adın tek kalması için o çağrı yeri
  `SceneMap.FixedThreshold`e çevrilmeli — T0 kararı.
- **`tools/sahne-yer-gercegi/sahneler.csv` bayat** (24 satır, 0,2 ile üretilmiş).
  Türeyen haritanın 66 kesimi ölçüm düzeneğinde üretildi; `tools/` `owns` dışı
  olduğu için dosya değiştirilmedi. Kaynağı ve komutu belgede.
- **`DefaultMinSceneSeconds` değişmedi.** 0,5 harita tarafında bedelsiz görünüyor
  (F2 0,972 → 0,988, P2 6/7 → 7/7, yanlış pozitif artmıyor) ama tüketici
  tarafındaki bedel (T98 anahtar kare aralığı, T104 penceresi) **hâlâ ölçülmedi**.
  Sayı hazır, karar ölçen tarafın.
- **Tüketiciyi ilgilendiren asıl değişiklik en uzun sahne**: 122,37 sn → 271,15 sn.
  Sahne 77 → 67, ortalama 13,46 → 15,47 sn, medyan 5,62 → 5,33 sn. Dağılım daha
  da sağa çarpık; aralık seçen taraf medyanı kullanmalı.
- **`docs/olcumler/sahne-haritasi.md` artık aşılmış yolu anlatıyor.** T105'in
  sayfası 0,105'i "karar eleği" diye tarif ediyor; o cümleler bugün sabit
  karşılaştırma yolunu anlatıyor. Dosya `owns` dışında, değiştirilmedi. Yeni
  sayfa `docs/olcumler/dinamik-esik.md` ona atıf yapıyor; iki sayfanın
  bağlanması T0 kararı.
- **`.claude/relay/YOL-HARITASI-2.md` ve `T98.md` de 0,105'e atıf yapıyor**;
  ikisi de `owns` dışında.
- **Teslim.** Dal `T109-dinamik-esik` itildi, son commit `1b952af`. `main`e
  birleştirilmedi. Dar filtre `SceneMapTests` 26/26 yeşil, atlanan 0;
  `tools/ci-gibi-kos.sh` (ffmpeg PATH'ten çıkarılmış) **1026 geçti / 0 kaldı /
  101 atlandı / 1127 toplam**, 13 dk 54 sn — gerileme yok.
- **Ölçülmeyenler** ayrı başlık halinde `docs/olcumler/dinamik-esik.md` içinde:
  taban 0,010'un altı, tavanı yukarıdan bağlayan bir ölçü, taban değişiminin
  süre bedeli (makine paylaşımlıydı, ayrılamadı), `DefaultMinSceneSeconds`
  0,5'in tüketici tarafindaki bedeli, kuralın kaynak dışı videolarda davranışı.
- **`.calisma/T109/` worktree-yerel ve gitignore'lu**; denetçi oradaki ham
  çıktıyı göremez, bu yüzden yer gerçeği listeleri ve tam ffmpeg komutları
  belgenin içine gömüldü.
