---
id: T118
title: ci-gibi-kos.sh artik CI'i temsil etmiyor ve ajanlar ona guveniyor
role: builder
model: sonnet
depends: [T115]
status: submitted
round: 2
verify: [bash tools/ci-gibi-kos.sh --self-test]
owns: [tools/ci-gibi-kos.sh, docs/olcumler/ci-benzetimi.md, docs/inceleme/handbrake-motoru.md]
side_effects: []
---

## Baglam

`tools/ci-gibi-kos.sh` adiyla bir soz veriyor: **CI'da olacak seyi yerelde goster.**
T115'ten sonra o soz yalan. Betik ffmpeg'i PATH'ten **siliyor**; CI ise artik
ffmpeg'i kuruyor ve goruyor (`33589639249`: `Total 1180, Skipped 17`).
Betigin urettigi tablo CI'in urettiginin **ayni sey degil**.

Bu teorik bir uyumsuzluk degil. T95 tur 3'te bu betikle kostu (`--no-build`),
cikis kodu 0 aldi ve o sonucu "tam suit" diye rapora yazacakti. Yani yanlis
temsil **zaten bir olcume karisti.**

Betigin sahibi yok. T66'dan miras, kimse guncellemedi, herkes kullaniyor.

## Kabul kriteri

1. **Bugunku fark olculur, tahmin edilmez.** Ayni commit'te betik ile CI ne
   veriyor? En az bir gercek CI kosumunun `Failed/Passed/Skipped/Total`
   dortlusu ile betigin ayni commit'teki dortlusu yan yana konur.
   CHECK: iki dortlu, kosum id, headSha.

2. **Betik ya CI'i temsil eder ya adini birakir.** Iki secenekten biri
   secilir ve **nedeni yazilir**:
   (a) ffmpeg silme kaldirilir, kapi parametreleri CI ile ayni olur
       (`-MinimumTotal` / `-MaximumSkipped`), betik adini hak eder;
   (b) betik "ffmpeg'siz ortami taklit eder" diye **yeniden adlandirilir**
       ve ciktisinin basina CI olmadigi yazilir.
   Ortada birakma. Bugunku hali ucuncu bir sey: CI diyor, CI degil.
   CHECK: secim, gerekce, uygulanmis hali.

3. **Yanlis kullanimi betik kendisi durdurur.** Kullanan bir ajan ciktinin
   CI'i temsil edip etmedigini **okumadan** anlayabilmeli: ilk satirda ne
   oldugu yazili olsun. Sessiz yanlis temsil kabul degil.
   CHECK: ciktinin ilk satiri, iki senaryoda da.

4. **`--self-test` eklenir.** Betik kendi sozunu sinayabilsin: beklenen
   ortami kurdu mu, ffmpeg gorunurlugu istenen halde mi, kapi parametreleri
   CI'inkiyle ayni mi. `verify` bunu kosturuyor.
   CHECK: `bash tools/ci-gibi-kos.sh --self-test` cikis kodu.

5. **T95'in yanlis okumasi kayda gecer.** `docs/olcumler/ci-benzetimi.md`
   icinde tek paragraf: betik hangi tarihten sonra yaniltmaya basladi, hangi
   olcume karisti, nasil fark edildi. Bu proje hatayi belgeden ogreniyor.

6. **Olcmedigin sey icin "olculmedi" yaz.**

## Sinirlar

- `owns` disina yazma. `.github/workflows/ci.yml` T115'in, `tools/kosum-kapisi/**`
  T115'in — **okuyabilirsin**, yazamazsin. Kapi parametrelerini oradan **okuyup**
  hizalarsin; oraya yazmazsin.
- Kod yorumu yazma.
- Tam suiti kosturma; betigin kendisi zaten kosturuyor, bir kez yeter.
- Makine paylasimli, dokuz ajan kosuyor. Sure sayisina damga bas.

## Ikinci is: kanit commit'leri hicbir ref'te degil

T115 tur 1'in kanit commit'leri `f2f05f5f` ve `0a56868f` **hicbir dalda ve
etikette degil** — `git gc` onlari silebilir ve 83 sayisinin birincil kaniti
yalniz GitHub kosum gunlugunde kalir. Ikisine birer etiket at
(`kanit/T115-f2f05f5f` gibi) ve **uzaga it**. Etiket adini ve neyi kanitladigini
`docs/olcumler/ci-benzetimi.md` sonuna yaz. Commit'lerin icerigini degistirme.

## Kayit noktasi

Dal: `T118-ci-benzetimi`. Kendi worktree'n: `.claude/worktrees/T118`.

## Cikti

Dal `T118-ci-benzetimi` (taban `origin/T115-ci-ffmpeg`), commit `ceec52c` + `045648e`,
ikisi de `origin`'e itildi. Olcum agaci: `origin/T115-ci-ffmpeg`, worktree HEAD `361b96e`
(headSha `0e122f2`'nin bir yorum-only commit ilerisi).

**K1 — iki dortlu (docs/olcumler/ci-benzetimi.md § "Iki dortlu"):**
CI (kosum 33589639249, headSha 0e122f2): Failed 1, Passed 1162, Skipped 17, Total 1180, 18m59s.
`ci-gibi-kos.sh` (ayni taban): Failed 0, Passed 1163, Skipped 17, Total 1180, 22m19s.
Skipped/Total birebir eslesiyor — betik CI ile ayni test yuzeyini kosturuyor.
Sure karsilastirilamaz (makine 9 ajanla paylasimli).
Failed farki **tur 2'de kimlikle kapatildi** (asagiya bakin, "Tur 2" basligi).
**Ayrica onemli bulgu:** bu makinede kapi (`kosum-kapisi.ps1`) `kod=66 "ozet yok"`
ile dustu — CI'daki gibi gercek bir basarisizliktan degil, dotnet'in turkce ozetini
canli yakalarken olasi bir kod sayfasi sorunundan (hipotez, dogrulanmadi, dosya
T115 owns'unda, duzeltilmedi). Yani betik CI'i dogru cagirsa da bu makinede kapi
guvenilir PASS/FAIL vermiyor.

**K2 — secim (a):** ffmpeg silme kaldirildi, `-MinimumTotal`/`-MaximumSkipped`
`.github/workflows/ci.yml`'den okunuyor, `kosum-kapisi.ps1` CI'daki gibi cagriliyor.
Gerekce ve uygulama detaylari `ci-benzetimi.md` § "Secim" ve § "Uygulama"da.

**K3:** ilk satir `CI TEMSILI: ...` yaziyor, iki senaryo da (yok, tek secenek
uygulandigi icin tek senaryo) kapsandi.

**K4:** `--self-test` eklendi, `verify` bu makinede gecti.

**K5/K6:** T95 vakasi ve "ölçülmedi" listesi `ci-benzetimi.md` icinde.

**Ikinci is:** `kanit/T115-f2f05f5f` ve `kanit/T115-0a56868f` etiketlendi, `origin`'e
itildi, ne kanitladiklari `ci-benzetimi.md` § "Kanit commit'leri"nde.

**Ucuncu is:** `docs/inceleme/handbrake-motoru.md` icindeki dokuz `auto-mod.md`
atfi satir numarasindan **capaya** cevrildi (baslik + cumle basi, satir numarasi
ikincil not). Sebep: ayni gun uc farkli agacta uc farkli satir numarasi olculdu
(`:223`/`:225` eski not, bu daldaki `:208`/`:210`, `origin/T111-auto-mod` ucundaki
`:229`/`:231`) ve T111'e tur 2 acildigi icin `auto-mod.md` birlesmeye kadar
kararli degil — bu bir borc degil, birlesme aninda bilinmesi gereken bir gercek.
Detay ve bayatlama mekanizmasi + yakalama onerisi `ci-benzetimi.md` § "Ek is"te.

**Acik soru T0'a:** `kosum-kapisi.ps1`'in bu makinede kod=66 ile dusmesi (K1'deki
bulgu) T115 owns'unda oldugu icin burada duzeltilmedi — ayri bir sozlesme gerekebilir.

### Tur 2 (T0'in isteğiyle, commit `9610815`)

1. **Failed farki kimlikle kapatildi, tahmin edilmedi.** CI logu (`gh run view
   33589639249 --log`) tek dusen testi adiyla verdi:
   `PerformanceCheckTests.IslemciZamaniSayaciDogruOkuyorMu`, sebep
   `Cannot load nvcuda.dll`. Ayni test bu makinede tek basina filtreyle
   kosturuldu (tam suit degil): **gecti**, 2dk3sn. Kok sebep de olculdu:
   `HasEncoder("h264_nvenc")` derlenmis ffmpeg kapasitesine bakiyor, gercek
   GPU'ya degil — CI'da derleme nvenc'i listeliyor ama surucu yok, bu makinede
   gercek GPU var ve calisiyor. Fark ffmpeg'ten degil donanimdan. Test dosyasi
   `tests/` owns disi, degistirilmedi.
2. **Donanim farki artik betige ogretildi, sessizce gecilmiyor.** `ci-gibi-kos.sh`
   gate'ten sonra kucuk bir gercek nvenc denemesi (320x240, 0,1sn) yapiyor;
   basariliysa "bu makinede GPU calisiyor, CI'da yok, GPU'ya bagli testler
   farkli sonuclanabilir" uyarisi basiyor. Ilk yazilan prob 64x64 kullanmisti
   ve nvenc'in kendi cozunurluk sinirina takilip hep yanlis "calismiyor"
   raporluyordu — standalone test edilip 320x240'a duzeltildi, sonra teslim
   edildi.
3. **Kapi parametreleri gercekten okunuyor, kopyalanmadi** — `ci-gibi-kos.sh`
   her kosumda `.github/workflows/ci.yml`'i `grep`ler, sabit deger yok; kanit
   `ci-benzetimi.md` § "Uygulama"da.
4. `kosum-kapisi.ps1` kod=66 bulgusu icin T115'in ajanina komut+cikti+encoding
   dogrulama verisi `ci-benzetimi.md`'ye eklendi (tur 2'de gate tekrar
   kosulmadi, tur 1'in verisi kullanildi).

Detay: `docs/olcumler/ci-benzetimi.md` § "Iki dortlu" (genisletildi) ve
§ "Uygulama" (iki yeni madde).

## Ucuncu is: handbrake-motoru.md atiflari kaydi (T0, 2026-09-02)

T111 `docs/olcumler/auto-mod.md`'ye ~400 satir ekledi ve
`docs/inceleme/handbrake-motoru.md` icindeki satir atiflari kaydi. Bugun yanlis
satiri gosterenler: `auto-mod.md:202-204`, `:209` (iki kez), `:214,216`, `:250`,
`:283-287`, `:289`.

T111'in verdigi iki dogru: preset 6 satiri **`:223`**, HandBrakeCLI komutu **`:225`**.
Kalanlari **kendin bul**, T111'in listesini oldugu gibi kopyalama — o da atifsiz
verdi. Her atifta hedef satiri acip **ne anlattigini** dogrula; sayiyi kaydirmak
yetmez, atfin isaret ettigi cumle hala orada mi ona bak.

Bu kusur bu projede kayitli ve tekrar ediyor: **yerlesim yeniden temellendirilirken
ustundeki cumle eskiyor.** Bu yuzden ikinci bir sey isteniyor: atiflarin **nasil
bayatladigi** `docs/olcumler/ci-benzetimi.md` sonuna iki cumleyle yazilsin, ve
bir daha bayatladiginda yakalanmasinin yolu varsa **onerilsin** (uygulama isteme,
oneri yeter).

`docs/olcumler/auto-mod.md` **T111'in**, dokunma — yalniz okursun.


## Denetim GECTI (a6099eeaaab764f70, 2026-09-02)

Denetlenen: `origin/T118-ci-benzetimi` `045648e` (tur 1). Yazma yok, `git archive`
kopyasinda calisildi. **`dotnet test` sayisi: 0.** Alti kriterin altisi da
kanitlandi; `? kanitsiz` yok.

Kanit ozeti: CI dortlusu `gh run view 33589639249 --log:265`, betik dortlusu
ajanin gunlugunden **bagimsiz** okundu; ikisi de belgedekiyle birebir. Kapi
parametrelerinin kopyalanmayip **okundugu** mutasyonla gosterildi (`ci.yml`'de
`9999/7` -> self-test ayni sayiyi yazdi; `-MinimumTotal` silinince
`KENDI-SINAMA DUSTU`, exit 1). Iki kanit etiketi `origin`'de ve annotated.
Dokuz atfin dokuzu da capa bicimine gecmis ve hedef cumleleri **iki ayri
agacta** bulunuyor — capa yaklasimi isini yapiyor.

### Muhur notuna girecek borclar

1. `Failed 1 <-> 0` farki icin belgedeki "kararsiz test" hipotezi **yanlis
   tarafa** isaret ediyordu; ikinci kosum (`33593652976`) ayni testin ayni
   sekilde dustugunu gosterdi. Tur 2 bunu kimlikle kapatti.
2. **Kalan temsil acigi: GPU.** `ci-gibi-kos.sh` bu makinede GPU oldugu icin
   o olcuyu hicbir zaman CI gibi kirmiziya cekemez. Betigin ilk satiri bunu
   soylemiyor.
3. `pwsh` yoklugunda betik **sessizce** PS 5.1'e geri dusuyor
   (`tools/ci-gibi-kos.sh:83-87`); CI `pwsh` 7 kullaniyor. Parametrelerde
   "CI gibi", konak kabukta degil. kod=66'nin kuvvetli adayi bu.
4. `--self-test` iki farki gormuyor: `pwsh` yoklugu, ve `-MaximumSkipped`
   bicimi degisirse yalniz UYARI yazip **exit 0** veriyor — CI'dan gevsek
   kapiyla "hizaliyim" diyor.
5. `GATE_LINE` secimi (`grep ... | tail -1`) kirilgan; kapinin altina aciklama
   satiri eklenirse parametreler bosalir. Guvenli yonde bozuluyor.
6. Betik dortlusunun birincil kaniti git'te degil, `.calisma/` icinde.
   Sozlesmenin "ikinci is"i bu sorunu T115 icin cozdu, kendi olcumu icin
   cozmedi.
7. `ci-benzetimi.md:85-87` birebir alinti diye verilen dizge kaynagindan kaymis
   (`ci.yml'den` <-> `ci.yml'dan`). Kucuk, ama bu deponun kronik sinifi.
8. **Bilgi:** `origin/main` ci.yml'inde ffmpeg kurulumu yok, main'in yesili bu
   olcuye hic ulasmiyor. T115 main'e girdigi anda main de kirmiziya doner.
   T123 ve T117 bu yuzden acildi.


## Denetim GECTI — tur 2 (a4a3999b9659811ed, 2026-09-02)

Denetlenen: `045648e -> 9610815`. Yazma yok. **`dotnet test` sayisi: 0.**
`git diff --name-only` = `ci-benzetimi.md` + `ci-gibi-kos.sh`, ikisi de `owns`
icinde. Tur 2'nin kapattigini **iddia ettigi** dort maddenin dordu de kanitlandi;
kapandi diye yanlis bildirilen borc yok.

Kimlik bagimsiz dogrulandi: `gh run view 33589639249 --log:217,219,230,265` ->
`PerformanceCheckTests.IslemciZamaniSayaciDogruOkuyorMu`, sebep
`Cannot load nvcuda.dll`, dusen satir `PerformanceCheckTests.cs:713`. Kok sebep
iddiasi da dogrulandi (`EncoderCapabilities.cs:27`, `PerformanceProbe.cs:81`).
nvenc probu denetcinin kendi kosumunda gercekten bir kare kodladi (`exit=0`,
`video:13KiB`); 64x64 itirafi da olculebilir cikti (`code -22`).

Tur 1'in sekiz borcundan **1 kapandi, 1 yari kapandi, 6 acik.**

### Muhur notuna girecek borclar (tur 2)

1. Belge betige yapmadigi bir isi atfediyor: `ci-benzetimi.md:140-141` "bu
   betigin taradigi yalniz bu ikisi" diyor, `ci-gibi-kos.sh:92-103` `tests/`
   altinda **hicbir tarama yapmiyor**. Sayi ajanin kendi grep'inden geliyor.
2. **Sayi dogru, etiketi yanlis** — bu deponun kayitli kusuru aynen tekrarladi.
   Ham grep 2 yer buluyor ama "`HasEncoder` ile korunan cagri yeri" predikati
   icin dogru sayi **1**: `EncoderCapabilitiesTests.cs:52` sabit metin
   fixture'i, gercek GPU'ya dokunmuyor, CI'da farkli sonuclanamaz.
3. Probun sessiz yanlis negatifi: yalniz basariliysa yaziyor. Bu makinede on
   dort ajan kosuyor ve NVENC esZamanli oturum sayisi sinirli — GPU var ama
   oturum doluyken prob duser ve betik susar.
4. Uyarinin yeri: tur 1 borc 2 **ilk satiri** istiyordu, uyari en sona kondu.
   Kirpilan bir ajan gunlugunde ilk satir kalir, uyari duser.
5. Kriter numaralari takas olmus (`:127` K3 diyor, sozlesmede K2).
6. Atif bir satir kaymis (`ilk 6 satir` -> gercekte 7) ve alinti `2>/dev/null`i
   dusurmus.
7. Govde cumlesi kendi "Olculmedi" bolumunden fazlasini iddia ediyor: esit
   sayilardan kume esitligi cikariyor, `:151-153` bunu geri aliyor. **Iki cumle
   ayni belgede ters yonde.**
8. "Tek test burada gecti, 2 dk 3 sn" olcumunun ciktisi git'te yok.
9. Kopyalanmis yazim hatasi iki yerde: "kimlik kimlik dogrulandi".

### Muhur sirasi

T118 `origin/T115-ci-ffmpeg` tabanli ve T115'in commit'lerini tasiyor. Tek
basina `main`e giremez: girerse ffmpeg CI'ya kurulur ve main
`IslemciZamaniSayaciDogruOkuyorMu`'dan kirmiziya doner. **T115 + T117 + T118
birlikte birlesir**, T123 ve T117'nin GPU gecidi kapandiktan sonra.
