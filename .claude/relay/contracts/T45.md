---
id: T45
title: Kurulum betiği kaynaktan derlemeyi bıraksın, yayınlanmış sürümü indirsin
role: builder
model: opus
depends: []
status: submitted
round: 1
audit: —
auditor_id: —
diff: —
verification: —
owns: [Install-VidShrink.ps1, install-vidshrink.sh, .github/workflows/release.yml, README.md, CHANGELOG.md, tests/VidShrink.Tests/InstallerTests.cs]
side_effects: []
---

## Amaç

Kullanıcının cümlesi: "kurulum betiğini baştan yaz temiz iş yapalım."

Bugün `Install-VidShrink.ps1` **kaynaktan derliyor**: `main.zip`'i indiriyor, .NET 8
SDK'sını `%LOCALAPPDATA%\Microsoft\dotnet` altına kuruyor, `dotnet publish` ile hem
uygulamayı hem başlatıcıyı yayımlıyor. Bu yüzden kurulum dakikalar sürüyor, makinede
bir SDK bırakıyor ve kullanıcının aldığı ikili dosya hiç kimsenin sınamadığı bir
derleme oluyor.

`v0.1.0` yayında ve dört hedef için arşiv, manifest ve sağlama dosyası taşıyor.
Kurulum artık onu indirmeli.

## Ölçülmüş iki kusur — bu sözleşme ikisini de kapatıyor

**1. Başlatıcı yayında yok.** Yayın akışı yalnız `src/VidShrink.App` projesini
yayımlıyor. `manifest-win-x64.json` içindeki 220 dosyanın arasında `VidShrink.App.exe`
var ama `VidShrink.exe` (başlatıcı) **yok**. Kurulum kaynaktan derlemeyi bırakırsa
başlatıcıyı alacak yeri kalmıyor — bu yüzden akış da değişmeli.

Başlatıcının rolü: kısayolun gösterdiği program o. Windows'ta çalışan bir exe üzerine
yazılamadığı için güncelleme, uygulama yüklenmeden **önce** başlatıcı tarafından
uygulanıyor (`src/VidShrink.Launcher/Program.cs`). Kısayol doğrudan uygulamayı
gösterirse otogüncelleme hiç çalışmaz — bugünkü betikte bunun uyarısı yorum olarak
duruyor, korunacak.

**2. `.update-version` işareti hiç yazılmıyor.** Ölçüldü: yerelde derlenmiş `app/` ile
CI'ın ürettiği arşiv arasında **220 dosyanın 191'i** farklı (derleme belirleyici değil).
İşaret olmadığı için taze kurulumdan sonraki ilk açılış `Diff` yapıyor ve 44 MB'lık
arşivin neredeyse tamamını yeniden indiriyor. Kurulum arşivi indirdiğine göre hangi
sürümü kurduğunu biliyor; işareti yazacak.

## Kurulum düzeni — değişmiyor

    <kök>/
      VidShrink.exe          <- başlatıcı; kısayollar bunu gösterir
      app/                   <- manifestin anlattığı klasör, güncellenen taraf
        VidShrink.App.exe
        .update-version      <- yeni: kurulan sürüm
      tools/ffmpeg/
        ffmpeg.exe
        ffprobe.exe

`src/VidShrink.Launcher/Program.cs` bu düzeni bekliyor; `Updater` `app/` klasörünü
tazeliyor, ffmpeg kurulum kökündeki `tools/ffmpeg` üzerinden PATH'e ekleniyor.

## Kabul kriterleri

### K1 — Yayın başlatıcıyı da taşısın

`release.yml`, `win-x64` için `src/VidShrink.Launcher` projesini de yayımlayacak ve
sonucu ayrı bir varlık olarak yayına koyacak. Ad, var olan adlandırma düzenine uysun —
bugünkü desen `vidshrink-<rid>.zip`, `manifest-<rid>.json`, `checksums-<rid>.txt`.

Başlatıcı `PublishSingleFile` ve `SelfContained`, hedefi csproj içinde `win-x64`'e
sabit. Diğer üç hedefte başlatıcı yok — orada kendini güncelleme zaten kapalı
(`UpdateCheck.CanSelfUpdate` yalnız Windows).

Varlığın sağlaması da yazılacak; kurulum indirdiğini doğrulayabilsin.

### K2 — Windows kurulumu indirsin, derlemesin

`Install-VidShrink.ps1` baştan yazılacak. Yeni akış:

1. Yayından `vidshrink-win-x64.zip`, başlatıcı varlığı ve `checksums-win-x64.txt` indir.
2. **SHA-256'ları doğrula.** Tutmayan dosyayla kuruluma devam edilmeyecek.
3. `app/` altına arşivi aç, başlatıcıyı köke koy.
4. `.update-version` dosyasına kurulan sürümü yaz.
5. ffmpeg'i bugünkü gibi WinGet'ten al ve `tools/ffmpeg` altına kopyala.
6. Kısayolları bugünkü gibi kur — hedef **başlatıcı**, uygulama değil.

**.NET SDK bootstrap'ı tamamen kalkacak.** `Find-DotNetSdk8`, `Install-DotNetSdk8`,
`dotnet publish`, `main.zip` indirme — hepsi gidiyor. Kurulan şey CI'ın sınadığı ikili
olacak.

Bugünkü parametre arayüzü korunacak: `-InstallRoot`, `-NoLaunch`, `-SkipShortcuts`.
Bugünkü koruyucu davranışlar da korunacak: kurulum kökünden çalışan `VidShrink` ve
`VidShrink.App` süreçlerini durdurmak, kökü temizleyip yeniden kurmak, kurulum sonunda
`VidShrink.exe` ile `app\VidShrink.App.exe` varlığını doğrulamak.

**Mimari dürüstlüğü:** `Get-RuntimeIdentifier` bugün `win-arm64` ve `win-x86`
döndürebiliyor ama yayında yalnız `win-x64` var. Sessizce yanlış dosya indirme — ya
arm64'te x64 arşivinin kurulacağını **açıkça yaz**, ya da desteklenmeyen mimaride
anlaşılır bir hatayla dur. Hangisini seçtiğini gerekçesiyle rapora yaz.

### K3 — macOS ve Linux kurulumu da indirsin

`install-vidshrink.sh` aynı akışa geçecek: `uname`'den rid, yayından arşiv, sağlama
doğrulaması, `~/.local/share/vidshrink` altına açma, `~/.local/bin/vidshrink` bağlantısı.

Orada başlatıcı yok; kısayol doğrudan uygulamayı gösterir ve bu doğru — kendini
güncelleme o platformlarda zaten kapalı, betik bugün de öyle davranıyor.

FFmpeg'i **kurmamaya** devam edecek: eksikse paket yöneticisi komutunu yazıp duracak.
Sebebi lisans — ffmpeg GPLv3, VidShrink MIT; ikili dağıtılmıyor, kullanıcının makinesine
kendi lisansıyla iniyor.

### K4 — Betikler sınanacak

Bugün ikisinin de hiçbir testi yok. En az şunlar penceresiz sınanacak:

- Betiklerin indirdiği varlık adlarının `UpdateCheck.ManifestAssetName` ve
  `ArchiveAssetName` ile **aynı** olduğu. Ad ayrışırsa kurulum çalışır ama güncelleme
  sessizce ölür; bu sözleşmenin kapattığı kusurun tekrarı olur.
- Kurulum düzeninin `Launcher/Program.cs`'in beklediği düzen olduğu: `app/` alt klasörü,
  kökte başlatıcı, `tools/ffmpeg`.
- Betiklerde `dotnet publish` ya da SDK indirme izinin kalmadığı.
- `.update-version`'ın yazıldığı ve `UpdateCheck.ReadVersionMarker` ile okunabildiği.

Betikleri gerçekten koşturmak bu makinedeki kurulumu değiştirir; **koşturma**. Metinden
ve `UpdateCheck` sabitlerinden okuyarak sına, koşturmadığını açıkça yaz.

## Doğrulama

- `dotnet build -c Release` 0 uyarı · `dotnet test` tamamı yeşil, **518'in altına düşmeden**
- `release.yml`'ın ürettiği varlık listesi — hangi ad, hangi hedef
- Betiklerin indirdiği adların güncelleyicinin beklediği adlarla eşleştiğinin kanıtı
- `README.md` ve `CHANGELOG.md` yeni akışı anlatacak: kurulum artık SDK istemiyor
- Koşturulamayan her adımın açık listesi

## Uyarılar

**Etiket itme, yayın oluşturma.** `release.yml`'ı değiştir ama yeni sürüm çıkarma —
mühür ve yayın kararı T0'da. Akışın doğruluğunu metinden ve var olan `v0.1.0` koşusunun
günlüğünden göster.

**`v0.1.0` yayında ve başlatıcı içermiyor.** K1'in ürettiği varlık ancak bir sonraki
sürümde görünecek. Betik, varlığı bulamazsa ne yapacağını **açıkça** karara bağlasın:
anlaşılır bir hatayla mı dursun, yoksa eski yola mı düşsün. Sessizce yarım kurulum
bırakmasın.

**Kod yorumu:** bu projede açıklayıcı özet yorumları var ve o üslup korunur; satır içi
anlatım yorumu yazma. Betiklerdeki Türkçe açıklama satırları bu projenin üslubu, koru.

**README ve depoya giden her belge İngilizce.** Betiklerin kullanıcıya bastığı mesajlar
bugün Türkçe; o kalsın.

**Geçici dosya:** `.calisma/` altına, iş bitince sil.

**Bu projenin tekrar eden kusuru:** kod doğru, onu özetleyen cümle yanlış — altı
sözleşmede tekrarladı. Rapora giren her sayıyı ham çıktıdan kopyala; koşturamadığını
"doğrulandı" diye yazma.

## Kayıt noktası

Her kriter bitince Rapor'a tek satır düş ve commit at.

## Çıktı

Rapor: kriter başına ne yapıldı, ham çıktılar, koşturulamayanlar, sapmalar.

---

## Sürdürme talimatı — 2026-08-26

Kodun diskte ve commit'li; eksik olan **rapor**. Aşağıdakini yap, sonra `status: submitted`
ve `round: 1` işaretle.

1. Rapor yazmadan önce kendi işini yeniden ölç: `dotnet build -c Release` ve `dotnet test`.
   Sayıları **ham haliyle** yaz, hafızadan yazma.
2. Raporu bu dosyanın sonuna yaz. Kriter başına (K1..K4) ne yapıldı, ham çıktı,
   koşturamadıkların, sapmalar.
3. Doğrulama listesindeki her maddeyi tek tek karşıla — özellikle betiklerin indirdiği
   adların `UpdateCheck.ManifestAssetName` / `ArchiveAssetName` ile eşleştiğinin kanıtı,
   ve varlık bulunamazsa ne yapıldığı kararı.

### Bilmen gereken olay

Senin üç dosyan — `Install-VidShrink.ps1`, `.github/workflows/release.yml`,
`install-vidshrink.sh` — paralel koşan T44 ajanının commit'lerine karıştı: `f6f1f62`
ve `cf157cd`. Senin kendi commit'in `29248ba` yalnız üç dosya taşıyor. Kayıp yok, atıf
yanlış. Raporunda tek satır değin; **düzeltmeye kalkma, geçmişi yeniden yazma.**

Kaydı: `~/.claude/teknesyum/openlogs/HATA-paralel-ajanlar-ayni-git-indeksini-paylasti.md`

### Commit kuralı

`git add` her zaman **açık yolla**. Asla `git add -A`, asla `git commit -a`.
Aynı anda salt-okuma bir denetçi ajanı koşuyor.

### Diskte ölçülen durum

| Dosya | Durum |
|---|---|
| `Install-VidShrink.ps1` | yeniden yazılmış, `dotnet publish` izi 0, satır 163 `.update-version` yazıyor |
| `install-vidshrink.sh` | yeniden yazılmış, `dotnet publish` izi 0, satır 157 `.update-version` yazıyor |
| `.github/workflows/release.yml` | satır 106 `src/VidShrink.Launcher` yayımlıyor — K1 |
| `tests/VidShrink.Tests/InstallerTests.cs` | yeni, 167 satır |
| `README.md`, `CHANGELOG.md` | güncellenmiş |

## Rapor — round 1

### Ölçüm, ham çıktı

`dotnet build VidShrink.sln -c Release`

    Oluşturma başarılı oldu.
        0 Uyarı
        0 Hata

`dotnet test VidShrink.sln -c Release`

    Başarılı!  - Başarısız:     0, Başarılı:   550, Atlanan:    16, Toplam:   566, Süre: 10 s

550 ≥ 518. Yalnız `InstallerTests` süzgeciyle: `Başarılı: 15, Başarısız: 0`.

İlk tam koşuda `QualityMeterTests.SameClipComparedToItselfScoresHighVmaf` bir kez düştü
(`VmafNegMean` null). Tek başına koşturuldu: `Başarılı: 2, Başarısız: 0`. İkinci ve
üçüncü tam koşuda da yeşil. Paralel yük altında oynak; T45'in dokunduğu hiçbir dosya
ffmpeg yolunda değil. Kapsam dışı, tek satır not.

### K1 — Yayın başlatıcıyı da taşıyor

`.github/workflows/release.yml`:

- satır 106: `dotnet publish src/VidShrink.Launcher/VidShrink.Launcher.csproj`,
  `if: matrix.rid == 'win-x64'`. Hedef csproj'de `win-x64`'e sabit, `SelfContained` ve
  `PublishSingleFile` orada duruyor; iş akışı bunları tekrar vermiyor.
- satır 139: `vidshrink-launcher-${{ matrix.rid }}.zip` yazılıyor.
- satır 133/140: sağlama listesi `checksummed` değişkeni üzerinden kuruluyor; başlatıcı
  arşivi varsa listeye ekleniyor, yani `checksums-win-x64.txt` üç satır oluyor.

Ad `vidshrink-launcher-<rid>.zip` — var olan `vidshrink-<rid>.zip` desenine uyuyor ve
adı çakışmıyor. Başlatıcı manifeste **girmiyor**: manifest `app/` klasörünü anlatıyor,
başlatıcı orada durmuyor ve kendini hiç güncellemiyor.

YAML ayrıştırma doğrulandı: `YAML_OK ['version', 'test', 'publish', 'release']`.

**Koşturulmadı:** iş akışı çalıştırılmadı, etiket itilmedi, yayın oluşturulmadı.
`publish-launcher` klasörünün ubuntu koşucusunda gerçekten oluştuğu ancak bir sonraki
etikette görülecek. Çapraz yayımın önündeki tek risk `net8.0-windows` hedefi ve
`GenerateSplashImage` adımıydı; SplashGen saf yönetimli koddur (`System.Drawing` ya da
`Skia` bağımlılığı yok, yalnız `System.IO.Compression` ile PNG yazıyor), o yüzden
Linux'ta çalışması bekleniyor — **ölçülmedi, beklenti.**

### K2 — Windows kurulumu indiriyor, derlemiyor

`Install-VidShrink.ps1` baştan yazıldı. Akış: son yayını GitHub API'sinden sor →
varlıkların yayında olduğunu doğrula → `checksums`, arşiv ve başlatıcı arşivini indir →
SHA-256 doğrula → `app\` altına aç, başlatıcıyı köke koy → `.update-version` yaz →
ffmpeg'i WinGet'ten al → kısayolları kur.

- satır 115-117: `vidshrink-$runtimeIdentifier.zip`,
  `vidshrink-launcher-$runtimeIdentifier.zip`, `checksums-$runtimeIdentifier.txt`.
- satır 163: `Set-Content ... '.update-version' -Value $version -Encoding UTF8 -NoNewline`.
- `dotnet publish`, `dotnet-install`, `--list-sdks`, `archive/refs/heads/main` izi: **0**.
  `Find-DotNetSdk8` ve `Install-DotNetSdk8` silindi.
- Korunanlar: `-InstallRoot` / `-NoLaunch` / `-SkipShortcuts`, LocalAppData\Programs
  kısıtı, kurulum kökünden çalışan `VidShrink` ve `VidShrink.App` süreçlerini durdurma,
  kökü silip yeniden kurma, sonda `VidShrink.exe` ve `app\VidShrink.App.exe` denetimi,
  kısayolların başlatıcıyı göstermesi ve bunu açıklayan yorum.
- Ayrıştırma doğrulandı (`Parser::ParseFile`, 0 hata). Dosya BOM'suz UTF-8 kaldı
  (`head -c 3` → `70 61 72`), README'nin anlattığı okuma yolu bozulmadı.

**Mimari kararı — durmak, sessizce x64 kurmak değil.** `Get-RuntimeIdentifier` artık
`X64` dışında istisna atıyor: "Bu mimari için yayın yok: <mimari>". Gerekçe: arm64'e x64
arşivi kurulsa uygulama emülasyonla açılırdı ama `UpdateCheck.Rid` `win-arm64` döndürür
ve güncelleyici yayında olmayan `vidshrink-win-arm64.zip` dosyasını arardı — çalışan ama
hiç güncellenmeyen bir kurulum. Bu sözleşmenin kapattığı kusurun aynısı. `win-arm64` ve
`win-x86` dizgeleri betikten tamamen kalktı.

**Varlık bulunamazsa — duruyor.** satır 134: indirmeden önce yayının varlık listesi
denetleniyor; arşiv, başlatıcı arşivi ya da sağlama listesinden biri yoksa
"Yayın <tag> bu varlığı taşımıyor: <ad>. Başlatıcısız kurulum yapılmaz" ile duruyor.
Eski yola düşmek seçilmedi çünkü eski yol SDK bootstrap'ının kendisi ve K2 onu yasaklıyor.
**Sonuç:** `v0.1.0` başlatıcı taşımadığı için betik bugün hiçbir şey kurmaz; K1'in
ürettiği varlığı taşıyan ilk yayınla çalışır hale gelir. Bilerek böyle.

**Koşturulmadı:** betik çalıştırılmadı — koşarsa bu makinedeki kurulumu siler.
İndirme, sağlama doğrulaması, arşiv açma, WinGet çağrısı ve kısayol kurma adımlarının
hiçbiri canlı ölçülmedi.

### K3 — macOS ve Linux kurulumu da indiriyor

`install-vidshrink.sh` aynı akışa geçti. satır 110-111: `vidshrink-$runtime.zip`,
`checksums-$runtime.txt`. satır 157: `printf '%s' "$version" > "$stage_root/.update-version"`.

- Sağlama: `sha256sum` yoksa `shasum -a 256`; beklenen özet `checksums` dosyasından
  `awk` ile alınıyor, tutmazsa duruyor.
- Gereken komutlar değişti: `tar` yerine `unzip` (yayın varlığı zip; GNU tar zip açmaz),
  ayrıca `awk` ve `sed`.
- Başlatıcı yok, `~/.local/bin/vidshrink` doğrudan `VidShrink.App` dosyasını gösteriyor —
  o platformlarda kendini güncelleme zaten kapalı.
- FFmpeg hâlâ kurulmuyor: eksikse paket yöneticisi komutu basılıp çıkılıyor.
- Mimari: `osx-arm64`, `osx-x64`, `linux-x64`. `linux-arm64` ve `linux-arm` kaldırıldı —
  yayında yoklar. Başka mimaride "Bu mimari için yayın yok: <makine>" ile duruyor.
- `sh -n install-vidshrink.sh` → `SH_OK`. Satır sonu LF kaldı (`.gitattributes`).

**Koşturulmadı:** betik bu makinede POSIX kabuk altında çalıştırılmadı; yalnız sözdizimi
denetlendi. `uname` dalları, `unzip`, `awk` süzgeci ve indirme canlı ölçülmedi.

### K4 — Betikler sınandı

`tests/VidShrink.Tests/InstallerTests.cs`, 15 ölçüm, hepsi yeşil. Betikler metin olarak
okunuyor, hiçbiri çalıştırılmıyor.

Ad eşleşmesinin kanıtı: ölçüm adı sabit dizge olarak yazmıyor,
`UpdateCheck.ArchiveAssetName("RID")` ve `UpdateCheck.ManifestAssetName("RID")`
çağırıp dönen dizgeyi `RID` üzerinden ön ek/son eke bölüyor, sonra betikte
`<önek>$runtimeIdentifier<sonek>` ve `<önek>$runtime<sonek>`, iş akışında
`<önek>${{ matrix.rid }}<sonek>` arıyor. `UpdateCheck` tarafındaki bir ad değişikliği
ölçümü düşürür.

    UpdateCheck.cs:57  ManifestAssetName(rid) => $"manifest-{rid}.json"
    UpdateCheck.cs:59  ArchiveAssetName(rid)  => $"vidshrink-{rid}.zip"
    release.yml:128    vidshrink-${{ matrix.rid }}.zip
    release.yml:131    manifest-${{ matrix.rid }}.json
    Install-VidShrink.ps1:115  "vidshrink-$runtimeIdentifier.zip"
    install-vidshrink.sh:110   "vidshrink-$runtime.zip"

Diğer ölçümler: kurulum düzeninin `Launcher/Program.cs` dosyasının beklediği düzen olduğu
(`app`, `tools/ffmpeg`, kökte `VidShrink.exe`); kısayolların `TargetPath` satırlarının
tamamının başlatıcıyı göstermesi; dört ayrı derleme izinin (`dotnet publish`,
`dotnet-install`, `--list-sdks`, `archive/refs/heads/main`) iki betikte de bulunmaması;
`.update-version` adının `UpdateCheck.VersionMarkerName` sabitiyle aranması; işaretin
BOM'lu yazıldığında bile `UpdateCheck.ReadVersionMarker` ile temiz okunması (PowerShell
5.1'in `-Encoding UTF8` anahtarı BOM yazar, `File.ReadAllText` onu soyar); desteklenmeyen
mimarinin durdurması; eksik varlığın durdurması; `-InstallRoot`/`-NoLaunch`/
`-SkipShortcuts` arayüzünün durması.

`.update-version` dosyasının gerçekten yazıldığı **koşturularak ölçülmedi** — betik
çalıştırılmadı. Ölçülen iki şey: betikte doğru adla yazma satırının bulunduğu, ve o adla
yazılmış bir dosyanın `ReadVersionMarker` ile okunduğu.

### Belge

- `README.md`: Windows ve Unix kurulum paragrafları yeniden yazıldı — SDK yok, yayın
  indiriliyor, sağlama doğrulanıyor, desteklenmeyen mimaride duruluyor. "Requirements"
  maddesi "No .NET runtime and no .NET SDK" oldu.
- `CHANGELOG.md`: `[Unreleased]` altına Changed (dört madde) ve Added (başlatıcı varlığı)
  girdileri. Sürüm numarası **değiştirilmedi**, etiket itilmedi, yayın oluşturulmadı.

### Sapmalar ve kapsam dışı notlar

1. **`.github/workflows/manifest.md` güncellenmedi — T0 kararı gerekli.** O dosya yayın
   varlık adlarının tanımı ve `owns` listesinde yok. Bugün üç varlık sayıyor; K1 dördüncüyü
   (`vidshrink-launcher-win-x64.zip`) ekledi ve `checksums-win-x64.txt` artık iki değil üç
   satır. Belge bu haliyle eksik.
2. **Commit atıfı karıştı.** `Install-VidShrink.ps1`, `install-vidshrink.sh` ve
   `.github/workflows/release.yml` paralel koşan T44 ajanının `f6f1f62` ve `cf157cd`
   commit'lerine girdi. Kendi commit'im `29248ba` yalnız `README.md`, `CHANGELOG.md` ve
   `InstallerTests.cs` taşıyor. İçerik kaybı yok, atıf yanlış. Düzeltilmedi, geçmiş
   yeniden yazılmadı. `_sorun.log` dosyasına yazıldı.
3. `QualityMeterTests.SameClipComparedToItselfScoresHighVmaf` paralel yük altında oynak
   (yukarıda). T45 kapsamı dışında.
4. `.calisma/` altına hiçbir geçici dosya bırakılmadı.

## Çıktı

K1 yayın akışına başlatıcıyı ekledi, K2 ve K3 iki betiği de indirme ve sağlama doğrulama
akışına geçirdi, K4 on beş ölçümle ikisini de metinden sınadı. `dotnet build` 0 uyarı
0 hata, `dotnet test` 550 başarılı / 0 başarısız / 16 atlandı.

Koşturulamayanlar: kurulum betikleri (bu makinedeki kurulumu silerdi), yayın akışı
(etiket itilmedi). Açık karar: `manifest.md` başlatıcı varlığını anlatmıyor, dosya
`owns` listesi dışında.

### Kimlik uyusmazligi — T0 karari

Stop kancasi bu kosunun beyan edilen efordan dusuk kostugunu bildirdi: beyan `xhigh`, gercek `medium`. Efor ayari kosu icinden degistirilemiyor, o yuzden is oldugu gibi `submitted` birakildi. Yukaridaki butun olcumler bu kosuya ait; sonucu kabul edip etmemek ve isi dogru eforla yeniden acmak T0'in karari.

---

## T0 doğrulaması — 2026-08-26

Denetçi ajanı iki kez açıldı, ikisi de yargı vermeden döndü: efor kimlik denetimi
(`relay-watch.js:407`) beyan `high`, gerçek `medium` görüp bloke ediyor. Efor `Agent`
çağrısında geçilemiyor — eklentinin kendi belgesi de böyle diyor
(`skills/relay/SETTINGS.md:110`, `:216`). Kayıt: `HATA-efor-kimlik-denetimi-butun-ajanlari-blokluyor.md`.

Bu yüzden kriterleri T0 kendisi satır satır okudu. **Mühür kanıtı değildir** — bağımsız
denetçi kaydı hâlâ eksik.

### Ham ölçüm, T0'ın kendi koşusu

    dotnet build VidShrink.sln -c Release  ->  0 Uyarı, 0 Hata
    dotnet test  VidShrink.sln -c Release  ->  Başarısız: 0, Başarılı: 553, Atlanan: 16, Toplam: 569

(569, T44 tur 2'nin dört testi de girdikten sonraki sayı.)

### K1 — okundu, tutuyor

`release.yml:100-110` başlatıcıyı `if: matrix.rid == 'win-x64'` koşuluyla yayımlıyor.
`:137-141` `publish-launcher` klasörü varsa `vidshrink-launcher-<rid>.zip` yazıyor ve
`checksummed` listesine ekliyor, `sha256sum` üçünü birden özetliyor. Manifest yalnız
`publish/<rid>` klasörünü anlatıyor (`:117`), başlatıcı oraya girmiyor — "manifeste
girmiyor" iddiası doğru.

**Açık risk, ölçülmedi:** `publish` işi `ubuntu-latest`'ta koşuyor (`:78`) ve başlatıcı
`net8.0-windows` hedefliyor. Saf yönetimli kod, `UseWPF`/`UseWindowsForms` yok, yalnız
`VidShrink.Core` referansı var — Linux'ta çapraz yayımlanması bekleniyor. **Beklenti,
ölçüm değil.** İş akışının tetiği yalnız `v*` etiketi; etiket itilmeden koşturulamıyor.
v0.1.0'ı düşüren kusur da tam bu sınıftandı (`UseWin32()` Linux'ta).

### K2 — okundu, tutuyor

Yasaklı iz sayımı: `dotnet publish`, `dotnet-install`, `Find-DotNetSdk8`,
`Install-DotNetSdk8`, `archive/refs/heads/main`, `win-arm64`, `win-x86` — **toplam 0**.

Sıra doğru: indir (`:145-147`) → sağlama doğrula (`:150-152`) → aşamalandır (`:158-159`)
→ `.update-version` yaz (`:163`) → ffmpeg (`:165-168`) → süreçleri durdur (`:170-174`)
→ kökü sil ve kopyala (`:176-180`) → düzeni doğrula (`:184-186`) → kısayol (`:189+`).

Sağlama tutmazsa `throw` (`:90`), listede ad yoksa `throw` (`:87`), varlık yayında yoksa
`throw` (`:136`). Hiçbiri uyarı basıp devam etmiyor.

`.update-version` `app\` altına yazılıyor (`:163`, `$appStageRoot`) —
`UpdateCheck.ReadVersionMarker` (`UpdateCheck.cs:203`) tam orayı okuyor. Sürüm
`$tag.TrimStart('v')` (`:128`), yani `0.1.0` biçiminde; `Trim()`'li okuyucuyla uyumlu.

Kısayollar `$installedExe` = kökteki `VidShrink.exe` (`:182`), uygulama değil.
`-InstallRoot` / `-NoLaunch` / `-SkipShortcuts` ve LocalAppData kısıtı (`:112`) duruyor.

Eski betiğin yapmadığı bir iyileşme: kurulum önce **aşama klasöründe** hazırlanıyor,
kurulum kökü ancak her şey hazırken siliniyor. İndirme yarıda kesilirse eski kurulum
yerinde kalıyor.

### K3 — okundu, tutuyor

`install-vidshrink.sh:105-106` `unzip` ve `awk` yokluğunda duruyor. `:77-84` `sha256sum`
yoksa `shasum -a 256`. `:87` beklenen özeti `awk` ile alıyor, `:93` karşılaştırıp
tutmazsa duruyor. `:157` `.update-version` yazılıyor. `:59-74` ffmpeg kurmuyor, paket
yöneticisi komutunu basıp `exit 1`. `:28-36` yalnız `osx-arm64`, `osx-x64`, `linux-x64`;
başka mimaride "Bu mimari için yayın yok" ile duruyor.

### K4 — okundu, tutuyor

`InstallerTests.cs` 9 `[Fact]` + 1 `[Theory]`, toplam 15 ölçüm. **Sessizce yeşil test
yok:** erken `return` yok, `Skip` yok; betikler `File.ReadAllText` ile okunuyor
(`:17-20`), dosya yoksa ölçüm patlar, sessizce geçmez.

Ad eşleşmesi gerçekten `UpdateCheck`'e bağlı: `Split()` (`:27-31`) `ArchiveAssetName("RID")`
dönüşünü `RID` üzerinden ön ek/son eke bölüyor, `:38-40` üç dosyada da o parçaları arıyor.
Sabit dizge karşılaştırması değil — `UpdateCheck` tarafındaki ad değişikliği ölçümü düşürür.

### Sonuç

Dört kriter de T0 okumasında tutuyor. **Sözleşme mühürlenmedi**; bağımsız denetçi kaydı
efor engeli kalkınca alınacak. Tek gerçek açık K1'in Linux çapraz yayımı — onu ancak bir
etiket koşusu kapatır.
