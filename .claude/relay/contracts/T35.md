---
id: T35
title: Paylaşım sağlayıcıları — storage.to ve uguu.se, uç nokta tablosu
role: builder
model: opus
depends: []
status: active
round: 0
audit: —
auditor_id: —
diff: —
owns:
  - src/VidShrink.Core/Share/
  - tests/VidShrink.Tests/ShareProviderTests.cs
  - tests/VidShrink.Tests/DriveShareTests.cs
  - tests/VidShrink.Tests/DriveRetentionTests.cs
  - paylasim-hedefleri.json
side_effects: []
---

## Amaç

Kullanıcı paylaşım hedefini seçti: **storage.to birincil, uguu.se ikincil.** Google Drive
yolu iptal edildi.

Bu sözleşme Drive kodunu kaldırır ve yerine iki anonim sağlayıcı koyar.

## Neden Drive gitti

Kullanıcı Cloud Console'da proje açmayı, uygulama kaydı yapmayı ve OAuth istemci kimliği
almayı reddetti. Gerekçesi tek cümleydi:

> ben tek tıklamayla dosyasını adam şuraya atsın istedim, sen bilmem nereden api alalım
> da onu programa atalım anca o zaman çalışır diyorsun

Ek olarak AGPL: kaynak yayımlandığı için ikilinin içine gizli anahtar konamaz. Anahtar
isteyen her sağlayıcı bu yüzden zaten elenir.

**Drive kodu ürüne hiç bağlanmamıştı** — `MainWindow.axaml.cs` içinde tek bir `Drive`
çağrısı yok. Yani bu bir geri alma değil, hiç açılmamış bir dalın kesilmesi.

## Ölçülmüş gerçekler — bunlara güven, yeniden ölçme

İki sağlayıcı da bu makineden (Türkiye) gerçek dosyayla uçtan uca denendi (24.08.2026).
Ayrıntı: `docs/taramalar/anonim-kisa-omurlu-video.md`.

### storage.to

Üç adımlı akış, hiçbir adımda kimlik doğrulama yok:

1. `POST https://storage.to/api/upload/init` — gövde `{filename, size, content_type, expiry_days}`,
   başlık `X-Visitor-Token: <istemcinin kendi ürettiği rastgele dize>`.
   Yanıt: `upload_url` (Cloudflare R2 presigned), `r2_key`.
2. `PUT <upload_url>` — `Content-Type` init'te verilenle **aynı** olmalı, gövde dosyanın ham içeriği.
3. `POST https://storage.to/api/upload/confirm` — gövde `{r2_key, filename, size, content_type, expiry_days}`.
   Yanıt: `file.url` (paylaşılacak bağlantı), `file.expires_at`, **`owner_token`**.

Silme: `DELETE https://storage.to/api/file/{id}`, başlık `Authorization: Owner <owner_token>`.
Ölçüldü: `{"success":true}` döndü, bağlantı **410 Gone** oldu.

- `expiry_days` 1-7 arası, anonim varsayılan 3. İstenen 1 gün, dönen `expires_at` tam 1 gün sonrasıydı.
- Tavan 25 GB.
- Paylaşım sayfası gerçek `<video controls playsInline preload="metadata">` içeriyor.
- Medya **ayrı alan adından** geliyor: `cdn.storagetobox.com`, imzalı URL, `expires`+`sig` parametreli.
  Ölçüldü: `Content-Type: video/mp4`, `Accept-Ranges: bytes`, `Content-Disposition` **yok**,
  aralık isteğine `206 Partial Content` + doğru `Content-Range`.
- İmzalı CDN URL'i ~30 dakikada sona eriyor **ama paylaşım sayfası her ziyarette yeniden
  imzalıyor.** Kullanıcıya verilecek bağlantı `file.url`, CDN URL'i **değil**. Bu ayrımı kod
  içinde yorumla belirt — yanlış olanı paylaşmak sessizce kırılan bir hata üretir.

### uguu.se

Tek adım: `POST https://uguu.se/upload?output=text`, çok parçalı alan adı `files[]`.
Yanıt düz metin olarak doğrudan URL (`https://d.uguu.se/XXXXXXXX.mp4`).

- Ömür **3 saat**, sabit, seçilemiyor. Tavan **128 MiB**.
- **Silme jetonu yok.** 3 saatlik otomatik silme onun yerine geçiyor — bu bir eksiklik,
  arayüz bunu kullanıcıya söylemek zorunda (T37'nin işi).
- Ölçüldü: `Content-Type: video/mp4`, `Accept-Ranges: bytes`, aralık isteğine `206`.

## Yapılacaklar

### Y1 — Uç nokta tablosu veriden okunsun

`paylasim-hedefleri.json` — uç noktalar, boyut tavanı, ömür seçenekleri, silme yeteneği
**koddan değil veriden** gelsin. Bir servis ölürse ya da tavanını değiştirirse düzeltme
JSON düzenlemesi olsun, sürüm çıkmasın.

**Kabul:** yeni bir hedef eklemek için tek satır C# değişmiyor.

Bu servislerin ortalama ömrü kısa ve storage.to genç bir servis — tablo bu yüzden var.

**Şema sabittir.** T36 (arayüz) bu dosyayı paralel olarak okuyor ve aynı şemaya göre
yazıldı. Alan adlarını değiştirme; değiştirmen gerekiyorsa önce T0'a haber ver.

```json
{
  "version": 1,
  "default": "storage.to",
  "targets": [
    {
      "id": "storage.to",
      "displayName": "storage.to",
      "maxBytes": 26843545600,
      "retentionDays": [1, 2, 3, 4, 5, 6, 7],
      "defaultRetentionDays": 3,
      "canDelete": true,
      "playsInBrowser": true,
      "endpoints": {
        "init": "https://storage.to/api/upload/init",
        "confirm": "https://storage.to/api/upload/confirm",
        "delete": "https://storage.to/api/file/{id}"
      }
    },
    {
      "id": "uguu.se",
      "displayName": "uguu.se",
      "maxBytes": 134217728,
      "retentionDays": [],
      "fixedRetentionHours": 3,
      "canDelete": false,
      "playsInBrowser": true,
      "endpoints": { "upload": "https://uguu.se/upload?output=text" }
    }
  ]
}
```

### Y2 — `IShareProvider` ve iki uygulaması

Ortak arayüz: yükle (ilerleme + iptal), silme jetonu döndür, sil.

Sağlayıcıya özgü davranış arayüzün arkasında kalsın. `uguu` silme desteklemiyor —
bunu çalışma zamanında `false` döndürerek bildirsin, `NotSupportedException` atmasın;
arayüz bu bayrağa bakıp düğmeyi gizleyecek.

### Y3 — Drive kodunun kaldırılması

Silinecek: `DriveAuth.cs`, `DriveClient.cs`, `TokenStore.cs`, `RetentionPolicy.cs`,
`SharedFileLog.cs`, `DriveShareTests.cs`, `DriveRetentionTests.cs`.

**Kalacak:** `ShareResult.cs` içinden `ShareFailure` ve `ShareLedger` — ikisi de
sağlayıcıdan bağımsız ve yeni yolda kullanılıyor.

Silmeden önce `t31-drive` etiketini at; kod git geçmişinde kalsın.

**Kabul:** `Drive` kelimesi `src/` altında hiç geçmiyor. Test sayısı düşüşünün tamamı
Drive testleri olacak, başka test kaybolmayacak.

### Y4 — Hata sınıflandırma

`RAPOR.md`'deki "stderr → eyleme dönüşebilir mesaj" fikri burada geçerli: HTTP durumu ve
gövdesi, kullanıcının **yapabileceği bir şeye** çevrilsin.

- Tavan aşıldı → hangi tavan, hangi hedef daha büyük
- Ağ yok / uç ölü → hedef listede gri, sebebi yazılı
- 403/429 → ne zaman tekrar denenebilir

`Unknown`'a düşen oran ölçülsün; %10'u geçiyorsa tablo yetersizdir, rapora yaz.

**Uç noktalara açılışta istek atma.** Sağlık kontrolü kullanıcı düğmeye bastığında,
tek `HEAD` ile. Uygulama açılışta sessizce ağa çıkmasın.

### Y5 — Kimlik

İstek `User-Agent` başlığında uygulamayı adıyla ve sürümüyle tanıtsın. Sebep: kötüye
kullanım şikâyeti kullanıcının IP'sine değil uygulamaya gitsin ve bize haber gelsin.

## Kabul kriterleri

- `dotnet build -c Release` 0 uyarı, `dotnet test` tamamı yeşil
- Sahte taşıyıcıyla (`IHttpTransport`) her iki sağlayıcı için birim testler: başarı,
  tavan aşımı, ağ hatası, iptal, silme
- **Bir kez gerçek ağ denemesi**: her iki sağlayıcıya `testsrc2` ile üretilmiş küçük bir
  klip yüklenip bağlantı açıldığı doğrulanacak, sonra storage.to'daki silinecek.
  Test dosyası sentetik olacak — kullanıcının videosu **kullanılmayacak**.
- `Drive` kelimesinin `src/` altında geçmediğinin kanıtı

## Doğrulama

Rapora: iki sağlayıcının uçtan uca çalıştığının kanıtı, silinen dosya sayısı, test
sayısındaki değişim ve düşen testlerin hepsinin Drive testi olduğu, koşturulamayanlar.

**Ortam notu:** `%LOCALAPPDATA%\Microsoft\dotnet\dotnet.exe` (8.0.424) kullan; PATH'teki
3.1.201 derlemeyi `MSB3644` ile düşürür. `VidShrink.sln` dosyasına dokunma.

## Kayıt noktası

Her adımdan sonra commit at. Uzun kapanış mesajı yazma; commit'ler işi taşısın.

**Durum (round 0):** Y1-Y4 yazıldı ve commit edildi (13e0a1e, b235fa6, 7b1efb8).
Kol c424984'te takılıydı, main'e ileri sarıldı — sözleşme o yüzden ilk denemede okunamamıştı.
`t31-drive` etiketi atıldı, Drive'ın yedi dosyası silindi. Release derleme 0 uyarı;
`dotnet test` 292 başarılı / 6 atlanan / 298 toplam, kaybolan 43 test yönteminin
tamamı Drive testi (isim listesiyle doğrulandı).
**Kalan:** gerçek ağ denemesi (testsrc2 klibi, iki sağlayıcı, storage.to'daki silinecek)
ve Rapor bölümü.
