---
id: T116
title: Kilitsiz olcerle konmus cipalar — geri alinir mi olculur
role: builder
model: opus
depends: [T110]
status: submitted
round: 1
verify: [dotnet test -c Release --filter "QualityMeterTests|MeasuredQualityTests"]
owns: [docs/olcumler/algi-olcusu.md, docs/olcumler/olculen-kaliteyle-plan.md, tools/cipa-yeniden/**]
side_effects: []
---

## Bağlam

T110 mühürlendi ve şunu ölçtü: ölçer bugüne kadar **kaymış kare eşlemesiyle**
ölçüyordu. `settb=AVTB,setpts=N` kilidi eklendikten sonra aynı çift üzerinde
çıpa **75,07 → 87,20**, yani **12,13 puanlık** hata. Kilit artık iki zincirin
de sonunda ve tek üretim yolundan geçiyor (`QualityMeter.cs:73,86-88,359`).

Bu bir ölçüm aracı düzeltmesi değil, **geriye dönük bir geçersizlik**. T110
kendi belgesinde §9.7'de "bugüne kadarki bütün çıpalar kaymış ölçüyle
konmuştu" diyor ama hangi belgelerin etkilendiğini saymadı. Denetim bir tanesini
adıyla buldu:

**`docs/olcumler/olculen-kaliteyle-plan.md:36-53`** bir `--measured-quality`
A/B tablosu taşıyor; hem "yeni" kolun çıpaları hem tablonun bütün
mean/harm/p10 sütunları **kilitsiz ölçerden** geçti. O tablonun sonucu —
*"kazanç yalnız HDR'de, +0,95"* — bugün **dayanaksız**. `--measured-quality`
kolunun açık kalıp kalmayacağı buna bağlı.

## Bu sözleşme neyi yapar

İki iş: **etkilenen çıpaları saymak** ve **kararı taşıyan tabloyu yeniden
ölçmek**. Yanında T110'un bıraktığı dokuz düzyazı borcu kapanır.

## Kabul kriteri

1. **Etkilenen belge listesi çıkarılır ve sayılır.** `docs/olcumler/` altında
   VMAF/XPSNR sayısı taşıyan her belge taranır; her biri için üçünden biri
   yazılır: "kilitli ölçerden geçti" · "kilitsiz ölçerden geçti, yeniden
   ölçüldü" · "kilitsiz ölçerden geçti, **yeniden ölçülmedi**". Üçüncüsü kabul
   edilebilir bir cevaptır — **sessizlik değil.**
   CHECK: belge başına bir satır, tam liste.

2. **`olculen-kaliteyle-plan.md:36-53` tablosu kilitli ölçerle yeniden
   üretilir.** Aynı kaynaklar, aynı hedefler, aynı kollar. Yeni tablo eskisinin
   yanına konur, **eski sütun silinmez** — "kilitsiz (geçersiz)" damgasıyla
   durur ki farkın büyüklüğü görünsün.
   CHECK: satır başına eski/yeni mean, harm, p10.

3. **"+0,95 yalnız HDR'de" sonucu yeniden yargılanır.** Yeni ölçümde kazanç
   nerede, ne kadar? İşaret değişiyorsa yazılır. **`--measured-quality` kolunun
   açık kalması gerekip gerekmediği ölçüye bağlanır** — bu bir üründe var/yok
   kararıdır, düzyazı değil.
   CHECK: kol başına kazanç, karar ve gerekçesi.

4. **12,13 puanın ne kadarının çıpaya taşındığı ölçülür.** Çıpa koşumda
   hesaplanıyor (`ComplexityProfile.cs:224`, `PlanCalculator.cs:234`), depoda
   saklı sabit değil. Yani kilit eklendiği anda çıpa kendiliğinden düzeldi mi,
   yoksa kalibrasyonun bir turu mu gerekiyor? **Ölç.**

5. **T110'un dokuz borcu kapanır.** Hepsi `docs/olcumler/algi-olcusu.md`'de.
   Her biri için ya "tablodan yeniden sayıldı" ya "ölçülemedi" yazılır:
   - `:30` §1 hâlâ **kilitsiz grafiği** basıyor (`[0:v]…[t];[1:v]…[r];[t][r]…`).
     Doğrusu §9.5'te. §9.11'in kirlenmiş satır envanteri bu satırı listelemiyor
     — envantere de girsin.
   - `:696` "Kalan 234 kare iki koşumda da aynı" — ham günlükte **8 kare
     farklı** (29, 30, 31, 133, 134, 135, 205, 206), yani 232. 31. kare 0,76
     puan oynuyor.
   - `:576` ve `:596` düzyazısı damgasız: "p1'de sinyal gürültünün yüzde biri"
     ve "üç içerikte ölçüldü ve kalite ayıramadı". Kilitli ölçüde p1'in sinyali
     3,1471, gürültüsü 1,3748 — **p1 ayırıyor.** Tablo damgalı, cümle değil.
   - `:972` "CI'ın **yaklaşık 80** ölçüyü atlaması" — ölçülen 106 (yerel betik)
     ve 100 (GitHub CI). Aynı bölümün tablosuyla uyuşmuyor.
   - `:118` damgası "değişmedi (97,4257)" diyor, damgaladığı `:120` **97,4256**.
     İki farklı klibin sayısı tek damgada birleştirilmiş; §9.10 yalnız p1'i
     ölçtü.
   - `:740` civarı — "(1, 2] kare iki kare kaydırıyor" bacağının **çıpası yok**;
     37,8810'un karşılığı iki karelik referans ölçülmemiş. Ölç ya da
     "ölçülmedi" yaz.
   - `:775` §9.6'da alıntılanan `Note` metni "20 ms (**1,2** kare)"; kod
     `InvariantCulture` kullandığı için gerçek çıktı "20 ms (**1.2** kare)".
   - `.calisma/T110/pin/kaymis-ref.mkv` belgede "ilk kare atılıp
     `setpts=N/FR/TB`" diye tarif ediliyor; gerçekte `-itsoffset` kopyası
     (video `start_time=0.020000` + aac akışı, h264 kopya). Sayı doğru,
     **düzeneğin tarifi yanlış.** Tarifi dosyaya uydur, dosyayı tarife değil.

6. **Ölçmediğin şey için "ölçülmedi" yaz.** Bu projede son on sözleşmenin
   sekizinde tur açtıran bulgu aynıydı: **türetilmiş bir sonuç "ölçülen" diye
   yazıldı.** Bu sözleşmenin konusu tam olarak o kusurun geriye dönük bedeli.

7. **Mutasyon kanıtı.** Bu turda kod değişirse (K3 kolu kapatmayı gerektirirse)
   değişen her sabit iki yönde bozulunca ölçü kırılmalı. İki sabiti
   karşılaştıran ölçü yazma, `Skip` yazma, ffmpeg yokluğunda sessiz erken dönüş
   yazma. ffmpeg gerektiren ölçü `[FfmpegFact]`.
   **Her mutasyon koşumundan önce `dotnet build VidShrink.sln -c Release
   --no-incremental`** — artımlı derleme mutasyonu ikiliye taşımıyor ve
   yalancı "kırılmadı" veriyor.

8. **Hangi koşum neyi ölçtü, yazılır.** "Tam süit yeşil" bir koşumdur, kanıt
   değil. `tools/ci-gibi-kos.sh` ffmpeg'i PATH'ten siliyor; oradaki yeşil
   `[FfmpegFact]` ölçüleri hakkında hiçbir şey söylemez. Teslim raporunda
   atlanan sayısı ve **kabul kriterlerini taşıyan ölçülerin o listede olup
   olmadığı** yazılı olsun.

## Sınırlar

- `owns` dışına yazma. `QualityMeter.cs` ve `QualityMeterTests.cs` T110 ile
  mühürlendi ve **şu an sahipsiz** — K3 bir kod değişikliği gerektiriyorsa
  önce bana söyle, `owns`'una eklerim. `PlanCalculator.cs` T107'nin,
  `ComplexityProfile.cs` T103'ün; okuyabilirsin, yazamazsın.
- **Telafi sabiti yazma.** Bir katsayıyı "böyle olunca sayı tutuyor" diye
  koyuyorsan neyi telafi ettiğini bul; telafiyi değil nedeni düzelt ya da
  nedeni bildir.
- **Harmonik ortalamaya yaslanma.** T110 denetimi `QualityMeter.cs:147`'deki
  `Math.Max(x, 1.0)` tabanını açığa çıkardı: `VmafNegMin = 0` olan satırlarda
  harmonik tabandan geliyor, p10'un altına düşebiliyor. Ortalama ve p10 sağlam.
- Ölçüm dosyaları `.calisma/` altına, düzenek `tools/` altına. `.calisma/`
  gitignore'lu ve worktree-yerel — **oraya koyduğun düzeneği denetçi göremez.**
  Rapora giren her sayının üreten komutu yazılı olsun.
- `.calisma/kaynak/` ortak, dokunma.
- Makine paylaşımlı, beş ajan koşuyor. Kodlama komutlarında iş parçacığını
  **sabitle** (`-threads N`, x265'te `pools`). Süre sayısına "makine
  paylaşımlıydı" damgası bas; kalite ve boyut sayısına basma.
- Tam süiti koşturma; yalnız `verify` filtresi.
- Kod yorumu yazma.

## Kayıt noktası

Her kabul kriteri bittiğinde commit. Dal: `T116-cipa-yeniden`.

## Çıktı

- Etkilenen belge listesi, üç durumdan biriyle işaretlenmiş.
- `olculen-kaliteyle-plan.md`: kilitli ölçerle yeniden üretilmiş A/B tablosu,
  eski sütun "kilitsiz (geçersiz)" damgasıyla yanında.
- `--measured-quality` kolu hakkında ölçüye bağlı karar.
- `algi-olcusu.md`: dokuz borç kapanmış.
- Kendi dalın, itilmiş. `main`e sen birleştirme.

### Teslim (tur 1)

**Dal:** `T116-cipa-yeniden`, itildi.

**En önemli sonuç, K4 — ölçü bozuktu, kararlar bozulmadı.** Çıpa saklanan bir
sabit değil, `ComplexityProfile.WithProbeQuality` her koşumda yeniden
hesaplıyor; kilit ürün koduna girdiği an (`822dd3a`) eski çıpa diye bir şey
kalmadı. **Kalibrasyon turu gerekmiyor.** Ölçüldü — aynı ağaçtan kilitli ve
kilitsiz iki ikili yayımlanıp aynı kaynaklarda koşuldu:

| kaynak | çıpa kilitsiz | çıpa kilitli | fark |
|---|---:|---:|---:|
| `parca-1.mkv` | 82,726502 | 87,192345 | **+4,465842** |
| `parca-2.mkv` | 91,550111 | 91,575281 | +0,025170 |
| `sdr-1.mkv` (ikame) | 82,277860 | 91,718219 | **+9,440358** |

`parca-1`in üçüncü penceresi T110'un çiftidir: 75,074404 → 87,203740,
**+12,129337**, rakamı rakamına. İkinci pencere onbeş basamağa kadar özdeş.

**Plan yedi hedefin hiçbirinde değişmedi.** `PlanCalculator.BuildDetailed` iki
ikiliyle koşuldu, plan alanları (genişlik, yükseklik, fps, kodlayıcı, kip, crf,
bit hızı, preset) karşılaştırıldı: `parca-1` 8/12/20/30/40/60/80 MB — **7/7**;
`parca-2` aynı yedi — **7/7**; `sdr-1` 8/12/20/30/40 MB — **5/5** (60 ve 80 MB
sayılmadı, kaynak 23,1 MiB, hedeften küçük). **Toplam 19 çift, 19'unda plan
özdeş.** Değişen tek alan `PredictedQuality` ve o da çıpa farkı kadar
(`parca-1` her hedefte +4,4658, `sdr-1` her hedefte +9,4404). `sdr-1`
ikamesindeki **+28,32** puanlık pencere hatası bile planı değiştirmedi.
Ölçülmedi: çıpanın plana hiçbir koşulda geçmediği ölçülmedi — ölçülen 3 kaynak ×
19 hedeftir; `SpreadHalvings` üçünde de 0 çıktığı için `PerHalving` kolu hiç
tetiklenmedi.

Çıpalar ikinci ve bağımsız bir yoldan da doğrulandı: `bench --measured-quality`
koşumlarının kendi günlükleri aynı pencere sayılarını basıyor (`parca-1`
87.43/86.95/87.2, `parca-2` 92.48/91.13/91.12, `sdr-1` 93.91/90.32/90.93).

**K1 — 20 belgenin 8'i VMAF/XPSNR taşıyor**, üçünden biriyle işaretlendi
(`algi-olcusu.md` §10.2). Yeniden ölçülenler: `olculen-kaliteyle-plan.md`
(tamamı) ve `algi-olcusu.md`in §9.2 / §9.4 / §7-p1 satırları. Hiç yeniden
ölçülmeyen beş belge adıyla yazıldı (`handbrake-acigi`, `auto-mod`,
`tepe-tavani-ve-psy`, `ornekte-vmaf-maliyeti`, `kazanc-kullaniciya-ulasiyor-mu`);
altıncısı `olcu-gecerliligi.md`, karışık ve T111'in. `auto-mod.md` için ölçüt
zaman değil düzenek: `tools/auto-mod-olcumu/*.sh` kendi ffmpeg çağrılarını
kuruyor, 8 ölçüm satırının 0'ında kilit var.

**K1 eki — envanterden sonra `main` iki belge daha getirdi.** T116 koşarken
`docs/olcumler/` 20'den **22'ye** çıktı. İkisi de VMAF/XPSNR taşıyor ve ikisi de
**kilitsiz ölçerden geçti, yeniden ölçülmedi**: `ab-duzenegi.md` (T95, 723 satır,
`tools/VidShrink.Ab`) ve `bppf-tabani.md` (650 satır, `bench shrink`, sayıları
`6fac0be` / 2026-08-25). `VidShrink.Ab` ürünün `QualityMeter`ını çağırıyor
(`AbRunner.cs:205-206`, `:338-339`), yani **dördüncü bir kilit yeri değil** —
`main`e uyduğu anda kilidi alır; borç yayımlanmış sayılarda. Kanıt okumayla değil
commit'ten: belgenin damgaladığı dört ölçüm commit'inin (`af7a0fe`, `94df05c`,
`a1994b3`, `381e8ab` — sonuncusu bugün 07:23, kilitten **iki buçuk saat sonra**)
dördünde de `QualityMeter.cs` `settb=AVTB` taşımıyor; T95'in dalı kilidi hiç
almamış. `ab-duzenegi.md` kendi metninde "libvmaf kareleri yanlış eşler ve
sessizce bir sayı üretir" diye aynı olguyu adlandırıyor ama kendi ölçümüne kilidi
takmamış. T116 bu iki belgeyi yeniden **ölçmedi** (ikisi de `owns` dışında,
ızgara kapandıktan sonra göründüler); kaymanın sonuçlarına ne yaptığı
**ölçülmedi**. Ayrıntı `algi-olcusu.md` §10.2 eki.

**K2 — ızgara ikame kaynaklarla, hem kilitli hem kilitsiz ikiliyle koşuldu.**
T89'un üç kaynağı o turda silinmiş, aynı kaynakla yeniden ölçmek **imkânsız**.
Eski tablo silinmedi, "kilitsiz (geçersiz), kaynak silinmiş, yeniden üretilemez"
damgasıyla yerinde; yeni tablo **ayrı bölümde**, aralarında "bu iki tablo aynı
kaynakları ölçmüyor, satırları karşılaştırılamaz" cümlesi var. `oyun` satırı
**ölçülmedi** olarak duruyor (48 fps av1 oyun kaydı yok). İkame damgası her
satırın kaynak sütununda. Üretim tarifleri `tools/cipa-yeniden/duzenek/`
altında: `ikame-kaynak.sh` (tonemap + libx264 preset fast 2 geçiş 3400k →
23,1 MiB), `tonemap-dogrulama.sh`, `ikili-kilit-denetimi.sh`, `ab-kos.sh`,
`ab-tablo.py`, `bolum-11-5.py`, `adcozum/`.

**K3 — kol açık kalır. Gerekçe ölçü.** Kilitli ölçerde dört hücrenin dördü:

| hücre (ikame) | plan değişti mi | Δmean | Δp10 | Δteslim |
|---|---|---:|---:|---:|
| klip 8 MB | hayır, özdeş | −0,01 | −0,30 | −0,012 MB |
| klip 20 MB | hayır, özdeş | +0,03 | −0,63 | ±0,000 MB |
| hdr parca-1 40 MB | **evet** 1458@60 2pass 5389k → 1612@60 crf 21 | **+1,60** | **+3,32** | **−1,594 MB** |
| hdr parca-2 40 MB | **evet** 1920@60 2pass 5258k → 1920@60 crf 23 | −0,01 | −0,01 | −0,340 MB |

İki SDR hücresinde plan **sekiz alanın sekizinde de özdeş**; kolun etkisi orada
tam olarak sıfır ve bu sayıdan değil plan özdeşliğinden okunuyor. Planı
değiştirdiği iki hücrede sonuç **+1,60** ve **−0,01**. **Ölçülmüş zarar yok,
ölçülmüş kazanç var** (+1,60 mean ve **daha küçük** dosya). İki kayıtla birlikte
okunmalı: (a) kazanan hücrede `yeni` kol hedef bandını **ıskaladı** — 40 MB'a
karşı 37,4 MB, %93,5, bant=dış; `eski` kol %97,5 ile bant içindeydi, yani kol
bütçenin 1,6 MB'ını harcamadan bıraktı; (b) `parca-2`de planın `crf 23` kararı
teslime **hiç ulaşmadı**, doldurma döngüsü onu 2 geçiş 5214k'ya ezdi ve dosya
`eski` kolun yerine oturdu — Δmean'in −0,01 kalmasının sebebi bu. Doldurma
döngüsünün planın kip kararını hangi koşullarda ezdiği **ölçülmedi**. Aynı planın iki
koşumu 0,0083 MB farklı dosya ve 0,0014 dB farklı XPSNR üretiyor — kodlama
yeniden üretilebilir değil; bu değişkenliğin mean VMAF cinsinden büyüklüğü
**ölçülmedi**. Kol zaten sevk edilmiş ve varsayılan:
`MainWindow.axaml.cs:1445` → `ProbeWithMeasuredQualityAsync(..., null, ...)` →
`ComplexityProbe.cs:53` `null` metreyi `QualityMeasurement.Instance`'a çeviriyor;
T100 bunu T89'un "+0,95 yalnız HDR'de" cümlesini gerekçe göstererek bağladı
(`f2bd2a6`).

**"+0,95 yalnız HDR'de" yargısı:** yön doğru, ifade fazla geniş, büyüklük
karşılaştırılamaz. Kazanç yine yalnız HDR kaynaklarda göründü ama **iki HDR
ikamesinin birinde**; HDR olmak yeterli koşul değil. Kazanan hücrede mekanizma
T89'un adlandırdığıyla aynı: yerleşim büyüyor, kip iki geçişten crf'ye dönüyor.
+1,60 ile +0,95 karşılaştırılamaz — kaynaklar farklı.

Ölçülmedi: kolun zaman bedeli (`ProbeSeconds` farkı +1,9 / +14,9 / +7,2 /
**−7,7** sn, işareti bile tutarsız, makine dokuz ajanla paylaşımlıydı), kazancın
genellenip genellenmediği, `oyun` sınıfı, kolun kaybettiği bir kaynak olup
olmadığı, bant ıskasının yinelenip yinelenmediği, aynı planın iki kodlaması
arasındaki değişkenliğin mean VMAF cinsinden büyüklüğü.

**Ölçerin karara etkisi ölçüldü ve yok.** Onaltı hücrenin onaltısı koşuldu; kol
farkı dört hücrenin dördünde iki ölçerde de aynı çıktı — kilitli/kilitsiz Δmean
−0,01/−0,00, +0,03/−0,01, **+1,60/+1,61**, −0,01/−0,01. **Ölçerin kusuru kol
yargısını ne yarattı ne gizledi**, ve sekiz hücrenin sekizinde plan iki ölçerde
özdeş çıktı — bu K4'ün ızgaradan gelen bağımsız doğrulaması.

**Buna karşılık ölçerin kendi hatası büyük ve içeriğe bağlı:** aynı kolda, aynı
dosyada, yalnız ölçer değişince `klip` ikamesi 20 MB'ta mean **+5,53** / p10
**+13,22**, `hdr parca-1`de +0,43 / +1,37, `hdr parca-2`de +1,22 / +0,41.
Kilitsiz ölçer kaliteyi sistematik olarak **düşük** gösteriyordu; neden içeriğe
bağlı olduğu **ölçülmedi**. Harmonik ortalama sütunları (aynı yerde +29…+43)
hiçbir yargıya sokulmadı — `QualityMeter.cs:147`'deki `Math.Max(x, 1.0)` tabanını
taşıyorlar.

**K5 — dokuz borcun dokuzu kapandı** (`algi-olcusu.md`):

1. §1'in kilitsiz grafik satırı kilitli haliyle yazıldı, §9.11 envanterine satır
   eklendi.
2. "Kalan 234 kare aynı" → **8 kare farklı, 232 aynı** (29, 30, 31, 133, 134,
   135, 205, 206); kare 31 36,7814 → 36,0200 (**−0,7613**), kare 135 −0,0328.
   ref1'in satırları rakamı rakamına yeniden üretildi.
3. `:576` "p1'de sinyal gürültünün yüzde biri" damgalandı: kilitli ölçümde p1'in
   sinyali **3,1471**, gürültüsü **1,3748** — **p1 ayırıyor**.
4. `:596` "üç içerikte ölçüldü ve kalite ayıramadı" damgalandı; aynı tablodan
   yeniden sayıldı, p2 ve p3 **ölçülmedi**.
5. "yaklaşık 80" → denetimin ölçtüğü **106** (yerel `ci-gibi-kos.sh`) ve **100**
   (GitHub CI). T116 tam süiti koşmadı, **kendi atlama sayısını ölçmedi**.
6. §9.10 damgası: 97,4257 / 97,4256 iki klibin sayısı tek damgaya karışmıştı,
   ayrıldı; p2/p3 **ölçülmedi**.
7. **Dairesel çıpa geri çekildi.** Belge "ilk kare atılıp `setpts=N/FR/TB`
   52,3008 veriyor" diyordu; o tarifle üretilen dosya **65,7226** veriyor.
   52,3008'i veren şey `-itsoffset 0.020` kopyasının **kendisi** (`k0.020.mkv`)
   — belge kendi kanıtını kendine dayandırmış. Çevrimsel olmayan çıpalar
   ölçüldü: kaydırma **içeriğe** uygulanıp (`select=gte(n,K),setpts=N/FR/TB`,
   sonra `libx264 -crf 16`) **kilitli** ölçüldü — kaydırma yok **98,3724**,
   1 kare **52,1174**, 2 kare **37,4526**. `(1, 2]` bacağının eksik çıpası budur.
8. §9.6'nın `Note` alıntısı koda uyduruldu: `InvariantCulture` yüzünden gerçek
   çıktı "20 ms (**1.2** kare)" — nokta. `QualityMeter.cs` owns dışı, **kod
   değiştirilmedi**, yalnız alıntı düzeltildi.
9. `kaymis-ref.mkv` tarifi dosyaya uyduruldu: `kaymis-1kare.mkv` /
   `kaymis-2kare.mkv`.

Yan ölçüm (borcun dışında, 6. maddenin ızgarasını sınamak için): `-itsoffset 0` ile
kurulmuş mkv kopyası `src.mp4`e **denk değil** — 91 kare üretiyor ve kilitsiz
81,8084 veriyor. Izgaranın 0 ms satırı `src.mp4` ile üretilmiştir.

**ref2'nin 240/239 kare uyuşmazlığı** — `algi-olcusu.md` §9.2'de, kendi
dosyamda, yazıldı: belge 240 diyor, bu turun `ref2.mkv`'si **239** kare ve
kilitli koşum 239'la eşleşti (81,4372 / 81,3171 / 81,0174). Sebep kesme adımı:
`-t 4` kabın 0,020 s kaymasından ötürü bir kare düşürüyor. T110'un sayısı geri
çekilmedi; hangi dosyanın ölçüldüğü ayrıştırılamadığı için **ölçülmedi**.

**Yan bulgu — CI'da koşan kilit ölçüleri ürünü pinlemiyor.** `VmafPoolingTests`in
iki düz `[Fact]` ölçüsü (`OlcumFiltresi_*`) CI'da koşuyor ama çağırdıkları
`MeasureFilterGraph` **`VidShrink.Bench`inki**: `Program.cs` hiç `namespace`
bildirmiyor, sınıf global ad uzayında ve kapsayan ad uzayının üyesi `using` ile
gelen adı yeniyor. Bu okunarak değil ölçülerek saptandı — testin ad uzayı ve
`using` kümesi birebir kopyalanıp `typeof(MeasureFilterGraph)` yazdırıldı
(`dotnet run --project tools/cipa-yeniden/duzenek/adcozum/adcozum.csproj` →
`derleme : VidShrink.Bench`). `grep -rn MeasureFilterGraph tests/` dört
kullanımın dördünü de o dosyada buluyor; **ürünün sınıfını anan tek bir ölçü
yok**, tek çağıranı `QualityMeter.cs:359`. İki `Build` imzası da farklı, yani
nitelenmiş yazılsa derlenmezdi. Düzeltilmedi — iki dosya da owns dışı.

**Ölçüm borcu, düzeltilmedi (owns dışı).** `sdr8-yeni-kilitli` koşumu `rc=0` ile
bitti, XPSNR yazdı, dört VMAF alanını `null` bıraktı, hiçbir hata satırı
üretmedi (`MeasureSeconds` 287,7). Sebep `tools/VidShrink.Bench/Program.cs:807`
`VmafNegAsync`: `RunLavfiAsync`in stderr'i çağrı yerinde **atılıyor**, çıkış kodu
**bakılmıyor**, sonra `if (!File.Exists(logPath)) return VmafPool.Empty;`.
Filtre kırılınca sonuç "hata" değil "ölçü yok" görünüyor. Bu, koordinatörün
`QualityMeter.cs:224` için kaydettiği borcun **ikinci örneği**. Koşum
tekrarlandı, ölçü geldi (mean 64,0037, 110,9 sn); kanıt
`.calisma/T116/gunluk/sessiz-bos/`. Kök sebep **ölçülmedi** — ffmpeg'in ne
dediği kaydedilmemişti.

**K6.** Ölçülmeyen her şeye "ölçülmedi" yazıldı; üçüncü seçenek kullanılmadı.

**K7 — ürün kodu değişmedi**, dolayısıyla mutasyon kanıtı gerekmedi. Değişenler:
iki belge ve yeni ölçüm düzeneği `tools/cipa-yeniden/`. Düzenek `src/` altındaki
hiçbir sabite dokunmuyor. Kilidin mutasyon bataryası T110'da koşuldu (§9.8).

**K8 — hangi koşum neyi ölçtü.**

    dotnet build VidShrink.sln -c Release --no-incremental            # rc=0
    dotnet test -c Release --no-build --filter "QualityMeterTests|MeasuredQualityTests"
    # Basarisiz: 0, Basarili: 24, Atlanan: 0, Toplam: 24, Sure: 4 dk 8 sn

**`MeasuredQualityTests` diye bir sınıf yok** — süzgecin ikinci yarısı hiçbir
şeyle eşleşmiyor, 24'ün 24'ü `QualityMeterTests`ten. Ölçülen kaliteyi gerçekten
sınayan üç ölçü `PlanCalculatorTests` içinde ve düz `[Fact]`, ayrı koşuldu:
`--filter "FullyQualifiedName~MeasuredQuality"` → **3 geçti / 0 kaldı /
0 atlandı**. Sözleşmenin `verify` alanına dokunulmadı (T0'ın); doğrusu şu
olurdu ve ikisini birden yakalar:

    dotnet test -c Release --no-build --filter "QualityMeterTests|FullyQualifiedName~MeasuredQuality"
    # Basarisiz: 0, Basarili: 27, Atlanan: 0, Toplam: 27, Sure: 1 dk 57 sn

Öneri denenmemiş değil, **koşuldu**: 27 = 24 + 3, ikisini birden yakalıyor.


`QualityMeterTests`in kapı dağılımı sayıldı: **11 düz `[Fact]`,
12 `[FfmpegFact]`, 1 `[TonemapFact]`** — ffmpeg'siz koşucuda 11 geçer,
**13 atlar** (T110 EK'inin ölçtüğü sayıyla aynı). Yerel koşumda ffmpeg PATH'te
olduğu için 0 atlandı. **Tam süit koşulmadı** (sözleşme yasakladı); T116 kendi
atlama sayısını **ölçmedi**. Kabul kriteri ölçülerinden `PlanCalculatorTests`in
üçü CI'da **koşar**; `QualityMeterTests`in beş kare kilidi ölçüsü `[FfmpegFact]`
ve CI'da **atlanır**.

Izgaranın 16 koşumu test değil, `bench` ikilileri elle koşuldu. Izgara 16 hücre
ama `bench` **17 kez** koştu — `sdr8-yeni-kilitli` sessizce boş VMAF döndürdüğü
için tekrarlandı, tabloya giren tekrarın sayısıdır; ayrıca çıpa için 3, süpürme
için 3 koşum daha var. Süre sayılarına **makine paylaşımlıydı** damgası basıldı;
kalite ve boyut sayılarına basılmadı.

Not: sözleşmenin kısıtı "beş ajan" diyor (`:123`); bu turda makinede **dokuz**
ajan koştuğu gözlendi, süre damgalarındaki sayı budur. Kısıt metnine
dokunulmadı.
