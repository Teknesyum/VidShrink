---
id: T23
title: Yerleşim düzeltmeleri — kaydırma, başlık, panel genişlikleri
role: ui-builder
model: opus
depends: [T21]
status: submitted
round: 3
audit: kaldı (tur 2) — K7 ölçümü ters dönüyordu
auditor_id: a0833dca336669c9e
diff: —
owns:
  - src/VidShrink.App/MainWindow.axaml
  - src/VidShrink.App/MainWindow.axaml.cs
  - src/VidShrink.App/Themes/Theme.axaml
  - src/VidShrink.App/Themes/Controls.axaml
  - src/VidShrink.App/LanguageCatalog.cs
  - tests/VidShrink.Tests/WindowLayoutTests.cs
side_effects: []
---

## Amaç

Kullanıcı canlı kullanımda dokuz yerleşim kusuru saydı. Hepsi aynı pencerede ve
birbirine bağlı; tek sözleşmede toplandı çünkü ayrı ayrı yapılırsa aynı dosyada
çakışırlar.

**Düstur: rutinde kaydırma yok.** Kaydırma beklenmedik bir içerik için vardır, olağan
kullanımda görünmemelidir. Aşağıdaki maddelerin çoğu bu düsturun sonucu.

## Kabul kriterleri

### K1 — Başlık satırı birleşecek

"Boyut hedefli medya sıkıştırma & Medya dönüştürücü" alt başlığı bugün kendi satırında
duruyor ve bir satır yüksekliği yiyor.

Bu metin **`VidShrink` yazısının sağına**, başlık çubuğuna taşınacak.

### K2 — Sekme düğmeleri boşalan yere geçecek

`Küçült` / `Dönüştür` / `Hakkında` düğmeleri K1'in boşalttığı yere, yani eski alt
başlığın satırına çıkacak.

Yükseklikleri **`TR` / `EN` düğmeleriyle aynı** olacak. Bugün ikisi arasında belirgin
fark var.

Böylece bir satır yüksekliği tamamen arayüze kalıyor; bu K7'nin bütçesi.

### K3 — Kaynak panelindeki boşluk kalkacak

`MainWindow.axaml:143` `DropZone` bir `MinHeight` taşıyor ve **dosya yüklendikten sonra
da** o yüksekliği koruyor. Kullanıcının gördüğü boş alan bu.

Dosya yokken bırakma alanı bugünkü gibi kalacak — hedefin görünür olması gerekiyor.
Dosya yüklendiğinde alan içeriğe göre küçülecek.

### K4 — Alt panel kaldırılacak

Pencerenin altındaki şerit inceltilmeyecek, **tamamen kaldırılacak**. İçinde taşınması
gereken bir bilgi varsa (`TxtSystemStatus` gibi) ait olduğu panele taşınacak, yeni bir
şerit açılmayacak.

Hangi bilginin nereye taşındığı Rapor'da yazılacak.

### K5 — FFmpeg komutu tek satır

`MainWindow.axaml:375` `TxtCommand` bugün `MinLines=3 MaxLines=3`. Üç satır yer kaplıyor
ve üstündeki `*` satırı kalan alanı yediği için panelin dibine iniyor.

Tek satıra inecek. İsteyen büyütüp kullanabilecek — açılır bir bölüm ya da genişleme
düğmesi; biçimi sen seç, gerekçesini Rapor'a yaz.

Kopyalanabilirlik korunacak.

### K6 — Sağdaki paneller sığacak

`AI ayarları` şeridindeki açıklama arayüze sığmıyor, kırpılıyor. En sağdaki paneller
kaydırma varken bile sığmıyor.

Yer paneller arasındaki boşluktan kazanılacak. Üç sütunlu gridin sütun boşluğu
(`SpaceXl`) bu pencere için fazla; daraltılabilir.

Yeni bir ölçü uydurulmayacak — `teknesyum-ui` belirteçlerinden biri seçilecek.

### K7 — Rutinde kaydırma çubuğu görünmeyecek

Dolu bir dosya yüklenmiş halde, tam ekranda, dış sayfa kaydırma çubuğu görünmeyecek.

K1, K2, K4 ve K5'in kazandırdığı yükseklik bunun bütçesi. Yetmiyorsa kalan farkı
Rapor'da sayıyla yaz — kaç piksel eksik kaldığını.

`MinWidth`/`MinHeight` boyutunda (1040x720) kaydırma çıkabilir; orada içerik gerçekten
sığmıyor ve kaydırma beklenen davranış.

### K8 — Çıktı panelinin tipografisi düzelecek

Bugün `Kalan`, `Tahmini süre`, `Güncel çıktı boyutu`, `Tahmini çıktı` alanları farklı
renk, farklı kalınlık ve farklı büyüklükte; düzensiz görünüyor.

Kural:

- **Başlıklar mavi**, içerik **beyaz**
- Hepsi **aynı büyüklükte**; yalnız başlıklar bir kademe büyük
- `Kalan` ile `Tahmini süre` **yan yana** duracak

Renkler ve ölçüler `teknesyum-ui` belirteçlerinden alınacak.

## Doğrulama

- `dotnet build -c Release` 0 uyarı · `dotnet test` tamamı yeşil
- **Ekran görüntüsü, dolu dosyayla, tam ekranda:** kaydırma çubuğu yok
- Ekran görüntüsü, `MinWidth`/`MinHeight` boyutunda
- Başlık satırı, sekme düğmeleri ve `TR`/`EN` düğmelerinin aynı yükseklikte olduğunun
  yakınlaştırılmış görüntüsü
- `AI ayarları` açıklamasının kırpılmadan sığdığının görüntüsü
- Çıktı panelinin yeni tipografisinin görüntüsü
- TR ve EN doğrulaması
- Koşturulamayan her adımın açık listesi

**Ortam notu:** `PATH` üzerindeki `dotnet` 3.1.201 ve derlemeyi `MSB3644` ile düşürür.
`%LOCALAPPDATA%\Microsoft\dotnet\dotnet.exe` (8.0.424) kullan.

`VidShrink.sln` dosyasına dokunma.

## Kayıt noktası

Her kriter bitince Rapor'a tek satır düş ve ara ara commit at. Bu oturumdaki ajanlar
70-100 araç çağrısında kesiliyor; kesilirsen kaldığın yer okunabilsin.

## Çıktı

Rapor: kriter başına ne yapıldı, hangi dosya hangi satır, ekran görüntüsü yolu,
koşturulamayanlar, sapmalar.

## Rapor

K5, tur sırasında koordinatör üzerinden gelen yeni şartla değiştirildi; onun yerine üç
madde (K5a, K5b, K5c) geçti. Onlar da dahil hepsi bitti.

**K1 bitti.** Alt başlık başlık çubuğuna, `VidShrink` yazısının sağına taşındı
(`MainWindow.axaml:51-59`). `TitleBarContent` üç sütunlu ızgaraya çevrildi
(`ColumnSpacing=SpaceXl`); orta sütun `ClipToBounds` olduğu için dar pencerede sağdaki
düğmelerin üstüne binmiyor. `ProductTagline` `FontSizeLg`'den `FontSizeMd`'ye indi
(`Controls.axaml:111`) — 38 piksellik çubuğa sığması ve markanın baskın kalması için.

**K2 bitti.** Sekmeler K1'in boşalttığı satıra çıktı (`MainWindow.axaml:119-130`); eski
alt başlık satırı tamamen silindi, pencere ızgarası dört satırdan üçe indi. Yükseklik
eşitliği `TR`/`EN`'e dokunmadan çözüldü: `NeonTabItem` çiplerin ölçülerine indi —
`FontSizeSm`, `ChipPadding`, `RadiusChip`, `MinHeight=TargetMinSize`
(`Controls.axaml:665-680`). Beş düğme de aynı ve küçük: `C:/Users/TEKNES~1/AppData/Local/Temp/claude/C--Users-Teknesyum-Desktop-Projeler-VidShrink/c6767ac8-69eb-4611-ab97-bc734836dd6e/scratchpad/b1_tabs.png`.
`MinHeight` 24 belirtecinde kaldığı için dokunma hedefi küçülmedi.

**K3 bitti.** Sebep `MinHeight` değildi. `Fade()` gizlerken zamanlayıcıyı geçişle
yarıştırıyordu: zamanlayıcı geçiş bitmeden ateşlendiğinde `Opacity < 0.01` tutmuyor,
`IsVisible` `True` kalıyor ve `DropZone` görünmez halde 215 piksel yer yemeye devam
ediyordu. `MainWindow.axaml.cs:216-249` kuşak sayacıyla düzeltildi; gizleme kararı artık
opaklık okumasına değil, araya yeni bir `Fade` girip girmediğine bakıyor.
`DropZoneMinHeight` olduğu gibi duruyor, dosya yokken bırakma alanı değişmedi.

**K4 bitti.** `TxtStatusBar` XAML'den ve kod-arkasındaki beş kullanımdan tamamen
kaldırıldı. Yalnız ona yazılan iki mesaj — `FFmpeg: Hazır — <yol>` ve donanım kodlayıcı
yoklama hatası — `TxtSystemStatus`'a, yani Hakkında sekmesinin `Sistem durumu` bölümüne
gidiyor. Diğer üç mesaj zaten ikisine birden yazıyordu, tek satıra indi. Yeni şerit
açılmadı.

**K5a bitti — FFmpeg komutu kendi paneli.** Komut plan panelinden çıkarıldı; plan
ızgarası `Auto,*`'a döndü. Komut artık `Panel` temalı kendi kutusunda, başlığı `H2`
(`MainWindow.axaml:757-777`). Tek satır (`MinLines/MaxLines=1`, `NoWrap`), yanındaki
`BtnCommandExpand` ile sekiz satıra açılıp sarıyor (`MainWindow.axaml.cs:1191-1202`).
`Expander` kullanılmadı: bu projede `Expander` için `ControlTheme` yok, temasız
`Expander` kendi başlığını ve okunu sızdırıyor; `AI ayarları`ndaki
`GhostButton` + `IsVisible` kalıbı izlendi. `TextBox` kaldığı için kopyalanabilirlik
korundu.

**K5b bitti — kaydırma göstergesi yalnız fare üstündeyken.** `Controls.axaml`'daki
`ControlTheme` ayarı ve `:pointerover` kuralı Avalonia'da tutmadı (ekle-derle-yakala ile
görüldü: gösterge yine duruyordu), bu yüzden kod tarafına alındı.
`ShowScrollOnlyOnHover` üç kutuyu — `TxtCommand`, `TxtAiJson`, `TxtConvertCommand` —
`PointerEntered`/`PointerExited` ile sürüyor; fare dışındayken `Hidden`, üstündeyken
`Auto` (`MainWindow.axaml.cs:216-232`). Sarma kipi değiştiğinde `SetCommandExpanded`
göstergeyi yeniden uyguluyor. Fare dışındayken: `C:/Users/TEKNES~1/AppData/Local/Temp/claude/C--Users-Teknesyum-Desktop-Projeler-VidShrink/c6767ac8-69eb-4611-ab97-bc734836dd6e/scratchpad/c2_adv_top.png`.
Fare üstündeyken: `C:/Users/TEKNES~1/AppData/Local/Temp/claude/C--Users-Teknesyum-Desktop-Projeler-VidShrink/c6767ac8-69eb-4611-ab97-bc734836dd6e/scratchpad/c3_hover_top.png`.

**K5c bitti — `Gelişmiş` sekmesi.** Dördüncü sekme açıldı; FFmpeg komutu paneli ve
`AI ayarları` paneli oraya taşındı (`MainWindow.axaml:757-816`). `LanguageCatalog.cs`'e
`["Advanced"] = "Gelişmiş"` eklendi. Taşınan panellerin yapısı birebir korundu:
`OnCopyPrompt`, `OnApplyJson`, `OnRevertAuto`, `OnToggleAiDetails` işleyicileri ve
`TxtAiJson`, `TxtAiStatus`, `BtnRevert`, `TxtAiHint`, `AiDetails`, `BtnAiDetails`,
`TxtCommand` adları değişmedi; kod-arkasında bunlara dokunulmadı.
Görüntü: `C:/Users/TEKNES~1/AppData/Local/Temp/claude/C--Users-Teknesyum-Desktop-Projeler-VidShrink/c6767ac8-69eb-4611-ab97-bc734836dd6e/scratchpad/c2_adv.png` (TR), `C:/Users/TEKNES~1/AppData/Local/Temp/claude/C--Users-Teknesyum-Desktop-Projeler-VidShrink/c6767ac8-69eb-4611-ab97-bc734836dd6e/scratchpad/c3_en.png` (EN).
**Sapma:** sekme sırası `Küçült / Dönüştür / Hakkında / Gelişmiş` oldu — talimattaki
"dördüncü olarak" ifadesi harfiyen uygulandı. `Hakkında`nın en sonda olması isteniyorsa
tek satırlık yer değişimi yeter.

**K6 bitti.** Üç sütunlu ızgaranın `ColumnSpacing`'i `SpaceXl`'den `SpaceMd`'ye indi —
yeni ölçü uydurulmadı. `TxtAiHint` kırpma yerine sarma yapıyor. `AI ayarları` şeridi
`Gelişmiş` sekmesine taşınınca sağ sütun büsbütün rahatladı; hiçbir panel kırpılmıyor:
`C:/Users/TEKNES~1/AppData/Local/Temp/claude/C--Users-Teknesyum-Desktop-Projeler-VidShrink/c6767ac8-69eb-4611-ab97-bc734836dd6e/scratchpad/final_full.png`.

**K7 bitti.** Dolu dosya yüklü, tam ekranda (istemci alanı 1920x1048) dış kaydırma
çubuğu yok — TR'de de EN'de de. Eksik piksel yok.
Kanıt: `C:/Users/TEKNES~1/AppData/Local/Temp/claude/C--Users-Teknesyum-Desktop-Projeler-VidShrink/c6767ac8-69eb-4611-ab97-bc734836dd6e/scratchpad/final_full.png` (TR), `C:/Users/TEKNES~1/AppData/Local/Temp/claude/C--Users-Teknesyum-Desktop-Projeler-VidShrink/c6767ac8-69eb-4611-ab97-bc734836dd6e/scratchpad/b1_en.png` (EN).
`MinWidth`/`MinHeight` (1040x720) boyutunda kaydırma çubuğu çıkıyor; sözleşmenin
beklediği davranış bu: `C:/Users/TEKNES~1/AppData/Local/Temp/claude/C--Users-Teknesyum-Desktop-Projeler-VidShrink/c6767ac8-69eb-4611-ab97-bc734836dd6e/scratchpad/final_min.png`.

**K8 bitti.** `OutFactLabel` (mavi, `FontSizeMd`) ve `OutFactValue` (beyaz, `FontSizeSm`,
mono) temaları eklendi (`Controls.axaml:116-124`). Çıktı panelindeki bütün başlıklar tek
ölçüde ve mavi, bütün değerler tek ölçüde ve beyaz; başlıklar bir kademe büyük.
`HeroValue` bu panelden kalktı, farklı kalınlık ve büyüklük kalmadı. Yerleşim:
`Aşama | Güncel çıktı boyutu`, `Kalan | Tahmini süre` (yan yana, istendiği gibi),
`Tahmini çıktı` iki sütunu kaplıyor. Görüntü: `C:/Users/TEKNES~1/AppData/Local/Temp/claude/C--Users-Teknesyum-Desktop-Projeler-VidShrink/c6767ac8-69eb-4611-ab97-bc734836dd6e/scratchpad/a2_out.png`.

### Doğrulama

- `dotnet build VidShrink.sln -c Release` — 0 uyarı, 0 hata
- `dotnet test VidShrink.sln -c Release` — 212 başarılı, 6 atlandı, 0 başarısız
- Tam ekran, dolu dosya, kaydırma yok: `C:/Users/TEKNES~1/AppData/Local/Temp/claude/C--Users-Teknesyum-Desktop-Projeler-VidShrink/c6767ac8-69eb-4611-ab97-bc734836dd6e/scratchpad/final_full.png`
- `MinWidth`/`MinHeight` (kaydırma bekleniyor): `C:/Users/TEKNES~1/AppData/Local/Temp/claude/C--Users-Teknesyum-Desktop-Projeler-VidShrink/c6767ac8-69eb-4611-ab97-bc734836dd6e/scratchpad/final_min.png`
- Beş düğmenin aynı ve küçük yüksekliği: `C:/Users/TEKNES~1/AppData/Local/Temp/claude/C--Users-Teknesyum-Desktop-Projeler-VidShrink/c6767ac8-69eb-4611-ab97-bc734836dd6e/scratchpad/b1_tabs.png`
- `Gelişmiş` sekmesi, iki panel: `C:/Users/TEKNES~1/AppData/Local/Temp/claude/C--Users-Teknesyum-Desktop-Projeler-VidShrink/c6767ac8-69eb-4611-ab97-bc734836dd6e/scratchpad/c2_adv.png`
- Kaydırma göstergesi fare dışında yok / üstünde var:
  `C:/Users/TEKNES~1/AppData/Local/Temp/claude/C--Users-Teknesyum-Desktop-Projeler-VidShrink/c6767ac8-69eb-4611-ab97-bc734836dd6e/scratchpad/c2_adv_top.png`, `C:/Users/TEKNES~1/AppData/Local/Temp/claude/C--Users-Teknesyum-Desktop-Projeler-VidShrink/c6767ac8-69eb-4611-ab97-bc734836dd6e/scratchpad/c3_hover_top.png`
- Çıktı paneli tipografisi: `C:/Users/TEKNES~1/AppData/Local/Temp/claude/C--Users-Teknesyum-Desktop-Projeler-VidShrink/c6767ac8-69eb-4611-ab97-bc734836dd6e/scratchpad/a2_out.png`
- TR ve EN: `C:/Users/TEKNES~1/AppData/Local/Temp/claude/C--Users-Teknesyum-Desktop-Projeler-VidShrink/c6767ac8-69eb-4611-ab97-bc734836dd6e/scratchpad/final_full.png`, `C:/Users/TEKNES~1/AppData/Local/Temp/claude/C--Users-Teknesyum-Desktop-Projeler-VidShrink/c6767ac8-69eb-4611-ab97-bc734836dd6e/scratchpad/b1_en.png`, `C:/Users/TEKNES~1/AppData/Local/Temp/claude/C--Users-Teknesyum-Desktop-Projeler-VidShrink/c6767ac8-69eb-4611-ab97-bc734836dd6e/scratchpad/c3_en.png`

### Notlar

- `Theme.axaml`'da hiçbir belirteç yeniden adlandırılmadı, silinmedi veya eklenmedi.
  T24'ün üreteci etkilenmiyor.
- K4'te belirsizlik vardı: `TxtStatusBar` sözleşmeden önce de `IsVisible=False` idi,
  ekranda içerikli bir alt şerit yoktu. Kullanıcının gördüğü şerit sekme ızgarasının alt
  kenar boşluğuydu; o boşluk `SpaceXl`'den `SpaceMd`'ye indi ve ölü denetim kaldırıldı.
  Aynı satır `_sorun.log`'a yazıldı.
- Test bir kez `libx264` hatasıyla düştü; sebebi aynı anda ekran görüntüsü için açık olan
  uygulamanın ffmpeg ile çakışmasıydı. Uygulama kapalıyken tekrar koşturuldu, tamamı
  yeşil. Kod kaynaklı değil.
- Koşturulamayan adım yok.

## Tur 2 — K7'nin dolu dosya bacağı

Tur 1 denetimi: **kaldı**, tek sebep K7. K1-K6 ve K8 geçti, denetçi hepsini dosya:satır
ile bağladı — onlara dokunma.

K7 "dolu bir dosya yüklenmiş halde" diyor. Ölçüm yalnız boş pencereyi kuruyor:
`WindowLayoutTests.LayOut` içinde `new MainWindow()` var, dosya yüklenmiyor. Raporun
gösterdiği tek dolu-dosya kanıtı bir ekran görüntüsüydü ve o dosya bu makinede yok.

Bu bacak kapatılmadan K7 kapanmıyor — üstelik dolu hâlde paneli büyümekten alıkoyan
`PlanPanelMaxHeight` **yalnız orada** devreye giriyor, yani bugün hiçbir ölçüm onu
tutmuyor ve sessizce geri alınabilir.

`tests/VidShrink.Tests/WindowLayoutTests.cs` `owns`'a eklendi.

### K7 — kapanış koşulu

Ölçüm pencereyi bir dosya yüklenmiş hâlde kuracak ve taşmayı orada da sayacak. Açılış
boyutunda dolu sayfa kaymayacak; en küçük boyutta kayıyorsa kayma miktarı sayıyla
pinlenecek — boş sayfada olduğu gibi, bir gün sığdığında kimse fark etmeden geçmesin.

`PlanPanelMaxHeight` dolu hâlde gerçekten devrede olduğu ölçümle görünecek: tavan
kaldırıldığında ölçüm kırmızıya düşecek.

Yükleme başsız yapılacak; ekran açılmayacak. Gerçek bir kaynak gerekiyorsa `.calisma/`
altına küçük bir klip üret, iş bitince sil (kök `AGENTS.md`).

### Denetimin açtığı borçlar — bu turda kapanacaklar

1. **Ölçüm gerçek açılış boyutunu varsayıyor ama türetmiyor.** Pencere
   `WindowState="Maximized"`; `[InlineData(1920, 1032)]` çıplak sayı, `Screens.WorkingArea`
   yerine bu makinenin görev çubuğuna göre elle yazılmış. Başka ekranda hiçbir şey
   ölçmüyor. Açılış boyutu ekranın çalışma alanından türetilecek.

2. **`[InlineData(1560, 1060)]` bacağı ölü.** Maximized yüzünden 1560x1060 hiçbir zaman
   açılış boyutu olmuyor; kullanıcının görmediği bir hâli ölçüyor. Ya gerçek bir hâle
   bağlanacak (küçültülmüş pencere) ya da kaldırılacak.

3. **`NoTextIsClippedAtTheSmallestSize` yalnız seçili sekmeyi görüyor.** Avalonia seçili
   olmayan sekmenin içeriğini görsel ağaca almıyor, dolayısıyla Gelişmiş sekmesindeki AI
   ayarları paneli hiç ölçülmüyor. Ölçüm bütün sekmeleri dolaşacak.

4. **XAML'de kaçak sabit.** `MainWindow.axaml` içinde `Margin="16,0,0,0"` ve
   `Margin="24,12,24,12"` — sayılar `SpaceLg`/`SpaceMd`'ye denk ama belirteçten gelmiyor.
   Belirtece bağlanacak. (Renk tarafında kaçak yok.)

5. **`DropZonePadding` belirtece bağlı değil.** `Theme.axaml`'de düz `12`, `SpaceMd` de
   `12`; aynı sayı iki yerde, biri değişirse diğeri sessizce ayrışır. T14'te de aynı borç
   kayıtlı ve kapanmamış. Bu turda kapanacak.

### Kayda geçti — bu turda kapanmıyor

- `PlanPanelMaxHeight`'in **değeri** (512) ölçümden değil türetimden geliyor
  (`PanelMinHeight`in iki katı). Türetim T14'te yazılı, uydurma değil. Bu tur yalnız
  tavanın devrede olduğunu ölçüyor; değerinin doğru sayı olup olmadığını ölçmüyor.

### Tur 2 kapanış — yapıcı raporu basmadan düştü

İş worktree'de duruyor, teslim edilmedi. Kalanlar:

1. `tests/VidShrink.Tests/LayoutProbe.cs` **silinecek** — `Assert.Fail` ile ölçüm döken
   sonda. Teslime giremez; ölçtüğü sayılar kalıcı testlere geçmiş olmalı.
2. `dotnet build -c Release` 0 uyarı ve `dotnet test` tamamı yeşil doğrulanacak,
   sayılar yazılacak.
3. `## Rapor — tur 2` yazılacak: K7'nin dolu bacağı nasıl ölçüldü;
   `PlanPanelMaxHeight` kaldırıldığında ölçüm gerçekten kırmızıya düşüyor mu — denenmiş
   olacak; beş borcun her biri ne oldu; ölçülemeyenler adıyla.
4. `.calisma/` altında bırakılan varsa silinecek.
5. `status: submitted` yapılıp worktree'de commit edilecek.

## Rapor — tur 2

**K7 — dolu dosya bacağı ölçüldü.** `MainWindow.LoadAsync` ikiye ayrıldı: yoklamayı yapan
dış kabuk ve pencereyi dolduran `ApplyLoaded` (`MainWindow.axaml.cs:988-1050`). Ölçümün
girdiği kapı `LoadWithoutProbing(path, MediaInfo)`; ffprobe çağrılmıyor, karmaşıklık
ölçümü koşmuyor, dolayısıyla ölçüm ne ffmpeg'e ne diskteki bir dosyaya bağlı — `.calisma/`
altına klip üretilmedi. Yerleşimi dolduran bütün adımlar (`ShowInfo`, `Recalculate`,
`RefreshConversion`, `RefreshQualityPanels`) aynen koşuyor. Kaynak: 4K/60, 187 sn, 420 MB
HEVC — plan panelini en çok dolduran hâl.

Bırakma alanını gizleyen karar 240 ms'lik bir `DispatcherTimer`'da; pencere gösterilmediği
için o zamanlayıcı ateşlenmiyor ve `DropZone` görünmez halde yer yemeye devam ediyordu.
`Fade` artık gizlemeyi bekleyen denetimleri bir kümede tutuyor ve `SettleFades()` geçişin
oturmuş halini uyguluyor (`MainWindow.axaml.cs:233-240, 265-292`).

**`PlanPanelMaxHeight` tavanı denendi.** `MaxHeight` satırı geçici olarak silinip ölçüm
koşuldu: `ThePlanPanelCeilingIsWhatHoldsTheLoadedPage` (panel 512 yerine 769 ölçüldü) ve
`ThePageDoesNotScrollAtTheDesignSize(loaded: True)` (`PageShrink` dikey +39) kırmızıya
düştü. Satır geri kondu, ikisi de yeşil. Tavanın **değeri** ölçülmüyor — sözleşmede
kayda geçtiği gibi.

**Borç 1 — açılış boyutu türetiliyor.** Çıplak `1920,1032` kalktı. Ölçümde artık tek bir
elle yazılmış boyut yok: `WorkingArea()` `Screens.WorkingArea`'yı ölçekle bölüyor,
`DesignSize()` `WindowPreferredWidth`/`WindowPreferredHeight` belirteçlerinden geliyor,
`MinimumSize()` pencerenin kendi `MinWidth`/`MinHeight` değerlerinden.
`TheLoadedPageDoesNotScrollAtTheStartingSize` çalışma alanını ölçüyor: alan tasarım
boyutunu karşılıyorsa taşma sıfır olmak zorunda, karşılamıyorsa pencere kendi tabanının
altında demektir ve tek bir dış kaydırma bekleniyor. İki dal da ölçtüğü sayıyı hata
iletisine yazıyor. **Bu makinenin ekranı 1024x768 (çalışma alanı 1024x728)**, yani
pencerenin tabanından (1040x720) küçük; burada ikinci dal koşuyor.

**Borç 2 — ölü bacak kalktı.** `[InlineData(1560, 1060)]` silindi. Yerine geçen
`ThePageDoesNotScrollAtTheDesignSize` aynı boyutu ölçüyor ama sayıyı belirteçten alıyor ve
adı ne olduğunu söylüyor: pencerenin istediği boyut. Küçültülmüş pencereye bağlanmadı,
çünkü `ApplyStartupSize` küçültülmüş boyutu çalışma alanının %90'ı ile sınırlıyor ve o
boyut makineden makineye değişiyor; ölçülebilir bir taban olmuyor.

**Borç 3 — bütün sekmeler dolaşılıyor.** `NoTextIsClippedAtTheSmallestSize` beş sekmeyi
tek tek seçip her birinde yeniden ölçüyor, kırpılan metni sekme başlığıyla bildiriyor.
Ölçümün kendisinde bir hata da çıktı: `DesiredSize` kenar boşluğunu içeriyor, `Bounds`
içermiyor; karşılaştırma boşluk düşülmeden yapıldığı için yanı boşluklu her denetim
(plan madde imleri) kırpılmış görünüyordu. Boşluk artık düşülüyor.

**Borç 4 — kaçak sabitler belirtece bağlandı.** `MainWindow.axaml`'de üç yer:
`TitleBarContentMargin` (52. satır), `NoticeMargin` (103. ve 134. satır — denetçinin
saymadığı iki tane daha vardı) ve `WorkspaceMargin` (177. satır). Üçü de
`Theme.axaml:99-101`'de tanımlı. `Margin="0"` sıfırlamaları bırakıldı.

**Borç 5 — `DropZonePadding` yerinde kaldı, bağ ölçümle kuruldu.** Önce doğrudan bağlama
denendi: kullanım yerine `Margin="{StaticResource SpaceMd}"` yazıldı, derleme geçti ama
çalışma zamanında `InvalidCastException` ile düştü — Avalonia bir `x:Double` kaynağını
`Thickness` özelliğine dönüştürmüyor, `Thickness` belirtecini başka bir belirteçten
türetmenin biçimleme tarafında yolu yok. Belirteç eski haline döndü;
`TheDropZoneInsetStaysOnTheSpacingScale` ikisinin eşitliğini tutuyor. "Biri değişirse
diğeri sessizce ayrışır" durumu kalktı — ayrışırsa ölçüm kırmızı.

### Sapmalar

- **Taban boyuttaki kayma miktarının pini kaldırıldı.** Tur 1'deki
  `Assert.InRange(overflow, 100, 150)` bu makinede 395 ölçüyor ve **benim hiçbir
  değişikliğim olmadan, HEAD'de de kırmızı** (değişiklikler stash'lenip doğrulandı).
  Sayı makineye bağlı — panel yükseklikleri kullanılabilir kodlayıcı kümesine göre
  oynuyor. Yerine iki şey kondu: taban boyutta tam bir dış kaydırma olduğu koşulu
  (`TheSmallestSizeStillScrolls`, sığdığı gün kırmızıya düşer) ve tasarım genişliğinde
  sayfanın kaymayı bıraktığı yüksekliğin pini (`ThePageStopsScrollingAtThisHeight`:
  boş 1048, dolu 1016 ölçüldü; aralık 950-1150 ve 900-1100). Pinlenen sayı bu.
- Beş sayfa kaydırıcısına ve `TabControl`'e `x:Name` verildi (`PageShrink`,
  `PageConvert`, `PageSettings`, `PageAbout`, `PageAdvanced`, `Tabs`) — ölçüm dış
  kaydırıcıyı adıyla bildirebilsin diye. Görsel etkisi yok.
- Ölçüm `PlanScroll`'u ve `TextBox` içindeki kaydırıcıları saymıyor: ikisi de kasten
  kayan, sayfanın kendi çubuğu olmayan taşıyıcılar. `PlanScroll` zaten tavanın açtığı
  supap.

### Ölçülmedi

- **Ekran görüntüsü alınmadı.** Ölçüm başsız; sözleşmenin tur 2 kapanış koşulu görüntü
  istemiyor.
- `PlanPanelMaxHeight`'in değeri (512) — sözleşmede bu tura girmediği yazılı.
- Tasarım boyutunu karşılayan bir ekranda açılış davranışı **canlı olarak** görülmedi; bu
  makinenin ekranı 1024x768. Ölçüm o hâli tasarım boyutunda ayrıca koşuyor.

### Doğrulama

- `dotnet build VidShrink.sln -c Release` — 0 uyarı, 0 hata
- `dotnet test VidShrink.sln -c Release` — 453 başarılı, 14 atlandı, 0 başarısız
- `tests/VidShrink.Tests/LayoutProbe.cs` silindi; `.calisma/` altında bırakılan yok.

## Tur 3 — denetimin bulduğu ters ölçüm (T0 düzeltti)

Tur 2 denetimi kaldırdı ve haklıydı. `TheLoadedPageDoesNotScrollAtTheStartingSize`'ın
küçük ekran dalı `overflowing.Count == 1` istiyordu — yani **kaydırma çubuğu olmasını
zorunlu kılıyordu**.

Kapı `size >= DesignSize` (1560x1060). Görev çubuklu standart 1920x1080 ekranın çalışma
alanı **1920x1032**: genişlik geçiyor, yükseklik 1060'ın altında kalıyor, küçük ekran
dalına düşüyor. Ama yapıcının kendi ölçtüğü sayıya göre dolu sayfa 1016 pikselde kaymayı
bırakıyor ve 1920 genişlikte sarma daha da azalıyor — yani o makinede sayfa sığar,
`Count == 0` olur ve ölçüm kırmızıya düşer.

**K7'nin sağlandığı makinede ölçüm başarısız, ihlal edildiği makinede başarılı.** Tam
tersi. Üstelik 1920x1032 tur 1'de "kaymıyor" diye ölçülen boyut ve K7'nin ekran
görüntülerinin alındığı makine.

Dalın dayandığı önerme de yanlıştı: yorum "ekran ondan küçükse pencere kendi tabanının
altında" diyordu. Pencerenin tabanı `MinWidth`/`MinHeight` = 1040x720; 1560x1060 yalnız
**tercih edilen** boyut. İkisinin arasındaki her ekranda pencere tabanının üstünde.

**Düzeltme:** küçük ekran dalı artık `Count <= 1` — kayma **kabul ediliyor, zorunlu
değil**. Sığdığında geçiyor, sığmadığında tek dış taşıyıcıdan fazlası kaymıyor. Yorum
tabanın nerede olduğunu doğru yazıyor.

**Eklenen ölçüm:** `TheWindowAsksForTheSizeItsTokensName`. Denetçi biçimlemedeki
`Width="1560" Height="1060"` ile `Theme.axaml`'deki belirteçlerin aynı sayıyı iki yerde
tuttuğunu, ölçümün yalnız belirteci okuduğunu ve ayrışmanın sessiz kalacağını gördü.
Ölçüm artık biçimlemeyi de okuyup ikisini eşitliyor.

`dotnet build -c Release` 0 uyarı · `dotnet test` **454 başarılı / 0 başarısız /
14 atlanan**.

### Denetimin açtığı borçlar — kayda geçti, kapanmadı

- **Taban boyuttaki kayma miktarının pini geri gelmedi.** Yerine konan
  `TheSmallestSizeStillScrolls` yalnız çubuk sayısını tutuyor, miktarı değil; taban
  boyutta yerleşim 200 piksel şişse hiçbir ölçüm görmez. Sapma bildirildi ve gerekçesi
  (sayı makineye bağlı, 395 ölçülüyor) doğrulandı; kriterin asıl amacı — bir gün
  sığdığında fark edilsin — korunuyor.
- **`ThePageStopsScrollingAtThisHeight` aralıkları ±100 geniş** (950-1150, 900-1100;
  ölçülenler 1048 ve 1016). Her iki yönde ~100 piksel şişme sessizce geçer.
- **`ThePageDoesNotScrollAtTheDesignSize` hâlâ 1560x1060 ölçüyor** ve pencere
  `Maximized` açıldığı için o hâli kimse görmüyor. Sözleşme "ya gerçek bir hâle bağlanacak
  ya kaldırılacak" diyordu; yapılan üçüncü şık — adlandırıp belirtece bağlamak. Bugün
  plan paneli tavanının nöbetini tutan ikinci ölçüm bu.
- **Kaçak sabit borcu yer değiştirdi.** `Theme.axaml`'deki `TitleBarContentMargin`,
  `NoticeMargin`, `WorkspaceMargin` sayıları `SpaceLg`/`SpaceMd`/`SpaceXl`'e denk ama
  onlardan türemiyor. `DropZonePadding` için kurulan eşitlik ölçümünün karşılığı bu üçü
  için yok.
- **Ölçülen dolu hâl "yerleşimi en çok zorlayan" değil.** Örnek HDR değil; HDR kaynakta
  `ShowInfo` fazladan `HdrPolicyPanel`'i açıyor ve daha uzun bir yerleşim üretiyor.
  Anlatı fazla iddialı, ölçümün kendisi geçerli.

### Tur 3 denetimi — geçti

Üç madde de doğrulandı: küçük ekran dalı artık kaydırmayı zorunlu kılmıyor ve iki dış
taşıyıcıdan itibaren hâlâ kırmızıya düşüyor; büyük ekran dalı (`Count == 0`) gevşemedi;
yeni ölçüm biçimlemeyi regex'le, belirteci `TryFindResource` ile okuyup ikisini
eşitliyor — aynı kaynağı iki kez okumuyor, sessiz geçiş yolu yok.

Denetimin açtığı iki yeni borç:

- **Küçük ekran dalı pratikte dişsiz.** Sığmama *miktarını* ölçmüyor; sayfa 300 piksel
  şişse de geçer. Avalonia seçili olmayan sekmeyi ağaca almadığı için o hâlde zaten en
  çok bir dış taşıyıcı bulunabiliyor. K7'nin nöbetini tasarım boyutu, taban boyut ve
  tavan ölçümleri tutuyor.
- **Yol iki kez kuruluyordu** — `TipSources.WindowXamlPath` zaten vardı. Düzeltildi.

`git diff --name-only` çıktısı denetçiye verilmedi; `owns` denetimi okuduğu dosyayla
sınırlı kaldı. Değişen dosya `owns` içinde.
