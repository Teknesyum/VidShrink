---
id: T99
title: Bppf tabani kazanan yerlesimi ariyor bile degil
role: builder
model: opus
depends: [T89]
status: submitted
round: 1
verify: [dotnet test -c Release --filter "PlanCalculatorTests|ComplexityScanTests"]
owns: [src/VidShrink.Core/CodecModel.cs, src/VidShrink.Core/ComplexityProfile.cs, src/VidShrink.Core/PlanCalculator.cs, tests/VidShrink.Tests/PlanCalculatorTests.cs, tests/VidShrink.Tests/ExtremeCompressionTests.cs, docs/olcumler/bppf-tabani.md]
side_effects: []
---

## Bağlam

Şikâyet işinde biz **882x496**'ya iniyoruz, HandBrake **1280x720@60**'ta
kalıyor ve eş boyutta 8,79 puan (ortalama) önde bitiriyor.

Sebep arama uzayında: `CodecModel.FloorBppf` av1 için 0,020, donanımda
`HardwareFloorFactor = 1.25` ile çarpılıp **0,025** oluyor
(`CodecModel.cs:20, 58-67`). `ComplexityProfile.FloorBppf` bunu uyguluyor
(`ComplexityProfile.cs:182-183`) ve `PlanCalculator.cs:612` bu tabanın
altındaki yerleşimleri **eliyor**.

790k video bütçesinde:
- 720p60 → 0,0143 bppf → **elenir, hiç denenmez**
- 882x496 → 0,030 bppf → geçer, seçilir

**HandBrake'in kazanan dosyası 0,0116 bppf'te koşuyor.** Yani tabanımız,
rakibin kazandığı yerleşimi aramaya bile başlamadan dışlıyor. Taban
"anlamlı görüntü için gereken alt sınır" diye konmuş; rakip o sınırın
yarısında bizi yeniyor.

Bu, tabanın toptan kaldırılması gerektiği anlamına **gelmez**. Taban gerçek
bir şeyi koruyor: çok düşük bppf'te blok artefaktı görüntüyü yıkar. Soru,
sınırın **doğru yerde olup olmadığı** — ve bu sayı bugün ölçüye değil, kabule
dayanıyor.

## T95 beklenmeden açıldı — gerekçe

Bu sözleşme `depends: [T89, T95]` ile yazılmıştı; A/B düzeneği iki kez
yeniden başladı ve açığın ikinci büyük kalemi boşta bekledi. Bağımlılık
kaldırıldı, sebebi şu:

T95'in aleti üç kapı sağlıyor — renk uzayı hizası, akış sayısı eşitliği,
boyut karşılaştırılabilirliği. Üçü de **iki farklı araç** karşılaştırılırken
gerekli. K2 öyle bir ölçüm değil: aynı kaynak, aynı kodlayıcı, aynı hedef
boyut, tek değişken **yerleşim**. Renk uzayı beş satırda da aynı, akış
sayısı aynı, boyutu hedef mekanizması zaten eşitliyor.

T102 tam bu şekilde ölçtü (GOP, tek değişken) ve sonuç aletsiz de sağlam
çıktı. Aynı gerekçe burada geçerli.

**Karşılığında senden istenen:** T95'in kapılarını elle uygula. Beş koşumun
renk etiketlerinin ve akış sayılarının aynı olduğunu `ffprobe` ile doğrula
ve raporda tek satırla yaz. Aletsiz yapamadığın bir şey varsa **rapora yaz**,
sessizce atlama.

## Kabul kriteri

1. **Tabanın nereden geldiği yazılır.** 0,020 ve 1,25 hangi ölçümden çıktı?
   `docs/olcumler/` altında dayanağı var mı, yoksa kabul mü? Cevap
   `docs/olcumler/bppf-tabani.md`'nin ilk bölümü. Dayanak yoksa **"dayanak
   bulunamadı" yaz** — uydurma.

2. **Eş boyutta yerleşim taraması.** Şikâyet işinin hedef boyutunda en az
   **beş** yerleşim gerçekten kodlanır ve ölçülür: 720p60, 720p30, 882x496
   (bugünkü seçim), 960x540, 540p60. Her biri için VMAF-NEG ortalama,
   harmonik, p10 ve gerçekleşen boyut. **Kazananı ölçü söyler.**
   CHECK: beş satırlık tablo raporda.

3. **Taban ölçülen kazanana göre yeniden konur.** K2'de kazanan bugünkü
   seçimden farklıysa taban o yerleşimi **aday yapacak** şekilde düşürülür ve
   yeni değerin hangi ölçüme dayandığı yazılır. Bugünkü seçim kazanırsa
   taban **değişmez** ve "ölçüldü, taban doğru yerde" yazılır. Sabiti
   ölçüsüz oynatma.

4. **Alt sınır hâlâ bir şeyi korumalı.** Taban düşürülürse, düşürülen
   değerin altında görüntünün gerçekten yıkıldığı bir ölçümle gösterilir —
   yani yeni sınırın da bir dayanağı olur. Gösterilemiyorsa sınır neye göre
   konduğu yazılır ve "keyfi" damgası basılır.

5. **`HardwareFloorFactor = 1.25` yargılanır.** Donanım kodlayıcı gerçekten
   %25 daha fazla bppf mi istiyor? Aynı yerleşimde donanım ve yazılım
   kodlayıcı eş boyutta ölçülür ve fark yazılır. 1,25 ölçüye uymuyorsa
   düzeltilir.

6. **Kullanıcıya görünen gerekçe doğru kalır.** `PlanCalculator.cs:201` ve
   `:207` kullanıcıya taban gerekçesini metinle anlatıyor. Taban değişirse o
   cümleler de değişir; metin anahtardan gelir, gömülü dizge yazma.

7. **Ölçülen dağılımın tamamen dışında kalan üç sabit.** T89, hareket üsteli
   için 13 nokta ölçtü ve şunları rapor etti:
   `DefaultMotionExponent = 0,25` ve `MotionCutIsExpensiveAbove = 0,5` —
   **ikisi de ölçülen dağılımın tamamen altında**; `MotionExponentMax = 1,0`
   ölçülen 13 noktadan ikisini (1,296 ve 1,319) kırpıyor. Üçü de
   `tests/VidShrink.Tests/ExtremeCompressionTests.cs` içinde sabitlenmiş
   olduğu için T89 değiştiremedi.
   Her üçü **ölçülen dağılıma göre yeniden konur**, ya da neden ölçümün
   dışında bırakıldığı yazılır. "Test öyle diyor" gerekçe değildir — test
   davranışı sabitler, davranışı belirlemez.

8. **Mutasyon kanıtı.** Taban değeri, donanım çarpanı ve eleme koşulu
   bozulunca ölçü kırılmalı. İki sabiti karşılaştıran ölçü yazma, `Skip`
   yazma, sessiz erken dönüş yazma. ffmpeg gerektiren ölçü `[FfmpegFact]`.

## Sınırlar

- `owns` dışına yazma. `FfmpegArguments.cs` T98'de, `QualityMeter.cs`
  T97'de, `SceneMap.cs` T96'da, `tools/VidShrink.Ab` T95'te,
  `HdrResolver.cs` T94'te.
- `tools/VidShrink.Ab` henüz `main`de değil (T95 koşuyor). Kapıları elle
  uygula — yukarıdaki gerekçe bölümüne bak.
- **Makine paylaşımlı: altı ajan, dört ffmpeg.** K2'nin beş koşumu kendi
  içinde sıralı gitsin, ama başka ajanların yükünü bekleme. Kodlama
  komutlarında iş parçacığını **sabitle** (`-threads N`, x265'te `pools`);
  sabitlemezsen kodlayıcı boştaki çekirdeğe göre farklı bölümleme seçer ve
  çıktın koşumdan koşuma değişir. Süre sayısına "makine paylaşımlıydı"
  damgası bas; kalite ve boyut sayısına **basma**.
- Kod yorumu yazma.
- Tam süiti koşturma; yalnız kendi filtreni.

## Kayıt noktası

Sekiz kriterin sekizi bitti. Dal: `T99-bppf-tabani` (itildi).

## Çıktı

**Rapor:** `docs/olcumler/bppf-tabani.md` (9 bölüm, kriter sırasında).

**Değişen sabitler** (`src/VidShrink.Core/CodecModel.cs`):

| Sabit | Eski | Yeni | Dayanak |
|---|---:|---:|---|
| `FloorBppf` av1 tabanı | 0,020 | 0,0095 | Yazılım kolunun doygunluk noktası 0,00669 teslim bppf ÷ 0,7028 (rapor §4) |
| `HardwareFloorFactor` | 1,25 | 1,52 | p10 yıkım noktasının donanım/yazılım oranı (rapor §5) |

`hevc` (0,025) ve `h264` (0,035) tabanları ölçülmedi, değişmedi.

**Kriter kriter:**

- **K1** — Taban kabule dayanıyormuş. 0,020 · 0,025 · 0,035 · 1,25 için "dayanak
  bulunamadı"; hepsi `6fac0be`'de T4'ün "başlangıç değerleri" reçetesinden gelmiş.
  Ölçülen tek sabit `FloorComplexityAnchor = 0,1264`.
- **K2** — Beş yerleşim, aynı 790k, iki pencere. **Kazanan 1280x720@60**, altı ölçünün
  beşinde önde; bugünkü seçime (882x496@60) karşı 480 penceresinde +6,39 ortalama.
  T95 kapıları elle uygulandı (renk etiketleri ve akış sayısı beş çıktıda aynı).
- **K3** — Taban kazananı geçirecek şekilde düşürüldü. Uygulanan av1_nvenc tabanı
  0,01757 → 0,01015; kazanan 0,01429'da artık aday. **Ama plan hâlâ 882x496 seçiyor** —
  eleme kalktı, `LayoutScore` kaybettiriyor. Skor modeli sözleşmede yok, oynatılmadı.
- **K4** — Yıkım ölçüldü: zor pencerede sekiz noktalı süpürme. 0,00669'un altında
  p10 eğrisi düzleşiyor (yarılanma başına 1,24 puan, üstünde 17,18) çünkü karelerin
  %3,6'sı zaten sıfırda. Taban oraya kondu. "Keyfi" damgası **gerekmedi**; keyfi olan
  tek şey doygunluğu değil de yıkımın başlangıcını seçmemek — o bir politika kararı ve
  §4.1'de öyle yazıldı.
- **K5** — 1,25 yargılandı ve **düştü**. Dört okuma: eşit ortalama kalitede 1,70 (kolay
  pencere) ve 1,64/1,31/1,12 (zor pencere), p10 yıkım noktası oranı 1,52, eşit p10'da
  0,79–0,97. 1,25 hiçbirine oturmuyor. Seçilen 1,52 — tabanı da üreten alet.
- **K6** — Gerekçe ölçüsü taban değerinden bağımsız yazıldı, taban değişince
  düzenlenmeden yeşil kaldı. Boş doğrulama kapısı (`quoted >= 2`) ve geçerli kültür
  biçimlendirmesi var.
- **K7** — `DefaultMotionExponent` 0,25 → 0,871 (13 pencerenin medyanı),
  `MotionCutIsExpensiveAbove` 0,5 → 0,848, `MotionExponentMax` 1,0 → 1,4 (tavanın
  kendisi ölçülmedi, "keyfi" damgalı).
- **K8** — Mutasyon kanıtı, taban ve çarpan **iki yönde**: M4 (0,0095→0,020),
  M4b (→0,0065), M5 (1,52→1,0), M5b (→2,2), M6 (eleme koşulu silindi, 6 ölçü düşüyor).
  Değeri tutan kapı `TheFloorAdmitsTheLayoutThatWonTheMeasurementAndStillRejectsTheSaturatedOne`
  av1 tabanını ~[0,0074 – 0,0134] aralığına hapsediyor.

**Verify:** `dotnet test -c Release --filter "PlanCalculatorTests|ComplexityScanTests"`
→ 58 geçti, 0 kaldı. Mutasyon koşuları geniş filtreyle
(`+ExtremeCompressionTests`, 75 ölçü, bozulmamış ağaçta 72/0/3).

## T0'a bırakılanlar

1. **Skor modeli şikâyeti çözmüyor.** Taban artık kazananı elemiyor ama plan değişmedi;
   `PlanCalculator.LayoutScore` 882x496'yı seçmeye devam ediyor. Ölçüm 1280x720@60'ı
   6,39 puan önde buluyor. Ayrı sözleşme adayı (rapor §3.1).
2. **`UsableBitrateK` donanım kolunda tabandan daha sıkı.** 1280x720@60'ta 706k =
   0,01277 bppf, yeni taban 0,01015; bu yerleşimde taban atıl. HandBrake'in 0,0116'lık
   noktası bu yüzden hâlâ dışarıda (rapor §3.2).
3. **Ölçülmemiş yan etki:** çarpan yükselince `hevc_nvenc` tabanı da 0,02196 → 0,02671
   oldu; o taban ölçülmedi (rapor §5.3).
4. **`verify` filtresi eksik kapsıyor:** tabanla ilgili üç ölçü `ExtremeCompressionTests`
   içinde ve filtrede yok. Denetim geniş filtreyi koşsun (rapor §9).
5. **Silinemeyen birikinti:** `.calisma/T99/` (~200 MB mp4/csv) — silme izni verilmedi,
   gitignore'da, worktree kaldırılınca gidiyor.

- Kendi dalın, itilmiş. `main`e sen birleştirme.
