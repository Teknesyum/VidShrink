---
id: T3
title: Doluluk bandı ve alt-taşma tekrarı
role: builder
model: sonnet
depends: [T2b]
owns: [src/VidShrink.Core/PlanCalculator.cs, src/VidShrink.Core/EncodePlan.cs, src/VidShrink.Ffmpeg/EncodeRunner.cs, src/VidShrink.App/MainWindow.xaml, src/VidShrink.App/MainWindow.xaml.cs, src/VidShrink.App/LanguageCatalog.cs, tests/VidShrink.Tests/FillBandTests.cs]
side_effects: []
status: submitted
round: 1
agent_id: T3-builder
audit: —
auditor_id: —
diff: —
verification: —
---

## Amaç

Kullanıcı hedefin **hemen altında** durmak istiyor: 180 MB dediyse sonuç 175–180 arasında
olmalı, hiçbir koşulda 170'in altına düşmemeli, hiçbir koşulda 180'i geçmemeli.

Bugün motor CRF modunda "şeffaflık tavanına" ulaşınca bilerek hedefin çok altında kalıyor.
Bu davranış bir özellikti ve korunacak — ama artık varsayılan değil. Varsayılan modda artan
bütçe **boşa harcanmaz, kaliteye çevrilir**: CRF düşürülür, dosya banda çekilir.

## Kabul kriteri

1. `PlanOptions.FillPolicy { FillTarget, QualityCeiling }`, varsayılan `FillTarget`.
2. Bant hedefe göre ölçekli, tek bir yerde hesaplanır (`FillBand.For(double targetMb)`):

   | Hedef | Bant alt sınırı | Sert taban |
   |---|---|---|
   | ≥ 50 MB | hedef × 0,972 | hedef × 0,944 |
   | 10–50 MB | hedef × 0,95 | hedef × 0,90 |
   | < 10 MB | hedef × 0,92 | hedef × 0,85 |

   Üst sınır her zaman hedefin kendisidir. 180 MB için: bant 175,0–180,0, sert taban 169,9.
3. `FillTarget` modunda plan bandın altında kalıyorsa:
   - önce CRF, kalite yönünde adım adım düşürülür (kalibre profille tahmin edilerek),
   - codec'in CRF alt sınırına gelinip hâlâ bandın altındaysa iki geçiş VBR'a geçilir ve
     bitrate bant merkezine kurulur.
   Hangi yolun seçildiği `ReasonCode` olarak kaydedilir ve arayüzde Türkçe görünür.
4. `QualityCeiling` modu bugünkü davranıştır: tavana ulaşıldığında dosya küçük bırakılır.
5. `EncodeRunner` alt-taşma tekrarı: sonuç sert tabanın **altındaysa** bitrate
   `bantMerkezi / gerçek` oranıyla artırılıp yeniden denenir. Üst taşma davranışı ve
   `ToleranceOver = 1.0` sert tavanı **aynen korunur** — hedefin bir baytı bile aşılmaz.
   Toplam deneme sayısı bugünkü `MaxAttempts` içinde kalır; her denemenin yönü
   (alt/üst) LOG'a değil, `EncodeResult` üzerinden görünür olur.
6. Alt-taşma tekrarı yalnızca `FillTarget` modunda çalışır. `QualityCeiling` modunda
   küçük çıktı bir hata değildir.
7. Arayüz: Hedef panelinde iki seçenek — `Hedefi doldur` / `Kalite tavanında dur`.
   Her ikisi de `?` rozetiyle iki dilde açıklanır: doldurmak hedefe yakın bir dosya ve
   mümkün olan en iyi kaliteyi verir; tavanda durmak gereksiz büyümeyi engeller ama
   dosya belirgin biçimde küçük çıkabilir. `teknesyum-ui` tokenları kullanılır, renk veya
   ölçü uydurulmaz. `LanguageCatalog` anahtarları **iki yönde de** benzersiz olmalı.
8. Testler (`FillBandTests`): üç hedef sınıfı için bant sınırlarının doğru hesaplandığı;
   bandın altında kalan bir planın CRF düşürülerek banda çekildiği; CRF sınırına gelince
   iki geçişe geçildiği; sert tabanın altındaki bir sonucun tekrar denemeyi tetiklediği;
   hedefin üstündeki sonucun bugünkü gibi ele alındığı.
9. `dotnet build VidShrink.sln -c Release` 0 uyarı, `dotnet test VidShrink.sln` yeşil.

## Arayüzler

T2'den gelen: kalibre `ComplexityProfile`. Tahmin bandı kalibre edildiğinde daralıyor;
T2 bunu profil üzerinde bir özellik olarak yayınladıysa `PlanCalculator.Estimate` içindeki
sabit `band` değerini (`0.14` / `0.32`) o özellikten oku. T2 Çıktı'sında bunu sana bıraktıysa
özelliği sen ekleme — `ComplexityProfile.cs` T2'nin `owns`unda; ihtiyacın olan alan yoksa
Çıktı'na yaz, T0 halleder.

Bugünkü ilgili yerler:

- `PlanCalculator.cs:18` `CrfFitMargin = 0.94` — bandın üst sınırını bu sabit belirliyordu,
  artık `FillBand` belirleyecek. Sabiti kaldırmadan önce kullanıldığı her yeri gözden geçir
  (`RecoverLayoutAtCeiling`, `Correct`).
- `PlanCalculator.Correct(plan, actualMb, targetMb, durationSeconds)` — bugünkü düzeltme
  yalnızca aşağı yönlü. İki yönlü olacak.
- `EncodeRunner.cs:15` `ToleranceOver = 1.0`, `MaxAttempts = 3`.
- `RecoverLayoutAtCeiling` — tavan modunda kalan bütçeyi çözünürlüğe çeviriyor; `FillTarget`
  modunda bu davranış bandı doldurma mantığıyla çakışabilir, hangisinin önce geldiğine
  karar ver ve gerekçesini Çıktı'ya yaz.

## Bağlam

- Test dosyası: `C:\Users\Administrator\Videos\gothic2026-08-15 14-01-29.mp4`
  — 830 MB, 52,6 sn, 1920x1080@48. Çıktıları masaüstüne yaz.
- 180 MB hedefte bu dosya `Balanced` rejime düşer (oran ~4,6×); 8 MB'da `Aggressive`,
  1 MB'da `Extreme`.
- Sert tavanın gevşetilmesi yasak: README ve Hakkında sekmesi "istediğinden büyük dosya
  vermez" diye söz veriyor.
- Kod yorumu yazma. Arayüz değiştiği için `docs/ui-requirements-history.md`'ye satır ekle.

## Doğrulama

```
dotnet build VidShrink.sln -c Release
dotnet test VidShrink.sln
```

Ek olarak gerçek dosyada 180 / 100 / 25 / 16 / 8 MB hedefleriyle çalıştır, sonuçların
bandın içinde olup olmadığını Çıktı'ya tablo hâlinde yaz.

## Kayıt noktası

Tamamlandı. T0'ın `owns`'a eklediği `EncodePlan.cs` ve `MainWindow.xaml.cs` ile blok kalktı:
`ReasonCode`'a `FillCrfLowered`/`FillTwoPassBandCenter` eklendi, `ReasonNote.BandLowerMb`
alanı eklendi, `DescribeReason` switch'ine TR/EN karşılıkları yazıldı, `CurrentOptions()`
`CmbFillPolicy`'yi okuyor, `OnStart` encode çağrısına `fillPolicy` parametresi geçiliyor.
`dotnet build VidShrink.sln -c Release` 0 uyarı/0 hata, `dotnet test VidShrink.sln` 69/69 yeşil.
Gerçek dosyada 5 hedef ölçüldü (aşağıda), çıktılar masaüstünde. Sözleşme submitted.

## Çıktı

**Değişen dosyalar:** `src/VidShrink.Core/PlanCalculator.cs`, `src/VidShrink.Core/EncodePlan.cs`,
`src/VidShrink.Ffmpeg/EncodeRunner.cs`, `src/VidShrink.App/MainWindow.xaml`,
`src/VidShrink.App/MainWindow.xaml.cs`, `src/VidShrink.App/LanguageCatalog.cs`,
`tests/VidShrink.Tests/FillBandTests.cs`, `docs/ui-requirements-history.md`.

**Ne yapıldı.** `PlanOptions.FillPolicy` (varsayılan `FillTarget`) ve `FillBand.For(targetMb)`
`PlanCalculator.cs`'e eklendi. Kalite-tavanı dalı tetiklendiğinde (`ceilingSizeMb < band.LowerMb`)
FillTarget modunda önce CRF düşürülüyor (`ComplexityProfile.CrfForBppf` ile bant merkezine
çözülüyor); CRF codec tabanının altına düşerse iki geçişli VBR'a geçilip bitrate doğrudan bant
merkezine kuruluyor. Her iki yol `ReasonCode.FillCrfLowered` / `ReasonCode.FillTwoPassBandCenter`
ile kaydediliyor ve `MainWindow.xaml.cs`'deki `DescribeReason` üzerinden TR/EN gösteriliyor.
`PlanCalculator.Estimate` artık sabit 0,14/0,32 yerine `ComplexityProfile.EstimateBandFor`
kullanıyor (T2b'nin yayınladığı kalibre bant). `PlanCalculator.Correct` iki yönlü: üst-taşma yolu
eskisiyle bit-bit aynı (yeni `PlanCalculatorTests.cs` testi bozulmadı), alt-taşma yolu
(`fillUnderBand: true`) bitrate'i `bandMerkezi/gerçek` oranıyla artırıyor.
`EncodeRunner.RunAsync`'e sona eklenen opsiyonel `fillPolicy` parametresiyle (varsayılan
`QualityCeiling`, mevcut iki çağrı sitesi pozisyonel imzayı koruduğu için bozulmadı) alt-taşma
tekrarı yalnızca FillTarget'ta tetikleniyor; sert tavan (`ToleranceOver=1.0`) ve üst-taşma
davranışı aynen korundu. `EncodeResult.UnderBand` eklendi.

Sıralama kararı: çözünürlük/kare hızı kurtarma (`RecoverLayoutAtCeiling`, eski `CrfFitMargin`
marjıyla) önce çalışıyor — "bedava" kalite kazancı önce alınıyor; kalan boşluk varsa CRF
düşürme, sonra gerekirse VBR banda çekiyor. Bu sıra korundu, `CrfFitMargin` sabiti
`RecoverLayoutAtCeiling` içinde ve `Correct`'in üst-taşma dalında bilerek değiştirilmedi
(geriye dönük testleri bozmamak için); yeni davranışın tamamı `FillBand` üzerinden.

Arayüzde Hedef panelinde `Fill Policy` etiketiyle iki seçenekli ComboBox eklendi (`Fill Target` /
`Stay At Quality Ceiling`), mevcut `?` rozeti kalıbıyla bilgilendirme, mevcut Intent/Codec/HDR
ComboBox desenine sadık kalındı (yeni RadioButton stili icat edilmedi, `teknesyum-ui` token'ları
dışına çıkılmadı). `LanguageCatalog` girdileri eklendi, iki yönde de benzersiz.

**Gerçek dosya ölçümü** (`gothic2026-08-15 14-01-29.mp4`, 830,2 MB, 52,6 sn, 1920x1080@48,
FillTarget, çıktılar masaüstünde `gothic_fill_<hedef>mb.mp4`):

| Hedef (MB) | Bant [alt, üst] | Sert taban | Sonuç (MB) | Bantta mı | Deneme |
|---|---|---|---|---|---|
| 180 | 174,96–180,00 | 169,92 | 175,06 | evet | 1 |
| 100 | 97,20–100,00 | 94,40 | 97,66 | evet | 1 |
| 25 | 23,75–25,00 | 22,50 | 24,65 | evet | 1 |
| 16 | 15,20–16,00 | 14,40 | 15,82 | evet | 1 |
| 8 | 7,36–8,00 | 6,80 | 7,97 | evet | 1 |

Beşi de ilk denemede bandın içinde kaldı (T2b'nin ölçtüğü %13,3 sapma bu dosya/hedef
kombinasyonlarında alt-taşma tekrarını tetikleyecek kadar büyük çıkmadı — CRF-düşürme/VBR-fill
mantığı zaten bandı doğrudan tutturdu). Alt-taşma tekrar mekanizmasının çalıştığını göstermek için
`FillBandTests.EncodeRunnerRetriesWhenUnderTheHardFloorInFillTargetMode` gerçek ffmpeg ile bilerek
banttan çok düşük bir ilk CRF/bitrate ile başlatıp `attempts > 1` doğruluyor (ToolLocator
mevcutken çalışır, testte doğrulandı).

**Not (kapsam dışı, düzeltilmedi):** `RecoverLayoutAtCeiling`'in iç arama döngüsü hâlâ eski
`CrfFitMargin` (0,94) marjıyla sınırlı; FillTarget ile birlikte bu marj gevşetilirse (örn. bandın
kendisine göre) çözünürlük kurtarma biraz daha büyük çözünürlüğe ulaşabilir — T4/T5'e bırakıldı,
mevcut ölçümlerde bandı tutturmaya yetti.

**Doğrulama:** `dotnet build VidShrink.sln -c Release` → 0 uyarı, 0 hata.
`dotnet test VidShrink.sln` → 69/69 yeşil (FillBandTests dahil 13 yeni test).
