---
id: T139
title: Ucuncu cevap uretimde hala tuketilmiyor — dort cagri yeri ve arayuz varsayilani iki degerli
role: builder
model: sonnet
depends: [T137]
status: submitted
round: 2
verify: [dotnet test -c Release --filter "PlanCalculatorTests|EncoderStateConsumptionTests|HdrResolverTests|PerformanceCheckTests|EncoderAvailabilityTests"]
owns:
  - src/VidShrink.Core/IEncoderAvailability.cs
  - src/VidShrink.Core/PlanCalculator.cs
  - src/VidShrink.Core/HdrResolver.cs
  - src/VidShrink.Ffmpeg/PerformanceProbe.cs
  - tests/VidShrink.Tests/EncoderStateConsumptionTests.cs
  - tests/VidShrink.Tests/PlanCalculatorTests.cs
  - tests/VidShrink.Tests/EncoderAvailabilityTests.cs
  - tests/VidShrink.Tests/SpeedModeTests.cs
  - tests/VidShrink.Tests/HardwareRateControlTests.cs
  - src/VidShrink.Core/EncodePlan.cs
  - src/VidShrink.App/MainWindow.axaml.cs
  - tests/VidShrink.Tests/PlanCalculatorProbeTests.cs
  - tests/VidShrink.Tests/PlanCalculatorProbeTests.cs
  - docs/olcumler/uclu-cevap-uretimde.md
side_effects: []
---

## Baglam

T137 ucuncu cevabi **uretti** ama hicbir uretim yolu onu **tuketmiyor**. Muhurden
once grep'le olculdu: `WorksAsEncoderState` ve `EncoderState` yalnizca testlerden
cagriliyor. Kullaniciya ulasan her yol hala iki degerli `WorksAsEncoder` uzerinden
calisiyor, yani "olcemedim" hala "calismiyor" diye okunuyor.

T137 bunu bilerek disarida biraktigi icin kusur degil, devredilen borctur. Bu
sozlesme borcu kapatir.

Dort cagri yeri (2 Eylul 2026, `4fae7e7`):

| Yer | Satir | Ne yapiyor |
|---|---|---|
| `PlanCalculator.PickCodec` | `src/VidShrink.Core/PlanCalculator.cs:889` | tercih edilen kodegi kabul/ret |
| `PlanCalculator.PickFastCodec` | `src/VidShrink.Core/PlanCalculator.cs:909` | aday listesinde ilk gecerliyi secer |
| `PerformanceProbe.SelectHardwareCodec` | `src/VidShrink.Ffmpeg/PerformanceProbe.cs:97` | donanim adayi secer |
| `HdrResolver` | `src/VidShrink.Core/HdrResolver.cs:82` | HDR yolunda kodek elemesi |

T137 raporu ilk ikisini ve ucuncusunu sayiyor; **`HdrResolver.cs:82` raporda yok.**
Dorttur, uc degil — sozlesmede "uc cagri yeri" diye yazma.

### Asil bulgu: daralma artik arayuz varsayilaninda

`src/VidShrink.Core/IEncoderAvailability.cs:22-23`:

```csharp
EncoderProbeState EncoderState(string codec) =>
    WorksAsEncoder(codec) ? EncoderProbeState.Working : EncoderProbeState.NotWorking;
```

Bu varsayilan, `EncoderState`i **override etmeyen her uygulayici icin** `Unmeasured`i
`NotWorking`e katliyor. Yani T137'nin `EncoderCapabilities.cs:70`te kaldirdigi daralma,
paylasilan arayuzun varsayilaninda yeniden dogmus durumda. Bugun bunu override eden iki
sinif var (`EncoderCapabilities`, `MainWindow.DeferredEncoderAvailability`); geri kalan
her uygulayici sessizce yanlis cevap veriyor.

Bir varsayilan, dogru cevabi vermeyi **secime** birakiyorsa varsayilan yanlis taraftadir.

### App gecidi bu sozlesmenin isi degil

`src/VidShrink.App/MainWindow.axaml.cs:1411` de ayni daralmayi tasiyor ama **T137 tur 2**
onu ustlendi (2 Eylul denetimi, KRITIK 1). O dosyaya dokunma; `owns` disinda.
Bu sozlesme uretim kararini veren dort yeri ve paylasilan arayuz varsayilanini kapatir.


## Kabul kriteri

**K1 — Kusur once olculur.** Dort cagri yerinin her biri icin, yoklama
`Unmeasured` dondugunde bugun ne oldugunu gosteren bir olcu yaz ve **once kirmiziya
dusur.** Dordunu tek bir olcuye sikistirma; hangi cagri yerinin hangi olcuyle
tutuldugu okunabilsin.
CHECK: kusur commit'i ayri, dort kirmizi cikti raporda.

**K2 — Arayuz varsayilani duzelir.** `IEncoderAvailability.EncoderState`in
varsayilani `Unmeasured`i `NotWorking`e katlamaz. Ucuncu durumu tasiyamayan bir
uygulayici icin dogru cevap ne ise onu ver ve **kararini yaz** — varsayilani
`Unmeasured` yapmak da mesru bir cevap, `NotWorking` degil.
Karari verirken dikkat: varsayilani degistirmek `WorksAsEncoder`i override eden ama
`EncoderState`i etmeyen **her test sahtesini** de etkiler. Kac sahteyi etkiledigini
say ve yaz.
CHECK: yeni varsayilan + etkilenen uygulayici sayisi + gerekce.

**K3 — Dort cagri yeri ucuncu durumu ayirt eder.** Her biri icin karar yazili
olmali: `Unmeasured` gelince ne yapiyor. Uc mesru cevap var — kodegi eleme ve
tekrar dene, elemeden gec, ya da kullaniciya "bilmiyorum" tasi. **Hangisini
sectigini ve nedenini yaz.** "Calismiyor gibi davran" mesru cevap degildir; bu
sozlesme tam onu kaldirmak icin var.
CHECK: dort satirlik karar tablosu + her karari tutan olcu adi.

**K4 — Mutasyon.** Her duzeltmeyi tek tek geri al, ilgili olcunun kirmiziya
dondugunu goster. `dotnet build -c Release --no-incremental` zorunlu; `--no-build`
yasak (bu depoda eski ikiliyi kosturup yesil okuma uretti, dahasi `-warnaserror`
altinda derlemesi kirilan bir mutasyon bile yesil basti).
CHECK: mutasyon x olcu izgarasi; her mutasyon yalniz kendi olcusunu kirmali.

**K5 — Uretimde secilen kodek degisti mi.** Bu sozlesme K3 geregi davranisi
**kasitli olarak** degistiriyor. Hangi girdide hangi kodek degisiyor, tablo halinde
yaz. Degismeyen girdiler icin de bir satir olsun.
Bu bir regresyon riski: bugun `Unmeasured` -> elenmis kodek bir yerde **dogru**
sonuc veriyor olabilir. Oyle bir yer varsa bul ve yaz.
CHECK: girdi x eski kodek x yeni kodek tablosu.

**K6 — Yesil kalir ve her verify kolu gercekten test buluyor.**
`dotnet test --list-tests` ile bes kolun her birinin kac test buldugunu say ve
raporda yaz. Sifir bulan kol varsa sozlesmeyi teslim etme, once bana bildir —
bu depoda sifir teste denk gelen kol iki muhurlu sozlesmede sessizce yesil dondu.
CI'a ittikten sonra bir kez bak.
CHECK: kol basina test sayisi + CI kosum kimligi ve durumu.


## Sinirlar

- `owns` disina yazma.
- **Zaman asimi sabitini buyutme.** Sorun sabitin degeri degil, ucuncu cevabin
  tuketilmemesi.
- Bandi genisletme, esigi gevsetme, iddiayi zayiflatma yoluyla yesil uretme.
- `N cagri yeri / N uygulayici / N sahte` diyen her cumlenin altindaki listeyi
  **kendin say.** Bu depoda tablo dogru cikip onu ozetleyen cumle 18+ kez yanlis cikti.
- Geri cektigin iddiayi dosyadan gercekten cikar; "kaldirdim" deyip satirda
  duzenlenmis halde birakma.
- Gecici her sey `.calisma/T139/` altina. **`.calisma/kaynak/` paylasilan 3,5 GB —
  dokunma.** `.calisma/ab/` de baska bir sozlesmenin, dokunma.
- Kod yorumu yazma.
- CI kosumunu bekleyip bosta durma; bir kez bak, surerse yaz ve devam et.
- Makinede baska olcum kosuyor olabilir; agir ffmpeg isi baslatma. Bu is mantik
  duzeltmesi, olcum degil.

## Kayit noktasi

Her kriter kendi commit'inde. Dal: `T139-uclu-cevap-uretimde`, `main`den ac,
`origin`e it. **`main`e birlestirme** — onu T0 yapar.

## Cikti

`docs/olcumler/uclu-cevap-uretimde.md` — K1'in dort kirmizisi, K2'nin karari ve
etkilenen uygulayici sayimi, K3'un dort satirlik karar tablosu, K4'un mutasyon
izgarasi, K5'in kodek degisim tablosu, K6'nin kol basina test sayisi.

## Kim yapiyor

Sole. Dal `sole/T139-uclu-cevap-uretimde`; `main`den acilir, `origin`e itilir,
**`main`e birlestirilmez** — onu T0 yapar.

### Test dosyasi neden yeni

Ilk yazimda `owns` icinde `PlanCalculatorProbeTests.cs` vardi; o dosya **T137 tur 2**
sozlesmesinin de `owns` kumesinde (T9 kriteri onun 413. satirini degistiriyor).
Ayni dosya iki aktif sozlesmeye atanamaz. Bu sozlesmenin olculeri
`tests/VidShrink.Tests/EncoderStateConsumptionTests.cs` icinde yeni bir dosyada
toplanir. `PlanCalculatorProbeTests.cs` verify kolundan cikarildi; ona dokunma.

### T137 tur 2 ayni anda kosuyor

`src/VidShrink.Ffmpeg/EncoderCapabilities.cs` ve
`src/VidShrink.App/MainWindow.axaml.cs` **T137'nin**; ikisine de dokunma.
T137 tur 2 `EncoderCapabilities.WorksAsEncoder` imzasini degistirebilir. O
birlestiginde bu dalin `main`e rebase'i T0'in isidir; sen kendi dalinda
bugunku `main`e gore calis.

### Iki test dosyasi K6 icin acildi (2 Eylul 2026)

K2 arayuz varsayilanini degistirdigi icin `WorksAsEncoder`i override edip `EncoderState`i
etmeyen her sahte kirildi. Sozlesme bunu ongoruyordu ("kac sahteyi etkiledigini say ve
yaz"), ama duzeltilecek sahteler `owns` disindaydi. T0 karari: **acildi.**

- `tests/VidShrink.Tests/PlanCalculatorTests.cs` — sahibi yoktu, dogrudan acildi.
  Sahteler: `FakeAvailability` (`:9`), `SurucusuzMakine` (`:506`).
  `OlculmemisMakine` (`:529`) zaten `IEncoderMeasurementState` tasiyor, ona dokunma.
- `tests/VidShrink.Tests/EncoderAvailabilityTests.cs` — T137'nindi; **T137 tur 2
  muhurlendi** ve dosya serbest kaldi. Sahte: `Makine` (`:9`).

**Sinir: yalniz uc sahteye `EncoderState` override'i eklenir.** Bu iki dosyada baska
hicbir satir degismez — ne iddia, ne band, ne esik, ne test adi. Bir olcunun beklentisini
degistirmen gerekiyorsa **yapma, bildir**: o zaman kirilan sey sahte degil, K2'nin kendisidir.

### Once `main`e rebase et

T137 tur 2 muhurlendi ve `EncoderCapabilities.WorksAsEncoder`in **govdesi degisti**:
olcemedigi kodek icin artik `false` degil `HasEncoder(codec)` donuyor. Senin K2 kararin
bununla ayni sorunun iki ucu. Guncel `main`e rebase et, K5'in kodek degisim tablosunu
**rebase sonrasi** yeniden uret; rebase oncesi tablo artik yanlis taban olcuyor.

### 30 sn butcesini 231 ms asan olcu

Makinede es zamanli uc sozlesme kosuyordu; bu deponun bilinen yuk yapintisi. Sabiti
degistirmemekle dogru yaptin. **Tek basina yeniden kostur**; yalniz basina da asiyorsa
bildir, sabit degil kok neden aranir.


### Uc test dosyasi daha acildi (2 Eylul 2026)

Rebase sonrasi tam suit `1330 basarili / 14 basarisiz`. Kirmizilarin hepsi K2'nin
ongordugu sey: `WorksAsEncoder`i override edip `EncoderState`i etmeyen eski sahteler.
Ucu de `owns` disindaydi.

- `tests/VidShrink.Tests/SpeedModeTests.cs` — `FakeAvailability`, 7 kirmizi.
- `tests/VidShrink.Tests/HardwareRateControlTests.cs` — `FixedAvailability`, 1 kirmizi.
- `tests/VidShrink.Tests/PlanCalculatorProbeTests.cs` — `RecordingAvailability`, 6 kirmizi.

T0 karari: **ucu de acildi.** Hicbir aktif sozlesme bu dosyalari sahiplenmiyor
(T137 muhurlendi, `PlanCalculatorProbeTests.cs` serbest kaldi).

**Bu, sozlesmenin ustundeki "`PlanCalculatorProbeTests.cs` verify kolundan cikarildi,
ona dokunma" sinirini gecersiz kilar.** O sinir T137 tur 2 es zamanli kostugu icin
vardi; T137 muhurlendi, sebep dustu.

**Sinir: yalniz uc sahteye `EncoderState` override'i eklenir.** Bu uc dosyada baska
hicbir satir degismez — ne iddia, ne band, ne esik, ne test adi. Bir olcunun
beklentisini degistirmen gerekiyorsa **yapma, bildir**: o zaman kirilan sey sahte degil,
K2'nin kendisidir ve karar T0'a aittir.

Teslim kapisi tam suitin tamami yesil olmadan kapanmaz — `kosum-kapisi.ps1`
`-MinimumTotal 1134` ile filtresiz kosuyor, 14 kirmizi CI'i kirmizi tutar.

## Tur 2 — KRITIK: surucusuz makine artik "donanim var" diyor

Denetim KALDI verdi. Bir KRITIK ve dort borc. Sozlesmenin kendi karari (K3) dogru,
kirilan sey **kararin arayuze sizma bicimi** ve K5'in beyani.

### Kusur

`MainWindow.axaml.cs:1495-1502` (`ProbeHardwareEncodersAsync`) ham
`EncoderCapabilities.Instance`i `SpeedMode.Fast` ile `PlanCalculator.Build`e veriyor,
sonra sunu yaziyor:

```
return ((IEncoderAvailability?)capabilities, CodecModel.IsHardware(plan.Codec), decision);
```

`available` degeri **yoklama sonucundan degil kodek adindan** turuyor.

`EncoderCapabilities` `EncoderState`i uygulamiyor (arayuz varsayilani `Unmeasured`) ve
`IEncoderMeasurementState` de tasimiyor, dolayisiyla `KnownState` onbellek sogukken
**hicbir seyi yoklamadan** `Unmeasured` donuyor. `PickFastCodec` bunu gorunce taramayi
durduruyor ve `FastHardwareOrder`in ilk adayini **gecici cevap** olarak veriyor
(`PlanCalculator.cs:902-907`), isareti `probe.NotMeasured`e koyuyor. Ama `Build` yolu
o isareti disari tasimiyor ve cagiran da okumuyor.

Sonuc, `docs/olcumler/surucu-yoklugu.md`deki gercek girdide (ffmpeg `av1_nvenc`i
listeliyor, surucu yok, yoklama tamamlanip basarisiz oluyor):

| | eski | yeni |
|---|---|---|
| `PickFastCodec` cevabi | `libx264` (gercekten yoklandi) | `av1_nvenc` (hic yoklanmadi) |
| `available` | `false` | **`true`** |
| `ChkFastGpu.IsEnabled` | kapali | **acik** |
| `_hardwareEncoderAvailable` | `false` | **`true`** |

Denetci bunu olctu: `EncoderCapabilities` bicimli, `IEncoderMeasurementState` tasimayan,
`av1_nvenc=Unmeasured` donen bir sahte ile eski govde `libx264`, yeni govde `av1_nvenc`
veriyor.

**Gecici cevap tasarim geregi mesrudur** — arayuz is parcaciginda ffmpeg dogurmamak
dogru karar. Kirilan sey su: gecici cevap, olculmus cevaptan **ayirt edilemeden** arayuze
ulasiyor.

### K7 — Gecici cevap arayuze olculmus gibi ulasmaz

`probe.NotMeasured` isareti cagiranin okuyabilecegi bir yere cikar ve
`ProbeHardwareEncodersAsync` onu okur: olculmemis bir donanim adayi `available = true`
uretemez. `EncodePlan`e alan eklemek serbest (`owns`a eklendi).

Bu **yeni bir yetenek degil**, T139'un kendi kararinin uretimde tamamlanmasidir. Bu
depoda ALTI kez "yetenek yazildi, olculdu, muhurlendi, uretimde kimse cagirmadi" oldu;
yedincisi olmasin.

CHECK: `docs/olcumler/surucu-yoklugu.md` girdisiyle eski/yeni tablo — dort satirin
dordu — ham ciktiyla. Ayrica `ChkFastGpu.IsEnabled`i pimleyen bir olcu.

### K8 — Mutasyon

K7'nin duzeltmesini geri al, olcunun kirmiziya dondugunu goster.
`dotnet build -c Release --no-incremental` **zorunlu**, `--no-build` yasak. Iki kolun da
ham ciktisi, adlari kolu soylesin.
CHECK: mutasyon x kol izgarasi, her hucrede ham cikti.

### K9 — K5 yeniden yazilir

Rapordaki K5 paragrafi **tersine yazilmis** ve oldugu gibi duruyor:

> "dogrudan `EncoderCapabilities` kullanan uretim yolunda secilen kodek degismiyor …
> Secim degisikligi … `DeferredEncoderAvailability` gecidinde gorulur."

Iki yarisi da yanlis. `DeferredEncoderAvailability` icin `EncoderState != Unmeasured`
ile `IsMeasured` **ozdes**: `Measure` `answer.Settled`i yalniz
`state != Unmeasured && Failure == null` iken kuruyor (`MainWindow.axaml.cs:1469`),
`Ready` de yalniz `Settled` iken `true` donuyor (`:1408`). Yani K5 tablosunun 1., 2., 3.
ve 5. satirlarindaki degisimler o girdide **gerceklesmiyor**. Degisen tam da doguridan
`EncoderCapabilities` yolu.

K5 tablosunu **oldugu gibi silme** — yanlis oldugunu yaz, dogrusunu altina koy.
CHECK: her satirin altinda onu ureten kosum.

### Borclar — kapat ya da neden kapatmadigini yaz

1. **K2 sayimi 19 degil 20.** `PlanCalculatorProbeTests.SwitchableAvailability`
   (`tests/VidShrink.Tests/PlanCalculatorProbeTests.cs:647`) listede yok;
   `IEncoderAvailability` uygulayip `EncoderState`i override etmiyor ve rebase tabani
   `e0eee12`de de vardi. Dosya `owns`a eklendi.
2. **K6 sayilari birlesme sonrasi bayat.** Rapor `PerformanceCheckTests = 21`, toplam
   `70`, tam suit `1362`. Teslim edilen `a125667`de gercek sayilar **22**, **71** ve
   CI'da **1484**. Yeniden say.
3. **K6'nin CI kimligi hic doldurulmamis.** Raporda "asagiya eklenecek" yer tutucusu
   duruyor. Dogru kimlik `33680674698`, `completed success`,
   `Failed: 0, Passed: 1465, Skipped: 19, Total: 1484`. Yaz.
4. **K1'in andigi kusur commit'i erisilemez.** Rapor `10f9073` diyor; rebase sonrasi
   daldaki karsiligi `d3f8962`. Isareti tazele.

### Sinirlar

- `owns` uc dosyayla genisletildi: `EncodePlan.cs`, `MainWindow.axaml.cs`,
  `PlanCalculatorProbeTests.cs`. **Baska hicbir yere yazma.**
- `MainWindow.axaml.cs` normalde T146'nin; T146 acik ve dagitilmadi, gecici olarak sana
  verildi. Orada **yalniz K7'nin gerektirdigi kadarini** degistir; baska bir sey
  duzeltme, temizleme, yeniden adlandirma.
- Renk ve olcu uydurma; `Themes/Theme.axaml` belirtecleri disina cikma.
- Testi silme, `Skip`e alma, filtreden cikarma yoluyla yesil uretme.
- Sayi veren her cumlenin altindaki listeyi **kendin say ve ham ciktiyi goster.**
- Gecici her sey `.calisma/T139/` altina. **`.calisma/kaynak/` paylasilan 3,5 GB —
  dokunma.** `.calisma/ab/` baska sozlesmenin.
- Kod yorumu yazma.
- Dal `sole/T139-uclu-cevap-uretimde` uzerinde devam et, `origin`e it. **`main`e
  birlestirme** — onu T0 yapar. Bash kancasi itmeyi engellerse PowerShell aracindan
  `$env:TEKNESYUM_GATE_OPEN = "1"` ile it.
- CI kosumunu bir kez bak, surerse yaz ve devam et.

## T0 kusuru — tur 2 onculunun gerekcesi yanlisti (3 Eylul 2026)

Sozlesme tur 2'de "`EncoderCapabilities` ne `EncoderState`i ne de
`IEncoderMeasurementState`i uyguluyor, bu yuzden `KnownState` yoklama yapmadan
`Unmeasured` donuyor" dedi. **Yanlis.** Yapici bildirdi, T0 tek komutla olctu:

```
git show origin/main:src/VidShrink.Ffmpeg/EncoderCapabilities.cs | grep -n "EncoderState"
108:    public EncoderProbeState EncoderState(string codec)
```

`EncoderState` uygulaniyor. KRITIK'in kendisi gercekti — surucusuz makinede arayuz
"donanim var" diyordu ve yapici bunu iki mutasyonla dogruladi — ama T0'in yazdigi
**mekanizma gerekcesi** yanlisti. Sonuc ayni, sebep farkli.

Bu, `gelen-duzeltme-de-bir-iddiadir` dersinin ters yonu: gelen duzeltme bu sefer
**dogruydu** ve tek komutla uretilebildigi icin olculup kabul edildi. Olculmeden
kabul edilmedi, olculmeden reddedilmedi de.

## Tur 2 denetimi — GECTI (3 Eylul 2026)

Denetci bagimsiz klonda her K'yi yeniden uretti. K7: `MainWindow.axaml.cs` diff'i
`--numstat` ile **15 ekleme / 1 silme = 16 satir**, iddia dogru; `owns` disina tek
satir yok. K8: iki mutasyon `--no-incremental` ile yeniden kosuldu, ikisi de ayni uc
testi kiriyor (`74` → `Basarisiz 3, Basarili 71`), diger dort kol yesil; pinler
`AppHost.Run` icinde gercek `MainWindow` kurup `ChkFastGpu.IsEnabled` okuyor, yani
**davranis olcuyor**. K9: alti satirin ana iddiasi kodla dogrulandi. Borclar: K2 = 20
(`SwitchableAvailability` `:644`, sozlesme `:647` diyor), K6 = 32/7/1/22/12 = 74,
sifir bulan kol yok. CI `33687394637` `completed success` (1473/1492).

**KRITIK yok.** Yedi borc:

1. **Uretim cagri yeri pimsiz.** Denetci `MainWindow.axaml.cs:1517`'yi KRITIK'i ureten
   eski haline geri aldi ve **74/74 yesil** kaldi. `HardwareAvailableFrom`a uretim
   yolundan gecen pim yok. Bugunku kod dogru; kirilan sey regresyon korumasi. Bu
   deponun "yetenek yazildi, uretimde kimse cagirmadi" sinifinin **pim tarafi**.
2. Rapordaki CI satiri bayat — "in_progress, sonucu gorulmedi" diyor, kosum yesil bitti.
3. Dal `origin/main`den 22 commit gerideydi; rebase T0'in isi, birlestirildi.
4. K9 CHECK'i yarim: "ayni" yazan dort satirin **dal** tarafi icin kosum ciktisi yok.
5. K6'da tur 1 kapi sayisi (`1345/1362`) etiketsiz kalmis; guncel toplamlar 1484 ve 1492.
6. `PickFastCodec` ilk olculmemis adayda duruyor — `av1_nvenc` dusup `hevc_nvenc`
   calisan makinede yanlis-negatif. Tur 1 kararinin urunu, **ayri sozlesme gerekiyor**.
7. Sozlesme metnindeki gerekce yanlisti, rapor dogru soyluyor — T0 olctu ve duzeltti,
   yukarida "## T0 kusuru" basligi altinda.

## owns duzeltmesi — hic var olmayan dosya (3 Eylul 2026)

`owns` `tests/VidShrink.Tests/HdrResolverTests.cs` diyordu. Bu dosya **hicbir zaman
var olmadi**: `git log --all --diff-filter=AD` bos donuyor, agacta yok, T139 ona hic
dokunmadi ve baska hicbir sozlesme sahiplenmiyor. Gercek dosya adi
`HdrArgumentsTests.cs` ve T139 onu da degistirmedi.

Yani `owns` satiri bir yazim hatasiydi ve iki tur boyunca kimse fark etmedi — mühür
kapisi yakaladi. Satir cikarildi; yerine baska dosya konmadi cunku dokunulan bir sey yok.

**Kapinin dogru davranisi:** `complete` "owns names files that do not exist" deyip
durdu. `owns` sahipligi ilan eder; var olmayan dosya sahiplenilemez.
