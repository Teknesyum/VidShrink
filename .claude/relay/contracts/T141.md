---
id: T141
title: Yoklama yedek yola dustugunde pencerenin kalitesi sessizce olculmuyor
role: builder
model: sonnet
depends: [T132]
status: active
round: 1
verify: [dotnet test -c Release --filter "ComplexityProbeTests|ComplexityScanTests|PlanCalculatorProbeTests"]
owns:
  - src/VidShrink.Ffmpeg/ComplexityProbe.cs
  - tests/VidShrink.Tests/ComplexityProbeTests.cs
  - docs/olcumler/yedek-yolda-kalite.md
side_effects: []
---

## Baglam

Bulguyu T132 yapicisi kendi isini olcerken buldu ve **duzeltmedi** â€” kapsam disiydi,
dogru davrandi. Ben bagimsiz dogruladim; asagidaki satir numaralari `b88bb66`
agacindan.

`ComplexityProbe.WindowSample` bes alanli ve sonuncusu kalite
(`src/VidShrink.Ffmpeg/ComplexityProbe.cs:733`):

```csharp
internal readonly record struct WindowSample(long FullBytes, long FullFrames, long HalfBytes, long HalfFrames, WindowQualityMeasurement? Quality = null);
```

Uc yerde kuruluyor. **Yalniz biri kaliteyi tasiyor:**

| Satir | Yol | Kalite |
|---|---|---|
| `:740` | `half is null` â€” yarim cozunurluk yoklanamiyor | **yok** (`Quality = null`) |
| `:748` | yedek yol â€” hizli bolunmus ornekleme basarisiz oldu | **yok** (`Quality = null`) |
| `:793` | hizli yol â€” `SplitSampleAsync` yerlesti | var |

`SampleWindowAsync` imzasi `IQualityMeasurement? qualityMeasurement` aliyor
(`:735`) ama o parametreyi **yalnizca** `SplitSampleAsync`e veriyor (`:743`).
Yedek yol (`:746-748`) olceri hic gormuyor.

Tuketen taraf farki anlamiyor (`:98`):

```csharp
if (sample.Quality is { Comparable: true, VmafNegMean: not null } quality) qualities.Add(quality);
```

`null` sessizce atlaniyor. Sonuc: yedek yola dusen pencerenin kalitesi
**olculmemis** olmasina ragmen hicbir yere "olculmedi" diye yazilmiyor;
`ProbeResult` daha az kalite olcumuyle donuyor ve arayanin elinde eksigi
ayirt edecek bir isaret yok.

**Bu, bu deponun kapatmakta oldugu kusurun ayni ailesi:** "olcemedim" ile
"olctum, sonuc bu" ayrimi kayboluyor (T137/T139 ayni ayrimi kodlayici
yoklamasinda kapatiyor). Burada kayip daha sinsi, cunku ucuncu bir deger bile
yok â€” kayit hic olusmuyor.

**Ne iddia etmiyorum:** yedek yolun ne siklikta kostugu bu depoda **olculmedi.**
Sifir olabilir. Once onu olc; K1 bunun icin var.

## Kabul kriteri

**K1 â€” Yedek yol ne siklikta kosuyor, once olc.** `SplitSampleAsync`in `null`
donme sartlarini oku ve yedek yola dusmeyi **kasitli olarak tetikleyen** bir olcu
kur. Ardindan `half is null` kolunun hangi girdide olustugunu yaz.
Bu iki kolun **hic kosmadigini** olcersen sozlesme burada durur: bulguyu "olu
kol" diye yaz, K2-K4'u atla, T0'a bildir. Olu kolu duzeltmek borc degil israftir.
CHECK: iki kolun tetiklendigi (ya da tetiklenemedigi) olcu adi + cikti.

**K2 â€” Kusur once kirmiziya duser.** Yedek yola dusen bir pencerenin kalitesinin
`ProbeResult`a **hic girmedigini** gosteren bir olcu yaz ve once kirmizi kaydet.
`qualities.Count` sabitine bakan bir olcu yazma â€” bu depoda iki sabiti
karsilastiran test davranis olcmedigi icin mutasyonu gecirdi. Olcu, ayni girdinin
hizli yol ve yedek yol hallerini **karsilastirmali.**
CHECK: kusur commit'i ayri, kirmizi cikti raporda.

**K3 â€” Karari sen ver, gerekcesini yaz.** Uc mesru cevap var:
(a) yedek yol da kaliteyi olcsun â€” maliyeti olc ve yaz;
(b) olcemedigini tasiyan bir isaret uretilsin, `null` ile "olctum ve
kiyaslanamaz" ayrilsin;
(c) yedek yol kaliteyi hic olcmesin ama bu **yazili** olsun ve arayan kac
pencerenin olculmedigini gorsun.
"Bugunku gibi kalsin" mesru cevap degil â€” bugun ucu de degil, sessizlik var.
CHECK: secilen sik + gerekce + karari tutan olcunun adi.

**K4 â€” `half is null` kolu da ayni karari alir.** `:740` ile `:748` ayni sessizligi
tasiyor. Ikisine farkli karar veriyorsan farki gerekcelendir.
CHECK: iki kol icin karar tablosu.

**K5 â€” Uretimde olculen kalite degisti mi.** K3 (a) ya da (b) secildiyse
`ProbeResult.QualityMeasurements` sayisi degisebilir. Hangi girdide kacken kaca
ciktigini tablo yap; degismeyen girdiler icin de bir satir olsun. Bu sayiyi
tuketen yerleri `grep` ile bul ve etkilenip etkilenmediklerini yaz.
CHECK: girdi x eski sayi x yeni sayi tablosu + tuketen yerlerin listesi.

**K6 â€” Mutasyon.** Her duzeltmeyi tek tek geri al, ilgili olcunun kirmiziya
dondugunu goster. `dotnet build -c Release --no-incremental` zorunlu;
**`--no-build` yasak.**
CHECK: mutasyon x olcu izgarasi; her mutasyon yalniz kendi olcusunu kirmali.

**K7 â€” Verify'in her kolu gercekten test buluyor.** `dotnet test --list-tests`
ile uc kolun her birinin kac test buldugunu say ve raporda yaz. Sifir bulan kol
varsa teslim etme, once T0'a bildir. `dotnet test` tamami yesil olmadan teslim yok.
CI'a ittikten sonra bir kez bak; `completed success` yaz, `in_progress` yetmez.
CHECK: kol basina test sayisi + CI kosum kimligi ve **sonucu**.

## Sinirlar

- `owns` disina yazma. `IQualityMeasurement.cs` ve `QualityMeter.cs` **senin
  degil**; imza degisikligi gerekiyorsa yazma, T0'a sor.
- Zaman asimi sabitlerini buyutme. `SampleTimeout` (`:49`) bu sozlesmenin konusu degil.
- Bandi genisletme, esigi gevsetme, iddiayi zayiflatma yoluyla yesil uretme.
- `N kol / N cagri yeri` diyen her cumlenin altindaki listeyi **kendin say.**
  Bu depoda tablo dogru cikip onu ozetleyen cumle 22 sozlesmede yanlis cikti.
- Geri cektigin iddiayi dosyadan gercekten cikar.
- **Kod yorumu yazma.** Var olan yorumlari da silme.
- Gecici her sey `.calisma/T141/` altina. **`.calisma/kaynak/` paylasilan 3,5 GB â€”
  dokunma.**
- Agir ffmpeg isi baslatma; makinede baska olcum kosuyor olabilir. Zamanlama
  bandi testini yukte kirmizi gorursen tek basina yeniden kostur.
- CI kosumunu bekleyip bosta durma; bir kez bak, surerse yaz ve devam et.

## Kayit noktasi

Her kriter kendi commit'inde. Dal: `T141-yedek-yolda-kalite`, `main`den ac,
`origin`e it. **`main`e birlestirme** â€” onu T0 yapar.

## Cikti

`docs/olcumler/yedek-yolda-kalite.md` â€” K1'in siklik olcusu, K2'nin kirmizisi,
K3'un karari ve gerekcesi, K4'un iki kollu karar tablosu, K5'in sayi tablosu ve
tuketen yerler, K6'nin mutasyon izgarasi, K7'nin kol basina test sayisi ve CI sonucu.


## T0 cevabi — K7'nin iki yarisi (3 Eylul 2026)

Yapicinin acik sorusu: "CI 33681585349 hala in_progress; K7'nin completed success
muhrunu sen mi alacaksin?" **Aldim, olctum.**

### CI yarisi — saglandi

`33681585349` **in_progress degil, `cancelled`**: `e31a923` uzerindeydi ve sonraki itme
onu gecersiz kildi. Teslim tepesinin kendi kosumu var ve yesil:

```
databaseId 33682771946
headSha    0ec46332f31b68acf9bd1e11b2abf0abcdc90775
status     completed
conclusion success
```

`0ec4633` dalin tepesi. K7'nin `completed success` sarti **karsilandi.**

### Filtresiz yerel suit yarisi — karsilanamiyor, ve sucu T141'de degil

Yapici uc denemede uc ayri yerde test ana isleminin coktugunu, ayni cokusun temiz bir
`origin/main` worktree'sinde de olustugunu bildirdi. **Bagimsiz dogrulandi:** `main`in
kendi CI kosumu `33689866767` (head `63d8e1a`) **success**. Yani cokus CI'da
uretilemiyor, yalniz bu makinede olusuyor.

Ayni tabloyu ayni gun ikinci bir kaynak da verdi: T149 denetcisi `63d8e1a` uzerinde
`Windows11ShellMenuTests.Sparse_package_really_registers_and_removes_on_Windows_11`in
**yerelde** dustugunu, mutasyonlu ve mutasyonsuz iki kosumda da dustugunu olctu — ayni
commit'in CI'i yesil.

Iki bagimsiz olcum, ayni sonuc: **bu makinede filtresiz suit kararsiz, CI'da degil.**
K7'nin bu yarisi T141'in kusuru degil ve T141'i durdurmaz. Denetciye gorev: dalin verify
kollarini ve K1-K6'yi dogrulamak; filtresiz suiti **tekrar kosturmasin** (bu depoda bir
turda 17 kosum oldu ve paralel kosum olcuyu kendisi kararsiz yapti).

### Sirada

Denetci **atanmadi**: paralel ajan tavani 2 ve dolu (T150 tur 2, T145). Bosalan ilk
yuvaya T141 denetimi girer — T151 ve T146'nin onunde, cunku T141 teslim edilmis durumda
ve teslim cevapsiz birakilmaz.

Denetciye verilecek not: `.calisma/T141/` altindaki fixture dosyalari **bilerek**
birakildi, denetim icin duruyor; muhurden sonra silinecek.

## Denetim — GECTI (3 Eylul 2026)

Denetci bagimsiz klonda `0ec4633`te olctu, depoya yazmadi. **KRITIK yok.**

- **K1'in cikis kapisi kapali degil, iki kol da canli.** Denetci rapora degil mutasyona
  bakti: `:737` ve `:746-748` bozulunca olculer kirmiziya dondu, yani olculer kollarin
  kostugunu varsaymiyor, kosmasina bagli. `half is null` uretim girisinden de geliyor
  (`ComplexityProbe.cs:71-73`, `EvenDown(Round(96*0.5))=48 < 64`).
- **K2 kirmizisi yeniden uretildi.** Kusur commit'i `1c87aaf` yalniz teste dokunuyor;
  denetci kendi derleyip kosturdu: `Basarisiz: 3, Basarili: 32, Toplam: 35`, raporun
  blogyla birebir. Uc olcu de karsilastirmali, `qualities.Count` sabiti yok.
- **K5'in tuketen yerlerini kendi saydi**: alti uretim/arac tuketicisi + `HasQuality`;
  raporun cumlesi tabloyla uyusuyor, sapma yok.
- **K6'nin iki mutasyonunu kendi uyguladi**, her kolda `--no-incremental`. M1 iki olcuyu,
  M2 bir olcuyu kirdi; capraz bulasma yok.
- **K7'nin uc kolunu kendi saydi**: 35 / 42 / 25, toplam 102 yesil, **atlanan 0** —
  `FfmpegFact` olculeri gercekten kostu. CI yarisi T0 tarafindan kapatilmisti, acilmadi.
- **owns temiz.** Yasakli `IQualityMeasurement.cs` ve `QualityMeter.cs` dokunulmamis.
- **Satir numaralari taze**: sozlesmedeki eski `:740` rapora sizmamis, dosyadaki gercek
  `:737` / `:746-748` yazilmis.

### Borclar

1. `.calisma/T141/` **hic yok** — worktree kapandi, fixture'lar gitti. Raporun bes
   fixture atfi dogrulanamiyor; denetci ham bloklari sifirdan yeniden uretti ve sayilar
   tuttu. Kritik degil ama "denetim icin bilerek birakildi" dedigim sey yerinde degildi:
   **bu T0 kusuru** — worktree'nin gitignore'daki ciktisi birlesmez
   (`denetci-worktree-yerel-dosyayi-eski-gorur`).
2. Zaman asimi bosuklugu: yeni kol olceri `ct` ile cagiriyor
   (`ComplexityProbe.cs:762`), hizli yol `SampleTimeout`lu `token` ile (`:809`).
   `QualityMeter`in kendi zaman asimi yok — iki yeni kol hizli yoldan uzun asilabilir.
   Rapor bunu kendi borc 1'inde yaziyor.
3. K3'un "maliyeti olc ve yaz" sarti **yapisal** cevaplanmis (+1 olcer cagrisi/pencere,
   ek ffmpeg kodlamasi yok). Koddan dogru, sure olarak hic olculmemis.
4. Yedek yol uretim girdisiyle tetiklenemiyor; pim ic imzaya zorlanmis `(161,121)`
   yarimla duruyor. Kolun gercek sikligi hala bilinmiyor — raporda durustce yazili,
   olu kol degil.
5. `ComplexityProbeTests.cs:1` basina BOM (U+FEFF) girmis; diff'in tek amacsiz satiri.
6. `kapsam.json`da `T141Kesif.cs` ve `T141Dokum.cs` girdileri var, dosyalar agacta yok.
   Gecici olcum dosyalari `.calisma/T141/` yerine `tests/` altina konup silinmis.
   `kapsam.json` kanca uretimi bir defter; girdi artigi zararsiz, ama gecici dosyanin
   `tests/` altina konmasi sozlesmenin "gecici her sey `.calisma/T141/` altina" sinirini
   deliyor.
7. `HasQuality`nin agactaki tek tuketicisi bir test. K5 tablosu onu etki satiri olarak
   yaziyor ama uretimde kimsenin okumadigini soylemiyor.
