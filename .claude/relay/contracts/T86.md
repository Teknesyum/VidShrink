---
id: T86
title: Üç ölçü süreçler arası paylaşılan duruma bakıyor, eşzamanlı koşumda rastgele düşüyor
role: builder
model: opus
depends: []
status: open
round: 2
verify: [dotnet test -c Release --filter "PerformanceCheckTests|ShellMenuTests|Windows11ShellMenuTests|UpdaterTests"]
owns: [tests/VidShrink.Tests/PerformanceCheckTests.cs, tests/VidShrink.Tests/ShellMenuTests.cs, tests/VidShrink.Tests/Windows11ShellMenuTests.cs, tests/VidShrink.Tests/UpdaterTests.cs, tools/kosum-kapisi/kosum-kapisi.ps1, tools/kosum-kapisi/test-kapi.ps1, tools/kosum-kapisi/fixtures/basarisiz-en.txt, tools/kosum-kapisi/fixtures/eksik-toplam-en.txt, tools/kosum-kapisi/fixtures/gecerli-en.txt, tools/kosum-kapisi/fixtures/gecerli-tr.txt, tools/kosum-kapisi/fixtures/kesinti-tr.txt, docs/olcumler/surecler-arasi-olcu-yalitimi.md]
side_effects: [makine genelinde AppX paketi kaydediyor ve kaldırıyor, HKCU kabuk anahtarı yazıyor]
---

## Bağlam

T85 süiti eşzamanlı koşturarak ölçtü. Altı koşumun altısı da `Toplam: 958`'e vardı, ama
ikisinde `Başarısız: 1` çıktı — ve düşen ölçüler her seferinde farklıydı. Üçü de aynı
kusuru taşıyor: **süreçler arası paylaşılan işletim sistemi durumuna bakıyorlar.**

1. `PerformanceCheckTests.OlcumArtikBirakmiyor` — sistemin `%TEMP%`'inde dizin sayıyor,
   aynı anda koşan öteki süitin bıraktığını da görüyor: `Expected: 1, Actual: 2`.
2. `ShellMenuTests.Every_command_calls_the_installed_launcher_with_the_path` — HKCU
   kabuk anahtarına iki süreç birden yazıyor, `New-Item` `IOException` veriyor.
3. `Windows11ShellMenuTests.Sparse_package_really_registers_and_removes_on_Windows_11` —
   makine genelinde AppX paketi kaydedip kaldırıyor; paket kimliği sabit olduğu için
   ikinci süreç birincisinin kaydını görüyor.

Bu üçü koşumdan koşuma rastgele kırmızıya döndüğü için **yeşil okuma hâlâ güvenilir
değil**: süit tamamlanıyor ama sonucu makinede o an başka ne koştuğuna bağlı.

Birincisi ayrıca `AGENTS.md`'nin yazılı kuralını deliyor: geçici dosya sistemin
`%TEMP%`'ine değil, projenin `.calisma/` klasörüne yazılır.

## Amaç

Bu üç ölçünün sonucu, makinede aynı anda başka ne koştuğundan bağımsız olsun.

## Kabul kriteri

1. **Üçü de kendi durumunu yalıtır.** Geçici klasör, kayıt defteri kökü ve paket kimliği
   koşan sürece özel olsun (süreç kimliği ya da koşum başına üretilen bir kök). Yalıtmanın
   mümkün olmadığı yerde — AppX paket kimliği manifest'te sabitse — adlandırılmış bir
   kilitle sıraya sokulsun ve **neden yalıtılamadığı** yazılsın.

2. **Ölçüsü: iki süit aynı anda, arka arkaya üç kez.** Altı koşumun altısı da
   `Toplam:` sayısına varsın **ve** altısında da `Başarısız: 0` olsun. Altı sonuç satırını
   rapora yapıştır. T85'te dördü tuttu, ikisi tutmadı; ölçü budur.

3. **Üçü de hâlâ gerçek bir kusuru yakalıyor.** Her ölçü için ayrı mutasyon: ölçtüğü
   üretim davranışını boz, ölçünün kırmızıya döndüğünü göster, geri al. Yalıtma bir ölçüyü
   körleştirmişse yalıtma yanlıştır.

4. **`%TEMP%` kullanımı `.calisma/` altına taşınır.** `AGENTS.md` bunu şart koşuyor.
   Taşınamıyorsa nedenini yaz ve orada dur.

5. **Hiçbir iddia zayıflamıyor.** Ölçü silinmez, `Skip` eklenmez, eşik gevşetilmez.
   Atlanan sayısı artmaz.

## Sınırlar

- **`src/**` senin değil.** Kusur ölçüm tarafında. Üretim kodu değişikliği gerekiyorsa
  yapma: neyin gerektiğini yaz ve o maddede dur.
- `tests/VidShrink.Tests/` altındaki öteki dosyalara dokunma.
- Kendi worktree'nde çalış. Paylaşılan çalışma ağacında (`Desktop/Projeler/Vidshrink`)
  yazma ve orada `dotnet test` koşturma — başka bir ajan orada olabilir.
- Yorum yazma; mevcut yorumları koru.
- Ara dosyalar `.calisma/` altına; iş bitince kendi bıraktığını sil.

## Çıktı

- `docs/olcumler/surecler-arasi-olcu-yalitimi.md` — her ölçü için hangi durum paylaşılıyordu,
  nasıl yalıtıldı, altı koşumun sonuç satırı, üç mutasyonun sonucu.
- Kendi dalın (`t86-olcu-yalitimi`), itilmiş. `main`e sen birleştirme.

## Kayıt noktası

Kesilirsen hangi ölçüde kaldığını ve o ana kadar aldığın koşum satırlarını rapora yaz.
Yarım ölçüm silme, "yarım" diye işaretle.

## Ek madde — dördüncü ölçü CI'da düştü

`UpdaterTests.TheDeletionStepWaitsOutATransientLock` (`UpdaterTests.cs:1084`) `main`de
CI koşumu 33502024892'de kırmızı:

    Assert.Contains() Failure: Sub-string not found
    String:    "BITTI"
    Not found: "yeniden denenecek"
    Standard Output: geçici kilit: çıkış 0, 2116 ms

Ölçü, silme adımının **geçici bir kilide takılıp yeniden denemesini** bekliyor. CI'da
kilit ilk denemeden önce serbest kaldı, yeniden deneme hiç gerekmedi ve ölçü düştü.
Yani ölçü bir davranışı değil bir **zamanlamayı** sınıyor; bu makinede geçiyor, daha
hızlı ya da daha yavaş bir makinede düşüyor.

Bu, sözleşmenin konusu olan sınıfın dördüncü örneği: ölçü kendi kurduğu yarışa bağlı.

**Gereken:** kilidin gerçekten tutulduğu, ölçünün kontrol ettiği bir düzenekle
kurulsun — silme denemesi başlamadan kilit alınmış olsun ve kilit ölçünün elinde
bırakılsın. Zamanlamaya değil duruma bakılsın. Yeniden deneme yolunun ölçüldüğü
korunur; gevşetilmez, `Skip`e alınmaz.

CHECK: `dotnet test -c Release --filter "UpdaterTests"` — arka arkaya üç koşumda üçü de
yeşil, üçünün de sonuç satırı rapora girer.

# Düzeltme turu 2

Bağımsız denetim **KALDI** verdi. Bir KRİTİK var: Ek madde'nin istediği düzeltme
yapılmamış, kusur ötelenmiş. Kriter 2 ve 3'ün kanıtı da elde yok.

Dal: `t86-olcu-yalitimi` sürdürülür.

## H1 — Ek madde uygulanmadı, zamanlama duruyor (KRİTİK)

`tests/VidShrink.Tests/UpdaterTests.cs:1076-1083` bırakma iş parçacığı hâlâ sabit
bir sayaca bakıyor:

    while (!File.Exists(started) && DateTime.UtcNow < deadline) Thread.Sleep(20);
    Thread.Sleep(300);
    stream.Dispose();

`.basladi` işareti `Remove-InstallRoot` **çağrılmadan önce** yazılıyor
(`UpdaterTests.cs:982` civarındaki `RemovalProbe` betiği: `Set-Content ... '.basladi'`
satırı `Remove-InstallRoot` satırından önce). Yani
`Assert.Contains("yeniden denenecek", log)` yalnız ilk `Remove-Item` denemesi işaretin
ardından 300 ms içinde düşerse tutuyor. Kilit ölçünün elinde değil, sabit sayacın
elinde.

Gerçekçi girdi: yüklü ya da yavaş bir koşucuda işaret ile ilk `Remove-Item` arası
300 ms'yi aşar. Kilit serbest kalır, silme ilk denemede başarır, günlükte `BITTI`
var `yeniden denenecek` yok → ölçü kırmızı. Bu, Ek madde'de alıntılanan CI koşumu
33502024892'nin düşüşünün ta kendisi. Sözleşme "zamanlamaya değil duruma bakılsın"
diyordu; bakılmıyor.

Diff'in eklediği `if (File.Exists(started)) File.Delete(started)` (`:1075`) ölü kod:
`logPath` `_root`'tan türüyor, `_root` her örnekte taze `Guid` içeriyor
(`UpdaterTests.cs:33`), bayat işaret hiç oluşamaz. Raporun
(`docs/olcumler/surecler-arasi-olcu-yalitimi.md:10`) "eski başlangıç işaretinin kilidi
erken bıraktırmaması için" gerekçesinin kodda karşılığı yok — düzeltilir.

**Gereken:** kilit, günlükte `yeniden denenecek` görülene kadar (ya da silme
denemesinin gerçekten düştüğünü söyleyen bir işaret yazılana kadar) tutulur, sonra
bırakılır. Duruma bakılır, süreye değil. Yeniden deneme yolunun ölçüldüğü korunur;
gevşetilmez, `Skip`e alınmaz.

CHECK: `Thread.Sleep` ile kurulan hiçbir pay kalmaz; `rg "Sleep\(3" tests/VidShrink.Tests/UpdaterTests.cs`
boş döner. Ölçü arka arkaya üç koşumda yeşil ve **üç sonuç satırı da** rapora girer.

### Taze kanıt — 1 Eylül 15:38, CI koşumu 33525962057

Koşum kapısı CI'a bağlandıktan sonraki ilk koşumda bu ölçü yine düştü, İngilizce
çıktıda:

    UpdaterTests.TheDeletionStepWaitsOutATransientLock [FAIL]
    Assert.Contains() Failure: Sub-string not found
    String:    "BITTI\r\n"
    Not found: "yeniden denenecek"
    Standard Output Messages:
     geçici kilit: çıkış 0, 874 ms

    Failed! - Failed: 1, Passed: 910, Skipped: 72, Total: 983, Duration: 9 m 36 s

874 ms — kilit ilk denemeden önce serbest kalmış.

**Ölçü kararsız, sürekli kırmızı değil — ve bu daha kötü.** Aynı `main` üzerinde:

| Koşum | Commit | Saat | Sonuç |
|---|---|---|---|
| 33525962057 | `616feff` | 15:28:37 | başarısız |
| 33526053553 | `7e006bf` | 15:29:28 | başarılı |
| 33526684668 | `43a2e5c` | 15:35:43 | başarılı |
| 33528545164 | `2db300c` | 15:53:44 | başarılı |

Kırmızı ile ilk yeşil arasında **51 saniye** var ve o commit yalnız
`.claude/relay/` altına dokundu — `tests/` ve `src/` arasında tek satır fark yok.
Yani ölçüyü değiştiren şey kod değil, koşucunun o andaki yükü.

Sürekli kırmızı bir ölçü işi durdurur, fark edilir. Kararsız ölçü **her yeşil
okumayı şüpheli yapar**: bu tur kapanana kadar `main`de alınan hiçbir yeşil,
ölçünün gerçekten geçtiğini değil, o koşumda şanslı düştüğünü gösterir.

Aynı koşum kapının kendisini de doğruladı: kapı `Başarısız/Failed özeti yok veya
sıfır değil.` deyip adımı düşürdü. Kapı çalışıyor.


## H2 — Altı koşumun sonuç satırı yapıştırılmamış, yeniden yazılmış

`docs/olcumler/surecler-arasi-olcu-yalitimi.md:38-43` altı satırı
`Süre: 669,1 sn` biçiminde veriyor. `dotnet test` bu depoda süreyi hiçbir zaman
ondalık saniye yazmıyor; aynı dosyanın 51. satırı gerçek biçimi taşıyor
(`Süre: 7 m 52 s`), T85'in raporundaki on beş sonuç satırı da öyle. Kriter 2
"altı sonuç satırını **yapıştır**" diyor; yapıştırılmamış, elle kurulmuş.

Bu, sayıların yanlış olduğunu söylemiyor — **doğrulanamaz** olduğunu söylüyor.
Ölçüm raporunda araç çıktısı yeniden yazılmaz.

**Gereken:** altı koşum yeniden yapılır ve sonuç satırları araç çıktısından
olduğu gibi yapıştırılır. Koşumun ham günlüğü `.calisma/` altında tutulur ve
rapordan hangi dosyadan alındığı tek satırla söylenir.

CHECK: `docs/olcumler/surecler-arasi-olcu-yalitimi.md` içindeki altı satırın biçimi
aynı dosyanın öteki `dotnet test` satırlarıyla birebir aynı.

## H3 — `ShellMenuTests` yalıtımı zaten vardı

`tests/VidShrink.Tests/ShellMenuTests.cs:51`'e PID eklendi, ama `main`de kök zaten
`HKCU:\Software\VidShrink-Test-<GUID>` idi: GUID süreçler arası çakışmayı önlüyordu.
PID eklemek yalıtımı değiştirmiyor.

Yani sözleşmenin bağlamında yazılı olan 2 numaralı kusurun
(`New-Item` `IOException`) gerçek nedeni ya bu değildi ya hâlâ duruyor.

**Gereken:** `IOException`ın hangi paylaşılan kaynaktan geldiği bulunur ve tek
cümleyle yazılır. Bulunamıyorsa "bulunamadı" yazılır ve mutasyonla gösterilir ki
bugünkü ölçü o durumu yakalamıyor. Kozmetik değişiklik düzeltme diye raporlanmaz.

## H4 — `Windows11ShellMenuTests` kilidi dar

`tests/VidShrink.Tests/Windows11ShellMenuTests.cs:108` kilit `Local\` ad alanında,
korunan kaynak (AppX kaydı) ise kullanıcı/makine genelinde. Farklı oturumdaki iki
koşum yine çakışır. Ayrıca kilidi tutan süreç çökerse sıradaki `WaitOne`
`AbandonedMutexException` fırlatır ve ölçü sıraya girmek yerine düşer.

`:112-114`'teki `before != "0"` erken dönüşü de duruyor: ölçü sessizce boş geçebilir.

**Gereken:** kilit `Global\` ad alanına alınır ve `AbandonedMutexException` yakalanıp
sıraya devam edilir. Erken dönüş sessiz kalmaz — atlanıyorsa neden atlandığı çıktıya
yazılır.

## H5 — `TEMP`/`TMP` süreç genelinde değişiyor

`tests/VidShrink.Tests/PerformanceCheckTests.cs:597-598` ortam değişkenlerini süreç
genelinde değiştiriyor. Bugün güvenli, çünkü `tests/VidShrink.Tests/LanguageTests.cs:13`
`DisableTestParallelization = true` diyor. Bu bağımlılık hiçbir yerde yazılı değil;
o satır kalkarsa süitin geri kalanı sessizce yönlendirilmiş `%TEMP%` görür.

**Gereken:** bağımlılık testte yazılı hale gelir — ya paralelliğin kapalı olduğunu
doğrulayan bir iddia, ya da ortam değişkeni yerine yolu doğrudan geçiren bir yol.

## H6 — Koşum kapısının kör noktaları

T85 tur 2 `tools/kosum-kapisi/` kapısını mühürledi ve bu sözleşme onu kullanacak.
Denetim kapıda iki delik buldu; ikisi de bu turda kapanır. `owns` genişletildi.

- Kesinti listesi bu deponun kendi dördüncü koşumunu tanımıyor:
  `Konak işleminden beklenmedik şekilde çıkış yapıldı` ve İngilizce
  `Test host process crashed` desende yok. Bugün yalnız üçüncü şart (düşük `Toplam:`)
  yakaladığı için birinci savunma hattı kör.
- Son eşleşme semantiği: iki özet satırı olan çıktıda ilki `Başarısız: 5`, sonuncusu
  `Başarısız: 0` ise kapı geçiriyor. Doğrusu **herhangi bir** `Başarısız`/`Failed`
  sıfırdan büyükse düşmek.

Üçüncüsü: CI adımında kapının çıkış kodu korunmuyor. Koşum 33525962057'de kapı 66
ile düşmesi gerekirken GitHub adımı `exit code 1` bildirdi. Hangi şartın düştüğü
çıkış kodundan okunamıyor; kapı kodunu koruyacak biçimde çağrılmalı ya da adım
kodu çıktıya da yazmalı.

Ayrıca fixture'ların beşi de tek satırlık elle yazılmış metin; F2 kaydedilmiş
**gerçek** `dotnet test` çıktısı istiyordu. Bu turda alınan koşumların ham çıktısından
en az biri fixture olarak kaydedilir. `kesinti-tr.txt` iki şarttan birden düşüyor
(`Toplam: 958` alt sınırın altında), yani birinci şartı yalıtan bir fixture yok — o da
eklenir.

CHECK: `pwsh -File tools/kosum-kapisi/test-kapi.ps1` yeşil ve yukarıdaki üç durum
için birer fixture var.

## Bu turun kuralları

- Hiçbir assertion gevşetilmez, hiçbir test `Skip`e alınmaz, hiçbir beklenti ölçümün
  kendi çıktısından türetilmez.
- Araç çıktısı **yeniden yazılmaz**; rapora olduğu gibi yapıştırılır ve ham günlüğün
  yolu yazılır.
- Her düzeltme için mutasyon; sonuç rapora girer.
- Tam süit kapıdan geçirilir. Çıktıda kesinti satırı varsa çıkış kodu 0 ve
  `Başarısız: 0` olsa bile koşum yarımdır.
- İtmeden önce `gh run list --branch t86-olcu-yalitimi` koş.
