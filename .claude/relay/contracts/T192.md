---
id: T192
title: Karelerde gorunen arayuz kusurlari
role: builder
model: opus
status: open
depends: []
owns:
  - src/VidShrink.App/Locales/**
  - src/VidShrink.App/Text/**
  - src/VidShrink.App/MainWindow.axaml
  - src/VidShrink.App/MainWindow.axaml.cs
  - src/VidShrink.App/LanguageCatalog.cs
  - tests/VidShrink.Tests/CasingTests.cs
  - tests/VidShrink.Tests/ChipTests.cs
  - tests/VidShrink.Tests/LanguageTests.cs
  - tests/VidShrink.Tests/BiciminTests.cs
  - docs/olcumler/kare-kusurlari.md
branch: T192-kare-kusurlari
---

## Amac

T189 depoya bir cekim duzenegi koydu (`tools/VidShrink.Shot`) ve on dort kare uretti.
T0 kareleri gozle inceledi; README'ye girmeden once duzeltilmesi gereken kusurlar
gorundu. Kaynak: `docs/gorseller/T189-kucult-en.png`.

## Baglam — T0'in gozlemi (8 Eylul 2026)

Hepsi **Ingilizce** karede gorunuyor:

1. **Sayi bicimi Turkce kalmis.** `15,6 MB`, `15,4 - 15,8 MB`, `%3,7`. Ingilizce
   arayuzde `15.6 MB` ve `3.7% of source` olmali. Ondalik ayirici ve yuzde isaretinin
   yeri kulturle degisiyor; bir yerde sabit `tr-TR` ya da sabit bicim dizesi var.
2. **Baslik kurali govde cumlesine uygulaniyor.** Karsilastirma panelinde
   `Load A File To See The Two Sides` yaziyor; bu bir cumle, baslik degil.
   `What It Will Do` ve `Quality And Compatibility` de ayni desende.
   `fd6fe0c1` bu kurali bir kez duzeltmisti ama bu satirlara ulasmamis — **once o
   commit'in ne yaptigini oku**, ayni cozumu genislet, ikinci bir mekanizma kurma.
3. **Kesilen satir.** `Cropping And Resolution` satirinin degeri panel kenarinda
   kirpiliyor: `May Lower Resolution · May Low…`.
4. **Ozet ile kutu uyusmuyor (dogrulanmadi).** Hedef kutusu `24` gosterirken altindaki
   ozet satiri `Target 16 MB` diyor. T0 bunu **olcmedi**; once gercek mi diye olc,
   gercek degilse "kusur degil" diye yaz ve gec.

## Kabul kriteri

### K1 — Once olc

`docs/olcumler/kare-kusurlari.md`: dort maddenin her biri icin kok neden, **hangi dosya
hangi satir**, ve ham grep/test ciktisi. Kusur olmayana "kusur degil" yaz, gerekcesiyle.

### K2 — Sayi bicimi kulturden geliyor

Ingilizce arayuzde `15.6 MB` ve nokta ondalik; Turkce arayuzde `15,6 MB` ve virgul.
Yuzde de kulture uyar.

CHECK: iki dil icin bicimlendirmeyi cagiran bir test; `Assert.Equal(<sabit>, <sabit>)`
degil, gercek bicimlendirici uzerinden

### K3 — Baslik kurali cumleye uygulanmiyor

Karsilastirma panelinin bos hali ve benzeri govde cumleleri dil dosyasindaki gibi kalir.
`fd6fe0c1`'in kurdugu mekanizma genisletilir, yenisi kurulmaz.

CHECK: bu satirlari kapsayan bir test; kirmizi/yesil ikisi de gosterilir

### K4 — Kesilen satir kesilmiyor

`Cropping And Resolution` degeri panel icinde tam gorunur ya da duzgun kisalir (ucnokta
tasarim geregi ise oyle yazilir). Renk ve olcu yalniz `Themes/Theme.axaml`
belirteclerinden; **yeni belirtec uydurma**.

### K5 — Kareler yenilendi

Duzeltmelerden sonra `dotnet run --project tools/VidShrink.Shot` kosulur ve
`docs/gorseller/T189-*.png` guncellenir. Yeni kare eklemiyorsun, var olanlari
tazeliyorsun.

**Dikkat:** `tools/VidShrink.Shot` ve `docs/gorseller/**` `owns` listende **yok** ve
bu bilerek boyle. T189 ayri bir dalda ve henuz birlesmedi. Duzeltmelerini yaz, kareleri
**yenileme** — T0 iki dali birlestirdikten sonra duzenegi kendisi kosturacak. Bu
kriteri raporunda "T0'a birakildi" diye isaretle.

### K6 — Layout pimleri

Bu depoda pinlenmis yerlesim testleri var (`WindowLayoutTests`, `AyarYuzeyiTests`,
`QualityTargetUiTests`). Metin uzunlugu degisirse kirilabilirler. Kirilan pimi
**susturma**; yerlesim gercekten degistiyse pimi yeniden temellendir ve **neden
degistigini** olcum belgesine yaz.

CHECK: `dotnet test --filter "FullyQualifiedName~WindowLayoutTests|FullyQualifiedName~AyarYuzeyiTests|FullyQualifiedName~QualityTargetUiTests"` yesil

### K7 — Temizlik

Kod yorumu (`//`) yok; `///` serbest. BOM yok. `owns` disina yazilmaz.

## Cikti

Kosulan komutlar ve ham cikti. Olcemedigin yere "olcemedim" yaz.

## T0 karari — owns genisletildi (8 Eylul 2026)

Sozlesme kendi kendisiyle celisiyordu: K2 sayi bicimlemesini istiyor, bicimleme
`MainWindow.axaml.cs` icinde (`Num`, `Percent`, `RefreshEstimateView`, `HumanDuration`),
ama `owns` yalniz `MainWindow.axaml` diyordu. K3 ayni sekilde `fd6fe0c1`'in mekanizmasini
genisletmeyi istiyor; o mekanizma `LanguageCatalog.cs` icinde.

**Hata sozlesmede.** T0 `owns`a `MainWindow.axaml.cs`, `LanguageCatalog.cs` ve
degistirilen uc test dosyasini ekledi. Yapicinin WIP commit'indeki degisiklikler geri
alinmaz; oldugu yerden surulur.

## Tur 2 — T0 karari (8 Eylul 2026)

Tur 1 denetimi **KALDI, 2 kritik**. Denetcinin ham ciktilari `.calisma/T192-denetim/`.
Gecen ve kapanan: K4 tasma olcusu, K6 pimleri, K7, `owns` temizligi, "ikinci mekanizma
kurulmadi" iddiasi, tek kirmizinin T192 oncesinden geldigi. Bunlari yeniden acma.

### K8 — K3'un kapsami olculmemis (KRITIK)

Rapor `docs/olcumler/kare-kusurlari.md:60-65` dort satirlik bir tablo veriyor ve
"4 gercek baslik bozulmadi" diyor. Denetcinin supurmesi (`k3-supurme.txt:1-2`):

```
kaydi degisen (baslik kolundan govde koluna gecen) metin sayisi: 124
bunlardan gorunur cikti farki olanlar: 123
```

950 dil anahtarinin **124'u** (en 88, tr 36) `CarriesFunctionWord` yuzunden kol
degistiriyor. Yan etkinin olcusu ne belgede ne testte var.

Kanit bagimsiz: teslim **uc test dosyasinda yedi beklenen degeri yeniden temellendirdi**
(`CasingTests` 5 satir, `ChipTests`, `LanguageTests`) — `"Target Is 16 MB"` ->
`"Target is 16 MB"`, `Why These Choices` -> `Why these choices`, `Back To The Start` ->
`Back to the start`. Belgede bu rebaseline'larin **tek satiri yok**; buna karsilik belge
satir 196-197'de "hicbiri yeniden temellendirilmedi" cumlesini tasiyor. O cumle K6
pimleri icin dogru ama okuyan genele yayiyor. **Imza kusuru: tablo dogru, onu ozetleyen
cumle yanlis.**

Surpriz kalemler K5'te T0'in yenileyecegi karelerde gorunecek:
`main.action.show-in-folder`, `main.drop.title`, `main.chip.whatsapp.label`,
`main.chip.128.label`.

Yapilacak:

1. Kapsami **gercek `LanguageCatalog.Title()` uzerinden** olc — denetci simulasyonla
   olctu, gercek yoldan dogrulayamadi (makinede .NET assembly'si yuklenemedi). Sen
   yukleyebilirsin: dil dosyasinin butun degerlerini gezen bir test yaz, kol degistiren
   her anahtari sayip listele.
2. Sayiyi belgeye yaz: kac anahtar, hangi dilde kac, kaci gorunur cikti farki uretiyor.
3. Yedi rebaseline'i belgede **tek tek** gerekcelendir: eski deger yanlis miydi, yeni
   deger dogru mu, hangi kural.
4. Satir 196-197'deki cumleyi kapsamiyla sinirla.
5. Yuz yirmi dordun icinde **istenmeyen** olan var mi? Ozellikle dort surpriz kalemi
   gozden gecir; biri yanlissa `CarriesFunctionWord`in kurali daraltilir.

CHECK: gercek `Title()` uzerinden gezen supurme testi ve ham sayisi; yedi rebaseline'in
gerekce tablosu

### K9 — `TxtFps` hala sabit invariant (KRITIK)

`MainWindow.axaml.cs:2586`:

```csharp
TxtFps.Text = info.Fps.ToString("0.##", CultureInfo.InvariantCulture);
```

Kaynak bilgi kutusundaki Kare Hizi Turkce arayuzde `29.97` (nokta) yaziyor. Belge
satir 26-27 "Ayni desen `HumanDuration`, `TxtSize`, `TxtOutSize`, `TxtEstimateNote` ve
kalite satirlarinda da vardi; **hepsi ayni gecide baglandi**" diyor. `TxtFps` ne
baglanmis ne "olcemedim" diye yazilmis. K2'nin kabul kriterine kismi aykiri.

Baglanmayan baska yer kaldi mi — `CultureInfo.InvariantCulture` gecen her satiri tara,
her biri icin ya bagla ya **neden invariant kaldigini yaz**. Kutular (`TxtTarget`,
`TxtQualityTarget`, `TxtQuality`) bilerek invariant olabilir (parse simetrisi); oyleyse
gerekcesi belgeye girer, bu da tur 1'in borc 5'i.

CHECK: `grep -n InvariantCulture src/VidShrink.App/MainWindow.axaml.cs` ciktisi ve satir
basina karar

### K10 — Tur 1 borclari

1. **InfoGrid "kusur degil" karari mutasyona olu.** Etiketlerde `TextWrapping="Wrap"` var
   (`MainWindow.axaml:262-291`), olcu `label.TextLayout.Width > cell.Bounds.Width`
   bakiyor — sarilan metnin layout genisligi hucreyi **hic asamaz**. Karar bugun dogru
   (112,6 < 140, sarma tetiklenmiyor) ama koruma yok. Olcuyu mutasyonla oldurulebilir
   hale getir ya da neden getirilemedigini yaz.
2. **1040 px olcumu teslimde yok.** `KareYerlesimTests.WindowSize` sabit `1600x1000`;
   belge satir 132-133'un "en dar 1040x720'de de ayni" iddiasi yalniz elle degistirilip
   kosulan bir dosyayla duruyor, **tekrarlanamaz**. Genisligi parametreye cevir ya da
   iddiayi geri al.
3. **`TahminAraligiSatiriDileUyar` gercek satiri uretmiyor** — format dizesini test icinde
   kopyaliyor (`BiciminTests.cs:84-88`), `RefreshEstimateView` cagrilmiyor. Rapor
   tablosu (satir 40) bunu "tahmin araligi satiri" diye sunuyor; ekran yolu olculmemis.
   Ya gercek yolu kos ya tabloda ne olctugunu duzelt.
4. **"Duzeltmelerden once" tabani `main` degil.** `test-taban.txt` WIP'in kismen geri
   alinmis hali uzerinde kosmus (yeni `BiciminTests` orada zaten var); belge satir 173
   bunu "once" diye etiketliyor. Tabani `origin/main` uzerinde yeniden kos ya da etiketi
   duzelt.

### Bilinmesi gereken

Denetcinin kendi tam suiti `a843711` uzerinde **1928 gecti / 3 kaldi** verdi; fazladan iki
kirmizi ortam kaynakli (tek basina yesil, temp klasorunde dosya kilidi). Teslimdeki
1930/1 sayisi bu yuzden makineye bagli; belgede bu **kosula** baglanir.

K5 (kareler) yine T0'in isi, dokunma.

## Tur 3 — T0 karari (8 Eylul 2026)

Denetim **KALDI, 1 kritik**. Denetci temiz klonda (`4c8246d`) her sayiyi kendi kosumundan
uretti. Tur 2'nin sayilarinin **tamami tuttu** — 124 (en 88 / tr 36), gorunur fark 123,
124-123'un tek anahtari (`tr/main.convert.drop` = `At`), 27 kisa kalem, K10 kirmizi taban
35/16/19 **ayni test adlariyla**, K6 pimleri 55/55, yedi yeniden temellendirme. Tur 1'in
kusuru (kapsamsiz cumle) K6'da kapanmis.

Kalan kritik, tur 2'nin **cevaplamadigi** soruda: 124 kalem gozden gecirildi denmis, 27'si
okunmus.

### K11 — Ad yazimi cumle ortasinda kayboluyor (KRITIK)

`docs/olcumler/kare-kusurlari.md:146` basligi **"Istenmeyen var mi — 124'un gozden
gecirilmesi"**, govdesi (`:148`) **"`<=3` sozcukluk 27 kalemin tamami elle okundu"**.
Okunmayan 96 kalemin **icinde uc gerileme var** (denetcinin `isim-kaybi.py` taramasi,
123 ciftin sozcuk sozcuk karsilastirmasi):

| Anahtar | Once | Simdi |
|---------|------|-------|
| `en / main.drop.hint` | `Any Format FFmpeg Can Open` | `Any format **ffmpeg** can open` |
| `en / main.reason.encoder-fallback-not-in-build` | `...Part Of This FFmpeg Build...` | `...part of this **ffmpeg** build...` |
| `tr / main.reason.encoder-fallback-not-in-build` | `...Bu FFmpeg Derlemesinde Yok...` | `...bu **ffmpeg** derlemesinde yok...` |

Kok neden `LanguageCatalog.cs`: `Names` sozlugu (`["ffmpeg"] = "FFmpeg"`) yalniz
`CapitaliseWord` icinde uygulaniyor, `Sentence()` ise onu **sadece satir basi sozcugu**
icin cagiriyor:

```
:179   builder.Append(lineStart ? CapitaliseWord(word, culture, true) : word);
```

Govde koluna gecen her metinde ilk sozcuk disindaki `ffmpeg` / `crf` / `mp4` / `gpu`
dil dosyasindaki yazimiyla kaliyor.

Asimetri kaniti: `tr / main.drop.hint` dil dosyasinda `Ffmpeg'in` yaziyor ve cikti
`FFmpeg'in` — ilk sozcuk, `Names` uygulaniyor. **Ayni sozcuk** `main.reason.encoder-
fallback-not-in-build` icinde ortada oldugu icin `ffmpeg` kaliyor. Kural metnin neresinde
durduguna gore farkli davraniyor.

Bu, `CasingTests.UnitsAndEncoderNamesKeepTheirSpelling`'in belgelenmis amacina
("kodlayici/ad yazimi bozulmaz") **dogrudan aykiri** ve K8 madde 5'in sordugu sorunun
cevabi. Belgenin verdigi cevap "istenmeyen yok, tek sinirda kalem `main.retry.title`";
dogru cevap **uc kalem**.

Cozum `CarriesFunctionWord`u daraltmak **degil** — denetci daraltmanin `main.plan.title`
kusurunu geri getirdigini bagimsiz dogruladi (`*.title` dislansa `What It Will Do` doner ve
`BiciminTests.GovdeCumlesiDilDosyasindakiGibiKalir(main.plan.title, false)` kirmiziya
doner). Cozum: **`Sentence()` icinde `Names` gecidini her sozcuge uygula.**

Sonra 124'un tamamini yeniden supur ve **hepsini** oku, 27'sini degil. Basligin kapsamiyla
govdenin kapsami esit olsun.

### K12 — Borclar

1. **`:60` bayat.** "`grep -n InvariantCulture MainWindow.axaml.cs` **otuz alti** satir
   veriyor" — teslim ucunda **33**. `k9-invariant-grep.txt` duzeltmelerden **once**
   alinmis; baglanan uc yer (1655, 3305, 3306) hala icinde. Satir numaralari dogru, sayi
   yanlis. Yeniden say.
2. **Karar tablosu eksik:** `:3742` `TimeSpan.TryParse(text, InvariantCulture, ...)`
   (`:3662`deki `mm:ss` yazimin okuma esi, zararsiz) ve `:570` tabloda yok. Belge "her
   `InvariantCulture` satirinin kararini tek tek yaziyor" diyor; ikisini de ekle.
3. **`:148-157` iki kumeyi karistiriyor.** "27 kalemin tamami okundu" dedikten sonra
   listelenen bes kalemin ucu (`main.drop.title`, `main.chip.128.label`,
   `main.chip.180.label`) **4+ sozcuk**, yani o 27'nin icinde degil. Ayir.
4. **Ham cikti disiplini.** `test-son-tur2.txt` ve `tek-kirmizi-tur2.txt` `tail`le
   kirpilmis, ikisinde de basarisiz testin **adi yok**. Belge bunu durustce "okuyamadim"
   diye yaziyor ama kanit dosyasi olmanin isini gormuyor. Tam cikti yaz.
5. **Kirmizinin adi artik biliniyor** (denetcinin kosumu, 1960/1939/2/19):
   `OynaticiBoruTests_DecoderPipe.Oldurulemeyen_surec_icin_KillTree_basarisiz_bildirir`
   — tek basina da kirmizi, `Win32Exception: Erisim engellendi`, yukseltilmemis oturum;
   ve `PanelHostTests.Parca_hazir_olunca_sag_yari_parcayi_gosterir` — **tek basina yesil**,
   yuk kaynakli. Ikisi de T192 disi. Yapicinin cikarimi dogru cikti; belgeye olculmus
   haliyle yaz.

### K13 — Ayri is olarak kaydedilir, bu turda kosulmaz

`ShrinkJobWindow.axaml.cs:274` — `result.OutputMb.ToString("0.0", CultureInfo.InvariantCulture)`,
`Say("main.run.over-ceiling", ...)` icine giriyor. **Kullaniciya giden ondalik, hala
invariant**: K2'nin kapattigi kusurun aynisi komsu pencerede. `owns` disinda ve K9'un
CHECK'i `MainWindow.axaml.cs` diyor — sozlesme ihlali degil, borc.

Kabul edilen mesru borclar: §5 px tablosu tur 1'den (kosulun kendisi tur 2'de yeniden
olculmus, dort kosumda tasma yok), kalite tavani bildirimi ekranda olculmemis. Denetci
ikisini de "teslimi zayiflatmiyor" diye isaretledi.

CHECK: uc gerilemenin ucu de kapali; 124 kalemin **tamami** okunmus ve belgede kapsam
basliga esit; `dotnet test -c Release`'in tam ciktisi kirpilmadan kanit dosyasinda,
basarisiz testlerin adiyla.

## Tur 4 — T0 karari (8 Eylul 2026)

Denetim **GECTI, 0 kritik**. Uc gerilemenin ucu de kapali (denetcinin kendi dokum farki),
yeni olcu **canli** (`KnownSpelling(word) ?? word` → `word` mutasyonu
`AdVeBirimYazimiCumleOrtasindaDaKorunur`i kirmiziya dusurdu: `gezilen 950 / kayip 15`),
yeniden temellendirme **susturma degil** (denetci govde kolunu tam baslik kuralina cevirdi,
test en'de 3819 tr'de 2505 sozcukle patladi).

Butun sayilar tuttu: 124 (88/36), 28/96, 15'in 3'u 124 icinde ve 12'si disinda, 33
`InvariantCulture`, suit 1961/1942/1/18, kirmizinin adi.

Bu tur **iki borcu** kapatir. Ikisinin de duzeltmesi denetci tarafindan bulunmus ve
dogrulanmis; yeni tasarim karari gerektirmiyor.

### K14 — `IsLetterOrDigit` degisikligi pimsiz (en buyuk borc)

`LanguageCatalog.cs:190` `IsLetterOrDigit` → `IsLetter` mutasyonunda
**135 testin 135'i yesil**, yerlesim pimlerinde 69/69 yesil. Sozlesmenin istemedigi,
**35 anahtarin** ekran ciktisini degistiren duzeltmeyi hicbir olcu tutmuyor.

Sebep `tests/VidShrink.Tests/LanguageTests.cs:1110`:

```csharp
if (!basAtlandi && kaynakSozcukler[i].Any(char.IsLetter))
```

Test kendi satir-basi atlamasinda **uretimin biraktigi yuklemi** kullaniyor. `{0}` harf
tasimadigi icin atlanmiyor, ondan sonraki sozcuk atlaniyor — degisikligin dokundugu tek
sozcuk tam da olcunun kor noktasinda.

Denetci duzeltmeyi dogruladi: `:1110`u `IsLetterOrDigit` yapinca dogru uretimde yesil (2/2),
mutasyonlu uretimde kirmizi (en 14 + tr 25 = 39 sozcuk). **Tek karakterlik borc.**

Yapilacak: duzeltmeyi koy, mutasyonu kendin kos ve kirmizinin sayisini belgeye yaz.

### K15 — "44" aslinda 49 (imza kusurunun kucultulmus hali)

`docs/olcumler/kare-kusurlari.md:247`. Kok neden: `TumCiktiDokulur`
(`tests/VidShrink.Tests/BiciminTests.cs:557`) `ReplaceLineEndings` **cagirmiyor** —
`KolDegistirenAnahtarlarSayilir`de duzeltilmis, burada degil. Dokum dosyalari her degerin
**yalniz ilk satirini** tutuyor; devam satirlarindaki degisiklikler gorunmuyor.

44, denetcinin 49'unun tam alt kumesi. Eksik bes satir:

```
en  main.convert.audio-bitrate.tip   "• 128 Is a compact..."  -> "• 128 is a compact..."
en  main.convert.frame-rate.tip      "• 60 Is smoother"       -> "• 60 is smoother"  (+30, +24)
tr  main.convert.audio-bitrate.tip   "• 128 Kompakt genel"    -> "• 128 kompakt genel"
tr  main.convert.custom-fps.tip      "• 25 Veya 29.97"        -> "• 25 veya 29.97"
tr  main.convert.frame-rate.tip      "• 60 Daha akici"        -> "• 60 daha akici"  (+30, +24)
```

Denetci besini de okudu: **hicbiri istenmeyen yonde degil**, hepsi duzelme. Kritik olmamasi
bundan. Ama `:254`teki "44 satirin tamami elle okundu" cumlesi icin o bes satir okunan
dosyada **hic yoktu**. Alt sayilar (15 = en 8/tr 7, 29 = en 10/tr 19) birebir tuttu; toplami
ve kapsam cumlesi tutmadi.

Yapilacak: `TumCiktiDokulur`e `ReplaceLineEndings` ekle, dokumu yeniden uret, sayiyi 49'a
cek, bes satiri belgeye yaz.

### K16 — Iki cumle kapsamina cekilir

1. `:246` basligi "Yer tutucudan sonraki sozcuk" diyor; `IsLetterOrDigit` **ciplak sayiyi da**
   satir basi yapiyor ve 49'un 6'si (en 2 / tr 4) yer tutucu degil **rakam** kaynakli. Govde
   metni (`:233-235`) "harf ya da rakam" diyor, baslik ve tablo demiyor. Esitle.
2. `:276` "Geri kalan kalemlerin hepsi ayni desende: `main.reason.*`, `main.error.*`,
   `playback.panel.*`. Hicbiri baslik degil" — kalanin icinde bu uc ailede **olmayan** alti
   `.title`/`.label` anahtari var: `en|tr/main.about.engine.title`, `en/main.about.loss.title`,
   `en/main.advanced.min-fps.label`, `en/main.advanced.min-resolution.label`,
   `en/settings-tab.advanced-default-open.label`. Denetci altisini da okudu, hepsi dogru
   cumle biciminde — **hukum dogru, cumlenin kapsami eksik**. Duzelt. `:269` "sinirdaki tek
   kalem `main.retry.title`" derken iki taban etiketi de sinirda sayilabilirdi; onu da yaz.

CHECK: `LanguageTests.cs:1110` duzeltmesiyle `IsLetter` mutasyonu **kirmizi**; dokum
`ReplaceLineEndings` sonrasi 49 satir veriyor; `dotnet test -c Release` tam ciktisi
kirpilmadan kanit dosyasinda.

## Tur 5 — T0 karari (8 Eylul 2026)

Tur 4 **kaldi, 1 kritik.** K14 ve K15 kapandi ve bagimsiz olculdu: pim tutuyor (mutasyon
en 14 + tr 25 = 39 sozcuk kirmizi, dogru uretimde 2/2 yesil), dokum `ReplaceLineEndings`
sonrasi 950 anahtarin 66'sini cok satirli veriyor ve 49 anahtar listesi denetcinin
bagimsiz listesiyle 49/49 birebir. Uretim diff'i sifir satir.

Kritik olan **K16'nin kendisi**: duzeltmek icin actigim cumle hala kapsamsiz. Bu deponun
imza kusuru bu sozlesmede ucuncu kez ayni yerde tekrar ediyor.

### K17 — kritik: `kare-kusurlari.md:319-321` olcuye esit degil

Cumle su anda soyle diyor: "Geri kalanin buyuk cogunlugu ayni desende: `main.reason.*`,
`main.error.*`, `playback.panel.*` ... Bu uc ailenin **disinda** alti kalem var."

Denetcinin olcumu (124'luk kumeyi bagimsiz yeniden uretti, en 88 / tr 36 — pimle ayni):

- uc aile toplami: **40** (124'un %32'si)
- belgenin "geri kalan"i: **108**
- 108'in uc aile disinda kalani: **68** — cumle bunu "alti" diye yaziyor
- dar okumayla, `.title`/`.label` ile biten ve uc aile disinda olan: **13**; tablo **6**
  listeliyor (`en / main.plan.title` tabloda yok, baska bolumde geciyor)

Uc cumleyi de olcuye esitle. Kopyalanacak sayilar: **124 / 40 / 108 / 68 / 13**. Tablonun
alti kaleminin ne oldugunu dogru soyle: "uc ailenin disindaki butun kalemler" degil,
"adi `.title`/`.label` ile biten ve bu bolumde ayrica tartisilmayan kalemler".

Hukmun kendisi (**hicbiri baslik degil**) dogru ve pimli — `DilDosyasindakiButun...` olcusu
950 anahtar uzerinde tutuyor. Hukmu degistirme, yalniz kapsam cumlesini.

**Bitirmeden once belgedeki her sayisal cumleyi tek tek oku ve dayandigi olcuyle karsilastir.**
Uc turdur ayni sinif kusur cikiyor; bu sefer cumleleri saymadan teslim etme.

### K18 — kanit cumlesi yanlis

`:509` "`git diff origin/main...HEAD -- src tests | grep '^\+\s*//'` **bos**" diyor; komut
**1 satir** donuyor (`+            // A digit before the first letter means...`). Hukum
dogru — main'de 5 yorum satiri var, `da72a19`'da da 5, yani yeni yorum eklenmedi, satir
`KnownSpelling` ayrilirken tasindi. Cumleyi komutun gercek ciktisina esitle.

### K19 — invariant tablosu

`:84-89`: `grep -c InvariantCulture` = 33 dogru, ama tablo 33 satir icin **37 numara**
tasiyor; birinci kumenin dort numarasi (1655, 2586, 3305-3306) artik `InvariantCulture`
icermiyor. Numaralari yeniden uret.

### K20 — kucuk

- `:350` "135 testin 135'i yesil" — hangi suzgecle kosuldugu yazili degil, belgeden
  tekrarlanamaz. Suzgeci yaz ya da cumleyi kaldir.
- `.calisma/T192-tur4/LanguageCatalog.cs.bak` artik dosya, sil (AGENTS.md).
- Dal `origin/main`den geride (merge-base `d56b98d`, main `cae1725`). `git merge origin/main`
  ile guncelle; sahip olunan dosyalarda cakisma yok.

### CHECK

- `kare-kusurlari.md`'deki her sayisal cumle, dayandigi olcunun kapsamini tam soyluyor;
  hicbir cumle bir alt kumenin olgusunu ust kumeye tasimiyor.
- `git diff origin/main -- src/` **bos** (bu tur de yalniz belge ve testler).
- `dotnet test -c Release` tam ciktisi kirpilmadan kanit dosyasinda; tek kirmizi
  `KillTree` (cevre kaynakli).
