---
id: T114
title: Sahne basina bit dagitimi — harita plana bagli degil
role: builder
model: opus
depends: [T107, T113]
status: active
round: 1
verify: [dotnet test -c Release --filter "PlanCalculatorTests"]
owns: [docs/olcumler/sahne-butcesi.md, tools/sahne-butcesi/00-pencereleri-kes.sh, tools/sahne-butcesi/01-olcumu-kos.sh, tools/sahne-butcesi/02-mutasyon.sh, tools/sahne-butcesi/03-duzenek-mutasyonu.sh, tools/sahne-butcesi/04-kapi-denemesi.sh, tools/sahne-butcesi/05-cikti-denetimi.sh, tools/sahne-butcesi/Butce.cs, tools/sahne-butcesi/DUZENEK.md, tools/sahne-butcesi/ESIKLER.md, tools/sahne-butcesi/Kabuk.cs, tools/sahne-butcesi/Program.cs, tools/sahne-butcesi/Rapor.cs, tools/sahne-butcesi/SahneButcesi.csproj, tools/sahne-butcesi/cikti-denetimi.py, tools/sahne-butcesi/kapi-fikstur.py]
side_effects: []
---

## Bağlam

Kullanıcının koyduğu hedef: **sahne bazlı analiz mekaniği otursun.** Bugün
oturmuş değil ve nerede durduğu ölçülebilir:

`SceneMap` sahne başına iki sayı zaten üretiyor — `Scene.Complexity` ve
`Scene.Bits` (`SceneMap.cs:12-13`). Bunları **hiçbir plan kararı okumuyor.**
Tüm ağaçta `SceneMap`'in tüketicisi iki yer: `QualityMeter.cs:177,219`
(ölçüm) ve `SceneDetector.cs:123-131` (üreticisi). `PlanCalculator` haritayı
görmüyor.

T113 haritayı `FfmpegArguments`'a bağlıyor ama oradaki tüketim **anahtar
kare** kararıdır — kesimin nereye düşeceği. Bit **miktarının** sahneye göre
dağıtılması ayrı bir karar ve hiç yok.

## Bu sozlesme neyi sorar

Tek soru: **kodlayıcının kendi hız denetimi zaten bunu yapıyor mu?**

x265 ve SVT-AV1'in lookahead'i sahne değişimini görüyor ve bit dağıtımını
kendi yapıyor (T112 kaynak okumasında doğrulandı). Bizim haritamızın üstüne
koyacağı bir şey varsa **ölçülerek gösterilmeli.** Yoksa bu iş yapılmaz.

**"Ölçüldü, kodlayıcı zaten daha iyi dağıtıyor" tam ve kabul edilir bir
sonuçtur** ve o durumda kod değişmez. İlkeyi kurtarmak için ölçüyü eğme;
T112 tam bu yüzden yazıldı.

## Kabul kriteri

1. **Bugünkü dağıtımın hatası ölçülür.** Referans: her sahneyi ayrı ayrı,
   sabit kalitede (CRF) kodlayıp o sahnenin **gerçekten hak ettiği** bit
   miktarını bulmak. Bugünkü tek geçişli/iki geçişli plan sahne başına ne
   veriyor? Fark sahneden sahneye ne kadar?
   En az **üç kaynak**, biri sahne yapısı belirgin (kesik çok), biri durgun.
   CHECK: kaynak × sahne tablosu, hak edilen / verilen / fark.

2. **Kodlayıcının kendi dağıtımı ayrı sütun olur.** K1'in "verilen" sütunu
   kodlayıcının kararıdır. Onu bizim haritamızın önerdiği dağıtımla yan yana
   koy. **İkisi aynı yöne bakıyorsa iş burada biter** — bunu yaz ve K5'e geç.
   CHECK: korelasyon + ters düşen sahnelerin listesi.

3. **Dağıtım kuralı `SceneMap`'in kendi sayılarından türetilir.**
   `Scene.Complexity` ve `Scene.Bits` sonda çıktısıdır; yeni bir sonda
   koşumu **eklenmez** (maliyet T96'da zaten %10,4). Kural bu iki sayıdan
   ve sahne süresinden çıkar. Çıkmıyorsa "mevcut sayılar yetmiyor" yaz ve
   neyin gerektiğini söyle — kendi başına yeni bir tarama açma.

4. **Uygulama yolu ölçüyle seçilir.** En az iki aday sınanır:
   - **zones** — x264/x265 `zones=` ile sahne aralığına qp/bitrate çarpanı
   - **iki geçiş yanlılığı** — birinci geçişin istatistiğine dokunmadan
     `qcomp` / `aq-strength` gibi dağıtım parametresini harita istatistiğine
     bağlamak
   Hangisinin K1'deki farkı daha çok kapattığı **ölçüyle** seçilir.
   Kodlayıcı `zones` desteklemiyorsa (av1_nvenc, libsvtav1) bu **yazılır** ve
   o kol için ne yapıldığı ayrıca söylenir.
   CHECK: aday × kodlayıcı ızgarası, destekleniyor mu / sapma ne oldu.

5. **Kazanç kalite seviyesinde gösterilir.** Aynı hedef boyutta, aynı kaynak,
   dağıtımlı ve dağıtımsız: VMAF-NEG ortalama ve **p10**. Asıl sayı p10 —
   sahne dağıtımının vaadi kötü sahneleri kurtarmaktır, ortalamayı yükseltmek
   değil. **En kötü sahnenin puanı ayrıca yazılır.**
   Harmonik ortalamaya yaslanma; T106 onu soruşturuyor.
   CHECK: üç kaynak, öncesi/sonrası, ortalama + p10 + en kötü sahne.

6. **Hedef boyut bozulmaz.** Dağıtım bitleri yeniden bölüştürür, toplamı
   değiştirmez. Her ölçümde gerçekleşen boyut hedef bandın içinde mi?
   **Aşan tek koşum bile varsa oranı yazılır.** Kapalı döngü hedef boyut
   T112'de doğrulanmış tek ayırt edici özelliğimiz — onu riske atan bir
   kazanç kazanç değildir.
   CHECK: koşum başına hedef oranı.

7. **Harita yanlışken ne oluyor?** T109 eşiği içerikten türetiyor ve T105
   ölçtü ki durgun içerikte harita kesim kaçırıyor. Kaçan kesim bit
   dağıtımını iki sahne arasında karıştırır. **Yanlış haritayla dağıtımın
   bedeli ölçülür**: bilerek bozulmuş bir harita (fazladan kesim ve eksik
   kesim, ikisi ayrı) ile aynı kaynak kodlanır, kayıp yazılır.
   Bedeli kazançtan büyükse bu **sonuçtur** ve dağıtım koda girmez.
   CHECK: doğru harita / eksik kesim / fazla kesim, üç satır.

8. **Sabit değişirse üstündeki cümle aynı commit'te değişir.** Bu projenin
   sekiz kez tekrarlayan kusuru: sayı değişiyor, onu özetleyen cümle
   eskiyor. Rapordaki her özet cümlesini tabloya karşı **ayrı bir geçişte**
   oku; sayan geçişle özetleyen geçiş aynı anda olmasın.

9. **Mutasyon kanıtı.** Dağıtım kuralının her parçası — girdi, katsayı,
   kıskaç uçları — iki yönde bozulunca ölçü kırılmalı. İki sabiti
   karşılaştıran ölçü yazma, `Skip` yazma, ffmpeg yokluğunda sessiz erken
   dönüş yazma. ffmpeg gerektiren ölçü `[FfmpegFact]`.

## Sınırlar

- `owns` dışına yazma. `PlanCalculator.cs` ve `CodecModel.cs` T107'nin,
  `FfmpegArguments.cs` T108'in, `SceneMap.cs` / `SceneDetector.cs` T109'un,
  `ComplexityProbe.cs` T103'ün — hepsini **okuyabilirsin.** Yeni mantık
  `SceneBudget.cs` içinde durur; çağrı yerine bağlanması ayrı bir turdur ve
  onu ben açarım. Bağlanmadan ölçüm yapabilmen için düzeneğin planı ve
  dağıtımı kendi içinde birleştirmesi gerekir — düzenek `tools/`ta, üretim
  kodunda telafi yazma.
- **Telafi sabiti yazma.** Bir katsayıyı "böyle olunca sayı tutuyor" diye
  koyuyorsan neyi telafi ettiğini bul; telafiyi değil nedeni düzelt ya da
  nedeni bildir.
- Düzenek `tools/sahne-butcesi/` altına — `owns`'unda, kapı geçirir.
  `.calisma/` gitignore'lu ve worktree-yerel; **oraya koyduğunu denetçi
  göremez.** Rapora giren her sayının üreten komutu yazılı olsun.
- `.calisma/kaynak/` ortak, dokunma.
- Makine paylaşımlı. Kodlama komutlarında iş parçacığını **sabitle**
  (`-threads N`, x265'te `pools`). Süre sayısına "makine paylaşımlıydı"
  damgası bas; kalite ve boyut sayısına basma.
- Teslimden önce **tam süiti** ya da `tools/ci-gibi-kos.sh` koştur. Dar
  `verify` filtresi kabul kriterini ölçer, gerilemeyi ölçmez — T99 bu kuralı
  yazdırdı, T98 aynı gün CI'da düştü.
- Kod yorumu yazma.

## Kayıt noktası

Her kabul kriteri bittiginde commit. Dal: `T114-sahne-butcesi`.

Olcum `origin/main` birlestirildikten **sonra** bastan kosuldu: T107 plan
cozunurlugunu degistirdi (`806x454` -> `1458x820` / `1728x972` / `1920x1080`),
birlestirme oncesi sayilar yanlis commit'i olcuyordu. Olculen sha rapora
yaziliyor.

- [x] **K4** — `zones=` yalniz `libx264` ve `libx265`'te calisiyor.
  `libsvtav1` sessizce yok sayiyor: fark 4830 bayt, tekrar gurultusu 1104
  bayt, ciktinin %1'i 7613 bayt — esigi gecmiyor. `hevc_nvenc` ve
  `av1_nvenc`'te zone parametresi hic yok. `qcomp` uc yazilim kodlayicisinda
  da etkili. Izgara `.calisma/T114/k4-izgara.csv`;
  uretim `SahneButcesi k4 maks p1-karisik`.
- [x] **K3** — kural mevcut harita sayilarindan cikiyor, yeni sonda kosumu
  yok: `Scene.Complexity`, `Scene.Bits`, sahne suresi.
  `gamma = 1 - qcomp = 0,40` telafi sabiti degil. T96'nin %10,4'u artmaz.
- [x] **K1** — 3 kol x 3 pencere bitti. `SahneButcesi k1 <kol> <pencere>`.
  Olculen 8 hucre, `yedek/p2-durgun` **bilinmiyor** (plan hevc passthrough,
  kodlama yok, sahneye bit dagitilmiyor — varsayilana dusurulmedi).
  MAE(verilen/harita, pp): maks 0,50/0,63 - 2,82/2,70 - 1,60/3,26;
  uyumlu 1,55/1,29 - 1,90/2,51 - 0,67/1,11; yedek 1,27/1,03 - bilinmiyor - 0,88/2,10.
  Sekiz hucrenin hepsi ayri bir betikle JSON'dan yeniden hesaplanip tabloyla
  karsilastirildi, birebir tuttu.

- [x] **Tam suit** — birlestirilmis agacta `--no-incremental` derleme + tam
  `dotnet test`: 1318 gecti, 0 kaldi, 18 atlandi (21 dk 11 sn).
- [x] **K2** — **kapi kapanmadi.** Olculen 8 hucrenin 1 tanesinde ucu birden
  saglandi, o da `maks/p3-hareketli` (n=2, sira korelasyonu anlamsiz).
  Kapi basina: rho 8/8, MAE 5/8, ters dusme 3/8. Sayim elle dogrulandi.
  Yani "kodlayici zaten en az bizim kadar iyi dagitiyor" denemez; ama K4 eki
  bunun tersini de gostermiyor — asagiya bak.

- [x] **K3 eki** — kural duzenek icinde denetleniyor (`SahneButcesi dogrula`),
  denetim uc bozucu mutasyonun ucunde de kirildi, temiz agacta gecti.
  **Bulgu: zone carpani araligi dar** — `p1` 0,842..1,279, `p2` 0,944..1,207,
  `p3` 0,999..1,003. `p3-hareketli`de dagitim taban ile ayni dosyayi uretir;
  o pencereden K5 kazanci cikamaz. Kazancin ust siniri bastan dar.
- [ ] **K4 eki** — iki aday ayni planla kosuldu, olcu K1'in MAE'si.
  `uyumlu` uc pencere bitti (taban / zones / qcomp, pp):
  p1 1,552 / 1,553 / 1,546 — p2 1,901 / 2,757 / 2,583 — p3 0,674 / 0,609 / 0,077.
  **Sozlesmenin konusu olan `zones` hicbir hucrede kayda deger kapatma
  yapmiyor**: +0,001, +0,856, -0,065 pp. `yedek` ve `maks` kosuyor
  (`maks` -> `libsvtav1` zones yolu yok, bilinmiyor bekleniyor).
  Uretim `SahneButcesi k4b <kol> <pencere>`.
  `maks` uc pencere de **bilinmiyor**: `zones`i `libsvtav1` yok sayiyor
  (K4 izgarasi), `qcomp`u duzenek olcmedi — izgara qcomp'un libsvtav1'de
  calistigini gosteriyor ama duzenek iki adayi da ayni bayraktan geciriyor.
  Iki sebep csv'de ayri yaziliyor; onceki hali izgarayla celisiyordu.
  `yedek/p1`: taban 1,273 / zones 1,229 / qcomp 1,272 — **`zones` ilk kez
  kazandi, 0,044 pp, K1 aciginin %18'i.** `yedek/p2` passthrough, bilinmiyor.
  Olculen 4 hucrenin tabani gecen 3'unde `zones` 1, `qcomp` 2 kez kazandi.
  Rapor bu ayrimi kendi sayiyor: `qcomp` tek kuresel skaler, sahne basina
  harita bilgisi tasimaz — kazandigi hucre "dagitim ise yariyor" kanitina
  sayilmaz.
- [ ] **Tekrar gurultusu** — `zones`in kazandigi 0,044 pp'nin gurultunun
  ustunde mi altinda mi oldugu ayri olculuyor: `SahneButcesi tekrar yedek
  p1-karisik` ayni plani ikinci kez kosar, sha256 / boyut / MAE'yi yan yana
  yazar. K4 izgarasindaki `kontrol` satiri bayt uzerinden olcuyordu; MAE
  normalize paylardan hesaplandigi icin bayttaki sifir fark pp'de sifir
  demek degil. Kosuyor.
- [x] **Sozlesme basligi duzeltildi** — `verify` filtresindeki
  `SceneBudgetTests` kolu sifir test esliyor ve sessizce geciyordu
  (`SceneBudgetTests.cs` agacta yok, dagitim koda girmedigi icin de
  yazilmayacak); kol filtreden cikarildi, kalan `PlanCalculatorTests`.
  `owns` satirindaki `tools/sahne-butcesi/**` globu `contract.js complete`
  tarafindan reddediliyordu; uretilen 16 dosyanin tam yolu tek tek yazildi.
  Uretilmeyecek `src/VidShrink.Core/SceneBudget.cs` ve
  `tests/VidShrink.Tests/SceneBudgetTests.cs` listeden cikarildi.
- [x] **Karar kodu denemesi** — rapordaki karari veren kod uydurma girdiyle
  kosuldu (`04-kapi-denemesi.sh`): dort sart saglanan girdide "koda girer"
  cikiyor, p10 kaybi / band asimi / K7 bedeli sartlari tek tek bozulunca
  "girmez"e donuyor, dosya yokken "karar verilemedi". Bes senaryo, besi gecti.
- [x] **Dosya tamligi** — kosum bir kez disaridan durduruldu, yarim bir
  `plan-yedek-p3-hareketli.mkv` (20 MB / ~60 MB) birakti; duzenek onu tam
  sayacakti. Kodlamalar artik `.yarim.mkv`e yazilip tasiniyor, her referans
  sahnesinin suresi denetleniyor. Geriye donuk denetim: 116 dosya, sapan 0.
- [ ] **K5/K6** — kosum sirada.
- [ ] **K7** — kosum sirada.
- [ ] **K8** — rapor yazildiktan sonra ayri gecis.
- [ ] **K9** — K5 kapisi gecerse `SceneBudget.cs` + mutasyon kaniti;
  gecmezse "yeni olcu yok, kural duzenekte kaldi".

## Çıktı

- `docs/olcumler/sahne-butcesi.md`: hak edilen/verilen tablosu, kodlayıcının
  kendi dağıtımıyla korelasyon, aday × kodlayıcı ızgarası, p10 ve en kötü
  sahne, hedef boyut oranları, bozuk harita bedeli.
- Ölçüye dayanan sahne bütçesi — ya da "ölçüldü, kodlayıcı zaten daha iyi
  dağıtıyor" ve girmeyen değişiklik.
- Kendi dalın, itilmiş. `main`e sen birleştirme.
