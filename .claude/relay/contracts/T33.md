---
id: T33
title: Oynatma mimarisi — ölçüm kapısı
role: builder
model: opus
depends: [T30]
status: submitted
round: 2
audit: —
auditor_id: —
diff: —
owns:
  - tools/VidShrink.Bench/Program.cs
  - docs/olcumler/T33-oynatma-olcumleri.md
side_effects: []
---

## Amaç

Kullanıcı iki videoyu aynı anda 60+ fps oynatan bir karşılaştırma paneli istiyor. Karar
verildi, tartışma kapalı.

Konsey mimariyi seçti. Bu sözleşme o mimarinin bu makinede gerçekten çalışıp
çalışmadığını ölçer. **Üretim dosyasına tek satır yazılmayacak.**

## Bağlam — konsey ne dedi

`docs/KONSEY-karsilastirma-paneli.md` ikinci turuna bakılacak.

İki üye de bağımsız olarak oynatıcı çekirdeği gömmeyi reddetti ve **ffmpeg'i sürekli kod
çözme borusuna çevirmeyi** önerdi. Kuruluma 0 MB ekliyor, çökme yalıtımı bedava, üç
platformda tek yol.

Opus bir adım öteye gitti ve önerisi seçildi: **tek ffmpeg süreci, iki girdi,
`fps` + `scale` + `hstack` grafiği, `-f rawvideo -pix_fmt bgra` ile boruya çift genişlikte
kare.**

Bu tek hamle üç sorunu kapatıyor: tek yüzey (airspace yok), tek süreç ve tek saat
(senkron pazarlığı yok), kod çözen taraf ayrı süreç (yalıtım duruyor).

**T30'un Ö1 kapısı oynatmayı bağlamıyor.** İkisi de bunu ayrıca söyledi: 775 ms süreç
açılışı ve GoP çözme maliyetiydi, akışta zaman atlaması başına ödeniyor. Oynatma için
bağlayıcı sayı Ö2, yani kodlama yavaşlaması.

**`docs/KONSEY-karsilastirma-paneli.md` §2 "duran kare, oynatma yok" maddesi hükümsüz.**
Aynı belgeye geçersizlik satırı düşülecek — bu sözleşmenin işi değil, T0 yapacak.

## İki aday ölçülecek — biri yedek değil

Konsey boruyu seçti ve mpv'yi yedeğe koydu. Kullanıcı bu sıralamaya itiraz etti ve
itirazı teknik olarak haklı:

> bir video oynatıcı gömmek veya kodlamak için neden bu kadar diretiyorlar, bu ses yok
> 1080p sınırlarından kurtulmuş olurduk

Konseyin gerekçeleri — +60-100 MB, Linux'ta self-contained kaybı, süreç içi çökme —
hepsi **bakımcı tarafındaki** maliyetlerdi ve kullanıcı bunları ödemeyi açıkça kabul etti.
Gerekçe orada biter.

Üstelik iki gerçek üstünlük boru yaklaşımının aleyhine:

- **Bant genişliği.** Boruda ham kareler işlemciden geçiyor; 2×1080p60'ta saniyede ~1 GB.
  mpv render API'si kareleri ekran kartında tutar, o kopya hiç olmaz. Kullanıcının aşmak
  istediği 1080p duvarı **borunun duvarı**.
- **Ses.** Boruda ses yok, çünkü Avalonia'nın ses çıkışı yok. mpv sesi kendi çalar.

Bu yüzden bu sözleşme **iki adayı da ölçer** ve karar sayıya bırakılır. Hangisinin
"yedek" olduğu baştan varsayılmaz.

## Ölçülecekler

### Ö1 — Boru akışının hızı

Tek süreç, iki girdi, `hstack`, BGRA boru.

Üç boyutta: **2×960×540**, **2×1280×720**, **2×1920×1080**.

Ölçülecek: sürdürülen fps, kare aralığı p50/p95/p99, MB/s, işlemci yüzdesi.

### Ö2 — Kodlama koşarken

Aynı ölçüm, `EncodeRunner` çalışırken.

İki sayı: akışın fps'i ne oldu, **kodlamanın kendisi ne kadar yavaşladı.**

T30 ayrı süreç sağanağı için %17,8-28,4 ölçmüştü. Kalıcı tek süreç daha hafif olmalı ama
bu bir tahmin — ölç.

### Ö3 — Donanım kod çözme

`-hwaccel d3d11va` + `hwdownload` grafiği kazandırıyor mu, kaybettiriyor mu?

Konsey uyardı: panel çözünürlüğünde `hwdownload` yazılım çözmeden **pahalı olabilir.**
Varsayma, ölç.

### Ö4 — Arayüze yükleme

Avalonia tarafı kareyi ekrana koyabiliyor mu? Çift tamponlu `WriteableBitmap` ile
atılabilir bir pencerede sunulan fps.

Boru 60 fps üretse de sunum yolu taşıyamıyorsa mimari orada tıkanır.

### Ö5 — Codec matrisi

h264, hevc, av1 × 1080p, 4K.

Hangi kombinasyon 60 fps'i tutturamıyor?

### Ö6 — Kare başına bellek ayırma

Konsey uyardı: 4-16 MB'lık tamponlar kare başına ayrılırsa çöp toplayıcı duraklamaları
60 fps'i öldürür.

Ölç: kare başına ayrılan bayt. Hedef sıfır — sabit havuz, sıfır kopya okuma.

### Ö7 — Aday B: libmpv render API

`OpenGlControlBase` üzerinde `mpv_render_context`, iki örnek, iki doku, bileşimlemeyi biz
yaparız.

Ö1 ile **aynı boyutlarda ve aynı kliplerle** ölçülecek ki karşılaştırma adil olsun:
sürdürülen fps, kare aralığı p50/p95/p99, işlemci yüzdesi.

Ayrıca ölçülecek:

- **Kuruluma eklenen MB** — üç platform için ayrı
- **Linux'ta durum:** dağıtımın `libmpv.so`'suna mı bağlanıyor, yoksa taşınabilir bir ikili
  bulunabiliyor mu? Self-contained sözü korunabiliyor mu, korunamıyorsa ne söylenmeli
- **Lisans:** mpv gövdesi GPLv2+/LGPL karışık. `RAPOR.md:106` shinchiro yapılarının
  lisanssız olduğunu söylüyor — o kaynak kullanılamaz. Hangi ikili kaynağı kullanılabilir,
  lisansı ne
- **Ses:** çalışıyor mu, iki akıştan hangisinin sesi duyulacak
- **4K:** boru yaklaşımının tıkandığı yerde mpv ne yapıyor
- **Çökme:** bozuk bir dosya verildiğinde uygulama düşüyor mu

`VideoView` yaklaşımı **ölçülmeyecek** — airspace yüzünden ayırıcı çizgi kurulamıyor,
bu zaten elenmiş durumda. Ölçülen şey render API'si, yani dokuya çizme.

## Kapılar

| Kapı | Ölçü | Düşerse |
|---|---|---|
| **G1** | 2×960×540'ta ≥60 fps, kare aralığı p99 < 25 ms | Mimari yanlış, vekil dilim yoluna geçilir |
| **G2** | Aynısı 2×1920×1080'de | Yakınlaştırma tavanı ölçüye göre kısılır |
| **G3** | Kodlama koşarken yavaşlama ≤ %10 | Kodlama sırasında oynatma kapatılır |

Kapılar **her iki aday için ayrı ayrı** uygulanacak.

**Karar kuralı:** ikisi de G1-G3'ten geçerse boru seçilir (0 MB, yalıtım, Linux). Boru
kalıp mpv geçerse **mpv seçilir** ve maliyetleri kabul edilir — kullanıcı bunu açıkça
söyledi. İkisi de kalırsa vekil dilim yoluna geçilir.

Ses ve 4K, mpv'nin lehine ayrı bir ağırlık taşıyor: boru bu ikisini hiç veremiyor. Boru
sınırda geçer ve mpv rahat geçerse bunu rapora yaz, kararı T0 verir.

Rapor hangi adayın hangi kapıdan geçtiğini **cümleyle** yazacak. Sonraki sözleşme o
cümleden yazılacak.

## Kabul kriterleri

### K1 — Sıfır üretim dosyası değişikliği

`src/` altına tek satır yazılmayacak. `git status` ile kanıtlanacak.

### K2 — Kullanıcının ffmpeg'i sınanacak

Kurulum WinGet'ten gelen ffmpeg'i kopyalıyor. `hstack`, `zscale`, `tonemap` veya bir
`hwaccel` **olmayabilir.**

Liste sorgusuyla değil, `RAPOR.md:40` gereği **gerçek denemeyle** sınanacak: küçük bir
girdiyle grafiği kur, stderr'de `Error parsing option` ara.

Hangi filtrenin var olduğu rapora yazılacak.

### K3 — Düşerken ne düşecek

Ölçüm, duvara çarpıldığında **fps'in mi çözünürlüğün mü düşürüleceğini** gösterecek
sayıyı üretecek.

Konseyin kararı: **fps düşer, çözünürlük düşmez.** Panelin varlık sebebi artefakt
incelemek; çözünürlüğü düşürmek incelenecek şeyi yok eder.

Ölçüm bu kararı destekliyor mu, çürütüyor mu — rapora yazılacak.

## Doğrulama

- `dotnet build -c Release` 0 uyarı · `dotnet test` tamamı yeşil
- Altı ölçümün tablosu
- Üç kapının her biri için geçti/kaldı ve sayısı
- `git status` ile `src/` altında değişiklik olmadığının kanıtı
- Kullanılan test klipleri ve nasıl üretildikleri
- Koşturulamayan her adımın açık listesi

**Ortam notu:** `PATH` üzerindeki `dotnet` 3.1.201 ve derlemeyi `MSB3644` ile düşürür.
`%LOCALAPPDATA%\Microsoft\dotnet\dotnet.exe` (8.0.424) kullan.

T30 test kliplerini `testsrc2` ile üretti ve komutlarını raporuna yazdı; aynı yolu kullan.
Bu makinede `av1_amf` çalışıyor.

`VidShrink.sln` dosyasına dokunma.

## Kayıt noktası

Her ölçüm bitince rapora satır düş ve commit at. Ölçümler uzun sürebilir.

## Çıktı

Rapor: altı ölçüm tablosu, üç kapı kararı cümleyle, hangi ffmpeg filtrelerinin mevcut
olduğu, koşturulamayanlar.

## Kayıt noktası — 24.08.2026, kullanıcı isteğiyle durduruldu

Rapor: `docs/olcumler/T33-oynatma-olcumleri.md`. İş bitmedi, mühürlenmedi.

**Durum alanı hakkında:** T0 `status: open` istedi, ama `contract-guard` kancası
`active -> open` geçişini engelliyor (merdiven tek yönlü). `blocked` de doğru değil —
iş tıkanmadı, yarıda kesildi. `active` bırakıldı; mühür yok, denetime gitmedi.

**Biten ölçümler:** K2 (filtre yoklaması), Ö1 (boru hızı), Ö1b (duvarın yeri), K3 (fps
merdiveni), Ö6 (kare başına ayırma), Ö3 (donanım kod çözme), Ö5 (codec matrisi).

**Kapılar:** Aday A G1'den geçti (309 fps, p99 5,02 ms), G2'de kaldı (37,5 fps, p99
30,5 ms). G3 karara bağlanmadı. Aday B hiç ölçülmedi.

**Ana bulgu:** duvar kod çözmede değil borunun kendisinde. 2×1080p'de grafik boruya
yazmadan 171,8 fps üretiyor, boruyla 37,3 fps; kapasitenin %78'i ham kareyi işlemciden
geçirirken kayboluyor. Kullanıcının "1080p sınırı" itirazının sayısal karşılığı bu.

**Sıradaki iş, bu sırayla:**

1. **Ö7 (libmpv) — hiç başlanmadı.** Makinede mpv kurulu değil; ölçüm `libmpv` ikilisinin
   indirilmesini gerektiriyor ve bu kullanıcının açık iznine bağlı. İzin bu oturumda
   istenmedi. Devam eden oturum **önce izni almalı**, sonra ölçmeli.
2. **Ö2 düzeltilmiş koşumu.** Yöntem hatası bulundu ve düzeltildi (tek `-stream_loop`
   borusu + 3 kodlamanın medyanı); kod derleniyor ama çalıştırılmadı.
   `bench play <a,b> --only p2 --seconds 10 --target 5`. G3'ün kararı buna bağlı.
3. **Ö4 (Avalonia sunum yolu) — ölçülmedi.** Bench konsol projesi, Avalonia referansı yok;
   csproj `owns` dışında olduğu için scratchpad'de ayrı harness gerekecek.

**Kapsam notu:** `src/` altına ve `VidShrink.sln`'e dokunulmadı, `git status` ile
doğrulandı. Değişen tek kaynak dosyası `tools/VidShrink.Bench/Program.cs`.

## Tur 1 — borunun duvarı gerçekten boru mu

Round 0 duvarı buldu: 2×1080p'de ffmpeg 172 fps üretiyor, boru 37,5 teslim ediyor,
**%78 taşımada kayboluyor.** Round 0 kendi raporunda iki şeyi denemediğini yazdı ve
ikisi de duvarı kaldırabilir. Bu tur onları ölçer.

**libmpv (Ö7) bu turda başlamayacak.** Sırası bunlardan sonra; ucuz olan pahalıyı
gereksiz kılabilir.

### Ö8 — Boru tamponu büyütülünce ne oluyor

Bugünkü boru varsayılan tamponla çalışıyor. Windows'ta anonim boru tamponu
`CreatePipe` çağrısında verilir; .NET tarafında `ProcessStartInfo` bunu açmıyor,
ama okuma tarafındaki tampon boyutu ve okuma bloğu bizim elimizde.

Ölç: okuma bloğu boyutunu kare boyutunun katlarına çıkardığında (1 kare, 2 kare,
4 kare) sürdürülen fps ne oluyor. 2×1080p'de 37,5'ten yukarı çıkıyor mu.

**Ölçüm gerçek olacak:** aynı kaynak, aynı süre, en az üç koşu, ortalama ve sapma.
Tek koşuya bakıp karar verme.

### Ö9 — Paylaşımlı bellek boruyu geçiyor mu

Boru yerine bellek eşlemeli dosya (`MemoryMappedFile`) ile halka tampon: ffmpeg
`-f rawvideo` çıktısını oraya yazsın, biz oradan okuyalım.

ffmpeg doğrudan paylaşımlı belleğe yazamıyor; iki yol var, hangisini seçtiğini yaz:
- ffmpeg boruya yazar, ince bir aracı boruyu paylaşımlı belleğe kopyalar (kopya bir
  fazla ama okuma tarafı bloklanmaz)
- ffmpeg bir FIFO/adlandırılmış boruya yazar, okuma tarafı bellek eşlemeli olur

Ölç: 2×1080p'de sürdürülen fps. Karar ölçütü **60 fps**.

### Ö10 — Duvar gerçekten taşıma mı, yoksa tüketici mi

Round 0 "%78 taşımada kayboluyor" dedi ama okuyan tarafın kendisi de yavaşlatıyor
olabilir. Kareyi okuyup **hiçbir şey yapmadan atan** bir tüketiciyle ölç: boru bu
durumda kaç fps taşıyor.

Bu sayı, Ö8 ve Ö9'un tavanını verir. Tüketicisiz boru da 37,5'te kalıyorsa duvar
gerçekten boru; 100+ fps çıkıyorsa duvar `WriteableBitmap` tarafında ve mimari
tartışması baştan yanlış yerde dönüyor demektir.

**Bu ölçüm önce yapılacak** — ötekilerin anlamını o belirliyor.

### Kabul

Üç ölçümün sayıları `docs/olcumler/T33-oynatma-olcumleri.md` dosyasına, round 0'ın
tablolarının altına eklenecek. Her ölçüm: koşu sayısı, ortalama, sapma, komut.

Karar: 2×1080p'de **60 fps**'e ulaşan bir yol çıkarsa boru yolu kurtulur ve libmpv'nin
+60-100 MB'ı, karışık lisansı ve süreç içi çökme riski hiç ödenmez. Çıkmazsa Ö7 açılır.

Sayı uydurma. Ölçemediğin şeye "ölçülemedi" yaz.

### Dal politikası

Tek `main` dalı üzerinden ilerliyoruz, worktree açılmayacak. **Her adımdan sonra
commit at** — bu depoda dört ajan işini commit etmeden durdu, biri işini neredeyse
kaybetti (`DURUM.md` §7).

### Tur 1 — kayıt noktası (restart için durduruldu)

Kullanıcının makineyi yeniden başlatması gerekti, ajan ölçüme girmeden durduruldu.
Yarım iş kaybolmasın diye T0 commit etti.

**Nerede kalındı:** ajan ölçüm düzeneğini yazıyordu (`tools/VidShrink.Bench/Program.cs`),
henüz tek bir ölçüm koşusu yapılmadı. `docs/olcumler/T33-oynatma-olcumleri.md` round 0'daki
hâlinde, hiç dokunulmadı. Hiç ffmpeg koşmadı.

**Devam eden oturum için:** yarım düzenek derleniyor (0 uyarı, 0 hata) ama tamamlanmadı.
Ajanın son adımı "dispatch ve seçenekler" idi — komut satırı ayrıştırma tarafı eksik.

Ölçüm sırası değişmedi: **Ö10 önce** (tüketicisiz boru tavanı), sonra Ö8 (tampon), sonra
Ö9 (paylaşımlı bellek). Ö10 100+ fps verirse duvar boruda değil `WriteableBitmap`
tarafında demektir ve mimari tartışması yanlış yerde dönüyor.

**Restart uyarısı:** ölçüm makinenin durumuna bağlı. Restart öncesi alınmış hiçbir sayı
yok, o yüzden karşılaştırma sorunu doğmuyor — bütün ölçümler yeniden başlayacak.

## Tur 1 — sonuç

Üç ajan üst üste düştü (oturum limiti, restart, bağlantı kopması). Düzeneği ajanlar yazdı,
**ölçümü T0 koşturdu**. Sayılar `docs/olcumler/T33-oynatma-olcumleri.md` sonundaki
"Tur 1 — duvar boru değilmiş" bölümünde.

### Ö10 ve Ö8 — round 0'ın yargısı çürüdü

Round 0: *"boru 37,5 teslim ediyor, %78 taşımada kayboluyor, duvar boru."*

Ölçüm: **boru 2×1080p'de 161,8 fps taşıyor** (3 koşu, sapma 0,65).

Kök neden Ö8'de tek satırda görünüyor:

| Okuma biçimi | 2×1080p fps |
|---|---|
| Kare tek okumada (15,8 MB) | 70,9 |
| Aynı kare 64 KB parçalarla toplanır | **148,0** |
| 2 kare tek okumada | 41,8 |
| 4 kare tek okumada | 22,6 |

Windows anonim borusunun iç tamponu küçük; 15,8 MB'lık tek okuma isteği tamamı gelene
kadar bloklayıp üretici ile tüketiciyi sıraya sokuyor. Küçük parçalarla okumak ikisini
birlikte çalıştırıyor. **Okuma bloğu büyüdükçe durum kötüleşiyor** — sezgiye aykırı ama
üç koşuda da sapma sıfıra yakın.

### Karar

**60 fps @ 2×1080p boru yoluyla karşılanıyor, 2,5 kat payla (148 vs 60).**

libmpv (Ö7) **açılmadı ve gerekmiyor**: +60-100 MB kurulum, karışık GPLv2+/LGPL lisans,
Linux self-contained sorunu ve süreç içi çökme riski kullanıcının istediği hedef için
ödenmeyecek. Ses ve 4K hâlâ yalnız libmpv'de kalıyor; onlar ayrı gerekçe, ayrı karar.

`docs/PLAN-karsilastirma-oynatici.md` §4'teki "Son satır adayın duvarı" ve "kullanıcının
aşmak istediği 1080p sınırı borunun sınırı" cümleleri **geçersiz**. §9'daki platform
ayrışması tartışması da konusuz kaldı — boru üç platformda da hedefi karşılıyor.

### Ö9 ölçülmedi

Paylaşımlı bellek, borunun yetersizliği varsayımı üzerine planlanmıştı. Varsayım
çürüdüğü için koşulmadı. Gerekirse sonra koşulur.

### Sıradaki — Ö4

Duvar okuma tarafından kalktı. Geriye Avalonia `WriteableBitmap` sunum yolu kalıyor:
148 fps üretilen kareyi ekrana koyabiliyor mu? İkinci görüş de bağımsız olarak buraya
işaret etmişti. Bu ölçülmeden panel mimarisi kapanmamalı.

### Uygulamaya geçerken

Kare okuma yolu **parçalı** olacak — 64 KB ile 256 KB arası fark yok (148,0 ve 148,3),
1 MB biraz kötü (143,9). Kare boyutunda tek okuma yapan her kod yolu bu ölçümün
gösterdiği cezayı öder.

## Tur 2 — G2 hâlâ cevapsız, çürütülen sayılar işaretsiz

Denetçi üç KRİTİK verdi. Üçü de ölçüm dürüstlüğü meselesi, kod meselesi değil.

**1. G2 cevaplanmadı ve belge kendi içinde çelişiyor.** `docs/olcumler/T33-oynatma-olcumleri.md:242`
"G2 KALDI, 37,5 fps, p99 30,5 ms" derken `:371` "60 fps @ 2×1080p karşılanıyor" diyor;
hangisinin geçerli olduğunu söyleyen satır yok. G2 iki koşullu (**≥60 fps ve p99 < 25 ms**)
ama tur 1'in 148 fps'i üreten yol hiç aralık istatistiği toplamıyor —
`tools/VidShrink.Bench/Program.cs:1265-1308` yalnız Fps/CpuPercent/Error dönüyor, P10'un
havuz satırı `PipeAsync`'in p50/p95/p99'unu `:1416-1421`'de atıyor. Kapının p99 ayağı hiç
ölçülmemişken "karşılanıyor" denmiş.

**2. Çürütülmüş sayılar düzeltilmeden duruyor.** `:91` "%78,3 boru kaybı", `:93` "duvar
borunun kendisi", `:120-130` "pratik tavan 30 fps", `:286` "duvarın sebebi ayrıştırılmadı" —
hiçbirine geçersizlik notu düşülmemiş, oysa `:352-357` aynı belgede bunları çürütüyor.
Belgeyi kapı tablosundan okuyan sonraki sözleşme yanlış sayıyı alır.

**3. Komut satırı hiçbir ölçümde yazılı değil.** Tur 1 kabul maddesi "her ölçüm: koşu
sayısı, ortalama, sapma, **komut**" diyor; `:306-345` arası tabloların hiçbirinde `bench play …`
satırı yok, `--only`/`--runs`/`--fps` seçenekleri yazılı değil.

### Bu turda yapılacak

1. Tur 1'in 148 fps'i üreten yolunu aralık istatistiği toplayacak şekilde tamamla ve
   **2×1080p'de p99'u ölç**. G2'yi iki ayağıyla birlikte geçti/kaldı diye tek cümleyle
   karara bağla.
2. Çürütülen bölümlerin başına geçersizlik notu düş — bölümü silme, hangi turun neyi
   çürüttüğünü yaz. Kapı tablosu tek doğruyu göstersin.
3. Her ölçüm satırına koşulan komutu ekle. Kaynak klip yeniden üretilebilir olsun
   (`testsrc2` tarifi ya da klibin kodek/çözünürlük/kare hızı envanteri).
4. G3'ü karara bağla ya da neden bağlanamadığını yaz.

Üretim dosyasına tek satır yazılmayacak — bu sözleşme yalnız sayı üretir. `owns` dışına
çıkma.
## Rapor — tur 2

Rapor dosyası: `docs/olcumler/T33-oynatma-olcumleri.md`, sonundaki
"Tur 2 — kapılar iki ayağıyla" bölümü. Commit: `3451103` (worktree
`agent-a762e217b56878f83`, tek commit).

### Denetçinin üç KRİTİK'i

**1. G2 cevaplandı, çelişki kapatıldı.** `PipeAsync` artık kareyi 64 KB parçalarla
topluyor (tur 1'in kazanan yolu) **ve** her kare sınırında aralık kaydediyor; yeni
`bench play --only p11` sürdürülen fps ile p50/p95/p99'u ve 25 ms'i aşan kare yüzdesini
birlikte veriyor. Beş koşu, iki kaynak.

- **G1 GEÇTİ:** 2×960×540'ta 348,0 fps (sentetik) / 278,6 fps (gerçek kayıt), en kötü
  koşunun p99'u 11,60 ms ve 22,47 ms — ikisi de 25 ms altında.
- **G2 fps ayağı GEÇTİ, p99 ayağı kaynağa göre bölünüyor:** 2×1920×1080'de 108,4 ve
  107,6 fps (hedefin 1,8 katı, beş koşuda da). p99 sentetik kaynakta 14,45 ms ortalama /
  16,41 ms en kötü, 25 ms'i aşan kare yok; gerçek 132 Mbit/s'lik ekran kaydında 28,22 ms
  ortalama / 59,63 ms en kötü, karelerin %1'i 25 ms'i aşıyor. Kapı harfi harfine
  uygulanırsa **G2 gerçek içerikte kalıyor** — kapasiteden değil, %1'lik kuyruktan.
- **G3 KALDI:** kodlama koşarken yavaşlamanın en iyi değeri %18,3, en kötüsü %181,9;
  kapı %10. Eşiğin altına inen tek yapılandırma yok. Ters yön güvenli: boru kodlamanın
  yanında da 60,3-60,8 fps tutuyor (fps kaybı %3,7-5,6).

**2. Çürütülen sayılar işaretlendi.** Ö1, Ö1b, K3, Ö2 ve tur 0'ın kapı tablosunun başına
geçersizlik notu düşüldü; hiçbiri silinmedi, hangi turun neyi çürüttüğü yazıldı.
"Ölçülmeyenler" listesindeki "boru duvarının sebebi ayrıştırılmadı" maddesi üstü çizilip
tur 1'e yönlendirildi. Tur 0 kapı tablosu artık kendi üstünde "bu tablo geçersiz, tur 2
tablosu geçerlidir" satırını taşıyor.

**3. Komutlar eklendi.** K2, Ö1, Ö1b, K3, Ö6, Ö3, Ö5, Ö2, P10, P8 ve tur 2'nin bütün
tabloları artık kendi `bench play …` satırını taşıyor (`--only`, `--seconds`, `--fps`,
`--runs`, `--target` yazılı). İki kaynak da yeniden üretilebilir hâle getirildi:
sentetik klibin `testsrc2` tarifi ve gerçek kaydın kodek/çözünürlük/kare hızı/bit hızı
envanteri belgeye kondu.

### Ölçülemedi

- **Ö12 (kuyruklu tüketici).** Düzenek yazıldı ve koşuldu ama **güvenilir sayı vermedi**,
  sonuçları rapora alınmadı: değerler kuyruk derinliğinde tekdüze değil (derinlik 1 →
  %25,6 kaçırma, derinlik 3 → %0, derinlik 8 → %14,8) ve panel boyutundan bağımsız
  çok saniyelik duraklamalar çıkıyor. Bu yüzden G2'nin %1'lik geç kare kuyruğunu hangi
  derinlikte bir tamponun yutacağı **açık kaldı.**
- **Ö4 (Avalonia sunum yolu)** bu sözleşmede ölçülmedi — `owns` konsol bench projesiyle
  sınırlı; T37 ayrıca ölçmüş.
- **Ö7 (libmpv), Ö9 (paylaşımlı bellek)** açılmadı, gerekçesi tur 1'de.
- **Ö1/Ö1b/K3/Ö6/Ö3/Ö5 tabloları parçalı okumayla yeniden koşulmadı.** Karşılaştırmaları
  ayakta (iki kol da aynı okuma biçimini kullanıyor) ama mutlak fps değerleri düşük;
  geçersizlik notları bunu söylüyor.

### Yolda görülen, kapsam dışı bırakılan

- `tools/VidShrink.Bench/Program.cs` içinde bir çökme düzeltildi (aralık listesi
  `maxFrames` ile boyutlandırılıyordu, döngülü boruda `int.MaxValue` olduğu için
  "Array dimensions exceeded supported range" veriyordu). Düzeltilmeden Ö2 hiç koşamazdı,
  o yüzden kapsam içi sayıldı.
- Sözleşmedeki ortam notu eskimiş: `PATH` üzerindeki `dotnet` artık 3.1.201 değil
  **9.0.316** ve `net8.0` hedeflerini sorunsuz derliyor.
  `%LOCALAPPDATA%\Microsoft\dotnet\dotnet.exe` bu makinede **yok**.

### Doğrulama

| Kontrol | Sonuç |
|---|---|
| `dotnet build VidShrink.sln -c Release` | 0 uyarı, 0 hata |
| `dotnet test VidShrink.sln -c Release --no-build` | 369 başarılı, 0 başarısız, 8 atlanan |
| `git status` | yalnız `owns` listesindeki iki dosya değişti; `src/` ve `VidShrink.sln` temiz |

Commit'ler: `3451103` (owns'taki iki dosya, isin kendisi) ve `4fe6258` (yalniz ajan hafizasi, `.claude/agent-memory/teknesyum-builder/`).

## T0 karari — G2 kuyrugu ve G3 (25.08.2026)

### G2: gecti sayiliyor, kuyruk borc olarak yaziliyor

Kapasite ayagi rahat gecti: 2x1920x1080te 108,4 ve 107,6 fps, hedefin 1,8 kati.
Kalan tek sey gercek 132 Mbit/s ekran kaydinda karelerin **%1i** 25 msi asiyor
(ortalama p99 28,22 ms). Sentetik klipte hic asan kare yok.

Kapiyi yazdigimizda elimizde tek bir p99 sayisi yoktu — kapi ortalamadan turetilmisti.
Yuzde birlik kuyruk gozle bir titreme, ozelligin coktugu nokta degil. G2 **gecti**;
kuyruk kapatilmis bir kriter degil, **acik borc** olarak duruyor:

- Ö12 kuyruklu tuketici duzenegi tutarsiz sayi verdi, sonuclari rapora alinmadi.
  Yuzde birlik gec kare kuyrugunu hangi tampon derinliginin yutacagi **olculmedi**.
- Bu borc tampon isini yapan sozlesmeye tasinacak, T33te kapanmayacak.

### G3: kaldi, sozlesmenin kendi cikis yolu uygulandi

En kotu yavaslama %181,9, kapi %10. Sozlesmenin "duserse" sutunu ne diyorsa o yapildi:
kodlama surerken oynatma kapaniyor. Ters yon guvenli — boru kodlamanin yaninda
60,3-60,8 fps tutuyor. G3 bir hata degil, olculup kabul edilmis bir sinir.

### Ortam notu eskidi

Sozlesme `%LOCALAPPDATA%\Microsoft\dotnet\dotnet.exe` (8.0.424) diyor; o kurulum artik
yok, `PATH`teki `dotnet` 9.0.316 ve dogru olan o. Sonraki sozlesmelerde bu not tekrarlanmasin.

## Denetim — tur 2 (25.08.2026, denetci aaf86cfa6ff598742)

GECTI. Tur 1in uc KRITIGI de kodda gercekten kapandi: olcum kare sinirinda
(`Program.cs:809-815`, kismi kare atiliyor), p99 turetilmis degil gercek aralik
listesinden nearest-rank (`:851`, `:286-292`), curutulen tur 0/tur 1 bolumleri
silinmemis gecersizlik notuyla duruyor. K1: `git show --stat 3451103` yalniz owns
icindeki iki dosyayi gosteriyor, `src/` ve `VidShrink.sln` temiz.

Borc (kapanmadi):

- Tur 0 "Cumleyle" paragraflari curutulen sayilari isaretsiz tekrarliyor
  (rapor:296-300); gecersizlik notu yalniz tabloya atif yapiyor.
- **148 fps ile 107,6 fps uzlastirilmadi** — tur 1 P8 "kare havuzu, 64 KB" 148,0
  (sapma 0,29), tur 2 Ö11 ayni kaynak/boyut/sure 107,6 (sapma 6,85). Kodda iki yol
  ayni (`PipeChunkedAsync:1294-1301` = `PipeAsync:809-815`). %27lik fark rapora
  yazilmamis. Iki sayi da hedefin cok ustunde, kapi karari degismiyor.
- CPU % payi surec acilisini iceriyor, paydasi acilis sonrasi sure
  (`Program.cs:837-847`) — butun tablolarda sistematik yuksek (or. %1022,5).
  CPU hicbir kapinin olcutu degil.
- Ayni satirda iki farkli toplama: p99 sutunu kosularin **en kotusu**, "25 ms ustu
  kare %" sutunu **ortalamasi** (`Program.cs:1612-1613`). Yontem rapor:482de yazili.
- Round 0 ortam tablosu eskimis (rapor:15), duzeltme yalniz tur 2 bolumunde.
- Ö12 olu duzenek: `PipeQueuedAsync` ve `PlayQueueAsync` kodda duruyor, sayi uretmiyor.
  T0 zaten tampon sozlesmesine tasidi.
- Kapi tablosu hucresi tek kelimelik karar vermiyor (rapor:585); literal karar :600de.

Not: rolye izleyicisinin worktree kaydi fiilen duzenlenen rapor dosyasini listelemiyor.
Muhur kapisi bu listeye baktigi icin `_sorun.log`a yazildi.
