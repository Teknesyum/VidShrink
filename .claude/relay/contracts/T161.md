---
id: T161
title: Onizleme durdur/baslat yapinca bastan basliyor — ayar degismedigi halde
role: builder
lang: tr
depends: []
status: open
round: 4
risk: high
verify:
  - dotnet build -c Release --no-incremental
  - dotnet test -c Release --filter "FullyQualifiedName~PlaybackResumeTests"
owns:
  - src/VidShrink.App/Playback/PanelHost.cs
  - src/VidShrink.App/Playback/SegmentEncoder.cs
  - src/VidShrink.Ffmpeg/Playback/PipeComparisonFrameSource.cs
  - tests/VidShrink.Tests/PlaybackResumeTests.cs
  - docs/olcumler/onizleme-bastan-baslama.md
side_effects: []
---

## Amac

Onizleme oynarken durdurup yeniden baslatinca, **hicbir ayar degismedigi halde** is bastan
basliyor. Kaldigi yerden devam etmeli.

## Baglam

Kullanicinin belirtisi: *"oynatirken durdur/baslat yapilinca, ayar degismedigi halde en
bastan isleme giriyor."*

Kod tarafinda simdiden goze carpan iki yer var — **bunlar ipucudur, teshis degildir**:

```
src/VidShrink.App/Playback/PanelHost.cs:926  ApplyPlayState()
    else { source.Pause();
           if (_aheadRunning) _segments.Cancel(); }     <- duraklatinca onden hazirlik iptal

src/VidShrink.App/Playback/PanelHost.cs:353  Restart() -> Chain -> RestartCore()
    RestartCore icinde: await Teardown(); ++_generation; _factory(); source.StartAsync(...)
    yani her Restart yeni bir kaynak sureci demek

Restart cagrilan yerler: :116 (_settle.Tick), :299, :308, :581, :640
```

Yani duraklatma onden hazirlanmis parcayi **atiyor**; devam edince yeniden kodlanmasi
gerekiyor olabilir. Olabilir — **once olc.**

## Kabul kriteri

**K1 — Once olc: durdur/baslat hangi cagriyi tekrarlatiyor?** Belirtiyi bir kosumda
uret ve **hangi cagrinin ikinci kez kostugunu say**: `RestartCore`, `StartAsync`,
`SegmentEncoder`in kodlama cagrisi, `_segments.Cancel`, `DropStandby`. Sayimi belirtiyi
uretmeyen bir kosumla (durdur/baslat yapilmayan) yan yana koy — **fark asil bulgudur.**
Belirtiyi susturma, nedenini bul.
CHECK: iki kosumun cagri sayimlari tek izgarada, ham cikti; hangi cagrinin fazladan
kostugu tek cumlede.

**K2 — Neden yazili.** K1'in gosterdigi fazladan cagri **neden** kosuyor. Duraklatma
onbellegi atiyorsa bunu goster; baska bir sey tetikliyorsa onu goster. "Muhtemelen" ile
biten bir cumle kabul degil.
CHECK: nedeni tasiyan kod yolu, dosya ve satir numarasiyla.

**K3 — Duzeltme: durdur/baslat isi bastan baslatmaz.** Ayar degismeden yapilan
durdur/baslat, kodlamayi yeniden tetiklemez ve oynatma kaldigi konumdan surer.
CHECK: duzeltme sonrasi K1'in ayni sayimi — fazladan cagri sifira inmis olmali, ham cikti.

**K4 — Mesru yeniden kurulum bozulmadi.** Panel yeniden boyutlaniyorsa, dosya
degistiyse, plan degistiyse `Restart` **hala** kosmali. Duraklatmayi ucuzlatirken bu
yollari kapatma.
CHECK: bu uc yol icin ucer olcu ve hepsinin gectigi ham cikti.

**K5 — Mutasyon.** Iki mutasyon, her birinden once
`dotnet build -c Release --no-incremental`; **`--no-build` yasak**:
(a) Duzeltmeyi geri al — durdur/baslat yine bastan baslasin. Bir olcu **olmeli**.
(b) K4'un yollarindan birini kapat (ornegin boyut degisiminde `Restart` cagirma).
    Bir olcu **olmeli**.
CHECK: iki satirlik izgara, kirilan olcu adlari, ham cikti.

**K6 — Kol sayisi.** `dotnet test -c Release --filter "FullyQualifiedName~PlaybackResumeTests" --list-tests`.
Sifir bulan kol varsa teslim etme, bildir — bu depoda sifir eslesen filtre kolu exit 0 ile
dort kez sessizce gecti.
CHECK: kolun buldugu test sayisi.

## Sinirlar

- `owns` disina yazma. `ComparisonPanel.axaml.cs`, `ComparisonSurface.cs`, `ControlStrip.axaml.cs`
  **okunacak, yazilmayacak**; duzeltme onlarda gerekiyorsa **yapma, bildir** — T0 `owns`i
  genisletir.
- `MainWindow.axaml` ve `MainWindow.axaml.cs` arayuz sozlesmesinin (T163) alani, dokunma.
- **Iki kodlamayi ayni anda baslatma.** ffmpeg sirali kosar; makinede baska ajan var.
- Kod yorumu yazma. Eskiyen docstring'i **guncelle** — `:926` ve `:350` civarindaki
  aciklamalar duzeltmeden sonra eskiyecek.
- "Iki", "hepsi", "hicbiri" diyen her cumlenin altindaki listeyi **kendin say ve ham
  ciktiyi goster.** Bu depoda tablo dogru cikip onu ozetleyen cumle 26 kez yanlis cikti.
- **Filtresiz `dotnet test` kosturma** — tam suit 25 dakika suruyor.
- Gecici her sey `.calisma/T161/` altina. `.calisma/kaynak/` paylasilan kaynak: **oku,
  yazma, silme.**
- `.claude/relay/**` T0'in alani.

## Kayit noktasi

Dal: `T161-onizleme-devam`, guncel `main`den ac, `origin`e it. **`main`e birlestirme.**

## Cikti

`docs/olcumler/onizleme-bastan-baslama.md` — K1'in iki kosum izgarasi, K2'nin kod yolu,
K3'un duzeltme sonrasi sayimi, K4'un uc yolu, K5'in mutasyon izgarasi, K6'nin sayisi.

## Kayit

Isledikce buraya yaz, sonda degil.

- Dal adi `T161-onizleme-devam` tur1'in worktree'sinde (`agent-a411d7dd7b1bae059`) hala
  checkout'lu (kilitli, 0 commit). Kendi worktree'mde ayni adi acamadim; ben
  `worktree-agent-a315b62e3f1b7e5d9` uzerinde calisip teslimde bu adla `origin`e itecegim
  (git iki worktree'de ayni dal adini es zamanli tutmuyor). T0 birlestirmeden once o
  worktree'yi kaldirmali.
- Tur1'in `tests/VidShrink.Tests/PlaybackResumeTests.cs` taslagi kopyalandi ve calisir
  hale getirildi (kendisi hic derlenmemis/kosturulmamisti). `SegmentEncoder.StartedEncodes`,
  `SegmentClips`, `TestPaths`, `FfmpegFact` zaten depoda vardi, degismedi.
- Test altyapisinda 3 kusur buldum, hepsi test dosyasinda duzeltildi (uretim kodu degil):
  1. `Yerlestir()` cıplak `panel.Measure/Arrange` cagiriyordu; StaticResource/tema
     coz(ul)mesi gercek bir `Window` koku olmadan calismiyor (bkz. `ComparisonPanelTests.
     LayOutAt`), bu yuzden `_panel.Frames.Bounds` hep 0x0 kaliyordu ve `RestartCore`
     `MinPanelEdge` kapisinda sessizce donuyordu. Duzeltme: panel gercek bir `Window`
     icine konup oradan Measure/Arrange ediliyor.
  2. Ikinci (ve sonraki) `Restart()` cagrisi `Teardown()` icinde `Task.Run(source.Dispose)`
     ile GERCEKTEN asenkron hale geliyor. `AppHost.Run` icinden cagrilirsa bu bekleme
     konak is parcacigina yapisik Avalonia `SynchronizationContext`'ine postalaniyor ve
     onu kimse pompalamiyor (`Dispatcher.UIThread` mesaj dongusu hic baslamiyor) —
     sonsuza dek asili kaliyordu. Konak disindan (xUnit is parcacigindan) cagrilirsa bu
     sefer `RestartCore`'un devami (`_panel.Controls.IsPlaying = true` vb.) yanlis is
     parcacigindan `AvaloniaObject.VerifyAccess()`e takilip `InvalidOperationException:
     Call from invalid thread` firlatiyor — sessizce yutuluyor (kimse `_restart`'i
     awaitlemiyor). Duzeltme: `LoadClipAsync` konak is parcacigindan baslatiliyor
     (`AppHost.Run(() => host.LoadClipAsync(4))`), tamamlanmasi `Bekle()` icine eklenen
     `Dispatcher.UIThread.RunJobs()` pompasiyla bekleniyor (bkz. `ShellIntegrationTests.
     Drain`).
  3. K1/K3 testinin `Source(_clips.Kaynak)` cagrisi varsayilan 12 sn sure veriyordu.
     `SegmentEncoder.Clamp` pencereyi kaynagin son `PreviewSegment.WindowSeconds`ina
     (5 sn) kadar geri cekiyor; 4-9 sn'lik etkin pencerede "sonraki" istek (9) 7'ye
     kirpiliyor, bu da `PrepareAheadAsync`in kendi tekillik kontrolunu
     (`ActiveClip.EndSeconds` karsilastirmasi) HER cagrida bozuyor — kontrol kosumu
     (durdur/baslat yok) bile 2 kere kodluyordu. Duzeltme: bu testte sure 30 sn'ye
     cikarildi, kirpilma devre disi kaldi.
- K1 ham olcum (hata DUZELTILMEDEN once, `.calisma/T161/K1-baseline-bug-var.txt`):
  kontrol=1, durdur/baslat=2, fark=1. K4'un iki kolu (dosya/plan degisimi) bu asamada
  zaten yesil — onlar `Restart`in BASKA cagiran yerlerini sinar, hatanin oldugu yolu
  degil.

- K3 duzeltme uygulandi (`PanelHost.ApplyPlayState`): duraklatmada artik `_segments.Cancel()`
  cagrilmiyor, onden hazirlik arka planda bitmeye birakiliyor. Ayni K1 izgarasi duzeltme
  sonrasi (`.calisma/T161/K3-K4-duzeltme-sonrasi.txt`): kontrol=1, durdur/baslat=1, fark=0.
  Uc test de (K1/K3/K4) tek kosumda yesil: `Dosya_degisimi_hala_yeniden_kurar`,
  `Plan_degisimi_hala_yeniden_kurar`, `Durdur_baslat_onden_hazirligi_bastan_baslatmaz`.

- K5 mutasyon izgarasi (her birinden once `dotnet build -c Release --no-incremental`):
  | mutasyon                                              | beklenen | sonuc |
  |--------------------------------------------------------|----------|-------|
  | (a) ApplyPlayState'teki duzeltme geri alindi (`_segments.Cancel()` yeniden eklendi) | Durdur_baslat testi FAIL | FAIL (Expected:1 Actual:2) — oldu |
  | (b) SetFiles'ta dosya degisince `Restart()` cagrisi kaldirildi (K4 yolu kapatildi)  | yalniz Dosya_degisimi testi FAIL, digerleri gecer | FAIL sadece o test, Plan_degisimi+Durdur_baslat yesil — oldu |
  Her iki mutasyondan sonra duzeltme/kod GERI ALINDI (dosyalar `.calisma/T161/PanelHost.cs.fixed`
  ile karsilastirilarak `SAME` dogrulandi), tekrar build edildi, uc test de yesile dondu.
- K6: `--list-tests` 3 test buluyor (`Durdur_baslat_onden_hazirligi_bastan_baslatmaz`,
  `Dosya_degisimi_hala_yeniden_kurar`, `Plan_degisimi_hala_yeniden_kurar`), sifir eslesen
  kol yok.
- Son dogrulama: `dotnet build -c Release --no-incremental` + filtreli `dotnet test`
  ikisi de yesil, ham cikti `.calisma/T161/K3-K4-duzeltme-sonrasi.txt` ile ayni.
- `docs/olcumler/onizleme-bastan-baslama.md` yazildi. status: submitted.

- Teslim: dal `T161-onizleme-devam-a315b` (contract'ta istenen `T161-onizleme-devam` adi
  tur1'in worktree'sinde hala kilitli oldugu icin acilamadi — yukarida not edildi),
  `origin`e itildi (https://github.com/Teknesyum/VidShrink/pull/new/T161-onizleme-devam-a315b).
  `main`e birlestirilmedi. status: submitted.

---

# Tur 3 — bagimsiz denetim `verdict: failed` dondu

Tur 2 teslim edildi, **kapanmadi**. `risk: high` (428 satir) oldugu icin surum-2 denetim
kosturuldu.

Denetcinin **dogruladigi ve yeniden acmayacagin** seyler:

- K1'in sayimi **gercek**: `.calisma/T161/K1-baseline-bug-var.txt` "kontrol 1 /
  durdur-baslat 2 / fark 1" satirlarini birebir tasiyor, ozet cumlede kalmiyor.
- K2'nin kok nedeni **dogru**: `PanelHost.cs:927` `ApplyPlayState()`.
- K3 belirtiyi susturmadi, **nedeni kaldirdi** ve duraklatma kendi isini hala yapiyor:
  `Follow` (`:738-740`) ilk satirda `if (!_panel.Controls.IsPlaying) return;` ile donuyor,
  duraklamisken yeni onden hazirlik hic baslamiyor. `Cancel()` mesru yerlerinde duruyor
  (`:194`, `:199`, `:283`, `:315`, `:990`). **Kusur takasi yok.**
- Denetci K3'u bagimsiz yeniden uretti: kendi kosumu `kontrol 1 / durdur-baslat 1 / fark 0`.
- `owns` disina yazim yok; `SegmentEncoder.cs` ve `PipeComparisonFrameSource.cs` hic
  degismemis.
- K5'in iki mutasyonu gercek: (a) `Expected:1 Actual:2` ile dusurmus, (b) dosya kolunu
  dusurmus.
- Bos gecen olcu yok.

Kapanmasi gereken dort sey:

## D1 — K4 uc yol istiyordu, iki yol olculdu; **boyut degisimi yolu icin hicbir olcu yok**

Sozlesme K4 CHECK: *"bu uc yol icin ucer olcu ve hepsinin gectigi ham cikti"* — panel
yeniden boyutlanmasi, dosya degisimi, plan degisimi. Ham ciktinin **iki** dosyasinda da
yalniz iki K4 satiri var; boyut satiri yok.

`docs/olcumler/onizleme-bastan-baslama.md` K4 bolumu bunu olcuyle degil *"koda
dokunulmadi"* gerekcesiyle geciyor. **Bu bir olcu degil, bir iddia.**

Yol canli ve zinciri belli:
```
PanelHost.cs:123   Frames.SizeChanged -> OnResized()
PanelHost.cs:478-492  OnResized -> _settle.Start()
PanelHost.cs:116   _settle.Tick -> Restart()
```
Boyut degisimi kolunu yaz ve olc.

## D2 — `Plan_degisimi_hala_yeniden_kurar` plan degisimini **olcmuyor**

`tests/VidShrink.Tests/PlaybackResumeTests.cs:249-268`: `SetPlan` bir kez ve `Open()`tan
**once** cagriliyor; ikinci bir plan hic verilmiyor. Olctugu sey `LoadClipAsync(4)` bitince
fabrikanin yeniden cagrilmasi (`PanelHost.cs:581`), yani *"ilk parca hazir olunca
Restart"*. `SetPlan` -> imza karsilastirmasi (`:202-204`) -> `_segmentDelay` ->
`ScheduleClip` zinciri **hic kosmuyor**; o zincir kirilsa bu test yine yesil kalir.

Test adi ve sozlesme kriteri "plan degisimi" diyor, olcu baska sey sayiyor. Bu deponun
bilinen kusur sinifi: **sayi dogru, onu adlandiran cumle yanlis.** Belge parantez icinde
("ilk parca hazir olunca") yarim itiraf ediyor ama baslik duzeltilmemis.

Ya gercek plan degisimini olc (ikinci bir `SetPlan`, imza zinciri kossun), ya kolu
oldugu seye gore **yeniden adlandir** ve K4'un plan yolunu ayrica olc. Yarim itirafi
belgede birakma.

## D3 — K3'un ikinci yarisi kanitsiz

Kriter: *"kodlamayi yeniden tetiklemez **ve oynatma kaldigi konumdan surer**."* Olculen
tek sey `SegmentEncoder.StartedEncodes`; konum/devam iddiasina dair hicbir dogrulama yok
(`SessizKaynak` saplamasi `Play`/`Pause`/`SeekAsync`i bos geciyor, `Position` gozlenmiyor).

Konumun korundugunu olc, ya da olcemiyorsan iddiayi **geri cek** ve kriterin bu yarisinin
neden olculemedigini yaz.

## D4 — Belgede yanlis satir numarasi

`docs/olcumler/onizleme-bastan-baslama.md` K4: *"`OnResized` -> `Restart`,
`PanelHost.cs:385`"*. `OnResized` **`:478`**'de; `:385` `RestartCore` icinde `BuildRequest`
civari. Ayrica `OnResized` `Restart`i dogrudan cagirmiyor, `_settle` uzerinden cagiriyor.

## Dal adi

Tur 2 `T161-onizleme-devam-a315b` dalina teslim etti cunku `T161-onizleme-devam` adi
tur 1'in worktree'si tarafindan kilitliydi. **T0 o worktree'yi kaldirdi, ad artik bos.**
Tur 3'u `T161-onizleme-devam` dalina teslim et.

## Tur 3 kabul kriteri

D1-D4'un **dordu de** kapanacak. Her biri icin: ne yaptin, hangi olcu tutuyor, o olcunun
**dusebildigini** gosteren mutasyon. Bir bulguyu kapatamiyorsan **uydurma ve gecistirme —
bildir, gerekcesiyle.**

Yeni yazdigin her olcu icin kendine sor: *bu kol hangi mutasyonla duser? Ve adi gercekten
olctugu seyi mi soyluyor?*

## Kayit

- Tur 3 teslim: dal `T161-onizleme-devam` (ad bostu, acildi), commit `06233e7`,
  `origin`e itildi. `main`e birlestirilmedi.
- D1 kapandi: `Boyut_degisimi_hala_yeniden_kurar` eklendi. Pencere 640x480 -> 320x240
  gercekten kuculuyor, `Frames.SizeChanged` kendiliginden atesenip `_settle` kuruluyor,
  tik gelince fabrika 1 -> 2. Sayacin kurulmasi (`ResizeSettling`: acilista False,
  kucultunce True) ayri olculuyor. Mutasyon 1 (`OnResized`den `_settle.Start()` cikti)
  yalniz bu kolu dusuruyor.
- D2 kapandi: `Plan_degisimi_hala_yeniden_kurar` artik acik bir pencere uzerine ikinci
  bir `SetPlan` veriyor (bit hizi 300 -> 900) ve imza zinciri kosuyor: sira 1 -> 2,
  gecikme kurulu True, kodlama 1 -> 2, fabrika 2 -> 3. Negatif kontrol: ayni plan
  verilince sira 1'de kaliyor, gecikme kurulmuyor. Tur 2'nin adi yanlis kolu
  `Ilk_parca_hazir_olunca_yeniden_kurar` olarak yeniden adlandirildi, belgedeki yarim
  itiraf kaldirildi. Mutasyon 2 (`SetPlan`den `ScheduleClip` cikti) yalniz plan kolunu
  dusuruyor.
- D3 kapandi, iki ayri olcuyle:
  (a) `Durdur_baslat_boruyu_yeniden_kurmaz` — fabrika 2 -> 2, ayakta duran boruya
      ikinci `StartAsync` yok (1 -> 1), Pause/Play/Seek 1/1/0, `ActiveClip` ayni ornek
      (4 sn -> 4 sn). Mutasyon 3 (`ApplyPlayState` devamda `Restart()` cagiriyor) dusuruyor.
  (b) `Duraklatilan_boru_kaldigi_karenin_ardindan_surer` — gercek
      `PipeComparisonFrameSource`, halka bosaltildiktan sonra `Play()` sonrasi ilk kare
      0,133 -> 0,167 sn (tam bir kare, 30 fps). Mutasyon 4 (`Play()` `_resume.Set()`
      yerine `SeekAsync(0)`) dusuruyor.
  GERI CEKILEN IDDIA: ekrandaki serit konumu (`Controls.Position`) olculmedi. O deger
  `Drain` icinde gercek `RequestAnimationFrame` dongusu akarken yaziliyor, basli olmayan
  pencerede dongu hic donmuyor. Belgede acikca yazili.
- D4 kapandi: belgedeki butun satir numaralari bu commit'e gore dogrulandi. `OnResized`
  `:506`; `Restart`i dogrudan degil `_settle` uzerinden cagiriyor, zincir belgede acik.
- Uretim kodunda tek yapisal degisiklik: `DispatcherTimer` bu olcum konaginda hic
  atesenmedigi icin tikin isi iki adlandirilmis yonteme alindi — `PanelHost.SettleElapsed`
  ve `PanelHost.SegmentDelayElapsed`, yaninda `ResizeSettling` / `ClipScheduled` okumalari.
  Davranis degismedi. Olculmeyen tek halka `DispatcherTimer`in kendi OS tiki.
- K5: yedi mutasyon, her birinden once `dotnet build -c Release --no-incremental`.
  a -> Durdur_baslat_onden_hazirligi (1/6) | b -> Dosya_degisimi (1/6) |
  1 -> Boyut_degisimi (1/6) | 2 -> Plan_degisimi (1/6) | 3 -> Durdur_baslat_boruyu (1/6) |
  4 -> Duraklatilan_boru (1/6) | 5 -> Ilk_parca + Plan_degisimi (2/5, beklenen: ikisi de
  son adimda ayni `Restart`tan geciyor; 2 numara plan kolunu tek basina dusurdugu icin
  kollar ayni seyi olcmuyor).
  Her mutasyondan sonra iki uretim dosyasi yedekten geri alindi, `diff` `SAME`, yeniden
  build, yedi kol yesil.
- K6: `--list-tests` 7 kol buluyor, sifir eslesen kol yok.
- Ham cikti `.calisma/T161/tur3-tum-olculer.txt`, duzenek `.calisma/T161/mutasyon.py` +
  `kos.sh`. Rapora giren her sayi `docs/olcumler/onizleme-bastan-baslama.md` icinde de var.

---

# Tur 4 — denetim uc kucuk bulguyla dondu

Tur 3 **isin ozunu dogru yapti**; denetci sunlari tek tek dogruladi ve **yeniden
acmayacaksin**:

- D1 (boyut) gercek: `PlaybackResumeTests.cs:129-138` pencereyi 640x480 -> 320x240
  gercekten `Measure/Arrange` ediyor, `SizeChanged` elle atilmiyor. Kapilarin gecildigi
  ayri olculuyor.
- D2 (plan) gercek **ve negatif kontrolu calisiyor**: ayni plan -> `PanelHost.cs:232`
  imza esitligi -> erken donus; farkli plan -> `:235 ScheduleClip` -> `:570`.
- Mutasyon 5'in akil yurutmesi dogru; iki kol ayni seyi olcmuyor.
- **Dikis kusuru tasimiyor.** Tik govdeleri davranis degistirmeden adlandirilmis
  yontemlere tasinmis, gercek `DispatcherTimer` yolu `:116`/`:121` uzerinden hala bagli.
  `AppHost` ileti dongusu calistirmiyor, ayni sinirlama `PanelHostTests.cs:12` ve
  `ComparisonPanelTests.cs:26`'da zaten yazili — dikis mesru.
- Geri cekilen iddia durust: `Controls.Position` yalniz `Drain()` icinde yaziliyor,
  `Drain`in tek cagirani `RequestAnimationFrame`; basilmayan pencerede donmez.
- **Bos gecen olcu yok.** Yedi kolun her birinde dusurucu bir yol var.
- `owns` disina yazim yok; `SegmentEncoder.cs` ve `PipeComparisonFrameSource.cs` hic
  degismemis, mutasyonlardan geri alim tam.
- Belgedeki **butun** satir numaralari bu commit'te dogru (on alti tanesi tek tek
  dogrulandi).
- Denetci belgedeki **her sayiyi** kendi kosumuyla birebir uretti.

Uc sey kaldi.

## E1 — Kapsam cumlesi yanlis

`docs/olcumler/onizleme-bastan-baslama.md:186-187` ve `## Kayit`in son maddesi:
*"Olculmeyen tek halka `DispatcherTimer`'in kendi OS tiki, yani cerceve kodu."*

Bu **yanlis**. Olculmeyen halka bundan buyuk:

```
PanelHost.cs:116   _settle.Tick += (_, _) => SettleElapsed();
PanelHost.cs:121   _segmentDelay.Tick += (_, _) => { _ = SegmentDelayElapsed(); };
PanelHost.cs:772   Follow -> PrepareAheadAsync tetigi
```

Ucu de **uretim kodu**, her birinin uretimdeki tek referansi kendisi ve **hicbir kol
onlari gecmiyor** — ikisinden birini silsen yedi kol yesil kalir.

Sayi dogru, onu anlatan cumle yanlis. Bu deponun en pahali tekrarlayan kusuru ve bu
sozlesme uzerinde ucuncu kez cikiyor. Cumleyi olculen seye indir: hangi satirlarin kol
tarafindan gecilmedigini **tek tek say ve yaz**.

## E2 — 12 saniyelik dosyaya `DurationSeconds = 30`

`tests/VidShrink.Tests/PlaybackResumeTests.cs:191`: gercek dosya 12 sn, `MediaInfo` 30 sn
diyor. Sayilan onden hazirlik penceresi 9-14 sn — yani **dosyanin disina tasan** bir
pencere. Olcu, dosyayla celisen bir `MediaInfo` uzerinde duruyor.

Kirpilmayi devre disi birakmanin **durust alternatifi vardi**: parcayi 4 yerine **2**
sn'den baslat — `Clamp(12, 7) = 7`, sonraki pencere 12 sn icinde kalir, `PrepareAheadAsync`
tekillik kontrolu (`PanelHost.cs:644`) bozulmaz.

Denetci sureyi buyutmenin gercek bir karistiriciyi kaldirdigini da dogruladi (mutasyon (a)
hala dusuruyor), yani olcu **kolaylastirilmis degil**. Yine de dosyayla celisen bir sure
ile olcum almak bu depoda kabul edilmez.

Parcayi 2 sn'den baslat ve sureyi gercek dosyaya esitle. Yapamiyorsan **yapma, bildir** —
ama belge (`:252-255`) 30 sn'yi tek care gibi sunmayi birakacak.

## E3 — Plan kolunun negatif kontrolunun mutasyonu yok

`tests/VidShrink.Tests/PlaybackResumeTests.cs:370-371`
(`Assert.Equal(oncekiSira, ayniPlanSira)`, `Assert.False(ayniPlanGecikme)`) yedi
mutasyonluk izgarada **hicbir mutasyonla dusurulmuyor**; 2 numara yalniz pozitif yariyi
dusuruyor.

Denetci bu yarinin **bos olmadigini** kanitladi — imza `null` donse ya da eslesmese
`ayniPlanSira` 2'ye cikar ve `:370` duserdi. Ama tur 3'un kendi sarti *"her olcu icin
dusebildigini gosteren mutasyon"* bu yari icin karsilanmadi.

Bir mutasyon ekle: `PanelHost.cs:232`deki imza esitlik kapisini kaldir (ayni plan da
`ScheduleClip`e dussun) — bu yarinin dusmesi gerekir.

## Tur 4 kabul kriteri

E1, E2, E3'un **ucu de** kapanacak. Uretim mantiginda **hicbir sey degismeyecek** — bu tur
bir belge duzeltmesi, bir olcum senaryosu duzeltmesi ve bir mutasyon eklemesidir.
`PanelHost.cs`in davranisina dokunma; dokunman gerektigini dusunuyorsan **yapma, bildir.**

- Tur 4 teslim: dal `T161-onizleme-devam`, commit `093dae7`, `origin`e itildi. Uretim
  kodu degismedi (`git diff --stat src/` bos).
- E1 kapandi, ama bulgu sozlesmedekinden **buyuk**: sonda olcumune gore `Drain` **kosuyor**
  (`:709` sondasi 6 kolu dusuruyor), kare gelmedigi icin `:743`ten sonrasi kosmuyor.
  Gecilmeyen uretim satirlari belgede tek tek sayili: `:116`, `:121`, `:743-758` ve
  yalniz o kuyruktan ulasilan bes yontem — `Follow` `:766-780`, `AdvanceClip` `:658-676`,
  `BeginHandover` `:805-815`, `OpenStandbyAsync` `:817-845`, `SwapToStandby` `:859-880`.
  Sonda izgarasi: 116->7/7 yesil, 121->7/7 yesil, 709->6 basarisiz, 743->7/7 yesil,
  766->7/7 yesil. Duzenek `.calisma/T161/kapsam.py` + `sonda.sh`.
- E2 kapandi: `duration: 30` uc cagridan da kalkti, `MediaInfo` gercek dosyaya esit
  (12 sn), `Hazirla` parcayi 2 sn'den basliyor (`Clamp(12,7)=7`). Mutasyon (a) hala
  dusuruyor (`Expected 1, Actual 2`). Belgedeki "30 sn tek care" sunumu kaldirildi.
- E3 kapandi: 6 numarali mutasyon (`PanelHost.cs:232-233` imza esitlik kapisi silindi)
  yalniz `Plan_degisimi_hala_yeniden_kurar`i dusuruyor — negatif kontrolun ta kendisi.
  Izgara artik sekiz mutasyon.
- Sekiz mutasyonun tamami yeniden kosuldu; her birinden once
  `dotnet build -c Release --no-incremental`, sonrasinda `diff` `SAME`.
- Dogrulama: build 0/0; filtreli test 7/7; `--list-tests` 7 kol.
- T0 notu: ham cikti ve duzenek `.calisma/T161/` altinda ve `.calisma/` gitignore'da.
  AGENTS.md "olcumu ureten duzenek tools/a tasinir" diyor. Denetime soruldu.
