---
id: T107
title: Yerlesim skoru olculen kaliteyi tahmin etmiyor
role: builder
model: opus
depends: [T99]
status: active
round: 1
verify: [dotnet test -c Release --filter "PlanCalculatorTests|ExtremeCompressionTests"]
owns: [src/VidShrink.Core/PlanCalculator.cs, src/VidShrink.Core/CompressionStrategy.cs, src/VidShrink.Core/CodecModel.cs, tests/VidShrink.Tests/PlanCalculatorTests.cs, tests/VidShrink.Tests/ExtremeCompressionTests.cs, docs/olcumler/yerlesim-skoru.md, docs/olcumler/bppf-tabani.md]
side_effects: []
---

## Bağlam

T99 bppf tabanını ölçtü ve indirdi (av1 0,020 → 0,0095). Taban artık kazanan
yerleşimi elemiyor. **Ama plan hâlâ kaybedeni seçiyor.**

Ölçülen: `1280x720@60`, `882x496@60`'ın **6,39 puan** önünde — iki ayrı
pencerede de aynı yönde. Sayılar `docs/olcumler/bppf-tabani.md` §K2'de.
Seçilen: `882x496@60`. Eleyen taban değil, **skor**.

Kaybettiren yer belli: `PlanCalculator.LayoutScore` (`PlanCalculator.cs:618`).

```
rate = level.AtReference - level.PerHalving * Log2(required / provided)
rate = Min(rate, CodecModel.QualityLimit(codec))
score = rate - ScalePenalty(scale, weights) - FpsPenalty(fps, sourceFps, weights)
```

Düşük çözünürlükte `provided` büyür, `required` küçülür, `rate` fırlar.
`ScalePenalty` bunu dengelemesi gerekiyor ama dengelemiyor — ölçüye göre
6,39 puan eksik dengeliyor. Yani modelin iki yarısı da tahmin: `rate`
eğrisi ve ceza eğrisi. **Hiçbiri ölçülmüş kaliteye karşı kalibre edilmedi.**

Bu, ürünün en üst hedefine doğrudan bağlı: hiçbir ayar bilmeyen kullanıcı
auto modda uzmana yakın sonuç alacaksa, **auto'nun seçtiği yerleşim
ölçülen en iyi yerleşim olmalı.** Bugün değil ve farkı biliyoruz.

## Bu sözleşme neyi değiştirir

Skor modelini **ölçüye göre** kalibre eder. Ölçüsüz oynatmaz.

`ScaleCandidates` ve `FpsCandidates` aday havuzunu üretiyor, `SearchLayout`
seçiyor. Havuz sorunu değil — kazanan havuzda var, seçilmiyor. Dokunacağın
yer skor, havuz değil. Havuzu değiştirmen gerektiğine ölçüyle kanaat
getirirsen ayrı yaz ve gerekçelendir.

## T99 denetiminin biraktigi borclar (T0, 2026-09-02)

T99 GECTI aldi; asagidakiler tur actirmadi ama kapanmadi. Dosyalari T99
muhurlenince serbest kaldi ve `owns`'una eklendi. **Bu turda kapansin.**

- **`CodecModel.FpsBitrateExponent` olu kaldi** — `src/VidShrink.Core/CodecModel.cs:12`.
  `main`de iki kullanicisi vardi (`ComplexityProfile.cs:73`,
  `ExtremeCompressionTests.cs:124`); T99 ikisini de kaldirdi, sabiti birakti.
  Tum agacta tek eslesme kendi tanimi. Cikitiyi etkilemiyor — sil.
- **Sutun "5'in altindaki kare", cumle "zaten sifirda"** —
  `docs/olcumler/bppf-tabani.md:163` basligi `5'in altindaki kare (n=3600)`,
  ama `:203` "karelerin %3,6'si **zaten sifirda**" diyor; ayni yanlis etiket
  `:263`'te ve olcu mesajinda `tests/VidShrink.Tests/PlanCalculatorTests.cs:411`
  ("one frame in thirty is at zero") tekrarliyor. 128/3600 = %3,56 dogru,
  **saydigi sey yanlis adlandirilmis.** Sonucu degistirmiyor, adi degistir.
  Bu projenin tekrar eden kusuru: betigin sutun adi saydigiyla uyusmuyor.
- **Iki taban korumasi bir kbit toleransla gevsetildi** —
  `tests/VidShrink.Tests/ExtremeCompressionTests.cs:66-68` ve
  `tests/VidShrink.Tests/PlanCalculatorTests.cs:319-320` artik
  `bppf + 1000/pixelRate >= floor || Notes.Contains(TargetBelowCodecFloor)`
  diyor; `main`de kosul katiydi. Denetci kati haliyle kosturdu, kirmizi: 1 MB
  hedefinde plan `libx264 384x216@25 128k`, uygulanan taban 0,061777, plan
  0,061728 — **0,000048 bppf altinda** ve `TargetBelowCodecFloor` notu yok.
  Bosluk bir tamsayi kbit adiminin (0,000482) onda biri. T99 §7.3 bunu
  adlandirip erteledi. **Sen ya toleransi kaldir ve plani tabanin ustune cikar,
  ya da notu uret** — gevsetilmis kosul kalici cozum degil.
- **"Tabani da ureten alet bu" ifadesi gevsek** — `bppf-tabani.md:256`. Taban
  p10 egrisinin **doygunluk** noktasindan (0,00669), carpan ise **en dik
  araligin** geometrik ortasindan (0,01387/0,00912) geliyor; ayni egrinin iki
  farkli ozelligi, tek alet degil. Cumleyi olctugu seye gore yaz.
- **`hevc_nvenc` tabani olculmeden %21,6 sikildi** (0,02196 → 0,02671),
  carpanin yan etkisi. T99 §5.3 bunu **olculmedi** diye acikca yaziyor —
  gecistirme yok, borc duruyor. Bu turda olcebiliyorsan olc, olcemiyorsan
  "hala olculmedi" diye tasi.
- **T99'un `verify:` satiri dardi** — tabana dair uc olcu
  (`ACheaperFamilyClearsALayoutTheDearerFamiliesReject`,
  `TheFloorFollowsTheContentAndTheFrameRate`,
  `OneMegabyteTargetCollapses...`) `ExtremeCompressionTests` icinde ve filtrenin
  disindaydi. Bu sozlesmenin `verify` satiri ikisini de kapsiyor; oyle kalsin.

## Kabul kriteri

1. **Skorun bugünkü tahmini ile ölçülen kalite yan yana konur.** En az
   **on iki yerleşim** için (çözünürlük × kare hızı ızgarası, aynı hedef
   boyut) iki sütun: modelin verdiği `score`, ve gerçekten kodlanıp
   ölçülen VMAF-NEG. En az **iki farklı kaynak** — biri hareketli, biri
   durgun.
   CHECK: yerleşim başına bir satır, iki sütun, iki kaynak.

2. **Sıralama hatası sayılır.** Model ile ölçüm aynı sıralamayı veriyor mu?
   Spearman sıra korelasyonu yazılır **ve** ters sıralanan çiftler tek tek
   listelenir. Asıl ürün korelasyon değil, **hangi çiftlerde yanıldığı**.
   CHECK: korelasyon + ters çift listesi.

3. **Hangi yarının yanıldığı ayrıştırılır.** İki aday var: `rate` eğrisi
   (`AtReference` / `PerHalving`) ve ceza eğrileri (`ScalePenalty` /
   `FpsPenalty`). Birini sabitleyip ötekini ölçüye oturt, sonra tersini
   yap. Hangisinin payı büyük?
   **Ayrıştıramıyorsan "ayrıştırılamadı" yaz** — ikisini birden elle
   oynatıp "şimdi uydu" demek kalibrasyon değil, aşırı uydurmadır.

4. **Yeni model aynı ızgarada doğrulanır.** K1'in ızgarasına uydurduğun
   model, **ölçüme girmemiş** en az dört yerleşimde sınanır. Bu dört
   yerleşim K1'in kaynaklarından değil, **üçüncü bir kaynaktan** gelir.
   Tutmuyorsa tutmadığı yazılır; uydurduğun ızgarada iyi olması sonuç
   değildir.
   CHECK: dört yerleşim, tahmin/ölçüm farkı.

5. **6,39 puanlık örnek kapanır ya da neden kapanmadığı yazılır.** Yeni
   modelde `1280x720@60` mı seçiliyor? Seçilmiyorsa ölçüm hâlâ onun daha
   iyi olduğunu mu söylüyor? İkisi çelişiyorsa çelişki yazılır, gizlenmez.
   CHECK: yeni modelin o kaynakta seçtiği yerleşim.

6. **Donanım kolu ayrı sınanır.** T99 §3.2: donanım kolunda
   `UsableBitrateK` (706k = 0,01277 bppf) yeni tabandan sıkı, taban orada
   atıl. Skor modeli değişince donanım kolunda ne oluyor? En az iki
   yerleşim donanım kodlayıcısıyla ölçülür. Ölçemiyorsan **"donanım kolu
   ölçülmedi"** yaz; sessizce yazılım kolunun sonucunu genelleme.

7. **Sabit değişirse üstündeki cümle aynı commit'te değişir.** Bu projenin
   tekrar eden kusuru: değer değişiyor, açıklaması eskiyor. `PenaltyWeights`
   içindeki her sayı için ya "ölçüldü, değişti" ya "ölçüldü, değişmedi" ya
   da "ölçülmedi" yazılır. Üçünden biri.

8. **Mutasyon kanıtı.** Kalibre edilen her sabit iki yönde bozulunca ölçü
   kırılmalı. İki sabiti karşılaştıran ölçü yazma, `Skip` yazma, ffmpeg
   yokluğunda sessiz erken dönüş yazma. ffmpeg gerektiren ölçü
   `[FfmpegFact]`.

## Sınırlar

- `owns` dışına yazma. T99 mühürlenince `CodecModel.cs`,
  `ExtremeCompressionTests.cs` ve `bppf-tabani.md` sana geçti — yukarıdaki
  borçlar için onlara yazabilirsin. `ComplexityProfile.cs` **yazamazsın**,
  `FfmpegArguments.cs` T108'in, `SceneMap.cs` T105'in; hepsini okuyabilirsin.
  Orada düzeltilmesi gereken bir şey bulursan rapora yaz, ayrı sözleşme açarım.
- **Telafi sabiti yazma.** Başka bir turun düzelttiği bir kusuru dengelemek
  için konan sabit koda girmez. T98 × T105 çakışmasında bu iki tur
  birbirinin işini bozuyordu; kural yol haritasında yazılı. Bir katsayıyı
  "böyle olunca sayı tutuyor" diye koyuyorsan **neyi telafi ettiğini bul**,
  telafiyi değil nedeni düzelt ya da nedeni bana bildir.
- **Harmonik ortalamaya yaslanma.** T106 ölçüm aracının AV1 çıktılarında
  harmonik sütunu eserli ürettiğini inceliyor. Sonucunu ortalama ve p10
  üzerine kur; harmonik yazacaksan "T106 sonuçlanmadı, geçici" damgası bas.
- Ölçüm dosyaları `.calisma/` altına, düzenek `tools/` altına. `.calisma/`
  gitignore'lu ve worktree-yerel — **oraya koyduğun düzeneği denetçi
  göremez.** Rapora giren her sayının üreten komutu yazılı olsun.
- `.calisma/kaynak/` ortak, dokunma.
- Makine paylaşımlı, altı ajan koşuyor. Kodlama komutlarında iş parçacığını
  **sabitle** (`-threads N`, x265'te `pools`). Süre sayısına "makine
  paylaşımlıydı" damgası bas; kalite ve boyut sayısına basma.
- Tam süiti koşturma; yalnız `verify` filtresi.
- Kod yorumu yazma.

## Kayıt noktası

Dal `T107-yerlesim-skoru` itildi, worktree `.claude/worktrees/T107`.
Commit'ler: `4a6377d` (T99 borclari), `9fa1cda` (skor modeli + uc olcu),
`1865c35` (olcum raporu), `0c00381` (bir cumle olctugu seye gore duzeltildi),
`ab70c85` (K2 ters cift listesi tam yazildi), `247eb74` (atlanan/kirmizi sayilari
CI kosumundan alindi), `2a42bf0` (donanim kolu ters cift sayilari duzeltildi,
dorduncu kirmizi eski modelde olculdu, asiri rejim uretim komutu yazildi).
Bes yeni commit yalniz `docs/`.
**Olculen commit `9fa1cda`** — `src/` altinda ondan sonra tek satir degismedi.

Sekiz kabul kriterinin sekizi de kosuldu ve yazildi. Verify filtresi yesil:
`dotnet test -c Release --filter "PlanCalculatorTests|ExtremeCompressionTests"`
-> 0 basarisiz / 35 basarili / 3 atlandi.

**Duruyor: `owns` disinda DORT kirmizi.** FillBandTests, QualityTargetUiTests,
SpeedModeTests, QualityTargetTests. Dordu de beklenen degeri eski modele
civilenmis duzenek. Dosyalara dokunulmadi. CI kosumu 33592420948 (headSha
`ab70c85`): Failed 4 / Passed 1074 / Skipped 105 / Total 1183. Ayrinti asagida.

## Çıktı

- `docs/olcumler/yerlesim-skoru.md`: model-tahmini ve ölçülen kalite yan
  yana, ters sıralanan çiftler, hangi yarının yanıldığı, kalibre edilmiş
  sabitler, doğrulama ızgarası, donanım kolu.
- Kalibre edilmiş skor modeli ve onu pimleyen ölçüler.
- Kendi dalın, itilmiş. `main`e sen birleştirme.

### Teslim (yapici, 2026-09-02)

**Rapor: `docs/olcumler/yerlesim-skoru.md`.** Olculen commit `9fa1cda`.
Dal `T107-yerlesim-skoru` itildi; `main`e birlestirilmedi.

Kusur bulundu ve kapatildi: sabit videoK'da cozunurluk yarilaninca `rate`
`PerHalving*(DetailExponent+2)*log2(1/scale)` kadar yukseliyordu. `provided`
bppf `1/scale^2` ile buyurken `required` yalnizca `scale^DetailExponent` ile
kuculuyor. Bu kredi icerige bagli (olculen: A 11,17 / B 14,98 / C 17,27 puan),
sabit `ScalePenalty(0.5)=7,00` bunu hicbir kaynakta dengeleyemez.
`Decompose` artik `rate`'i kaynak izgarasinda hesapliyor; iki cozunurlugu
ayiran tek sey `ScalePenalty` oldu.

**Hicbir sabit degistirilmedi.** Degisen tek sey `rate` teriminin bicimi.
Telafi sabiti yok. Gerekce K4'te: uydurulmus ceza sabitleri sinanmamis
kaynakta daha iyi cikmadi (dort varyantin dordu de C'de `+0,667`, 6/28).

| Kriter | Durum | Nerede |
|---|---|---|
| K1 12 yerlesim x 2 kaynak, iki sutun | yapildi (3 kaynak) | rapor §3 |
| K2 Spearman + ters cift listesi | yapildi | §4 |
| K3 hangi yari yaniliyor | yapildi: `rate` | §5 |
| K4 ucuncu kaynakta dogrulama | yapildi (8 yerlesim) | §7 |
| K5 6,39 puanlik ornek | kapandi | §8 |
| K6 donanim kolu | olculdu, **yeni model daha kotu** | §9 |
| K7 her sabit etiketlendi | yapildi | §11 |
| K8 mutasyon kaniti (iki yon) | yapildi | §13 |

Sayilar: eski model Spearman A `+0,478` / B `-0,687` / C `-0,286`; yeni model
A `+0,643` / B `+0,967` / C `+0,667`. Ters cift 29->19, 57->4, 18->6.
C ucuncu kaynak, hicbir uydurmaya girmedi.

**Gizlemedigim uc kusur:**
1. Donanim kolunda (`av1_nvenc`) yeni model **daha kotu**: eski 1/10 ters,
   yeni 9/10 (esit boyut olmayan 1080p satiri atilinca 1/6'ya karsi 5/6).
   Kucultme orada gercekten kazaniyor.
2. Asiri rejimde (350k) iki model de siralayamiyor (`+0,261` / `+0,285`);
   yeni modelin isareti 1080p-720p ciftinde yanlis.
3. Sebep ikisinde de ayni: **olcek bedeli bit hizina bagli.** A'da 400k'da
   540p, 800k/1600k/3200k'da 1080p kazaniyor; gecis 400k-800k arasinda.
   Olcegin tek fonksiyonu olan bir ceza terimi bunu ifade edemez.
   Rapor §12'de acik kusur olarak duruyor.

**T99 borclari:** olu `FpsBitrateExponent` silindi; "sifirda kare" etiketi
sayilan seye gore adlandirildi; bir kbitlik tolerans iki olcuden kaldirildi ve
plan tabanin ustune cikariliyor; `bppf-tabani.md:256` cumlesi olctugu seye gore
yazildi; **`hevc_nvenc` tabani olculdu** (sekiz nokta, 0,005016-0,037958 bppf) —
ne 0,02196'da ne 0,02671'de diz var, iki aday da cokusun cok ustunde, taban
yeniden konmadi.

### T0 karari gerekli: dort olcu `owns` disinda

Degisiklik dort olcuyu dusuruyor, dordu de `owns` listesinde degil, dosyalara
dokunulmadi. **CI bu yuzden kirmizi** (kosum 33592420948, headSha `ab70c85`).

| Olcu | Dosya | Ne diyor |
|---|---|---|
| `FillBandTests.FillTargetReachesTheBandWhenTheCeilingWouldLeaveItUnfilled` | `tests/VidShrink.Tests/FillBandTests.cs:97` | "adjust the fixture if the model changes" — duzenek seffaflik tavaninin bandin altina dusmesine civilenmis |
| `QualityTargetUiTests.SinirDurumuEkrandaYazili` | `tests/VidShrink.Tests/QualityTargetUiTests.cs:167` | duzenek artik tabana dayanan plan uretmiyor, "taban" satiri bos |
| `SpeedModeTests.QualityModeLeavesTodaysPlansUntouched` | `tests/VidShrink.Tests/SpeedModeTests.cs:346` | elle yazilmis plan dizisi anlik goruntusu |
| `QualityTargetTests.SearchCostIsBoundedAndCounted` | `tests/VidShrink.Tests/QualityTargetTests.cs:313` | `KeyNotFoundException: 'BelowFloor'` — sayac sozlugunde artik taban altina dusen plan yok |

Ilk ucunun eski modelde yesil oldugunu olctum: `rate` terimi geri alinmis halde
ayni filtre 0 basarisiz / 52 basarili / 1 atlandi; yeni modelde 3 basarisiz.
Dorduncusunu de ayrica olctum: eski modelde 0 basarisiz / 1 basarili (1 dk 23 sn),
yeni modelde 1 basarisiz (2 dk 51 sn).
Dordu de eski modele civilenmis beklenti; hicbiri yeni modelin bozuk oldugunu
soylemiyor. Duzeltmek `owns` disina yazmayi gerektiriyor.

### Tam suit ve atlananlar

Yerel `bash tools/ci-gibi-kos.sh` koydugum 540 sn sinirinda **iptal edildi**
(gunlugun sonu: "Test ana islemi kilitlendi"); bastigi
`3/1032/67/1102` ozeti kismi kosumun ozetiydi ve dorduncu kirmiziyi
(`SearchCostIsBoundedAndCounted`) hic calistirmamis; o olcu sonradan tek basina
kosuldu ve iki modelde ayri ayri olculdu.
Gecerli sayilar CI kosumu 33592420948'den:
`Failed 4 / Passed 1074 / Skipped 105 / Total 1183`, 14 dk 9 sn.
**Kabul kriteri olculeri atlanan listesinde degil** — K8'in bes durumu CI'in
105 atlanani icinde degil, ucu de `ComplexityProfile.FromProbe` uzerinden
calisiyor ve kodlayiciya gitmiyor. Atlananlarin sinif dagilimi rapor §14'te.

### Rapor: kapsam disinda gorulen, dokunulmadi

- `tests/VidShrink.Tests/QualityTargetTests.cs:17` — `OneScanStep = 1.005` elle
  kopya. `PlanCalculator.QualityScanStep` artik public, kopyanin sebebi kalmadi.
- `tests/VidShrink.Tests/ChipTests.cs:241` — `Assert.Equal(7, count)`, altin sayi.
- `docs/inceleme/argumanlar.md:100` ve `docs/cpu-algoritma-checkup.md:85` —
  silinen `FpsBitrateExponent`'e atif yapiyor, bayat.
- Bit hizi yukseltmesi icin `ReasonCode` uretilmedi; `EncodePlan.cs` owns'ta degil.
- `ComplexityProfile.SlopeMeasured` uc kaynakta da `False`; model 6 varsayilanini
  kullaniyor, olculen `PerHalving` 1,60 / 4,733 / 13,018. Ayri sozlesme.
- `CodecModel.UsableBitrateK("av1_nvenc",1920,1080,60)` = 1590k diyor, olcum ayni
  yerlesimde 729,0 kbps teslim etti. Tahmin olcumle uyusmuyor.
- `tools/yerlesim-skoru/Skor.csproj` `VidShrink.sln`'de degil; `dotnet build
  VidShrink.sln` onu derlemiyor ve `--no-build` eski ikiliyi kosturuyor. Bir kez
  yanlis okuma uretti, ayrica derlendi.
- `tools/ci-gibi-kos.sh` 540 sn'de kesilince arkasinda kilitli `testhost` biraktı
  (PID 29220); sonraki derlemeler MSB3021 verdi. Yalniz o PID oldurüldu,
  `_sorun.log`'a yazildi.

## T99 muhurlendi — tur 3 denetiminden kalan borclar da sana (T0, 2026-09-02)

Yukaridaki `## T99 denetiminin biraktigi borclar` bolumune ek olarak tur 3
denetiminden dort sey geldi:

1. **Atif bir satir kaymis.** O bolumde `QualityTargetTests.cs:16` yaziyor;
   `OneScanStep = 1.005` dalda **17**. satirda (16 `LadderStepThreshold`).
   Denetci bunu yerinde saydi. Duzelt.
2. **`ChipTests.cs:241` `Assert.Equal(7, count)` altin sayi.** Beklenen 7,
   davranistan turetilmiyor; taban ya da ustel mesru bir sebeple degisirse olcu
   yine elle guncellenecek. Senin `owns`'unda degil — **dokunma**, ama
   `PlanCalculatorTests.cs` ve `ExtremeCompressionTests.cs` icinde ayni kalibin
   ornegi varsa onlar senindir ve duzelt.
3. **Kesim asiri belirlenmis.** `bppf-tabani.md:428` artik dogru: taban da ustel
   de tek basina eliyor. `MotionExponentMax` kelepcesi `ComplexityProfile.cs:259`
   yalniz **olculmus** profilde bagliyor; olculmemiste `:260`
   `DefaultMotionExponent`'e dusuyor. Yerlesim skorunu olculen kaliteye
   baglarken bu ikili yolu hesaba kat — olculmemis profille gelen kullanici
   kelepceyi hic gormuyor.
4. **T95'ten gelen gozlem** (`ab-duzenegi.md` `sonuc-parca.json` `Settings`):
   uc parcada uc ayri karar cikti — parca-1 **1382x778 @4861k**, parca-2
   **1920x1080 @4712k**, parca-3 1190x670 @4712k. **Kazandigimiz tek satir
   1920x1080 olani** (95,84 ↔ 95,78, XPSNR 51,25 ↔ 47,11). Yani cozunurluk
   dusurmedigimiz yerde kazaniyoruz. Yerlesim skoru bunu soyluyorsa neden
   digerlerinde dusurmeyi sectigini olc; soylemiyorsa skor yanlis yere bakiyor.

**Ayrica bir olcum disiplini borcu var, senin dosyanda degil ama ayni tuzak:**
T99'un mutasyon betikleri `--no-incremental` kullanmiyordu ve yalnizca dosya
mtime'ina guveniyordu. Bu projede o tuzak bir kez yanlis kirmizi uretti. Senin
her mutasyon kosumun oncesinde `dotnet build VidShrink.sln -c Release
--no-incremental` **zorunlu**.
