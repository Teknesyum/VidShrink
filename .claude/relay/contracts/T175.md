---
id: T175
title: Kalici kodcozucu borusu — her aramada ffmpeg baslatmak birakiliyor
role: builder
model: sonnet
depends: [T167]
blocked-by: []
status: submitted
round: 2
verify:
  - dotnet test --filter "OynaticiBoruTests"
owns:
  - tools/VidShrink.PlaybackProbe/
  - src/VidShrink.Ffmpeg/Playback/DecoderPipe.cs
  - src/VidShrink.Ffmpeg/Playback/AudioSink.cs
  - src/VidShrink.Ffmpeg/Playback/PlaybackClock.cs
  - src/VidShrink.Ffmpeg/VidShrink.Ffmpeg.csproj
  - tests/VidShrink.Tests/OynaticiBoruTests.cs
  - docs/olcumler/oynatici-boru.md
side_effects: []
---

## Baglam

T167 dort sutunda olctu ve kararini yazdi
(`docs/olcumler/oynatici-hatti.md`): **kendi ffmpeg borumuz + NAudio**, LibVLC degil.
Gerekce olculmus: ses maliyeti 518.848 B'ye karsi 106-293 MB, kurulum boyutu
96,66 MB / 234 dosyaya karsi 197-376 MB / 654-1491 dosya, senkron beraberlik
(kendi 1,9 ms — vlc 2,1 ms, 123 s uzerinde).

**Kaybettigi tek sutun arama gecikmesi**: LibVLC medyan 2,9-3,7 kat hizli, surec
baslatma tabani dusuldukten sonra 2,0-2,6 kat. Olculen kendi gecikmemiz 142-193 ms.

Sebep olculdu: **her aramada yeni bir `ffmpeg` sureci basliyor.** T167 kazanci da
olctu — kalici bir kodcozucu borusu **en az 51,6 ms**, ust sinir 91-141 ms kazandirir.

Bu sozlesme o boruyu yaziyor. **Arayuz yok** — T176'nin isi. Burada yalniz hat var:
kodcozucu borusu, ses cikisi, saat.

**Geri donus kriteri T167'de yazili ve bu sozlesmede baglayicidir:** urun icinde arama
gecikmesi **150 ms'in altina inmezse** LibVLC'yi acmak mesrudur. O durumda bu sozlesme
kirmizi teslim edilir — olcuyu gizleyip yesil yazma.

## Kabul kriteri

**K1 — Once bugunku gecikmeyi kendi makinende yeniden olc.** T167'nin sayilari baska
bir kosumdan; taban cizgini **kendin uret**. En az 30 arama, medyan ve 95. yuzdelik,
kisa/orta/uzun uc kaynakta. `.calisma/kaynak/` altindaki kaynaklari kullan.
CHECK: uc kaynak icin ham gecikme dizisi, medyan ve p95, kosum sayisi acikca yazili.

**K2 — Kalici boru.** `DecoderPipe` tek bir `ffmpeg` surecini ayakta tutar; arama
surece **yeniden baslatmadan** yapilir. Surec olurse (cokme, kaynak kaybi) boru
sessizce olmez: yeniden kurar ve bu olayi disari bildirir.
CHECK: bir arama dizisi boyunca baslatilan `ffmpeg` surec sayisi — 30 aramada kac surec?
Ham surec sayimi, ve surec oldurulup yeniden kuruldugu bir kosum.

**K3 — Gecikme 150 ms'in altinda.** K1'in ayni olcusunu yeni boruyla tekrarla. Medyan
**ve** p95 uc kaynagin **hepsinde** 150 ms'in altinda olmali. Bir kaynak asiyorsa
**gizleme, yaz** ve LibVLC geri donusunu acikca oner — T167'nin kriteri budur.
CHECK: oncesi/sonrasi tablosu, kaynak basina medyan ve p95, kazanc ms cinsinden.

**K4 — Ses.** NAudio ile ses cikisi calisir; ses ve goruntu **senkron kalir**. T167'nin
olctugu bicimde olc: en az 120 s boyunca kayma. T167 kendi hattinda 1,9 ms olctu; bu
hat onu **bozmamali**. Ses kaynakta yoksa oynatici sessiz oynar, cokmez.
CHECK: kayma olcusunun ham ciktisi, sure ve ornek sayisi; sessiz kaynakla bir kosum.

**K5 — Kurulum boyutu buyumez.** T167 kendi hattini 96,66 MB / 234 dosya olctu. NAudio
girdikten sonra **yeniden olc**. Buyume varsa sayisiyla yaz.
CHECK: `dotnet publish` ciktisinin boyut ve dosya sayimi, oncesi/sonrasi.

**K6 — Mutasyon.** Iki mutasyon, her birinden once
`dotnet build -c Release --no-incremental`; **`--no-build` yasak:**
(a) Boruyu her aramada yeniden baslat (eski davranis) — bir olcu olmeli;
(b) Ses saatini goruntu saatinden ayir (senkronu boz) — bir olcu olmeli.
CHECK: iki satirlik izgara, kirilan olcu adlari, ham cikti.

**K7 — Kol sayisi.** `verify` satirini `--list-tests` ile kos. Sifir bulan kol varsa
teslim etme, bildir. Bu depoda **dort** kez oldu.
CHECK: filtre kolu icin test sayisi.

## Sinirlar

- `owns` disina yazma. `src/VidShrink.App/**` **tumuyle disarida** — arayuz T176'nin
  isi, ve T173/T174 su anda o agaci yaziyor.
- `src/VidShrink.Core/**` ve `EncodeRunner.cs` okunacak, yazilmayacak. Kucultme yolu
  bu sozlesmede **degismez**; boru ayri bir hattir.
- LibVLC paketi ekleme. Olcu 150 ms'i asarsa **oneriyi yaz, paketi ekleme** — o ayri
  bir karardir.
- `VidShrink.PlayerProbe` T167'nin olcum duzenegi; okuyabilirsin, `VidShrink.sln`e
  ekleme (CI'ya LibVLC/NAudio cekiyor).
- Kod yorumu yazma.
- **Filtresiz `dotnet test` kosturma** — tum suit bu makinede 29 dakika suruyor.
- Gecikme olcumu **sessiz makine ister**. Olcumden once calisan agir isi bekle; makine
  mesgulken alinan sayi bu sozlesmede gecersizdir.
- Gecici her sey `.calisma/T175/` altina. `.calisma/kaynak/` paylasilan — okuyabilirsin,
  **yazma ve silme.**
- "Hepsi", "her", "altinda" diyen her cumlenin altindaki sayiyi **kendin olc ve ham
  ciktiyi goster.** Bu depoda tablo dogru cikip onu ozetleyen cumle 26 kez yanlis cikti.
- `.claude/relay/**` T0'in alani; sozlesme dosyasina ve `LOG.md`e dokunma.

## Kayit noktasi

Dal: `T175-oynatici-boru`, guncel `main`den ac, `origin`e it.
**`main`e birlestirme** — onu T0 yapar.

## Cikti

`docs/olcumler/oynatici-boru.md` — K1'in taban cizgisi, K2'nin surec sayimi, K3'un
oncesi/sonrasi gecikme tablosu ve 150 ms hukmu, K4'un kayma olcusu, K5'in boyut
karsilastirmasi, K6'nin mutasyon izgarasi, K7'nin kol sayisi.

## Tur 1 — teslim reddedildi (T0, denetciye gitmeden)

Teslim uc kez yapildi, ucunde de kabul kriterlerinin hicbirinin ham ciktisi yoktu.
Bugune kadar biten is: kilitlenme kusuru duzeltmesi, CS1998 duzeltmesi, yorum temizligi,
olcum kaynaklari, olcum duzenegi ve K5 (234 dosya / 101.414.967 bayt; T167 temeli
228 dosya / 100.835.627 bayt -> +6 dosya / +579.340 bayt).

Eksik: K1 temel gecikme, K2 surec sayimi ve cokme kurtarmasi, K3 yeni boru gecikmesi
(uc kaynakta medyan **ve** p95 < 150 ms), K4 kayma, K6 iki mutasyon,
`docs/olcumler/oynatici-boru.md`.

Bu sozlesme, alti kalemin altisi ham ciktisiyla gelene kadar denetciye verilmez.
Teslim, sozlesmenin bittigini soyler; koda dokunuldugunu degil.

## Tur 1 — denetim bulgulari (HUKUM: KALDI, 11 bulgu, 2 kritik)

Denetci `owns` disina yazilmadigini dogruladi (6 dosya), uc uretim hatasinin ucunu de
bagimsiz dogruladi (`ffmpeg 9.0-full_build` bu makinede `-vsync` icin
`exit=8, Unrecognized option 'vsync'` veriyor), K5'i yeniden sayip tutturdu
(234 dosya / 101.415.623 bayt), K1/K3 tablosundaki on iki sayiyi ham loglardan yeniden
hesaplayip dogruladi, mutasyon sonrasi agacin temiz oldugunu ve 15/15 yesili gordu.

**KRITIK 1 — K2 "geciyor" hukmu kendi ham sayiminin tersini soyluyor.**
`docs/olcumler/oynatici-boru.md:33` "30 aramanin hicbirinde 30 yeni surec baslamiyor;
cogu arama var olan sureci surduruyor" diyor ve `:153` K2'yi gecmis sayiyor. Ham sayac:
`.calisma/T175/k3-orta-v2.log` -> `surec_baslatma=32` (n=30);
`.calisma/T175/k3-uzun-v2.log` -> `surec_baslatma=36` (n=30). 30 aramada 36 surec,
atilmak istenen "her aramada bir surec" davranisindan **daha kotu**. K2'nin baglayici
metni "arama surece yeniden baslatmadan yapilir" diyor. Kalicilik yalniz mixed modda
(7/7/11) gercek. Duzelt: ya kalicilik gercekten saglanir, ya K2 kirmizi ilan edilir.

**KRITIK 2 — K3'un kok neden cumlesi kendi K1 tablosuyla celisiyor; LibVLC onerisi o
yanlis nedene dayaniyor.** `oynatici-boru.md:63-68` kuyrugu "K1 tabaninin kendisinde de
gorulen 800 ms'lik uc degerlerle ayni kokten besleniyor, borunun yeniden baslatma
mekanigi bunu ortadan kaldirmiyor" diyor. Ayni kaynak, ayni hedef listesi:

| uzun/uniform | medyan | p95 | maks | >250 ms ornek |
|---|---|---|---|---|
| K1 tabani (`k1-uzun.log`) | 146,1 | 179,2 | 226,3 | 0 / 30 |
| K3 boru (`k3-uzun-v2.log`) | 160,0 | 1066,4 | 1142,2 | 13 / 30 |

ffmpeg'in arama+ilk-kare maliyetini izole eden olcu 226 ms'i **hic asmiyor**. Kuyrugu
boru uretiyor, miras almiyor: 5-6 katina cikariyor. Atilan bir olcum bunu ayrica
gosteriyor: `ForwardWaitTimeout=4 sn` kosumunda (`k3-orta.log`) p95 **4166 ms**,
`surec_baslatma=21`. Yani kuyrugun buyuklugunu `DecoderPipe.cs:14` ile `:131-134`'teki
yeniden deneme penceresi belirliyor — boru yeniden baslattiktan sonra kareyi bazen hic
teslim etmiyor, bir sonraki yeniden baslatma kurtariyor. **LibVLC'ye gecmeden once bu
kusur kendi hattinda aranacak.**

### Borc bulgulari

3. `oynatici-boru.md:61-63`, `:154` — K3 hukmu yalniz p95'i kirmizi ilan ediyor; medyan
   da uniform modda iki kaynakta esigi asiyor (`orta` 163,2 / `uzun` 160,0). Gercek durum
   rapordan bir kademe daha kotu. n=30'da `ceil(0.95*n)` en kotu ornegi dusuruyor;
   maksimumlar (1142,2 / 1106,3 / 1063,9) raporda hic gecmiyor — yaz.
4. K4'un "1,9 ms" karsilastirmasi elma-armut. `docs/olcumler/oynatici-hatti.md:252-254`,
   `:287`: T167'de 1,9 ms mutlak kayma degil, -1.645,6 ms'lik bir ofsetin 123 sn'deki
   **degisimi**. T167'nin kendi olcutuyle T175 126 sn'de -121,01 -> -134,62, yani ~13,6 ms
   birikiyor ve mutlak ofseti T167'nin hattindan 12 kat **iyi**. Hukum gereksiz karamsar.
5. K4 aslinda ses/goruntu senkronunu olcmuyor. `.calisma/T175/olcum/Program.cs:158-160`
   `sink.PositionSeconds - clock.PositionSeconds` aliyor; `PlaybackClock.cs:73-77` duz bir
   `Stopwatch`. 125 sn'lik dongude tek kare cekilmiyor (`:150`'de bir kez `SeekAsync(0)`).
   T167 `videoPts - audioPts` olcmustu. K4'un "T167'nin olctugu bicimde olc" sarti
   karsilanmamis — yeniden olc.
6. K6(b) olcunun iki teriminden birini (saati) 1,15 ile carpiyor; metrik tanimi geregi
   kayar, senkron tespiti kanitlanmaz. Olen "olcu" suitte de degil, elle kosulan harness.
   15 testin hicbiri ses/goruntu kaymasini korumuyor.
7. K6(a)'da olan sey testin FAIL vermesi degil, kosumun asilip VSTest bekcisince iptali
   (`k6-a-hang.log`). Kapatilmamis canlilik kusuru: `DecoderPipe.cs:110-146` `SeekAsync`
   dongusunun ust siniri ve geri cekilmesi yok — `RestartVideo` kalici basarisizsa
   (`:199-204`) suresiz doner. `-vsync` asmasi bayrak duzeyinde duzeltildi, sinif olarak
   degil.
8. `ProcessesStarted` yalniz video yeniden baslatmalarini sayiyor
   (`DecoderPipe.cs:197`); `SeekAudio` (`:322-347`) sayilmayan ikinci bir ffmpeg
   baslatiyor. K2 tablosu "baslatilan ffmpeg surec sayisi" diyor, gercekte video-only.
9. K1 tabani ile K3 ayni isi yapmiyor ve bu aciklanmamis: taban `-frames:v 1` ile **tam
   kareyi** cozuyor, boru `-skip_frame nokey` + `FloorIndex` ile **taban anahtar kareyi**
   donduruyor (`DecoderPipe.cs:107,160-168,187`) ve onbellekten hic is yapmadan
   cevaplayabiliyor (`kisa/uniform` 7/30, `kisa/mixed` 6/30 ornek <5 ms). Yanlilik
   borunun lehine.
10. Testler sentetik klipleri sistem `%TEMP%`'ine yaziyor ve hic silmiyor:
    `tests/VidShrink.Tests/OynaticiBoruTests.cs:26` (`Path.GetTempPath()`), `:62`
    (`DisposeAsync` bos). `AGENTS.md` bunu acikca yasakliyor.
11. Raporun "Sonuc" listesi (`:152-160`) K6'yi hic anmiyor.

### T0 notlari

K7 bulgusu bagimsiz dogrulandi: `--filter "PlaybackPipeTests"` -> eslesen test yok; tam
filtre -> 15 test. Kusur sozlesme metnindeydi (`T175.md:11`), kodda degil. **Tur 2'de
`verify` satirindan `PlaybackPipeTests` kolu cikarildi.**

Olcum duzenegi `.calisma/T175/olcum/` icinde ve worktree kaldirilinca ham kanitla
gidiyor; `AGENTS.md` onu `tools/`a tasimayi istiyor ama `owns` `tools/`a yazmayi
yasakliyordu. **Tur 2'de `tools/VidShrink.PlaybackProbe/` `owns`a eklendi** (T167'nin
`VidShrink.PlayerProbe`'u degil, ayri bir klasor; ikisi de `VidShrink.sln`e girmez); duzenek oraya
tasinir, ham loglar `docs/olcumler/oynatici-boru/` altina kopyalanir.

## Tur 2 — teslim kurali (T0)

Tur 2'de `contract.js submit` **dort kalem ham ciktisiyla dalda commit edilmeden**
kosulmaz: (1) yeni K2 surec sayimi, (2) K3'un duzeltme sonrasi medyan/p95/maks tablosu,
(3) K4'un `videoPts - audioPts` olcusu, (4) K6 izgarasi. Tur 1'de teslim uc kez bos
kosuldu; her biri bir tur kaybettirdi. Teslim isin bittigini soyler, koda dokunuldugunu
degil.
