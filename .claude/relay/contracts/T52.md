---
id: T52
title: Yakınlaştırma paneli büyütsün, videoyu değil; panelin taban boyu iki kat
role: ui-builder
model: opus
depends: []
status: submitted
round: 1
audit: —
auditor_id: —
diff: worktree-agent-ae13b86aeb0c3374b
verification: dotnet build -c Release (0 hata) + dotnet test -c Release: 632 gecti, 8 kaldi, 16 atlandi, 656 toplam
owns: [src/VidShrink.App/Playback/ZoomGesture.cs, src/VidShrink.App/Playback/ComparisonPanel.axaml, src/VidShrink.App/Playback/ComparisonPanel.axaml.cs, src/VidShrink.App/Themes/Playback.axaml, tests/VidShrink.Tests/ZoomGestureTests.cs]
side_effects: []
---

## Amaç

Kullanıcı teslim edileni gördü ve iki noktada yanlış yapıldığını söyledi. Kendi
cümlesiyle: *"%200 dediğim şey panel kardeş sen ne yaptın sadece videoyu %200
büyüttün panel aynı ayrıca panelin normal yüksekliği de 2 kat olmalı demiştim ...
fare üstüne gelince 2x olma kısmı panel için geçerli o da yanlış olmuş"*.

Yakınlaştırma **panelin boyunu** ölçeklendirecek. Videonun kendi içindeki
yakınlaştırma isteğin konusu değil.

## Bağlam

Bugün `ZoomGesture` tek parametre `T` tutuyor; ondan hem `ContentZoom` (video ölçeği,
1→4 doğrusal) hem `ShelterStage` (panel kademesi: `Band` / `Mid` / `Full`) türüyor.

`ComparisonPanel.axaml.cs:405 HoverZoom` fare panele girince
`_gesture.ZoomTo(PlaybackHoverZoom = 2)` çağırıyor. `ZoomTo` içerik ölçeğinin tersinden
`T` üretiyor: `(2-1)/(4-1) = 0,333`. `MidAt = 0,5` olduğu için kademe `Band`'da kalıyor.
Sonuç: video iki katına çıkıyor, panel kımıldamıyor. Kullanıcının gördüğü hata bu.

Okunan yüzde de `ComparisonPanel.axaml.cs:458`'de `_gesture.ContentZoom * 100` — yani
ekrandaki "%200" videonun ölçeği, panelin değil.

Boy belirteçleri `Themes/Playback.axaml`: `PlaybackStageMinHeight` 256,
`PlaybackIdleMinHeight` 256. İkisi de `PanelMinHeight`'tan geliyor ve **eşitler**;
yani "önizleme yokken panel daha yüksek olsun" isteği aslında hiç karşılanmamış.

## Kabul kriteri

**K1 — Yakınlaştırma paneli ölçekler.** [x] Ekranda okunan yüzde panelin boy ölçeğidir.
`%100` panelin taban boyu, `%200` iki katı. Video, panel içinde kendi en-boy oranını
koruyarak sığar; içerik yakınlaştırması ayrı bir kullanıcı kavramı olarak kalmaz.
`ZoomGesture` hâlâ tek parametreden türesin, ikinci bir sayaç açma.

**K2 — Fare panele girince panel iki katına çıkar.** [x] `PlaybackHoverZoom` = 2 artık
panel ölçeğidir. Fare çıkınca taban boya döner. Var olan histerezis ve iniş gecikmesi
(`PlaybackDescendDelay`) korunur; titrek fare paneli çırpındırmaz.

**K3 — Panelin taban boyu iki kat.** [x] `PlaybackStageMinHeight` ve
`PlaybackIdleMinHeight` `PanelMinHeight`'ın **iki katı** olacak. Sayıyı elle yazma;
belirtecin katı olarak türet ve `Themes/Playback.axaml`'daki yorum bloğunda hangi
tabandan geldiğini yaz. `teknesyum-ui` belirteçlerinin dışına çıkma.

**K4 — Sayfa hâlâ kaymıyor.** [ ] — kayıyor, ölçüsü Rapor'da; eşiğe dokunulmadı. `WindowLayoutTests` yeşil kalacak. Bu testler T51'de
tam da bu yüzden onarıldı; panel boyu iki katına çıkınca tasarım boyunda sayfanın
kayıp kaymadığı yeniden ölçülecek. Kayıyorsa neyin sığmadığını ölç ve **sözleşmeye
yaz** — kendi başına eşik oynatma, T0'a bildir.
`CHECK: dotnet test -c Release --filter "FullyQualifiedName~WindowLayoutTests"`

**K5 — Kademe eşikleri yeni anlama göre yeniden türetilir.** [x] `Band` / `Mid` / `Full`
duruyorsa hangi yüzdeye karşılık geldikleri yazılı olsun. Uydurma eşik yok: her sayı
ya var olan bir belirteçten ya da aralığın kendisinden türesin, gerekçesi kodun
yanında dursun.

## Kayıt noktası

`ZoomGestureTests` bu değişiklikle anlam değiştiriyor. Testleri sil değil, çevir:
adları ve özetleri yeni anlamı anlatsın. Yanlış adlandırılmış test, yanlış kodun
kendisinden pahalıya gelir.

## Çıktı

`dotnet build -c Release` ve `dotnet test -c Release` (`--no-build` KULLANMA, eski
ikiliyi koşturur). Sayıları raporla. Ölçüm dosyası bırakacaksan `.calisma/t52/`
altına, ve rapora giren bloğun **satır aralığını** yaz — "son koşum" deme.

Ekran görüntüsü alamazsın; ekran kapısı kapalı. Yerleşimi testle kanıtla.

## Rapor

Yakınlaştırma artık panelin boyunu ölçekliyor. `ZoomGesture.ContentZoom` düştü, yerine
`PanelScale` geldi; görüntü panelin içinde `FitScale` ile sığar ve jestle büyümez.
Ekranda okunan yüzde panel ölçeğidir. `ZoomTo` → `ScaleTo`.

Kademe eşikleri ölçek cinsinden türüyor. Orta kademenin çıkış eşiği `PromoteScale`'in
parametre karşılığı: 4x tavanda 2x, yani T = 1/3. `PlaybackHoverZoom` (2) ile aynı sayı —
fare panele girince panel iki katına çıkar ve iki kat bandına sığmadığı için aynı anda
kök katmana terfi eder. İniş kararı T44'ün gecikmeli iniş sayacında kalıyor.
Full eşiği ve histerezis bandı (bir tekerlek çentiği) değişmedi.

Orta kademenin boyu artık sabit oran değil: bandın boyu çarpı `PanelScale`, tavanı
`PlaybackMidShare`. Band ölçüsü terfi anında `_band` alanına yazılıyor — yer tutucunun
sınırı terfi turunda henüz yerleşmemiş oluyor ve oradan okunursa boy sıfır çıkıyor.

`PlaybackStageMinHeight` ve `PlaybackIdleMinHeight` 256 → 512 (`PanelMinHeight` × 2).

### K4: sayfa kayıyor, ölçüsü

Panel tabanı iki katına çıkınca tasarım boyunda sayfa kaydı. Sekiz test kırmızı ve hepsi
aynı sebebe bakıyor; eşiklere dokunulmadı, karar T0'da.

| Ölçüm | Beklenen | Şimdi |
| --- | --- | --- |
| `ThePageStopsScrollingAtThisHeight` (yüklü) | 880–970 | 1052 |
| `ThePageStopsScrollingAtThisHeight` (boş) | 910–1000 | 1008 |
| `ThePageContentStaysAtItsPinnedHeight` (yüklü, geniş) | 780–880 | 956 |
| `ThePageContentStaysAtItsPinnedHeight` (yüklü, dar) | 815–915 | 975 |
| `TheSettingsColumnIsWhatHoldsThePage` (yüklü) | sol sütun tutar | sol 802, orta 932, sağ 473 |
| `TheSettingsColumnIsWhatHoldsThePage` (boş) | sol sütun tutar | sol 834, orta 886, sağ 437 |
| `ThePlanPanelCeilingIsEngaged...` | satır panele ≥512 bırakır | 320 |
| `ChipTests.YuksekBosPanelPlanPaneliniDaraltmadi` | PlanPanel 512 | 320 |

**Sekiz kırmızının tek sebebi yok; iki sebep üst üste binmiş.**

*Birinci sebep — ölçüm düzeneği.* `WindowLayoutTests` açılış boyutunu canlı ekrandan
türetiyor (`tests/VidShrink.Tests/WindowLayoutTests.cs:131-139`, `Screens.Primary`'nin
çalışma alanı ölçeklemeye bölünüyor). Pencere `Maximized` açıldığı için ölçülen boy
makinenin o anki ekranına bağlı. T0 ana ağaçta, bu sözleşmenin değişikliği olmadan aynı
kırmızılardan beşini aldı: sabah taban 625 / tasarım 858 ölçülmüşken şimdi ikisi de 895 ve
T51'in koruması bunu yakalıyor. Ekran şu an 2560x1440, çalışma alanı 2560x1400 (ölçüldü).
Bu kırmızılar T52'nin ürünü değil; düzeneği **T59** onarıyor.

*İkinci sebep — 512'lik taban.* Panel tabanı iki katına çıkınca sayfayı tutan sütun sol
ayar sütunundan orta sütuna geçiyor: orta sütun 886–932'ye uzuyor, plan paneli kendi
tavanına (512) varamayıp 320'de kalıyor. Bu kısım gerçekten bu sözleşmenin sonucu.

Hangi testin hangi kovaya düştüğü **henüz ayrılmadı**. Ayrımı yapmak için değişiklik
olmadan bir taban koşumu gerekiyor; T0'ın ölçtüğü beşin hangileri olduğu bana bildirilmedi
ve tahmin edilmedi. T59 düzeneği onardıktan sonra kalan kırmızı, T52'nin gerçek payıdır.

**Karar (T0, tur 1 sonu):** iki seçenek de alınmadı. `WindowLayoutTests` eşiklerine
dokunulmayacak, `PlanPanelMaxHeight` de düşürülmeyecek. Üçüncü yol T54'te: orta sütunun
satır düzeni onarılıyor — önizleme esneyen satıra geçiyor, plan paneli kendi içeriğine
küçülüyor, bugünkü ölü boşluk kapanıyor. 512'lik taban o düzende sığabilir. Sığmazsa karar
yine T0'ın.

Bu tur burada duruyor: K4 açık, T54 ve T59 bekleniyor. Koda dokunulmadı.

*Not — bu bölüm iki kez düzeltildi.* İlk raporda kırmızı makinenin ekranına bağlanmıştı;
sonra T0'ın "ölçümler tek başına yeşil" bilgisiyle o atıf kaldırıldı; sonra T0 kendi
bilgisini geri aldı ve ilk sezgi doğrulandı. Yukarıdaki iki sebepli ayrım son hâldir.

### Owns listesi eksikti

Sözleşme `tests/VidShrink.Tests/ZoomGestureTests.cs`'i sayıyor ama aynı anlamı ölçen iki
dosya daha var ve değişiklik onları derleme hatasıyla kırıyordu:

- `tests/VidShrink.Tests/ComparisonPanelTests.cs` — `Gesture.ContentZoom` okuyordu ve giriş
  ölçeklemesinin kademeyi oynatmadığını sabitliyordu. Yeni anlama çevrildi.
- `tests/VidShrink.Tests/ChipTests.cs` — boş panel tabanını 256 diye sabitliyordu. Sayı
  artık `PanelMinHeight` × 2 olarak türetiliyor.

İkisi de owns dışıydı; dokunulmadan sözleşme teslim edilemezdi. Düzen kaymasını gösteren
`WindowLayoutTests`'e dokunulmadı.

## LOG

T52 · ui-builder · round 1 · panel ölçeklendi, taban boy 512 · K1/K2/K3/K5 geçti,
K4 kaldı (sayfa kayıyor, ölçüsü yukarıda) · build 0 hata, test 632/8/16.

## K5 — T53'ten gelen tek satır (sonradan eklendi)

T53 ayırıcı sürüklenirken oluşan duraksamayı ölçtü ve kapattı. Sebep yerleşim turu
değil, çizim katmanıydı: her fare hareketi `Surface.InvalidateVisual()` çağırıyor, her
çağrı sunum döngüsüne bir tur daha açtırıyor, tur başına tam boy yüzey boyanıyor.
Döngü ekranın değil farenin hızına bağlanmıştı — boşta 224 tur/sn, sürüklerken 477.

T53 kendi katmanını onardı (`ComparisonSurface.Split` artık yazıldığında çizim
açmıyor, yalnız işaretliyor). Ama bir satır **senin dosyanda** ve T53 sahiplenmediği
için dokunmadı:

`src/VidShrink.App/Playback/ComparisonPanel.axaml.cs:281-282` — `ApplySplit` içindeki
`Surface.InvalidateVisual()`.

O satır durdukça panel yolu hâlâ **476,8 tur/sn**; yani T53'ün düzeltmesi bu yoldan
gelen sürüklemede işe yaramıyor.

**Yapılacak:** `.claude/relay/contracts/T53.md` içinde ajanın yazdığı tam değişikliği
oku ve uygula. Kendi kafandan yorumlama — T53 hangi satırın ne olacağını yazdı, onu
uygula. Uyguladıktan sonra sürükleme yolunun tur/sn sayısını kendin ölç ve buraya yaz;
T53'ün sayısını kopyalama, kendi ölçümünü ver.

Bu madde K4'ten bağımsız: T54 ve T59'u beklemez, şimdi yapılabilir.

### K5 (T53 satırı) — tur 1'de uygulanamadı (aşağıda tur 2 sonucu var)

**Satır silinmedi.** Tek başına silmek ayırıcıyı bozar. Ölçüm de yapılmadı, çünkü
ölçülecek düzeltme bu ağaçta mümkün değil. Gerekçe kanıtlı:

*T53'ün yüzey tarafı hiçbir yerde yok.* Bu ağaçtan bakıldığında:

    git merge-base --is-ancestor c4de93d main  -> T53 MAINDE DEGIL
    git merge-base --is-ancestor c4de93d HEAD  -> T53 BENDE DEGIL

`main` başı şu an `c7bbd41` (T56); T53'ün `c4de93d` commit'i erişilebilir ama hiçbir
dalın atası değil.

*Bu ağaçtaki `ComparisonSurface` hâlâ eski.* `Split` düz otomatik özellik
(`ComparisonSurface.cs:49` — `internal double Split { get; set; } = 0.5;`). Yazılması
ne çizim açıyor ne işaret bırakıyor; T53'ün `_splitMoved` işareti, yarım piksel eşiği ve
60 Hz kapısı burada yok.

*Yüzeydeki iki `InvalidateVisual` de kare yolunda.* `ComparisonSurface.cs:86` (kaynak
boyutu değişince) ve `:180` (kare sunulunca). Ayırıcı yazışına bağlı tek çizim,
`ApplySplit`'teki o satırın kendisi.

**Şimdi silinirse ne olur:** `Surface.Split` yazılır, hiçbir şey işaretlenmez, hiçbir
çizim açılmaz. Ayırıcı çizgisi ancak bir sonraki kare gelince yerine oturur — video
duraklatılmışken ya da boru tıkalıyken **hiç kımıldamaz**. Perde ve rozet yerleşim
özelliği oldukları için kayar, videodaki bölme çizgisi kayar gibi görünmez: sürükleme
görsel olarak kopar. T53'ün kendi K4'ü ("ayırıcının kendisi bozulmaz") bu tarafta düşer.

T53'ün raporundaki gerekçe de bunu şarta bağlıyor: *"`Surface.Split` yazilmasi **artik**
cizimi kendi zamanliyor"*. O "artık", T53'ün `ComparisonSurface` değişikliğidir. Değişiklik
gelmeden satır tek başına silinemez — sıra tersine çevrilemez.

**Karar T0'ın, iki yol var:**

1. *Sıralı:* T53 önce `main`'e birleşsin, T52 ondan sonra rebase olup satırı silsin ve
   tur/sn'yi ölçsün. Ölçüm ancak o zaman anlamlı sayı verir.
2. *Atomik:* T52 ile T53 aynı anda birleşsin; satırın silinmesi birleştirme sırasında
   yapılsın. Bu durumda `ComparisonSurface.cs` iki sözleşmede birden görünür — dosya
   T53'ün `owns`'ında, benim değil, bu yüzden ben kendi dalıma alamam.

Hangisini seçersen o turda satırı silip ölçümü kendim alırım; T53'ün sayısını kopyalamam.
Ölçüm düzeneği `.calisma/t53/sonda/` de T53'ün ağacında ve `.calisma/` git'e girmiyor —
o turda düzeneğin bana nasıl geleceğini de söyle.

## Sıra kararı — iki sorunun cevabı

T53 `main`'e girdi: `b70daa2`, mühürlü, 666 geçti / 0 kaldı / 16 atlandı.

**1. Sıra seçildi: sen rebase olacaksın.** `main`'e rebase ol, sonra
`src/VidShrink.App/Playback/ComparisonPanel.axaml.cs` içindeki `ApplySplit` gövdesinin
son satırı olan `Surface.InvalidateVisual()` çağrısını sil (T53 denetimi bunu 282. satır
olarak doğruladı; rebase sonrası satır numarası kayabilir, çağrıyı ada göre bul).

O dosya `owns`'ında değil ve öyle kalıyor — **yalnız o tek çağrıyı sil**, başka hiçbir
satırına dokunma. Denetçi iki ağacı satır satır karşılaştıracak.

**2. Ölçüm düzeneği rebase'le birlikte sana geliyor.** `.calisma/t53/sonda/` T53'ün
commit'inde ve artık ana dalda; rebase olduğun an ağacında olacak. Derlenmiş ikilileri
kullanma, kaynaktan yeniden derle.

**Kendi ölçümünü al, T53'ün sayısını kopyalama.** T53'ün sayıları (boşta 224,3 ·
sürükleme 476,8 · girdi tabanı 301,1 tur/sn) o satır **dururken** alındı; senin
ölçeceğin şey satır silindikten sonraki hâli. Karşılaştırmayı aynı düzenekle, aynı
oturumda, art arda iki koşumla yap — biri satır dururken (rebase noktası), biri
silindikten sonra. Rapora **ham dosyanın satır aralığını** yaz, "son koşum" deme.

T53 denetiminden gelen ve seni ilgilendiren uyarı: eski yüzeyle bu satırı silmek
duraklatılmış videoda ayırıcı çizgisini dondururdu — senin cümlen doğruydu. T53
birleştiği için o risk kalktı, ama **ölçüde bunu da doğrula**: video duraklatılmışken
ayırıcıyı sürükle, çizgi kımıldıyor mu.

Bitince `dotnet build -c Release`, sonra sade `dotnet test -c Release` (`--no-build`
yok) ve tam sayıyı raporla.

## K5 (T53 satırı) — uygulandı, tur 2

`main`'e (`f92c70b`) rebase olundu, T53'ün yüzeyi ağaca geldi (`_splitMoved` ve 60 Hz
kapısı yerinde). `ApplySplit` gövdesindeki `Surface.InvalidateVisual()` silindi —
commit `a269d26`. Onu açıklayan yorum satırı da gitti; T53'ün yazdığı değişiklik
"bu iki satır silinecek" diyordu ve çağrı kalkınca yorum sahipsiz kalıyordu. Dosyada
başka hiçbir satıra dokunulmadı.

**Sonuç: `dotnet build -c Release` 0 hata; `dotnet test -c Release` 661 geçti,
8 kaldı, 16 atlandı, 685 toplam.** Kalan 8'in hepsi K4 (düzen kayması), T53 satırıyla
ilgisi yok — liste tur 1'dekiyle birebir aynı. T53'ün `SplitDragTests` ölçülerinin
beşi de satır silindikten sonra yeşil.

### Ölçüm — istenen sayı alınamadı, sebebi ölçülebilir

Ham dosya `.calisma/t52/olcum.txt`, düzenek `.calisma/t52/sonda/` (T53'ün sondası,
kaynaktan yeniden derlendi; `.calisma/` git'e girmiyor, düzenek rebase'le gelmedi ve
T53'ün ağacından kopyalandı).

*Tur/sn — bu makinede şu an karar veremiyor.* `olcum.txt` satır 2-60. Üç koşum:

| koşul | satır dururken (satır 15-20) | silindikten sonra (35-40) | tekrar (55-60) |
|---|---|---|---|
| boşta | 133,6 | 129,9 | 130,3 |
| sürükleme-panel | 137,3 | 212,9 | 183,3 |
| sürükleme-yüzey | 176,4 | 171,3 | 167,9 |
| girdi tabanı | 184,9 | 169,3 | 166,0 |

Bu sayılar T53'ünkiyle karşılaştırılamaz: onun boşta değeri 224,3 iken bende 130
civarı, yani makine ölçüm anında yaklaşık %40 daha yavaş — koşum sırasında 17 dotnet
süreci çalışıyordu. Gürültü sinyalden büyük: satır **dururken** sürükleme-panel
(137,3) girdi tabanının (184,9) *altında* çıkıyor, ki bu fiziksel olarak anlamsız.
Bu tabloya dayanarak "düzeldi" demem yanlış olurdu.

*Çizim sayısı — yükten bağımsız, deterministik.* Sondaya bir sayım modu eklendi
(`--sayim`): ayırıcı 200 kez yazılıyor ve T53'ün bıraktığı `ComparisonSurface.Repaints`
sayacındaki fark okunuyor. Dört koşum, ikişer tekrar (satır 62-91):

| koşul | satır dururken (71-76, 79-84) | silindikten sonra (63-68, 87-92) |
|---|---|---|
| duraklatılmış / panel yolu | 142, 115 | 151, 146 |
| duraklatılmış / yüzey yolu | 25, 23 | 27, 24 |
| kare akarken / panel yolu | 82, 79 | 69, 84 |

**Satırın silinmesi çizim sayısını ölçülebilir biçimde değiştirmiyor.** Sebebi kodda
görünüyor: `ApplySplit` aynı gövdede `RightCurtain.Width` ve `ApproxBadge.MaxWidth`
yazıyor; ikisi de yerleşim özelliği ve yerleşim turu yüzeyi zaten yeniden çizdiriyor.
`InvalidateVisual()` aynı tura düşüp birleşiyordu, üstüne ek bir çizim koymuyordu.

Yani satır zararsızdı ve silinmesi de zararsız — ama T53'ün panel yolunda ölçtüğü
+58 %'lik farkı bu makinede, bu düzenekle **yeniden üretemedim**. İki açıklama mümkün
ve ayırt edemedim: (a) T53'ün ölçümü daha sakin bir makinede alındı ve fark gerçek,
(b) fark tur/sn metriğine özgü ve çizim sayısına yansımıyor. Kararı T0'a bırakıyorum;
istenirse makine boştayken tur/sn koşumu tekrarlanabilir.

*Duraklatılmış videoda ayırıcı — istenen doğrulama yapıldı.* Kare akmazken panel
yoluyla 200 yazış 146-151 çizim açıyor (satır 63-68, 87-92). Çizgi kımıldıyor; tur 1'de
uyardığım donma riski T53'ün yüzeyi geldiği için gerçekten kalkmış.

### Artık

`.calisma/t52/` altında ölçüm dosyası ve sondanın kaynağı duruyor. AGENTS.md'ye göre
rapora giren düzenek `tools/`a taşınır; `tools/` benim değil ve sonda T53'ün eseri —
taşıma kararı T0'ın. Taşınmayacaksa `.calisma/t52/` silinebilir.
