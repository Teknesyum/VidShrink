---
id: T111
title: Auto mod olculeri kaymis kare eslemesiyle uretildi
role: builder
model: opus
depends: [T106]
status: active
round: 1
verify: [dotnet test -c Release --filter "SettingsTabTests"]
owns: [docs/olcumler/auto-mod.md, tools/auto-mod-olcumu/**, docs/olcumler/olcu-gecerliligi.md]
side_effects: []
---

## Bağlam

T102 mühürlendi ve iyi iş yaptı: uzman açığını ölçtü, kendi "yerleşim sebeptir"
iddiasını çürüttü, sekiz sayının hepsi arşivden yeniden üretilebilir hâle geldi.
**Ama ölçüldüğü boru hattı kaymış çıktı ve bunu ölçen T106'ydı.**

T106 denetçisi doğrudan T102'nin ham verisinden saydı
(`.calisma/t102/vmaf/*.json`):

> **altı AV1 koşumunun `<1,0` kümesi birebir özdeş, iki x265 koşumunda boş.**

Sebep artık biliniyor: kaynağın video akışı `0,020000 s`'de başlıyor, bizim
ffmpeg çağrımız çıktıyı `0,016667 s`'ye taşıyor, ffmpeg framesync her kareyi
komşusuyla eşliyor. **AV1 koşumları bu kaymayı yaşıyor, HandBrake koşumları
yaşamıyor** — HandBrake `start_time`'ı düşürmüyor.

Yani T102'nin tablosunda **iki ayrı sınıf koşum yan yana konmuş**: biri kaymış,
öteki değil.

## Hangi karşılaştırma sağlam, hangisi değil

| Karşılaştırma | Durum |
|---|---|
| auto ↔ uzman-biz (ikisi de AV1) | **muhtemelen sağlam** — aynı kayma iki tarafta da var, `<1` kümesi birebir özdeş |
| y1 ↔ y2 ↔ y3 (üçü de AV1) | **muhtemelen sağlam** — aynı gerekçe |
| auto ↔ uzman-handbrake | **sağlam değil** — bir taraf kaymış, öteki değil |

"Muhtemelen" kelimesi burada bilerek duruyor. Kümenin özdeş olması yanlılığın
**aynı yönde** olduğunu gösterir; **aynı büyüklükte** olduğunu göstermez. Bunu
ölçmek bu sözleşmenin ilk işidir.

## Bu sözleşmenin ürünü

`docs/olcumler/auto-mod.md`'deki her sayının **kare kilidiyle yeniden
ölçülmüş** hâli, ve eski/yeni farkının tablosu. Yeni bir soru sormuyoruz;
sorulmuş soruların cevaplarını doğru aletle bir kez daha alıyoruz.

## Kabul kriteri

1. **Kayma her koşum için tek tek doğrulanır.** On bir arşivlenmiş koşumun
   kaynağına ve çıktısına ayıklama testi uygulanır:
   `ffprobe -v error -select_streams v:0 -show_entries stream=start_time -of csv=p=0 <dosya>`
   CHECK: koşum başına kaynak `start_time` / çıktı `start_time` / kayma var mı.
   HandBrake koşumlarının temiz çıkması **beklentidir, doğrulanmadan yazılmaz.**

2. **Yanlılığın büyüklüğü ölçülür.** İki AV1 koşumunda kayma aynı yöndeyse
   farkta ne kadarı sağ kalıyor? Kare kilidiyle yeniden ölçüp **eski farkla yeni
   farkı yan yana koy.** Uzman açığı +0,400'dü — kilitle kaç?
   CHECK: karşılaştırma başına eski fark / yeni fark.
   **Bu, "muhtemelen sağlam" cümlesini ya ölçüye çevirir ya çürütür.**

3. **AV1 ↔ HandBrake yeniden ölçülür.** T102'de auto 94,462 / uzman-hb2 95,731
   idi — yani HandBrake'in uzman çıktısı bizim auto'muzun **1,269 puan
   önünde** görünüyordu ve o sayı kirli. Kilitle gerçek fark ne?
   Bu sayı ürünün en üst hedefine doğrudan bağlı: piyasadaki en iyi auto mod
   bizde olacaksa, **açığın gerçek büyüklüğünü bilmek zorundayız.**
   CHECK: auto / uzman-biz / uzman-handbrake, kilitli, üç metrikle.

4. **Kilit T106'nınkiyle aynı olur.** `settb=AVTB,setpts=N`. Başka bir şey
   seçeceksen neden olduğunu ve hangi ölçüyle daha iyi olduğunu yaz. Üç yerde üç
   farklı kilit, bir sonraki ayrışmanın tohumudur — bench T106'nın, ürün kodu
   T110'un, bu düzenek senin.

5. **Arşiv yenilenir, eskisi işaretlenir.** `tools/auto-mod-olcumu/vmaf/*.json.gz`
   kaymış ölçüyle üretildi. Yeni arşiv yanına konur ve eskisi **silinmez**;
   dosya adında ya da bir `AGENTS.md` satırında "kaymış eşleme, T111 öncesi"
   diye işaretlenir. Eskisi denetim izidir.

6. **Değişen her sonuç cümlesi aynı commit'te değişir.** Bu projenin tekrar eden
   kusuru: sayı değişiyor, onu anlatan cümle eskiyor. Belgede sayıya dayanan her
   yargı — uzman açığı, K4 ayrıştırması, kusur listesi, üç öneri — yeni sayıya
   göre yeniden okunur. Değişmeyen için "ölçüldü, değişmedi" yazılır.

7. **Sonuç tersine dönerse tersi yazılır.** Yeni ölçüm T102'nin bir yargısını
   çürütürse çürütülür. T102 bunu kendi turunda yaptı ve doğru olan buydu.

8. **Ölçmediğin şey için "ölçülmedi" yaz.** Son yedi sözleşmenin beşinde tur
   açtıran bulgu buydu.

## Sınırlar

- `owns` dışına yazma. `tools/VidShrink.Bench/Program.cs` T106'nın,
  `QualityMeter.cs` T110'un — **okuyabilirsin ve okumalısın**, kilidin doğru hâli
  orada. `PlanCalculator.cs` T107'nin, `FfmpegArguments.cs` T98'in.
- **Üretim kodu değiştirme.** Bu sözleşme ölçüyor.
- `tablolar.py:24`'ün etiketi `main`de düzeltildi ("1 puan altı kare"); hesabı
  (`x < 1.0`) değişmedi. Yeni ölçümde `<1` kümesi boşalmalı — boşalmıyorsa
  kilit çalışmıyor demektir ve bu yazılır.
- Ölçüm dosyaları `.calisma/` altına, düzenek `tools/` altına. `.calisma/`
  gitignore'lu ve worktree-yerel — **oraya koyduğun düzeneği denetçi göremez.**
- `.calisma/kaynak/` ortak, dokunma.
- Makine paylaşımlı, altı ajan koşuyor. Aynı anda tek ağır kodlama; iş
  parçacığını **sabitle** (`-threads 4`, SVT'de `lp=4` — T102'nin `hizalama.sh`'i
  bunu yapıyor, ondan devral). Süre sayısına "makine paylaşımlıydı" damgası bas;
  kalite ve boyut sayısına basma.
- Kod yorumu yazma.

## Kayıt noktası

Her kabul kriteri bittiğinde commit. Dal: `T111-auto-mod-yeniden`.

## Çıktı

- `docs/olcumler/auto-mod.md`: kilitli ölçümle her sayı, eski/yeni fark tablosu,
  koşum başına kayma doğrulaması, değişen ve değişmeyen yargılar.
- Yenilenmiş `vmaf/` arşivi; eskisi işaretli, silinmemiş.
- Kendi dalın, itilmiş. `main`e sen birleştirme.
## T112 denetiminin biraktigi kunye borcu (T0, 2026-09-02)

`docs/olcumler/auto-mod.md:209` **iki kez yanlış künye** veriyor: "auto libsvtav1
preset 6" aslında `:208`'de, HandBrakeCLI komutu aslında `:210`'da. İçerik aynı
tabloda, bir-iki satır ötede — olgu doğru, işaretçi yanlış.

Dosya senin `owns`'ında. Yeniden ölçerken düzelt.

Ayrıca T112 mühürlendi ve senin öncülünü güçlendirdi: HandBrake'in **hedef boyut
yolu yok** (`TargetSize` okuyan yok, `muxcommon.c:564-568` yalnız günlük satırı).
Yani `uzman-hb2` koşumu hedef boyuta nişan almıyordu, CRF'ten çıkan boyutu
alıyordu. Bunu yeniden ölçerken hesaba kat: **karşılaştırma hedef boyutta değil,
iki farklı kipin ürettiği boyutta yapılıyor.** Eş boyut kapısı bu yüzden daha da
kritik.

## T110 onculu degistirdi — dagitimdan once oku (T0, 2026-09-02)

**Denetimde, henuz muhurlenmedi.** T110 muhurlenirse asagisi baglayicidir;
KALDI alirsa bu bolum guncellenir. Dagitimdan once durumu ben kontrol ederim.

T110 kare kilidini olctu ve sozlesmesinin onculunu **cururttu**:

- Fark **kaymis** kaynakta degil, **temiz** kaynakta cikti: min 2,5864 ->
  30,2347. 240 karenin 6'si yanlis eslesiyordu cunku referansin kendi
  `start_time`'i 0,501000'de.
- Kaymis kaynakta kilit yalnizca **kaybolan kareyi** geri getiriyor
  (239 -> 240) — buyuk sapmayi o uretmiyor.
- Mekanizma: grafige giren kayma = video `start_time` eksi kaptaki **en erken
  akisin** `start_time`'i. Yani kaymayi kaynagin kendi damgasi degil, kaptaki
  ses/altyazi akisiyla arasindaki fark uretiyor.
- Kilitsiz zarar `ceil(kayma/kare)`; kilitli her kaymada 98,3724.

**Bunun T111'e uc sonucu var:**

1. **K1'in ffprobe maddesi genisliyor.** Her kosumun `start_time`'ini olcmek
   yetmez; **kaptaki her akisin** `start_time`'ini oku ve en erkenini bul.
   Komut: `ffprobe -v error -show_entries stream=index,codec_type,start_time
   -of csv=p=0 <dosya>`. Kaymanin buyuklugu bu farktan cikiyor.

2. **"Alti AV1 kosumunun `<1,0` kumesi birebir ozdes, iki x265 kosumunda
   bos" bulgusunun aciklamasi degisebilir.** T106 denetcisinin sayimi
   duruyor — o gozlem gecerli. Ama sebebi "kaynak kaymisti" degil, "AV1
   kaplarinda ses akisi videodan once basliyordu" olabilir. **Hangisi
   oldugunu olc**; ikisini birbirinin yerine yazma.

3. **Eski arsivin damgasi yeniden yazilir.** "kaymis esleme, T111 oncesi"
   yeterince kesin degil. Damga olculen mekanizmayi soylesin: hangi
   kosumlarda kap-ici kayma vardi, ne kadardi, ve kilitsiz olcumde kac kare
   yanlis eslesti. Olcemedigin kosum icin "olculmedi" yaz.

T110'un raporu `docs/olcumler/algi-olcusu.md` §9 ve "Yeniden uretim -> T110"
bolumunde; **oku, tekrar olcme.** Orada `[FfmpegFact]` uyarisi da var: kare
kilidini tutan olculer CI'da atlaniyor (T115 bunu kapatiyor).

## T106 muhurlendi — borclari sana gecti (T0, 2026-09-02)

`docs/olcumler/olcu-gecerliligi.md` artik senin `owns`'unda. T106 GECTI aldi;
asagidakiler tur actirmadi ama kapanmadi. **Bu turda kapansin.** Hicbiri
sevkedilen kodu degistirmiyor — hepsi *veriyi ozetleyen cumlenin veriden
kaymasi*, yani bu projenin adi konmus kronik kusuru.

1. **Kosum sayisi uc ayri yerde uc farkli.** `:34` "**dokuz** AV1 kosumunun
   `<1,0` kumesi ayni" diyor; `:20-25` tablosunda **alti** AV1 satiri var,
   `:66` "alti kosumda" diyor, `:5` toplami "**sekiz** VMAF JSON" diyor.
   Ayrica `:31` "Tam sifir sayisi **dortte** 25" diyor, tabloda 25 gosteren
   **bes** satir var (`:20-24`). Tabloyu say, cumleyi ona gore yaz.
2. **`:130-133` HandBrake "tek istisna" iddiasi veriyle celisiyor.** Olculdu:
   **139 kare tam -1,00 ms**, toplam **180 kare negatif** sapmali, aralik
   -1,00 … **+1,67 ms** — yani "±1 ms icinde kaliyor" da dogru degil. Tek olan
   yani istisna olmasi degil **hareketli olmasi** (`motion_sad` 0,052 -> 1,012).
   "1 ms'lik negatif kayma bile tam kare kaydiriyor" sonucu yanlis onculden
   cikarilmis; kayma 139 karede de var, yalniz durgun ekranda gorunmuyor —
   mekanizma belgenin kendi `:63-64`'unde yazili. **Cumleyi olctugu seye gore
   yaz; sonucu koruyacaksan neyin tasidigini soyle.**
3. **`:116` "Fark her karede -3,3 ms" ve `:404` "Sapma sabit".** Olculdu: kare
   basina sapma **-1,67 … -4,33 ms**, ortalama **-3,02 ms**. 3,3 yalniz 0.
   karenin farki. Kaynagin damgalari titresimli (belgenin kendi `:114`
   gozlemi). Tani ve duzeltme etkilenmiyor, cumle fazla soyluyor.
4. **`:480-484` bassiz olu tablo parcasi** — `657f472`'nin yarim kalmis
   duzenlemesi. `---|---|` ile basliyor (baslik satiri yok, markdown'da cop
   render olur) ve `FrameLock` satirinda **canli tablodan farkli bir mutasyon
   adi** veriyor: `:484` `"null"`, `:461` `settb=AVTB`. Hangisinin
   kosturuldugu belirsiz — **koshan hangisiyse onu birak, otekini sil.**
5. **`:500-506` "iki bagimsiz kanit"in ikincisi gecersiz.** Yerel kirmiziyi
   eleyen gerekce olarak "ayni commit'te CI yesil" sunuluyor, ama
   `PerformanceCheckTests.OlcumYukAltindaYalnizAgirlasiyor`
   (`tests/VidShrink.Tests/PerformanceCheckTests.cs:393`) `[FfmpegFact]` ve
   CI'da ffmpeg yok — o olcu CI'da **hic kosmuyor**. Belge bunu `:447`'de
   kendisi yaziyor, `:505`'te unutuyor. Tek gercek dayanak "tek basina
   kosturulunca gecti".
6. **`:471-472` "goreli kayma ayri bir olcuyle pimlenmis" iddiasi fazla.**
   `tests/VidShrink.Tests/VmafPoolingTests.cs:180-195` elle yazilmis sabit
   dizgiyi `Build`'e karsi yalniz `Assert.NotEqual` ile kuruyor; hicbir
   `FrameLock` mutasyonu onu kiramaz. Bizim kodumuzu degil ffmpeg'i olcuyor.
   O dosya **T106 ile muhurlendi ve simdi sahipsiz** — pimi gerceklestirmek
   istersen once bana soyle, `owns`'una eklerim.
7. **`:447` "83 test bu sebeple atlaniyor"** — CI kosumu 33580719123
   `Skipped: 86` diyor. Uc yeni olcu eklenmeden onceki sayi yazili kalmis.
8. **Raporun birincil veri yolu artik yok.** `:3-5` kaynagi
   `.claude/worktrees/agent-a3f969792f618f57f/.calisma/t102/` gosteriyor; o
   worktree kaldirildi, sekiz T102 VMAF JSON'u diskte yok. `:18-27` tablosu ve
   `:175-178`'in "eski" sutunu yeniden turetilemiyor. Dolayli dogrulama var:
   `docs/olcumler/auto-mod.md:214` ayni sayilari tasiyor — **ve o dosya
   senin.** Ikisinin ayni sayiyi verdigini pimle, ya da olculemez oldugunu
   belgenin basina yaz.

**Kural:** her duzeltilen cumle icin ya "tablodan yeniden sayildi" ya da
"olculemedi" yazilir. Ucuncusu yok.

## Kayit noktasi (builder, tur 1)

Worktree: `.claude/worktrees/T111`, dal `T111-auto-mod`. Olcum dizini
`.calisma/t111/` (worktree-yerel).

**Onculun cokmesi:** T102'nin 11 kodlama ciktisi diskte yok
(`.claude/worktrees/agent-a3f969792f618f57f/` kaldirilmis). Ham VMAF arsivi
(`tools/auto-mod-olcumu/vmaf/*.json.gz`, 11 kosum) **duruyor ve T102'nin her
sayisini birebir yeniden veriyor** — T106'nin "birincil veri yolu yok" borcu
bu yuzden yanlis oncule dayaniyordu; arsiv git'te.

Olculemeyen sey ciktilarin kendisi: kilitli olcum icin 11 kosum yeniden
kodlaniyor. Bu yuzden tasarim uc terimli:
- **kilit etkisi** = ayni yeni dosyanin kilitli/kilitsiz olcumu (temiz, tek degisken),
- **yeniden kodlama payi** = T102 arsivi ↔ T111 kilitsiz (ikisi de kilitsiz),
- belgeye giren toplam degisim = arsiv ↔ T111 kilitli.

Yapildi:
- `t111-uret.sh` / `t111-olc.sh` / `t111-kayma.sh` / `t111-damga.sh` /
  `t111-tablolar.py` yazildi.
- `auto` kosumu urun kod yolundan yeniden uretildi: 15 496 155 bayt
  (T102: 15 766 933, **-1,72%**), `deneme=2`.
- Arsivden yeniden sayildi: **dokuz** AV1 kosumunun `<1` kumesi birebir ayni
  (26 kare); tam sifir **yedi** kosumda 25, **iki** kosumda 24
  (`uzman-biz3` ve `y3-hizali-boyutesit`). T106 `:31`'in "dortte 25"i yanlis.
- Dokuz yeniden kodlama kosuyor.

Sirada: kilitli/kilitsiz VMAF (22 olcum), kayma yoklamasi, damga farki,
`FrameLock` mutasyonu (T106 borc 4), CI atlanan sayisi (borc 7).

## Kayit noktasi (builder, tur 1 — ikinci blok)

Olculdu ve yazildi (T106 borclari):

- **Borc 1 kapandi.** Arsivden yeniden sayildi: dokuz AV1 kosumu, `<1` kumesi
  birebir ayni (26 kare); tam sifir yedide 25, ikide 24. Tablo on bir satira
  cikarildi (T106'nin tablosu diskteki sekiz JSON'la sinirliydi).
- **Borc 2 kapandi.** HandBrake "tek istisna" degil: sabit ofset cikarilinca bizim
  AV1 ciktimizda da **139 kare tam -1,00 ms, 180 kare negatif, -1,00 … +1,67 ms** —
  denetcinin HandBrake'te saydigi uc sayinin birebir ayni. Dagilim kodlayicinin
  degil kaynagin ozelligi. 2721'i ayiran sey hareket.
- **Borc 3 kapandi.** Kare basina sapma -1,67 … -4,33 ms, ortalama -3,02;
  -3,3 yalniz 0. karenin farki. Kaynagin kare araligi 14,00 … 19,00 ms.
- **Borc 4 kapandi.** `settb=AVTB` ve `"null"` **ikisi de kosturuldu**, ikisi de
  ayni 2 olcuyu dusuruyor. Olu tablo parcasi silindi. Ek bulgu: `setpts=N`
  (settb duser) **0 olcu dusuruyor ama esdeger degil** — gercek olcumde kaynak
  1/1000, cikti 1/15360 tabanda; olculdu: 3624 kare / 95,647 → **7012 kare /
  26,090**, `<1` kare 0 → **5099**. Suitte bunu goren olcu yok.
- **Borc 5 kapandi.** `PerformanceCheckTests.cs:393` `[FfmpegFact]`; "CI yesildi"
  gerekcesi geri cekildi.
- **Borc 6 kapandi.** `VmafPoolingTests.cs:181-195` hicbir `FrameLock` mutasyonuyla
  kirilmiyor; "goreli kayma pimlenmis" iddiasi geri cekildi. Pimi kurmak o dosyayi
  gerektiriyor, T111'in `owns`'unda degil — **yapilmadi**.
- **Borc 8 kapandi.** Arsiv git'te ve T102'nin on bir kosumunun her sayisini
  birebir veriyor; "birincil veri yolu yok" onculu yanlisti.

Ek olcum (T106'nin "olcemedim" dedigi sey): `PTS-STARTPTS` ile indeks kilidi
yan yana kondu. Ortalama farki **0,010**, en dusuk kare farki **17,955**
(74,421 @ 2721 → 92,376 @ 3411). Kilidin ustunlugu artik gerekce degil olcum.

Kayma mekanizmasi ses akisiyla olculdu (T110 notu madde 2): dort olcum, damgalara
dokunulmadan yalniz ses akisi atilarak. Iki taraf da sessizken olcu duzeliyor
(94,448 → 95,637, `<1` 26 → 0). Kayma kaynagin damgasinda degil **iki kabin
ofsetleri arasindaki farkta**.

Borc 7 (CI atlanan sayisi) ve K1-K3 kilitli olcumleri siradaki blokta.
