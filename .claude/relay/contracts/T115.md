---
id: T115
title: CI ffmpeg gormuyor — olculerin dortte biri orada atlaniyor
role: builder
model: sonnet
depends: []
status: open
round: 2
verify: [dotnet build VidShrink.sln -c Release -warnaserror]
owns: [.github/workflows/ci.yml, tools/kosum-kapisi/**, docs/olcumler/ci-ffmpeg.md]
side_effects: [aginternet]
---

## Bağlam

CI koşucusunda ffmpeg yok. Sonucu: `[FfmpegFact]` ile işaretli her ölçü
**yerelde koşuyor, CI'da atlanıyor.** Son üç koşumun rakamları:

| Koşum | Passed | Skipped | Total |
|---|---|---|---|
| T99 CI (`33579921167`) | 1030 | 95 | 1125 |
| T98 CI (`cf8a630`) | 1046 | 97 | 1144 |
| T106 yerel tam süit | 1106 | 17 | 1123 |

Yerelde 17 atlanıyor, CI'da 95-97. Aradaki ~80 ölçü ffmpeg isteyenler.
`[FfmpegFact]` on bir dosyaya yayılmış: `ComplexityProbeTests`,
`EncodeRunnerTests`, `FpsDropTests`, `FrameGrabberTests`, `PanelHostTests`,
`PerformanceCheckTests`, `PreviewSyncTests`, `QualityMeterTests`,
`QualityTargetTests`, `SceneMapTests`, `SegmentEncoderTests`.

Yani **motorun kendisini ölçen her şey** CI'nın kör noktasında. T106 tur 2
kare kilidini tutan pimi yazdı ve o pim CI'da korumuyor; yapıcı bunu kendisi
bildirdi. Aynı şey sahne haritasına, kalite ölçerine, kodlama koşucusuna da
geçerli.

Bu proje "yerel yeşil, CI kırmızı"yı on üç itme boyunca yaşadı ve kural
yazıldı: teslimden önce tam süit koş. Kural CI'nın gördüğünü büyütmüyor —
CI hâlâ motorun dörtte birini görmüyor.

## Kabul kriteri

1. **ffmpeg CI koşucusuna kurulur ve sürümü sabitlenir.** `windows-latest`
   imajının kendi ffmpeg'ine güvenme — imaj değişince sessizce kayar.
   Sürüm açıkça yazılır ve **yerelde kullanılanla karşılaştırılır**; farklıysa
   fark belgeye yazılır.
   CHECK: iş akışı çıktısında `ffmpeg -version` satırı.

2. **libvmaf'in orada olduğu ayrıca doğrulanır.** Kalite ölçümü `libvmaf`
   filtresine bağlı ve her ffmpeg derlemesinde yok. `ffmpeg -filters | grep
   libvmaf` koşar; yoksa **hangi ölçülerin hâlâ atlandığı sayılır ve yazılır.**
   VMAF model dosyası da gerekiyorsa yolu sabitlenir.
   CHECK: filtre listesi çıktısı + atlanmaya devam eden ölçü sayısı.

3. **Atlanan sayı ölçülür, tahmin edilmez.** Kurulumdan önce ve sonra aynı
   dalda CI koşar; `Passed / Skipped / Total` üçlüsü yan yana yazılır.
   Beklenen ~80 ölçünün kaçının gerçekten koştuğu **sayılır**.
   CHECK: öncesi/sonrası üçlü.

4. **Koşan yeni ölçülerin hepsi yeşil mi?** Yeşil değilse bu **iyi haberdir** —
   CI'nın körlüğü gerçek bir kusuru saklıyormuş demektir. Düşen her ölçü
   `dosya:satir` ile listelenir. **Düşen ölçüyü sen düzeltme**; hangi
   sözleşmenin dosyası olduğunu bul ve rapora yaz, turu ben açarım.
   Düzeltmen gereken tek şey iş akışının kendisi.
   CHECK: düşen ölçü listesi ya da "hepsi yeşil".

5. **Koşum süresi ölçülür ve bütçelenir.** CI bugün 9-19 dakika sürüyor.
   ffmpeg isteyen ~80 ölçü eklenince ne oluyor? İki katına çıkıyorsa bu bir
   karardır, sessizce kabul edilmez — süreyi yaz ve kabul edilebilir mi
   söyle.
   CHECK: öncesi/sonrası süre.

6. **`kosum-kapisi.ps1 -MinimumTotal 950` yeniden yargılanır.** Kapı toplam
   ölçü sayısına bakıyor ve toplam artacak. 950 hâlâ doğru taban mı?
   Değiştiriyorsan **neye göre** değiştirdiğini yaz; "yeni sayıya uydurdum"
   gerekçe değildir. Kapının asıl işi asılı kalan test konağını yakalamak —
   o işi hâlâ yapıyor mu, ölç.

7. **Kurulum başarısız olursa koşum kırmızı olur.** ffmpeg indirilemezse iş
   akışı sessizce devam edip ölçüleri atlamamalı; **düşmeli.** Yoksa bugünkü
   körlük geri gelir ve kimse fark etmez. Bunu bilerek bozup gösterebiliyorsan
   göster.
   CHECK: kurulum adımının hata davranışı.

8. **Lisans notu doğru kalır.** `release.yml` başındaki not "ffmpeg hiçbir
   arşivin parçası değil, GPLv3 ve VidShrink AGPL-3.0-or-later" diyor. CI
   koşucusuna kurmak **dağıtım değildir**, ama bu ayrımın yazılı olması
   gerekiyor — `ci.yml` içine tek cümlelik not.

## Sınırlar

- `owns` dışına yazma. Test dosyalarına, `src/` altına, `release.yml`'a
  **dokunma.** Düşen ölçü bulursan rapora yaz.
- Bu sözleşme bir ölçüyü yeşile boyamak için değil, **görünür kılmak** için
  var. Kırmızı çıkan sonuç kabul edilir ve değerlidir.
- Rapora giren her sayının üreten komutu ve koşum numarası (`gh run view
  <id>`) yazılı olsun.
- Kod yorumu yazma. (`ci.yml` içindeki açıklama satırları ayrı: onlar
  kararı anlatıyor ve K8 bir tanesini istiyor.)

## Kayıt noktası

Her kabul kriteri bittiğinde commit. Dal: `T115-ci-ffmpeg`.

## Çıktı

- `docs/olcumler/ci-ffmpeg.md`: sürüm, libvmaf durumu, öncesi/sonrası
  Passed/Skipped/Total, düşen ölçü listesi, süre, kapı kararı.
- ffmpeg gören bir CI — ya da "kurulamadı" ve nedeni.
- Kendi dalın, itilmiş. `main`e sen birleştirme.

## Tur 2 — esik gevsetildi, gerekce olculmemis tarihsel orana dayaniyor (T0, 2026-09-02)

Denetim **KALDI** verdi. Sekiz kriterin yedisi gecti ve sozlesmenin asil bulgusu
(78-83 olcunun CI'da kor kostugu) **her yonden dogrulandi**: sha256 pini gercek
ve gunlukte hesaplanmis, K7'nin sert dusmesi gercek, K5'in alti suresi API
damgalariyla birebir, K4'un tek kirmizisi uc bagimsiz kosumda tek ve sebebi
`Cannot load nvcuda.dll` olarak gunlukte yazili. 78 sayisi aritmetikle degil
**isim kumesiyle** dogrulandi (`comm` ile kesisim disi tam 78). Total artisini
Skipped farkindan ayirmis olman da ayrica dogrulandi — bu projenin kronik kusuru
bu turda tekrarlamamis.

Tek KRITIK, K6'da.

### KRITIK — 1119 olculmedi, gercek taban 983

Rapor esik turetmesini soyle kuruyor: oran `950/1119 ~ %85` (ust sinir),
`1174 x 0,85 ~ 998` → **1000**, ve "eski esigin biraktigi boslugu (1119 - 950 =
169 ~ %15) korur, **gevsetmez**". Gerekcede de acikca yaziyor: *"o andaki gercek
Total'i geriye donuk olcmedim — 1119'u yaklasik referans aliyorum."*

Denetci olctu. Esigin konduğu commit `9ae6dce`nin kendi CI kosumu
**`33525816911`**:

```
Passed!  - Failed: 0, Passed: 911, Skipped: 72, Total: 983, Duration: 10 m 52 s
KOSUM KAPISI GECTI: basarisiz=0 toplam=983 alt-sinir=950
```

Gercek tarihsel Total **983**, gercek oran **950/983 = %96,6**, gercek bosluk
**33 olcu (%3,4)** — raporun "169 ~ %15" dedigi yer. Uc hata ic ice:

1. **%85 ust sinir degil, alt sinirdir.** Total zamanla yalniz buyur, yani
   `Total_tarihsel <= 1119` ve dolayisiyla `950/Total_tarihsel >= %85`. Yon ters
   yazilmis ve tam o ters yon esigi gevsek tarafa dusuruyor.
2. **"Gevsetmez" yanlis beyan.** Oran korunsaydi `0,966 x 1174 ~ 1134` cikardi.
   Secilen 1000, 1174 ustunde **174 olcu** bosluk birakiyor — kapinin kendi
   tasarim boslugunun (33) **5,3 kati**. Kapi korunmadi, gevsetildi.
3. **Olculmedi, oysa tek komutluk mesafedeydi.** `33525816911` zaten sorguladigin
   is akisinin listesinde. K6 "**neye gore** degistirdigini yaz; 'yeni sayiya
   uydurdum' gerekce degildir" diyor — yanlis oncul cikarilinca geriye kalan tam
   olarak `1174 x 0,85 ~ 998 → 1000`.

Ayrica K6'nin son cumlesi — *"kapinin asil isi asili kalan test konagini
yakalamak; o isi hala yapiyor mu, **olc**"* — olculmemis, iddia edilmis
("mekanizma degismedi"). Denetci `kosum-kapisi.ps1 -InputFile` ile olctu:

| Beslenen ozet | `-MinimumTotal 1000` sonucu |
|---|---|
| `Failed: 0, Passed: 1079, Skipped: 95, Total: 1174` (korluk tumuyle geri gelmis) | **GECTI**, exit 0 |
| `Failed: 0, Passed: 1079, Skipped: 17, Total: 1096` (78 olcu busbutun yok olmus) | **GECTI**, exit 0 |

Yani yeni esik seksen olcu yine kaybolsa **yakalamaz**. Kapi olu kod degil
(`33584487781`de `kod=66` ile dusuyor), gercek bir kapi ve gevsek kaldi.

### Bu turda yapilacak

1. **Esigi olculen tabandan turet.** `33525816911` / `Total 983` / bosluk 33 —
   bu sayilarin kosum numarasi ve ureten komutu rapora yazilir. Yeni esik bu
   tabandan cikar; `1174 x 0,85` yolu terk edilir.
2. **"Gevsetmez" cumlesi duzeltilir.** Ayni commit'te. Bu projede "geri cektim"
   denen iddianin satirda duzenlenmis halde durdugu goruldu — cumleyi ara ve
   her gectigi yerde duzelt, yalnizca K6 bolumunde degil.
3. **Kapinin korlugu yakalayip yakalamadigi olculur.** Denetcinin iki satirlik
   `-InputFile` olcumunu kendin tekrarla ve sonucu yaz. Yeni esik ikisini de
   kirmiziya dusurmuyorsa **bunu acikca yaz** — "yakalar" diye yazma.
4. **Yapisal soru, cevabi zorunlu:** `Total = Passed + Failed + Skipped`
   oldugu icin `-MinimumTotal` korlugu **hicbir degerde** yakalayamaz. Denetci
   bunu yapisal olarak gosterdi. Uc secenekten biri secilir ve **nedeni
   yazilir**: (a) `-MaximumSkipped` gibi ikinci bir kapi eklenir, (b) eklenmez
   ve neden gerekmedigi olculur, (c) ayri sozlesmeye birakilir ve sinirlari
   yazilir. **Karar vermek zorunda degilsin ama secmeden ve gerekcesiz
   birakamazsin.**

### Bu turda duzeltilecek iki kayma (kritik degil, ama ayni commit'te)

- **`.claude/relay/contracts/T115.md` Rapor bolumu**: *"78 olcu ... **bunlarin
  icinde** T110'un 5 kare-kilidi olcusu"*. Bes olcu o 78'in icinde **olamaz** —
  `33582206982` gunlugunde bes adin hicbiri gecmiyor, cunku T110 taban kosumdan
  sonra birlesti. Besi 83'un icinde (denetci 5/5 saydi). `ci-ffmpeg.md` bunu
  dogru yazmis ("Ayri, isimli dogrulama"); kayma yalnizca bu satirda.
- **78 senin elindeki daha iyi olcuden dusuk.** Kontrollu cift
  `33582483001` (`3868d621`, ffmpeg yok, Skipped 100 / Total 1124) →
  `33582768395` (`0a56868f`, ffmpeg'li, Skipped 17 / Total 1126) neredeyse ayni
  agac ve isim kumesi farki **83**. Manset cift arada T106/T109/T110 tasiyor.
  Iki sayiyi da yaz, hangisinin neyi olctugunu ayir.

### Borclar (tur acmaz, muhur notuna girecek)

- Kare-kilidi "gecti" kaniti yokluk kaniti (bes ad sonrasi gunlukte hic yok).
  Cikarim dogru, `--filter QualityMeterTests` ile adlariyla basan bir kosum
  daha saglam olurdu.
- Teslim edilen `ci.yml` hic kosmadi: olculen kosumlarin **hepsi**
  `-MinimumTotal 950` ile kostu. 1000'e ceken `545d8ef` ve dal ucu `9e15c02`
  kosumlari denetim aninda `in_progress`ti. Tur 2'de bunu kapat.
- Kanit commit'leri `f2f05f5f` ve `0a56868f` hicbir ref'te yok; birincil kanit
  yalnizca GitHub kosum gunlugu olarak dayanikli.
- K4'un onerdigi onarim yanlis yerde: gercek prob **zaten var**,
  `EncoderCapabilities.cs:30` `WorksAsEncoder(codec) => Probe(codec).Succeeded`.
  Yapilacak is yeni prob yazmak degil, `PerformanceProbe.cs:81`deki
  `HardwareCandidates.FirstOrDefault(availability.HasEncoder)` cagrisini mevcut
  proba baglamak. **Senin `owns`'unda degil — T117'ye gecti**, raporundaki
  oneriyi duzelt yeter.

**T99'un muhur borcu sende kapandi:** rapor olculen commit sha'sini her satirda
tasiyor ve kosum numarasi vermeyen sayi yok. Eksik olan yalnizca K6'nin 1119'u.
