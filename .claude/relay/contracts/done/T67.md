---
id: T67
title: Komut satırından gelen dosya yolu açılışta yüklensin
role: builder
model: opus
depends: []
status: submitted
round: 1
audit: passed
auditor_id: a509d069918af5a73
diff: 5 dosya, owns disi yok
verification: dogrulama agacinda 5581a08: dotnet test -c Release 796 gecti / 0 kaldi / 16 atlandi / 812 toplam · denetci build -warnaserror 0 uyari 0 hata · denetci hicbir dosyaya yazmadi
verify: [dotnet test -c Release --filter ShellIntegrationTests]
owns: [src/VidShrink.Core/ShellIntegration.cs, src/VidShrink.App/Program.cs, src/VidShrink.App/App.axaml.cs, src/VidShrink.App/MainWindow.axaml.cs, tests/VidShrink.Tests/ShellIntegrationTests.cs]
side_effects: []
---

## Amaç

Kullanıcı bir videoya sağ tıklayıp "Bu Videoyu VidShrink ile Aç" diyebilsin.
Kabuk tarafını T68 kuracak; **bu sözleşme uygulamanın kendi tarafı**: VidShrink
kendisine argüman olarak verilen dosya yolunu açılışta yüklesin.

Bugün yüklemiyor. `Program.cs:10` argümanları Avalonia'ya veriyor,
`App.axaml.cs` pencereyi argümansız kuruyor. Başlatıcı argümanları zaten geçiriyor
(`src/VidShrink.Launcher/Program.cs:70`), yani zincirin tek kopuk halkası burası.

## Ne yapılacak

**1. Ortak yüzey — `src/VidShrink.Core/ShellIntegration.cs` (yeni).**

`MediaExtensions` bugün `MainWindow.axaml.cs:59-63` içinde gömülü ve tek yerde
duruyor. Core'a taşı; hem dosya seçici hem T68'in yazacağı kayıt defteri girdileri
aynı listeden beslensin. Uzantı listesi **değişmeyecek** — 24 kalem, aynı sıra.

Aynı dosyada argüman çözümü dursun: verilen dizinin ilk **var olan dosya** yolunu
döndüren, hiçbir şey bulamazsa `null` dönen saf bir fonksiyon. Kabuk `"%1"` ile tek
yol geçirir ama boşluklu yollarda birden çok parça gelebildiği için birleştirmeyi
dene; sıralı bir tarama yeter, ayrıştırıcı yazma.

**2. Açılışta yükleme.** `Program.cs` argümanları `App`'e taşısın, `App` pencereyi
kurarken yolu versin, `MainWindow` pencere göründükten sonra sürükle-bırakla aynı
`LoadAsync` yolundan geçirsin. İkinci bir yükleme yolu açma.

**3. Kötü girdi patlatmayacak.** Var olmayan dosya, klasör, medya olmayan dosya ve
boş argüman — dördünde de uygulama normal açılır. Medya olmayan dosyada mevcut
kaynak hatası satırı görünür; sessizce yutma da yapma, çökme de.

## Kabul kriteri

1. `ShellIntegration.MediaExtensions` 24 uzantıyı taşır ve `MainWindow` dosya
   seçicisi artık kendi kopyasını değil bu listeyi okur. Uygulamada uzantı listesi
   tek kopya kalır.
   CHECK: `grep -rn '"mkv"' src/` yalnız ShellIntegration.cs'i döndürür
2. Var olan bir dosya yolu tek argüman olarak verildiğinde çözümlenir; boşluk içeren
   ve tırnaksız parçalanmış yolda da çözümlenir.
3. Var olmayan yol, klasör yolu ve boş dizi `null` döner — istisna fırlatmaz.
4. `MainWindow` argümanla kurulduğunda sürükle-bırakın kullandığı yükleme yolundan
   geçer; ikinci bir kopya yükleyici doğmaz.
   CHECK: `grep -c "LoadAsync" src/VidShrink.App/MainWindow.axaml.cs` sayısı artmaz
5. Medya olmayan bir dosya argümanla verildiğinde uygulama açılır ve kaynak hatası
   satırı görünür; işlenmeyen istisna oluşmaz.
6. `dotnet build -c Release` 0 hata 0 uyarı; `dotnet test -c Release` tamamı yeşil.
   CHECK: `dotnet test -c Release`

## Kayıt noktası

Her adım ayrı commit. Dal adı `worktree-agent-<kendi id>`.

## Kayıt noktası

Üç adım da bitti ve commit edildi: 3e57150 (Core yüzeyi), c16b523 (zincir), fb872a3 (ölçüm).
Dal `worktree-agent-a7144291bcf637e88`, taban `06ace01`.

## Rapor

Sözleşmenin "bugün yüklemiyor" öncülü eksikti: `MainWindow.axaml.cs:382` zaten
`Environment.GetCommandLineArgs().Skip(1).FirstOrDefault(File.Exists)` ile yüklüyordu
(774b187'den beri). Eksik olan Program/App zinciri değil, boşluklu yolun çözümü ve
listenin tek merkezde durmasıydı; o satır kaldırıldı, zincir sözleşmenin istediği gibi
kuruldu. Bu notu `live/_sorun.log` dosyasına da yazmak istedim, guard.js kabuktan
`live/` yazımını engelliyor — not yalnız burada.

## Çıktı

**Ne değişti**

- `src/VidShrink.Core/ShellIntegration.cs` (yeni): `MediaExtensions` (24 kalem, eski
  sıra) ve `ResolveStartupPath(IReadOnlyList<string>?)`. Çözüm her başlangıç
  noktasından en uzun birleşimi önce dener, tırnağı kırpar, `File.Exists`'e bakar;
  bulamazsa `null`. Uzantı süzmez.
- `src/VidShrink.App/MainWindow.axaml.cs`: gömülü `MediaExtensions` dizisi silindi,
  dosya seçici `ShellIntegration.MediaExtensions` okuyor. `MainWindow(string?)` kurucusu
  eklendi (parametresizi ona zincirlendi). `OnWindowLoaded` içindeki
  `Environment.GetCommandLineArgs` satırı `await LoadStartupFileAsync()` oldu;
  `LoadStartupFileAsync` yolu varsa sürükle-bırakın `LoadAsync`'ine verir, yoksa
  `Task.CompletedTask`.
- `src/VidShrink.App/App.axaml.cs`: `App(string?)` kurucusu, pencereyi
  `new MainWindow(_startupFile)` ile kuruyor.
- `src/VidShrink.App/Program.cs`: `Main` argümanları `ShellIntegration.ResolveStartupPath`
  ile çözüp `AppBuilder.Configure(() => new App(startupFile))`'a veriyor. Tasarımcının
  beklediği parametresiz `BuildAvaloniaApp()` duruyor.
- `tests/VidShrink.Tests/ShellIntegrationTests.cs` (yeni): 9 ölçü.

**Kabul kriterleri**

1. `grep -rn 'mkv' src/` medya listesi olarak yalnız `ShellIntegration.cs:13` dönüyor
   (diğer isabetler kap/kodek eşlemeleri ve MIME tablosu, liste kopyası değil).
2. `Single_existing_path_resolves`, `Quoted_path_loses_its_quotes`,
   `Path_broken_into_pieces_on_its_spaces_resolves` — geçti.
3. `Missing_path_returns_null`, `Folder_returns_null`,
   `Empty_and_blank_arguments_return_null` — geçti.
4. `grep -c "LoadAsync" src/VidShrink.App/MainWindow.axaml.cs` öncesi 4, sonrası 4.
   `Startup_file_goes_through_the_loader_drag_and_drop_uses` yüklemenin `LoadAsync`
   üzerinden geçtiğini dosya adı ve hata satırından ölçüyor.
5. Aynı ölçü medya olmayan `.txt` ile koşuyor: pencere kuruluyor, `TxtSourceStatus`
   görünür ve dolu, `LoadStartupFileAsync` istisnasız bitiyor.
6. `dotnet build VidShrink.sln -c Release -warnaserror`: 0 uyarı 0 hata.
   `dotnet test -c Release`: 788 geçti / 0 kaldı / 16 atlandı / 804 toplam.
   `sh tools/ci-gibi-kos.sh` (ffmpeg'siz, CI'ın gördüğü hal): 733 geçti / 0 kaldı /
   71 atlandı / 804 toplam.

## Mühür notu — GEÇTİ, tur 1

Altı kriterin altısı da denetçinin kendi koşturduğu ölçülerle karşılandı.

### Sözleşmenin öncülü yanlıştı, ajan çürüttü

"Uygulama argümanla gelen dosyayı açılışta yüklemiyor" diye yazmıştım. Yüklüyordu:
`06ace01:MainWindow.axaml.cs:381`, `774b187`'den beri
`Environment.GetCommandLineArgs().Skip(1).FirstOrDefault(File.Exists)`. Ben
`Program.cs`'e bakıp `MainWindow`'un içine bakmadan öncülü yazdım. **T0 hatası.**

Gerçek boşluk daha dardı ve kapandı: tırnaksız parçalanmış boşluklu yol hiçbir
parçada `File.Exists` bulamıyordu, ve uzantı listesi `MainWindow` içinde gömülü
olduğu için T68 aynı listeden beslenemiyordu.

Eski satır **kaldırıldı**, yan yana iki yükleme yolu bırakılmadı:
`grep -rn "GetCommandLineArgs" src/` artık boş, `grep -c "LoadAsync"` taban 4 → 4.
Açılış dosyası sürükle-bırakın `LoadAsync` yolundan geçiyor.

### Ölçüler

Doğrulama ağacında `0fc43f3`: `dotnet test -c Release` → 788 geçti / 0 kaldı /
16 atlandı / 804 toplam. Denetçi ayrıca `build -warnaserror` (0 uyarı) ve
`tools/ci-gibi-kos.sh` (733 / 0 / 71 / 804) koşturdu; üç sayı da ajanınkiyle birebir.

`MediaExtensions` 24 kalem, `06ace01`'deki sırayla birebir aynı — sessiz ekleme
ya da düşme yok.

### Borçlar

`ShellIntegration.Exists` `File.Exists`'i `try/catch` ile sarıyor; .NET'te o çağrı
geçersiz yolda istisna atmaz, false döner — ölü savunma ve ileride gerçek bir hatayı
yutabilir. `ResolveStartupPath` yolu normalleştirmiyor: `..` içeren göreli argüman
ham haliyle `LoadAsync`'e gidiyor; kabuk `"%1"` mutlak yol verdiği için gerçekçi
girdide tetiklenmiyor. `Window_without_an_argument_loads_nothing` içindeki
`Assert.NotEqual("", fileName)` satırı zayıf — dosya yüklenseydi de geçerdi; aynı
ölçünün diğer iki iddiası gerçek. Kriter 1'in `CHECK` komutu yazıldığı gibi tutmuyor
(`ConversionArguments.cs` kap eşlemesini de döndürüyor); kriterin özü tutuyor,
sözleşme metni hatası.

## Mühür notu — GEÇTİ, tur 1

### Sözleşmenin öncülü yanlıştı, ajan çürüttü

"Uygulama argümanla gelen dosyayı açılışta yüklemiyor" diye yazmıştım. Yüklüyordu:
`06ace01:MainWindow.axaml.cs:381`, `774b187`'den beri. `Program.cs`'e bakıp
`MainWindow`'un içine bakmadan öncülü yazdım. **T0 hatası.**

Gerçek boşluk daha dardı ve kapandı: tırnaksız parçalanmış boşluklu yol hiçbir
parçada `File.Exists` bulamıyordu, ve uzantı listesi `MainWindow` içinde gömülü
olduğu için T68 aynı listeden beslenemiyordu. Eski satır kaldırıldı, yan yana iki
yükleme yolu bırakılmadı: `grep -rn "GetCommandLineArgs" src/` boş.

### İki denetim, biri boşa

İlk denetim GEÇTİ verdi ama mühür kapısı kaydı reddetti — ajanı yanlış tiple
açmıştım, kayıt `worker` diye etiketlendi. Beş koşumluk bir denetim tamamen boşa
gitti. `.claude/agents/auditor.md` bunun için yazıldı; `tools/muhur.js` de eklenti
yolunu artık kendi çözüyor (`marketplaces/` altındaki sabit yol taşınmıştı, betik
hiç çalışmıyordu).

### Borçlar

`ShellIntegration.cs:47` `File.Exists`'i `try/catch` ile sarıyor — .NET'te o çağrı
atmaz; ölü savunma. `ResolveStartupPath` yolu normalleştirmiyor; kabuk `"%1"` mutlak
yol verdiği için gerçekçi girdide tetiklenmiyor. `ShellIntegrationTests.cs:116`
`Assert.NotEqual("", fileName)` tautoloji — dokuz ölçüden kırılınca kırmızıya
dönmeyen tek iddia. K1'in `CHECK` komutu yazıldığı gibi tutmuyor; kriterin özü
tutuyor, sözleşme metni hatası.
