---
id: T8
title: Arayüz — "Hızlı Düşür (GPU)" seçeneği
role: ui-builder
model: sonnet
depends: [T7]
owns: [src/VidShrink.App/MainWindow.xaml, src/VidShrink.App/MainWindow.xaml.cs, src/VidShrink.App/LanguageCatalog.cs]
side_effects: []
status: done
round: 0
agent_id: ui-builder-T8
audit: passed
auditor_id: T8-auditor
diff: MainWindow.xaml, MainWindow.xaml.cs, LanguageCatalog.cs
verification: build 0 uyari 0 hata, dotnet test 144/144, canli ekran dogrulandi
---

## Amaç

Kullanıcı seçenekler arasında "hızlı düşür, GPU kullanılır" diye bir seçenek görmek
istiyor. Bugün böyle bir şey yok; en yakını `Compression Algorithm` açılır listesindeki
`Fast - NVENC` maddesi ve o bir kodlayıcı adı, bir vaat değil. Kullanıcı kodlayıcı
adlarından hangisinin hızlı olduğunu çıkarmak zorunda kalmamalı.

T7 motora `SpeedMode` seçeneğini ekledi. Bu sözleşme onu arayüze getirir.

## Kabul kriteri

1. **Yeni onay kutusu.** `May Lower Resolution` ve `May Lower Frame Rate` kutularının
   olduğu satırın altına yeni bir satır açılır ve içine `ChkFastGpu` adında bir
   `CheckBox` konur. Metni: Türkçe `Hızlı Düşür (GPU)`, İngilizce `Fast Shrink (GPU)`.
   Varsayılan kapalı. Yanında mevcut `InfoButton` deseniyle bir `?` düğmesi.
   Stil `CheckStyle` ve `InfoButton` kaynaklarından gelir; yeni renk, yeni ölçü,
   yeni fırça **tanımlanmaz**. `App.xaml` bu sözleşmenin sahipliğinde değil.

2. **İpucu metni.** `?` düğmesinin ipucu şunu anlatır, ölçülen rakamlarla:
   ekran kartı kodlamayı kat kat hızlandırır; VidShrink kartın en iyi kodlayıcısını
   seçer ve modern kartlarda AV1 kodlayıcısı yazılım kodlayıcısıyla neredeyse aynı
   kaliteyi yaklaşık yedi kat hızlı verir; daha eski kartlarda kazanılan hızın bedeli
   megabayt başına bir miktar kalitedir. Metin İngilizce yazılır ve `LanguageCatalog`
   içine Türkçe karşılığı eklenir — dosyadaki mevcut düzen budur.

3. **Kutu motora bağlanır.** `CurrentOptions()` içine
   `SpeedMode = ChkFastGpu.IsChecked == true ? SpeedMode.Fast : SpeedMode.Quality`
   eklenir. `Click="OnOptionChanged"` ile plan yeniden hesaplanır.

4. **GPU yoksa kutu kapalı ve sebebi görünür.** Pencere açılırken
   `EncoderCapabilities.Instance.WorksAsEncoder` ile donanım kodlayıcıları yoklanır.
   Hiçbiri çalışmıyorsa `ChkFastGpu.IsEnabled = false` olur ve ipucu metni
   "bu bilgisayarda kullanılabilir bir donanım kodlayıcı bulunamadı" satırıyla değişir.
   Yoklama arayüzü kilitlememeli — `Loaded` olayında arka planda koşsun, bittiğinde
   kutuyu güncellesin.

5. **Kodlayıcı listesindeki `Fast - NVENC` maddesi kalkar.** Artık iki yerde iki farklı
   "hızlı" vaadi olmaz. `Compression Algorithm` listesi üç maddeye iner:
   `Automatic - Engine Decides`, `Compatible - H.264`, `Max Compression - H.265`.
   `CodecFromIndex` buna göre sadeleşir. Listenin ipucu metninden NVENC cümlesi çıkarılır;
   yerine hızın artık ayrı bir seçenek olduğu tek cümleyle söylenir.
   Türkçe karşılıkları `LanguageCatalog` içinde güncellenir; artık kullanılmayan
   anahtarlar silinir.

6. **Plan özetinde görünür.** Hızlı mod açıkken `What It Will Do` panelinde hangi
   donanım kodlayıcısının seçildiği okunabilmeli. Plan zaten kodlayıcı adını içeriyorsa
   ek satır yazma; içermiyorsa tek satır ekle.
   Motor GPU isteğine rağmen yazılıma düştüyse (`AdviceCode.EncoderFallback`) kullanıcı
   bunu görmeli. `MainWindow.xaml.cs:339` civarındaki `AdviceCode` çeviri tablosu bunun
   yeri.

7. **Türkçe ve İngilizce eksiksiz.** `TR` / `EN` düğmeleriyle geçildiğinde yeni metinlerin
   hiçbiri İngilizce kalmaz. Bu bir "sonra bakarız" maddesi değil, kabul kriteri.

## Arayüzler

T7'nin verdiği:

```csharp
enum SpeedMode { Quality, Fast }
PlanOptions.SpeedMode { get; set; }
bool IEncoderAvailability.WorksAsEncoder(string codec);
```

## Bağlam

- `MainWindow.xaml:145-151` mevcut onay kutusu satırı; yeni satır bunun hemen altına.
- `MainWindow.xaml:133-142` kodlayıcı açılır listesi ve ipucu.
- `MainWindow.xaml.cs:115` `CurrentOptions()`.
- `MainWindow.xaml.cs:222-228` `CodecFromIndex`.
- `MainWindow.xaml.cs:339` `AdviceCode` çeviri tablosu.
- `LanguageCatalog.cs:36` ve `:46` bugünkü NVENC metinleri.
- Neon arayüz standardı context'ine önyüklü; **renk ve ölçü uydurma**, mevcut
  kaynaklardan al.
- Bu depoda **kod yorumu yazılmaz**.
- Türkçe yaz.

## Doğrulama

- `dotnet build` sıfır uyarı.
- Uygulamayı çalıştır, gerçek bir dosya yükle, kutuyu aç ve kapa: plan özeti her
  seferinde değişiyor mu, ffmpeg komut kutusundaki kodlayıcı adı değişiyor mu — gör.
- `TR` ve `EN` arasında geçiş yap, yeni metinlerin ikisinde de doğru çıktığını gör.
- Ekran görüntüsü al ve yolunu Çıktı bölümüne yaz.

## Kayıt noktası

Yedi kriterin yedisi de kapandı. Durum dosyası: `.claude/relay/live/T8-durum.md`.
Taban commit `50882e2`, iş commit `d6aa14d`.

## Çıktı

Değişen dosyalar:

- `src/VidShrink.App/MainWindow.xaml` — `ChkFastGpu` onay kutusu, `?` düğmesi ve
  `TipFastGpu` ipucu; `Fast - NVENC` maddesi listeden çıktı; kodlayıcı ipucundan
  NVENC cümlesi çıktı, yerine hızın ayrı seçenek olduğu cümle geldi.
- `src/VidShrink.App/MainWindow.xaml.cs` — `CurrentOptions()` içine `SpeedMode`;
  `CodecFromIndex` üç maddeye indi; `Loaded` içinde `ProbeHardwareEncodersAsync`
  arka planda; `ApplyFastGpuTip`; `AdviceCode.EncoderFallback` hızlı mod açıkken
  GPU'ya özel metin veriyor; `AdviceCode.HardwareCodecCostsQuality` metni NVENC yerine
  genel donanım kodlayıcısını anlatıyor.
- `src/VidShrink.App/LanguageCatalog.cs` — dil anahtarları.

Eklenen dil anahtarları:

- `Fast Shrink (GPU)` → `Hızlı Düşür (GPU)`
- GPU ipucu metni (ekran kartı, AV1, yedi kat)
- Donanım kodlayıcı bulunamadı metni
- Kodlayıcı listesinin yeni ipucu metni

Silinen dil anahtarları:

- `Fast - NVENC`
- Kodlayıcı listesinin eski ipucu metni (NVENC cümleli hâli)

GPU yoklaması arayüzü hiç bekletmiyor: `Task.Run` içinde koşuyor, `Loaded` dönüşünü
tutmuyor. Yoklamanın kendi maliyeti bu makinede ölçüldü — ilk aday `av1_nvenc` için
ffmpeg deneme kodlaması 236 ms. Arayüz bu süre boyunca çizili ve tıklanabilir; kutu
yoklama bitince güncelleniyor.

Ekran görüntüleri:

- `.claude/relay/live/gorseller/T8-tr.png` — dosya yokken, kutu kapalı
- `.claude/relay/live/gorseller/T8-yuklu.png` — kalite modu, plan `libx264 · Preset slow`
- `.claude/relay/live/gorseller/T8-hizli-tr.png` — hızlı mod açık, plan `av1_nvenc · CRF 28 · Preset p5`
- `.claude/relay/live/gorseller/T8-hizli-en.png` — aynı durum İngilizce, `Fast Shrink (GPU)`

Kapatılamayan yok. İki not:

- Kriter 4'ün "GPU yok" dalı bu makinede canlı görülemedi; makinede `av1_nvenc` çalışıyor.
  Kod yolu yazıldı ve derlendi, kutunun kapanması ve ipucunun değişmesi ekranda
  doğrulanamadı. Donanım kodlayıcısı olmayan bir makine gerekiyor.
- İpucu metinleri tek paragraf yazıldı. Neon standardı §3.2 blok istiyor ama dosyadaki
  bütün ipuçları tek paragraf; tek bir ipucunu ayırmak tutarsızlık olurdu.

## Rapor

Ekran görüntüsü için computer-use erişimi alınamadı — `request_access` "VidShrink" ve
"VidShrink.App" adlarının ikisini de tanımadı. Yerine PowerShell + `PrintWindow` ile
pencere doğrudan yakalandı, süreç yolu her karede doğrulandı.

WPF sekme içeriği UI Automation ağacında görünmüyor (pencerenin altında yalnızca 23
öğe var, `ChkFastGpu` yok). Onay kutusu koordinat tıklamasıyla açıldı, dil düğmesi
`BtnEn` üzerinden `InvokePattern` ile çevrildi.

## Denetim

GECTI. Yedi kabul kriteri de dogrulandi.

"Donanim kodlayici yok" dali canli gorulemedi (bu makinede av1_nvenc calisiyor),
kod okunarak gecirildi: yoklama `Task.Run` icinde ve `Loaded`'da beklenmiyor, arayuz
donmuyor; false donerse kutu gercekten kapaniyor ve ipucu gercekten degisiyor.
`Fast - NVENC` dizesi kaynak agacinda hic gecmiyor, silinen iki dil anahtarinin
referansi kalmamis, kayitli ayar riski yok cunku uygulama hic ayar okumuyor.

Bilincli atlanan iki madde turu tetiklemedi: ipucu blok bolme ve ikinci imza blogu
T8'in Kabul kriterlerinde gecmiyor.

Denetimin birakti dort not, ucu T9'a devredildi:

1. `MainWindow.xaml.cs:35 HardwareEncoderOrder`, `PlanCalculator.cs:45-48
   FastHardwareOrder`'in birebir kopyasi. Core sabiti private oldugu icin builder
   paylasamazdi, ama T9 donanim yoluna dokunacagi icin iki liste sessizce ayrisir.
2. `MainWindow.xaml.cs:52 _ = ProbeHardwareEncodersAsync()` istisnayi sessizce yutuyor.
   Bugun alt yollar try/catch'li, ama firlarsa kutu acik ve ipucu yanlis kalir.
3. Yoklama bitene kadar kutu etkin. Donanim kodlayicisi olmayan makinede 7 aday x 4 sn
   zaman asimi ile pencere uzayabilir; kullanici o sirada Fast plani gorup sonra kutunun
   kendiliginden kapandigini gorur. Sozlesme asenkron yoklamayi istedigi icin ihlal degil.
4. `_hardwareEncoderAvailable` alaninin tek okuyucusu olu bir kosul.

Denetci not birakti: `skills/teknesyum-ui` bu makinede bulunamadi, neon standardinin
§3.2 ve §4 maddelerinin lafzi dogrulanamadi. Maddeler Kabul kriterinde olmadigi icin
turu tetiklemedi.
