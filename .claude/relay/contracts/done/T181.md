---
id: T181
title: PlayerView dil aboneligi sizintisi ve BtnMaximize kirpilmasi
role: builder
model: opus
depends: [T176]
blocked-by: []
status: done
result: passed
round: 1
verify:
  - dotnet test tests/VidShrink.Tests/VidShrink.Tests.csproj -c Release --filter "KabukIstegiTests|HardwareVerdictTests|LanguageTests|LocalizationTests"
  - dotnet test tests/VidShrink.Tests/VidShrink.Tests.csproj -c Release --filter "WindowLayoutTests|OynaticiGirdiTests"
owns:
  - src/VidShrink.App/Playback/PlayerView.axaml
  - src/VidShrink.App/Playback/PlayerView.axaml.cs
  - src/VidShrink.App/Locales/en/main.json
  - src/VidShrink.App/Locales/tr/main.json
  - tests/VidShrink.Tests/OynaticiGirdiTests.cs
  - tests/VidShrink.Tests/WindowLayoutTests.cs
side_effects: []
---

## Baglam

T176 ile `main`e giren iki kusur, 0.3.0 surumunu tikiyor. Ikisi de T0 tarafindan
**main uzerinde kosularak** dogrulandi, ucuncu tarafin iddiasi degil.

### Kusur 1 — dil aboneligi kopmuyor

**DUZELTME (tur 1, olculdu).** Bu bolumun ilk yazimi yanlisti: `Strings.Changed -=
OnLanguageChanged` `OnDetachedFromVisualTree` icinde zaten vardi (`PlayerView.axaml.cs:176`,
T176'dan beri). Gercek neden: `LanguageTests` gibi olcumler `MainWindow`'u hic `Close()`
etmiyor, oynatici gorsel agaca ekli kaliyor; ardindan `LocalizationTests` / `KabukIstegiTests`
`Strings.Use`'u xUnit is parcacigindan cagiriyor ve `RefreshState()` `TextBlock.Text`'i
**UI is parcacigi disindan** yaziyor. Cozum is parcacigi marshal'i.

Uretim izi (main, `4f86ad73` sonrasi):

```
at Avalonia.Controls.TextBlock.set_Text(String value)
at VidShrink.App.Playback.PlayerView.RefreshState() PlayerView.axaml.cs:373
at VidShrink.App.Playback.PlayerView.OnLanguageChanged(Object, EventArgs) PlayerView.axaml.cs:368
at VidShrink.App.Localization.Strings.Use(String language) Strings.cs:114
at VidShrink.Tests.KabukIstegiTests.GerekceOlcusuSurecDilindenEtkilenmez KabukIstegiTests.cs:195
```

```
--filter "KabukIstegiTests|HardwareVerdictTests|LanguageTests|LocalizationTests"
Basarisiz: 1, Basarili: 147, Atlanan: 2, Toplam: 150
```

Kusur **siraya bagli**: tek basina `--filter "LocalizationTests|LanguageTests"`
89/89 yesil doner. Bu yuzden bugune kadar gorunmedi. Testin sirasini degistirerek
yesile getirmek **cozum degildir** — abonelik gercekten kopmalidir.

### Kusur 2 — BtnMaximize kirpiliyor

`WindowLayoutTests.NoTurkishBoxLabelIsClipped(loaded: False, narrow: False)` kirmizi:
`Oynatici · BtnMaximize [□]: gereken 13,8, kalan yer 10`.

## Is

1. `Strings.Changed` aboneligi `OnDetachedFromVisualTree` icinde cikarilir.
2. `OnLanguageChanged` UI is parcacigina marshal edilir (`Dispatcher.UIThread`),
   boylece olay hangi is parcacigindan gelirse gelsin `RefreshState()` guvenli kosar.
   Iki onlem de alinir; birincisi sizintiyi kapatir, ikincisi ayni sinifin baska
   yollardan tetiklenmesine karsi taban olur.
3. **Kusur 2 urun kusuru degil, olcum kusuru** (tur 1'de olculdu). `NoTurkishBoxLabelIsClipped`
   bayat `Bounds.Width` okuyor: yazi tipi yedekleme onbellegi Oynatici sekmesindeki `⋮` ile
   isiniyor ve **sonraki** `□` olcumu 9,66'dan 13,78'e siciriyor. Taze olcumde metin 14 px,
   gereken 13,78, dugme 42 px — kirpilma yok. Yani `Theme.axaml`'de degistirilecek bir olcu
   yok; onarim testin kendisinde: `WindowLayoutTests.cs:1320` civarinda olcumden once
   yedeklemeyi isit ya da `InvalidateMeasure()` + yeniden yerlestir, sonra `Bounds`'u oku.
   Test **tek basina** kosuldugunda da yesil olmali — su an sirasina bagli yesil.

## Kabul kriteri

- CHECK: `dotnet test tests/VidShrink.Tests/VidShrink.Tests.csproj -c Release --filter "KabukIstegiTests|HardwareVerdictTests|LanguageTests|LocalizationTests"` → Basarisiz **0**.
- CHECK: `dotnet test tests/VidShrink.Tests/VidShrink.Tests.csproj -c Release --filter "WindowLayoutTests|OynaticiGirdiTests"` → Basarisiz **0**.
- CHECK: `OynaticiGirdiTests` icine yeni test — bir `PlayerView` gorsel agaca eklenip
  **ayrildiktan sonra** `Strings.Use(...)` cagrilir ve hicbir istisna atilmadigi iddia
  edilir. Test, duzeltme geri alindiginda **kirmizi olmalidir**; mutasyonu kos ve
  raporunda kanitini yaz.
- Her filtrenin kac test yakaladigi `--list-tests` ile olculur ve rapora yazilir.
  Sifir teste denk gelen bir kol sessizce yesil doner; bu depoda olculdu.

## Kayit noktasi

Dal: `T181-oynatici-dil-sizintisi`. Bitince `origin`'e it, `main`e birlestirme.

## Cikti

Rapor: ne degisti, mutasyon kaniti, her verify komutunun tam ciktisi ve yakaladigi
test sayisi, son commit sha'si.
