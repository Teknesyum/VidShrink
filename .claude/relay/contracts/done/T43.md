---
id: T43
title: Panel ana pencereye bağlanacak — sentetik kare biter, gerçek kaynak başlar
role: ui-builder
model: opus
depends: [T38, T39, T40]
status: done
round: 2
audit: passed
auditor_id: abed09ba44524a02b
diff: 4d0fd00..73e19b9
verification: dotnet build -c Release VidShrink.sln 0 uyari 0 hata; dotnet test 369 basarili 0 basarisiz 8 atlanan
owns: [src/VidShrink.App/MainWindow.axaml, src/VidShrink.App/MainWindow.axaml.cs, src/VidShrink.App/Playback/PanelHost.cs, src/VidShrink.App/LanguageCatalog.cs]
side_effects: [src/VidShrink.App/Playback/ComparisonPanel.axaml, src/VidShrink.App/Playback/ComparisonPanel.axaml.cs]
---

## Amaç

T38 kare kaynağını, T39 sunum yüzeyini, T40 denetim şeridini kurdu. Üçü de ayrı ayrı
denendi ama **program hiçbirini göstermiyor** — panel `MainWindow`'a hiç girmedi ve
sentetik kareyle çalışıyor.

Bu sözleşme üçünü birleştirir. Kullanıcının istediği tablo bundan sonra ekranda olacak:

> ortadaki ekranda bir video olacak (…) solda orjinal kalite sağda sahte kalite

## Kabul kriterleri

### K1 — Panel ana pencerede, ortada

Panel `MainWindow` düzeninin ortasına girecek. T14'ün üç sütunlu düzeni bozulmayacak:
Kaynak+Hedef · **panel** · Çıktı+AI ayarları. Yapılacak işlem paneli yerini panele
bırakacaksa nereye gittiği Rapor'da yazılacak.

- Boş durumda açılışta **kaydırma çubuğu çıkmayacak** — T14/K1 hâlâ geçerli.
- Dosya yüklenmeden panel boş durum gösterecek, yer kaplamayacak kadar küçük olacak.
- Terfi eden panel kök katmana çıkıyor; yer tutucu bu düzende doğru yerde kalacak.

### K2 — Gerçek kaynak bağlanacak

Sentetik üreteç kalkacak, `IComparisonFrameSource` bağlanacak.

- Sol taraf **kaynak dosya**, sağ taraf **işlenmiş dosya**. İşlenmiş dosya henüz yoksa
  sağ taraf "bu kısım işleme sokulacak" der ve **sahte çıktı sunmaz** — planın §6
  dürüstlük kuralı, kullanıcının kendi düzeltmesi.
- Kare `TryTake` ile alınacak, çizildikten sonra `Return` ile havuza iade edilecek.
  İade edilmeyen kare havuzu kurutur.
- Kaynak `Kullanilamiyor` durumuna düşerse panel sebebi iki dilde gösterecek ve
  program çalışmaya devam edecek — panel yokluğu programı bozmayacak.
- Pencere kapanırken kaynak `Dispose` edilecek; öksüz ffmpeg kalmayacak.

### K3 — Panel ölçüsü kareyi belirler

`ComparisonFrameRequest.PanelWidth`/`PanelHeight` **panelin o anki ölçüsünden**
gelecek, sabit sayıdan değil. Ölçü değişince akış yeniden kurulacak ama:

- Pencere sürüklenirken her pikselde yeniden kurulmayacak — yerleşme beklenecek.
- Yakınlaştırma akışı yeniden kurmayacak. Yakınlaştırma sunum katmanının işi
  (T39/K3), kod çözme tetiklemez. Bu kural burada da bozulmayacak.

### K4 — Oynatma programı yavaşlatmayacak

Kodlama koşarken panelin ne yapacağı T42'nin konusu, burada karar verilmiyor. Ama
**boştaki program** panel açıkken de akıcı kalacak: hedef kutusuna yazmak, dil
değiştirmek, kaydırıcıyı sürüklemek donmayacak.

Panel kapalıyken hiçbir ffmpeg süreci ayakta olmayacak — açılmamış panel maliyet
çıkarmayacak. Bunu süreç sayarak kanıtla.

### K5 — Dil ve erişilebilirlik

Panelin bütün metinleri `LanguageCatalog` üzerinden, iki dilde. Gömülü metin
bırakılmayacak. Panel klavyeyle açılıp kapanabilecek.

## Doğrulama — toplu yapılacak

Tek oturumda, arka arkaya, ekran görüntüsüyle:

1. Açılış — panel boş durumda, kaydırma çubuğu yok
2. Dosya yüklenmiş, panel gerçek kareyi gösteriyor
3. Ayırıcı sürüklenmiş — solda kaynak, sağda işlenmiş
4. Yakınlaştırılmış panel
5. Terfi etmiş panel, altındaki bant yer tutucuyla duruyor
6. TR/EN geçişi
7. Panel kapalıyken ayakta ffmpeg süreci sayısı (0 olmalı)
8. Panel açıkken hedef kutusuna yazmak — donma var mı

Ölçülecek ve Rapor'a yazılacak: panel açıkken ekrana konan kare/saniye, düşen kare,
kapanışta ayakta kalan ffmpeg süreci.

Kaynak: `C:\Users\Administrator\Videos\gothic2026-08-15 14-01-29.mp4`.

`dotnet build -c Release` 0 uyarı · `dotnet test` tamamı yeşil.

## Kayıt noktası

K1-K5 kodu yazıldı, `dotnet build -c Release` 0 uyarı. Sırada test ve tek oturumluk
arayüz doğrulaması var.

Doğrulama koşusu çalışıyor: panel ortada, gerçek kare geliyor, sağ taraf perdeli.
Bulunan ve düzeltilen iki hata: kaynağın kapatılması arayüz kuyruğunda bekleyince program
donuyordu (kapatma havuz kuyruğuna alındı), boş durum ilk karede kalkmıyordu (sunum turu
bir tur sonra çalışıyor).

## Çıktı

- `src/VidShrink.App/MainWindow.axaml` — orta sütun iki katmanlı ızgara oldu: üstte
  "Preview" başlığı + aç/kapa düğmesi + `ComparisonPanel`, altında yapılacak işlem paneli.
  Üç sütunlu düzen bozulmadı, işlem paneli aynı sütunda aşağı indi.
- `src/VidShrink.App/MainWindow.axaml.cs` — `PanelHost` kuruluyor; dosya yüklenince ve
  kodlama bitince `RefreshPreviewSource`, dil değişiminde `SetLanguage`, kapanışta
  `Dispose`. Panel dosya yüklenince kendiliğinden açılıyor.
- `src/VidShrink.App/Playback/PanelHost.cs` — yeni. Paneli `IComparisonFrameSource`'a
  bağlayan taraf: TryTake→kopyala→Submit→Return döngüsü RequestAnimationFrame üstünde,
  kare ölçüsü panonun o anki ölçüsünden (en boy oranı korunarak), yerleşme 360 ms
  (MotionSlow) beklemeli, kurulumlar zincirle sıralı, kapatma havuz kuyruğunda.
- `src/VidShrink.App/LanguageCatalog.cs` — panel metinleri sözlüğe girdi; `Localize` ve
  `Upper` (tr-TR kültürlü) yardımcıları eklendi.
- `src/VidShrink.App/Playback/ComparisonPanel.axaml(.cs)` — bağlama yüzeyi: `Controls`,
  `SetCompact`, `SetNotice`, `SetRightNotice`, sağ perde (işlenmiş dosya yokken görüntü
  yerine "bu kısım işleme sokulacak"), metinler tek geçitten.

## Rapor

Standart yürürlükte (`~/.claude/teknesyum-ui.json`), renk/ölçü uydurulmadı — perde ve
metinler mevcut Playback/Theme belirteçlerinden.

K1: panel ortada, işlem paneli altında; varsayılan açılışta (büyütülmüş, dosya yok)
kaydırma taşması 0,0. K2: sentetik üreteç üretim yolundan çıktı (yalnız tasarım kipinde
kalıyor; dosyanın kendisi owns dışında, _sorun.log'da), sol kaynak sağ perde, kare
TryTake/Return ile aynı turda iade, Kullanilamiyor sebebi iki dilde boş duruma düşüyor,
kapanışta Dispose. K3: ölçü panodan, yerleşme beklemeli, yakınlaştırma ve terfi akışı
yeniden kurmuyor (ffmpeg 1→1, kare ölçüsü sabit kaldı). K4: kapalıyken hstack'li ffmpeg
0 (WMI komut satırından sayıldı), yazma sırasında en kötü arayüz turu 1,0-3,9 ms.
K5: bütün metinler LanguageCatalog'dan, TR/EN ekran görüntülü, aç/kapa düğmesi klavyeyle
erişilebilir Button.

Doğrulama tek oturumda, ekran kapısına uygun ekran dışı gerçek pencereyle
(scratchpad/T43Probe, depoya dosya yazılmadı): 9 ekran görüntüsü + canli.log.
Ölçülen: ekrana konan kare/saniye 27,8-35,3 (kaynak 48 fps, -re temposu), kaynak
sayaçları 935-1010 üretilen / 345-387 düşen (halka en eskisini düşürüyor, gecikme
sınırlaması — sunulan akış akıcı), kapanışta ayakta ffmpeg 0.

Doğrulamanın yakaladığı ve düzeltilen üç hata: kaynak kapatması arayüz kuyruğunda
deadlock (Task.Run'a alındı), boş durumun ilk karede kalkmaması (sunum turu gecikmesi),
her hedef tuşunda akışın yeniden kurulması + yarışan kurulumların öksüz ffmpeg bırakması
(SetFiles aynı dosyada erken çıkıyor, kurulumlar zincirlendi).

Denenemedi: kodlama koşarken panel davranışı (T42'nin konusu), gerçek işlenmiş dosyayla
sağ taraf (koşumda kodlama yapılmadı; yol `_lastOutput` üzerinden bağlı).

`dotnet build -c Release` 0 uyarı · `dotnet test` 369 başarılı 0 başarısız 8 atlanan.

## Tur 2 — denetim GEÇTİ, T0 iki kusur kapattı

Denetim GEÇTİ verdi ama düşük kare hızının sebebini buldu ve borç olarak bıraktı. T0
ölçtü, sebebi doğruladı ve kapattı; ölçüm sırasında ikinci ve daha ciddi bir kusur
ortaya çıktı.

### Birincisi — tur başına tek kare, kalıcı geri kalma

Tüketici iki kademede de sunum turu başına **bir** kare alıyordu: `PanelHost.Drain`
kaynaktan bir kare, `ComparisonSurface.Round` kendi kuyruğundan bir tampon. Tur hızı
besleme hızının altına düştüğü an geri kalma kalıcılaşıyor, halka her turda taşıyor ve
ekrana konan görüntü gerçek zamandan geriye kayıyordu.

İkisi de **yetişmeli** hâle getirildi: tur başına kuyruk boşaltılıyor, yalnız sonuncusu
sunuluyor, atlananlar çizilmeden havuza dönüyor. Yetişiliyorken davranış değişmiyor —
sırada tek kare varsa o tek kare sunulur; atlama yalnız gerçekten geride kalınca oluyor.
`PanelHost` tarafında atlama tur başına 8 kareyle sınırlı.

Ölçüldü (aynı kaynak, gerçek pencere, panel 904×254):

| | Ekrana konan fps |
|---|---|
| Düzeltmeden önce (T43 turu, ekran dışı) | 27,8-35,3 |
| Sonra, ekran dışı | 40,8 |
| Sonra, **görünür pencere**, 6 koşu ortanca | 40,0 · 44,0 · 44,0 · 44,7 · 44,7 · 46,0 |

Kaynak 48 fps besliyor, ölçülen besleme 47-53. Ekrana konan sayı artık **kaynağın
hızında**; tüketici duvar değil.

### İkincisi — `hstack` sondası yanlış "çalışmıyor" diyordu

Ölçüm koşularının birinde panel hiç açılmadı ve kullanıcıya *"Bu ffmpeg yapısında hstack
filtresi çalışmıyor"* dendi. Aynı makinede aynı ffmpeg ile diğer koşular çalıştı — sonda
yanlış negatif veriyordu.

İki kusur üst üste binmişti: sondanın 6 saniyelik süresi makine yüklüyken yetmiyor, ve
**süre dolması yetenek cevabı sayılıp önbelleğe yazılıyordu.** Bir kez düşen sonda
oturum boyu paneli kapatıyor, üstelik sebebi kullanıcının ffmpeg'ine yıkıyordu.

Sonda artık üç cevap veriyor: çalışıyor · çalışmıyor · **belirsiz**. Yalnız kesin
cevaplar önbelleğe giriyor, belirsiz olan bir kez daha deneniyor ve önbelleğe hiç
yazılmıyor. Süre 20 saniyeye çıkarıldı. Belirsiz durumda kullanıcıya gösterilen metin
de değişti: makinenin meşgul olabileceğini söylüyor, ffmpeg'i suçlamıyor.

Düzeltmeden sonra **altı görünür koşu, tek bir yanlış düşme yok.** Kapanışta ayakta
ffmpeg 0.

### Denetçinin duran üç borcu

- `StartAsync` await sonrası `_disposed`'ı yeniden kontrol etmiyor; milisaniyelik
  pencerede öksüz ffmpeg mümkün (`PipeComparisonFrameSource.cs:102-128`).
- İşlenmiş dosya yokken `RightPath=_left`: perdenin arkası için kaynak ikinci kez kod
  çözülüp ölçekleniyor (`PanelHost.cs:178`) — görünür çıktı dürüst, yalnız boşa maliyet.
- `_lastOutput`, `result.Success` kontrol edilmeden atanıyor
  (`MainWindow.axaml.cs:1654-1657`); `File.Exists` kapısı yanlış bağlamayı engelliyor.

`dotnet build -c Release` 0 uyarı · `dotnet test` 369 başarılı.
