---
id: T64
title: Başarım denetçisi arayüzü — Gelişmiş sekmesinde okunur cevap
role: ui-builder
model: opus
depends: []
status: submitted
round: 1
audit: passed
auditor_id: a233f73ac89d48f10
diff: 4 dosya, owns disi yok
verification: dogrulama agacinda 1739e01: build 0 hata 0 uyari · dotnet test 761 gecti / 0 kaldi / 16 atlandi / 777 toplam
owns: [src/VidShrink.App/MainWindow.axaml, src/VidShrink.App/MainWindow.axaml.cs, src/VidShrink.App/Performance/PerformanceReportText.cs, tests/VidShrink.Tests/PerformanceCheckUiTests.cs]
side_effects: ["kullanici dugmeye bastiginda kisa ffmpeg kodlamalari kosar (en cok 20 sn), CPU ve GPU yuklenir; kendiliginden hic kosmaz"]
---

## Amaç

T63 ölçüyor, sen gösteriyorsun. Kullanıcının cümlesi:

*"serkanın fps düşüyormuş oyunda kayıttan mı diye soruyor gelişmiş kısmına bi
performance checker gibi bi tool koysak iyi olabilir"*

Ekrandaki panelin cevaplayacağı soru: **bu makinede video kodlaması işlemciden ne
alıyor, ve kayıt sırasındaki kare hızı düşüşünü bu açıklar mı?**

## Önce sınırı oku — burası bu işin en kolay yanlış yapılan yeri

VidShrink **kayıt yapmaz.** Serkan'ın oyununu kaydeden şey OBS, ShadowPlay ya da
Game Bar. VidShrink'in ölçtüğü şey **bu makinede kodlamanın maliyeti**, kaydedicinin
ne yaptığı değil.

Panel bu ayrımı **ekranda söylemek zorunda.** "Kaydın FPS'ini düşürüyor" ya da
"kaydın sorun değil" diyen bir cümle yazma hakkın yok — o ölçüm yapılmadı. Panel
şunu söyleyebilir: bu makinede donanım kodlayıcısı çalışıyor mu, yazılım kodlaması
kaç çekirdek istiyor, ve kaydedicinin ayarına bakmanın neden ilk adım olduğu.

T63 sözleşmesinde bu paragrafın karşılığı zaten var; oradaki sınırı daralt, genişletme.

## Bağlam

Gelişmiş sekmesi `src/VidShrink.App/MainWindow.axaml:974` — `PageAdvanced` adlı bir
`ScrollViewer`, içinde `Panel` temalı iki `Border` var: `CommandPanel` ve `AiPanel`.
İkisi de aynı kalıpta: `H2` başlık, sağda `▾` `GhostButton`, altta `IsVisible="False"`
açılan ayrıntı bloğu. Üçüncü panelini **aynı kalıpta** yaz, kendi düzenini uydurma.

T63'ün ürettiği yüzey (`src/VidShrink.Core/PerformanceCheck.cs`):

- `PerformanceCheckResult` — `Impact`, `HardwareCodec`, `SoftwareCodec`,
  `HardwarePipelineRealtimeFactor`, `SoftwareRealtimeCores`, `LogicalCores`,
  `ElapsedMs`, `CpuAccountingFactor`, `Findings`
- `RecordingImpact` — `Unknown`, `HardwareOffload`, `SoftwareLightLoad`, `SoftwareHeavyLoad`
- `PerformanceFinding` — `Code` + sayılar. **Serbest metin yok, bilerek yok.**
- `PerformanceFindingCode` — 12 değer, hepsi `PerformanceCheck.cs:51-100` arasında belgeli.
- Ölçümü başlatan: `PerformanceProbe.RunAsync(...)`, `src/VidShrink.Ffmpeg/PerformanceProbe.cs:70`.
  Bütçesi 20.000 ms (`BudgetMs`).

Cümleyi kuran taraf **sensin**: `PerformanceReportText` bulgu kodunu + sayılarını alır,
kullanıcının okuyacağı satırı döner. Bu ayrım kasıtlı — cümle Core'da olsaydı ölçüm ile
anlatım aynı yerde durur ve biri değişince öteki sessizce eskirdi.

## Sınırlar

- `src/VidShrink.Core/**` ve `src/VidShrink.Ffmpeg/**` **sana kapalı.** T63 mühürlendi.
  Ölçümde eksik bulursan düzeltme — sözleşmeye yaz, T0 karar versin.
- Renk, boşluk, yazı boyu: **yalnız `Themes/Theme.axaml` belirteçleri.** Yeni renk
  ekleme, ölçü uydurma. Mevcut `Panel`, `H2`, `Hint`, `GhostButton`, `SpaceMd`,
  `SpaceLg`, `SectionMargin` temaları yeterli.
- Arayüz dizgileri **İngilizce** — sekme başlıkları zaten İngilizce ("Advanced",
  "FFmpeg command", "AI settings"). Sözleşme ve rapor Türkçe kalır.
- Ölçüm **kendiliğinden koşmaz.** Uygulama açılışında, sekme açılışında, dosya
  yüklenince — hiçbirinde. Yalnız kullanıcı düğmeye basınca.

## Kabul kriteri

**K1 — Panel Gelişmiş sekmesinde ve mevcut kalıpta.**
`PageAdvanced` altına üçüncü bir `Panel` temalı `Border` girer; `H2` başlık, sağda
`▾` `GhostButton`, altta kapalı açılan blok. `CommandPanel`/`AiPanel` ile aynı
`Grid` sütun düzeni.
CHECK: `dotnet test -c Release --filter PerformanceCheckUiTests` yeşil.

**K2 — Ölçüm kendiliğinden koşmuyor.**
Ölçü, pencere kurulduktan sonra `PerformanceProbe`'un hiç çağrılmadığını gösterir;
yalnız düğme tıklanınca çağrılır.
CHECK: pencere kurulur, sahte sonda çağrı sayacı 0; düğme tetiklenir, sayaç 1.

**K3 — Her bulgu kodunun tam bir karşılığı var, `_ =>` yok.**
`PerformanceReportText` `PerformanceFindingCode`'un **12 değerinin hepsini** ayrı ayrı
karşılar. Ölçü `Enum.GetValues` ile döner ve her değer için boş olmayan, birbirinden
farklı bir satır ister. Eşleme içinde `_ =>` ya da `default:` **yasak** — T63'ün
tur 2'sinde tam bu serbest geçiş kaldırıldı, geri getirme.
CHECK: enum üzerinde dönen ölçü kırmızıya düşmeden geçer; kaynakta `_ =>` yok.

**K4 — Ölçülmeyen şey "ölçülmedi" diye görünür.**
`HardwareCpuCostNotMeasured` bulgusu geldiğinde panel bunu **ekranda** söyler.
Sessizce atlanamaz: donanım bacağının işlemci maliyeti bilinmiyor, kullanıcı bunu
"bedava" diye okumamalı. Aynısı `CpuAccountingUnreliable` için de geçerli.
CHECK: iki bulgu içeren sahte sonuç verilir, üretilen metinde ikisinin de satırı vardır.

**K5 — Panel kaydedici hakkında iddia kurmuyor.**
Üretilen hiçbir satır OBS/ShadowPlay/Game Bar'ın ne yaptığını söylemez, "kaydın
FPS düşürüyor/düşürmüyor" demez. Panelde **kalıcı** bir sınır cümlesi durur:
ölçülen şey bu makinede kodlamanın maliyetidir, kaydedicinin davranışı değil.
CHECK: tüm bulgu kodları için üretilen metinlerde yasak kalıplar aranır ve bulunmaz;
sınır cümlesi her sonuçta bulunur.

**K6 — Sayı ekranda, ölçümdeki haliyle.**
`SoftwareRealtimeCores` "cores", `HardwarePipelineRealtimeFactor` "× realtime" diye
görünür. Hız oranı **çekirdek diye etiketlenemez** — T63 tur 1'de tam bu yapıldı ve
denetim kritik yazdı. Birimler karışırsa ölçü kırmızıya düşsün.
CHECK: iki alanın birimi ölçüde pinlenir.

**K7 — Sayfa taşmıyor.**
Panel açıkken Gelişmiş sekmesi kendi kaydırıcısında kalır, pencereyi büyütmez.
`WindowLayoutTests`'teki tavan ölçüsüyle aynı yaklaşım.
CHECK: içerik yüksekliği görüş alanı + 0,5 içinde ya da kendi kaydırıcısında.

**K8 — Süit yeşil.**
CHECK: `dotnet build -c Release` 0 hata 0 uyarı; sade `dotnet test -c Release` tamamı
yeşil, ana ağacın taban sayısından düşük değil.

## Kayıt noktası

Her turda: dal adı, commit özeti, hangi kriterin hangi ölçüyle karşılandığı.
Ham çıktı `.calisma/t64/` altına. İş bitince oradan rapora giren sayı `docs/`e taşınır,
gerisi silinir.

## Çıktı

Dalını `worktree` yalıtımında aç. Teslimde: commit hash'i, `dotnet test` ham sayıları
(geçti/kaldı/atlandı/toplam), her kabul kriterinin karşısına ölçü adı. Ekran görüntüsü
isteme — ekran kapısı kapalı, panel gözle görülmeden teslim edilir; bu yüzden ölçüler
gözün yerini tutacak kadar somut olmalı.

## T63 mühürlendi — devraldığın borç

T63 tur 4'te mühürlendi (main `7716491`, süit 751/0/16/767). Devraldığın **bir borç
var ve panelin tasarımını doğrudan bağlıyor**:

**`Impact` tek başına manşet yapılamaz.** Ölçüldü: makine meşgulken `Impact`
`HardwareOffload`'dan `SoftwareLightLoad`'a kayabiliyor — oysa o koşumda
`h264_nvenc` çalışıyor ve `SoftwareLightLoad`'ın kendi tanımı "donanım **yok**"
diyor. Sebebi `NotCpuBoundTolerance` yük altında düşünce `Evaluate`'in donanım
dalını atlaması.

Bulgu kümesi yanıltmıyor: aynı sonuç nesnesi `HardwarePathWorks` ve
`HardwareCodec=h264_nvenc` taşımaya devam ediyor. Yani **doğru bilgi `Findings`
içinde**, `Impact` ise sınırda yanlış cevap verebiliyor.

Panel bunu şöyle çözer: manşeti `Impact`ten değil **bulgulardan** kur. Donanım
kodlayıcısının varlığı `HardwarePathWorks`ten okunur, işlemciye bağlı olmadığı
`HardwareNotCpuBound`tan, yazılım maliyeti `SoftwareRealtimeCost`tan. `Impact`
gösterilecekse tek başına değil, dayandığı bulgularla birlikte gösterilir.

**K9 — panel `Impact` ile bulguları çelişir halde göstermiyor.**
`HardwarePathWorks` taşıyan ama `Impact` = `SoftwareLightLoad` olan sahte bir sonuç
verilir; üretilen metin "bu makinede donanım kodlayıcısı yok" **demez**.
CHECK: bu sahte sonuç için üretilen metin ölçüyle pinlenir.

## Ölçülmüş sayılar

Bunlar bu makinede ölçüldü. Ölçü yazarken sahte veri kuracaksın; gerçekçi olsun diye
buradan al. Yazılım `libx264 veryfast` gerçek zamanlı kayıt için boşta 0,53-0,60
çekirdek, 15 meşgul iş parçacığı altında 0,93-1,02 çekirdek. Donanım `h264_nvenc`
boru hattı gerçek zamanın ~16 katı hızda; **işlemci maliyeti ölçülmedi.** Eşik
(`HeavyLoadCores`) 1,0.

## Sayı yasağı — T63'ten devreden kural

Docstring'de ve kod açıklamasında **ölçüm sonucu bildiren sayı yasak.** Sayı yalnız
ham günlükte ve raporda yaşar. Kodun ne yaptığını tanımlayan sayı (sabitler, biçimler,
açıklayıcı örnek) serbest. Ayrım şu: birincisi yarın yanlış olabilir, ikincisi kodun
kendisi değişmedikçe olamaz.

Ham günlüğe **satır numarasıyla atıf yapma** — günlük eklemeli, her koşumda büyüyor,
bugün doğru olan atıf yarın başka şeyi gösterir. En fazla dosyanın adını göster.


## Kayıt noktası — tur 1

Dal: `agent/abe220bcf012ae690` (worktree). Ham günlük `.calisma/t64/olcum.txt`.

Yazılan dosyalar: `MainWindow.axaml` (üçüncü panel), `MainWindow.axaml.cs` (aç/kapa,
düğme, sonuç yazımı), `Performance/PerformanceReportText.cs` (yeni), `tests/
VidShrink.Tests/PerformanceCheckUiTests.cs` (yeni, 10 ölçüm). Owns dışına çıkılmadı.

Kriterlerin karşılığı:

- K1 `ThePanelSitsInTheAdvancedTabInTheSamePatternAsTheOthers`
- K2 `TheProbeRunsOnlyWhenTheButtonIsPressed`
- K3 `EveryFindingCodeHasItsOwnLine`, `TheMappingHasNoFallThrough`
- K4 `WhatWasNotMeasuredIsSaidOnScreen`
- K5 `NoLineMakesAClaimAboutTheCaptureTool`,
  `TheBoundarySentenceIsInEveryResultAndOnScreenBeforeAnyMeasurement`
- K6 `EachNumberCarriesTheUnitItWasMeasuredIn`
- K7 `TheOpenPanelStaysInsideItsOwnScroller`
- K8 `dotnet build -c Release` 0/0, `dotnet test -c Release` 761/0/16/777
- K9 `TheHeadlineFollowsTheFindingsNotTheImpactField`

## Rapor

- Ölçümde eksik bulunmadı; `src/VidShrink.Core/**` ve `src/VidShrink.Ffmpeg/**`
  okundu, değiştirilmedi.
- Yerleşim ölçümünde bir tuzak çıktı ve ölçümün içinde çözüldü:
  `WindowLayoutTests.LayOutAt` sekme değiştikten sonra yeni sekmenin içeriğini
  ölçmüyor (bounds 0x0 kalıyor). Bu, `WindowLayoutTests`'in sekme gezen ölçümünün
  ilk sekme dışında boşa dönüyor olabileceği anlamına geliyor — o dosya `owns`
  dışında, karar T0'ın.
- Ekrandaki metin `LanguageCatalog.Title` geçidinden geçtiği için her sözcük büyük
  harfle başlıyor. Kodlayıcı adı ve birim bundan zarar görüyordu ("libx264" ->
  "Libx264", "ms" -> "Ms"); sayı bloğu bu yüzden koşulardan kuruluyor ve geçide
  uğramıyor. Cümleler geçitten geçmeye devam ediyor.

---

## Kapanış — mühür notu

Denetim **tur 1'de geçti**, kritik yok. Dokuz kabul kriterinin dokuzu da kaynaktan
doğrulandı.

Doğrulama: `.calisma/dogrulama` ağacında bağımsız koşum, `1739e01` —
`dotnet build -c Release` 0 hata 0 uyarı, sade `dotnet test -c Release`
**761 geçti / 0 kaldı / 16 atlandı / 777 toplam**. Taban 751'di, +10 ölçü.

`owns` envanteri **kapalı**: değişen dosyalar tam olarak sahiplenilen dördü, artı
ajanın kendi hafızası ve sözleşme. `src/VidShrink.Core/**`, `src/VidShrink.Ffmpeg/**`,
`LanguageCatalog.cs` ve `WindowLayoutTests.cs` dokunulmamış.

### K9 gerçekten çalışıyor

T63'ten devreden borç panelde kapandı. `PerformanceReportText` manşeti dört dalda da
`Findings`'ten okuyor; `result.Impact` dosyanın hiçbir yerinde okunmuyor ve panel
`Impact`i hiç göstermiyor. Ölçü borcu yeniden üretiyor: hwSingle 500 / hwFree 360 =
1,39 > `NotCpuBoundTolerance` 1,25 → `Impact = SoftwareLightLoad` ama
`HardwarePathWorks` duruyor; manşet donanım dalına düşüyor, "donanım yok" demiyor.

### Borçlar

**Kullanıcının göreceği tek kusur:** `BudgetExhausted` satırı ekranda
"(21000 Ms Of 20000 Ms)" diye çıkıyor. Sayı bloğu büyük harf geçidinden `Run`'larla
kaçırıldı ama cümleler hâlâ `Localize`'dan geçiyor, `ms` → `Ms` oluyor — SI'da
megasaniye. Yalnız bütçe dolduğunda görünür. **T65'e taşındı.**

**Gelişmiş sekmesinde kırpılma koruması ölü.** `WindowLayoutTests.LayOutAt` sekme
seçimi değiştikten sonra yeni sekmenin içeriğini ölçmüyor, `Bounds` 0×0 kalıyor ve
`block.Bounds.Width > 0` süzgeci hepsini eliyor. Yani `NoTextIsClippedAtTheSmallestSize`
ilk sekme dışında boşa dönüyor. Yeni panel en küçük boyutta hiç ölçülmüyor.
Ajan bunu kendi ölçüsünde ikinci bir geçişle aştı ama dosya `owns` dışıydı,
doğru davranıp dokunmadı. **T65'e taşındı.**

Öteki borçlar: K5'in yasak kalıp listesi dar (bugünkü 12 satır geçmiyor ama yarınki
satıra karşı zayıf); K2 ölçüsünün kurucu kör noktası var (canlı kusur yok);
K7'nin yatay dalı `HorizontalScrollBarVisibility="Disabled"` yüzünden yanlışlanamaz,
gerçekte pinlediği şey iç içe ikinci kaydırıcının olmaması; "yirmi saniye" cümlesi
`PerformanceProbe.BudgetMs`'e pinli değil; `Assert.DoesNotContain("_ =>")` düz dizge
taraması, `_=>` yazımı kaçar; K9 çelişkinin tek biçimini ölçüyor.

### Kullanıcıya

Panel Gelişmiş sekmesinde, `CommandPanel` ve `AiPanel` ile aynı kalıpta. Kendiliğinden
hiç koşmuyor — `PerformanceProbe`'un tek çağrısı düğmenin altında. Kullanıcı paneli
açtığında, düğmeye basmadan, manşeti ve sınır cümlesini görüyor: VidShrink kayıt
yapmaz, ölçtüğü şey bu makinede kodlamanın maliyetidir.
