---
result: passed
id: T163
title: Arayuz — gelismis ayarlar bolumu ve onizleme/nedenler yer paylasimi
role: builder
lang: tr
depends: [T162, T165, T161]
blocked-by: [T165]
status: done
round: 2
verify:
  - dotnet build -c Release --no-incremental
  - dotnet test -c Release --filter "FullyQualifiedName~AdvancedPanelTests"
owns:
  - src/VidShrink.App/MainWindow.axaml
  - src/VidShrink.App/MainWindow.axaml.cs
  - src/VidShrink.App/Themes/Theme.axaml
  - src/VidShrink.App/Themes/Controls.axaml
  - src/VidShrink.App/Locales/en/main.json
  - src/VidShrink.App/Locales/tr/main.json
  - tests/VidShrink.Tests/AdvancedPanelTests.cs
  - docs/olcumler/arayuz-gelismis-ayarlar.md
  - tests/VidShrink.Tests/WindowLayoutTests.cs
side_effects: []
---

## Amac

Kullanicinin iki cumlesi:

1. *"arayuzu eksiksiz hallet ... suanki halindeki kadar basit ayarlar degil daha kompleks
   kullanici kodegine kadar secebilmeli varsayilan oto mod tabi ama yinede bilincli bir
   kullanici ne isterse onun ciktisini alir"*
2. *"onizleme bolumu fazla buyuk onun alanini asagidaki ozellikleri ve nedenlerini sunan
   pencere alsin biraz"*

T162 ve T165 motor tarafini acti. Bu sozlesme o sozlesmeyi arayuze cikarir ve orta
sutunun yer paylasimini duzeltir.

## Baglam

Danisma turu: `docs/danisma/004-arayuz-yonu-gorusu.md`. Kararlari seni baglar.

Bugunku orta sutun (`MainWindow.axaml:478`):

```xml
<Grid Grid.Column="1" RowDefinitions="*,Auto" RowSpacing="...">
  <playback:ComparisonPanel Grid.Row="0" x:Name="Preview" VerticalAlignment="Stretch"/>
  <Border x:Name="PlanPanel" Grid.Row="1"
          MinHeight="{StaticResource PlanPanelMinHeight}"     <- Theme.axaml:385 = 320
          MaxHeight="{StaticResource PlanPanelMaxHeight}">     <- Theme.axaml:386 = 512
```

Dosyanin kendi yorumu bugunku paylasimin **bilerek** boyle oldugunu soyluyor:

> T54/K4: artan yer onizlemeye gider. Onizleme esneyen satirdadir, plan paneli Auto
> satirda. Eski duzende esneyen satir MaxHeight'li plan panelindeydi ve artan yer kimseye
> gitmiyordu.

Yani kullanicinin geri istedigi sey gecmiste bir sozlesmenin bilerek verdigi karar.
**Ikisini birden gecerli tutan cozum ayiricidir** — varsayilan konum bir olcume dayanir,
kullanicinin cektigi yer onun karari olur ve kalici olur.

Kalici ayar mekanizmasi zaten var: `MainWindow.axaml.cs:791 SaveSettings()` ve
`:173-187` `Watch(...)` baglantilari. Ayirici konumu **oraya** gider — `Theme.axaml`e
degil; o bir olcu belirteci degil kullanici verisidir.

Nedenler yapisal olarak zaten var: `ReasonCode` (16 kalem) ve `AdviceCode` (21 kalem),
toplam 37. Panelde bugun `PlanFacts` izgarasi, bir ayirici, `PlanReasonsHead` basligi ve
liste var (`MainWindow.axaml:482-510`).

Pencere: `Width="1560" Height="1060" MinWidth="1040" MinHeight="720"`, sayfa `*,*,*`.

## Kabul kriteri

**K1 — Once olc: nedenler bugun sigiyor mu?** Panel buyutmeden once **olcum yap**: en cok
neden ureten gercek bir plan kur ve `PlanBody`nin istedigi yuksekligi olc. 512 px'e
siginiyor mu, kac piksel tasiyor, liste kendi icinde kaydiriliyor mu, satir yuksekligi ne?
Kullanicinin sikayeti panelin kucuklugu degil **listenin sigmamasi** olabilir.
CHECK: en cok neden ureten planin kaynak/hedef bilesimi, uretilen neden sayisi, olculen
istenen yukseklik ve bugunku tavan, ham cikti.

**K2 — Varsayilan konum olcuden gelir, damaktan degil.** Ayiricinin varsayilan konumu
K1'in olcusune dayanir: nedenler listesinin gercekten istedigi yukseklik ne ise varsayilan
o olur, `PlanPanelMaxHeight` gerekiyorsa o olcuye gore degisir. **Sayiyi uydurma** — bu
depoda renk ve olcu uydurmak yasak ve `teknesyum-ui` bu makinede kurulu degil. Uydurman
gerektigini dusunuyorsan **yapma, bildir.**
CHECK: varsayilan konumun hangi olcuden ciktigi, tek satirda; degistirilen belirtec adi ve
eski/yeni degeri.

**K3 — Ayirici calisiyor ve konumu kaliyor.** Kullanici onizleme ile plan paneli
arasindaki ayiriciyi surukleyebilir; konum `MinHeight`/`MaxHeight` sinirlari icinde kalir;
uygulama kapanip acilinca ayni yerde durur. Sabit oran **degil**, icerige gore buyume
**hic degil** — ikincisi her planda zipladigi icin onizlemeyi titretir, T54/K4 karari da
bu yuzden alinmisti.
CHECK: uc olcu — surukleme sinirlarda duruyor, konum kaydediliyor, yeniden acilista geri
yukleniyor.

**K4 — Gelismis ayarlar bolumu.** T165'in actigi sekiz kalem
(`EncodeMode`, CRF, preset/hiz, ses kbps, ses kanali, cozunurluk tabani, kare hizi tabani,
kodlayici yolu) ve T162'nin kodek kilidi arayuzden secilebilir. Bolum **katlanir** ve
varsayilan kapali; kapaliyken bugunku sayfa gorunumu degismez. Her kalem "Otomatik"
secenekli acilir ve motorun o an sectigi degeri yaninda gosterir.
CHECK: dokuz kalemin her biri icin — kontrolun adi, varsayilani, ve motorun sectigi degeri
gosterdigini kanitlayan olcu. Dokuzu da olacak; eksik birakiyorsan **yapma, bildir.**

**K5 — CRF sabitlenince hedef boyut alani ne diyor.** T165 CRF sabitlendiginde hedefi
tahmine ceviriyor. Arayuz bunu **tek satirda** soyler ve hedef alani artik zorlamadigini
gosterir. Kullanici hangisinin kazandigini tahmin etmek zorunda kalmaz.
CHECK: CRF sabitlenmis haldeki hedef alaninin gorunumu ve metni, iki dilde.

**K6 — Iki dil eksiksiz.** Eklenen her metin `Locales/en/main.json` ve
`Locales/tr/main.json` icinde **ikisinde de** var. Tek dilde kalan anahtar yok.
CHECK: eklenen anahtarlarin listesi ve iki dosyadaki sayilarin esitligi, ham cikti.

**K7 — Renk ve olcu uydurulmadi.** Yeni her renk ve olcu `Themes/Theme.axaml`
belirteclerinden gelir. Yeni bir belirtec gerekiyorsa **degerini uydurma**; mevcut
belirteclerden turet ya da **bildir.**
CHECK: eklenen/kullanilan belirteclerin listesi, her birinin kaynagi.

**K8 — Mutasyon.** Uc mutasyon, her birinden once
`dotnet build -c Release --no-incremental`; **`--no-build` yasak**:
(a) Ayirici konumunun kaydini kaldir. Bir olcu **olmeli**.
(b) Bir gelismis kalemi arayuzden motora baglama — secilse de plana gecmesin.
    Bir olcu **olmeli**.
(c) Bir metni tek dilde birak. Bir olcu **olmeli**.
CHECK: uc satirlik izgara, kirilan olcu adlari, ham cikti.

**K9 — Kol sayisi.** `dotnet test -c Release --filter "FullyQualifiedName~AdvancedPanelTests" --list-tests`.
Sifir bulan kol varsa teslim etme, bildir — bu depoda sifir eslesen filtre kolu exit 0 ile
dort kez sessizce gecti.
CHECK: kolun buldugu test sayisi.

## Sinirlar

- `owns` disina yazma. `src/VidShrink.App/Playback/**` T161'in alani —
  **okunacak, yazilmayacak.** Onizleme panelinin ic davranisina dokunma; bu sozlesme
  ona ayrilan **yeri** degistirir, kendisini degil.
- `src/VidShrink.Core/**` T162 ve T165'in alani; **okunacak, yazilmayacak.** Motorda eksik
  bir sey varsa **kendin ekleme, bildir** — T0 sozlesme acar.
- T165 kapanmadan baslama.
- Renk ve olcu yalniz `Themes/Theme.axaml` belirteclerinden. **Uydurma yasak.**
- Kod yorumu yazma. `MainWindow.axaml:470` civarindaki T43/K1 ve T54/K4 yorumlari bu
  degisiklikten sonra eskiyecek — **guncelle.**
- "Dokuz", "hepsi", "hicbiri" diyen her cumlenin altindaki listeyi **kendin say ve ham
  ciktiyi goster.** Bu depoda tablo dogru cikip onu ozetleyen cumle 26 kez yanlis cikti.
- **Filtresiz `dotnet test` kosturma** — tam suit 25 dakika suruyor.
- Pencere acan olcum icin ekran kapisi gerekir; kapi kapaliysa **acmaya calisma, bildir.**
- Gecici her sey `.calisma/T163/` altina. `.calisma/kaynak/`: **oku, yazma, silme.**
- `.claude/relay/**` T0'in alani.

## Kayit noktasi

Dal: `T163-arayuz-gelismis`, T165 birlestikten sonra guncel `main`den ac, `origin`e it.
**`main`e birlestirme.**

## Cikti

`docs/olcumler/arayuz-gelismis-ayarlar.md` — K1'in olcusu, K2'nin turetmesi, K3'un uc
olcusu, K4'un dokuz satiri, K5'in gorunumu, K6'nin anahtar sayimi, K7'nin belirtec
listesi, K8'in mutasyon izgarasi, K9'un sayisi.

## Kayit

Isledikce buraya yaz, sonda degil.

---

# Tur 2 — tur 1 adim tavanina carpti, is duruyor

Tur 1 **calisan kod teslim etti** ama adim tavanina (150) carpip kanitlari
tamamlayamadi. Isi kaybolmadi: dal `T163-arayuz-gelismis`, commit `64125dc`,
`origin`e itildi.

## Tur 1'in bitirdikleri

- Dokuz gelismis ayar kontrolu, katlanir bolum, `GridSplitter` ve ucuncu satir.
- On dokuz `ReasonCode.Manual*` kolu — **T165'in biraktigi borc kapandi**.
- `TxtTargetCrfLockedNotice` (K5: CRF sabitken hedef alani).
- Ayirici konumu kalici (`layout.json`; `Core`a dokunulmadi).
- Iki dile on dokuz `manual-reason` + `main.advanced.*` + `main.target.crf-locked` +
  `main.plan.splitter` anahtarlari.
- `PlanSplitter` ControlTheme — **yalniz var olan** `SpaceMd` / `NeonBlueBorder`
  belirtecleri kullanildi, yeni olcu uydurulmadi.
- `tests/VidShrink.Tests/AdvancedPanelTests.cs`, 21 kol, 21/21 yesil,
  `--list-tests` 21 (sifir-kol riski yok).
- Build 0 hata 0 uyari.

## Tur 1'in K1 olcusu — ve onun soyledigi sey

**Olculdu:** en cok gerekce ureten gercek bilesim (kilit kodek + preset + CRF + kip +
ses + cozunurluk + fps + yol) **11 gerekce** uretiyor; `PlanScroll.Extent.Height`
tasarim genisliginde **232 px**.

Bugunku taban 320, tavan 512. **Yani panel zaten yetiyor** ve K2 geregi
`PlanPanelMinHeight`/`MaxHeight`e **dokunulmadi**.

Bu, danisma turunun (`docs/danisma/004`) tam da uyardigi sonuc: kullanicinin sikayeti
panelin kucuklugu degil olabilir. **Bu bulgu belgeye yazilmali** — tur 1 yazamadan durdu.

## Tur 2'de bitecekler

**P1 — Olcum belgesi.** `docs/olcumler/arayuz-gelismis-ayarlar.md` hic yazilmadi.
K1'in 11 gerekce / 232 px olcusu, K2'nin "dokunulmadi" karari ve gerekcesi, K3'un uc
olcusu, K4'un dokuz kalemi, K5, K6, K7, K8 oraya girecek.

**P2 — K6 sayimi.** Iki dile anahtarlar elle eklendi ama **sayilmadi**. Eklenen
anahtarlarin listesini ve iki dosyadaki sayilarin esitligini ham ciktiyla goster.

**P3 — K8 mutasyonlari hic yapilmadi.** Uc mutasyon, her birinden once
`dotnet build -c Release --no-incremental`:
(a) ayirici konumunun kaydini kaldir — bir olcu **olmeli**;
(b) bir gelismis kalemi arayuzden motora baglama — bir olcu **olmeli**;
(c) bir metni tek dilde birak — bir olcu **olmeli**.

**P4 — `WindowLayoutTests` etkisi DOGRULANMADI ve bu bir risk.** `PreviewPlanGrid`e
ucuncu satir eklendi ve `Grid.Row` 1'den 2'ye kaydi. Tur 1 bu dosyanin plan paneli
olculerini (`ThePlanPanelIsSizedByItsContentBetweenItsFloorAndItsCeiling`,
`TheCeilingStopsALongPlanAndHandsTheOverflowToItsOwnScroller`) bozup bozmadigini
**kosturamadi**. `owns`a `tests/VidShrink.Tests/WindowLayoutTests.cs` eklendi — **once
kostur**, kirilmissa duzelt, kirilmamissa yesil oldugunu goster.

**P5 — `OluUyeTests` beklenen kirilma.** On dokuz `Manual*` artik "sifir tuketicili"
degil; pim **fazla satir** verip kirmiziya donecek. Bu sozlesmenin **kendi metni bunu
bekliyordu**. Dosya `owns` **disinda** — kirildigini dogrula ve **bildir**, duzeltme,
T0 ayri sozlesme acar.

## Sinirlar

- Tur 1'in kodunu yeniden yazma; uzerine kur.
- **Renk ve olcu uydurma.** Tur 1 bunu dogru yapti, sen de yap.
- `.claude/relay/**` T0'in alani — `## Kayit` metnini raporunda ver.
- **Filtreli kosumlar on planda kosar.** Filtresiz `dotnet test` kosturma.
- Rapora giren ham ciktiyi `.calisma/` altinda birakma.

---

# Tur 3 — K4 karsilanmadi ve bir olcu gevsetildi

Tur 2 **cok is bitirdi** ve iki seyi durustce bildirdi. Sunlar **kapandi, yeniden
acmayacaksin**: olcum belgesi yazildi, K6 sayimi yapildi (en 40 / tr 40, kumeler birebir),
K8'in uc mutasyonu kosuldu ve her biri kendi kolunu dusurdu, P4 riski olculdu ve
kapatildi, P5 beklendigi gibi kirildi ve **duzeltilmedi** (dogru davranis).

Iki sey kaldi ve **ikisi de kullanicinin asil istegine dokunuyor.**

## Q1 — K4 karsilanmadi: kapali bolum sayfayi buyutuyor

Sozlesme K4: *"Bolum **katlanir** ve varsayilan kapali; **kapaliyken bugunku sayfa
gorunumu degismez.**"*

Tur 2'nin kendi olcusu bunun **saglanmadigini** gosterdi
(`docs/olcumler/arayuz-gelismis-ayarlar.md:61-68`): bolumun her zaman gorunen baslik
satiri (baslik + katlama dugmesi) **tek basina sayfayi ~106-115 px uzatti**.

`TheAdvancedSectionIsCollapsedByDefault` bunu hic olcmuyor — yalniz
`AdvancedBody.IsVisible == false` bakiyor. Yani kriterin yarisi test edilmemis, ve
test edilen yarisi da yanlis olani.

**Bu kullanicinin istediginin tersi.** Kullanici *"onizleme bolumu fazla buyuk"* dedi;
sayfa 106 px **uzadi**.

Kapat: kapali bolumun sayfa yuksekligine katkisi **sifir** olacak, ya da neden sifir
olamayacagini olcuyle goster ve K4'u o olcuye gore yeniden yaz. Ve **kriterin iki
yarisini da olcen** bir kol yaz: kapali mi, **ve** kapaliyken sayfa yuksekligi
degismemis mi?

## Q2 — Gevsetilen tolerans geri gozden gecirilecek

`ThePageScrollsAtMostDownAtTheDesignSize(loaded: true)`: tasarim boyutunda dolu sayfa
artik **+106 px dikey tasiyor**, ve tur 2 bos sayfa icin zaten kabul edilmis toleransi
**dolu hale genisletti** (`docs/...:215-218`).

Pin'leri yeniden temellendirmek mesru — yerlesim gercekten degisti. Ama **bir sayfanin
tasarim boyutunda kaydirma gerektirmesi** kabul edilmis bir davranis degildi; tolerans
genisletilerek gecirildi.

Q1 kapaninca bu tasma buyuk olcude gitmeli. Gitmiyorsa: toleransi geri dar haline al ve
tasmanin nereden geldigini olcuyle yaz. **Iddiayi zayiflatarak gecirme.**

## Sinirlar

- Tur 2'nin isini yeniden yazma; uzerine kur. Dal `T163-arayuz-gelismis` (`9f740f1`).
- **Renk ve olcu uydurma.** Yeni bir sayi gerekiyorsa **yapma, bildir.**
- `OluUyeTests` beklenen kirilmasi `owns` disinda kaliyor — dokunma, T0 ayri sozlesme
  acacak.
- **Filtreli kosumlar on planda kosar.** Filtresiz `dotnet test` kosturma.
- `.claude/relay/**` T0'in alani — `## Kayit` metnini raporunda ver.

## Kayit

- Tur 3 dali: `T163-arayuz-gelismis`, commit `9580bb5`, `origin`e itildi (tur 2'nin
  `9f740f1`i uzerine).
- **Q1 kapandi. Fark iki kaynakti:** (a) bolumun kendi `Border` paneli +103 px,
  (b) K5 uyarisinin actigi altinci izgara satiri `RowSpacing` yuzunden +12 px.
  Katlama kolu hedef panelinin **var olan** baslik satirina tasindi ve `TargetMinSize`
  ile o satirin boyuna sabitlendi; uyari satiri yongalarla ayni izgara satirini
  paylasiyor.
  Olculen (dolu sayfa, tasarim boyutu):

  | olcu | 8da585f (T163 oncesi) | tur 2 | tur 3 |
  |---|---|---|---|
  | sol sutun | 940 | 1043 | **940** |
  | hedef paneli | 657 | 669 | **657** |
  | PageShrink dikey tasma | 0 | +106 | **0** |

  Sol sutun T163 oncesiyle **birebir ayni**. Kapali bolumun bedeli 0 px, acik 1822 px.
- Yeni kol `TheCollapsedAdvancedSectionCostsThePageNoHeight`, eski
  `TheAdvancedSectionIsCollapsedByDefault`in yerine gecti. **Kriterin iki yarisini da**
  olcuyor (kapali mi + kapaliyken bedeli sifir mi) ve ucuncu bir olcu bos esitlige karsi
  duruyor. O ucuncu olcu **gercekten kurtardi**: ilk yazimda `LayOutAt` gorunurluk
  degisikliginden sonra yeniden Measure kosturmuyordu, uc sayi da 973 cikiyordu ve
  "fark 0" hicbir sey soylemiyordu. Duzeltme `Relayout(...)` ile geldi.
- Mutasyonlar: dugmeden `Width`/`Height`/`Padding` sabitlemesi kalkinca 940 -> 951 ve
  yeni kol dusuyor; K5 uyarisi yeniden kendi satirina alininca dolu sayfa +3 px tasiyor
  ve `ThePageScrollsAtMostDownAtTheDesignSize(loaded: True)` dusuyor.
- **Q2 kapandi, gevsetilerek gecirilmedi.** `ThePageScrollsAtMostDownAtTheDesignSize`
  eski kati haline dondu (dolu sayfa hic kaymaz); `ThePageContentStaysAtItsPinnedHeight`
  dort pini ve `ThePageStopsScrollingAtThisHeight` iki pini T163 oncesi degerlerine geri
  alindi. Mesru kalan tek degisiklik `TheThreeColumnsStartAtTheSameTop` satir sayisi 3 —
  `GridSplitter` gercek bir satir.
- Yeni renk/olcu **uydurulmadi**: yalniz `TargetMinSize` ve `FontSizeSm` belirtecleri ile
  `Padding="0"`.
- `OluUyeTests` beklenen kirilmasi `owns` disinda, dokunulmadi.
- Verify: build 0/0; `AdvancedPanelTests` 21/21 (`--list-tests` 21);
  `WindowLayoutTests` 40/40.

---

# Tur 4 — KUSUR TASINDI: bolum calisan uygulamada acilamiyor

Denetim tur 3'un **olculerini** dogruladi ve sunlar **kapali, yeniden acmayacaksin**:

- Q1'in yukseklik olcusu **gercek**: `SettingsColumnHeight` sol sutunu `PageShrink`
  icerigi uzerinden okuyor, `Relayout` tum torunlarda `InvalidateMeasure()` cagiriyor,
  ucuncu olcu bos esitlige karsi gercek bir bekci. Kol `TargetPanel`in var olan baslik
  satirina girmis, K5 uyarisi yeni satir acmiyor.
- **Q2 geri alma gercek:** `git diff 8da585f 9580bb5 -- WindowLayoutTests.cs` tek islevsel
  degisiklik `Assert.Equal(2, rows) -> Assert.Equal(3, rows)`. Uc pin ailesi **birebir
  eski degerlerinde**; tolerans gevsetmesi geri alinmis, baska yere **kaydirilmamis**.
- **Uydurma yok:** `Theme.axaml` diff'te **hic yok**. XAML'de ciplak sayi yalniz
  `Padding="0"`.
- K1 anlatimi dogru (232 px, taban 320 / tavan 512, belirteclere dokunulmadi).
- **On dokuz `Manual*` kolu tam:** enum 19 uye, switch 19 kol, iki kume `diff` ile
  birebir esit; 19 anahtar iki dilde de var. **T165'in borcu kapandi.**
- K6: en 348 / tr 348, kumeler birebir ayni.
- `owns` disina yazim yok; `OluUyeTests` dokunulmamis (dogru davranis).

Uc bulgu var ve **birincisi urunu kiriyor.**

## R1 — Katlama dugmesi bagli degil

`src/VidShrink.App/MainWindow.axaml:299-313`, `BtnAdvancedToggle`da **`Click` yok.**

Tur 2'de vardi — `git show 9f740f1:src/VidShrink.App/MainWindow.axaml:486`:

```xml
Click="OnToggleAdvanced"/>
```

Tur 3 dugmeyi hedef panelinin baslik satirina **tasirken olay baglantisini birlikte
tasimadi**. `grep -rn "OnToggleAdvanced" src/ tests/` yalniz tanimi buluyor
(`MainWindow.axaml.cs:951`); `.Click +=` ile de baglanmiyor.

**Sonuc: calisan uygulamada `AdvancedBody.IsVisible` sonsuza dek `False`. Dokuz gelismis
kontrolun hicbirine kullanici erisemiyor.**

K4'un "bolum katlanir" yarisi karsilanmiyor. Q1'in "kapali bedel 0 px" kazanci, bolumun
**hic acilamamasi** pahasina elde edilmis.

Bu bu deponun olculmus kusur sinifi: olcuyu duzeltirken acilan yeni giris noktasi kusuru
kapatmaz, **tasir**. Bu sozlesmede ikinci kez oluyor.

## R2 — Yirmi bir kolun hicbiri R1'i goremez

`tests/VidShrink.Tests/AdvancedPanelTests.cs:369` genisletmeyi `window.ExpandAdvanced()`
**test kancasiyla** yapiyor, dugmeye tiklayarak degil. `MeasureMaxReasonLayout` (`:130`)
da ayni kancayi kullaniyor.

Yani dugme tumden silinse bile **21/21 yesil kalir.** R1'i duzeltmek yetmez; **R2
kapatilmadan ayni kusur bir sonraki turda geri gelir.**

Genisletme adimi **dugmeye tiklamali**. En az bir kol bunu yapsin.

## R3 — `TheCeilingFitsTheMostReasonProducingContent` bos geciyor

`AdvancedPanelTests.cs:176-182`:

```csharp
var layout = MeasureMaxReasonLayout();
Xunit.Assert.True(layout.Ceiling >= layout.Floor, ...);
```

Kolun adi ve K2 iddiasi *"tavan, olculen icerige siger"* diyor; assert ise **olculen
`layout.PlanBodyHeight`e hic bakmiyor**, yalniz iki belirteci (512 >= 320) kiyasliyor.
Pahali yerlesim olcusu kurulup **atiliyor**.

Assert olculen yuksekligi tavanla karsilastirsin.

## Tur 4 kabul kriteri

R1, R2, R3'un **ucu de** kapanacak. R2 icin: en az bir kol dugmeye tiklayarak acsin ve
o kol, `Click` kaldirildiginda **dussun** — mutasyonla goster.

Baska hicbir seye dokunma; olculer ve pinler dogru.

- Tur 4 dali: `T163-arayuz-gelismis`, commit `a329db6`, `origin`e itildi (tur 3'un
  `9580bb5`i uzerine).
- **R1 kapandi.** `MainWindow.axaml:302`ye `Click="OnToggleAdvanced"` geri kondu.
  Tur 3 dugmeyi hedef panelinin baslik satirina tasirken olay baglantisini birakmisti;
  calisan uygulamada `AdvancedBody.IsVisible` sonsuza dek `False` idi ve dokuz gelismis
  kontrole hic erisilemiyordu. Duzeltme tek satir; yeni renk/olcu/belirtec yok.
- **R2 kapandi ve asil ders burasi.** Yeni yardimci `ClickAdvancedToggle(window)` test
  kancasini hic kullanmiyor, gercek `Button.ClickEvent` yukseltiyor. Yeni kol
  `TheAdvancedSectionOpensAndClosesFromItsButton` (kapali -> tikla -> acik -> tikla ->
  kapali, yon oku dahil) ve `TheCollapsedAdvancedSectionCostsThePageNoHeight`in ucuncu
  olcusu artik dugmeye basiyor.
  Mutasyon (`Click` silindi, oncesinde `--no-incremental` build): **iki kol dusuyor**:

  ```
  TheAdvancedSectionOpensAndClosesFromItsButton
     R1: dugmeye basildi ama gelismis bolum acilmadi
  TheCollapsedAdvancedSectionCostsThePageNoHeight
     Olcu bosa dusuyor: bolum acilinca sol sutun buyumedi (kapali 940, acik 940)
  Basarisiz: 2, Basarili: 20, Toplam: 22
  ```

  **Tur 3'te bu mutasyonda sifir kol duserdi.**
- **R3 kapandi.** `TheCeilingFitsTheMostReasonProducingContent` artik
  `Ceiling >= PlanBodyHeight` kiyasliyor (512 >= 232) ve bos olcuye karsi
  `PlanBodyHeight > 0` bekcisi var.
- Baglanti yerindeyken olculenler: baslangic gorunur=False ok=▾; birinci tiklama
  gorunur=True ok=▴; ikinci tiklama gorunur=False ok=▾. Sol sutun kapali 940,
  yerlesimden cikarilmis 940, acik 1822. **Q1'in kazanci korundu ve bolum artik gercekten
  aciliyor.**
- Olculere ve pinlere dokunulmadi: `Theme.axaml`, `Controls.axaml`, iki dil dosyasi,
  `MainWindow.axaml.cs`, `WindowLayoutTests.cs` diff'te **yok**.
- `OluUyeTests` beklenen kirilmasi `owns` disinda, dokunulmadi.
- Verify: build 0/0; `AdvancedPanelTests` **22/22** (`--list-tests` 22);
  `WindowLayoutTests` 40/40.

---

# Tur 5 — ucuncu bos olcu: kirpmayi urun degil test yapiyor

Denetim R1, R2, R3'un **ucunu de** kapali buldu ve sunlari dogruladi — **yeniden
acmayacaksin**:

- R1: `MainWindow.axaml:302` `Click="OnToggleAdvanced"` var; govde
  (`MainWindow.axaml.cs:951-956`) `_advancedExpanded`i ceviriyor, `IsVisible`i esitliyor,
  oku `▴`/`▾` yapiyor. Tek abone XAML baglantisi.
- R2: `ClickAdvancedToggle` (`:95-99`) kancaya **hic dokunmuyor**, gercek
  `Button.ClickEvent` yukseltiyor. Kanca yalniz `:144`te K1 olcusunde kaldi.
  Denetci mutasyonu **kosturmadi** (kaynak dosyaya yazmak denetimi gecersiz kilar) ama
  statik kanitla **tam iki kolun** dusecegini gosterdi — yazicinin bildirdigiyle ayni.
- R3: `Assert.True(PlanBodyHeight > 0)` bekcisi ve `Ceiling >= PlanBodyHeight` var.
- Q1 kazanci korundu: 940 / 940 / 1822, ve ucuncu olcu artik **dugmeyle** aciyor.
- Son turda baska bir sey oynamadi: `Theme.axaml`, `Controls.axaml`, iki dil dosyasi,
  `MainWindow.axaml.cs`, `WindowLayoutTests.cs` tur 4 diff'inde **yok**.
- `owns` disina yazim yok; `OluUyeTests` dokunulmamis.
- `AdvancedPanelTests` 22/22, `WindowLayoutTests` 40/40, build 0/0.

Tek bir sey kaldi ve bu sozlesmede **ucuncu** bos olcu.

## S1 — K3'un birinci yarisi olculmemis

`tests/VidShrink.Tests/AdvancedPanelTests.cs:204-223`,
`DraggingTheSplitterStaysWithinFloorAndCeiling`:

```csharp
window.SetSplitterHeightForTest(Math.Clamp(requested, floor, ceiling));
Xunit.Assert.True(window.SplitterIsPixelForTest);
Xunit.Assert.InRange(window.SplitterHeightForTest, floor, ceiling);
```

**Kirpmayi testin kendisi yapiyor.** Urun tarafi (`MainWindow.axaml.cs:1778`) tek satir:
`PlanPanelRow.Height = new GridLength(pixels, GridUnitType.Pixel)` — **hicbir sinir
uygulamiyor**; `:1779` ayni degeri geri veriyor. `InRange` kendi hesapladigi sayiyi kendi
kiyasliyor.

Sinirlar `RowDefinition`dan tumden kalksa (`MainWindow.axaml:576-578`) `floor=0`,
`ceiling=+∞` olur, `Clamp` hicbir sey yapmaz, `InRange` yine gecer — **kol, dogruladigini
iddia ettigi seyin tumden silinmesini goremiyor.** Uc `InlineData` (1, 5000, 420) ayni
totolojiyi uc kez kosuyor.

Kodda **gercek** kirpma `MainWindow.axaml.cs:1763`, `RestoreSplitterSettings`:

```csharp
PlanPanelRow.Height = new GridLength(Math.Clamp(height, floor, ceiling), GridUnitType.Pixel);
```

Bunun **hic kolu yok**: `TheSplitterPositionSurvivesAReopen` (`:242`) diske zaten aralik
ici bir orta deger yaziyor, kirpma yolu hic calismiyor. `Math.Clamp(...)` `height` ile
degistirilse **22/22 yesil kalir**.

Ustelik olcum belgesi (`docs/olcumler/arayuz-gelismis-ayarlar.md:45`) bunu *"1, 5000, 420
istekleri hepsi kirpiliyor"* diye anlatiyor — **kirpan urun degil test.** Bu deponun
kayitli en pahali kusuru.

**Kapat:** kolu urunun gercek yoluna bagla — sinir disi bir `planPanelHeight` (1 ve 5000)
`layout.json`a yaz, `RestoreSplitterSettingsForTest()` cagir, donen degerin `[320, 512]`
icinde oldugunu **olc**. Mutasyonla `:1763`teki `Math.Clamp`i kaldirip kolun **dustugunu**
goster. Belgedeki cumleyi de olculen seye indir.

Surukleme yolunun (`:1778`) sinir uygulamamasi ayri bir soru: uygulamali mi, yoksa
`GridSplitter`in kendi `MinHeight`/`MaxHeight`i zaten mi tutuyor? **Olc ve yaz** —
tutuyorsa bunu kanitla, tutmuyorsa bildir.

## Tur 5 kabul kriteri

Yalniz S1. **Baska hicbir seye dokunma** — R1/R2/R3, Q1, pinler ve olculer dogru.

- Tur 5 dali: `T163-arayuz-gelismis`, commit `1ce1280`, `origin`e itildi (tur 4'un
  `a329db6`i uzerine).
- **S1 kapandi.** `DraggingTheSplitterStaysWithinFloorAndCeiling` **silindi** — kirpmayi
  testin kendisi yapiyordu. Yerine urunun **iki gercek yolunu** olcen iki kol:
  (a) `AnOutOfRangeSavedPositionIsClampedByTheProductOnRestore` sinir disi
  `planPanelHeight`i `layout.json`a yazip `RestoreSplitterSettings()` cagiriyor ve
  **donen** degeri olcuyor: 1 -> 320, 5000 -> 512, 420 -> 420. Testte `Math.Clamp` yok;
  beklenen deger `"floor"/"ceiling"/"same"` etiketiyle geliyor.
  (b) `TheGridRowHoldsAnOutOfRangeDragRequest` surukleme yolunu olcuyor.
- Mutasyonlar (her birinden once `--no-incremental` build):
  `:1763` `Math.Clamp(height, floor, ceiling)` -> `height`: **Basarisiz 2, Basarili 22**.
  `RowDefinition` `MinHeight`/`MaxHeight` silindi: **Basarisiz 6, Basarili 18**.
  **Eski kolda iki mutasyonda da sifir kol duserdi.**
- **Surukleme yolu sorusu cevaplandi: sinir tutuluyor, bildirilecek kusur yok.**
  `:1778` gercekten hicbir sinir uygulamiyor (5000 istegi satirin `Height`inde ham
  kaliyor) ama **yerlesim tutuyor**: satirin aldigi boy 320 / 512, `PlanPanel` 320 / 512.
  Tutan `RowDefinition.MinHeight/MaxHeight` — `PlanPanel` Border'inin kendi sinirlari
  mutasyonla silindiginde iki olcu de degismedi, Border'inki yedek.
  `RowDefinition.ActualHeight` kendinden onceki `RowSpacing`i tasiyor (ham 332 / 524);
  `SpaceMd` belirteci **okunup** dusuldu, sayi elle yazilmadi.
- Belge duzeltildi: K3'teki *"1, 5000, 420 istekleri hepsi kirpiliyor"* cumlesi
  kaldirildi; yerine yanlis oldugunun kaydi, iki yolun tablosu, hangi sinirin tuttugu ve
  mutasyon izgarasi kondu.
- Urun tarafina eklenen tek sey `SplitterRowActualHeightForTest` okuyucusu (1 satir).
  `MainWindow.axaml`, `Theme.axaml`, `Controls.axaml`, iki dil dosyasi ve
  `WindowLayoutTests.cs` diff'te **yok**. Yeni renk/olcu uydurulmadi.
- `OluUyeTests` beklenen kirilmasi `owns` disinda, dokunulmadi.
- Verify: build 0/0; `AdvancedPanelTests` **24/24** (`--list-tests` 24);
  `WindowLayoutTests` 40/40.

- Tur 6 dali: `T163-arayuz-gelismis`, commit `a5279c9`, `origin`e itildi (tur 5'in
  `1ce1280`i uzerine). Kapanis kapisinda duran manset bulgusu kapandi: K3'un surukleme
  yolu tablosuna `ham ActualHeight` ve `dusulen SpaceMd` sutunlari eklendi; cumledeki
  332/12/320 ve 524/12/512 sayilari artik tablonun kendi hucrelerinde.
  Baska hicbir seye dokunulmadi — kod, olcu, mutasyon izgarasi, K1/K2/K4 diff'te yok.
  Verify: manset temiz, build 0/0, AdvancedPanelTests 24/24.

## Closed - unmet (2026-09-05)

Is gecti ama muhur atilamadi: denetim kaydi 1ce1280'e baglandiktan sonra manset kapisi tek bir sayida dustu ve duzeltme zorunlu olarak yeni commit (a5279c9) uretti. Ayni tur icinde kayit yenilenemiyor, HEAD geri alinamiyor (o surumde manset dusuyor), round elle artirilamiyor. Iki bagimsiz denetim verdict passed / findings none dondu; T0 son commit'i kendi dogruladi (src ve tests diff'i bos, manset temiz, AdvancedPanelTests 24/24, WindowLayoutTests 40/40). Bu kapanis is icin degil, arac kilidi icin; hemen reopen ile tur 2'de temiz denetimle muhurlenecek.

Not sealed. Acceptance was not met; the work stays in the tree.
