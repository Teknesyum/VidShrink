---
id: T182
title: DecoderPipe arama dongusu asiliyor, ffmpeg cocugu oksuz kaliyor
status: done
role: builder
model: opus
round: 2
owns:
  - src/VidShrink.Ffmpeg/Playback/DecoderPipe.cs
  - tests/VidShrink.Tests/OynaticiBoruTests.cs
  - tests/VidShrink.Tests/OynaticiGirdiTests.cs
depends: []
---

## Amac

CI ve yerel koşum aynı sebeple kırmızı: test sunucusu kapanmıyor çünkü altında yaşayan bir
ffmpeg çocuğu var. Bu bir test yavaşlığı değil, üründe süreç sızıntısı.

## Kanit (uydurma degil, olculdu)

Yerel tam süit, `--blame-hang --blame-hang-timeout 3m`, `0266be9a`:

```
Basarili! - Basarisiz: 0, Basarili: 313, Atlanan: 5, Toplam: 318, Sure: 2 m 8 s
Etkin test calistirmasi iptal edildi. Nedeni: Test ana islemi kilitlendi
'Blame': Bu islem agacinin dokumu aliniyor (alttan).
'Blame': > 47464 - testhost.
'Blame':  +- 40352 - ffmpeg.
Kilitlenme olustugunda calisan test:
VidShrink.Tests.OynaticiGirdiTestsGercekBoru.OnHizliTikGercekBoruyaKarsiBirikirVeAramalarSinirdaKalir
```

Dump dosyalari: `.calisma/hang-full/ccb693ec-.../testhost_47464_*.dmp` ve `ffmpeg_40352_*.dmp`.

CI'da (kosum 34052378141) ayni sey kapanista oluyor:
`Failed: 0, Passed: 1785, Skipped: 20, Total: 1805, Duration: 21 m 40 s` sonrasi
`All tests finished running, Sequence file will not be generated.` + 10 dakika asilma.

Bu makinede su an hala oksuz bir `ffmpeg.exe` (PID 17436, ~2 GB) duruyor.

## Suphe (dogrula, korukorune kabul etme)

`DecoderPipe.SeekAsync` (`:202-269`) `while (true)` dongusu. Kacis yolu yalnizca
`MaxRestartAttemptsPerSeek = 8` (`:200`); mutlak zaman asimi yok.

`StopVideoOnly` (`:503-522`): `process.Kill(true)` cagriliyor ama `catch { }` ile yutuluyor,
`WaitForExit` yok, `Process` hic `Dispose` edilmiyor. Oldurme basarisiz olursa
`reader?.Join(2000)` 2 saniyede birakiyor ve **hem is parcacigi hem ffmpeg sureci sizyor**.
Sekiz yeniden baslatma × 10 arama = sekiz kat sizinti.

Once bunu **olc**: `pipe.ProcessesStarted` ile baslatilan, `tasklist` ile hayatta kalan
ffmpeg sayisini kars. Suphenin dogru olup olmadigini kanitla, sonra duzelt.

## Yapilacak

1. `SeekAsync`'e **mutlak zaman asimi** koy. Suresi uydurma degil olcumden gelsin:
   kac ms'de kare geliyor, en kotu durum ne. Asildiginda `RaiseFault` + `null`.
2. `StopVideoOnly` sureci gercekten oldursun: `Kill(true)` sonrasi sinirli
   `WaitForExit(<ms>)`, olmezse yeniden dene, `Process` `Dispose` edilsin.
   Oldurulemedigi durumu yut**ma** — `RaiseFault` ile bildir.
3. Ayni kalibi `_audioProcess` ve `PipeComparisonFrameSource` icin de kontrol et;
   ayni kusur oradaysa **soyle**, duzeltmeyi bu sozlesmede yapma (owns disinda).
4. Yerelde kirmizi olan `OynaticiBoruTests.cs:324`
   `Surec_disaridan_oldurulunce_boru_Faulted_yayar_ve_kendini_kurar` — CI'da yesil,
   burada kirmizi. Ayni kok mu, ayri mi? Olc ve raporla.

## Kabul kriteri

- CHECK: `dotnet test -c Release --filter "OynaticiBoruTests|OynaticiGirdiTests|PlaybackFrameSourceTests" --blame-hang --blame-hang-timeout 2m` yesil doner ve asilma satiri **icermez**.
- CHECK: Koşum bittikten sonra `tasklist /FI "IMAGENAME eq ffmpeg.exe"` cikitisinda oksuz surec **yok**.
- CHECK: `dotnet test -c Release --blame-hang --blame-hang-timeout 5m` (tam suit) yesil ve
  `Test Run Aborted` / `Test host process crashed` satiri yok.
- Sureci oldurulemez halde birakan yolu kapatan **mutasyon testi**: duzeltmeyi geri al,
  test kirmizi olsun. Sabit karsilastiran test degil, davranis olcen test.
- Yeni sabit (zaman asimi) icin kod yorumu yazma; sayinin nereden geldigini
  `docs/olcumler/` altina olcum dosyasi olarak yaz.

## Kayit noktasi

Her adimda commit at. Dal: `T182-boru-asilmasi`.

## Tur 2 — denetim KALDI dedi

Denetim GEÇTİ demedi. Tek kritik var, gerisi borç.

### KRİTİK — CHECK 1 yesil donmuyor

`tests/VidShrink.Tests/OynaticiBoruTests.cs:324`
`Assert.True(pipe.ProcessesStarted > startedBeforeCrash)` yaris kosulu tasiyor.

Denetcinin olcumu: `7743974d` uzerinde **15 kosumun 4'u kirmizi**. Ayni satir tabanda
(`ed42c2db`) da **6 kosumun 2'sinde** kirmizi — yani gerileme degil, tasinan bir
kararsizlik. Ama dosya `owns` icinde ve sozlesmenin Yapilacak-4 maddesi bu testi
adiyla veriyordu; CHECK 1 "yesil doner" diyor, donmuyor.

Bir satir oncesi `Assert.NotNull(frame)` geciyor: urun dogru cikti veriyor, kare geliyor.
Test kareyi onbellekten aldigi icin "yeniden baslatti" iddiasi dusuyor.

**Yapilacak:** yarisi kapat. Ya oldurmeden once hedef karenin onbellekte olmadigini
garantile, ya da iddiayi surec sayacina degil davranisa bagla. Sayaci gizlemek degil,
olctugu seyi dogru olcmek.

**CHECK:** ayni filtre **10 kez arka arkaya** kosulacak ve 10'u da yesil donecek:
`for ($i=1; $i -le 10; $i++) { dotnet test -c Release --filter "OynaticiBoruTests" }`
Cikitilari raporuna koy.

### Borclar (muhur notuna, bu turda zorunlu degil)

- Olcum duzenegi silinmis (`docs/olcumler/boru-kapanis-zaman-asimlari.md:4`):
  120x3 arama ve 20 surec oldurme tablolari yeniden uretilemiyor. AGENTS.md duzenegin
  `tools/`a tasinmasini istiyor. **Bu turda duzeltilebilirse duzelt.**
- `KillTree` donusu uc cagri yerinde yok sayiliyor: `DecoderPipe.cs:197, :407, :513`.
- `SeekAbsoluteTimeout = 3000` hicbir testle pimli degil.
- `Oldurulemeyen_surec_icin_KillTree_basarisiz_bildirir` Windows disinda sessizce bos gecer.
- `PipeComparisonFrameSource.cs:441-442` sinirsiz `WaitForExitAsync` + yutulan oldurme
  hatasi — owns disi, ayri sozlesme.

### Denetcinin duzelttigi bir sayi

Yapicinin "ffmpeg 4730 bayt yaziyor" rakami **sabit degil**: denetci ayni komutu farkli
yollarda kosturunca 4747 / 4787 / 4794 olcmus. Cikti yolu ffmpeg'in stderr'inde
yankilaniyor. Maddi iddia (>4096) dogru, rakam yola bagli. Belgede sabitmis gibi
duruyorsa duzelt.

## Muhur

Denetim GECTI (tur 2, bagimsiz denetci). Dokuz maddenin dokuzu da dogrulandi:
kritik kapandi (10/10 yesil, 20 kol, sifir oksuz ffmpeg), sayac gercekten
`process.Start()` sonrasi artiyor, onbellek muhafizi deterministik, olcum
duzenegi belgedeki sayilari mertebe olarak yeniden uretti, "4730 bayt" yola
bagli olarak duzeltilmis, eklenen satirlarda `//` sayisi 0, BOM yok, owns
ihlali yok, CI yesil (34132920569).

### Borclar

1. `OynaticiBoruTests.cs:330-332` `ProcessesStarted` iddiasi komsu testi
   (`:283-299`) tekrarliyor; kurtarmayi asil pimleyen satir `Assert.NotNull(frame)`.
2. Sayac bu sozlesmenin hedefledigi sizintiyi goremez: oldurulemeyip geride kalan
   ffmpeg varken de bir sonraki `RestartVideo` sayaci artirir (`DecoderPipe.cs:313-333`).
3. `docs/olcumler/boru-kapanis-zaman-asimlari.md:124` "tek seferlik ~0,9 sn kodlama"
   diyor; olculen bedel yerelde kosum basina +2,5 sn, CI tam suitte +4 dk 9 sn.
   Cumle yalniz kodlamayi sayiyor. (Bu deponun imza kusuru: tablo dogru, cumle yanlis.)
4. `:107-111` yapay gecikme tablosunun `tools/boru-kapanis`'te alt komutu yok;
   belge `:4`'te "Tablolar yeniden uretilebilir" diyor.
5. `tools/boru-kapanis` `VidShrink.sln`'de degil; ne cozum ne CI derliyor,
   `DecoderPipe`'in internal yuzeyine dayandigi icin sessizce curur.
6. Fixture buyumesi onbellek rejimini degistirdi: 128 MB tavan eski klipte 60
   anahtar karenin tamamini tutuyordu, yenisinde 400'un ~34'unu. 10 kosumda
   kararsizlik gorulmedi.
