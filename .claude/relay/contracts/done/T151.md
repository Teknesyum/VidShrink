---
id: T151
title: PickFastCodec ilk olculmemis adayda duruyor — sonraki adaya gecmiyor
role: builder
model: sonnet
depends: [T139]
status: done
round: 1
verify: [dotnet test -c Release --filter "PlanCalculatorProbeTests|EncoderStateConsumptionTests"]
owns:
  - src/VidShrink.Core/PlanCalculator.cs
  - tests/VidShrink.Tests/PlanCalculatorProbeTests.cs
side_effects: []
---

## Baglam

`PlanCalculator.PickFastCodec` (`src/VidShrink.Core/PlanCalculator.cs:899-913`) hizli
kipte aday kodeklerini sirayla geziyor:

```csharp
foreach (var candidate in FastHardwareOrder)
{
    var state = availability.KnownState(candidate);
    if (state == EncoderProbeState.Unmeasured)
    {
        probe.NotMeasured = true;
        return candidate;
    }
    if (state == EncoderProbeState.Working) return candidate;
}
```

**Ilk olculmemis adayda duruyor ve donuyor.** Sirada calisan bir kodek olsa bile ona
hic bakmiyor. Somut sonuc: `av1_nvenc` olculmemis ama `hevc_nvenc` calisan bir makinede
plan `av1_nvenc`i seciyor, `CodecNotMeasured` isaretini tasiyor ve T139 tur 2'nin
getirdigi dogrulama onu **donanim yok** diye kapatiyor — oysa makinede calisan donanim
kodlayicisi **var**. Yanlis-negatif.

Bu tur 1 kararinin urunu, T139 tur 2'nin degil. Tur 2 denetcisi borc 6 olarak bildirdi
ve "ayri sozlesme gerekiyor" dedi; bu o sozlesme.

**Ucuz cevap yanlis olabilir.** "Olculmemisi atla, calisani sec" demek adayin hic
olculmemesine yol acar — sira hep calisan eski kodege duser ve yeni donanim yolu
hic denenmez. Karar bu iki riski birlikte tartmali.

## Kabul kriteri

**K1 — Davranis olculur, sonra secilir.** Uc mesru cevap var; birini sec ve **olcumle**
gerekcelendir:

1. **Devam et, sonuna kadar gez** — olculmemisi hatirla ama donme; calisan bulursan onu
   dondur, hicbiri calismiyorsa hatirladigin olculmemisi dondur.
2. **Olc, sonra karar ver** — olculmemis adayi o anda yokla. Maliyeti var; **kac ms**
   surdugu olculmeli.
3. **Bugunku davranis dogru** — o zaman yanlis-negatifin neden kabul edilebilir oldugunu
   olcumle goster.

CHECK: secim + gerekce + gerekceyi tutan kosum.

**K2 — Yanlis-negatif senaryosu teste doner.** `av1_nvenc` olculmemis, `hevc_nvenc`
calisan bir `IEncoderAvailability` kurulur ve planin ne sectigi assert edilir. Bugun
bu test **kirmizi** olmali (kusur duruyor), duzeltmeden sonra yesil.
CHECK: kirmizi kolun ham ciktisi + yesil kolun ham ciktisi.

**K3 — Mutasyon.** Duzeltmeyi geri alan bir mutasyon uret; K2'nin testi **olmeli**.
`dotnet build -c Release --no-incremental` zorunlu; `--no-build` yasak — bu depoda uc
kez eski ikiliyi kosturup yalanci yesil verdi.
CHECK: mutasyonun eski/yeni kolu, her hucrede ham cikti.

**K4 — T139'un pinleri kirilmaz.** `EncoderStateConsumptionTests` T139 tur 2'nin
KRITIK'ini koruyor. Yesil kalmali; kaliyorsa ham ciktiyi goster, kalmiyorsa **dur ve
bildir** — o zaman iki sozlesme carpisiyor demektir.
CHECK: kol basina test sayisi (`--list-tests`) + birlesik kosum.

## Sinirlar

- `owns` disina yazma. `MainWindow.axaml.cs` ve `EncodePlan.cs` **T139'un**; oraya
  dokunman gerekiyorsa **yapma, bildir**.
- `FastHardwareOrder` sirasini olcumsuz degistirme.
- Testi silme, `Skip`e alma, filtreden cikarma yoluyla yesil uretme.
- Gecici her sey `.calisma/T151/` altina. **`.calisma/kaynak/` paylasilan 3,5 GB —
  dokunma.**
- Kod yorumu yazma.
- **T139 muhurlenmeden acilmaz** — ayni dosyaya yaziyor.

## Kayit noktasi

Dal: `T151-ilk-olculmemis-aday`, guncel `main`den ac, `origin`e it. **`main`e
birlestirme** — onu T0 yapar.

## Cikti

`docs/olcumler/ilk-olculmemis-aday.md` — K1'in secimi ve olcumu, K2'nin iki kolu,
K3'un mutasyon izgarasi, K4'un kol sayilari.

## T0 sirasi — T150 tur 2'den sonra (3 Eylul 2026)

T151 bugun acilmadi, **bilerek.** Bu sozlesme `PlanCalculator.cs:899-913`'te
`EncoderProbeState.Unmeasured` ve `Working` tuketimini degistiriyor. T150'nin pimi
(`OluUyeTests.TheZeroConsumerSetIsThePinnedSet`) tam olarak **enum uyelerinin uretim
tuketicisini** sayiyor.

Bugun bu sinif zaten bir KRITIK uretti: `main`in `PerformanceProbe.cs:97` satiri
`EncoderProbeState.NotWorking`i tuketilir yapinca T150'nin sayisi 27'den 26'ya indi ve
olcu birlesik agacta kirmizi dondu. T151'i simdi acmak ayni carpismayi ucuncu bir
sayiyla tekrarlar: T150 tur 2 pimi 26'ya temellendirirken T151 altindan sayiyi
degistirir.

**Sira: T150 tur 2 muhurlenir, sonra T151 guncel `main`den acilir.** Acilirken yapiciya
sunlar sart kosulacak — kendi verify kolunun yaninda `dotnet test -c Release --filter
"OluUyeTests"` de kosulacak, sayi degisiyorsa **degistigi rapora yazilacak** ve pim
T151'in kendi dalinda yeniden temellendirilecek.

## Acildi — 3 Eylul 2026

T150 tur 2 muhurlendi (`de1ed9f` / `460ba73`), engel kalkti. Guncel `main` = `498ae44`.

**T150'nin pimi bugun 26'dir**, 27 degil — birlesik agacta yeniden temellendirildi
(`OluUyeTests.cs:363-370` docstring'i sebebini yaziyor). Bu sozlesme
`PlanCalculator.cs:899-913`te `Unmeasured` ve `Working` tuketimini degistiriyor, yani
sayiyi dogrudan etkileyebilir. Sart: kendi verify kolunun yaninda
`dotnet test -c Release --filter "OluUyeTests"` de kosulacak; sayi degisiyorsa
degistigi rapora yazilacak ve **pim T151'in kendi dalinda yeniden temellendirilecek.**

**T146 sirada bekliyor** — o da `PlanCalculator.cs`e sahip. Iki aktif sozlesme ayni
dosyayi almaz; T151 muhurlenince T146 acilir.

## Denetim — GECTI (3 Eylul 2026)

Denetci bagimsiz klonda `1f439d5`te olctu, depoya yazmadi. **KRITIK yok.**

- **K1'in merkezi iddiasini kaynaktan izledi.** Sozlesmenin itirazi "olculmemisi atlarsan
  aday hic olculmez" idi; iddia bunun gerceklesmedigi yonundeydi ve **dogru cikti**:
  `IEncoderAvailability.cs:27-29` `Unmeasured` donunce `IsMeasured` cagriliyor,
  `MainWindow.axaml.cs:1330` onu `Ready(...)`e bagliyor, `:1414-1418` `Measure`i
  `Task.Run` ile arka planda baslatiyor. Adayin **uzerinden gecmek** onu yoklamaya
  yolluyor. Olcuyle de gorundu: eski davranista yoklamaya gitmeyen alti aday
  isimleriyle listeleniyor.
- **Secenek 2'nin maliyeti sahte gecikmeyle olculmus ve raporda acikca sahte diye
  yazilmis**; denetci sayiyi kendi uretti (164-249 ms). Gercek yoklama alintisi da
  dogru: `ui-yoklama-donmasi.md:40` 173-469 ms.
- **K2'nin iki kolunu da kendi kosturdu**, ikisinde de `--no-incremental`. Kirmizi kol
  `Basarisiz: 3, Basarili: 35, Toplam: 38`, yesil kol 38/38. Olculer iki sabit
  karsilastirmiyor — gercek `DeferredEncoderAvailability` gecidi altinda plan kodegini
  assert ediyorlar.
- **K3'un uc mutasyonundan ikisini kendi uygulayip kosturdu**; ikisi de uc olcuyu kirdi.
- **K4** kollari `--list-tests` ile kendi saydi: 7 + 31 = 38, birlesik kosumla ayni.
- **T150'nin pimini kendi kosturdu**: 11/11 yesil, `sifir tuketici: 26`. Sayi degismedi.
- **owns temiz.** `MainWindow.axaml.cs` ve `EncodePlan.cs` dokunulmamis,
  `FastHardwareOrder` diff'te yok, silinen `[Fact]` ve `Skip` yok.
- **CI `33733955371`** `success`, `headSha` denetlenen commit'in ta kendisi.

### Borclar

1. **`PlanCalculator.cs:201` yedek kodek notu artik yanlis sey soyluyor.** T151 bu satira
   yeni bir yol aciyor: `av1_nvenc` olculmemis + `hevc_nvenc` calisan makinede eskiden
   `codec == preferredCodec` oldugu icin not hic cikmiyordu, bugun cikiyor ve
   "could not be used on this machine" diyor — dogrusu **"olculmedi"**. Yapici bunu
   kendi raporunda kapsam disi diye bildirdi; denetci KRITIK saymadi cunku planin
   tasiyici ciktisi olan kodek secimi dogru ve eski haline gore kesin iyilesme, kusur
   tavsiye metninin ifadesinde. **Ayri sozlesme:** bu kayit T152'nin gerekcesidir.
2. `AdayBasinaYoklamaMaliyetiTaramaBoyuncaCarpiliyor` siniri kendi olctugu `calls`tan
   turetiyor (`Elapsed >= delay * (calls - 1)`); her `Record` zaten `delay` kadar
   uyudugu icin neredeyse totoloji, gerileme pimi olarak is gormez. Gosterim duzenegi;
   K1'in istedigi "kac ms" sayisi gercek ve denetci de uretti.
3. M3'un ham ciktisi yalniz `.calisma/T151/k3-mutasyon.txt`te, gitignore'da, daldan
   okunamiyor. M1 ve M2 kendi kosturuldugu icin K3 kanitli; ucuncu satir tek kaynakli.
   Bugun ucuncu kez ayni kusur (T141, T145, T151) —
   `denetci-worktree-yerel-dosyayi-eski-gorur`.
4. Olcum belgesi 15 000 ms tavani icin `ui-yoklama-donmasi.md:109` diyor; cumle 107.
   satirda. Sayi ve anlam dogru, satir atfi iki kayik.
