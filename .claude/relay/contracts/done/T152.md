---
id: T152
title: Yedek kodek notu olculmemis adaya "bu makinede kullanilamadi" diyor
role: builder
model: sonnet
depends: [T151, T146]
status: done
round: 1
verify: [dotnet test -c Release --filter "PlanCalculatorProbeTests|PlanCalculatorTests"]
owns:
  - src/VidShrink.Core/PlanCalculator.cs
  - tests/VidShrink.Tests/PlanCalculatorProbeTests.cs
side_effects: []
---

## Baglam

T151 denetcisinin birinci borcu. `PlanCalculator.cs:201`:

```csharp
reason.Add($"the {preferredCodec} encoder could not be used on this machine, so encoding falls back to {codec}")
```

T151 bu satira **yeni bir yol acti.** Eskiden `av1_nvenc` olculmemis + `hevc_nvenc`
calisan bir makinede `codec == preferredCodec` oluyordu ve not hic cikmiyordu. Bugun
`PickFastCodec` sonraki adaya geciyor, `codec != preferredCodec` oluyor ve not cikiyor —
kullaniciya **"bu makinede kullanilamadi"** diyor. Dogrusu **"olculmedi"**.

Bu, bu deponun kapatmakta oldugu kusurun ayni ailesi: "olcemedim" ile "olctum, sonuc bu"
ayrimi kayboluyor (T137, T139, T141 ayni ayrimi baska yerlerde kapatti). Burada ayrim
kodda **var** — `EncoderProbeState.Unmeasured` duruyor — ama kullaniciya ulasan cumle
onu tasimiyor.

**KRITIK degil, borc:** planin tasiyici ciktisi olan kodek secimi dogru ve T151 kesin
iyilesme. Kusur yalnizca tavsiye metninde.

## Kabul kriteri

**K1 — Uc yol ayrilir.** Bugun tek cumle uc farkli durumu anlatiyor: (a) tercih edilen
kodlayici **olculdu ve calismiyor**, (b) **olculmedi**, (c) baska sebeple secilmedi.
Her biri icin ayri cumle uret. Cumleler `Themes`e degil metin kaynagina ait; yerellestirme
anahtari gerekiyorsa **once bildir**, uydurma.
CHECK: uc durumun her biri icin girdi + uretilen cumle.

**K2 — Kusur once kirmiziya duser.** `av1_nvenc` olculmemis + `hevc_nvenc` calisan
girdide bugunku cumlenin **yanlis oldugunu** gosteren bir olcu yaz, once kirmizi kaydet.
Cumlenin metnini sabit karsilastirmaktan kacin — davranisi pimle: olculmemis adayin
notunda "could not be used" gecmemeli, olculmus-calismayan adayinkinde gecmeli.
CHECK: kusur commit'i ayri, kirmizi cikti raporda.

**K3 — Mutasyon.** Duzeltmeyi geri al, K2'nin olcusu olmeli.
`dotnet build -c Release --no-incremental` zorunlu; `--no-build` yasak.
CHECK: mutasyonun eski/yeni kolu, her hucrede ham cikti.

**K4 — T150'nin pimi ve T151'in olculeri kirilmaz.**
`dotnet test -c Release --filter "OluUyeTests"` ve T151'in kollari yesil kalmali.
CHECK: kol basina test sayisi + ham cikti.

## Sinirlar

- `owns` disina yazma. Yerellestirme dosyalari ve `MainWindow.axaml.cs` **senin degil**.
- Yeni yetenek yazma; bu bir ifade sozlesmesidir.
- Gecici her sey `.calisma/T152/` altina. `.calisma/kaynak/` paylasilan 3,5 GB — dokunma.
- Kod yorumu yazma.
- **T146 muhurlenmeden acilmaz** — ayni dosyaya yaziyor.

## Kayit noktasi

Dal: `T152-olculmedi-ile-calismiyor`, guncel `main`den ac, `origin`e it.
**`main`e birlestirme** — onu T0 yapar.

## Cikti

`docs/olcumler/olculmedi-ile-calismiyor.md` — K1'in uc durum tablosu, K2'nin kirmizisi,
K3'un mutasyon izgarasi, K4'un kol sayilari.

## Neden `beklemede/` altinda (3 Eylul 2026)

`owns` cakismasi: `src/VidShrink.Core/PlanCalculator.cs` su anda T146'nin. Iki aktif
sozlesme ayni dosyayi almaz, yani bu sozlesme **dagitilamaz.**

`contracts/` kokunde `status: open` durunca `Stop` kapisi onu her turda "atanmamis is"
diye isaretliyor ve turu kesiyor — kapi `depends` alanini **hic okumuyor** (olculdu:
`teknesyum-core/0.7.3/hooks/watch.js:228-238`, yalniz `status` ve canli ajan kaydina
bakiyor). `depends: [T151, T146]` yazmak bu yuzden sesi kesmedi.

Sozlesme silinmiyor, kaybolmuyor: T146 muhurlenince `contracts/` koküne geri tasinir ve
dagitilir. Iceriginde tek satir degismedi.

Bulgunun kendisi ayrica T151'in muhur notunda birinci borc olarak duruyor
(`contracts/done/T151.md`), yani bu dosya kaybolsa bile kayit tek yerde degil.

## T0 duzeltmesi — olu verify kolu (3 Eylul 2026)

Dagitimdan once olculdu:

```
grep -rn "class PlanReasonTests" tests/ --include=*.cs   -> bos
tests/VidShrink.Tests/PlanCalculatorProbeTests.cs:18 public sealed class PlanCalculatorProbeTests
tests/VidShrink.Tests/PlanCalculatorTests.cs          var
```

`PlanReasonTests` bu depoda **yok** ve `owns` kumesinde de yok — yani yapicinin acmasi
beklenen bir dosya degildi. Kol sifir teste denk geliyordu ve vstest'in varsayilan
alt-dize operatoru altinda **exit 0 ile sessizce gecerdi.** Satiri T0 yazdi.

Kol var olan `PlanCalculatorTests` ile degistirildi. Bu, bu deponun **dorduncu** olu-kol
vakasi; ilk uc kez muhurlu sozlesmelerde bulundu, bu kez dagitimdan once yakalandi.

**K4 bu yuzden genisletildi:** yalniz T150/T151 kollarini degil, **verify satirindaki iki
kolu da** `--list-tests` ile kendin say. Sifir bulan kol varsa teslim etme, bildir.

## Teslim (3 Eylul 2026)

Dal `T152-olculmedi-ile-calismiyor`, uc **`0fa30a7`**. Kusur commit'i `d599853`, duzeltme
`ee96c9a`, rapor `0fa30a7`. Dal tabani `ee0d8d5`; uc dosya: `PlanCalculator.cs`,
`PlanCalculatorProbeTests.cs`, `docs/olcumler/olculmedi-ile-calismiyor.md`.

## Denetim — GECTI (3 Eylul 2026)

Denetci `.calisma/T152-denetim/klon` altindaki tek kullanimlik klonda `0fa30a7`te olctu;
`live/a050c041c904d306d.json` `files: []`. **KRITIK yok, `owns` ihlali yok.**

- **K1 uc yolu gercekten ayiriyor.** `PlanCalculator.cs:909` `EncoderFallbackReason`;
  uc dal uc ayri dizge (`is not part of this ffmpeg build` / `has not been measured on
  this machine` / `could not be used on this machine`). Besleyen yazimlar `PickFastCodec:975,981`
  ve `PickCodec:940,943`; tek `ProbeState` ornegi `:137`de kuruluyor, `:185`te doluyor,
  `:215`te okunuyor. Tavsiye kodegi (`:414,476`) `probe=null` asiri yuklemesini kullaniyor —
  durumu kirletmiyor.
- **K2 kirmizisi gercek.** Denetci `d599853`i `--no-incremental` derleyip kostu:
  `Basarisiz: 3, Basarili: 31, Toplam: 34`. Raporun `3/0/3` satiri uc yeni olcuye
  daraltilmis filtrenin ciktisi — **etiket saydigiyla uyusuyor**, sisirme yok.
- **Olculer davranis olcuyor.** Sabit-sabit karsilastirmasi yok:
  `UcDurumUcFarkliCumleUretiyor` `Distinct(Ordinal).Count() == 3` sayiyor;
  `OlculmemisAdayinNotu...` iki girdiyi yan yana kurup alt-dize varligi/yoklugu **ve**
  `Assert.NotEqual` pimliyor.
- **K3 izgarasini denetci kendi uyguladi**, her mutasyondan once `--no-incremental` derleme:

  | mutasyon | ham cikti | ayakta kalan |
  |---|---|---|
  | tek cumleye dondur | `Basarisiz: 3, Basarili: 0, Toplam: 3` | — |
  | `PreferredCodecState` yazimini sil | `Basarisiz: 2, Basarili: 1, Toplam: 3` | `DerlemedeOlmayanAdayinNotuOlcumIddiasiTasimiyor` |
  | `PreferredCodecInBuild` yazimini sil | `Basarisiz: 2, Basarili: 1, Toplam: 3` | `OlculmemisAdayinNotuBuMakinedeKullanilamadiDemiyor` |

  Sag kalan her seferinde otekinin kolu — izgara birebir tutuyor.
- **Tasarim gerekcesi ayakta.** Denetci `EncoderFallbackReason`u durumu ikinci kez
  `availability.KnownState(...)` ile soracak bicimde yeniden yazdi: `PlanCalculatorTests`
  `Basarisiz: 4, Basarili: 28` ve dusen dort olcu **tam olarak raporun adlandirdiklari**.
  Ne fazla ne eksik.
- **K4'te olu kol yok.** `--list-tests`: `PlanCalculatorProbeTests` 34,
  `PlanCalculatorTests` 32, kesisim yok, 66 = 34 + 32. Kosum `0/66`; `OluUyeTests` `0/11`.
  **Bu sozlesmenin verify satiri dagitimdan once duzeltilmisti** (ustteki T0 duzeltmesi);
  dorduncu olu kol vakasiydi ve ilk kez muhurden **once** yakalandi.
- **CI `33746360125`** `conclusion: success`, `headSha` `0fa30a7` — denetlenen ucun ta kendisi.
- **`owns` temiz.** `git diff --name-only ee0d8d5..0fa30a7` uc dosya; `CodecModel.cs`,
  `MainWindow.axaml.cs` ve `Locales/**` dokunulmamis.

Birlesmis agacta yeniden olculdu (`--no-incremental` derleme + iki kol):
`PlanCalculatorProbeTests|PlanCalculatorTests` -> `Basarisiz: 0, Basarili: 66`.

### Borclar

1. **Duzeltme kullaniciya ulasmiyor.** Cekirdek uc cumleyi ayiriyor ama arayuz uc durumda
   da "bu makinede kullanilamadi" diyor: `MainWindow.axaml.cs:2353` `plan.ReasonCodes`
   uzerinden tek anahtar cagiriyor (`main.reason.encoder-fallback`, `Locales/en/main.json:285`
   ve `tr/main.json:285`). `plan.Reason`in tek dis okuyucusu `tools/surucu-yoklugu/Program.cs:203`.
   `ReasonCode` enum'u `EncodePlan.cs:23` — `owns` disi. **Sozlesme "anahtar gerekiyorsa
   once bildir, uydurma" dedigi icin bildirmek dogru davranisti.** Ayri sozlesme.
2. **T136/K6 pimi artik boslugu ortuyor.** `PlanCalculatorProbeTests.cs:765`
   `ArayuzunDusmeCumlesiCoreunkiyleAyniSeyiSoyluyor` Core ile arayuz cumlesinin
   ayrismasini yakalamak icin yazilmisti. Ayrisma bugun uc durumun **ikisinde var**, ama
   olcunun tek girdisi (`RecordingAvailability`) tam da ayrismayan ucuncu duruma dusuyor —
   o yuzden yesil kaliyor. Borc 1 kapatilirken **bu olcu de genisletilmeli.**
3. **`AdviceCode.EncoderFallback` / `main.advice.encoder-fallback`** ayni bicimde uc durumu
   ayirmiyor. Yapici bildirdi, borc 1'le ayni sozlesmeye dusuyor.
4. **Sahte nesne gercekle ufak ayrisiyor.** `MissingCodecAvailability.EncoderState` eksik
   kodek icin `NotWorking`, `HasEncoder` ise `false` donuyor. Bugun zararsiz —
   `!PreferredCodecInBuild` dali durum sorulmadan kisa devre yapiyor — ama sahte, uretimin
   anlamayacagi bir cevap veriyor.

### Sonraki sozlesmeye dusen

- **Uc cumleyi kullaniciya tasimak** (borc 1 + 2 + 3) — `EncodePlan.cs`, `Locales/*/main.json`,
  `MainWindow.axaml.cs` ve genisletilecek `:765` olcusu. Bu uc borc **tek sozlesmedir**;
  ayri ayri acilirsa arayuz yarim ayrisir.
- **Iki `TurboFirstPassIsSafe`i birlestirmek** (T153 borc 3) — `PlanCalculator.cs` artik bos,
  T155'in `owns` kumesinde.
