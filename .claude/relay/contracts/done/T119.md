---
id: T119
title: Kare kilidinin yarisini hicbir olcu korumuyor, ve kilit iki yerde yaziyor
role: builder
model: opus
depends: [T111]
status: done
round: 2
verify: [dotnet test -c Release --filter "VmafPoolingTests|QualityMeterTests"]
owns: [src/VidShrink.Ffmpeg/QualityMeter.cs, tests/VidShrink.Tests/VmafPoolingTests.cs, tools/VidShrink.Bench/Program.cs, docs/olcumler/kilit-olcusu.md]
side_effects: []
---

## Baglam

T111 kare kaymasini **kimlige** cevirdi: kilit takiliyken referansi `setpts=N+1`
ile kaydirinca kilitsiz olcumun dort sayisi da birebir geri geliyor. Kilit
calisiyor ve olcunun tamamini tasiyor — HandBrake acigi +1,269'dan +0,097'ye
bu yuzden indi.

Ama T111 kapatamadigi bir kusur buldu (kusur 8):

**Kilidin `settb=AVTB` yarisini hicbir olcu korumuyor.** Sabit
`QualityMeter.cs:75`'te `"settb=AVTB,setpts=N"`. `settb=AVTB` silinince
**15 olcu yesil kaliyor.** Bu esdeger mutasyon **degil**: ayni mutasyon olcum
duzeneginde eslenen kare sayisini 3624'ten **7012**'ye cikariyor. Yani mutasyon
gercek davranisi degistiriyor, olculer gormuyor.

T111 mutasyonu yalniz Bench kopyasinda kosturdu; **uretim kopyasi olculmedi.**

## Ikinci kusur: kilit iki yerde yaziyor

`src/VidShrink.Ffmpeg/QualityMeter.cs:75` ve
`tools/VidShrink.Bench/Program.cs:2547` **ayni sabiti ayri ayri** tasiyor.
Ikisi bugun ayni; hicbir sey ayni kalmalarini zorlamiyor. Bu projede "ortak
diye bilinen iki parca sessizce farkli cikti" vakasi kayitli ve bir A/B'yi
haksiz yapti.

## Ucuncu kusur: olcum sessizce yok donuyor

`QualityMeter.cs:224` — `if (!File.Exists(logPath)) return null;`. Filtre
zinciri bozulursa sonuc **hata degil, "olcum yok"** olarak okunuyor. T108 ve
T116 bunu borc olarak yazdi, sahibi olmadi. Simdi sahibi sensin.

Not: T108 bir hipotezi olctu ve **curuttu** — `EscapeFilterPath` (`:442-443`)
hem kacis hem tirnak ekliyor ve gecen bicim budur. Yani bu madde "kacis bozuk"
degil, "bozuk olan sessiz kaliyor" maddesidir.

## Kabul kriteri

1. **`settb=AVTB` mutasyonu kirmiziya dusen bir olcuye baglanir.**
   Olcu **davranisi** olcer: mutasyon uretim kopyasinda uygulaninca eslenen
   kare sayisi degisiyor — olcu o degisimi gorur. Iki sabiti karsilastiran
   olcu yazma; `Skip` yazma; ffmpeg yoklugunda sessiz erken donus yazma.
   CHECK: mutasyon uygulanmis halde kirmizi, geri alinca yesil, ikisi de
   `--no-incremental` derlemeyle.

2. **Mutasyon uretim kopyasinda kosturulur.** T111'in eksigi buydu. Bench
   kopyasindaki sonuc uretim icin kanit degil.
   CHECK: hangi dosyada mutasyon yapildi, kac olcu dustu.

3. **`setpts=N` yarisi da ayni sekilde sinanir.** Kilidin iki yarisi var;
   birini korumak digerini korumaz. Iki yarim icin **ayri** mutasyon kaniti.
   CHECK: iki mutasyon, iki sonuc.

4. **Sabit tek yerde durur.** `QualityMeter.cs` ile `Bench/Program.cs`
   arasindaki kopya kaldirilir — Bench uretim sabitini **cagirir**. Cagiramayan
   bir sebep varsa (bagimlilik yonu ters) sebep yazilir ve iki kopyanin ayni
   kalmasini **zorlayan bir olcu** konur.
   CHECK: ya tek sabit, ya ikisini esitleyen olcu.

5. **Sessiz `null` gorunur olur.** `:224`'un iki durumu ayrisir: olcum
   **yapilmadi** (mesru) ile olcum **basarisiz oldu** (kusur). Bugun ikisi de
   `null`. Ayrimin uretimde nasil okunacagi yazilir; cagiran taraf hatayi
   yutuyorsa **bu rapora yazilir** (cagiran dosya senin `owns`'unda degilse
   yazmakla yetin).
   CHECK: bozuk filtre zinciriyle bir kosum — ne donuyor, nasil ayirt ediliyor.

6. **Degisen her sabitin ustundeki cumle ayni commit'te degisir.**

7. **Olcmedigin sey icin "olculmedi" yaz.** Bu projede on bir sozlesmede tur
   actiran bulgu ayniydi: tablo dogruyken onu ozetleyen cumle yanlisti.

## Sinirlar

- `owns` disina yazma. `tests/VidShrink.Tests/VidShrink.Tests.csproj` T95'in —
  Bench'e basvuru zaten var (`:32`), yeni basvuru gerekmez. `AbTests.cs` T95'in.
- Kod yorumu yazma.
- Her mutasyon kosumundan once `dotnet build VidShrink.sln -c Release
  --no-incremental`. Artimli derleme mutasyonu ikiliye tasimiyor — bu projede
  bir kez yanlis "yesil" urettirdi.
- Esdeger mutasyon test acigi degildir; oyleyse **oyle yaz** ve neden esdeger
  oldugunu goster.
- Tam suiti kosturma; yalniz `verify` filtresi.
- `main`in CI'inda ffmpeg **yok**; oradan gelen yesil ffmpeg gerektiren olculer
  hakkinda bir sey soylemiyor. ffmpeg gerektiren olcu `[FfmpegFact]`.
- Makine paylasimli, on ajan kosuyor. Sure sayisina damga bas.

## Kayit noktasi

Dal: `T119-kilit-olcusu`, tur 1 commit `cc816a7`, tur 2 commit `98e94ad`, itilmis.
Worktree: `.claude/worktrees/T119`. Yedi kabul kriteri de kapandi.

## Cikti

- `docs/olcumler/kilit-olcusu.md`: iki yarim icin mutasyon kaniti, kopya sabitin
  akibeti, sessiz `null`'un ayrisan iki durumu.
- Kendi dalin, itilmis. `main`e sen birlestirme.

## Rapor

**Verify:** `dotnet test -c Release --no-build --filter "VmafPoolingTests|QualityMeterTests"`
-> **0 kirmizi / 43 gecti / 0 atlandi** (temiz taban 04:54 UTC ve mutasyon geri
alindiktan sonra 04:57 UTC, ikisi de `--no-incremental` derlemeyle). Ilk halde
39 olcuydu; dordu bu sozlesmede eklendi.

**K1 + K2 + K3 — iki yarim, iki mutasyon, uretim kopyasinda.** Mutasyon
`src/VidShrink.Ffmpeg/QualityMeter.cs:80`'deki uretim sabitine uygulandi
(T111 yalniz Bench kopyasini kosturmustu).

| mutasyon | damga (UTC) | kirmizi |
| --- | --- | ---: |
| M1 `FrameLock` -> `"setpts=N"` (settb yarisi) | 04:54-04:55 | 4 |
| M2 `FrameLock` -> `"settb=AVTB"` (setpts yarisi) | 04:55-04:56 | 4 |

Her iki mutasyon da **dorder** olcu dusuruyor; tablo bunu diyor, ozet de bunu
demeli. **Yalitik** olan, yeni iki olcunun birbirine gore konumu: M1'in dort
kirmizisinin icinde yeni olculerden yalnizca
`UretimKilidi_ZamanTabanlariFarkliGirdileri_BireBir_Esler` var (60 kare beklenirken
119) ve `...AltKareKaymasinda...` yesil kaliyor; M2'de tam tersi. Yani her yarim
kendi olcusune bagli, otekinin golgesinde degil.

**T111'in bulgusu duzeltildi, cürütülmedi.** M1'de mevcut `VmafPoolingTests`
olculerinin hicbiri dusmedi — T111'in "15 yesil" gozlemi Bench kopyasi icin
dogru ve burada yeniden uretildi. Ama uretim kopyasi **tumuyle korumasiz
degilmis**: `QualityMeterTests`'in uc olcusu M1'i zaten yakaliyor, cunku ucu de
`.mkv` ile `.mp4` karsilastiriyor, yani girdilerin zaman tabanlari farkli. Bu
ilk kez burada olculdu. Kilidi **adlandiran ve yalitan** olcu yine de yoktu;
eklenen o.

**K4 — sabit tek yerde.** `tools/VidShrink.Bench/Program.cs:2547`'deki kopya
kaldirildi: `public static string FrameLock => VidShrink.Ffmpeg.MeasureFilterGraph.FrameLock;`.
Uretim tarafinda `const` -> `static readonly` (const derleme aninda cagirana
kopyalanir; bu projede artimli derlemenin bayat ikili kosturdugu vaka kayitli).
Birlesmenin isledigi M2'de goruldu: uretim sabiti mutasyona ugrayinca Bench
grafigini kullanan `KareKilidi_AltKareKaymasinaRagmen_KareleriDogruEsler`
kirmiziya dondu — birlesme oncesi o mutasyon oraya hic ulasmiyordu. Iki sabiti
karsilastiran olcu yazilmadi, gerekmedi.

**K5 — sessiz `null` ayristi.** `MeasureVmafAsync` artik `null` donmuyor
(donus turu `VmafAggregate?` -> `VmafAggregate`); gunluk yoksa/bossa/bozuksa
yeni `QualityMeasurementFailedException` firlatiyor. "Olculmedi" karari yalniz
disaridaki `HasFilter("libvmaf")` kapisinda. `QualityMeasurement.MeasureWindowAsync`
basarisizligi `null` yerine `Comparable=false` + `Message` dolu bir
`WindowQualityMeasurement` ile donduruyor.

Bozuk zincirle gercek kosum (`GunlukUretmeyenZincir_Olculmedi_Degil_Basarisiz_DiyeOkunur`):
uretim `Build`'iyle kurulan zincirde `libvmaf` `log_path`siz kosuyor,
**ffmpeg cikis kodu 0**, gunluk yok, `ReadVmafScoresAsync` istisna firlatiyor —
eskiden ayni yer `null` donup "olcum yok" diye okunuyordu.

**Owns disi, duzeltilmedi, yazildi:** `src/VidShrink.Ffmpeg/ComplexityProbe.cs:472-477`
cagriyi `catch { quality = null; }` ile sariyor ve `:74` yalniz `Comparable: true`
olanlari topluyor; `src/VidShrink.App/MainWindow.axaml.cs:1505` ayni suzgeci
uyguluyor. Basarisiz olcum artik nesne olarak ayirt edilebiliyor ve iletisini
tasiyor ama **hicbir cagiran o iletiyi gostermiyor**.

**K6 — sabitin ustundeki cumle.** `FrameLock`'un **degeri degismedi**, yalniz
bildirimi ve Bench kopyasi degisti. Kodda ustunde yorum yok. Sabiti anlatan
duzyazi `docs/olcumler/olcu-gecerliligi.md:398-412` ve
`docs/olcumler/algi-olcusu.md:747-748`; ikisi de elle okundu, degisiklikten
sonra hala dogru (algi-olcusu'nun "T106'nin kullandigi kilidin birebir aynisi"
cumlesi artik derleyici tarafindan zorlaniyor). Iki dosya da `owns` disinda,
duzeltme gerekmedi.

**K7 — olculmeyenler.** CI hakkinda hicbir sey soylenmiyor: `main`in CI'inda
ffmpeg yok, yeni dort olcunun ucu `[FfmpegFact]` ve orada **atlanir**. Kilit
CI'da korunmuyor, bu sozlesme onu degistirmedi. Sayilar tek makine ve tek
ffmpeg surumunden (`9.0-full_build`, Windows 11). `PTS-STARTPTS` almasigi
yeniden olculmedi. `1/1000` ve `1/64000` taban degerleri kaplarin varsayilani;
baska surumde degisirse olcunun onculu `Assert.NotEqual` ile pimli, sessizce
bosa dusmez.

**Beklenmeyen durum yok** — `_sorun.log`'a satir yazilmadi.

## Cikti

- `docs/olcumler/kilit-olcusu.md` — iki yarim icin mutasyon kaniti, kopya
  sabitin akibeti, sessiz `null`'un ayrisan durumlari, olculmeyenler.
- `src/VidShrink.Ffmpeg/QualityMeter.cs` — `FrameLock` `static readonly`,
  `QualityMeasurementFailedException`, `ReadVmafScoresAsync`, `MeasureVmafAsync`
  artik `null` donmuyor, `MeasureWindowAsync` basarisizligi ayirt ediyor.
- `tests/VidShrink.Tests/VmafPoolingTests.cs` — dort yeni olcu (uc `[FfmpegFact]`,
  bir saf `[Fact]`), hepsi uretim `MeasureFilterGraph.Build` uzerinden.
- `tools/VidShrink.Bench/Program.cs` — kopya sabit kaldirildi.
- Dal `T119-kilit-olcusu`, commit `cc816a7`, `origin`e itildi. `main`e
  birlestirilmedi.

## Denetim tur 1 — GECTI, ama T0 bir borcu KRITIK'e cikardi (2026-09-02, uc cc816a7)

Denetci depoya hicbir dosya yazmadi; `git archive cc816a7` kopyasinda calisti. Yedi
kriterin yedisi de karsiligini buldu, `owns` ihlali yok (dort dosya, dordu de listede).

**Yalitim iddiasi bagimsiz olarak dogrulandi ve denetci fizigi kendisi yeniden uretti:**

```
tb mkv: 1/1000   tb nut: 1/64000
TABAN-FARKI  settb=AVTB,setpts=N  kare=60   min=inf
TABAN-FARKI  setpts=N             kare=119  min=17.15
TABAN-FARKI  settb=AVTB           kare=60   min=inf
ALT-KARE     settb=AVTB,setpts=N  kare=60   min=inf
ALT-KARE     setpts=N             kare=60   min=inf
ALT-KARE     settb=AVTB           kare=60   min=23.53
```

Iki mutasyonun dort kirmizisi **ayni ucluyu paylasmiyor** — kesisim tek olcu
(`SubFrameTimestampSlipDoesNotCostTheScoreAWholeFrame`, `QualityMeterTests.cs:211-214`),
cunku o olcu iki duzenegi birden barindiriyor. Liste `kilit-olcusu.md:25-37`de yazili.

### T0 karari — denetcinin 3. borcu KRITIK'tir, tur 2 aciliyor

Denetci sunu buldu: `QualityMeter.MeasureAsync`/`MeasureWindowAsync`
(`QualityMeter.cs:170-177`) artik `QualityMeasurementFailedException` sizdiriyor ve
`tools/VidShrink.Bench/Program.cs:76` ile `:86` hicbir `catch` tasimiyor. Eskiden `null`
alanli JSON basip `return 0` yapan yol **simdi yakalanmamis istisnayla dusuyor.**

Denetci bunu borc saydi. **Katilmiyorum.** Kriter acik: gercekci girdide yanlis cikis
kodu KRITIK'tir. Bozuk olcum zinciri gercekci bir girdidir — T119 onu kendisi uretti ve
belgeledi — ve `Bench` bu sozlesmenin `owns`unda. Kural bozulmuyor, kriter uygulaniyor.

**Tur 2'de kapanacaklar:**

1. **KRITIK — `Bench`in dusme yolu.** `Program.cs:76` ve `:86` istisnayi tutsun. Eski
   davranisa (`null` alanli JSON + `return 0`) donmek **dogru cevap olmayabilir** —
   sessiz basarisizlik bu sozlesmenin kapatmaya calistigi seyin ta kendisiydi. Dogru
   cikis kodunun ne oldugunu **sen sec ve gerekcesini yaz**: olcum basarisizsa Bench
   sifir mi donmeli, yoksa sifirdan farkli mi? Sectigin davranis bir olcuyle pimlensin.
2. Ayni yolda **`xpsnr` ve `ssim` de kayboluyor** (denetcinin 4. borcu):
   `QualityMeter.cs:206` firlattigi icin `:208-214` hic kosmuyor; onceden vmaf `null`
   donerken oteki iki olcu yine hesaplaniyordu. Gurultulu dusmek K5'in istedigi sey ama
   **veri kaybi hicbir yerde yazili degil.** Ya kaybi onle, ya belgeye yaz — hangisini
   sectigini gerekcelendir.
3. **Iki tip hala ayni adi tasiyor** (denetcinin 5. borcu):
   `VidShrink.Ffmpeg.MeasureFilterGraph` ve genel ad alanindaki Bench kopyasi
   (`Program.cs:2545`). `VmafPoolingTests.cs:136/150/162` niteliksiz adi **Bench'e**
   cozuyor, `:358` uretimi tam nitelemek zorunda kaliyor. K4 sabiti tekledi, **tip adini
   teklemedi** — ve T111'in yanlis kopyayi olcmesine yol acan tuzak aynen duruyor. Bu
   sozlesmenin acilis gerekcesi buydu; adi tekle.
4. **`GunlukUretmeyenZincir_...` sandigi seyi olcmuyor** (denetcinin 6. borcu):
   `VmafPoolingTests.cs:284` `Assert.False(File.Exists(gunluk))` bos bir iddia, cunku
   `vmaf.json` yolu ffmpeg'e hic verilmiyor (`:280-282`). Olcunun gercek katkisi tek:
   uretim grafigi + `libvmaf` **cikis kodu 0** veriyor. Ya olcuyu gercekten sordugu seye
   baglayacak sekilde yaz, ya adini ve belgedeki cumlesini (`kilit-olcusu.md:138`)
   olctugu seye gore duzelt.

**Borc kalanlar (tur 2'de acilmayacak, muhur notuna):**

5. Paketin ozet cumlesi tabloyu yanlis ozetliyor (`T119.md:126-128` "M1'de **yalniz**
   ..." ama tablo dort kirmizi diyor). **Kanonik belge dogru yazmis**
   (`kilit-olcusu.md:39-41`); kusur pakete gecen ozet cumlede. Kronik sinifin **on
   ucuncu** tekrari.
6. "T111 yanlis bir sey soylemedi" fazla yumusak (`kilit-olcusu.md:56`). Sozlesmenin
   kendi onculu (`T119.md:23`) **curutuldu** — T111'in Bench olcumu ayakta, ondan
   turetilen genelleme degil. Curutulmus oncul gecerli teslimdir; sorun sonucun kendisi
   degil, adinin konmamasi.
7. **Kilit CI'da korumasiz** — dort yeni olcunun ucu `[FfmpegFact]`, dorduncusu duz
   `[Fact]` ama kilide dokunmuyor. `main`de ffmpeg yok. T115 birlesene kadar boyle
   kalir; bu T119'un kusuru degil, **muhur sirasinin bir gercegi.**
8. Sozlesme kendi icinde celisiyordu: K1 "`Skip` yazma" (`:54`), Sinirlar "ffmpeg
   gerektiren olcu `[FfmpegFact]`" (`:96`). Ajan Sinirlar'i izledi ve K7'de acikca
   bildirdi — **dogru karar**, yeniden tartisilmayacak. Celiski benim yazim hatam.

---

## Tur 2 raporu

**Verify:** `--filter "VmafPoolingTests|QualityMeterTests"` -> **0 kirmizi / 45 gecti /
0 atlandi** (06:49-06:51 UTC, `--no-incremental` derlemeden sonra). Tur 1'de 43'tu;
iki olcu daha eklendi.

**1 (KRITIK) — Bench dusme yolu. Secim: sifirdan farkli.** Gerekce: bu sozlesmenin
kapattigi kusur "basarisiz olcumun basarilidan ayirt edilememesi"ydi; cikis kodu 0'a
donmek onu geri getirir. Yeni `BenchOlcum.YazAsync` uc durumu uc kodla ayiriyor:

| durum | cikis kodu | stdout | stderr |
| --- | ---: | --- | --- |
| olcum basarili | 0 | tam skor JSON'u | — |
| olcum basarisiz | **2** | **kismi** skor JSON'u (`Comparable=false`) | ileti |
| kullanim hatasi | 1 | — | kullanim iletisi |

2 secildi cunku 1 bu programda zaten kullanim hatasi. Kismi JSON yine basiliyor:
gurultulu dusmek veri atmayi gerektirmiyor. `Program.cs:76` ve `:86` artik bu islevi
cagiriyor. Pim: `BasarisizOlcumde_Bench_SifirDonmez_KismiSonucu_YineDeYazar` ve
`BasariliOlcumde_Bench_SifirDoner_VeSkoruYazar`, ikisi de saf `[Fact]`. M3
(`Basarisiz` 2 -> 0) ve M5 (kismi skor basilmaz) birer kirmizi verdi.

**2 — `xpsnr`/`ssim` kaybi ONLENDI, ama sirasi PIMLI DEGIL.** Hata artik yakalanip
saklaniyor, iki olcu hesaplaniyor, istisna kismi skoru tasiyarak firlatiliyor
(`QualityMeasurementFailedException.PartialScore`). **Olculdu:** M4 mutasyonu (catch
govdesi `throw;`, eski sira geri getirildi, `--no-incremental`, 06:48-06:49 UTC)
**0 kirmizi / 45 yesil** birakti. Sebep: gercek bir vmaf basarisizligini uretim
`MeasureAsync`'i uzerinden zorlamak icin gunluk yolunun disaridan verilebilmesi
gerekiyor; o uye `private` ve bu sozlesmede acilmadi. Pimlenen tek sey istisnanin
kismi skoru tasimasi ve Bench'in onu basmasi. Yani hem duzeltildi hem "olculmedi"
diye yazildi (`kilit-olcusu.md` 4.2 ve "Olculmeyen" listesi).

**3 — tip adi teklendi.** Bench'in kopyasi `BenchMeasureFilterGraph` oldu ve govdesi
uretim `Build`'ine devrediyor. Nitelenmemis `MeasureFilterGraph` artik her yerde
uretim tipi; `VmafPoolingTests.cs`'in dort cagrisi yeni ada tasindi, `:358` tam
niteleme zorunlulugundan kurtuldu. T111'i yanlis kopyayi olcmeye goturen tuzak
kapandi. Yan etki yazildi: Bench grafiginin referans dali artik
`[1:v]null,settb=AVTB,setpts=N` oluyor — `null` gecirgen filtre, sonuc degismiyor
(M1/M2 tur 2'de ayni dort olcuyu dusurdu, taban 45/45 yesil), ama Bench'in bastigi
komut dizesi eski arsiv gunluklerinden bu ek kadar farkli.

**4 — bos iddia kapatildi.** `GunlukYazilamayanZincir_FfmpegSifirDonuyor_OkuyucuBasarisizDiyor`:
`log_path='olmayan/vmaf.json'` artik ffmpeg'e **veriliyor**, hedef klasor yok.
Olculen: ffmpeg stderr'e `could not open file` yaziyor ve **cikis kodu 0** donuyor,
gunluk yazilmiyor, `ReadVmafScoresAsync` istisna firlatiyor. `File.Exists` iddiasi
artik bos degil — sessiz basarisizligin uretimdeki gercek bicimi bu. Olcunun adi ve
`kilit-olcusu.md`'deki cumlesi olctugu seye gore degistirildi.

**Tur 2 mutasyon ozeti** (her biri `--no-incremental` derlemeden sonra, 45 olcu):

| # | mutasyon | damga (UTC) | kirmizi |
| --- | --- | --- | ---: |
| M1 | `FrameLock` -> `"setpts=N"` | 06:45 | 4 |
| M2 | `FrameLock` -> `"settb=AVTB"` | 06:45-06:46 | 4 |
| M3 | `BenchOlcum.Basarisiz` 2 -> 0 | 06:46-06:47 | 1 |
| M4 | vmaf hatasi aninda yeniden firlatilir | 06:48-06:49 | **0** |
| M5 | kismi skor basilmaz | 06:47 | 1 |

Tabloyu ozetleyen cumle: dort mutasyonun besi degil, **dordu** kirmizi uretti; M4
uretmedi ve bu bir acik olarak yazildi. M1 ve M2'nin dordunun tamami tur 1'dekiyle
ayni olculer.

**Muhur notuna giden borclar** — tur 2'de acilmadi: paket ozet cumlesi kusuru
(yukarida duzeltildi), "T111 yanlis bir sey soylemedi" yumusakligi
(`kilit-olcusu.md`'de "sozlesmenin onculu curutuldu" diye yeniden yazildi),
K1/Sinirlar celiskisi (T0'in yazim hatasi, tartisilmadi).

**Beklenen bir sey yok**; CI kosumu izlenmedi.

## Tur 2 ciktisi

- `src/VidShrink.Ffmpeg/QualityMeter.cs` — `PartialScore` tasiyan istisna; vmaf
  hatasi artik xpsnr/ssim'i yutmuyor.
- `tools/VidShrink.Bench/Program.cs` — `BenchOlcum` (basarisizlikta cikis kodu 2,
  kismi JSON yine stdout'a), `BenchMeasureFilterGraph` (uretim `Build`'ine devrediyor).
- `tests/VidShrink.Tests/VmafPoolingTests.cs` — iki yeni saf olcu, bos iddia
  duzeltildi, dort cagri yeni tip adina tasindi. 45 olcu.
- `docs/olcumler/kilit-olcusu.md` — bolum 4 (tur 2) eklendi, bozuk zincir bolumu ve
  T111 cumlesi yeniden yazildi, olculmeyenler listesi buyudu.
- Dal `T119-kilit-olcusu`, commit `98e94ad`, itildi.
