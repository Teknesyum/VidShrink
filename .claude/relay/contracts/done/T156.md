---
id: T156
title: Uc pimsiz borc — Comparable kapisi, tek satira binen ayrim, bayat pim docstringi
role: builder
model: sonnet
depends: [T154]
status: done
round: 1
verify: [dotnet test -c Release --filter "QualityMeterTests|UretimYoluTests|OluUyeTests"]
owns:
  - src/VidShrink.Ffmpeg/QualityMeter.cs
  - tests/VidShrink.Tests/QualityMeterTests.cs
  - tests/VidShrink.Tests/UretimYoluTests.cs
  - tests/VidShrink.Tests/OluUyeTests.cs
  - docs/olcumler/uc-kucuk-borc.md
side_effects: []
---

## Baglam

T154 muhurlendi ve denetci uc borc birakti. Ucu de **olcum boslugu**: bugun kod dogru
calisiyor, ama yanlis yapildiginda hicbir sey kirmiziya donmuyor. Bu sozlesme ucunu de
kapatir. **Davranis degismez** — degisen yalnizca korumadir.

**Borc 1 — `Comparable` kapisi pimsiz.** `src/VidShrink.Ffmpeg/QualityMeter.cs:68`:

```csharp
if (!score.Comparable || score.VmafNegMean is null) return null;
```

T154 denetcisi `!score.Comparable ||` kismini sildi ve **45/45 yesil kaldi.** Yani
kiyaslanamaz bir olcum sonucu gecerli bir kalite olcumu gibi disari cikabilir ve hicbir
olcu bunu soylemez. Bu kapinin isi tam olarak budur: `Comparable: false` "bu iki klip
karsilastirilamaz" demektir, `VmafNegMean` degeri o durumda anlamsizdir.

Kapinin gercek bir girdiyle kirmiziya donmesi mumkun — `QualityMeterTests` bunu zaten
uretiyor: `HdrAndTonemappedSdrAreNotComparable` (`:78-94`) ve `:119` `Assert.False(...Comparable)`
ile HDR referans + tonemap edilmis SDR ornek ciftinde `Comparable: false` cikiyor.

**Borc 2 — ayrim tek silinebilir satira bindi.** `tests/VidShrink.Tests/UretimYoluTests.cs:312`:

```csharp
var tasinan = Assert.IsType<QualityMeasurement>(olcer).Scenes;
Assert.NotNull(tasinan);
```

T154 oncesinde iki kolu **tip** ayiriyordu (`SceneAwareQualityMeasurement` vs
`QualityMeasurement`); gövdeler birlesince iki kol da ayni tipi donduruyor ve ayrim
tumuyle `Assert.NotNull` satirina bindi. Bugun tasiyici — T154 denetcisi mutasyon (b) ile
olctu — ama **o tek satir silinirse olcu sessizce korelir** ve kimse fark etmez.

**Borc 3 — pim degisti, ustundeki cumle degismedi.** `tests/VidShrink.Tests/OluUyeTests.cs:363`
docstring'i hala "Sayi 27'den 26'ya indi (T150 tur 2)" diyor; ayni dosyanin `:484` satiri
"pimlenen 31 satir" diyor ve gercek sayi 31:

```
awk 'NR>=372 && NR<=442' OluUyeTests.cs | grep -c "^        new("   ->  31
```

**Bu depoda ayni kusur sinifinin ucuncu gorunusu** — pin degisirken ustundeki cumle
guncellenmiyor.

## Kabul kriteri

**K1 — `Comparable` kapisini pimle.** `QualityMeasurement.MeasureWindowAsync`in
kiyaslanamaz bir skorda **`null` dondugunu** olcen bir sey yaz; once kirmizi kaydet
(kapiyi kaldirip olcunun dustugunu goster, sonra geri al).

Yol secimi senin: (a) `QualityMeterTests`in HDR/tonemap ciftini kullanan gercek bir
ffmpeg kosumu, ya da (b) karari saf bir yardimciya cikarip (`QualityScore` ->
`WindowQualityMeasurement?`) onu dogrudan pimlemek. (b) daha ucuz ve daha kararli;
seciyorsan **davranis birebir ayni kalmali** ve cikarilan yardimci `internal` olmali.
Ikisini birden yapma.

`IQualityMeasurement` (`src/VidShrink.Core/IQualityMeasurement.cs`) `owns` kumende
**degil** — degistirmen gerekiyorsa **yapma, bildir.**
CHECK: kirmizinin ham metni + geri alinca yesilin ham metni.

**K2 — Ayrimi tek satirdan kurtar.** `UretimYoluTests.cs:312`teki iki kolun ayrimi bir
`Assert` satirinin varligina bagli olmasin. Iki kolu **ayni olcude yan yana** kur ve
`Assert.NotEqual` / `Assert.NotSame` gibi **iki tarafi da okuyan** bir iddiaya bagla —
boylece bir satir silmek olcuyu susturmak yerine kirar.

Var olan olcuyu **gevsetme**; T154'un mutasyon (b)'si bu turdan sonra da olmeli.
CHECK: eski/yeni olcunun tam metni + mutasyon (b)'nin yeniden kosumu.

**K3 — Bayat docstring'i duzelt ve dosyanin geri kalanini tara.** `OluUyeTests.cs:363`
gercek sayiyi soylesin. Duzeltirken **ayni dosyadaki her sayi iddiasini kendin say** —
`:484` ve varsa digerleri. Bu bir metin duzeltmesi degil, **bir tarama**: dosyada kac tane
sayi iddiasi var, kaci bugunku kaynakla tutuyor? Ham ciktiyi goster.

Pimin kendisini oynatma — 31 satir 31 kalir; degistirmen gerekirse **once neden
degistigini yaz.**
CHECK: taramanin ham ciktisi + duzeltilen her cumlenin eski/yeni hali.

**K4 — Mutasyon izgarasi.** Uc mutasyon, her biri ayri, her birinden once
`dotnet build -c Release --no-incremental`; **`--no-build` yasak:**
(a) `!score.Comparable ||` kismini sil — K1'in olcusu olmeli.
(b) `QualityMeasurement` ctor'unun harita parametresini yoksay — T154'un olcusu olmeli.
(c) K2'nin yeni iddiasindan **tek satir** sil — olcu **kirilmali**, sessizce gecmemeli.
Bir mutasyon hicbir olcuyu kirmiyorsa **yaz ve birak**; izgarayi doldurmak icin olcu uydurma.
CHECK: uc satirlik izgara, her hucrede kirilan olcu adlari ve ham cikti.

**K5 — Uc verify kolunu da say.** `--list-tests` ile kol basina test sayisi. T0'in
dagitimdan onceki olcumu: `QualityMeterTests` 32, `UretimYoluTests` 13, `OluUyeTests` 11.
**Bu sayilar senin dogrulamandan once bir iddiadir** — kendin say ve tutmuyorsa soyle.
Sifir bulan kol varsa teslim etme, bildir; vstest'in varsayilan operatoru alt-dize ve
sifir eslesen kol exit 0 ile sessizce gecer. Bu depoda **uc** muhurlu sozlesmede oldu.
CHECK: kol basina sayi + CI kosum kimligi ve `completed success`.

## Sinirlar

- `owns` disina yazma. `IQualityMeasurement.cs`, `PlanCalculator.cs`, `CodecModel.cs`,
  `FfmpegArguments.cs` ve `MainWindow.axaml.cs` **baskasinin** — ortadaki uc dosya su anda
  kosan T155'in.
- **Davranis degismez.** Bu bir pim sozlesmesidir. Var olan hicbir olcunun beklentisi
  gevsemez; degistirmen gerekiyorsa satir satir gerekce yaz.
- Bandi genisletme, esigi gevsetme, iddiayi zayiflatma yoluyla yesil uretme.
- "Hepsi", "hicbiri", "N satir" diyen her cumlenin altindaki listeyi **kendin say ve ham
  ciktiyi goster.** Bu depoda tablo dogru cikip onu ozetleyen cumle 26 kez yanlis cikti.
- **Filtresiz `dotnet test` kosturma** — bu makinede tum suit cokuyor, CI yesil.
- **`QualityMeterTests` gercek ffmpeg kosturuyor ve yavas.** Makine sakinken kos; kac
  ffmpeg sureci dogurdugunu yaz. Pencere acan / surec doguran arayuz olcusu kosturma.
- Gecici her sey `.calisma/T156/` altina. **`.calisma/kaynak/` paylasilan 3,5 GB —
  okuyabilirsin, yazma ve silme.**
- Kod yorumu yazma. K1 (b) yolunu secersen `QualityMeasurement`in docstring'i eskiyecek —
  **guncelle.** Bu sozlesmenin ucuncu borcu tam olarak bu kusur.
- `.claude/relay/**` T0'in alani; sozlesme dosyasina ve `LOG.md`e dokunma.

## Kayit noktasi

Dal: `T156-uc-kucuk-borc`, guncel `main`den ac, `origin`e it.
**`main`e birlestirme** — onu T0 yapar.

## Cikti

`docs/olcumler/uc-kucuk-borc.md` — K1'in kirmizisi ve secilen yolun gerekcesi, K2'nin
eski/yeni iddiasi, K3'un tarama ciktisi, K4'un uc satirlik izgarasi, K5'in kol sayilari
ve CI kimligi.

## T0 kusurlari — uc oncul, ikisi yanlisti (3 Eylul 2026)

Yapici uc oncul bildirdi. **Gelen duzeltme de bir iddiadir**; ucunu de bagimsiz olctum.

**1. K2'nin "sessizce korelir" cumlesi yanlisti.** Sozlesme "tek satir silinirse olcu
sessizce korelir" diyordu. Olculdu: T154 surumunde `tasinan` tipi `SceneMap?`, `Assert.NotNull`
onu daraltiyor ve alt satirda `tasinan.Scenes.Count` okunuyor. Satir silinirse daralma
kalkar ve derleme kirilir (`CS8602`) — bu depoda uyarilar hata sayiliyor. **Suit sessizce
gecmez, hic derlenmez.** Borc gercekti ama tarifi baskaydi: asil zayiflik haritasiz kolun
hic kurulmamasi, `null` sabitinin elle yazilmasiydi. Yapicinin cozumu (iki kolu da
`ProbeMeter` ile kurup tek `Assert.NotEqual` ile okumak) o zayifligi kapatiyor.

**2. K3'un "docstring bayat" onculu yanlisti.** Sozlesme `OluUyeTests.cs:363`teki
"Sayi 27'den 26'ya indi" cumlesinin bayat oldugunu, gercek sayinin 31 oldugunu soyluyordu.
Olculdu — olcunun kendi ciktisi:

```
uye: 129  bu dosyada adi gecmeyen: 93  pimlenen: 31
sifir tuketici: 26  hic kullanilmayan: 5
```

`OluUyeTests.cs:42` `Flagged => ZeroConsumer || Unused`. Yani **31 = 26 + 5**. Cumledeki 26
`ZeroConsumer` sayisidir ve **bugun dogrudur**; yaniltici olan tek sey konumu — 31 kayitlik
`Pinned` dizisinin ustunde duruyor.

**Kusur bende ve iki kere tekrarladi.** 26'yi T146'nin muhur notundan tasidim, **neyi
saydigini olcmedim**, T154'un muhur notuna "bayat" diye yazdim, oradan bu sozlesmenin
oncululune tasidim. Bu depoda 26 kez olculen kusur sinifinin ta kendisi: **sayi dogru,
onu ozetleyen cumle yanlis.** T154'un muhur notu duzeltildi.

**3. Worktree yolu yanlisti — bu da bende.** Ajana `...\Vidshrink\.claude\worktrees\T156`
dedim; worktree'yi `git -C <T0 worktree>` ile **goreli yol** vererek actigim icin gercekte
`...\Vidshrink\.claude\worktrees\T0\.claude\worktrees\T156` altinda olustu. T155'te de
ayni. Ajan dogru yolu kendi buldu ve `_sorun.log`a yazdi. Bundan sonraki `worktree add`
cagrilarinda **mutlak yol** kullanilacak.

Yapicinin `owns` disina cikmadigi olculdu: `git diff --name-only 53bea13 368bbf8` tam olarak
bes owns dosyasi. CI `33752378032` `success` `ab2557a`; `ab2557a..368bbf8` tek commit ve
yalniz rapora CI kimligini ekliyor.

## Muhur — GECTI (3 Eylul 2026)

Denetci bagimsiz klonda (`368bbf8`) olctu, **KRITIK yok**. Klon `git status` bos ciktiktan
sonra kaldirildi; denetci hicbir dosyaya yazmadi.

**K1 gecti.** Cikarilan yardimci `internal`, disari sizmamis — `QualityMeter.cs:78`:

```csharp
internal static VidShrink.Core.WindowQualityMeasurement? ToWindowMeasurement(
    QualityScore score, double referenceStartSeconds, long elapsedMilliseconds)
```

Denetcinin kendi mutasyonu (`!score.Comparable ||` silindi, `--no-incremental`):

```
AnIncomparableScoreNeverLeavesTheWindowMeasurement [FAIL]
   Assert.Null() Failure: Value is not null
   Actual: WindowQualityMeasurement { ... Comparable = True, ... }
Basarisiz: 1, Basarili: 24, Toplam: 25
```

Yolun (b) secilmesinin gerekcesi de dogrulandi: `Comparable: false` ureten iki yer
(`QualityMeter.cs:220`, `:252`) **ikisi de ilk dort alani `null` veriyor**, yani (a) yolu
mutasyonu olduremezdi.

**K2 gecti.** T154'un mutasyonu (ctor harita parametresini yoksay) hala oluyor:

```
Assert.NotEqual() Failure: Values are equal / Expected: Not 55 / Actual: 55
  UretimYoluTests.cs:line 330
Basarisiz: 1, Basarili: 24, Toplam: 25
```

**K3 gecti — alti duzeltilen cumlenin altisi da bugunku kaynakla tutuyor.** Denetci
her birini ayri ayri kaynaga karsi olctu: `:363` (31 = 26 + 5), `:368` (uretim=9,
tuketim=1), `:385` (uc okuyan satir), `:399` (2517-2519), `:407` (11 uye, dort okuma
yeri), `:423` (10 okuma, biri `!=`). **Dogru bir cumle yanlisla degistirilmemis.**

Pim oynatilmamis:

```
awk 'NR>=375 && NR<=445' OluUyeTests.cs | grep -c '^        new('   -> 31
gecmis: ab2557a 31 · 073a43a 31 · 89b00dd 31 · 008033a 32
```

**K5 gecti.** Denetcinin kendi `--list-tests` sayimi: `QualityMeterTests` 33,
`UretimYoluTests` 13, `OluUyeTests` 11; birlesim **57**, cakisan kol yok, sifir bulan kol
yok. T0'in dagitim oncesi 32'si K1'in ekledigi tek testle 33 olmus.

`git diff --name-only 53bea13 368bbf8` tam bes `owns` dosyasi, tasma yok.
CI `33752378032` `success` `ab2557a`.

**T0'in birlesme sonrasi kendi olcumu.** `main`de `dotnet build -c Release --no-incremental`
(0 uyari, 0 hata), ardindan uc kol:

```
Basarili! - Basarisiz: 0, Basarili: 57, Atlanan: 0, Toplam: 57
```

Denetcinin klonda olctugu sayinin ayni — birlesmede kaybolan olcu yok.

### Borclar

1. **Raporun ozet cumlesi kendi tablosuyla tutmuyor — bu sinifin 27. gorunusu.**
   `docs/olcumler/uc-kucuk-borc.md:127-128` "24 iddiadan **18'i tuttu**, 6'si bayatti"
   diyor. Denetcinin mekanik sayimi:

   ```
   6 bayat / 17 tuttu / 1 yaniltici     (toplam 24)
   ```

   Tablo dogru, cumle yanlis: 18 ancak "yaniltici" satiri "tuttu" sayilirsa cikiyor.
   Kod, olcu ve tablonun kendisi dogru; yanlis olan tek sey `docs/` altindaki bir sayi.

2. **Butun `Assert.NotEqual` deyimini silmek sessizce geciyor** (denetcinin olcumu:
   `Basarili: 13, Toplam: 13`). Yazili kriter "tek satir" diyordu ve o kosul saglaniyor —
   tek satir silmek `CS1501` ile derlemeyi kiriyor. Tek iddiali hicbir test kendi
   iddiasinin **yoklugunu** olcemez; genel sinir, bu sozlesmenin kusuru degil.

3. **K3'un duzelttigi satir numaralari hala pimsiz.** `PlanCalculator.cs:342`,
   `MainWindow.axaml.cs:2284` ve `:2517-2519`. Bu sozlesme **bugunku metni** duzeltti,
   bayatlama mekanizmasini degil. Ayni cumleler yeniden bayatlayabilir ve hicbir olcu
   kirmiziya donmez.

4. **Dal ucu `368bbf8` CI gormemis**; yesil okuma `ab2557a`da. Sebep kasitli —
   `ci.yml` `paths-ignore: docs/**` tasiyor ve son commit yalniz `docs/`. Yine de
   "dal ucu yesil" cumlesi ucu degil bir alttakini gosteriyor.

5. `UretimYoluTests.cs:326-332` ayni iki degeri bir kez `WriteLine`, bir kez `Assert`
   icin hesapliyor — dort `AggregateVmaf` cagrisi. Saf fonksiyon, sonuc ayni.

**NOT.** Sozlesmenin "K1 (b) secersen `QualityMeasurement` docstring'i bayatlar" onculu
bosa dustu: o sinifta hic docstring yok (`grep -c '///' QualityMeter.cs` -> 0).
Bu, **T0'in dorduncu yanlis onculu** bu sozlesmede.
