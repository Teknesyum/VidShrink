---
id: T184
title: Onizleme — ses, tekerlek zoom, rozetler, duraklat/devam
role: builder
model: opus
status: done
depends: []
owns:
  - src/VidShrink.App/Playback/**
  - src/VidShrink.App/Locales/tr/playback.json
  - src/VidShrink.App/Locales/en/playback.json
  - tests/VidShrink.Tests/PlaybackPanelTests.cs
  - tests/VidShrink.Tests/CasingTests.cs
  - tests/VidShrink.Tests/ComparisonPanelTests.cs
  - tests/VidShrink.Tests/ZoomGestureTests.cs
branch: T184-onizleme-dortlusu
---

## Amac

Onizleme paneli urunun en ikna edici ani; bugun dort ayri yerden sakat. Kullanici
bunlari uc ayri turda soyledi. Yol haritasi: `docs/plan.md` bolum A.

## Bag lam

`src/VidShrink.Ffmpeg/Playback/` altinda ses yolu **bastan sona yazilmis**:
`AudioSink.cs` NAudio ile 48 kHz / 16 bit / stereo cikis kuruyor,
`DecoderPipe.SeekAudio` ffmpeg'den ayri bir PCM borusu aciyor. Eksik olan tek sey
`VidShrink.App` icinde `AttachAudioSink` cagrisi — `grep AttachAudioSink src/VidShrink.App/`
bos donuyor, bu yuzden `_sink` daima null ve `SeekAudio` ilk satirda geri donuyor.

Rozet metinleri `src/VidShrink.App/Playback/ComparisonPanel.axaml.cs` icinde
`LeftBadgeText` / `RightBadgeText` / `ApproxBadgeText` uzerinden yaziliyor.

## Kabul kriteri

### K1 — Onizlemede ses cikiyor

`VidShrink.App` onizleme baslatildiginda `AttachAudioSink` cagriliyor; sesi olan bir
klipte `HasAudio` true iken `SeekAudio` erken donmuyor. Sessiz klipte cikmiyor ve
hata vermiyor.

CHECK: `grep -rn "AttachAudioSink" src/VidShrink.App/ | wc -l` sifirdan buyuk
CHECK: `dotnet test VidShrink.sln -c Release --filter "Playback"` yesil

### K2 — Tekerlek zoom %100 ile %200 arasinda calisiyor

Fare tekerlegi onizleme alani uzerinde zoom degerini degistiriyor; %100 ve %200
sinirlarinda takilmiyor. Zoom girip cikarken kare **kararmiyor** — mevcut kararma
kusuru gideriliyor.

CHECK: zoom degerinin tekerlek olayindan degistigini gosteren bir test yesil

### K3 — Rozetler

`playback.approximate-preview` ("Yaklasik onizleme") kalkiyor. Yerine orta panelin
**solunda ustte** `ORIJINAL`, **saginda ustte** `ISLENMIS · CRF <x>` — `<x>` motorun
o an hesapladigi CRF. Iki dilde de karsilik yazilir.

Rozetler perde hareket ederken **sabit durur**; ortulen tarafin etiketi soner.

CHECK: `grep -rn "approximate-preview" src/ | wc -l` sifir

### K4 — Duraklat/devam basa sarmiyor

Ayar degismeden durdur/baslat yapilinca onizleme kaldigi yerden devam ediyor,
bastan isleme girmiyor.

CHECK: duraklat/devam sonrasi konumun korundugunu gosteren test yesil

### K5 — Temizlik

Kod yorumu (`//`) eklenmiyor; `///` XML belge satirlari serbest. Dosyalara BOM
eklenmiyor. `owns` disina yazilmiyor.

## Kayit noktasi

Her kabul kriteri bitince commit at ve dala it. Yarim kalirsa bir sonraki ajan
kaldigi yerden devam edebilsin.

## Cikti

Her kriter icin calistirilan komut ve ciktisi. Olculemeyen yer "olcemedim" diye
yazilir, uydurulmaz.


## Muhur

- Muhurleyen: T0, 7 Eylul 2026
- Yapici: opus, dal `T184-onizleme-dortlusu`, sha `bf9c233a`
- Denetim: bir tur, **KRITIK bulgu vardi** — kodda degil sozlesmede; owns genisletilerek kapatildi
- Denetcinin dort kabul kriteri icin karari: **dordu de olculu ve yesil**

### KRITIK bulgu ve kapanisi

Denetci K5'in "owns disina yazilmiyor" sartinin delindigini gosterdi:
`CasingTests.cs`, `ComparisonPanelTests.cs`, `ZoomGestureTests.cs` owns'ta yoktu ama
uculine de yazilmisti. **Yapici hatasi degil, T0 hatasi:** K3 `approximate-preview`
anahtarinin kalkmasini emrediyor, o anahtari pinleyen test owns disindaydi; K3 buyuk
harf istiyor, onu yasaklayan test owns disindaydi. Sozlesme kendi kendisiyle celisiyordu.
Owns uc satirla genisletildi, yeni tur acilmadi.

### Kok nedenler — ikisi de T0'in ongordugunden baska cikti

**Ses.** Sozlesme ve yol haritasi "VidShrink.App hicbir yerde `AttachAudioSink`
cagirmiyor, grep bos donuyor" diyordu. **Yanlisti** — `PlayerView.axaml.cs:292` zaten
cagiriyordu. Gercek bosluk oynaticida degil **karsilastirma panelindeydi**:
`PipeComparisonFrameSource` yalniz ham video tasiyor. Cozum kaynak dosyaya ayri bir
`DecoderPipe` acip `AudioSink`'i ona takmak oldu (`PreviewAudio.cs`).
Sozlesmenin K1 CHECK'i (`grep ... sifirdan buyuk`) bu yuzden **ayirt edici degildi**:
is yapilmadan once de yesildi.

**Zoom.** Panelin bandindaki yuva yildiz olculu (`MainWindow.axaml:594`), kabuk kendi
boyunu isteyerek buyutemiyor. Yapicinin ilk denedigi "band boyunu olcekle" yaklasimi
olcumde coktu (`want=1227,6 desired=1060 bounds=990`) ve geri alindi. Bandda buyuyen
sey artik goruntunun kendisi. Kararma **ayri bir kusurdu**: `ComparisonSurface.Configure`
her cagrida panoyu yeniden yaratiyordu.

### Denetcinin kendi kosumu

| olcum | sonuc |
|---|---|
| `--filter "Playback"` | 42 basarili, 2 atlanan, 44 toplam, 5 sn |
| `~PlaybackPanelTests` | 8 test, **8'i de kostu**, hicbiri atlanmadi (ffmpeg bu makinede var) |
| genis kol (denetcinin kendi dizgesi) | 231 basarili, 2 atlanan, 233 toplam, 1 dk 29 sn |
| `grep AttachAudioSink src/VidShrink.App/` | 4 |
| `grep approximate-preview src/` | 0 |
| eklenen satirda `//` | 0 |
| yeni dosyalarda BOM | yok |

Denetci `--no-build` **kullanmadan** olctu.

### Uc owns-disi dosyanin degerlendirmesi

- `ComparisonPanelTests.cs:1143` — **mesru**. K3 anahtarin kalkmasini emrediyor, pim
  zorunlu olarak eskiyordu. Yerine gelen satir ayni sertlikte, gevsetme yok.
- `CasingTests.cs` — **pim tazelemesi degil, ev kuralina yeni istisna.** Yapicinin
  ucunu birden "pim yeniden temellendirmesi" diye etiketlemesi yanlis.
- `ZoomGestureTests.cs` — "eski test yanlis bir sey pinliyordu" iddiasi **yanlis**.
  Eski test T52 kodunda gercekten dogru olan bir ozelligi pinliyordu; K2 o ozelligi
  bilerek kaldirdi. Davranis degisti, test yanlis degildi. Yeni test daha siki;
  denetci elle dogruladi (viewport 800x450, kaynak 1600x900, FitScale=0.5).

### Ev kuralina acilan delik — T0 karari

Sozlukte "tumu buyuk harf" yasagina `playback.badge.original` ve
`playback.badge.processed` icin istisna acildi. **Onaylandi.** Kullanicinin cumlesi
rozetlerin buyuk harf olmasini istiyor; ev kurali bagiran metni yasakliyor; cumle kazanir.
Delik anahtar duzeyinde, ordinal, jokersiz — baska hicbir metin kacamiyor.
Yapicinin kendi actigi deligi kendi onaylamadigi denetciyle dogrulandi.

### Borclar

1. **Delik anahtarin degerini sinirlamiyor.** `CasingTests.cs:89-93` o iki anahtarin
   altina ne yazilirsa yazilsin susar. Bugun `PlaybackPanelTests.cs:223-226` dort degeri
   birebir pimliyor ama iki dosya birbirine **atif yapmiyor**; o pim kalkarsa delik
   sessizce genisler. Muhurden once `CasingTests` docstring'ine bu bagimlilik yazildi.
2. **"Terfi esiginde gorunur boy sicramiyor" iddiasi pimsiz.** `ZoomGesture.cs:153-155`
   docstring'de soyluyor, hicbir test iki yani karsilastirmiyor; test
   `if (panel.Shelter != ShelterStage.Band) break;` ile esigin ustune hic bakmiyor.
3. **`ZoomGesture.cs:219` vs `:224` siralamasi.** Terfi centiginde ofset eski kademe
   formuluyle kelepceleniyor, kademe degisince yeniden kelepcelenmiyor. Gercek uygulamada
   `SetViewport` -> `ClampOffset` kendi kendine duzeltiyor; saf jest duzeyinde bir tur
   bayat kaliyor. Denetci gercekci girdide yanlis cikti gosteremedi.
4. **`SyncAudioAsync` govdesi ciplak `catch { }`**, `PreviewAudio.AttachAsync` da acilis
   hatasini yutup sessizce `Attached=false` birakiyor. Ses hic gelmezse kullaniciya da
   gunluge de tek satir cikmiyor. Bu depoda ayni desen daha once sizintiyi sekiz kat
   buyutmustu.
5. **`PanelHost.cs:404` `SetNotice("playback.panel.first-frame")` olu kaldi.** K2'nin
   istenen yan etkisi: `Configure` kareyi atmadigi icin `EmptyState.IsVisible` false
   kaliyor, `EmptyHint` yalniz orada yasiyor.
6. **`ComparisonPanel.axaml.cs:309-310`** klavye adimi sabiti `SplitKeyStep` (0.01)
   ikinci bir anlamda "tamamen ortuldu" esigi olarak kullaniliyor; iki kavram tek sabite
   bagli. `_split == 0.01` noktasinda sol rozet, sol yarinin %1'i hala gorunurken soner.
7. **`ComparisonSurface.Configure` ayni olcude `Repaint()`'e varmadan donuyor** (`:139`);
   onceden ayni olcude de yeniden cizim tetikliyordu.
8. **Gozle dogrulama yapilmadi.** Ekran kapisi kapali; rozetlerin ve zoom'un gorunusu
   olculmedi, yalniz duzen ve olcek sayilari pimlendi. Kullaniciya soruldu.

### Muhurden once T0'in yaptigi iki duzeltme

- `CasingTests.cs` docstring'i `ORIJINAL`/`ISLENMIS`'i hala "esigin yakaladigi bagiran
  kelime" ornegi diye anlatiyordu; artik muaflar. Tazelendi, ustune borc 1'deki
  bagimlilik yazildi.
- `ZoomGestureTests.cs` `Olcek_buyurken_goruntu_panoya_sigmis_kalir` docstring'i "jest
  goruntuyu buyutmez" diyordu; bandda artik buyutuyor. Tazelendi.

Ikisi de bu deponun imza kusuru: kod ve tablo dogru, onu ozetleyen cumle yanlis.
