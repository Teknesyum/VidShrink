---
id: T171
title: Kabuk kucultme istegi uretimde tuketilmiyor — hedef boyut sessizce dusuyor
role: builder
model: sonnet
depends: [T169, T170]
blocked-by: []
status: done
result: passed
round: 3
verify:
  - dotnet test --filter "ShrinkRequestTests|ShellIntegrationTests|KabukIstegiTests"
  - dotnet test --filter "OluUyeTests"
owns:
  - src/VidShrink.App/Program.cs
  - src/VidShrink.App/ShrinkJobWindow.axaml
  - src/VidShrink.App/ShrinkJobWindow.axaml.cs
  - src/VidShrink.App/App.axaml.cs
  - src/VidShrink.App/Locales/en/main.json
  - src/VidShrink.App/Locales/tr/main.json
  - tests/VidShrink.Tests/KabukIstegiTests.cs
  - tests/VidShrink.Tests/OluUyeTests.cs
  - docs/olcumler/kabuk-istegi-tuketimi.md
side_effects: []
---

## Baglam

T169 kayit defteri girdilerini yazdi, T170 arguman cozumunu ve tek kuyrugu yazdi.
**Ucuncu parca yok**: uygulama bu istegi tuketmiyor.

Tek giris noktasi `src/VidShrink.App/Program.cs:11`:

```csharp
public static void Main(string[] args)
    => Build(ShellIntegration.ResolveStartupPath(args)).StartWithClassicDesktopLifetime(args);
```

`ResolveStartupPath` yalniz **var olan ilk dosya yolunu** bulur; `--kucult` bayragina ve
ondan sonraki hedef boyuta bakmaz (`src/VidShrink.Core/ShellIntegration.cs:38-52`,
docstring'i de "Uzantiya bakmaz" diyor). Sonuc: kullanici sag menude
**"VidShrink ile Kucult -> 250 MB"** dedigi anda ana pencere aciliyor, dosya yukleniyor,
**secilen 250 MB kayboluyor**. Program hicbir sey yanlis yaptigini soylemiyor.

Kullanmayi bekleyen, **calisir durumda** ve uretimde sifir tuketicisi olan iki duzenek var:

| duzenek | dosya | bugunku tuketicisi |
|---|---|---|
| `ShrinkRequestResolver.Resolve(args, quickTargets)` | `src/VidShrink.Core/ShrinkRequest.cs:25-60` | uretimde yok, yalniz `ShrinkRequestTests` |
| `ShrinkRequestQueue` (mutex + adlandirilmis boru) | `src/VidShrink.Core/ShrinkRequest.cs:62-217` | uretimde yok |

`ShrinkArgumentResult.Problem` alanini `src/**` altinda okuyan **tek satir yok** — bunu
`OluUyeTests` pimi bugun borc olarak yaziyor: `ShrinkArgumentProblem`'in bes uyesi de
`hic-okunmayan-tur` bicimiyle pimde duruyor.

Kodcozme/kodlama tarafinda pencere gerektirmeyen bir API zaten var:
`EncodeRunner.RunAsync` (`src/VidShrink.Ffmpeg/EncodeRunner.cs:87`), ilerlemeyi
`EncodeProgress(Fraction, Elapsed, Remaining, OutputMb, Stage)` ile veriyor.

Hizli boyut listesi iki yerde **sabit** yaziyor ve bugun kullanici ayari degil:
`ShellIntegration.QuickShrinkTargetsMegabytes` ve `Install-VidShrink.ps1:191`
(`100, 250, 500, 1024, 2048`). **Bu sozlesme o listeyi degistirmiyor.**

Bu kusur bu depoda olculmus bir sinifin yeni ornegidir: *istek yeni giris noktasinda
sessizce dusuyor.* T165'te ayni kusur dort turda yedi giris noktasinda bulundu. Burada
giris noktasi **bir** gorunuyor — ama once **say**, varsayma.

## Kabul kriteri

**K1 — Once dusen istegi olc, sonra duzelt.** Kurulu uygulamayi degil, `dotnet run` ile
yapiyi kullan. `--kucult 250 <dosya>` ile baslat ve **bugunku** davranisi ham ciktiyla
goster: hangi pencere aciliyor, hedef boyut nereye gidiyor. Ayni sekilde `ShrinkFlag`
tasiyan **her** kabuk komut satirini kayit defterinden oku (`Install-VidShrink.ps1`
`Write-ShellShrinkMenu`) ve kac ayri giris noktasi oldugunu **say**.
CHECK: giris noktasi sayisi + tam liste, ve bugunku davranisin ham ciktisi.

**K2 — Istek tuketilsin, ana pencere acilmasin.** `--kucult` ile baslatildiginda:

- ana pencere **acilmaz**;
- gorev cubugunda kucuk, kapatilabilir bir **ilerleme penceresi** acilir
  (`ShrinkJobWindow`) ve `EncodeProgress` degerlerini gosterir;
- kodlama biterse pencere kendini kapatir, ciktinin yolu kullaniciya gosterilir;
- ayni anda birden cok dosya secilip kucultulurse `ShrinkRequestQueue` devreye girer:
  **ikinci surec kendi kodlamasini baslatmaz**, istegi boruyla sahibe verir. Kayit
  defteri girdilerinde `MultiSelectModel=Player` yazili, yani Windows tek surece
  birden cok `%1` gondermez — **bunu dogrula, varsayma.**

CHECK: iki kosum — tek dosya, ve ayni anda uc dosya. Her ikisinin ham ciktisi, acilan
pencerelerin sayisi ve baslatilan `ffmpeg` surec sayisi.

**K3 — Bes gerekcenin besi de kullaniciya ulassin.** `ShrinkArgumentProblem`'in
`NoTarget`, `TargetNotANumber`, `TargetNotPositive`, `TargetNotInQuickList`, `NoPath`
uyelerinin **her biri** icin kullanicinin gordugu bir cumle olsun. Pencere kalir, tek
satir neden ve "Uygulamada ac" dugmesi. Cumleler iki dilde yazilir
(`Locales/en`, `Locales/tr`); `LanguageTests` iki dilde ayni metni kabul etmez.

Bu K bittiginde `OluUyeTests` pimi **kirmizi olacak** — bes satir artik tuketiliyor,
`hic-okunmayan-tur` degil. Pimi **duzelt**: bes satiri dusur, satir sayisini ve
docstring'deki degisim anlatisini olcunun kendi ciktisindan gelen sayilarla guncelle
(`uye: N  bu dosyada adi gecmeyen: N  pimlenen: N` satiri K2 olcusunun ciktisinda basilir).
CHECK: bes gerekcenin bes ekran cumlesi, ve pimin guncel hali + olcunun ham sayim satiri.

**K4 — Mutasyon.** Uc mutasyon, her birinden once
`dotnet build -c Release --no-incremental`; **`--no-build` yasak:**
(a) `Program.cs`te istegi tuketen kolu kaldir, eski `ResolveStartupPath` davranisina don —
    bir olcu olmeli;
(b) `TargetNotInQuickList` gerekcesinin ekran cumlesini sessiz `return`e cevir —
    bir olcu olmeli;
(c) `ShrinkRequestQueue`un sahiplik kontrolunu her zaman `true` yap (her surec kendi
    kodlamasini baslatsin) — bir olcu olmeli.
CHECK: uc satirlik izgara, kirilan olcu adlari, ham cikti.

**K5 — Kol sayisi.** `verify` satirlarinin her birini `--list-tests` ile kos. Sifir
bulan kol varsa teslim etme, bildir; vstest'in varsayilan operatoru alt-dize ve sifir
eslesen kol exit 0 ile sessizce gecer. Bu depoda **dort** kez oldu.
CHECK: her filtre kolu icin test sayisi.

## Sinirlar

- `owns` disina yazma. `src/VidShrink.Core/ShrinkRequest.cs`, `ShellIntegration.cs`,
  `EncodeRunner.cs` ve `Install-VidShrink.ps1` **okunacak, yazilmayacak** — bu sozlesme
  cekirdegi degil, **tuketicisini** yaziyor. Cozum cekirdek degisikligi gerektiriyorsa
  **yazma, bildir.**
- `MainWindow.axaml` ve `MainWindow.axaml.cs` de `owns` disinda. Ilerleme penceresi ayri
  bir pencere; ana pencereyi yeniden duzenleme.
- Hizli boyut listesini (`100, 250, 500, 1024, 2048`) degistirme, uzatma, ayara baglama.
- Renk ve olcu uydurma — yalnizca `src/VidShrink.App/Themes/Theme.axaml` belirtecleri.
- Kod yorumu yazma.
- **Filtresiz `dotnet test` kosturma** — tum suit bu makinede 29 dakika suruyor.
  Filtreli kosumlar **on planda** kosar.
- Gecici her sey `.calisma/T171/` altina. `.calisma/kaynak/` paylasilan — okuyabilirsin,
  **yazma ve silme.**
- "Bes", "hepsi", "hicbiri", "tek giris noktasi" diyen her cumlenin altindaki listeyi
  **kendin say ve ham ciktiyi goster.** Bu depoda tablo dogru cikip onu ozetleyen cumle
  26 kez yanlis cikti.
- `.claude/relay/**` T0'in alani; sozlesme dosyasina ve `LOG.md`e dokunma.

## Kayit noktasi

Dal: `T171-kabuk-istegi-tuketimi`, guncel `main`den ac, `origin`e it.
**`main`e birlestirme** — onu T0 yapar.

## Cikti

`docs/olcumler/kabuk-istegi-tuketimi.md` — K1'in giris noktasi sayimi ve bugunku
davranisin ham ciktisi, K2'nin iki kosumu (pencere ve surec sayilariyla), K3'un bes
cumlesi ve guncel pim sayimi, K4'un uc satirlik izgarasi, K5'in kol sayilari.

## Tur 1 - denetim bulgulari (HUKUM: KALDI, 2 kritik, 6 borc)

**Bagimsiz dogrulanan gecen taraflar.** K1 kayit defteri sayimi **tam tuttu**: denetci
`k1-kayit-defteri-tam.tsv`yi kendi ayristirdi - 120 satir, 120'sinde `--kucult`, her
satirda `%1` sayisi tam **1** (120/120), `%*` **0**, `MultiSelectModel` **120/120 Player**,
hedef normalize edilince tek sablon, farkli uzanti 24, 24x5=120. Kalan 24 = duz `VidShrink`
fiili; `Install-VidShrink.ps1:299-311` (uzanti basina 1) + `:313-336` (uzanti basina 5)
kaynaktan dogrulandi, 144 = 120+24. Kayit yazimi yalniz `<Root>\SystemFileAssociations`
altina (`Get-ShellMenuAssociationRoot`, `:253-255`); sayim betigi tam o agaci geziyor,
eksik dal yok.

Giris noktasi iddiasi da dogru: `src/VidShrink.App/Program.cs:49`,
`src/VidShrink.Launcher/Program.cs:29`, `src/VidShrink.ShellExtension/*.cpp:90`. Launcher
argumanlari `start.ArgumentList.Add` ile aynen iletiyor, tuketmiyor; kabuk uzantisi
`GetItemAt(0)` ile tek yol alip `ShellExecuteW`e veriyor, `--kucult` uretmiyor;
`EnumSubCommands` `:125-130` gercekten `E_NOTIMPL`. `MainWindow.axaml.cs` komut satirina
hic dokunmuyor. **T165'teki yedi-giris kusuru burada yok.**

K3 pim satiri denetcinin kendi kosumunda **birebir ayni**:
`uye: 162  bu dosyada adi gecmeyen: 116  pimlenen: 32`, `mesru: 10  borc: 22`; 27+5=32.
K4'un uc mutasyonu denetci tarafindan **kendi uygulandi** (her birinden once temiz
`-c Release --no-incremental`): (a) 2/42, (b) 1/43, (c) 1/43 - ucu de birebir tuttu.
K5: `--list-tests` 44 (16+9+19) ve 13, sifir bulan kol yok, kosumlar 44/44 ve 13/13;
`LanguageTests|LocalizationTests|CasingTests|VisibleTextTests|ThemeTokenTests` 90/90.
`owns` temiz: `git diff main...HEAD --name-only` 9 dosya, hepsi `owns` icinde;
`ShrinkRequest.cs`, `ShellIntegration.cs`, `EncodeRunner.cs`, `Install-VidShrink.ps1`,
`MainWindow.axaml`, `MainWindow.axaml.cs` el degmemis; hizli liste iki yerde de
`{100, 250, 500, 1024, 2048}`. Renk/olcu temiz: `ShrinkJobWindow.axaml`teki **17
belirtecin 17'si** `Themes/Theme.axaml`te tanimli, tek ciplak sayi
`ProgressBar Minimum="0" Maximum="1"`.

**"2 pencere" savunmasi ayakta.** `gozle.ps1:60,69-70,77,82` - `$pencereGoruldu` bir
hashtable, anahtari `baslik | olcu`, hem satir sonu hem ozet ayni `Count`u basiyor:
kumulatif kume sayisi, es zamanli pencere sayisi degil. `k2-tek-dosya.txt:4,44` iki
`PENCERE` satirinda da pid **7460**; 436x272 -> 436x290 yalniz yukseklik, `BtnReveal`in
gorunur olmasiyla uyumlu, kodlama bitisiyle ayni saniye.

**"Ana pencere acilmaz" kodla kanitli** (rapor gerekcesini yalniz piksele dayandirmis):
`App.axaml.cs:42-49` `_shrink is not null` kolunda `desktop.MainWindow = new
ShrinkJobWindow(...)`, `MainWindow` **hic kurulmuyor**; `MainWindow` yalniz
`ShrinkJobWindow.OnOpenInApp` (`:279`) icinde doguyor. `MainWindow.axaml:8-9`
`Width="1560" Height="1060" WindowState="Maximized"` - olculen 436 px o olamaz.

### KRITIK 1 - Kusur yeni giris noktasina kaydi, yazmama gerekcesi tutmuyor

`kabuk-istegi-tuketimi.md:151-155`. Yapici dogru olcmus
(`k2-player-tek-surec-uc-yol.txt`): tek surece
`--kucult 100 buyuk-1.mp4 buyuk-2.mp4 buyuk-3.mp4` verilince **ilk dosya kuculuyor, kalan
ikisi sessizce dusuyor**. T171'in kapatmak icin yazildigi kusur sinifi - *"istek yeni
giris noktasinda sessizce dusuyor"* - yeni giris noktasinda aynen duruyor.

Rapor "duzeltmesi cekirdek degisikligi gerektiriyor" diyor; **gerektirmiyor**:

- `ShrinkRequest.cs:27-60` `Resolve(args, quickTargets)` **saf bir fonksiyon**, arguman
  listesini disaridan aliyor. `Program.cs:38` bugun onu bir kez cagiriyor; kalan yollari
  gezip **yol basina bir kez** cagirmak `ShellShrinkStartup.From` icinde, yani `owns`
  icinde biter.
- Pencere zaten cokluya hazir: `ShrinkJobWindow.axaml.cs:75`
  `private readonly Queue<ShrinkRequest> _pending`, `:155-160` `Accept()`, `:175-193`
  `PumpAsync()` sirayla kuyrugu bosaltiyor. Boru uzerinden gelen N istek icin var olan
  duzenek ayni argv'den gelen N istek icin de calisir.

Ayrica K2 bunu acik bir CHECK olarak yazmisti: *"Windows tek surece birden cok `%1`
gondermez - bunu **dogrula, varsayma**."* Rapor `:251-252`de bunun **olculmedigini**
kabul ediyor. Uc ayagin ucu de eksik: varsayim olculmedi, "gelirse ne olur" olculdu ve
kotu cikti, duzeltme kapsam disi degil.

### KRITIK 2 - K2'nin ana iddiasinin ham dayanagi yok

`kabuk-istegi-tuketimi.md:126, 129-130`. Rapor `:129`: *"Uc istek de tam bir kez islendi,
**hicbiri** dusmedi, ikinci ve ucuncu surec kendi kodlamasini baslatmadi."* Sozlesme
`:132-134` bunu acikca yasakliyor.

Denetci `k2-uc-dosya.txt`nin 129 satirini okudu: **tek bir cikti dosyasi adi ya da boyutu
gecmiyor**. Tum `.calisma/T171/*.txt` icinde `98,5` dizgesi **hic yok**; `98,4` yalniz
`k2-player-tek-surec-uc-yol.txt:5`te, o da baska bir kosum. `.mp4` dosyalari da silinmis.
`:126`daki `98,5 / 98,5 / 98,4 MB` ucluisu ve `:97`deki `98,4 MB` **kaynaksiz**.

Bu sus sayisi degil, K2'nin cekirdek kaniti. Ham gunlukte `VidShrink.App sureci`
**112 ornegin 112'sinde 1**; gozlemci **hicbir an** 2-3 surec gormedi. Uc surecin
basladigi yalniz `baslat:` satirlarindan, ikisinin *istegi boruyla verip* ciktigi ise
**hicbir yerden** okunmuyor - cokmus olsalar gunluk birebir ayni gorunurdu. Ayirt eden
tek delil uc ciktinin varligiydi ve o delil dosyada yok. Rapor `:4` ayrica *"her sayinin
yaninda onu ureten dosyanin yolu var"* diyor; bu cumle kendisi yanlis.

### Borc bulgulari

1. `kabuk-istegi-tuketimi.md:95` `| en cok es zamanli ffmpeg | 2 (ffprobe + kodlayici) |`
   - **parantez yanlis**. `gozle.ps1:75` `Get-Process ffmpeg` cagiriyor; `ffprobe` bu
   sayiya **hic girmez** (ayri surec adi). Sayi 2 dogru, aciklamasi yanlis - imza kusuru.
   Ikinci es zamanli ffmpeg'in gercek kaynagi buyuk olasilikla
   `ComplexityProbe`/`CalibrationProbe` (`ComplexityProbe.cs:785,852,972`); `EncodeRunner`
   tek seferde bir ffmpeg kosuyor.
2. `:72` `buyuk-1/2/3.mp4, her biri 129,735 MB, 40 s, 1920x1080` da **kaynaksiz**;
   `.calisma/T171/` altinda bu uc sayiyi ureten hicbir cikti yok.
3. `:51` K1 taban kosumunun gozlemci betigi **korunmamis**. `k1-bugun.txt` `sinif=` basiyor
   ve `olcu=` basmiyor; `gozle.ps1` tersini yapiyor. K1'i ureten surum K2'ninkiyle uzerine
   yazilmis, o kosum yeniden uretilemez.
4. `:94` "ana pencere acildi mi -> hayir, 436 piksel genislik" tek basina zayif cikarsama
   ve `KabukIstegiTests.cs`te bunu tutan **hicbir olcu yok**. `App.axaml.cs:42-49` bir gun
   degisirse sessizce geri doner.
5. `KabukIstegiTests.cs:132` `Assert.False(string.IsNullOrWhiteSpace(text.MessageText))`
   **gevsek**: bes gerekcenin hepsi ayni cumleyi bassa bu olcu olmez; mutasyon (b)'yi
   yalniz `:131`deki `State` kontrolu yakaladi.
6. `.calisma/T171/cikti.txt` (0 bayt) ve `hata.txt` **artik**. `hata.txt` ara bir
   gelistirme cokmesini (`ShrinkJobWindow..ctor` `NullReferenceException`) tasiyor,
   rapora girmiyor, bugunku koda karsiligi yok.
7. *(gozlem, T171'in kusuru degil)* Bes gerekce cumlesi `LanguageCatalog.Display`
   (`LanguageCatalog.cs:171-172`) uzerinden gectigi icin Ingilizcede tam cumle Title Case
   basiliyor: *"The Target Size That Came From The Shell Menu Is Not A Number, So It Cannot
   Be Used As A Size Budget."* Depo geneli boyle ve `CasingTests` yesil; ama govde metni
   icin bes uzun cumle bu bicimi ilk kez gorunur kiliyor.

## Tur 2 - kapsam (T0)

**K6 - Coklu yol tek argv'de dusmesin.** `ShellShrinkStartup.From`u coklu yol dondurecek
bicimde genislet: bayraktan sonraki **var olan her yol** icin bir `ShrinkRequest`.
`ShrinkJobWindow.Begin` hepsini `Accept` etsin - kuyruk (`:75`, `:155-160`, `:175-193`)
zaten var. Denetci bunun `owns` icinde bittigini gosterdi; `ShrinkRequest.cs`e
**yazmadan** cozulur. Cozum yine de cekirdek degisikligi gerektiriyorsa **yazma, bildir**
ve neden gerektirdigini kodla goster.
CHECK: tek argv'de uc yol -> uc istek; gercek kosumun ham ciktisi (uc cikti dosyasinin
adi ve boyutu) ve `AcceptedCount == 3` diyen bir olcu.

**K7 - K2'yi kanitiyla birlikte yeniden kos.** Iki kosumu (tek dosya, ayni anda uc dosya)
tekrarla ve gozlemci betigine **sonda cikti dokumu** ekle:
`Get-ChildItem *_shrunk.mp4 | Select-Object Name,Length` (ya da ciktilarin gercek adi).
Ham dosyaya yazilsin. Ayrica sahibin kuyruktan kac istek aldigini (`AcceptedCount`) bir
olcuye bagla - "ucu de islendi" iddiasi surec sayimindan degil **sayilan bir degerden**
gelsin. Kaynak dosyalarin (`129,735 MB / 40 s / 1920x1080`) `ffprobe` dokumu de ham
dosyaya girsin (borc 2).
CHECK: iki kosumun ham ciktisi, cikti dosyalarinin ad+boyut listesi, `AcceptedCount`
olcusunun adi ve sonucu.

**K8 - Ana pencere acilmadigini bir olcu tutsun.** Borc 4. `App.axaml.cs:42-49` kolu
degisirse kirmizi olacak bir olcu ekle (`KabukIstegiTests.cs`). Piksel genisligine
dayanmasin.
CHECK: olcunun adi, ve kolu bozan bir mutasyonun onu dusurdugunun ham ciktisi.

**K9 - `KabukIstegiTests.cs:132` sikilassin.** Borc 5: bes gerekcenin hepsi ayni cumleyi
bassa olcu olmuyor. Beklenen anahtarin karsiligiyla esitlik kontrolune cevir; mutasyonla
(iki gerekceyi ayni anahtara bagla) dustugunu goster.
CHECK: mutasyon izgarasi, kirilan olcu adi, ham cikti.

**K10 - Borc temizligi.** 1 (ffprobe parantezi - gercek ikinci ffmpeg'in kaynagini
`ComplexityProbe`/`CalibrationProbe`te **say ve goster**, varsayma), 2 (kaynak dosya
sayilari - K7 ile birlikte), 3 (`gozle.ps1`in K1 surumu ya geri getirilsin ya da K1 kosumu
guncel betikle **yeniden kosulsun**), 6 (`cikti.txt` ve `hata.txt` artiklarini sil).
Borc 7 kapsam disi - **raporda acikca borc olarak dursun**, sessizce gecme.
CHECK: her duzeltilen sayinin yaninda uretildigi ham dosyanin yolu.

**Siniri hatirlat:** `ShrinkRequest.cs`, `ShellIntegration.cs`, `EncodeRunner.cs`,
`Install-VidShrink.ps1`, `MainWindow.axaml`, `MainWindow.axaml.cs` okunacak,
**yazilmayacak**. Hizli boyut listesi degismeyecek.

## Tur 2 - denetim bulgulari (HUKUM: KALDI, 1 kritik, 7 borc)

**Bagimsiz dogrulanan gecen taraflar.** `git diff main...HEAD --name-only` 11 dosya -
9'u `owns` icinde, 2'si `trash/T171/`; `src/VidShrink.Core/**`, `Install-VidShrink.ps1`,
`MainWindow.axaml(.cs)`, `src/VidShrink.Ffmpeg/**` uzerinde diff **0 satir**. Hizli liste
iki yerde de yerinde. `ShrinkJobWindow.axaml`te ciplak renk/olcu yok - **18 baglamanin
hepsi** `StaticResource`.

**K7/K6 ham ciktilar gercek ve birbirinin kopyasi degil:** satir sayilari 74/149/152,
md5'ler ayri, pid'ler ayri (10124/16548/6964), zaman damgalari tutarli (kaynak ffprobe
07:31 -> kosumlar 07:35-07:40). Bayt sayilarinin **hepsi** ham dosyada birebir:
tek dosya `103080918`; uc surec `103083019 / 103151996 / 103090835`, `t=99s`;
tek surec tek argv uc yol `103074177 / 103063054 / 103098372`, `t=103s`. Ucunde de
`en cok es zamanli ffmpeg sureci (bizim): 1`. Kosum 2'nin "100 ornekte 99 kez 1" cumlesi
de dogru.

Tur 1'in kaynaksiz sayilari belgeden **gercekten cikmis**: `98,5` ve `129,735` yalniz
"geri getirilemedi / cikarildi" cumlelerinde geciyor, hicbir tabloda yok. Borc 2'nin
`ffprobe` dokumu `t2-kaynak-ffprobe.txt`te tam (130627831 / 130641603 / 130637498 bayt,
40 s, 1920x1080, h264). Borc 3'un K1 yeniden kosumu `t2-k1-bugun.txt`te (tek pencere
1936x1056, kendi ffmpeg 9, cikti 0); `:76`daki "46 farkli VidShrink ffmpeg komut satiri"
da dogru (46 `[BIZIM]`, 6 `[YABANCI]`).

**K8 seam gercek, oz-gondergeli degil:** `App.axaml.cs:57-60` `StartupWindow()`, `:44`te
`OnFrameworkInitializationCompleted` icinden cagrilip `desktop.MainWindow`a ataniyor -
olcu uretim yolunun tam ustunde.

**Mutasyon izgarasinin yedisinden besini denetci kendi uyguladi** (repo disi kopya, her
birinden once temiz `-c Release --no-incremental`): (a) 5 gercek olu (+2 flake),
(d) 2/46, (e) 1/47, (f) 3/45, (g) 5/43 - **hepsi rapordaki gibi**.

**K5 kollari:** `--list-tests` 48 (`KabukIstegiTests` 20 + `ShellIntegrationTests` 9 +
`ResolverTests` 12 + `QueueTests` 7) ve 13; sifir bulan kol yok; kol 2 13/13, koruyucular
90/90. **Borc 1'in statik sayimi birebir tuttu:** `src/**` altinda
`ToolLocator.StartInfo(ToolLocator.Ffmpeg` -> **20 satir / 12 dosya**, dagilim
`EncoderCapabilities` 4, `ComplexityProbe` 3, `Playback/DecoderPipe` 3, `QualityMeter` 2,
kalan sekiz dosya 1'er. Ikinci ffmpeg'in yabanci oldugu iddiasi **olcum, varsayim degil**:
`t2-k7-tek-dosya.txt` icinde `VideoEdit\edits\yesil-gozluk\...` ve
`Temp\pytest-of-Teknesyum\pytest-57xx\...` komut satirlari tam metin duruyor.

### KRITIK 1 - Tur 2'nin K9 sikilastirmasi kararsiz (flaky) bir olcu dogurdu

`KabukIstegiTests.cs:146-158` (`GerekcePencereyeYazilir`) beklenen metni **`:149`**'da
**test is parcaciginda** `LanguageCatalog.Display(Strings.Get(anahtar, ...))` ile
hesapliyor; gorulen metni ise **`:127-138`** `PencereMetni` uzerinden **arayuz is
parcaciginda** aliyor. O is parcaciginda dil **iki kez global olarak** degisiyor:
`tests/VidShrink.Tests/AppHost.cs:87` `Strings.Use("en")`, hemen ardindan
`ShrinkJobWindow.axaml.cs:96` `Strings.Use(ResolveLanguage(settings.Language, ...))`
(bu makinede `tr`). Beklenti **surec-genelinde degisen** bir durumdan okundugu icin olcu
kosumdan kosuma renk degistiriyor.

Denetcinin olcumu, teslim commit'i `3618cf8` uzerinde, **kod degisikligi olmadan**:

| kosum kumesi | kirmizi / toplam kosum | dusen olcu |
|---|---|---|
| `ShrinkRequestTests\|ShellIntegrationTests\|KabukIstegiTests` (repo disi) | **2 / 11** | `GerekcePencereyeYazilir(TargetNotPositive)`, `(TargetNotInQuickList)` |
| `KabukIstegiTests\|LocalizationTests\|LanguageTests` (kopya) | **4 / 6** | ayni iki kol (bir kosumda 1) |
| ayni filtre, **teslim agacinin kendisinde** | **1 / 2** | `(TargetNotPositive)`, `(TargetNotInQuickList)` |

Hata metni yonun **iki tarafa da** savruldugunu gosteriyor - ayni kosumda biri
`Expected: "The Target Size..."` / `Actual: "Kabuk Menusunden..."`, digeri tam tersi.

Bu kusur **tur 2'de dogdu**: tur 1'in ayni olcusu
(`git show ee6785b:tests/VidShrink.Tests/KabukIstegiTests.cs`,
`Assert.False(string.IsNullOrWhiteSpace(text.MessageText))`) dilden bagimsizdi.
`LanguageTests`/`LocalizationTests` (`Strings.Use`, `Strings.UseRoot`) tam suitte hep
birlikte kostugu icin `AGENTS.md`'deki *"dotnet test tamami yesil olmadan teslim yok"*
kurali bu haliyle **tutmaz**. Raporun `:322` "kol 48/48 yesil" cumlesi tek bir sansli
kosumun ifadesi; izgaradaki `1/47`, `5/43` sayilari da ayni gurultuyu tasiyor (denetci
mutasyon (a)'da 5 gercek olu + 2 flake = 7/41 gordu).

### Borc bulgulari

1. `kabuk-istegi-tuketimi.md:263-268` **pim sayilarinin kaynagi yanlis gosterilmis**.
   "`.calisma/T171/t2-k5-kol2-kosum.txt`" deniyor; o dosya **8 satir** ve icinde yalniz
   vstest ozeti var (`Basarili: 13`), `uye:`/`mesru:` satiri **yok**. Blok aslinda tur 1'in
   `k3-pim-sonra.txt:44` ve `:775` satirlarindan. Sayilar dogru (denetci kendi kostu:
   `uye: 162  bu dosyada adi gecmeyen: 116  pimlenen: 32`, `mesru: 10  borc: 22`); kusur
   sayida degil **kunyede** - imza kusurunun tam bicimi.
2. `.calisma/T171/t2-k4-mutasyon-{a..g}.txt` **yapi kaniti yok**: yedisi de dogrudan
   vstest afisiyle basliyor, `dotnet build -c Release --no-incremental` ciktisi hicbirinde
   yok. Rapor `:307` "her mutasyondan once kosuldu" diyor; davranisin degismesi bir yapi
   oldugunu kanitlar, **temiz** yapi oldugunu degil.
3. `src/VidShrink.App/Program.cs:121-128` **kismi teslimde cifte kodlama**: sahip olmayan
   kolda `handed` bayragi tek. Ilk `Submit` basarili, ikincisi basarisiz olursa
   `handed=false` olup bu surec **butun** istekleri kendi penceresinde kodluyor - sahibe
   teslim edilmis olan ilk dosya dahil. Tur 1'de istek tekti, bu yol **yeni acildi**.
4. `Program.cs:30-31` docstring'i *"hicbiri sessizce dusmez"* diyor; **dusuyor**.
   Denetcinin sondasi (`D3`): `--kucult 100 a.mp4 yok.mp4 b.mp4` -> **istek 2, problem
   yok**, kullaniciya var olmayan yol hakkinda tek cumle yok. T171'in kapattigi kusur
   sinifinin kucuk kalintisi.
5. `Program.cs:66-87` **en-uzun-eslesme sezgiseli goreli yollarda yanlis dosya secebiliyor**.
   Sonda `D7`: dizinde `a.mp4`, `b.mp4` ve adi `a.mp4 b.mp4` olan ucuncu bir dosya varken,
   cwd o dizinken `--kucult 100 a.mp4 b.mp4` -> **1 istek**, yolu `[a.mp4 b.mp4]`; iki
   gercek istek de dusuyor. Kayit defteri sablonu hep mutlak `%1` verdigi icin uretimde
   erisilemez, ama rapor sezgiselin bu bedelini hic yazmiyor. **Diger sondalar temiz:**
   tirnakli bosluklu yol 1 istek, tirnaksiz 3 parcaya bolunmus bosluklu yol yine 1 istek ve
   yol birebir dogru, bayrak sonrasi yol yok -> `NoPath`, tekrarli yol -> 2 istek,
   sayi adli dosya hedefle karismiyor.
6. `.calisma/T171/gozle.ps1` **komut satiri okunamayan ffmpeg'i sessizce YABANCI sayiyor**.
   `t2-k7-tek-dosya.txt` ve `t2-k6-tek-surec-uc-yol.txt` ozetlerinde bos komut satirli birer
   `ffmpeg [YABANCI]:` girdisi var. "Kendi es zamanli ffmpeg sayisi 1" bu yuzden bir
   **alt sinir**; rapor `:355-356` bunu kesin sayi gibi yaziyor.
7. `trash/T171/cikti.txt` ve `hata.txt` **`owns` disinda**. K10 borc 6 "sil" diyordu;
   yapici `.gitignore`'lu `.calisma/`den alip **git'e** ekledi (2 yeni izlenen dosya).
   Zararsiz ve genel kurala uygun, ama `owns` ve `side_effects: []` bunu kapsamiyor.

**Olculmeyen, borc olarak kalmasi dogru** (rapor `:396-401`te acikca yazmis): Explorer'in
`Player` kipinde dosya basina ayri surec actigi surulmedi, kurulu uygulamayla gercek sag
menu tiklamasi yapilmadi. K6 tek argv'deki coklu yolu kapattigi icin kusurun bu varsayima
bagimliligi kalkti - **kritik degil, borc**. Borc 7 (Title Case govde metni) `:390-395`te
acik borc olarak duruyor.

## Tur 3 - kapsam (T0)

**K11 - Kararsiz olcuyu kapat.** `KabukIstegiTests.cs:146-158` beklenen metni
`AppHost.Run` govdesinin **icinde** hesaplasin ya da `Strings.GetIn(<dil>, ...)` kullansin -
surec-genelinde degisen `Strings.Use` durumundan okuma. Ayni desenin **baska olculerde de
olup olmadigini** ara (`Strings.Get` cagiran her test kolu), varsa hepsini kapat.
CHECK: `dotnet test --filter "KabukIstegiTests|LocalizationTests|LanguageTests"`
**arka arkaya 10 kosum**, onunun de yesil ham ciktisi tek dosyada. Ayrica K4 izgarasini
**yeniden kos** - tur 2'nin `1/47`, `5/43` sayilari bu gurultuyu tasiyordu.

**K12 - Borc 3: kismi teslimde cifte kodlama.** `Program.cs:121-128` `handed` bayragi
istek basina olsun; sahibe teslim edilen istek bu surecte **yeniden kodlanmasin**.
CHECK: ikinci `Submit`i basarisiz kilan bir mutasyon/sonda ile once/sonra davranis, ve
bunu tutan bir olcu.

**K13 - Borc 4: var olmayan yol sessizce dusmesin.** `--kucult 100 a.mp4 yok.mp4 b.mp4`
bugun 2 istek uretip **hicbir sey soylemiyor**. Kullanici en az bir cumle gormeli (iki
dilde). Ya `Program.cs:30-31` docstring'ini gercekle uyumlu hale getir **ve** kalintiyi
kapat - docstring'i gercege uydurup kusuru birakma.
CHECK: sondanin ham ciktisi, kullaniciya giden tam metin (iki dilde), ve olcu adi.

**K14 - Borc 5: sezgiselin bedeli olculsun ve yazilsin.** En-uzun-eslesme goreli yollarda
yanlis dosya secebiliyor (`D7`). Uretimde erisilemez oldugunu **kendin dogrula** (kayit
defteri sablonunun her zaman mutlak yol verdigini goster) ve bu bedeli rapora yaz; ya da
sezgiseli duzelt. Hangisini secersen sec gerekcesini yaz.
CHECK: `D7` sondasinin ham ciktisi + uretim yolunun mutlak oldugunun kaniti.

**K15 - Borc 1, 2, 6, 7 temizligi.**
- Borc 1: `:263` kunyesi ya `k3-pim-sonra.txt` olsun ya da pim tur 3'te **gercekten
  kosulup** ciktisi ayri bir ham dosyaya yazilsin.
- Borc 2: mutasyon izgarasi yeniden kosulurken (K11) **yapi ciktisi ham dosyaya girsin**.
- Borc 6: `gozle.ps1` komut satiri okunamayan ffmpeg'i ayri bir kova olarak (`BILINMEYEN`)
  saysin; "kendi es zamanli ffmpeg sayisi" cumlesi alt sinir oldugunu soylesin ya da kesin
  olsun.
- Borc 7: `trash/T171/`teki iki dosya - `owns` disi oldugu icin **T0'a birak**, dokunma,
  raporda bildir.
CHECK: her duzeltilen sayinin yaninda uretildigi ham dosyanin yolu.

**Siniri hatirlat:** `ShrinkRequest.cs`, `ShellIntegration.cs`, `EncodeRunner.cs`,
`Install-VidShrink.ps1`, `MainWindow.axaml`, `MainWindow.axaml.cs` okunacak,
**yazilmayacak**. Hizli boyut listesi degismeyecek. `AppHost.cs` `owns` disinda -
gerekiyorsa **bildir, yazma**.

## Tur 3 - teslim

Dal `T171-kabuk-istegi-tuketimi`, HEAD `23834b4` (itildi, agac temiz). Tur 2 teslimi
`3618cf8`, devralinan ara HEAD `c6604cb`.

Yapicinin beyani: K11-K14 devralinan agacta kod duzeyinde dogruydu, kod degistirilmedi;
tur 3 iki commit ekledi, ikisi de yalniz `docs/olcumler/kabuk-istegi-tuketimi.md`.
K11 CHECK: 10 kosum, 10/10 yesil, her biri 98/98, `.calisma/T171/t3-k11-kararlilik.txt`.
K4 izgarasi yeniden kosuldu, 10 mutasyon (a-j), olen: 6/3/1/4/1/3/7/1/1/1, taban 55/55,
rastgele dusen kol yok; on dosyanin onunda da `--no-incremental` yapi ciktisi var (borc 2).

Itiraflar: uctan uca sonda hala yok (K13 `Program.StartupFor` + `ShrinkJobWindow`
seviyesinde); devralinan on `t3-` ham dosyasi reddedildi ve `.calisma/T171/devralinan/`
altina alindi (biri kirli agacta kosulmus, biri baslikta "uc yol" derken 1 yol basiyor).

Owns disi bulgular (T0 alani): `tests/VidShrink.Tests/WindowLayoutTests.cs:867` ayni
is-parcacigi desenini tasiyor, bugun kirmiziya donmuyor; borc 7 `trash/T171/cikti.txt` ve
`hata.txt` hala git izinde.

## Tur 3 - denetim (HUKUM: GECTI, 0 kritik, 9 borc)

Denetci teslim commit'ini repo disi kopyaya cikardi, temiz `-c Release --no-incremental`
yapi kurdu ve **10 bagimsiz kosum** yapti (her biri ayri `dotnet test` cagrisi):
10/10 yesil, hepsi **98/98**, 10:30:05 - 10:33:00, sureler 5-16 s. Tur 2'nin gordugu
2/11, 4/6, 1/2 kirmizi **yeniden uretilmedi**. Ham cikti denetcinin `denetci-k11.txt`
dosyasinda. Diger kol 4 kez kosuldu, hepsi 55/55; `OluUyeTests` 13/13.

Kollar denetcinin kendi `--list-tests` sayimiyla dolu: K11 filtresi 27+56+15 = **98**,
verify kol 1 27+9+12+7 = **55**, kol 2 **13**. Sifir eslesen kol yok.

`t3-k11-kararlilik.txt` kopyala-yapistir degil: 11 ayri `===== KOSUM` basligi, **11 farkli
zaman damgasi** (10:03:42 -> 10:13:57, ~21 s arali), `grep -c "^exit=0"` = 11. Belgedeki
satir kunyeleri (8/17/26/35/44/53/62/71/80/89, izgara sonrasi 115) birebir dogru.

Kod duzeyinde kapanma gercek: `ShrinkJobWindow.axaml.cs:353-357` `Say` artik
`Strings.GetIn(_language, ...)`, `_language` kurulusta bir kez cozulup `Language` ile disa
veriliyor. "Ayni desen baska yerde var mi" iddiasi da tuttu - denetci `tests/` altinda
`Strings.Get`/`Strings.Use` cagiran her satiri taradi; test is parcacigindan okuyan tek
yer `WindowLayoutTests.cs:867`.

**K4 izgarasi.** On ham dosyanin onunda da `--no-incremental` yapi basligi ve
`Olusturma basarili oldu` bir kez - **borc 2 kapandi**. Sonuc satirlari belgeyle birebir:
a 6/49, b 3/52, c 1/54, d 4/51, e 1/54, f 3/52, g 7/48, h 1/54, i 1/54, j 1/54,
hepsi `Toplam: 55`. Denetci **altisini repo disi kopyada kendi kostu** (a/d/f/h/i/j) -
altisinin altisi da sayi **ve** olcu adi duzeyinde ayni, rastgele dusen kol yok.
Tur 2'nin `1/47`, `5/43` gurultusu kapanmis.

`owns` temiz: `git diff main...23834b4 --name-only` 11 dosya, 9'u `owns` icinde + 2
`trash/T171/`. `ShrinkRequest.cs`, `ShellIntegration.cs`, `EncodeRunner.cs`,
`Install-VidShrink.ps1`, `MainWindow.axaml(.cs)`, `AppHost.cs` uzerinde **0 satir** diff.
`git diff c6604cb..23834b4 --stat` tek dosya - yapicinin "tek satir kod degistirmedim"
iddiasi dogru.

Imza kusuru bu turda **kritik duzeyde yok**: denetci tur 3 bolumundeki butun mutlak
nicelik cumlelerini ham dosyaya karsi denedi, hepsi tuttu. Iki cumle tablodan genisti,
ikisi de borc duzeyinde (asagida 1 ve 2) ve **T0 `dafef66` ile duzeltti**.

### Borclar

1. K14 cumlesi `"%1"` sablonunu "mutlak yol" diye ozetliyordu; olculen sey yurutulebilirin
   mutlak yolu ve tirnakli tek yer tutucu, `%1`'in tam nitelikli yola acilmasi bu depoda
   **olculmedi**. Sonuc yine de ayakta (tur 1: komut satiri basina `%1` sayisi 120/120 = 1).
   **T0 `dafef66` ile duzeltti.**
2. K11'in "sebep `UpdateSettings.Load()` - sira bagimli" cumlesi olcuye degil tahmine
   dayaniyordu. **T0 `dafef66` ile tahmin olarak isaretledi.**
3. Kurulu uygulamayla gercek sag menu tiklamasi / uctan uca sonda hala **yok**; K13 sondasi
   `Program.StartupFor` + `ShrinkJobWindow` seviyesinde. Belgede aciktan yazili.
4. Mutasyon (i) yalniz `GerekceOlcusuSurecDilindenEtkilenmez(tr)` kolunu dusuruyor;
   `en` kolunun ayni mutasyon altinda oldugu makineden bagimsiz gosterilmedi (denetcinin
   kendi kosumunda da yalniz `tr` dustu).
5. `tests/VidShrink.Tests/WindowLayoutTests.cs:867` ayni is-parcacigi desenini tasiyor -
   bugun yesil (paralellik kapali, `MainWindow` kurulusta dili degistirmiyor), `owns` disi.
   **Acik borc, sonraki turlara devir.**
6. `ShrinkJobWindow.axaml.cs:103` hala `Strings.Use(_language)` ile surec genelindeki dili
   degistiriyor. Olcu oradan okumadigi icin kirmizi vermiyor ama seam duruyor.
   **T0 `dafef66` ile belgeye yazdi; kod duruyor.**
7. `trash/T171/cikti.txt` ve `hata.txt` git izinde. **T0 karari: bu bir kusur degil** -
   RULES.md "olu dosya silinmez, projenin `trash/`ine tasinir; `trash/`i bosaltmak
   kullanicinin cagrisi" diyor. Borc kapandi.
8. Butun ham kanit `.gitignore`'lu `.calisma/T171/` altindaydi ve yalniz worktree'de
   yasiyordu. **T0 birlestirmeden once ana depoya kopyaladi** (`.calisma/T171/`,
   `devralinan/` alt klasoru dahil); worktree kaldirildiginda kunyeler askida kalmiyor.
   Git'e girmiyor - yerel kanit.
9. Kozmetik kunye kaymasi: belge `ShrinkJobWindow.axaml.cs:103` diyordu, sabitleme atamasi
   `:102`. **T0 `dafef66` ile `98,102,125` yapti.**
