---
id: T69
title: Başarım ölçüsü boş taban varsayıyor ama boşluğu doğrulamıyor
role: builder
model: opus
depends: []
status: submitted
round: 2
audit: passed
auditor_id: acb2fc3b7e9ccae85
diff: 3 dosya, owns disi yok
verification: dogrulama agacinda 5581a08: dotnet test -c Release 796 gecti / 0 kaldi / 16 atlandi / 812 toplam · ajan ci-gibi-kos.sh 741/0/71/812 · mutasyon denetimi iki iddiada da kirmiziya dondu
verify: [dotnet test -c Release --filter PerformanceCheckTests]
owns: [tests/VidShrink.Tests/PerformanceCheckTests.cs, src/VidShrink.Core/PerformanceCheck.cs, src/VidShrink.Ffmpeg/PerformanceProbe.cs]
side_effects: []
---

## Amaç

`PerformanceCheckTests.OlcumYukAltindaYalnizAgirlasiyor` kararsız. Tek başına
koşunca geçiyor, makine meşgulken kırmızıya dönüyor. Bir denetim turunda üç tam
koşumun üçü de kırmızıydı; aynı commit'te başka iki koşum yeşildi.

`sh tools/ci-gibi-kos.sh` yeşil kalıyor çünkü ölçü `[FfmpegFact]` kapısında —
CI bunu hiç koşturmuyor. Yani **yalnız yerelde kırmızı**, ve yerelde yeşil okuma
alan biri sorunu göremiyor.

## Mekanizma — bu bir hipotez, ölçüp doğrula ya da çürüt

Ölçü üç bacak koşuyor: `bos1`, `bos2` (`PerformanceCheckTests.cs:342-343`) ve
`CpuLoad` altında `yuklu` (`:350`). Sonra `yuklu >= bos1 * 0.8` diye yön iddia
ediyor (`:368`).

"Boş" bacaklar makinenin boş olduğunu **hiçbir yerde doğrulamıyor.** Dışarıdan
bir yük varsa — mesela paralel koşan ikinci bir `dotnet test` — `bos1` şişiyor,
`yuklu` görece düşük çıkıyor ve iddia tersine dönüyor. Gözlenen kırılma:
`yuk altinda maliyet dustu: bos 1.833, yuklu 1.148`.

İkinci kırılma biçimi `:374`: yük altında `HardwarePathWorks` bulgusu hiç gelmiyor.

**Hipotezi kendin sına.** Yanılıyor olabilirim; T66'da hipotezim ölçüyle çürütüldü.
Kırılmayı kasten üret (arka planda ikinci bir yük koşarken ölçüyü koştur), sonra
düzeltmenin o üretimi kapattığını göster.

## Ne yapılacak

Ölçüyü **susturma**. Boş makinede yön gerçekten yanlışsa ölçü hâlâ kırmızıya
dönmeli — mutasyon denetimi yap: düzeltmeden sonra `PerformanceProbe`'un maliyet
hesabını kasten bozup ölçünün kırıldığını göster.

Çözüm yolunu sen seç. İki yön akla yakın, ikisini de değerlendir:

- Ölçü tabanın gerçekten boş olduğunu doğrulasın; değilse **atlansın**
  (`Skip`), kırmızıya dönmesin. Atlama sebebi çıktıda görünsün.
- Yoklama dış yükü kendi raporlasın, ölçü de o bilgiye baksın.

Sessiz tolerans büyütmek (0.8'i düşürmek) çözüm değil — gerçek bir yön hatasını
da yutar.

## Kabul kriteri

1. Kasten üretilen dış yük altında ölçü **kırmızıya dönmüyor**; ya geçiyor ya
   sebebi yazılı olarak atlanıyor.
2. Boş makinede ölçü hâlâ gerçek bir şey ölçüyor: `PerformanceProbe`'un maliyet
   hesabını kasten bozunca kırmızıya dönüyor. Bunu koş ve çıktısını rapora yaz.
3. `HardwarePathWorks` bulgusunun yük altında gelmemesi de aynı disiplinle
   ele alınmış — sessizce kaldırılmamış.
4. Tam süit **arka planda ikinci bir yük koşarken** yeşil.
   CHECK: `dotnet test -c Release`
5. `sh tools/ci-gibi-kos.sh` yeşil ve atlanan sayısı 71'de kalıyor.
6. Docstring'e **ölçüm sayısı yazma** — T63'ün kuralı. Kodun ne yaptığını
   tanımlayan sayı serbest, ölçülmüş sayı değil.

## Kayıt noktası

Her adım ayrı commit. Dal adı `worktree-agent-<kendi id>`.

## Rapor

Sözleşme dosyası verilen worktree yolunda yoktu: dal `03e2a8b`'deydi, T69 o commit'te
henüz açılmamıştı. Dal `6a0136a`'ya ff-merge edildi, dosya geldi. `live/_sorun.log`'a
yazamadım, kanca kabuktan yazmayı engelliyor.

`.calisma/t69/` silinemedi: silme izni reddedildi. İçinde ölçüm günlüğü ve yük
başlatma/durdurma betikleri var, git'e sızmıyor.

## Çıktı

### Hipotez: kısmen doğrulandı, kırmızıyı üreten mekanizma başkası

Doğrulanan yarı: doğrulanmayan "boş" taban **gerçekten şişiyor**. Boş makinede altı
arka arkaya okuma 0.557–0.612 aralığında; arka planda 32 dış yakıcı süreç koşarken aynı
okuma 0.756–0.891'e çıktı. Yani dışarıdan gelen yük tabanı yarı yarıya şişirebiliyor ve
bildirdiğin 1.833 bu bandın çok üstünde, ağır bir dış yükle tutarlı.

Çürütülen yarı: dış yükü kasten üretip **gerçek ölçüyü koşturdum, kırmızı geldi — ama
senin yazdığın sebepten değil.** Kırılan iddia şuydu:

    yuk altinda maliyet dustu: bos 0.756, yuklu 0

`yuklu` **sıfır**. Maliyet düşmedi; yazılım bacağı 30 s bütçe dolduğu için hiç
ölçülmedi. `SoftwareRealtimeCores` ölçülmeyen bacakta da 0 döndüğü için ölçü
"ölçülmedi"yi "maliyet düştü" diye okuyup yanlış bir cümle bastı. Aynı kök ikinci
kırılmayı da açıklıyor: yük altında `HardwarePathWorks` gelmiyor çünkü donanım bacağı da
bütçeye takılıyor (bir koşumda boş donanım okuması 30716 ms sürüp `NotMeasured,
BudgetExhausted` döndü).

Ayrıca sınadım ve çürüttüm: **soğuk başlangıç yanlılığı yok** — ilk okuma (0.576) sonraki
beşten farklı değil. Ve `CalibrateCpuClock` dış yük göstergesi olarak **kullanılamaz**:
boş makinede 2.1–38.4 arası saçılıyor, yük altında 1.0–1.7'ye oturuyor, yani ters ve
gürültülü. Sözleşmedeki "yoklama dış yükü kendi raporlasın" yolu bu sayaç üzerinden
kurulamıyordu.

### Ne değişti

`src/VidShrink.Core/PerformanceCheck.cs` — `PerformanceCheckResult`'a `SoftwareMeasured`,
`HardwareMeasured`, `BudgetExhausted`. "Ölçülmedi" ile "sıfır maliyet" artık ayrı okunuyor,
ve eksik bacağın sebebinin bütçe olup olmadığı ayrı.

`tests/VidShrink.Tests/PerformanceCheckTests.cs`

- `OlcumYukAltindaYalnizAgirlasiyor`: taban tek okuma yerine üç okuma, biri yük
  kalktıktan sonra. Kirlenme sayıyı yalnız yukarı ittiği için **en düşük okuma** taban
  alınıyor — bu eşiği gevşetmek değil, aynı büyüklüğün daha doğru kestirimi; `0.8` payı
  el değmeden duruyor. Okumalar birbirini `TabanUyumBandi` içinde doğrulamıyorsa makine
  sessiz değildi, iddia kurulmuyor ve sebebi yazılıyor.
- Ölçülemeyen her bacak **bütçeyle açıklanmak zorunda**. Bütçe dolmadan eksilen bacak
  kırmızı. Bütçe dolduysa iddia kurulmuyor, sebep hem `ITestOutputHelper` çıktısına hem
  `.calisma/t63/olcum.txt`'ye `[atlandi]` satırı olarak yazılıyor (xunit 2.9 koşum
  sırasında test atlayamıyor, `Skip` dinamik değil).
- Her bacağın bütçesi 30 s'den 60 s'ye çıkarıldı: eksik bacak istisna olsun diye. Bütçe
  bir zaman aşımı, bir eşik değil — dolması hâlâ ele alınıyor.
- Aynı dosyada K4 için iki kardeş ölçü de düzeltildi: `BuMakinedeKodlamaNereyeDusuyor`
  bütçe dolduğu için alınamayan bacağı kusur sayıyordu; `ButceGercektenBagliyor` bütçenin
  bir katıyla (3x) kurulmuş mutlak bir sınır kullanıyordu — bütçe dolunca süreç
  öldürülüyor, başlatma ve öldürme bütçenin dışında kalır ve makine meşgullestikçe büyür,
  o sınır bütçe pekâlâ bağlıyorken kırmızıya dönüyordu (yüklü koşumda 8711 ms ve 3445 ms
  ölçüldü, sınır 2700 ms). Bağlama artık aynı makinede geniş bütçeyle alınan koşumla
  karşılaştırılıyor.

### Koşulan ölçüler

- Boş makinede `OlcumYukAltindaYalnizAgirlasiyor` yeşil, **hiç atlanan iddia yok**: üç
  taban okuması da ölçüldü, bantta ve aynı kararda; yüklü 1.036, donanım iki tarafta da
  ölçüldü.
- **Mutasyon denetimi**, boş makinede, `PerformanceProbe.Subtract` üzerinde:
  - çıkarma ters çevrildi (`baseline - measured`) → KIRMIZI:
    `yazilim bacagi butce dolmadan olculemedi: NotMeasured`
  - maliyet yerine hız bildirildi (`VideoMs^2 / net`) → KIRMIZI:
    `yuk altinda maliyet dustu: en dusuk bos okuma 1.469, yuklu 0.968`
  - üçüncü mutasyon (`WallMs` ile `VideoMs` yer değiştirdi) de kırmızı döndü.
  Her mutasyon geri alındı; `PerformanceProbe.cs` sonuçta değişmedi.
- `dotnet test -c Release --filter PerformanceCheckTests`, **arka planda 32 dış yakıcı
  süreç koşarken**: 19 geçti / 0 kaldı / 0 atlandı. (T0 tam süit koşumumu kıstı; tam
  süiti koşturmadım.)
- `sh tools/ci-gibi-kos.sh`: 741 geçti / 0 kaldı / **71 atlandı** / 812 toplam.

### Yolda görülen, dokunulmadı

`PerformanceProbe.Subtract` net süreyi `Math.Max(0, ...)` ile kırpıyor; `Usable` da
`WallMs > 0` istiyor. Yük altında gürültü ölçülen geçişi tabanın altına düşürürse
**başarıyla koşmuş** bir donanım geçişi "kodlayıcı başarısız" ile aynı görünüyor —
yüklü koşumda donanım bacağı bütçe dolmadan `olculdu=False` döndü. Ölçü bunu sessizce
geçmiyor (sebebi yazıyor) ama kaynaktaki bu karışıklık ayrı bir iş.

## Tur 2 — düzeltme kendi tuzağına düştü

Denetim tam süiti **boş makinede** koşturdu: `Başarısız: 2, Başarılı: 794,
Atlanan: 16, Toplam: 812`. İkisi de aynı sınıftan ve biri senin eklediğin iddia.

**Kırmızı 1 — `PerformanceCheckTests.cs:462`.** Yük altında donanım bacağı bütçe
dolmadan kayboluyor: `HardwareEncoderFailed, SoftwareRealtimeCost, SoftwareCostsCores`.
Kendi günlüğün de aynısını yazmış: `yuklu ... olculdu=False butce=False`. Sen bunu
raporun "yolda görülen" bölümüne yazmışsın ama **eklediğin iddianın tam da bu durumu
kırmızıya çevirdiğini** görmemişsin — çünkü tam süiti hiç koşturmadın, süzgeci koştun.
Bütçeyi ben kısmıştım; bu yarısı benim payım.

Mesele şu: yük altında nvenc'in kaynak bulamayıp düşmesi **makine gerçeği**, kod
kusuru değil. Ama her ölçülemeyen bacağı atlarsan ölçü boşalır.

**Kırmızı 2 — `PerformanceCheckTests.cs:591`**, `IslemciZamaniSayaciDogruOkuyorMu`:
`kalibrator Windows'ta olcemedi`, `duzeltme=0x`. Bu ölçüye dokunmamışsın ve
`a226ce0`'da yeşildi. Aynı kök: sayaç okunamadığında ölçü bunu kusur sayıyor. Oysa
bu makinede sayacın bozuk olduğu T63'te zaten ölçüldü — `CpuAccountingUnreliable`
tam bunun için var.

## Tur 2'de yapılacak

Her iki ölçü de **"makine okuma üretemedi"** ile **"kod yanlış"** arasını ayırsın.
Ayrım gerçek olsun, atlamak boşaltmasın:

- Donanım tarafında gerileme koruması **boş koşumdan** gelsin: boş bacak ölçüldüyse
  `HardwarePathWorks` orada iddia edilir. Yüklü bacak kaybolduysa sebebi yazılıp
  atlanır — bütçe olsun olmasın.
- Sayaç ölçüsü, sayacın çalışmasını değil, **sayaç okunamadığında ürünün doğru
  davrandığını** iddia etsin: `CpuAccountingUnreliable` bildiriliyor mu, sonuç
  yanlış sayı üretiyor mu.

## Tur 2 kabul kriteri

1. `dotnet test -c Release` **tam süit**, boş makinede yeşil. Süzgeç yetmez —
   geçen turda kırmızıyı tam da bu kaçırdı.
   CHECK: `dotnet test -c Release`
2. Mutasyon denetimi: ölçüler hâlâ gerçek bir şey ölçüyor. `HardwarePathWorks`
   iddiasını ve sayaç iddiasını ayrı ayrı kasten kır, kırmızıya döndüklerini göster,
   geri al.
3. `Atlandi` çağrılarının hepsi sebebini yazıyor ve sebep makineye ait bir olguya
   dayanıyor — "iddia kurulamadı" tek başına yeterli değil.
4. Denetçinin tur 1'de yazdığı üç borç da kapansın ya da sözleşmede gerekçesiyle
   borç kalsın: `:453` boş koşum bütçe sorgusuz atlıyor; `:498` bütçe sınırı artık
   makinenin yavaşlığıyla ölçekleniyor; `SoftwareMeasured` `Usable` kapısı yüzünden
   `> 0` ile aynı doğruluk değerini taşıyor.

**Koşum bütçesi:** tam süit en çok iki kez. `ci-gibi-kos.sh` en çok bir kez.


## Tur 2 çıktısı

### Ne değişti

Yalnız `tests/VidShrink.Tests/PerformanceCheckTests.cs`. `PerformanceCheck.cs` ve
`PerformanceProbe.cs` bu turda değişmedi: iki kırmızının ikisi de ürünün davranışında
değil, ölçünün kurduğu iddiadaydı.

**`OlcumYukAltindaYalnizAgirlasiyor` (:462 idi).** Donanım gerileme koruması boş
koşuma taşındı: boş bacak ölçüldüyse `HardwarePathWorks` orada iddia ediliyor, yüklü
bacak da ölçüldüyse aynı bulgu ve aynı kodlayıcı adı orada da isteniyor. Yüklü bacak
kaybolduysa artık bütçe sorulmuyor — sebep, sonucun kendi bulgularından okunan üç
makine olgusundan biri olmak zorunda: bütçe doldu, kodlayıcı listede var ama geçişi
başarısız döndü, ya da makinede donanım kodlayıcısı yok. Üçünden hiçbiri yoksa bacak
sebepsiz kaybolmuştur ve ölçü kırmızıya döner (`DonanimAtla`, `PerformanceCheckTests.cs`).

**`IslemciZamaniSayaciDogruOkuyorMu` (:591 idi).** Artık sayacın çalıştığını iddia
etmiyor. Kalibratörün bu makinede verdiği okuma ürüne veriliyor ve ürünün onu doğru
rapor ettiği iddia ediliyor: güvenilirlik yargısı okumayla tutarlı mı,
`CpuAccountingUnreliable` tam da güvenilmez olduğunda mı geliyor, karar sağlam sayaçlı
koşumla aynı mı kalıyor, sayaç yüzünden yanlış bir çekirdek sayısı üretiliyor mu.
Sayaç güvenilir okumadıysa "sayacın doğruluğu iddia edilmedi" diye atlanıyor, sebep
ölçülen katsayıyla birlikte yazılıyor.

**`ButceGercektenBagliyorVeSebebiniSoyluyor`.** Taşıyıcı iddia zamansız hale getirildi:
dar bütçeyle hiçbir bacak ölçülemiyor, geniş bütçeyle en az biri ölçülüyor. Bu iddia
makinenin hızına hiç bakmıyor. Süre karşılaştırması ikincil kaldı ve geniş bütçeli
koşum hiçbir bacak alamazsa sebebiyle atlanıyor. Üçüncü bir koşum, tek bir süreç bile
başlatılamayacak kadar dar bir bütçeyle alınıp günlüğe yazılıyor: bütçenin kesemediği
sabit pay bu, ve o koşum da sebebini bildirmek zorunda.

Bütçe dolduğu için atlanan iki iddianın sebebine geçen süre eklendi.

### Koşulan ölçüler

- **Tam süit, boş makinede**: `dotnet test -c Release` →
  `Başarısız: 0, Başarılı: 796, Atlanan: 16, Toplam: 812`, 3 dk 30 sn.
  (Denetimin gördüğü iki kırmızı bu koşumda yok.)
- `sh tools/ci-gibi-kos.sh` → `741 / 0 / 71 / 812`. Atlanan sayısı 71'de kaldı.
- **Mutasyon denetimi**, iki iddia ayrı ayrı kırıldı, ikisi de geri alındı; çalışma
  ağacı sonunda temiz:
  - `PerformanceCheck.Evaluate` içinden `HardwarePathWorks` bulgusunun üretimi silindi →
    `OlcumYukAltindaYalnizAgirlasiyor` KIRMIZI, `PerformanceCheckTests.cs:466`,
    `Assert.Contains() Failure: Filter not matched in collection` (bulgu listesinde
    `HardwareNotCpuBound, HardwarePipelineHeadroom, HardwareCpuCostNotMeasured,
    SoftwareRealtimeCost, CpuAccountingUnreliable` var, `HardwarePathWorks` yok).
  - `PerformanceCheckResult.CpuAccountingTrustworthy` → `CpuAccountingFactor >= 0` →
    `IslemciZamaniSayaciDogruOkuyorMu` KIRMIZI, `PerformanceCheckTests.cs:657`,
    `Assert.Equal() Failure: Expected: False, Actual: True`.
- Boş makinede okunan sayılar (`.calisma/t63/olcum.txt`): üç taban okuması
  0.654 / 0.656 / 0.657, yüklü 1.335, donanım bacağı hem boş hem yüklü tarafta
  ölçüldü. Sayaç katsayısı 19.2x — güvenilmez, ve ölçü bunu kusur değil makine olgusu
  sayıp atladı. Bütçe: dar koşum 964 ms ve hiçbir bacak ölçmedi, geçişsiz taban koşum
  601 ms, geniş koşum 7798 ms ve iki bacağı da ölçtü.

### Borç durumu (K4)

1. **`:453` boş koşum bütçe sorgusuz atlıyordu — kapandı.** Boş koşumun atlaması da
   `DonanimAtla`'dan geçiyor; sebep üç makine olgusundan birine bağlanmazsa atlama yok,
   kırmızı var.
2. **`:498` bütçe sınırı makinenin yavaşlığıyla ölçekleniyordu — kapandı, ama
   istenen yoldan değil.** Sınırı mutlaklaştırmadım: bütçe dolunca süreç öldürülüyor ve
   öldürme masrafı bütçenin dışında, makineyle birlikte büyüyor; ona konacak her mutlak
   sayı uydurma olurdu. Onun yerine iddianın ağırlığı süreden alındı — bütçenin
   bağladığı artık "dar bütçeyle hiçbir bacak ölçülmedi, geniş bütçeyle ölçüldü" diye
   kanıtlanıyor. Süre karşılaştırması yerinde duruyor ama artık tek dayanak değil.
3. **`SoftwareMeasured`, `Usable` kapısı yüzünden `> 0` ile aynı doğruluk değerini
   taşıyor — borç olarak duruyor.** Kapatmak, "geçiş başarılı oldu ama net maliyeti
   taban çözme geçişinin altında kaldı" durumunu "geçiş başarısız oldu"dan ayırmayı
   gerektiriyor; bu ayrım yeni bir `PerformanceFindingCode` istiyor.
   `PerformanceReportText.Line` bilerek `default` geçişsiz yazılmış — enum'a eklenen her
   değer orada da bir satır istiyor — ve
   `src/VidShrink.App/Performance/PerformanceReportText.cs` `owns` dışında. Dokunmadım.
   Kapsamı genişletmek T0'ın kararı.

### Yolda görülen, dokunulmadı

Tur 1'de yazılan `Subtract` kırpması hâlâ orada: yük altında gürültü ölçülen geçişi
tabanın altına düşürürse başarıyla koşmuş bir geçiş "kodlayıcı başarısız" ile aynı
görünüyor. Ölçü bunu artık kusur saymıyor (sebebini yazıp atlıyor) ama kaynaktaki
karışıklık duruyor — yukarıdaki 3 numaralı borçla aynı iş.

### Tur 2 kurulum notları

Sözleşme dosyası dalın bulunduğu commit'te (`03e2a8b`) yoktu; dal `main`e (`2765f37`)
ff-merge edildi, dosya geldi.

`.calisma/t63/` silinemedi: silme izni hem `rm` hem `Remove-Item` için reddedildi.
İçinde bu turun ham ölçüm günlüğü var, `.gitignore`'da, git'e sızmıyor; rapora giren
sayılar oradan çıktı.

`live/_sorun.log`'a yazamadım, kanca `live/` altına elle yazmayı engelliyor; iki not
da buraya yazıldı.


## Mühür notu — GEÇTİ, tur 2

### Hipotezim yarı doğru, ve yanlış olan yarısı daha önemliydi

"Doğrulanmayan boş taban şişiyor" dedim. Şişiyor — boşta 0.557–0.612, dış yük altında
0.756–0.891. Ama kırmızıyı üreten o değildi. Asıl kusur ürün kodunda:
`SoftwareRealtimeCores` hem "bu bacak ölçülemedi" hem "maliyeti sıfır" için `0`
dönüyordu, ölçü de bunu "maliyet düştü" diye okuyordu. `PerformanceCheck.cs` artık
`SoftwareMeasured` / `HardwareMeasured` taşıyor.

Ajan iki şeyi daha sınayıp çürüttü: soğuk başlangıç yanlılığı yok, ve sözleşmede
önerdiğim "yoklama dış yükü kendi raporlasın" yolu kurulamıyor — `CalibrateCpuClock`
boşta 2.1–38.4, yük altında 1.0–1.7 veriyor, yani ters ve gürültülü.

### Tur 2'yi ben açtırdım, sebebi de benim kısıntım

Tur 1'in düzeltmesi boş makinede iki kırmızı bıraktı. Biri ajanın kendi eklediği
iddiaydı: yük altında nvenc bütçe dolmadan `HardwareEncoderFailed` ile düşüyor,
iddia bunu kod kusuru sayıyordu. Ajan bu olguyu raporunun "yolda görülen" bölümünde
kendisi yazmış ama iddiasının onu kırmızıya çevirdiğini görmemiş — **çünkü ona tam
süiti koşturma, süzgeç yeter demiştim.** Maliyeti kısarken yanlış yeri kestim.

Tur 2 ürün koduna hiç dokunmadı; iki kırmızının ikisi de ölçünün kurduğu iddiadaydı.

### Ölçüler

Doğrulama ağacında `5581a08`: `dotnet test -c Release` → 796 geçti / 0 kaldı /
16 atlandı / 812 toplam. Ajan `tools/ci-gibi-kos.sh` ile 741/0/71/812 aldı; atlanan
71'de kaldı. Mutasyon denetimi iki iddiada da kırmızıya döndü ve geri alındı.

### Borçlar

**Donanım gerileme koruması `HardwareEncoderFailed` ile aşılabiliyor.** Donanım
yolunu gerçekten bozan bir gerileme makine olgusundan ayırt edilemiyor ve iki bacak
da atlanıyor; ayakta kalan koruma yalnız "kodlayıcı ölçülüyor ama bulgu bildirilmiyor"
mutasyonunu yakalıyor. "Gerileme koruması boş koşumdan gelsin" isteğim yarım karşılandı.

`ButceGercektenBagliyor`'un docstring'i "geniş bütçeyle en az biri ölçülüyor" diyor
ama o yarı `if` kapısı, iddia değil — **rapor özeti veriden kayıyor kalıbının aynısı**.
`dar = 900` ile `CpuCalibrationMs = 600` arasında pinlenmiş bağ yok. Sayaç ölçüsünün
iki ana iddiası üretim kodunun gövdesini birebir tekrarlıyor: gövde yeniden yazılırsa
yakalar, kural yanlışsa yakalamaz. `SoftwareMeasured` `Usable` kapısı yüzünden hâlâ
`> 0` ile aynı doğruluk değerini taşıyor; kapatmak owns dışındaki
`PerformanceReportText.cs`'i gerektiriyor.
