---
id: T44
title: Önizleme paneli tekerlekle büyüsün, fare gidince kendiliğinden insin, köşedeki anahat görünsün
role: ui-builder
model: opus
depends: []
status: submitted
round: 2
audit: passed
auditor_id: a8826ff0441cd7821
diff: src/VidShrink.App/Playback/ZoomGesture.cs, src/VidShrink.App/Playback/ComparisonPanel.axaml, src/VidShrink.App/Playback/ComparisonPanel.axaml.cs, src/VidShrink.App/Playback/HoverZone.cs, src/VidShrink.App/Themes/Playback.axaml, tests/VidShrink.Tests/ZoomGestureTests.cs, tests/VidShrink.Tests/ComparisonPanelTests.cs
verification: dotnet build -c Release 0 uyari 0 hata · dotnet test basarisiz 0, basarili 553, atlanan 16, toplam 569 · uc kademe olculdu band 610x144, orta 1713,6x891, tam 1904x990, yer tutucu uc kademede de 144px · kose kesme 608x142 yaricap 16, (1,1) kesmenin disinda · fare pencereden cikinca inis sayaci kuruluyor, terfi kalkinca dinleyici sokuluyor
owns: [src/VidShrink.App/Playback/ZoomGesture.cs, src/VidShrink.App/Playback/ComparisonPanel.axaml, src/VidShrink.App/Playback/ComparisonPanel.axaml.cs, src/VidShrink.App/Playback/HoverZone.cs, src/VidShrink.App/Themes/Playback.axaml, tests/VidShrink.Tests/ZoomGestureTests.cs, tests/VidShrink.Tests/ComparisonPanelTests.cs]
side_effects: []
---

## Amaç

Kullanıcının üç cümlesi:

1. "Yakınlaştır dediğim anda ilk etapta panel sabit, video yakınlaşıyor; sonradan videoyu
   maksimuma alınca panel de maksimuma geliyor. Ben maksimuma gelmeden önce de panelin
   genişliği artabilsin, diğer panellerin üstüne çıksın istiyorum."
2. "Fare ile odak onun üstünde değilse 2 saniye boyunca geri ilk açıldığı gibi minimize
   olacak."
3. "Ortadaki minimize hâlindeyken mavi anahat köşelerde gözükmüyor, bunu da çöz."

## Bugünkü mekanizma — okuman gereken yer burası

**`ZoomGesture.cs`** tek parametre tutuyor: `T`, 0'dan 1'e. `ContentZoom = 1 + T*(4-1)`,
yani tekerlek çevrildikçe **görüntü** sürekli yakınlaşıyor. Ama `Promoted` yalnız
`T >= PromoteAt (1.00)` olunca doğru oluyor, `T < DemoteAt (0.92)` olunca yanlış.
`NotchStep` bu bandın genişliği (0.08), yani tavandaki tek çentik inişi tetikliyor.
Sınıf bilerek saf: hiçbir Avalonia türü kullanmıyor, penceresiz sınanabiliyor.

**`ComparisonPanel.axaml.cs`** — `SyncShelter()` `_gesture.Promoted`'ı okuyup `Promote()`
ya da `Land()` çağırıyor. `Promote()` hep-ya-hiç: `Shell` `Root` ızgarasından sökülüyor,
`OverlayLayer`'a ekleniyor, `Fill()` ile **pencerenin tamamına** yayılıyor. Yerinde
`Placeholder` aynı boyutta görünür kalıyor, alttaki düzen zıplamıyor.

Kullanıcının şikâyeti tam olarak bu: T=1'e kadar panel kıpırdamıyor, T=1'de bir anda
tam ekran oluyor. Arada hiçbir şey yok.

**`HoverZone.cs`** — ikinci maddenin deseni zaten burada: kuşak sayaçlı gecikmeli
gizleme. Fare çıkınca zamanlayıcı kurulur, araya yeni bir gösterme girerse eski tik
kendi kuşağını taşımadığını görüp kararını uygulamaz. Denetim şeridi bunu kullanıyor.
Yeniden yazma — ya bu sınıfı genelleştir ya da aynı kuşak korumasını kullan.

**Düzen.** `MainWindow.axaml:461` — üç sütunlu ızgara, önizleme ortadaki sütunun başında
(`Grid.Column="1"`, `Grid.Row="0"`). "Yan panellerin üstüne taşmak" soldaki ve sağdaki
sütunun üstüne taşmak demek; bunun bugünkü tek yolu `OverlayLayer`.

## Kabul kriterleri

### K1 — Panel maksimuma gelmeden önce de büyüsün: üç kademe

Bugün iki durum var (bant, tam pencere) ve aradaki geçiş T=1'de bir anda oluyor. Araya
**bir kademe** girecek:

1. **Bant** — bugünkü yerinde, ortadaki sütunun içinde.
2. **Orta boy** — `OverlayLayer`'da çizilir, soldaki ve sağdaki sütunun üstüne taşar,
   ama pencerenin tamamını kaplamaz.
3. **Tam pencere** — bugünkü `Fill()` hâli.

Kademe geçişleri `ZoomGesture`'daki bugünkü desenle olacak: eşik + histerezis. Yükseliş
ve iniş eşikleri farklı kalacak ki titrek tekerlek kademeler arasında çırpınmasın.
`NotchStep` bugün bandın genişliğine eşit; üç kademede de aynı ilişki korunsun, yani
tekerlek çentiği ile eşikler birbirini tutsun ve hiçbir kademe tek çentikle atlanamasın.

Tek parametre kuralı bozulmayacak: kademe de `T`'den türeyecek, ikinci bir sayaç
açılmayacak. `ZoomGesture` saf kalacak — Avalonia türü girmeyecek. Görüntü
yakınlaştırması (`ContentZoom`) bugünkü gibi sürekli kalacak; kademelenen yalnız panelin
boyu.

**Orta boyun ölçüsü temadan gelecek**, koda gömülmeyecek — yeni bir ölçü uydurma, var
olan belirteçlerden türet ya da `Playback.axaml`'a var olanların yanına aynı üslupla bir
belirteç ekle.

`Placeholder` **hep bandındaki özgün boyu** tutacak; kademe değişince yer tutucu
değişmeyecek, altındaki düzen hiç zıplamayacak.

**Sürekli büyütme denenmedi ve bilerek seçilmedi.** İkinci görüş (fable): sürekli
modelde yer tutucu her tekerlek tikinde boy değiştirir, ya alttaki düzen sürekli akar ya
da panel ile boşluk ilişkisi kopar; ayrıca panelin kenarı farenin altında hareket ettiği
için K2'nin "fare üstünde mi" testi kendi kendini tetikler ve iniş titrer. Ayrık
kademeler bu döngüyü kırıyor.

### K2 — Fare üstünde değilken 2 saniyede insin

Panel büyümüşken fare panelin dışına çıkarsa 2 saniye sonra `T` tabana dönecek ve panel
bandına inecek.

İnişi engelleyen sebepler (`HoverZone.Hold` deseni): fare panelin üstünde, klavye odağı
panelde, kullanıcı ayırıcıyı ya da panoyu sürüklüyor. Biri bile doğruyken sayaç çalışmaz.

Fare kenardan çıkıp hemen geri girerse panel inip kalkmayacak — kuşak koruması bu yarışı
zaten çözüyor, aynısını kullan.

İki saniye **temadan** gelecek, koda gömülmeyecek. `Playback.axaml` içindeki süre
belirteçlerinin yanına yeni bir belirteç koy; adı ne yaptığını söylesin.

`HoverZone.MotionReduced` doğruyken geçiş animasyonu olmayacak, iniş anında olacak —
K5'in bugünkü kuralı korunuyor.

**İki tuzak, ikisi de ikinci görüşten çıktı — ikisini de kapat:**

1. **Zaman aşımı `T`'yi de sıfırlamalı.** Geri dönüşün iki tetikleyicisi var: tekerleğin
   geri çevrilmesi ve 2 saniyelik zaman aşımı. İkisi aynı durum makinesine girmezse
   çatışırlar — panel küçülür ama `T` tavanda kaldığı için sonraki tek tekerlek dokunuşu
   paneli doğrudan tam pencereye fırlatır. Zaman aşımı `Demote()`/`Reset()` yolundan
   geçsin.
2. **"Fare panelin üstünde mi" testi hedef kademenin sınırına karşı yapılacak**, panelin
   animasyon sırasındaki anlık sınırına karşı değil. Panel farenin altına doğru büyüyüp
   küçülürken `PointerEntered`/`PointerExited` yanlış ateşlenir.

### K3 — Mavi anahat köşelerde görünecek

`Shell` `Panel` temasını kullanıyor: `BorderBrush = NeonBlueBorder`,
`CornerRadius = RadiusPanel (16)`. İçindeki `Stage` bir `Grid` ve
`Background="{StaticResource AppBg}"` taşıyor — `Grid`'in köşe yarıçapı yok, bu yüzden
kare arka plan yuvarlak kenarlığın dört köşesini örtüyor. Kenarlık çiziliyor ama
köşelerde görünmüyor.

Düzelt: iç arka plan da aynı yarıçapla kessin. Yeni renk ya da yeni ölçü **uydurma** —
`RadiusPanel` zaten var, onu kullan.

Aynı kusur `RightCurtain`'da da olabilir (`ComparisonPanel.axaml:42`, yine `AppBg`,
yine köşesiz); panelin sağ köşelerine değiyorsa onu da düzelt.

## Doğrulama

- `dotnet build -c Release` 0 uyarı · `dotnet test` tamamı yeşil, **518'in altına düşmeden**
- K1: `ZoomGesture` üzerinden penceresiz ölçüm — üç kademenin de `T`'den doğru çıktığı,
  eşiklerin histerezisli olduğu ve hiçbir kademenin tek çentikle atlanamadığı
- K2: iniş sayacının kuşak korumasının sınandığı ölçüm — çık/gir/çık dizisinde panelin
  inip kalkmadığı; ayrıca zaman aşımından sonra `T`'nin tabanda olduğu ve ilk tekerlek
  dokunuşunun paneli tam pencereye fırlatmadığı
- K3: köşe pikselinin gerçekten kenarlık rengini taşıdığı ya da iç arka planın yarıçapı
  taşıdığı, biçimlemeden ya da ölçümden okunarak
- Koşturulamayan her adımın açık listesi

## Uyarılar

**Ekran kapısı kapalı.** `VidShrink.App.exe` açamazsın, `dotnet run` da engellenir.
Doğrulamayı test projesinden yap: `tests/VidShrink.Tests/AppHost.cs` Avalonia'yı kendi
STA iş parçacığında kuruyor ve `AppHost.Run(() => new MainWindow())` çalışıyor
(`WindowLayoutTests.cs:68`). Pencere açmadan ölçmenin yolu bu.

**Renk ve ölçü uydurma.** Hepsi `Themes/Theme.axaml` ve `Themes/Playback.axaml`
belirteçlerinden gelecek. Yeni bir süre belirteci eklemen gerekiyorsa var olanların
yanına, aynı üslupla ekle.

**Kod yorumu:** bu projede açıklayıcı `///` özet yorumları var ve o üslup korunur; satır
içi "ne yaptığını anlatan" yorum yazma.

**Geçici dosya:** hepsi `.calisma/` altına, iş bitince sil.

**Bu projenin tekrar eden kusuru:** kod doğru, onu özetleyen cümle yanlış. Altı
sözleşmede tekrarladı. Rapora giren her sayıyı ham çıktıdan kopyala, hafızandan yazma;
koşturamadığını "doğrulandı" diye yazma.

## Kayıt noktası

Her kriter bitince Rapor'a tek satır düş ve commit at.

## Çıktı

Rapor: kriter başına ne yapıldı, ham ölçümler, koşturulamayanlar, sapmalar.

## Rapor

- **K1 — üç kademe.** `ZoomGesture` artık `ShelterStage` (Band/Mid/Full) döndürüyor; sınıf
  hâlâ saf, tek parametre `T`. Eşikler: Mid çıkış 0,50 (aralığın tam ortası), Mid iniş 0,42;
  Full çıkış 1,00, Full iniş 0,92. İki iniş bandı da tam bir çentik (`NotchStep` = 0,08).
  Ölçüm (1560x1060 istenmiş, pencere 1904x990 çalışma alanına oturuyor): band 610x144,
  orta 1713,6x891 @ (95,2 · 49,5), tam 1904x990. Orta kademenin oranı `PlaybackMidShare`
  (0,9 — `WindowWorkAreaShare` ile aynı sayı). Yer tutucu üç kademede de 144 px kaldı.
- **K2 — 2 saniyede iniş.** Gecikme `PlaybackDescendDelay` (00:00:02) belirtecinden.
  Sayaç `HoverZone`'un kendi kuşak koruması; ikinci kopya yazılmadı, sınıfa yalnız
  `PointerWithin`/`Reset`/`ShouldHide` kapıları eklendi. Tuzak 1: zaman aşımı `Descend()`
  yolundan geçiyor — ölçüm zaman aşımından sonra `t=0`/Band, ardından tek çentikte
  `t=0,08`/Band veriyor, yani panel tam pencereye fırlamıyor. Tuzak 2: fare testi
  `StageTarget` (hedef kademenin sınırı) üstünden; kabuğun anlık sınırı hiç okunmuyor.
  Tutma sebepleri: ayırıcı sürükleme, pano sürükleme, panel içindeki bir öğede klavye odağı
  (kabuğun terfi anında kendi aldığı odak sayılmaz, yoksa iniş hiç çalışmazdı).
- **K3 — köşe.** `Stage` panosu `RadiusPanelScalar` (16) yarıçaplı bir `RectangleGeometry`
  ile kesiliyor; kesme bütün çocukları kapsadığı için `RightCurtain` da aynı köşeyi taşıyor,
  ikinci bir düzeltme gerekmedi. Ölçüm: kesme 608x142, yarıçap 16, (1,1) noktası kesmenin
  dışında.
- **Ham doğrulama.** `dotnet build -c Release` → 0 Uyarı, 0 Hata.
  `dotnet test` → Başarısız 0, Başarılı 535, Atlanan 16, Toplam 551 (taban 518/534).
- **Koşturulamayan.** Duvar saatiyle 2 saniye beklenmedi: ölçüm iş parçacığında ileti
  döngüsü yok, `DispatcherTimer` tik atmıyor. Bunun yerine zamanlayıcının çağırdığı iki
  şey ayrı ayrı ölçüldü — karar kapısı `HoverZone.ShouldHide(kuşak)` ve zaman aşımının
  gittiği yol `ComparisonPanel.Descend()`. Ekran kapısı kapalı olduğu için hiçbir ekran
  görüntüsü alınmadı; köşe kanıtı piksel değil geometri (kesme şeklinin `FillContains`
  sonucu).
- **Sapma.** Kök katmanın (`OverlayLayer`) sınırı başsız ölçümde 0x0 kalıyor — katman bir
  `Canvas` ve ölçüsünü çocuklarından almıyor. Kademe ölçüsü bu yüzden katmanın sınırı boşsa
  katman yöneticisinin sınırından okunuyor (`OverlayArea`). Bu, bugünkü `Fill()` yolunda da
  sessizce 0 boy üretebilecek bir açıktı.

---

## Tur 2 — denetim KALDI, düzeltilecek

Denetçi: `audit: failed`, tur 1. K1 ve K3 geçti, **K2 kaldı**, Doğrulama kanıtsız.

### K2 kusuru — T0 tarafından koddan doğrulandı

**Fare pencereden çıkınca 2 saniyelik iniş sayacı hiç kurulmuyor.**

Terfi ederken tek dinleyici kuruluyor:

    ComparisonPanel.axaml.cs:457
        top.AddHandler(PointerMovedEvent, OnGlobalPointerMoved, RoutingStrategies.Tunnel);

`_descent`'in "fare içeride mi" durumunu güncelleyen tek yol bu (`:526 TrackPointer`,
`:528 OnGlobalPointerMoved`). Fare pencerenin dışına çıktığında `PointerMoved` artık
atmaz; `HoverZone._pointerInside` `true` takılı kalır, `Evaluate()` hiç çağrılmaz,
`ScheduleHide()` hiç koşmaz. Tam kademede kabuk bütün pencereyi kapladığı için bu, panelin
**kendi kendine hiç inememesi** demek.

Kullanıcının cümlesi buydu: "eğerki fare ile odak onun üstünde değilse 2 sn boyunca geri
ilk açıldığı gibi minimize olacak." Fareyi pencereden çıkarmak bunun en olağan hâli.

`PointerExited` bugün yalnız iki yerde bağlı (`:87` ayırıcı tutamağı, `:100` şerit) —
kabuğun ya da `TopLevel`'ın çıkışını kimse dinlemiyor.

### İstenen

1. Fare pencereden çıktığında da iniş sayacı kurulsun. `TopLevel` üzerinde
   `PointerExitedEvent` ile ya da eşdeğer bir yolla; hangisini seçtiğini gerekçesiyle yaz.
2. Terfi kaldırılırken (`:588` çevresi) eklenen her dinleyici geri sökülsün.
3. Bunu **penceresiz ölçen bir test** yaz — fare pencereden çıkmış sayıldığında
   `_descent`'in iniş kararına geçtiğini doğrulasın. Var olan tuzak korunsun: karar
   `StageTarget` hedef sınırına karşı verilir, kabuğun animasyon sırasındaki anlık
   sınırına karşı değil.

### Doğrulama — bu tur zorunlu

Rapora **ham çıktı** yaz, cümleyle özetleme:

- `dotnet build -c Release` çıktısının uyarı/hata satırı birebir
- `dotnet test` çıktısının "Başarısız / Başarılı / Atlanan / Toplam" satırı birebir
  (tur 1 sonrası taban: 535 başarılı, 16 atlanan, 551 toplam — altına düşmesin)

### Commit kuralı

`git add` her zaman **açık yolla**, yalnız `owns` kümendeki dosyalar. Asla `git add -A`,
asla `git commit -a`. Tur 1'de bu kural sana verilmedi ve senin commit'lerin başka bir
sözleşmenin üç dosyasını süpürdü (`f6f1f62`, `cf157cd`). Kayıt:
`~/.claude/teknesyum/openlogs/HATA-paralel-ajanlar-ayni-git-indeksini-paylasti.md`
Geçmişi düzeltme, sadece bir daha yapma.

## Rapor — tur 2

- **K2 kusuru kapatıldı.** Terfi ederken `TopLevel` üzerine ikinci bir dinleyici bağlanıyor:
  `top.AddHandler(PointerExitedEvent, OnGlobalPointerExited, Tunnel | Bubble | Direct)`.
  Neden `TopLevel`: fare pencerenin dışındayken `PointerMoved` hiç atmıyor, dolayısıyla
  yalnız harekete bakan sayaç "fare içeride" hâlinde takılı kalıyordu; kabuğun kendi
  `PointerExited` olayını dinlemek de yetmezdi, çünkü tam kademede kabuk zaten bütün
  pencereyi kaplıyor ve o kademede "kabuktan çıkmak" ile "pencereden çıkmak" aynı şey.
  Yönlendirme ilkesi üç seçenekle birden verildi ki olayın Direct mi Bubble mı kayıtlı
  olduğu kararı değiştirmesin. Çocuktan kabaran çıkışlar `ReferenceEquals(e.Source, sender)`
  ile eleniyor: panel içindeki bir öğeden diğerine geçmek pencereyi terk sayılmıyor.
  Karar yolu değişmedi — çıkış `_descent.PointerWithin(false)` diyor, hedef sınır kuralı
  (`StageTarget`) yerinde duruyor, kabuğun anlık sınırı hiçbir yerde okunmuyor.
- **Sökme.** `Settle()` içinde `top.RemoveHandler(PointerExitedEvent, OnGlobalPointerExited)`
  eklendi; `KeyDown` ve `PointerMoved` zaten sökülüyordu. Üçü de aynı blokta.
- **Penceresiz testler (4 yeni).** `Fare_pencereden_cikinca_inis_sayaci_kurulur` gerçek bir
  `PointerExitedEvent`'i pencerenin üstünde `RaiseEvent` ile atıyor ve sayacın kuşağının
  ilerlediğini, bekleyen tikin artık inişe karar verdiğini doğruluyor — yani dinleyicinin
  bağlı olduğunu olayın kendisiyle kanıtlıyor.
  `Terfi_kalkinca_pencere_dinleyicileri_sokulur` panel bandına indikten sonra aynı olayı
  atıyor ve kuşağın kıpırdamadığını görüyor.
  `Pencere_disina_cikis_hedef_sinira_gore_kararlidir` çık/gir dizisinde eski tikin kararını
  uygulamadığını gösteriyor.
  `Inis_baslamisken_tekerlek_geri_yukari_cevrilebilir` bekleyen inişin iptal edildiğini
  doğruluyor.

### Ham çıktı

`dotnet build -c Release`:

    0 Uyarı
    0 Hata

`dotnet test`:

    Başarılı!  - Başarısız:     0, Başarılı:   553, Atlanan:    16, Toplam:   569, Süre: 12 s - VidShrink.Tests.dll (net8.0)

Toplam sayı tabandan (535/16/551) 18 fazla: 4'ü bu turda eklendi, kalan 14 bu tur sırasında
`main`e giren T45 sözleşmesinden geliyor.

### Koşturulamayan

Duvar saatiyle iki saniye yine beklenmedi: ölçüm iş parçacığında ileti döngüsü yok,
`DispatcherTimer` tik atmıyor. Ölçülen şey zamanlayıcının iki ucu — kurulup kurulmadığı
(kuşak ilerlemesi) ve tikin vereceği karar (`HoverZone.ShouldHide`). Sürenin kendisi
`PlaybackDescendDelay` belirtecinden okunarak ayrıca sınanıyor.

### Kimlik uyuşmazlığı — tur 2 sonu

Durdurma kancası bu ajanın beyan edilen eforla koşmadığını bildirdi: beyan `xhigh`, gerçek
`medium`. Efor kendi elimde olan bir ayar değil, ajan bu kimlikle başlatıldı; bu turun
işini olduğu gibi bırakıyorum, yeniden açma kararı T0'ın. Yapılan iş ve ham çıktılar
yukarıda; kabul edilip edilmeyeceği doğru eforla yeniden koşulduktan sonra kararlaştırılsın.


### T0 kararı — efor uyuşmazlığı

Tur yeniden koşulmuyor. Efor, işin doğruluğunu değil üretim biçimini anlatır; ortada
ölçülebilir bir çıktı var ve T0 onu bağımsız doğruladı:

    dotnet build -c Release   ->  0 Uyarı, 0 Hata
    dotnet test               ->  Başarısız: 0, Başarılı: 553, Atlanan: 16, Toplam: 569

K2 düzeltmesi koddan görüldü: `ComparisonPanel.axaml.cs:458` `PointerExitedEvent`
dinleyicisi, `:545` `OnGlobalPointerExited` (`ReferenceEquals` süzgeciyle), `:556`
`PointerLeftWindow() => _descent.PointerWithin(false)`, `:613` sökme.

Kimlik/efor uyuşmazlığı eklenti katmanının işi, sözleşmenin değil. İş denetime gidiyor;
kararı denetçi verecek.
