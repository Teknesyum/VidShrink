---
id: T123
title: Surucusuz makinede donanim kodlayici seciliyor
role: builder
model: opus
depends: []
status: active
round: 2
verify: [dotnet test -c Release --filter "EncoderAvailabilityTests|PerformanceCheckTests"]
owns: [src/VidShrink.Ffmpeg/PerformanceProbe.cs, tests/VidShrink.Tests/EncoderAvailabilityTests.cs, docs/olcumler/surucu-yoklugu.md, tools/surucu-yoklugu/Program.cs, tools/surucu-yoklugu/Dagilim.cs, tools/surucu-yoklugu/surucu-yoklugu.csproj]
side_effects: []
---

## Baglam

T118 CI'da dusen tek olcuyu kimlikle buldu: `PerformanceCheckTests.
IslemciZamaniSayaciDogruOkuyorMu`, sebep `Cannot load nvcuda.dll`. Ayni olcu bu
makinede yesil. T118 dogru tesbit etti: `HasEncoder` **derlenmis ffmpeg
yetenegine** bakiyor, gercek surucuye degil.

Bu bir olcu kusuru degil. Uretim kodunda ayni yanlis okuma karar veriyor.

`PlanCalculator.cs` icinde iki kardes fonksiyon var ve **ikisi ayni soruyu
farkli soruyor**:

```
PickFastCodec  : availability.WorksAsEncoder(candidate)   -> gercek yoklama
PickCodec :780 : availability.HasEncoder(preferred)       -> yalniz derleme listesi
```

`PreferredCodecFor(CodecPreference.Fast)` = `h264_nvenc`. Yani nvenc'li
derlenmis bir ffmpeg'i olan ama **calisir surucusu olmayan** bir makinede
"Hizli" kipi `h264_nvenc` seciyor ve kodlama calisma aninda dusuyor.

`PerformanceProbe.cs:81` ayni yanlis okumayi tekrarliyor:
`HardwareCandidates.FirstOrDefault(availability.HasEncoder)`.

Dogru desen zaten **ayni dosyada, yan komsuda** duruyor. Bu bir tasarim karari
degil, gozden kacma.

Neden onemli: hedefimiz "hicbir ayar bilmeyen kullanici uzmana yakin sonuc
alsin". Ayar bilmeyen kullanici tam olarak surucusunu bilmeyen kullanicidir.

## Kabul kriteri

1. **Once uret, sonra duzelt.** Surucusuz bir ortamda "Hizli" kipinin
   `h264_nvenc` sectigini **olc.** CI bunun icin hazir bir yatak: nvenc'li
   ffmpeg var, NVIDIA surucusu yok, T118 belirtinin orada ciktigini gosterdi.
   Yerelde sahte `IEncoderAvailability` ile de uretilir — ikisi de kabul, ama
   uretilmeden duzeltme yazilmaz.
   CHECK: kusurun uretildigi komut ve cikti.

2. **Yoklamanin maliyeti olculur.** `WorksAsEncoder` surec doguruyor,
   `HasEncoder` dogurmuyor. `PickCodec`'i gercek yoklamaya bagladiginda plan
   hesabi ne kadar yavasliyor? Sicak ve soguk onbellekte ayri ayri, milisaniye.
   Yavaslama kabul edilemez cikarsa **sebep budur** diye yazilir ve ucuncu bir
   yol aranir (ornegin arka planda isitma — `WarmEncoderOption` deseni).
   CHECK: yoklamali/yoklamasiz plan hesabi suresi.

3. **`Probe` onbellegi surec omru boyunca.** `EncoderCapabilities.Probe`
   sonucu sakliyor; bu iyi ama **basarisiz yoklama da saklaniyor.** Surucu
   sonradan yuklenirse ne oluyor? Olc ve yaz. Davranis degistirilecekse
   gerekcesi yazilir.

4. **`PerformanceProbe.cs:81` ayni karara baglanir.** Donanim adayini derleme
   listesinden secmek, olcumun kendisini surucusuz makinede anlamsiz kiliyor.

5. **Olcu davranis olcsun.** Sahte `IEncoderAvailability` ile: derleme listesi
   `h264_nvenc` iceriyor ama yoklama basarisiz -> secilen kodlayici **yazilim**
   olmali. Mutasyon: `WorksAsEncoder`'i `HasEncoder`'a geri cevir, olcu kirmizi
   donsun. Iki sabiti karsilastiran olcu yazma, `Skip` yazma.
   CHECK: mutasyon iki yonde de kirmizi.

6. **Geri dusme zinciri yazilir.** Bugun `FallbackCodecFor(Fast)` = `libx264`.
   Donanim yoklamasi duserse kullaniciya ne oluyor: sessizce yavaslıyor mu,
   haber veriliyor mu? Kullanici dostu cevap "sessizce dogru olani yap" ise
   oyle kalir, ama **olculur ve yazilir.**

7. **Olcmedigin sey icin "olculmedi" yaz.** Bu projenin kronik kusuru: dogru
   tablonun ustune yanlis ozet cumlesi. On bir kez tekrarladi.

## Sinirlar

- **Iki asamali sahiplik.** K1-K4 bugun yapilabilir ve hicbir carpismasi yok:
  `PerformanceProbe.cs` bos, olcum ve olcu yazimi serbest. `PlanCalculator.cs`
  T107'nin, `EncoderCapabilities.cs` T94'un — **ikisine de yazma.** T107
  kapaninca `PlanCalculator.cs` senin `owns`'una eklenecek ve `PickCodec`
  duzeltmesini o zaman yazacaksin. O ana kadar duzeltmeyi **hazirla ve olcuyle
  kanitla**, uygulama. `EncoderCapabilities.cs`'te degisiklik gerekiyorsa
  rapora yaz, T0 yonlendirir.
- Tam suiti kosturma; yalniz `verify` filtresi.
- Olcum dosyalari `.calisma/` altina, duzenek `tools/` altina.
- Makine paylasimli, on dort ajan kosuyor. Sure sayisina "makine paylasimliydi"
  damgasi bas.
- Kod yorumu yazma.

## Kayit noktasi

Her kabul kriteri bittiginde commit. Dal: `T123-surucu-yoklugu`.

## Cikti

- `docs/olcumler/surucu-yoklugu.md`: kusurun uretimi, yoklama maliyeti,
  onbellek davranisi, geri dusme zinciri.
- Surucusuz makinede dogru kodlayiciyi secen plan hesabi.
- Kendi dalin, itilmis. `main`e sen birlestirme.


## Aciliyet — bu sozlesme T115'in muhur kapisinda (T0, 2026-09-02)

T118'in denetcisi olctu: `origin/main` ci.yml'inde ffmpeg kurulumu yok
(`grep -c 'codexffmpeg'` -> 0). Bu yuzden **main'in bugunku yesili bu olcunun
gectigini degil hic kosmadigini gosteriyor.** T115 ffmpeg'i CI'ya kuruyor;
kurdugu anda `PerformanceCheckTests.IslemciZamaniSayaciDogruOkuyorMu` kosmaya
basliyor ve `Cannot load nvcuda.dll` ile kirmiziya donuyor.

Iki farkli commit, iki kosum, ayni test — kararsizlik degil, belirlenimci:

```
33589639249  commit 0e122f2  Failed: 1  ...  Cannot load nvcuda.dll
33593652976  commit 045648e  Failed: 1  Passed: 1162  Skipped: 17  Total: 1180
```

Olcunun ortam gecidi T117'nin isi. **Senin isin uretim kodundaki ayni yanlis
okuma** — kullanici surucusuz makinede `h264_nvenc` ile kaliyor. Ikisi ayri
kusur, ayni kok.


## Kayit noktasi — teslim

K1-K6 bitti. Dal `T123-surucu-yoklugu`, itildi (8554132). Bes dosya, hepsi `owns` icinde.
`PlanCalculator.cs` ve `EncoderCapabilities.cs`'e yazilmadi — `git diff HEAD --` ikisi
icin de bos.

## Rapor

**verify:** `dotnet test -c Release --filter "EncoderAvailabilityTests|PerformanceCheckTests"`
-> Basarisiz: 0, Basarili: 9. **`PlanCalculatorCodecTests` diye bir sinif depoda yok**,
filtre o addan 0 olcu esledi; pratikte yalniz `EncoderAvailabilityTests` kosuyor.
`_sorun.log`'a yazildi.

**K1 — kusur uretildi.** Sahte `IEncoderAvailability` (derleme listesinde nvenc var,
yoklama yalniz yazilimi geciriyor), `Codec=Fast, SpeedMode=Quality`:
secilen kodlayici `h264_nvenc`, `EncoderFallback` notu **yok**. Ayni makinede
`SpeedMode=Fast` dogru cevabi (`libx264` + `EncoderFallback`) veriyor. Uretim komutu ve
tam cikti raporda.

**K2 — maliyet olculdu, kabul edilemez.** Sicak onbellekte maliyet yok (0,16 -> 0,18 ms).
Soguk onbellekte bos makinede 237 ms, on dort ajan kosarken 2951 ms, yedi adayin tamami
yoklandiginda **90 517 ms**. Kabul edilemez olan ortalama degil yayilim: ayni is, ayni
makinede 12,4 kat fark (2951/237, tek kodlayicilik tablosu); yedi adaylik soguk yuruyus
28 163 ms ile `PerformanceProbe.BudgetMs`'i (20 000) tek basina asiyor.
**[Tur 2'de geri cekildi: 360 kat iki ayri duzenegin tablosundan birlestirilmisti.
90 517 ms tabloda duruyor ama artik hukum tasimiyor; hukum 28 163'e dayaniyor.]** Ucuncu yol onerisi raporda: `WarmEncoderOption` deseni, arka planda isitma.
**Sure sayilari makine paylasimliydi.**

**K3 — onbellek.** Basarisiz yoklama da kosulsuz saklaniyor: 97,33 -> 0,13 -> 0,05 ms.
Surucu sonradan yuklenirse hicbir sey olmuyor: `_probed`'i bosaltan yol yok (`Clear`,
`Remove`, sure asimi — ucu de kodda gecmiyor), uygulama surec omru boyunca "donanim yok"
diyor. Cozum kapatip acmak, kullaniciya soylenmiyor. `EncoderCapabilities.cs` T94'un,
davranis degistirilmedi.

**K4 — `PerformanceProbe.cs:81` duzeltildi.** `SelectHardwareCodec`: derleme
listesindeki adaylar gercek yoklamayla yurunur, **calisan ilk aday** secilir. Hicbiri
calismiyorsa `null` degil listedeki ilk adaya donulur — cunku
`PerformanceCheck.Evaluate` `HardwareEncoderFailed` ile `NoHardwareEncoder`'i ayiriyor ve
`null` donmek birincisini ulasilmaz yapardi. Secim `RunAsync` butcesine baglandi;
`Stopwatch` artik secimden once basliyor, `BudgetMs`'in belge cumlesi guncellendi.

**K5 — mutasyon iki yonde de kirmizi.**
`SelectHardwareCodec` `WorksAsEncoder`->`HasEncoder`: 3/9 olcu dustu.
`PickFastCodec` `WorksAsEncoder`->`HasEncoder`: 1/9 dustu
(`SurucusuzMakinedeHizliKipYazilimaDusuyor`). Ikinci mutasyon `PlanCalculator.cs`
uzerinde olculup geri alindi; `git diff HEAD --` o dosya icin bos.

**K6 — geri dusme zinciri.** Sessizce yavaslamiyor: `EncoderFallback` notu ve gerekce
cumlesi var, tahmini kalite de dusmuyor
**[Tur 2'de geri cekildi: 74,3 / 76,5 sayilarinin ureten komutu yoktu; `geridusme`
kodlayici, not ve gerekce basiyor, kalite puani basmiyor. Geri dusmenin kaliteye
etkisi olculmedi.]**. Ama gerekce
cumlesi "not available on this ffmpeg build" diyor; surucusuz makinede kodlayici
derlemede **var**, eksik olan surucu. Bugun cumle dogru, `PickCodec` gercek yoklamaya
baglandigi an yalan soylemeye baslar.

### T0 kararı gerekli

1. **`PickCodec` duzeltmesi tek basina yetmiyor.** Yoklamanin kendisi yuke duyarli:
   ayni makinede, NVIDIA surucusu kurulu ve calisirken, `h264_nvenc` yoklamasi yuklu
   kosumda **False** (4539 ms), bos kosumda **True** (308 ms) dondu. Sebep
   `EncoderCapabilities.RunProbe`'un `WaitForExit(4000)` siniri: asan yoklama
   "calismiyor" diye okunuyor ve K3 geregi surec omru boyunca onbellekte kaliyor.
   `PickCodec` bugunku haliyle `WorksAsEncoder`'a baglanirsa deterministik yanlis cevap
   yuke bagli yanlis cevapla degisir. On kosul: (a) sure asimi "calismiyor" degil
   "olculemedi" olsun ya da sinir yukselsin, (b) sure asimiyla dusen yoklama
   onbellege girmesin. Ikisi de `EncoderCapabilities.cs`, T94'un.
2. **`PlanCalculator.cs` gerekce metni** de duzeltmeyle birlikte degismeli (K6). T107'nin.
3. `KusurKaydiPickCodecYoklamayaDegilDerlemeListesineBakiyor` bugunku yanlis ciktiyi
   sabitleyen bir kusur kaydi. `PickCodec` duzeltilince kirmiziya doner; duzeltmeyi
   yazan tur onu cevirmeli.

## Cikti — teslim edilen

- `docs/olcumler/surucu-yoklugu.md` — kusurun uretimi, yoklama maliyeti (iki yuk
  altinda), onbellek davranisi, yuk duyarliligi, geri dusme zinciri, olculmeyenler.
- `src/VidShrink.Ffmpeg/PerformanceProbe.cs` — `SelectHardwareCodec`, butceye bagli.
- `tests/VidShrink.Tests/EncoderAvailabilityTests.cs` — 9 olcu, mutasyon iki yonde kirmizi.
- `tools/surucu-yoklugu/` — olcum duzenegi.
- Dal `T123-surucu-yoklugu` itildi. `main`e birlestirilmedi.

**Surucusuz makinede dogru kodlayiciyi secen plan hesabi teslim edilmedi** — sinir geregi
`PlanCalculator.cs`'e yazilmadi. Duzeltme hazir ve olcuyle kanitlandi, uygulanmadi.

## Denetim tur 1 — GECTI, KRITIK yok (2026-09-02, uc 8554132)

Denetci depoya yazmadi; `git archive 8554132` kopyasinda calisti, tek `dotnet test`
kosturdu. Yedi kriterin yedisi karsilandi, `owns` ihlali yok (bes dosya, besi de listede).

**En agir iddia bagimsiz olarak dogrulandi — ajanin cumlesi degil, kod:**
`EncoderCapabilities.cs:111-115` sure asiminda `return false` veriyor;
`EncoderProbeResult` (`HardwareVerdict.cs:6`) sure asimini tasiyan bir alan icermiyor;
`Probe` sonucu kosulsuz onbellege yaziyor (`:83`); `_probed` icin `Clear`/`Remove` yok.
`HardwareVerdict.cs:17` ikisini **bilerek** ayni kovaya koyuyor. Yani calisan GPU'lu
kullaniciya surec omru boyunca "donanim yok" denmesi gercek. Duzeltme planinin on kosulu
gecerli — **once yoklamayi sertlestir, sonra `PickCodec`'i bagla** sirasi dogrulandi.

K4 gerekcesiz degil: `PerformanceCheck.cs:281-290` `HardwareEncoderFailed` ile
`NoHardwareEncoder` ayrimini yapiyor, `PerformanceProbe.cs:167` bu ayrimi
`hardwareCodec is not null` ile besliyor. Bu yuzden `SelectHardwareCodec` hicbiri
calismadiginda `null` degil ilk adayi donuyor.

### Borclar (tur acmaz, muhur notuna)

1. **`verify:` satiri yarim olcuyor — benim yazim hatam.** `PlanCalculatorCodecTests`
   depoda yok; dokuz olcunun tamami `EncoderAvailabilityTests.cs:56-158`. Filtre yesili
   "iki sinif gecti" degil, "bir sinif gecti, obur ad 0 olcu esledi" demek.
2. Degisen uretim kodunun asil olcusu `verify` disinda: `PerformanceProbe.RunAsync`i
   kullanan `PerformanceCheckTests.cs:318, 399-410, 531-539, 610, 631` filtreye girmiyor.
3. **Kronik desen.** `PerformanceCheckTests.cs:525-527` "tek bir surec bile
   baslatilmadan biten bu kosum" diyor; yeni `SelectHardwareCodec` butceyi adaylar
   arasinda kontrol ediyor (`PerformanceProbe.cs:96`), yani `RunAsync(..., 1)` ilk adayi
   yokluyor ve bir ffmpeg sureci doguruyor. Dosya `owns` disinda.
4. Secimin butceyi tumuyle yiyebildigi yol olculmedi: `:96` butcenin %100'unu
   harcayabilir, harcarsa `Remaining` 0 doner, `GenerateSampleAsync` `:253`te `false`
   verir, kosum `Incomplete` olur — **hicbir bacak olculmez.** Gercek surucusuz makinede
   dusen yoklamalar 48-96 ms'de donuyor, yani gerceci girdide dolmuyor; KRITIK degil.
   Ama `Olculmeyenler` listesinde de yok.
5. K6 dogru kusuru **eksik adreste** gosteriyor. Kullanicinin gordugu cumle
   `src/VidShrink.App/Locales/{en,tr}/main.json:283`, `MainWindow.axaml.cs:1918`den
   basiliyor. Core'daki cumleyi duzeltip arayuzdekini yalan birakma tuzagi acik.
6. Ureten komutu olmayan sayi: `surucu-yoklugu.md:226` "donanim 74,3/100, yazilim
   76,5/100" — `tools/surucu-yoklugu/Program.cs:198-200` bu sayiyi basmiyor.
7. Duzenek uretim kodunu kopyaliyor: `Program.cs:110-117` `SecimYurutusu`,
   `SelectHardwareCodec`in butcesiz kopyasi. Mansetteki 90 517 ms teslim edilen
   fonksiyondan degil bu kopyadan olculdu.
8. **90 517 ms belgenin kendi tablosuyla tutmuyor.** §4 (`:139-147`, kosum 1) ayni yedi
   adayin toplamini 28 163 ms veriyor — ayni kosum, 3,2 kat fark. 90 517/7 = 12,9 s,
   4000 ms'lik oldurme siniri (`EncoderCapabilities.cs:111`) cok altinda. "BudgetMs'i
   tek basina asiyor" hukmu bu tek, caprazlanmamis sayiya dayaniyor.
9. Ozet cumlesi iki tabloyu karistiriyor (`:87-88`): 12 kat tek kodlayicilik plan
   hesabindan, 360 kat yedi adaylik secimden. Iddia ikinci tablo icin dogru, cumlenin
   hemen ustunde saydigi sayilar icin degil. Kronik sinifin **on dorduncu** tekrari.

## Tur 2 — kapsam (T0, sozlesmeye gec yazildi)

**Usul notu, once bu.** Tur 1 **KRITIK'siz gecti.** Kendi kuralim tur acmayi
yalniz KRITIK'e baglar; ben tur 2'yi **borc uzerine** actim (borc 8: manset
90 517 ms belgenin kendi tablosuyla 3,2 kat tutmuyor). Kural bukuldu. Is
yapildi ve degerli cikti, ama bu kayit olmadan bukulme gorunmez kalirdi.
Ileride ayni sey olursa dogru yol borcu muhur notuna yazip sozlesmeyi
kapatmak, sonra gerekiyorsa **yeni** sozlesme acmaktir.

Kapsam ajana dagitimda sozlu verildi ve dosyaya yazilmadi — bu da ikinci
usul hatasi; denetci neyi olcecegini sozlesmeden okuyamaz hale geldi. Asagisi
teslim edilen isten ve kapattigi tur 1 borclarindan **geriye donuk** yazildi;
tur 2'nin talimatinin birebir kopyasi degildir.

Tur 2'de kapatilan:

- Sure asiminin **yuke gore dagilimi** olculdu; 4000 ms sinirinin hangi yuk
  basamaginda asildigi ve siniri yukseltmenin cozup cozmedigi ayrildi.
- **Uc yol** ayri ayri yazildi: (a) siniri yukselt, (b) sure asimini
  "olculemedi" say ve onbellege yazma, (c) arka planda isit.
- `_probed` temizleme yolu — surucu sonradan kurulursa.
- Tur 1 borclarindan dordu kapatildi: `verify` filtresi gercek sinif adlarina
  cevrildi (borc 1), 90 517 hukmu iki tablonun **ortak** soyledigine indirildi
  (borc 8), 12/360 kat cumlesi tablolarina ayrildi (borc 9), ureten komutu
  olmayan 74,3/76,5 sayilari cikarildi (borc 6).

Kapatilmayanlar tur 1 borc listesinde duruyor: 3, 4, 5, 7.

Borc 2 (degisen uretim kodunun asil olcusu `verify` disinda) tur 2'de kapandi:
yeni filtre `PerformanceCheckTests`'i iceriyor ve 28 olcu topluyor. Denetci
dogruladi; bu satir tur 2'de guncellendi.
