---
id: T132
title: Duvar saatine bagli bes iddia — CI'i kararsiz yapan son yuzey
role: builder
model: sonnet
depends: []
status: submitted
round: 3
verify: [dotnet test -c Release --filter "PerformanceCheckTests|UpdaterTests|ComplexityProbeTests"]
owns:
  - tests/VidShrink.Tests/PerformanceCheckTests.cs
  - tests/VidShrink.Tests/UpdaterTests.cs
  - tests/VidShrink.Tests/ComplexityProbeTests.cs
  - docs/olcumler/duvar-saati-iddialari.md
side_effects: []
---

## Baglam

T127 `SplitDragTests`in duvar saati bagimliligini kapatti: saat enjekte edildi,
`Assert.True(repaints >= 1)` gibi genis bir bant `Assert.Equal(15, repaints)`e
sikildi ve mutasyon (`SplitPaintInterval` 1/60 -> 1/50) olcuyu gercekten kirdi.
Ayni sinif kusur o dosyada **uc kez CI'i kirmizi yapti** ve dorduncude sozlesmeye
dondu.

Denetci ayni taramayi genisletti ve **kapsam disinda bes iddia daha** buldu.
Bunlar bugun kirmizi degil; **kararsiz.** Makine yuklendiginde, CI kosucusu
yavasladiginda ya da paralel kosum arttiginda dusecek olan yuzey burasi.

Alti iddia var; altincisi asagida K0 olarak ayri duruyor cunku o bugun dustu.
Ilk bes, yerleriyle:

| # | yer | iddia | genislik |
|---|---|---|---|
| 1 | `PerformanceCheckTests.cs:757` | `if (OperatingSystem.IsWindows()) Assert.InRange(saat.ElapsedMilliseconds, 1500, 15_000)` | **10 kat** — en genisi |
| 2 | `PerformanceCheckTests.cs:618` | `Assert.True(basladi.ElapsedMilliseconds < genisSaat.ElapsedMilliseconds / 2, ...)` | iki duvar saatinin orani |
| 3 | `PerformanceCheckTests.cs:854` | `Assert.True(clock.ElapsedMilliseconds > 0, "...olculebilir bir sure kosmadi")` | sifirdan buyuk |
| 4 | `UpdaterTests.cs:316` | `Assert.True(stopwatch.Elapsed < ceiling, ...)` | tavan |
| 5 | `UpdaterTests.cs:1173` | `Assert.True(stopwatch.Elapsed > TimeSpan.FromSeconds(1), "hic beklenmedi")` | taban |

Bes iddia ayni sey degil. Uctan sonuncusu (`> 0`) neredeyse hicbir sey olcmuyor;
birincisi bir buyukluk sirasi bant. **Hepsini ayni sekilde duzeltme.**

## Bu sozlesmenin sordugu soru

Her iddia icin tek soru: **bu olcu neyi tutmak istiyor, ve duvar saati o seyi
tutmanin tek yolu mu?**

Uc cevap mumkun ve ucu de kabul edilir:

- **(a) Saat enjekte edilebilir.** T127'nin yolu. Olcunun tuttugu sey bir *karar*
  (butce bagladi mi, zaman asimi tetiklendi mi) ise saat disaridan verilir ve
  iddia kesin sayiya sikilir. Tercih edilen cevap budur.
- **(b) Duvar saati kacinilmaz.** Olcu gercek bir dis surecin (ffmpeg, ag, dosya
  kilidi) gercek suresini olcuyorsa saat enjekte edilemez. O zaman **neden
  kacinilmaz oldugunu yaz** ve bandi **olculmus** veriyle daralt — tahminle degil.
- **(c) Olcu zaten bir sey olcmuyor.** `> 0` gibi bir iddia mutasyonla kirilmiyorsa
  ya davranissal bir iddiaya cevrilir ya da **"bu satir olcmuyor" diye yazilir.**
  Sessizce birakilmaz.

## K0 — altinci iddia bugun DUSTU, oncelik onda

Bu sozlesme "kararsiz ama bugun yesil" bes iddia icin yazildi. 2 Eylul 2026'da
altincisi bulundu ve o **kararsiz degil, dusmus durumda**: `main` kirmizi.

- Kosum `33627045645`, commit `f33c13e`.
- Dusen: `ComplexityProbeTests.DetailedProbeExposesWindowQualityThroughCoreContract`
- Hata: `1 pencere olculdu` — `ComplexityProbeTests.cs:61`,
  `Assert.True(meter.Calls >= 2, ...)`
- Ayni test **yerelde tek basina yesil** (olculdu: `--filter` ile tek test, 2 sn,
  Basarili 1). Yani kusur testin kendisinde degil, **yuk altindaki davranisinda.**

Bu T128'in getirdigi bir gerileme **degil**: T128 birlesmesi
(`cacc422`) yalnizca `PlanCalculator.cs` ve uc test dosyasina dokundu,
`ComplexityProbe` yolu disinda kaldi. Ayni kod `29fa27e`'de yesildi; arada
bu testin dosyasi hic degismedi.

**Iki aday sebep var ve hangisi oldugunu OLCECEKSIN. Tahminle kapatma.**

- **(i) Sayac yarisi.** `ComplexityProbeTests.cs:22` — `public int Calls { get; private set; }`
  ve `:26` `Calls++`. Pencereler `ComplexityProbe.cs:84-87`'de `Task.WhenAll` ile
  **es zamanli** kosuyor; `Calls++` atomik degil. Iki artis carpisirsa biri kaybolur
  ve sayac 2 yerine 1 okunur. Ayni dosyadaki `CancellingMeter` bunu dogru yapiyor
  (`Interlocked.Increment`, `Volatile.Read`) — `FakeMeter` yapmiyor.
- **(ii) Pencere sayisi ortama bagli.** Klip `testsrc2 8 sn 12 fps 320x240`;
  pencere sayisi `PlanWindows` icinde `SecondProfileAsync`in urettigi saniye-bit
  profilinin `Heterogeneity` degerinden cikiyor. O profil **kodlayicinin urettigi
  bayta** bagli, yani CI'daki ffmpeg surumunde yerelden farkli olabilir. O zaman
  gercekten 1 pencere planlanmistir ve iddia yanlis degil, **klip yetersizdir.**

Ikisi ayirt edilebilir: `result.QualityMeasurements.Count` ve planlanan pencere
sayisi loglanirsa (i) ise plan >= 2 ama sayac 1, (ii) ise plan da 1'dir.

### Kabul kriteri

- **K0.1 — Sebep olculur.** (i) mi (ii) mi, kaniti ile. Iki adayi da elemek
  serbest, ucuncu bir sebep bulursan onu yaz. **"Muhtemelen" yasak.**
  CHECK: hangi aday, hangi olcum, hangi cikti satiri.
- **K0.2 — Duzeltilir ve mutasyonla dogrulanir.** Sebep (i) ise sayac atomik
  yapilir; (ii) ise iddia ya klipten turetilen gercek pencere sayisina baglanir
  ya da klip pencere sayisini garanti edecek sekilde uzatilir. **Band genisletme
  ve `>= 1`e dusurme yasak** — K4 burada da gecerli.
  CHECK: mutasyon izgarasi.
- **K0.3 — Yuk altinda kosar.** Duzeltmeden sonra testi **tam suit icinde**
  en az 5 kez kostur; besinde de yesil olsun. Tek basina kosum kanit degil —
  bu kusur zaten tek basina yesildi.
  CHECK: bes kosumun ozeti.
- **K0.4 — CI yesile doner.** Dal itildikten sonra `gh run list` ile bak.
  `main` T0 tarafindan birlestirilecek; senden istenen dalinin yesil olmasi.
  CHECK: kosum kimligi ve durumu.

**Bu kriter otekilerden once yapilir.** `main` kirmizi kaldigi surece hicbir
sozlesme muhurlenmiyor.

## Kabul kriteri

**K1 — Bes iddianin her biri siniflanir.** `docs/olcumler/duvar-saati-iddialari.md`
icinde bes satirlik tablo: yer, iddia, ne tutmak istiyor, cevap (a/b/c), gerekce.
Tahmin yazma; olcemedigine "muhtemelen" deme, **"olculmedi"** de.
CHECK: bes satirin besi de doldurulmus, bos hucre yok.

**K2 — Gercek dagilim olculur, band ondan turetilir.** (b) diyecegin her iddia icin
o olcuyu **en az 20 kez** kostur ve gecen surenin dagilimini yaz: en dusuk, en
yuksek, ortanca, p95. Band bu dagilimdan cikar. Bugunku 1500-15_000 bandinin
gercek dagilimin neresine dustugunu de yaz — belki taban hic yaklasilmiyor,
belki tavan zaten dar.
CHECK: kosum sayisi, bes sayilik dagilim, eski band / yeni band karsilastirmasi.
**Bos makinede ve yapay yuk altinda ayri ayri olc.** Bu deponun olculmus kusuru:
paralel kosum olcuyu kendisi kararsiz yapti.

**K3 — Mutasyon.** Her degistirdigin olcu icin: tuttugu davranisi boz, olcunun
kirmiziya dondugunu goster. Kirilmiyorsa olcu davranis olcmuyor demektir — ya
olcuyu degistir ya (c) yaz. **`dotnet build -c Release --no-incremental` zorunlu**;
artimli derleme mutasyonu tasimiyor ve bu depoda tam bu yuzden bir kez yanlis
yesil okundu. `--no-build` yasak.
CHECK: mutasyon x olcu izgarasi, hangisi oldu hangisi yasadi.

**K4 — Genisletme yasak.** Bandi buyuterek, zaman asimini artirarak ya da
`[Skip]` koyarak hicbir iddia kapatilmaz. Daraltma serbest, genisletme
**yalniz K2'nin olculmus dagilimi gerektiriyorsa** ve o zaman sayiyla gerekcelenir.
CHECK: her band degisikligi icin yon (daraldi/genisledi) ve gerekce.

**K5 — Sinifi tara, sadece bes satiri degil.** Iki dosyada duvar saatine bagli
baska iddia var mi? `Thread.Sleep`, `Task.Delay`, `Stopwatch`, `DateTime.Now`
gecen her yer. Varsa **listele**; duzeltmek zorunda degilsin, gizlemek yasak.
CHECK: tarama komutu ve bulunan satirlarin listesi.

**K6 — Yesil kalir.** `verify` adimi tamamen yesil olmadan teslim yok. Ve
**yerelde yesil, CI'da kirmizi** bu deponun on uc itme boyunca fark edilmemis
kusuru: ittikten sonra `gh run list` ile bir kez bak.
CHECK: yerel kosum ozeti + CI kosum kimligi ve durumu.

## Sinirlar

- `owns` disina yazma. Uretim kodu **degismiyor** — bu bir olcum hijyeni
  sozlesmesi. Uretimde bir kusur bulursan **duzeltme, yaz**; ayri sozlesme olur.
- Gecici her sey `.calisma/` altina. **`.calisma/kaynak/` paylasilan 3,5 GB
  kaynak havuzu — dokunma, silme.**
- Kod yorumu yazma.
- CI kosumunu bekleyip **bosta durma.** Bir kez bak, surerse yaz ve devam et.
- Tam suiti gereksiz tekrar kosturma; `--filter` kullan.

## Kayit noktasi

`git worktree add .claude/worktrees/T132 -b T132-duvar-saati origin/main`
Depo kokunde dal acma, `main`in calisma agacina yazma. Her kriter kendi commit'i.
Dalini `origin`e it; **`main`e sen birlestirmezsin.**

## Rapor

**Beş iddianın cevabı, baştan:** 1 → (b), 2 → (b), 3 → **(c)**, 4 → (b), 5 → (b).
Hiçbiri (a) değil; beşinde de saat üretim kodunun içinde ve bu sözleşme üretimi
değiştirmiyor.

**K0.1 — sebep (i), sayaç yarışı.** `FakeMeter.Calls++` atomik değil ve iki pencere
`ComplexityProbe.RunDetailedAsync` içinde `Task.WhenAll` ile eşzamanlı ölçülüyor.
(ii) elendi: `ProductionPlan = SamplingPlan.Fixed` olduğu için `secondBits` hiç
hesaplanmıyor, 8 sn klip her koşumda 2 pencere; 45 koşumda `plan=2`, 45/45.
(iii) gözlenmedi: aynı 45 koşumda `QualityMeasurements.Count=2`. Yarışın kendisi
ayrı bir düzenekle ölçüldü (`.calisma/t132/yaris/`): 200.000 denemede 156 kayıp
artış (%0,078) 16 çekirdekte, iki çekirdeğe sabitlendiğinde 0.

**K0.2** — sayaç `Interlocked.Increment` / `Volatile.Read` oldu (aynı dosyadaki
`CancellingMeter` zaten böyleydi), iddia `>= 2` → `== 2` daraltıldı. İki mutasyon
da kırdı.

**K0.3** — tam süit beş kez koşuldu, beşi de `Başarısız 0 / Başarılı 1328 /
Atlanan 17 / Toplam 1345`. Süreler 11 m 26 s, 11 m 28 s, 14 m 13 s, 12 m 5 s,
19 m 51 s.

**K0.4 / K6** — dal koşumu `33637321742`, sonuç `success`. Yerel `verify`:
Başarısız 0, Başarılı 101, Atlanan 3, Toplam 104.

**K2** — üç bant ölçülen dağılımdan daraltıldı (her biri 20 boş + 20 yüklü koşum,
boş koşumlardan önce `Get-Process ffmpeg` ile makine boş doğrulandı):
`PerformanceCheckTests.cs:757` tavanı 15.000 → 5.000, `UpdaterTests.cs:302`
payı 500 → 250 ms, `UpdaterTests.cs:1173` tabanı 1 sn → 5 sn. Üçü de daralma;
genişleyen bant yok, `[Skip]` yok.

**K3** — dört mutasyondan üçü ilk denemede kırdı. `RemoveAttempts` 6 → 5
mutasyonu eski 1 sn tabanında **yaşadı** (4147 ms), yeni 5 sn tabanı öldürüyor;
iddia 5 bu yüzden fiilen (c) iken (b) oldu. `ManifestTimeout` mutasyonu ilk
denemede kendini gizledi — sabit testin kendi tavanını da büyütüyor; kullanım
yerini (`HttpClient.Timeout`) mutasyona uğratınca kırıldı.

**Değiştirilmeyen iki satır, gerekçesiyle:** iddia 2'nin oranı olduğu gibi kaldı
(üstündeki iki `Assert.False` asıl kanıt, bu satırı bağımsız kıran bir mutasyon
bulunamadı); iddia 3 (c) olarak yazıldı, satır kaldırılmadı.

**Üretimde bulunan, düzeltilmeyen kusur:** `ComplexityProbe.SampleWindowAsync`
kalite ölçerini yalnız `SplitSampleAsync` hızlı yolunda çağırıyor; o yol null
dönerse yedek yol pencereyi sessizce ölçmeden bırakıyor, rapora da hiçbir şey
düşmüyor. Ayrı sözleşme konusu.

**Not:** son commit (`270fae3`) yalnız belgeye dokunuyor; bu commit için henüz
CI koşumu görünmedi, kodun bulunduğu `7952308` koşumu yeşil.

**Aksama:** worktree eski bir tabanda ve önceki turun commit'lenmemiş işiyle
duruyordu; yedeklenip `origin/main`e ileri sarıldı, bütün ölçümler yeni tabanda
yeniden türetildi. `live/_sorun.log`a yazıldı.

## Cikti

Dal `T132-duvar-saati`, `origin`e itilmiş, dört commit:
`e955ac8` (K0), `623cadb` (K2-K4), `7952308` (K1-K5), `270fae3` (K0.3 özeti).

Belge: `docs/olcumler/duvar-saati-iddialari.md`.
Değişen ölçüler: `ComplexityProbeTests.cs:63`, `PerformanceCheckTests.cs:757`,
`UpdaterTests.cs:302`, `UpdaterTests.cs:1173`.
Üretim kodu değişmedi.

## Tur 2 — denetim KALDI, bir KRITIK (2 Eylul 2026)

Denetci izole kopyada (`git archive origin/T132-duvar-saati`) olctu, depo agacina
yazmadi, **yerel test kosturmadi** (0 kosum) — gerekcesi yazili ve dogru: CI kosumu
`33637321742` filtresiz tam suiti kosuyor, yukte tekrar kosturmak T137'yi de olcuyu
de bozardi. K2'nin on iki dagilim satirinin **hepsini** ham CSV'den yeniden hesapladi;
birebir tuttu. Manset kaymasi yok. Isin govdesi saglam.

### KRITIK — K5 delindi: uc bant listelenmedi, ustune yokluk beyani yazildi

`docs/olcumler/duvar-saati-iddialari.md:147`:

> "Besin disinda **assert edilen** yeni bir duvar saati bandi bulunmadi."

Yanlis. Ayni iki dosyada listelenmemis uc assert var:

| Yer | Ne |
|---|---|
| `tests/VidShrink.Tests/UpdaterTests.cs:915` | `Assert.True(offlineFirst < TimeSpan.FromSeconds(3), ...)` |
| `tests/VidShrink.Tests/UpdaterTests.cs:916` | `Assert.True(offlineSecond < TimeSpan.FromSeconds(3), ...)` |
| `tests/VidShrink.Tests/UpdaterTests.cs:1141` | `Assert.True(release.Join(TimeSpan.FromSeconds(10)), ...)` |

`:915/:916` gercek bir surec acilisina (`MeasureLaunch`, `Process.Start` +
`WaitForExit`) konmus **3 saniyelik sabit tavan**; testin adi
`EveryLaunchChecksAndStaysWithinTheTimeout`. Sozlesmenin aradigi "yuk altinda
dusecek yuzey" tarifinin tam ornegi ve iddia 4'ten (1050 ms) daha kirilgan.

Sebep belgede duruyor: K5'in yazili tarama komutu (`:130`) sunu ariyor —
`Thread.Sleep|Task.Delay|Stopwatch|DateTime.Now|DateTimeOffset.Now|ElapsedMilliseconds|\.Elapsed`.
`TimeSpan.From`, `DateTime.UtcNow` ve `Join(` desende **yok**. Bu yuzden `:915`,
`:916`, `:1141` ve ayrica `:796` (`DateTime.UtcNow + TimeSpan.FromSeconds(20)`),
`:1126` (`+30 s`) hic gorunmedi.

**Kusur eksik bulmak degil, eksik bulup "yok" demek.** K5'in lafzi: "Varsa listele;
duzeltmek zorunda degilsin, **gizlemek yasak**." Kod degisikligi gerekmiyor.

## Tur 2 kabul kriteri

**T1 — KRITIK kapanir.** Uc bandi (`:915`, `:916`, `:1141`) belgeye ekle ve `:147`
cumlesini **geri cek**. "Kaldirdim" deyip satirda duzenlenmis halde birakma.
Tarama komutunu da duzelt: `TimeSpan.From`, `DateTime.UtcNow`, `Join(` desene girsin,
**duzeltilmis komutu ve ham ciktisini** belgeye yaz. Denetcinin sayimi sekiz bant
(`UpdaterTests.cs:316,915,916,1141,1173` + `PerformanceCheckTests.cs:618,757,854`);
kendi taramanla bunu dogrula, **ondan fazlasi cikarsa hepsini yaz.**
CHECK: yeni tarama komutu + ham cikti + geri cekilen cumlenin eski/yeni metni.

**T2 — Daralan iki tavan mutasyonla pimlensin.** Denetcinin yapisal gozlemi: dort
mutasyondan yalniz biri (iddia 5) eski bandi yasatip yeni bandi olduruyor. Iddia 1'in
mutasyonu 15 ms uretip **tabani** kiriyor, iddia 4'unki 3006 ms uretip hem eskiyi hem
yeniyi asiyor. Yani 15.000->5.000 ve 500->250 daralmalarinin **daralma olmasi**
hicbir olcuyle tutulmuyor. Her iki tavan icin **eski bantta yasayan, yeni bantta olen**
birer mutasyon uret. Uretemiyorsan bunu yaz ve nedenini yaz — bu depoda iki sabiti
karsilastiran test davranis olcmedigi icin mutasyonu gecirdi.
CHECK: iki mutasyon x (eski bant sonucu / yeni bant sonucu) tablosu.

**T3 — K0'in (iii) belirsizligi yazili olsun.** 45 yerel kosum CI'daki tek dususe
karsi (iii)'u eleyemez; denetci `SampleWindowAsync` yedek yolunun yavas kosucuda
`SampleTimeout` ile tetiklenip tam olarak `Calls=1` uretebilecegini gosterdi. Belge
bugun "gozlenmedi" diyor — dogru kelime, ama **artik bilinen bir mekanizma var.**
Onu yaz ve **T141**'e baglantile. Sebep atamasini "kesin" diye sunma.
CHECK: eklenen paragraf + T141 atfi.

**T4 — Eskiyen satir numarasi.** Belge `ComplexityProbeTests.cs:61` diyor; sinif basina
iki satir eklendigi icin degisen iddia artik **`:63`**. Sozlesmenin `## Cikti` bolumu de
ayni eski numarayi tasiyor. Ikisini de duzelt.
CHECK: eski/yeni numara.

**T5 — Adi yalan soyleyen kayit.** `.calisma/t132/verify-final.log` (12:03, taban
sifirlamasindan **once**) `Toplam 73, Basarisiz 1` ile bitiyor; dusen
`PerformanceCheckTests.DonanimYoluKapatilincaKararDegisiyor`, duvar saati degil.
Raporda iddia edilen nihai yerel verify (`Toplam 104, Basarisiz 0`) icin saklanmis
kayit **yok**. Ya kaydi uret ya adi duzelt ve belgede "nihai kanit CI + bes tam suit
kosumu" diye acikca yaz.
CHECK: yeni dosya adi ya da uretilen kayit + belgedeki cumle.

**T6 — Olu ham veri agactan cikar.** `.calisma/t132/claim1.csv` ve `claim2.csv`
sifirlama oncesi turdan kalma ve belgedeki tabloyla **uyusmuyor** (claim1 bos dilimi
1500-1516, belgede 1500-1500; claim2 genis butce 10848-27666, belgede 6714-7236).
Yeni okuyan yanlis dosyaya bakar. `.calisma/t132/onceki-tur/` altina tasi; silme.
CHECK: yeni yol + tasindigini gosteren listeleme.

**T7 — Yaris olcumunun ifadesi daraltilsin.** Duzenek (`yaris/Program.cs`) iki gorevi
`Spin(jitter)` / `Spin(40-jitter)` ile **bilerek** ayni ana denk getiriyor. Yarisin var
oldugunu kanitlar; ama belgedeki "ayni eszamanlilik deseni" fazla iddiali — gercek
testte iki artis arasinda ayri ayri ffmpeg kodlamalari var, carpisma olasiligi
%0,078'in cok altinda. Ifadeyi duzelt ve ham ciktiyi sakla ya da "saklanmadi" de.
CHECK: eski/yeni cumle + kayit yolu veya yokluk beyani.

**T8 — K1 tablosunun eksik sutunu.** Sozlesme bes sutun istiyordu; tabloda "iddia"
sutunu yok. Ekle.
CHECK: yeni tablo basligi.

### Tur 2 sinirlari

- **Uretim kodu degismez.** Bu tur belge, olcu ve kayit turudur. Denetcinin dogruladigi
  uretim kusuru (`ComplexityProbe.cs:734-747`, yedek yolda `Quality = null`) **T141'e
  acildi**; ona dokunma.
- `owns` disina yazma.
- Hicbir bandi genisletme, hicbir zaman asimini buyutme. T1'de eklenen uc bandi
  **duzeltmen istenmiyor** — listelemen isteniyor.
- `--no-build` yasak; mutasyonda `--no-incremental` zorunlu.
- Makinede baska olcum kosuyor olabilir; agir ffmpeg isi baslatma.
- CI kosumunu bekleyip **bosta durma**; bir kez bak, surerse yaz ve devam et.

### Tur 2 borcu (kapatilmasi istenmiyor, muhur notuna girecek)

- Iddia 4'un yeni tavani (1050 ms) en riskli daralma: `ManifestTimeout=800`
  (`src/VidShrink.Core/UpdateCheck.cs:177`) ustune 250 ms pay. Dagilim yalniz bu
  makinede olculdu, CI'dan tek yesil ornek var. Izlenecek, bu turda dokunulmayacak.

## Tur 2 raporu

Sekiz kriterden **yedisi kapandi, biri olculdu ama pimlenemedi** (T2'nin iddia 1 yarisi).
Uretim kodu degismedi; degisen tek dosya `docs/olcumler/duvar-saati-iddialari.md`.

**T1 (KRITIK) kapandi.** Tarama deseni `TimeSpan.From`, `DateTime.UtcNow`,
`DateTimeOffset.UtcNow`, `.Join(`, `.WaitOne(`, `Timeout` ile genisletildi; komut,
desen dosyasi ve **63 satirlik ham cikti** belgeye gomuldu (`.calisma/` git'e
girmiyor, denetci gormezdi). Bantlarin sayimi: `PerformanceCheckTests.cs:618,757,854`
+ `UpdaterTests.cs:316,915,916,1141,1173` = **sekiz**, denetcinin sayimiyla ayni,
fazlasi cikmadi. `:915/:916` gercek surec acilisina konmus 3 sn'lik sabit tavan
(`[LiveLauncherFact]`; `VIDSHRINK_LAUNCHER_EXE` kurulu degilse atlaniyor ve CI bunu
kurmuyor), `:1141` calisan bir `[Fact]` icinde 10 sn'lik `Join`. Ucu de listelendi,
duzeltilmedi. Yokluk beyani ustu cizili alintiyla geri cekildi, yerine sayim cumlesi
kondu; belgede duzenlenmis hali kalmadi.

Kapsam disi ama tarandi: `EncoderCapabilitiesTests.cs:312,341` (5 sn'lik `Wait`) ve
`ShareProviderTests.cs:261`. Olculmedi, siniflanmadi, yazildi.

**T2 yarim.** Iddia 4 pimlendi: `HttpClient { Timeout = 1150 ms }` (sabit 800'de
birakildi) eski tavanda (1300 ms) **1160 ms ile yasadi**, yeni tavanda (1050 ms)
**1159 ms ile oldu**. Iddia 1 pimlenemedi ve sebebi mutasyon degil: `:757`'den once
`if (!guvenilir) Atlandi(...)` var; bu turda katsayi uc kosumda 9,232 / 8,005 / **12,0
(mutasyonsuz denetim kosumu)** okundu, ucunde de test `:757`'ye hic ulasmadi. Yani
bandi gizleyen sey testin kendi kapisi. Bunun iki sonucu belgede yazili: (1) bant
yalnizca sayacin guvenilir okudugu kosumlarda degerlendiriliyor — duvar saati
bandinin dusmesini bekleyecegimiz yuklu kosucu, tam da bandin bakilmadigi kosum;
(2) K2'deki iddia 1 dagilimi kapinin onune konmus gecici olcumden geliyordu, bandin
degerlendirildiginin kanidi degil. Bos makinede `Thread.Sleep(6000)` mutasyonunun
eski tavani gecip yeni tavanda olecegi **beklenir**, kosulmadi, kanit diye yazilmadi.
Katsayinin neden yuksek okundugu izole edilmedi; yukle birlikte gozlendi (%51-79),
yukun sebep oldugu olculmedi.

**T3 kapandi.** (iii) "gozlenmedi" kaldi ama artik "elenmedi" diye yaziyor: yedek yol
`SampleTimeout` ile tetiklendiginde tam olarak `Calls=1` uretir, bu **T141**'e
baglandi. K0 basligi "Sebep: (i)" yerine "En olasi sebep (i); (iii) elenmis degil"
oldu, tablo satiri "sebep bu" yerine "mekanizma var" diyor.

**T4 kapandi.** `ComplexityProbeTests.cs:61` → `:63`, belgede uc yerde; bu bolumun
Cikti listesinde de duzeltildi.

**T5 kapandi.** Tur 2'nin `verify` kosumu uretildi ve saklandi
(`.calisma/t132/verify-tur2.log`, `Basarisiz 0, Basarili 101, Atlanan 3, Toplam 104,
2 m 59 s`); ozet satiri belgede. Adi yalan soyleyen dosya
`.calisma/t132/onceki-tur/sifirlama-oncesi-verify.log` oldu. Nihai kanitin bes tam
suit kosumu + CI oldugu belgede aciktan yaziyor.

**T6 kapandi.** `claim1.csv` ve `claim2.csv` silinmedi,
`.calisma/t132/onceki-tur/` altina tasindi; listeleme belgede.

**T7 kapandi.** "Ayni eszamanlilik deseni" ifadesi geri cekildi: duzenegin iki artisi
`Spin` ile **bilerek** hizaladigi, yani yarisin varligini kanitladigi ama sikligini
kanitlamadigi yaziyor; gercek testte arada ffmpeg kodlamalari oldugu icin carpisma
olasiligi %0,078'in cok altinda. Duzenek yeniden kosuldu, ham cikti belgede:
`deneme=200000 yuk=3 cekirdek=16 kayip=105 oran=%0,0525 sure=471ms`.

**T8 kapandi.** K1 tablosu bes sutuna cikti: yer, **iddia**, ne tutmak istiyor, cevap,
gerekce.

**CI.** Kod bu turda degismedi; `ci.yml` `paths-ignore` ile `docs/**` ve `**/*.md`
kosumu tetiklemiyor, o yuzden belge commit'leri icin yeni kosum yok. Kodun bulundugu
son commit `7952308`, kosum **`33637321742`**, sonuc `success`.

## Tur 2 ciktisi

Dal `T132-duvar-saati`, itilmis. Tur 2'nin uc commit'i:

- `0c371f7` — T4, T5, T8
- `eec17e7` — T1 (KRITIK), T3, T7
- `cdc7853` — T2

Degisen tek dosya: `docs/olcumler/duvar-saati-iddialari.md`.
Degisen olculer: `ComplexityProbeTests.cs:63`, `PerformanceCheckTests.cs:757`,
`UpdaterTests.cs:302`, `UpdaterTests.cs:1173` (tur 1'den, tur 2'de dokunulmadi).
Uretim kodu degismedi.


## Tur 3 acildi (2 Eylul 2026) — iki KRITIK

Tur 2 denetimi **KALDI** dondu. Denetci tam suiti tekrar kosturmadi, yalniz tek testlik
bir kontrol probu kosturdu; olctugu her sayiyi kendi kosumuyla dogruladi. Alti kriter
(T3-T8) **GECTI**; T1 ve T2 KRITIK ile kaldi.

### KRITIK 1 — T2'nin gerekcesi olculmedi, `Atlandi` yanlis okundu

Tur 2 raporu T2'yi su mekanizmayla kapatiyor: "`:757`'den once `if (!guvenilir)
Atlandi(...)` var ... test `Atlandi` dalina giriyor ve `:757` hic calismiyor."

**Bu yanlis.** `Atlandi` bir `private void` gunlukcu (`PerformanceCheckTests.cs:48-52`);
`return` yok, `throw` yok, `Skip` yok. Cagri yerinde de `return` yok — `:753-757` duz akis.

Denetci izole kopyada `:757`'yi `InRange(..., 999_000, 999_999)` yapip kosturdu:

```
[atlandi] bu makinenin islemci zamani sayaci ... guvenilir okumadi (duzeltme=32x) ...
Assert.InRange() Failure: Value not in range
Range:  (999000 - 999999)
Actual: 1500
   at ...PerformanceCheckTests.IslemciZamaniSayaciDogruOkuyorMu() ... line 757
```

`[atlandi]` **basildi** ve `:757` **yine de kostu.** Kapi diye anlatilan sey yok.

Bu, T2'nin "uretemiyorsan nedenini yaz" muafiyetini gecersiz kiliyor: pim uretilebilirdi.
`CalibrateCpuClock(1500)` yakiciyi `durationMs` kadar **duvar saati** donduruyor
(`src/VidShrink.Ffmpeg/PerformanceProbe.cs:215-228`), `saat` de o cagriyi sariyor:

- mutasyon (a) `durationMs * 5` -> `saat ~ 7500 ms`
- mutasyon (b) `Join()` sonrasi `Thread.Sleep(6000)` -> `saat ~ 7500 ms`

7500, eski bant `[1500, 15_000]` **icinde**, yeni bant `[1500, 5_000]` **disinda** —
tam istenen mutasyon. Yapicinin kendi kayitlari bunu yariya kadar zaten gosteriyor;
ikisi de **eski bantta** yesil: `.calisma/t132/t2/eski-bant.txt:19` (mutasyon a),
`.calisma/t132/t2/iddia1-eski-bant.txt:20` (mutasyon b). **Yeni bant kolu hic
kosulmamis** — iki dosya da `eski-bant` adini tasiyor.

Ayrica belgenin `:227-235`'te bundan turettigi iki sonuc da dusuyor. Ozellikle (1):
"`:757`'nin bandi yalniz ... guvenilir okudugu kosumlarda degerlendiriliyor" — hayir,
Windows'ta **her** kosumda degerlendiriliyor.

### KRITIK 2 — T1: dokuzuncu bant var, ustune yine mutlak sayim cumlesi yazildi

Duzeltilmis desen `TimeSpan.From`, `.Join(`, `.WaitOne(`, `Timeout` aldi ama
`WaitForExit(` almadi. Ayni dosyada listelenmemis bir bant daha var:

```
tests/VidShrink.Tests/UpdaterTests.cs:890
    Assert.True(process.WaitForExit(60_000), "gecis sureci cikmadi");
```

Gercek bir surec acilisina (`Process.Start`, `:889`) konmus **60 saniyelik sabit tavan**,
`TheIncomingBinaryRenamesItselfOntoTheTargetName`, `[LiveLauncherFact]`. Yapisi
`:1141`'in birebir aynisi, kategorisi `:915/:916` ile ayni — ikisi listeye alindiysa bu
da alinmaliydi. Belgede hicbir yerde gecmiyor.

Buna ragmen belge uc yerde **mutlak sayim** veriyor: `:10` ("toplami bes degil sekiz"),
`:374` ("fazlasi cikmadi"), `:421-423` ("**sayisi sekiz**"). Tur 1'in kusuru — eksik
tarama + ustune yokluk beyani — bicim degistirip tekrarliyor: bu sefer "yok" degil "sekiz".

## Tur 3 kabul kriteri

**U1 — T2'nin eksik kolu kosulur.** Iki mutasyonun her biri icin **yeni bantta olen**
kolu kostur; ham cikti raporda. `Atlandi`nin kapi olmadigi belgede duzeltilir ve
`:227-235`'ten turetilen iki sonuc **geri cekilir** — geri cekmek demek satiri
duzenleyip birakmak degil, dosyada duzeltilmis halde kalmamasi demektir.
CHECK: dort log (iki mutasyon x eski/yeni bant) + duzeltilmis paragraf.

**U2 — Dokuzuncu bant listeye girer, sayim cumlesi gercege doner.** `:890` bant listesine
eklenir. Desen `WaitForExit(` da alacak sekilde genisletilir ve **yeniden kosturulur** —
genisletilmis desen baska bant buluyorsa hepsi yazilir. `:10`, `:374`, `:421-423`'teki
mutlak sayim cumleleri **duzeltilir**; sayiyi degistirmek yetmez, sayimin nasil
uretildigini soyleyen cumle de olmali.

Uyari: **bu ucuncu kez.** Tur 1'de "yok", tur 2'de "sekiz" yazildi; ikisi de tarama
eksikti. Bu turda mutlak sayi yazmadan once tarama **iki farkli desenle** kosulur ve
ikisinin de ham ciktisi belgeye girer.
CHECK: yeni desen + iki tarama ham ciktisi + duzeltilmis uc cumle.

**U3 — `PerformanceCheckTests.cs:629` siniflanir.** `Assert.True(butce.WallMs > dar, ...)`
— `WallMs` uretim tarafinda duvar saati (`PerformanceCheck.cs:6,244`). Bandi sabit degil
turetilmis; denetci "emin degilim, sayima katilmali mi tartisilir" dedi ama "tarama
deseninin bu sinifi hala gormedigi kesin" dedi. **Karar ver ve gerekcesini yaz**: bant mi,
degil mi. Ikisi de mesru; sessiz birakmak degil.
CHECK: karar cumlesi + gerekce.

## Tur 3 borcu (kapatilmasi istenmiyor, kayda geciyor)

- **`Atlandi` cagri yerlerinde tek istisna `:753`.** Diger yedi cagrinin hepsi ya `return;`
  ile bitiyor (`:336`, `:437`, `:526`, `:537`), ya `else` dalinda (`:457`, `:622`), ya
  metodun son ifadesi (`:567`). Yalniz `:753`'te `return` yok — yazarin niyeti digerleriyle
  ayniydi, eksik `return` bir test kusuru. **Bu sozlesmede duzeltme**; ayri sozlesme.
- **`Atlandi` gercek SKIP degil.** xUnit 2 kosum sirasinda test atlayamiyor; `[atlandi]`
  dusen test sayimda **Basarili** gorunuyor. `verify-tur2.log`'daki 3 gercek `Atlanan`
  bunlar degil, `[LiveLauncherFact]`in `Skip` alanindan geliyor. "101 Basarili" icindeki
  kac olcunun iddiasini kurmadan gectigi bu kosumdan **sayilamiyor.** Gorunurluk borcu;
  T117'nin ayni borcu.
- **`T132.md:68`** hala `ComplexityProbeTests.cs:61` diyor. T4 yalniz `## Cikti`yi
  adlandirdigi icin kriteri delmiyor. Bu turda duzelt, tek satir.
- **Kapsam disi iki aday** (`EncoderCapabilitiesTests.cs:312,341`, `ShareProviderTests.cs:261`)
  gercekten `owns` disinda ve "olculmedi, siniflanmadi" beyani durust. Dokuzuncu bant
  onlar degil, `:890`.

## Tur 3 kim yapiyor

Yapici + **`advisor`** (protokol: ucuncu turda zorunlu). Ayni dal `T132-duvar-saati`
uzerinde devam; `main`e birlestirme.


## Danisman goruşu (2 Eylul 2026) — U2 ve U3 degisti

Protokol geregi ucuncu turda danisman acildi. Uc soruya uc cevap; T0 her dayanagini
kendi kosumuyla dogruladi.

**S1 — Iki KRITIK dogru siniflanmis.** K-1 icin kontrol akisi belirleyici: `:753`'un
govdesi tek ifade, `return` yok, `:757` Windows'ta her kosumda degerlendiriliyor.
Yapici bir neden yazdi ve neden **yanlis**; sonuc bir eksik degil, belgeye girmis
yanlis olgu. K2 dagilimi ve K4 daralma gerekcesi hakkinda da yanlis sonuc doguruyor.

**S2 — Cozum yanlis kurulmus. Desen genisletmek dorduncu tekrari onlemez.**

Danisman iki bagimsiz yontemle kendi taramasini yapti (semantik kategori; uretimdeki
saat turevi uyeler + bekleme donus degerleri). Ikisi de ayni kumeyi verdi:
**dokuz bant**, sekiz degil. Dokuzuncu `UpdaterTests.cs:890`.

Asil bulgu: **hicbir metin deseninin bulamayacagi iki iddia daha var.**

```
PerformanceCheckTests.cs:462   Assert.True(yuklu.SoftwareRealtimeCores >= taban * YonPayi, ...)
PerformanceCheckTests.cs:542   Assert.True(yuklu.SoftwareRealtimeCores > bos.SoftwareRealtimeCores, ...)
```

`RealtimeCores => VideoMs <= 0 ? 0 : WallMs / VideoMs` — `src/VidShrink.Core/PerformanceCheck.cs:37`.
Yani `:462` duvar saatinden **turetilmis** bir buyukluge konmus sabit katsayili
(`YonPayi = 0.8`, `:358`) bant; `:542` yuk altinda iki duvar saati okumasinin
karsilastirmasi. Satirlarda `Elapsed`, `Stopwatch`, `TimeSpan`, `Timeout`, `Wait`,
`Join` **gecmiyor** — desen ne kadar genisletilirse genisletilsin bulunamaz.
Ikisi de sozlesmenin aradigi "yuk altinda dusecek yuzey" tarifinin tam ornegi.

**S3 — `:629` bant degil.** `:627` bulgunun varligini sart kosuyor; bulgu ancak
`elapsedMs > budgetMs` iken uretiliyor (`PerformanceCheck.cs:237,243-244`); `:628`
`budgetMs == dar`i civiliyor. `:629` uretimin kendi koruyucusundan **totolojik olarak**
cikiyor. Makine yavasladikca `WallMs` buyur, iddia saglamlasir — kararsizlasma yonu yok,
tavan yok, sabit esik yok.

Ama "olcmuyor" da degil: `WallMs: 0` ya da `WallMs: budgetMs` mutasyonu `:629`u oldurur,
yani "gecen sure bulguya tasiniyor mu" sorusuna cevap veren bir **tasima iddiasi**.

## U2 ve U3 yeniden yazildi

**U2 (yerine gecer) — sayim metinden degil turden cikarilir.**

Anahtar kelime listesi kapali bir kume degil; o yuzden hicbir turda bitmiyor. Kapali
kume su:

1. Uretimde saatten tureyen uyeleri say — `WallMs`, `ElapsedMs`, `RealtimeCores`,
   `RealtimeFactor`, `ReportedCpuParallelism`.
2. BCL'de zaman asimi argumani alan bekleme cagrilarini say — `WaitForExit`, `WaitOne`,
   `Join`, `Wait`, `WaitAsync`.
3. Bu ikisinin herhangi birine **dokunan** her `Assert`i listele.

Bu kume kaynaktan turetilebilir, dolayisiyla **sonlanir**. Uc dosyanin tamami taranir;
cikan her satir ya bant olarak listelenir ya gerekcesiyle elenir. Sessiz birakilan
satir kalmaz.

`:462` ve `:542` bu taramaya girer ve **siniflanir**: bant mi, elenmis mi, gerekcesi ne.
`:629` da girer ve S3'teki gerekceyle **elenmis** diye yazilir — bir sonraki denetci de
onu bulacak, "gorulmedi" izlenimi kalmamali.

**U2b — sayim bir olcuyle pimlenir.** Kalici hale getirmenin yolu desen degil, testtir.
`UpdaterTests.cs` bu depoda zaten kaynak metni okuyup uzerine `Assert` kuran testler
tasiyor (`:1185-1196`, `Program.cs`i okuyup `IndexOf` ile siralama iddiasi kuruyor).
Ayni kalipla: bu uc test dosyasini okuyup saat turevi uyelere dokunan `Assert`leri
sayan ve **belgedeki sayiya esitleyen** bir `[Fact]` yaz. Onuncu bant eklendigi anda
CI kirmizi olur; bir sonraki turun raporuna kalmaz.

Bu olcunun kendisi de mutasyona tabidir: sayiyi bir artir, testin kirmiziya dondugunu
goster.

**U3 (yerine gecer) — `:629` elenmis olarak siniflanir.** Bant listesine girmez;
"saat turevi ama bant degil — uretimin kendi koruyucusundan cikiyor, yonu guvenli,
tasima iddiasi" kategorisiyle yazilir. Ayri bir karar uretmene gerek yok, gerekce
yukarida; **dogrulugunu kendi kosumunla teyit et** ve yaz.
