---
id: T172
title: Ses hicbir hedefte dusurulmez — AudioDropped kolu kalkiyor
role: builder
model: sonnet
depends: []
blocked-by: []
status: active
round: 1
verify:
  - dotnet test --filter "PlanCalculatorTests|SesTabaniTests|ManualOverrideTests"
  - dotnet test --filter "OluUyeTests|LanguageTests"
owns:
  - src/VidShrink.Core/PlanCalculator.cs
  - src/VidShrink.Core/CompressionStrategy.cs
  - src/VidShrink.App/MainWindow.axaml.cs
  - src/VidShrink.App/Locales/en/main.json
  - src/VidShrink.App/Locales/tr/main.json
  - tests/VidShrink.Tests/SesTabaniTests.cs
  - tests/VidShrink.Tests/OluUyeTests.cs
  - docs/olcumler/ses-tabani.md
side_effects: []
---

## Baglam

Kullanicinin dogrudan sozu: **"bu boyutta ses tutulamiyor ne demek, ses her zaman olmak
zorunda."**

Bugun motor kucuk hedeflerde sesi **tamamen atiyor**.
`src/VidShrink.Core/PlanCalculator.cs:1231-1235`:

```csharp
if (audioK < 16 && totalK < 96)
{
    notes.Add(AdviceCode.AudioDropped);
    return (0, null);
}
```

Kullanicinin gordugu cumle `Locales/tr/main.json:356`:
`"Bu boyutta ses tutulamıyor, çıkarıldı."`

Hemen altindaki iki satir zaten bir taban ve bir mono kolu tanimliyor
(`PlanCalculator.cs:1237-1244`):

```csharp
audioK = Math.Max(24, audioK);
if (audioK < baseK) notes.Add(AdviceCode.AudioReduced);
int? channels = null;
if (stereo && audioK <= 56) { channels = 1; notes.Add(AdviceCode.AudioMono); }
```

Yani 24 kbps mono taban **zaten var**; dusurme kolu ondan once devreye girip yolu kesiyor.

`AdviceCode.AudioDropped` (`CompressionStrategy.cs:17`) uretimde tek yerde uretiliyor
(`PlanCalculator.cs:1233`) ve tek yerde tuketiliyor (`MainWindow.axaml.cs:2724`). Bu
sozlesme kolu kaldirinca uye **hicbir yerde uretilmez**; o zaman uyeyi de kaldir, olu
birakma. Kaldirmak `OluUyeTests` pimini etkileyebilir — pimi olcunun kendi ciktisiyla
guncelle, sayilari uydurma.

`main.advice.audio-dropped` anahtari iki dilde de silinir; `LanguageTests` iki dilde
eksik anahtar kabul etmez, iki dosyayi birlikte guncelle.

**Bu bir motor karari degisikligidir.** Ses tabani video butcesinden dusulur; hedef
boyutun tutmaya devam ettigi **olculmeden** teslim etme.

## Kabul kriteri

**K1 — Once say: bugun hangi hedeflerde ses dusuyor?** `PickAudio`'nun girdileri
(`totalK`, `regime`, kaynak ses bit hizi, kanal sayisi) uzerinden **kolu tetikleyen
butun hedef araligini** cikar. Hangi hedef MB / sure birlesimlerinde `totalK < 96`
oluyor? Somut ornekler ver: kac dakikalik video kac MB hedefte sesini kaybediyor.
CHECK: tetikleyen araligin tablosu ve tabloyu ureten hesabin ham ciktisi.

**K2 — Ses her ciktida var.** Kol kaldirilir. K1'in tablosundaki **her** aralik icin
gercek bir kodlama kos (`.calisma/kaynak/` altindaki kaynaklarla, kisa kesitlerle
olabilir) ve `ffprobe` ile ciktinin **ses akisi tasidigini** dogrula. Sifir ses akisi
olan tek bir cikti kalirsa teslim etme.
CHECK: her kosum icin `ffprobe` ciktisinin ses akisi satiri, ham metin.

**K3 — Hedef hala tutuyor.** Ses tabani video butcesinden dusuluyor; K1 tablosundaki her
aralikta ciktinin boyutu **hedefi asmamali**. Asan bir aralik varsa **gizleme, yaz** —
ve o aralikta motorun ne yaptigini anlat (cozunurluk mu dustu, kare hizi mi, CRF mi).
CHECK: hedef MB / gercek MB / fark tablosu, `EncodeResult` ham degerleriyle.

**K4 — Kaynakta ses yoksa.** `info.HasAudio == false` kolu (`PlanCalculator.cs:1219`)
**degismez**: kaynakta ses akisi yoksa cikti da sessizdir ve bu bir kusur degildir.
Kullanicinin cumlesi "ses her zaman olmak zorunda" kaynakta olani korumakla ilgilidir,
olmayani uydurmakla degil. Bu ayrimin olcusunu yaz.
CHECK: sessiz kaynakla bir kosum, ciktinin da sessiz oldugu ve **hicbir uyari
cumlesinin** "ses cikarildi" demedigi.

**K5 — Olu uye ve olu metin kalmaz.** `AdviceCode.AudioDropped` uretilmiyorsa uyeyi,
`MainWindow.axaml.cs:2724`'teki kolunu ve iki dildeki `main.advice.audio-dropped`
anahtarini kaldir. `OluUyeTests` ve `LanguageTests` yesil olmali; pim satir sayisini
olcunun kendi ciktisindaki `uye: N ... pimlenen: N` satirindan al.
CHECK: iki olcunun ham ciktisi ve guncel pim sayimi.

**K6 — Mutasyon.** Iki mutasyon, her birinden once
`dotnet build -c Release --no-incremental`; **`--no-build` yasak:**
(a) 24 kbps tabanini 0'a indir — bir olcu olmeli;
(b) Dusurme kolunu geri koy — bir olcu olmeli.
CHECK: iki satirlik izgara, kirilan olcu adlari, ham cikti.

**K7 — Kol sayisi.** `verify` satirlarinin her birini `--list-tests` ile kos. Sifir
bulan kol varsa teslim etme, bildir. Bu depoda **dort** kez oldu.
CHECK: her filtre kolu icin test sayisi.

## Sinirlar

- `owns` disina yazma. `EncodeRunner.cs`, `ConversionArguments.cs`, `FfmpegArguments.cs`
  **okunacak, yazilmayacak.** Cozum onlari degistirmeyi gerektiriyorsa **yazma, bildir.**
- Elle gecersiz kilma (`LockedAudioKbps`, `AudioChannels`) kollari **degismez** — T165'in
  alani. Kullanici sesi elle "yok" derse cikti sessiz kalir; bu sozlesme otomatik
  dusurmeyi kaldiriyor, kullanicinin kendi istegini degil.
- Bandi genisletme, esigi gevsetme, iddiayi zayiflatma yoluyla yesil uretme.
- Kod yorumu yazma.
- **Filtresiz `dotnet test` kosturma** — tum suit bu makinede 29 dakika suruyor.
  Filtreli kosumlar **on planda** kosar.
- Gecici her sey `.calisma/T172/` altina. `.calisma/kaynak/` paylasilan — okuyabilirsin,
  **yazma ve silme.**
- "Her", "hepsi", "hicbiri" diyen her cumlenin altindaki listeyi **kendin say ve ham
  ciktiyi goster.** Bu depoda tablo dogru cikip onu ozetleyen cumle 26 kez yanlis cikti.
- `.claude/relay/**` T0'in alani; sozlesme dosyasina ve `LOG.md`e dokunma.

## Kayit noktasi

Dal: `T172-ses-tabani`, guncel `main`den ac, `origin`e it.
**`main`e birlestirme** — onu T0 yapar.

## Cikti

`docs/olcumler/ses-tabani.md` — K1'in tetikleme araligi tablosu, K2'nin `ffprobe`
satirlari, K3'un hedef/gercek tablosu, K4'un sessiz kaynak kosumu, K5'in pim sayimi,
K6'nin mutasyon izgarasi, K7'nin kol sayilari.
