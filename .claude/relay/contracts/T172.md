---
id: T172
title: Ses hicbir hedefte dusurulmez — AudioDropped kolu kalkiyor
role: builder
model: sonnet
depends: []
blocked-by: []
status: active
round: 4
verify:
  - dotnet test --filter "PlanCalculatorTests|SesTabaniTests|ManualOverrideTests"
  - dotnet test --filter "OluUyeTests|LanguageTests"
owns:
  - src/VidShrink.Core/PlanCalculator.cs
  - src/VidShrink.Core/CompressionStrategy.cs
  - src/VidShrink.App/MainWindow.axaml.cs
  - src/VidShrink.App/Locales/en/main.json
  - src/VidShrink.App/Locales/tr/main.json
  - tests/VidShrink.Tests/SesTabaniTests.cs
  - tests/VidShrink.Tests/OluUyeTests.cs
  - tests/VidShrink.Tests/ManualOverrideTests.cs
  - docs/olcumler/ses-tabani.md
side_effects: []
---

## Baglam

Kullanicinin dogrudan sozu: **"bu boyutta ses tutulamiyor ne demek, ses her zaman olmak
zorunda."**

Bugun motor kucuk hedeflerde sesi **tamamen atiyor**.
`src/VidShrink.Core/PlanCalculator.cs:1231-1235`:

```csharp
if (audioK < 16 && totalK < 96)
{
    notes.Add(AdviceCode.AudioDropped);
    return (0, null);
}
```

Kullanicinin gordugu cumle `Locales/tr/main.json:356`:
`"Bu boyutta ses tutulamıyor, çıkarıldı."`

Hemen altindaki iki satir zaten bir taban ve bir mono kolu tanimliyor
(`PlanCalculator.cs:1237-1244`):

```csharp
audioK = Math.Max(24, audioK);
if (audioK < baseK) notes.Add(AdviceCode.AudioReduced);
int? channels = null;
if (stereo && audioK <= 56) { channels = 1; notes.Add(AdviceCode.AudioMono); }
```

Yani 24 kbps mono taban **zaten var**; dusurme kolu ondan once devreye girip yolu kesiyor.

`AdviceCode.AudioDropped` (`CompressionStrategy.cs:17`) uretimde tek yerde uretiliyor
(`PlanCalculator.cs:1233`) ve tek yerde tuketiliyor (`MainWindow.axaml.cs:2724`). Bu
sozlesme kolu kaldirinca uye **hicbir yerde uretilmez**; o zaman uyeyi de kaldir, olu
birakma. Kaldirmak `OluUyeTests` pimini etkileyebilir — pimi olcunun kendi ciktisiyla
guncelle, sayilari uydurma.

`main.advice.audio-dropped` anahtari iki dilde de silinir; `LanguageTests` iki dilde
eksik anahtar kabul etmez, iki dosyayi birlikte guncelle.

**Bu bir motor karari degisikligidir.** Ses tabani video butcesinden dusulur; hedef
boyutun tutmaya devam ettigi **olculmeden** teslim etme.

## Kabul kriteri

**K1 — Once say: bugun hangi hedeflerde ses dusuyor?** `PickAudio`'nun girdileri
(`totalK`, `regime`, kaynak ses bit hizi, kanal sayisi) uzerinden **kolu tetikleyen
butun hedef araligini** cikar. Hangi hedef MB / sure birlesimlerinde `totalK < 96`
oluyor? Somut ornekler ver: kac dakikalik video kac MB hedefte sesini kaybediyor.
CHECK: tetikleyen araligin tablosu ve tabloyu ureten hesabin ham ciktisi.

**K2 — Ses her ciktida var.** Kol kaldirilir. K1'in tablosundaki **her** aralik icin
gercek bir kodlama kos (`.calisma/kaynak/` altindaki kaynaklarla, kisa kesitlerle
olabilir) ve `ffprobe` ile ciktinin **ses akisi tasidigini** dogrula. Sifir ses akisi
olan tek bir cikti kalirsa teslim etme.
CHECK: her kosum icin `ffprobe` ciktisinin ses akisi satiri, ham metin.

**K3 — Hedef hala tutuyor.** Ses tabani video butcesinden dusuluyor; K1 tablosundaki her
aralikta ciktinin boyutu **hedefi asmamali**. Asan bir aralik varsa **gizleme, yaz** —
ve o aralikta motorun ne yaptigini anlat (cozunurluk mu dustu, kare hizi mi, CRF mi).
CHECK: hedef MB / gercek MB / fark tablosu, `EncodeResult` ham degerleriyle.

**K4 — Kaynakta ses yoksa.** `info.HasAudio == false` kolu (`PlanCalculator.cs:1219`)
**degismez**: kaynakta ses akisi yoksa cikti da sessizdir ve bu bir kusur degildir.
Kullanicinin cumlesi "ses her zaman olmak zorunda" kaynakta olani korumakla ilgilidir,
olmayani uydurmakla degil. Bu ayrimin olcusunu yaz.
CHECK: sessiz kaynakla bir kosum, ciktinin da sessiz oldugu ve **hicbir uyari
cumlesinin** "ses cikarildi" demedigi.

**K5 — Olu uye ve olu metin kalmaz.** `AdviceCode.AudioDropped` uretilmiyorsa uyeyi,
`MainWindow.axaml.cs:2724`'teki kolunu ve iki dildeki `main.advice.audio-dropped`
anahtarini kaldir. `OluUyeTests` ve `LanguageTests` yesil olmali; pim satir sayisini
olcunun kendi ciktisindaki `uye: N ... pimlenen: N` satirindan al.
CHECK: iki olcunun ham ciktisi ve guncel pim sayimi.

**K6 — Mutasyon.** Iki mutasyon, her birinden once
`dotnet build -c Release --no-incremental`; **`--no-build` yasak:**
(a) 24 kbps tabanini 0'a indir — bir olcu olmeli;
(b) Dusurme kolunu geri koy — bir olcu olmeli.
CHECK: iki satirlik izgara, kirilan olcu adlari, ham cikti.

**K7 — Kol sayisi.** `verify` satirlarinin her birini `--list-tests` ile kos. Sifir
bulan kol varsa teslim etme, bildir. Bu depoda **dort** kez oldu.
CHECK: her filtre kolu icin test sayisi.

## Sinirlar

- `owns` disina yazma. `EncodeRunner.cs`, `ConversionArguments.cs`, `FfmpegArguments.cs`
  **okunacak, yazilmayacak.** Cozum onlari degistirmeyi gerektiriyorsa **yazma, bildir.**
- Elle gecersiz kilma (`LockedAudioKbps`, `AudioChannels`) kollari **degismez** — T165'in
  alani. Kullanici sesi elle "yok" derse cikti sessiz kalir; bu sozlesme otomatik
  dusurmeyi kaldiriyor, kullanicinin kendi istegini degil.
- Bandi genisletme, esigi gevsetme, iddiayi zayiflatma yoluyla yesil uretme.
- Kod yorumu yazma.
- **Filtresiz `dotnet test` kosturma** — tum suit bu makinede 29 dakika suruyor.
  Filtreli kosumlar **on planda** kosar.
- Gecici her sey `.calisma/T172/` altina. `.calisma/kaynak/` paylasilan — okuyabilirsin,
  **yazma ve silme.**
- "Her", "hepsi", "hicbiri" diyen her cumlenin altindaki listeyi **kendin say ve ham
  ciktiyi goster.** Bu depoda tablo dogru cikip onu ozetleyen cumle 26 kez yanlis cikti.
- `.claude/relay/**` T0'in alani; sozlesme dosyasina ve `LOG.md`e dokunma.

## Kayit noktasi

Dal: `T172-ses-tabani`, guncel `main`den ac, `origin`e it.
**`main`e birlestirme** — onu T0 yapar.

## Cikti

`docs/olcumler/ses-tabani.md` — K1'in tetikleme araligi tablosu, K2'nin `ffprobe`
satirlari, K3'un hedef/gercek tablosu, K4'un sessiz kaynak kosumu, K5'in pim sayimi,
K6'nin mutasyon izgarasi, K7'nin kol sayilari.

## Tur 1 - sonuc: KALDI (K3 kirmizi, birlestirilmedi)

Tur 1 kolu, uyeyi, tuketicisini ve iki dildeki anahtari dogru kaldirdi; K1, K2, K4, K5,
K6, K7 karsilandi ve olcum durustce kirmizi bildirdi. **K3 dustu**, dal `main`e
alinmadi. Kanit `docs/olcumler/ses-tabani.md`, dal `T172-ses-tabani`, commit `6202408`.

| kombinasyon | hedef MB | ESKI: gercek / basari / ses | YENI: gercek / basari / ses |
|---|---|---|---|
| 1dk/0,3MB  | 0,3 | 0,342 / basarisiz / dosya yok | 0,547 / basarisiz / dosya yok |
| 2dk/0,8MB  | 0,8 | 0,762 / **basarili** / ses YOK | 1,105 / **basarisiz** / dosya yok |
| 5dk/2MB    | 2   | 1,95  / **basarili** / ses YOK | 2,787 / **basarisiz** / dosya yok |
| 10dk/5MB   | 5   | 4,962 / **basarili** / ses YOK | 5,587 / **basarisiz** / dosya yok |
| 10dk/8MB   | 8   | 7,71  / basarili / ses VAR      | 7,71  / basarili / ses VAR |

Bes kombinasyonun **ucunde** eski kod hedefi tutarak sessiz teslim ediyordu, yeni kod
hic dosya teslim etmiyor. Kullanicinin sikayeti "sesim yok"tu; teslim edilen sey
"dosyam yok" olamaz. **Bu haliyle birlestirilmez.**

**Kok neden (tur 1'in kendi teshisi, dogru):** `PlanCalculator.cs:391`
`videoK = Math.Max(MinVideoBitrateK, totalK*ContainerOverhead - audioK - DeliveryReserveK(codec))`,
`MinVideoBitrateK = 48` (`:159`). Eski kodda `audioK = 0` oldugu icin ham deger
(53k/53k/67k) 48'in ustunde kaliyordu. Yeni kodda 24k ses payi dusulunce ham deger 48'in
**altina** iniyor ve videoK sabit 48'e cakiliyor - yani plan **butcesinden fazlasini**
istiyor. Floor'a carpan plan iki-pas kodlamada uc denemede de hedefi ~%45 asiyor.
`FrameRateCutForFloor` notu yalniz yeni kosumlarda var, eskide yok.

## Tur 2 - kapsam (T0)

**K8 - Floor'a carpan planda hedef tutsun, ses de kalsin.** Kusur "ses varligi" degil,
`MinVideoBitrateK` floor'unun butceyi asmasi. Cozum `owns` icinde, `PlanCalculator.cs`te:
butce `MinVideoBitrateK`in altina dustugunde videoK'yi sabit 48'e **cakma** - butcenin
gerektirdigi degeri kullan ve o bit hizinin tasinabilmesi icin cozunurluk/kare hizini
gercekten dusur. `EncodeRunner.cs`, `ConversionArguments.cs`, `FfmpegArguments.cs`
**okunacak, yazilmayacak**; cozum onlari degistirmeyi gerektiriyorsa **yazma, bildir.**
CHECK: tur 1'in bes kombinasyonunun **besi** de: (a) dosya teslim etmeli,
(b) `ffprobe` ile ses akisi tasimali, (c) hedefi **asmamali**. Ucunun de ham ciktisi
tabloda. Karsilanamayan kombinasyon varsa **gizleme, satir satir yaz** ve motorun o
aralikta ne yaptigini (cozunurluk mu, kare hizi mi, CRF mi) anlat.

**K9 - Kirmizi test kalmaz.** Tur 1'de `ManualOverrideTests` kirmizi kaldi (golden deger
T172 oncesi davranisi bekliyor). `ManualOverrideTests.cs` bu turda `owns`a alindi:
golden degerleri **yeni dogru davranisla** guncelle. Eski degeri korumak icin uretim
kodunu geri sarma; degeri gevsetip testi anlamsizlastirma da yok - degisen davranisin ne
oldugunu tek satirla olcunun icine yaz.
CHECK: `verify` kollarinin **hepsi** yesil, ham cikti.

**K10 - Tur 1'in olcusu bozulmasin.** K1-K7 yeniden kosulacak ve
`docs/olcumler/ses-tabani.md` guncellenecek; K3 tablosu artik ESKI / TUR 1 / TUR 2
olmak uzere **uc** sutun tasiyacak. K4 (sessiz kaynak) ve K6 (mutasyon) yeniden kosulur.
CHECK: guncel tablo ve her K icin ham cikti.

## Tur 2 - denetim bulgulari (HUKUM: KALDI, 9 bulgu, 4 kritik)

**Motor dogru, belge guvenilir degil.** Denetci K8'i, K9'u, K10'u, K4'u, K5'i, K6'yi ve
`owns` sinirini bagimsiz dogruladi ve hepsi tuttu; dusuren sey belgenin dort yerde kendi
ham verisiyle celismesi - biri duzeltilmemis bir kusuru "olculdu, temiz" diye kapatiyor.

Bagimsiz dogrulanan gecen taraflar: K8'in bes kombinasyonunun besi de `basarili=True`,
`codec_type=audio codec_name=aac channels=1`, hedefin altinda (bayt boyutlarindan yeniden
hesaplandi: 0,290 / 0,771 / 1,927 / 4,855 / 7,712 MiB). K9 diff'i **tek satir**, assert
govdesi gevsetilmemis. Temiz Release yapidan sonra iki kol: 122/122 ve 66/66, sifir bulan
kol yok. K5 pim sayimi olcunun kendi ciktisindan. K6 tur 2'de gercekten yeniden kosuldu
(tur 1 muadilleriyle yalniz zaman damgalarinda farkli). `EncodeRunner.cs`,
`ConversionArguments.cs`, `FfmpegArguments.cs` yazilmamis.

### KRITIK 1 - Envanter dort degisikligin ikisini sakliyor, kok neden yanlis atifli

`ses-tabani.md:169` tablosu `grep -n MinVideoBitrateK` ciktisi diye sunuluyor, `:183`
"6 kullanimin 2'si (391, 545) bu turda floor'suz" diyor. Gercek diff (`ba705cf`,
4 insertion / 4 deletion) **dort** yerde floor kaldirmis: **391, 545, 980, 988**.
980 ve 988 tabloda **hic yok**, gerekce sutunu bos. grep satiri main'de 10 (1 tanim +
9 kullanim), HEAD'de 7 (1 tanim + 6 kullanim).

Bu tasiyici bir hata: `PlanCalculator.cs:988` `Correct()` yeniden-deneme kolunda
`corrected.VideoBitrateK = Math.Max(0, ...)`. K8'in bes kosumunun **besi de 2. denemede**
basarili oldu ve o denemelerin videoK'lari **14 / 26 / 26 / 40** / 78 - dordu 48'in
altinda. Eski floor 988'de dursaydi 2. deneme 48'e cakilir, hedefi asar, dosya yine
teslim edilmezdi. Belge (`:218-220`) duzelmeyi tumuyle "391/545"e yaziyor.

### KRITIK 2 - Uc sutunlu K3 tablosunun ESKI hucresi ham ciktiyla celisiyor

`ses-tabani.md:231`, 1dk/0,3MB satiri, ESKI sutunu `0,342 / basarili / ses YOK` diyor.
`.calisma/T172/eski-k2k3.txt` ilk blok:
`[encode] basarili=False ciktiMB=0,342 denemeler=3 hata=Stayed over the 0,3 MB target
after 3 attempts ...; no file was written.` Dogrusu **basarisiz / dosya yok**.
`eski-ffprobe.txt` de yalniz dort dosya iceriyor, 1dk ciktisi yok. Ayni belge `:62`'de ve
sozlesme `T172.md:147`'de bu hucre **dogru** yazili - dosya kendi icinde iki celiskili
deger tasiyor ve turun manset tablosu yanlis olani tasiyor, ESKI tabani oldugundan iyi
gosteriyor.

### KRITIK 3 - "505/511/520/528 hic tetiklenmedi" iddiasi deponun kendi olcusuyle yalanlaniyor

`:175-178` ve `:183-184` bu dort satir icin "bu dal hic tetiklenmedi" diyor; bu, onlari
duzeltmemenin gerekcesi. Teslim edilen supurmenin kendi icinde
(`.calisma/T172/tur2-baseline-sweep-v2.tsv`, 171 veri satiri - sayi dogru):

```
30dk  10MB  videoK=48  tahmini=15,527  fark=+5,527   BudgetExceedsCeiling,FillTwoPassBandTooNarrowForCrf
60dk  15MB  videoK=48  tahmini=31,054  fark=+16,054
90dk  30MB  videoK=48  tahmini=46,581  fark=+16,581
120dk 40MB  videoK=48  tahmini=62,109  fark=+22,109
```

**11 satir videoK=48'e tam cakili, 9'u hedefi asiyor** (en fazla +%55). Not sutunundaki
`BudgetExceedsCeiling` + `FillTwoPassBandTooNarrowForCrf` tam olarak `:528`'in dali.
Aritmetik de dogruluyor: 10 MB / 1800 s ~ 45,5 kbit/s, eksi 24k ses ~ 21,5k video,
`Math.Max(48, 21,5) = 48`. Ajanin kendi mutasyon dosyasi `tur2-mut-511-520-528.tsv`
baseline'dan **22 satir** farkli. Dal hem tetikleniyor hem floor baglayici. Bu, tur 1'in
teshis ettigi kusurun **aynisi**, komsu dalda, duzeltilmeden duruyor.

**Agirlastirici, T0'in kendi hatasi:** `4fc0e82` temizligi tekrari silerken bu iddiayi
**duzelten** metni atti. Silinen K8-ek bolumu aynen sunu diyordu: *"Onceki ajanin
'tetiklenmiyor' iddiasi yanlisti (yol calisiyor)."* Yanlis surum kaldi.

### KRITIK 4 - K1 sayimi: 44 denmis, gercek 55

`:23` "grep -c True .calisma/T172/k1-ham.txt = 44 satir" diyor. Dosya 113 satir
(1 baslik + 112 veri; bu kisim dogru), ama `AudioDropped=True` olan satir **55**:
1dk=5, 2dk=11, 5dk=17, 10dk=22. Alttaki esik tablosu (`:16-21`) satir satir dogru
(1dk 0,6->0,7, 2dk 1,2->1,4, 5dk 3,0->3,5, 10dk 6,0->7,0, hepsinde totalK 81,9->95,6).
Yine ayni desen: tablo dogru, ozet cumle yanlis.

### Borc bulgulari

1. **Bos olculer kanit diye duruyor.** `.calisma/T172/` icinde `tur2-mut-505.tsv`,
   `tur2-mut-505-v2.tsv`, `tur2-mut-505-again.tsv`, `tur2-fix-511-520-528.tsv`,
   `tur2-k8ek-fixed-sweep.tsv` - **besi de baseline ile bayt bayt ayni**. 528 floor'u
   0,0'a inseydi yukaridaki 11 satir degismek zorundaydi. Ajanin bildirdigi "sed
   mutasyonlarim sessizce HEAD'e sifirlandi" notuyla birebir ortusuyor: bu kosumlar
   mutasyonsuz ikiliye karsi yapilmis, hicbir sey olcmuyor.
2. **Turun asil duzeltmesini hicbir test pimliyor.** `MinVideoBitrateK`i 391/545/980/988'e
   geri koyan biri sifir test dusurur. `ManualOverrideTests.cs:73` golden'i videoK=56
   bekliyor, 48'in ustunde. K6 izgarasi yalniz ses tabanini kapsiyor.
3. **20-120 dk araligi hic kodlanmadi.** Floor'un hala cakili oldugu satirlar yalniz plan
   seviyesinde. Yeniden-deneme kolu (`:988`) muhtemelen kurtariyor ama **olculmedi**.
4. **`ManualOverrideTests.cs:62-67` yorumu artik yalan** ("beklenen degerler uydurulmadi:
   9b092e9 agacinda ayni bes bilesim kosuldu"); dorduncu satir artik T172 motorundan
   geliyor. Test adi `K1_VarsayilanT165OncesiMotorlaBirebirAyni` de o satir icin yanlis.
5. **`:93-97`'deki "ham cikti" blogu aynen degil** - gercek `k4-ham.txt` `boyutMB=24,73`,
   `denemeler=1` ve `[deneme 1]` satirini da iceriyor, blok kirpilmis.

## Tur 3 - kapsam (T0)

**K11 - `:528` dalindaki floor'u gercekten kapat.** KRITIK 3 duzeltilmemis bir kusur:
uzun kaynak + gorece kucuk hedefte (30-120 dk) videoK 48'e cakiliyor ve plan hedefi
%55'e kadar asiyor. Bu, tur 1'de duzeltilen kusurun komsu daldaki aynisi.
`505/511/520/528` kollarini ayni yaklasimla duzelt (`391/545` icin ne yapildiysa) ve
**supurmeyi yeniden kos**: `tur2-baseline-sweep-v2.tsv`nin 171 satirinda videoK=48'e
cakili kac satir kaldi, hedefi asan kac satir kaldi - once/sonra iki tablo, ham TSV ile.
CHECK: once/sonra sayim, ham TSV, ve floor'a cakili kalan her satirin gerekcesi.

**K12 - 20-120 dk araligi gercek kodlamayla dogrulansin.** K8'in bes kombinasyonu
1-10 dk. KRITIK 3'un araligindan **en az uc** kombinasyon (ornegin 30dk/10MB, 60dk/15MB,
120dk/40MB) gercek `EncodeRunner` ile kosulacak; kisa kesit kullanmak serbest ama
**sure/hedef oranini koru**. Ucunde de: dosya var, `ffprobe`'da ses akisi var, hedefi
asmiyor.
CHECK: ucunun ham ciktisi ve `ffprobe` satirlari.

**K13 - Floor kaldirmayi pimleyen bir olcu.** `MinVideoBitrateK`i 391/545/980/988 (ve
K11'den sonra 505/511/520/528) yerlerine geri koyan bir mutasyon **en az bir olcuyu
dusurmeli**. Bugun sifir olcu dusuyor. Testi `SesTabaniTests.cs`e ekle; mutasyonu kos ve
kirilan olcunun adini ham ciktiyla goster.
CHECK: mutasyon izgarasi, kirilan olcu adi, ham cikti.

**K14 - Belgeyi kendi verisiyle uyumlu hale getir.**
- `:169-185` envanter tablosuna **980 ve 988**'i ekle, "6 kullanimin 2'si" -> gercek sayi;
  K11'den sonraki durumu yansit.
- `:218-220` kok neden anlatisina `Correct()` yeniden-deneme kolunu (`:988`) ekle: bes
  kosumun besi de **2. denemede** basarili oldu, videoK'lari 14/26/26/40/78.
- `:231` ESKI hucresi 1dk/0,3MB -> **basarisiz / dosya yok**.
- `:23` K1 sayimi **55** (1dk=5, 2dk=11, 5dk=17, 10dk=22).
- `:93-97` K4 blogunu ham dosyadan **aynen** al (kirpma).
- Borc 1'deki bes bos TSV'yi ya sil ya da "bu kosumlar mutasyonsuz ikiliye karsi yapildi,
  hicbir sey olcmuyor" diye acikca isaretle - kanit gibi durmasin.
- Borc 4'u (yorum ve test adi artik yaniltici) rapora borc olarak yaz.
CHECK: her duzeltilen sayinin yaninda uretildigi ham dosyanin yolu.

**Siniri hatirlat:** `EncodeRunner.cs`, `ConversionArguments.cs`, `FfmpegArguments.cs`
okunacak, yazilmayacak. Cozum onlari degistirmeyi gerektiriyorsa **yazma, bildir.**

## Tur 3 - denetim bulgulari (HUKUM: KALDI, 1 kritik, 8 borc)

K11, K12, K13, K14'un dordu de bagimsiz dogrulandi ve **tuttu**. Dusuren sey K11'in
kapsam disinda biraktigi sey: `videoK=0` planlarinin **calisamayan** bir ffmpeg komutu
uretmesi.

**Bagimsiz dogrulanan gecen taraflar.** `grep -n MinVideoBitrateK` HEAD'de 3 satir
(159 tanim, 625 LockedCrf, 868 QualityFloorTargetMb), main'de 10 - sekiz yer floor'suz.
Iki TSV gercekten farkli ikiliden (`cmp` DIFFERENT, `diff` 22 satir = 11 cift); tur 2'nin
ozdes-TSV tuzagina dusulmemis. Denetcinin kendi awk sayimi: 171 veri satiri, `videoK==48`
**11 -> 0**, `fark>0` **62 -> 51**, `videoK==0` 51 -> 51. Raporun yazmadigi, lehine olan
olcu: yeni 51 asan satirin **tamami** eskide de asiyordu (`comm -13` = 0), eskiden asan
**20** satir artik hedefin altinda - plan seviyesinde sifir regresyon. K12'nin uc ham
ciktisi `basarili=True denemeler=2`, ffprobe'da ucunde de `aac ... channels=1`, bayt
boyutlari yeniden hesaplandi (0,984 / 0,729 / 0,984 MiB). K13 pimi denetcinin **kendi
kostugu** mutasyonla dogrulandi (repo disi kopyada): 123/123 -> 122/1, dusen tek olcu
`K11_BuceTavanaCakiliKalanDalHedefiAsmaz`; esik gevsek degil (gercek 9,704 / esik 10,5 /
mutasyonla 15,527). **Fazladan:** `391/545/980/988` mutasyonu da ayni olcuyu dusuruyor -
tur 2'nin borc 2'si kapandi. K14'un dordu de kapali: K1=**55** (5/11/17/22), envanterde
980/988, kok nedende `Correct()` ve 14/26/26/40/78, `:257` ESKI hucresi basarisiz/dosya
yok, K4 blogu dort satir birebir, bes bos TSV silinmis, borc 4 yazilmis. Kollar:
temiz Release yapidan sonra **123/123** ve **66/66**, sifir bulan kol yok. `owns` temiz;
`EncodeRunner.cs`, `ConversionArguments.cs`, `FfmpegArguments.cs` yazilmamis.

### KRITIK 1 - `videoK=0` plani calistirilamayan bir ffmpeg komutu uretiyor

Rapor (`ses-tabani.md:345-352`) kalan 51 asan satiri *"tur 1'de kabul edilen ses-tabani
odunu"* diye kapatiyor. Bu niteleme yanlis; denetci olctu.

`PlanCalculator.cs:391` + `:545` (floor tur 1/2'de kaldirildi) `videoK`'yi **0**'a
indirebiliyor. Zincir: `FfmpegArguments.cs:411,415` -> `-b:v 0k -maxrate 0k -bufsize 0k`,
plan modu `2pass` (`PlanCalculator.cs:544-545`) -> `-pass 1` / `-pass 2`. Gercek ffmpeg
kosumu (10 s testsrc2 + sine, libx264):

```
[libx264] CRF/CQP is incompatible with 2pass.
[vost#0:0/libx264] Error while opening encoder - maybe incorrect parameters such as bit_rate, rate, width or height.
[vost#0:0/libx264] Task finished with error code: -22 (Invalid argument)
Conversion failed!        -> zero2p.mp4 = 0 bayt
```

Tek-pasta ise `-b:v 0k` sessizce **CRF 23 varsayilanina** dusuyor: `kb/s:2694.69`
(3,41 MB / 10 s), `-b:v 48k` referansi `kb/s:52.38` (0,11 MB / 10 s). Sifir bit hizi
"video yok" degil, "yaklasik kayipsiza yakin kodla" demek.

`EncodeRunner.cs:450-451` sifir olmayan cikis kodunu `InvalidOperationException`
firlatiyor, dis `catch { TryDelete(partialPath); throw; }` yeniden firlatiyor. Kullanici
"hedef tutmadi, dosya yazilmadi" cumlesini degil **ham ffmpeg hatasini** goruyor.

Kurtarma yok: `PlanCalculator.cs:988`
`corrected.VideoBitrateK = Math.Max(0, (int)Math.Round(Math.Min(previousVideoK*factor, videoBudgetK)))`
- `previousVideoK == 0` iken `0*factor = 0`, `Math.Min(0, ...) = 0`. **0 sabit nokta.**
Denetcinin olcumu: `correct1=0  correct2=0`.

Bandin buyuklugu (`tur3-after-sweep.tsv`, 171 satirin 51'i; her sure icin en buyuk hedef):

| sure | videoK=0 olan en buyuk hedef |
|---|---|
| 5 dk | 0,5 MB |
| 10 dk | 1,5 MB |
| 20 dk | 3 MB |
| 30 dk | 5 MB |
| **60 dk** | **10 MB** |
| 90 dk | 15 MB |
| **120 dk** | **20 MB** |

60dk/10MB (Discord) ve 120dk/20MB gundelik senaryolar. Aritmetik icerikten bagimsiz:
`PickAudio` tabani `Math.Max(24, audioK)` oldugu icin kaynakta ses varsa `audioK >= 24`;
`hedefMB*8388,608/sure_s <= ~24` olan her yerde `videoK = 0`.

**main'e gore regresyon:** `eski-ciktisi.tsv` ayni satirlarda `videoK=48` veriyordu -
kosum temiz bitiyor, hedefi kaciriyor, `EncodeRunner.cs:187` durust mesaji yaziyordu
("Stayed over the 10 MB target after 3 attempts; no file was written."). Simdi ayni
kombinasyon **ffmpeg istisnasi** ile oluyor. Tur 1'in dusme gerekcesi bu bantta
*"dosyam yok + ham hata"*a donusmus.

**Plan kendi tahminini de yanlis soyluyor:** `PlanCalculator.cs:997-998`
`EstimatedMb = SizeMb(0, 24, sure)`. 60dk/10MB icin ekrana **10,351 MB** yaziyor;
gercekte uretilecek sey CRF-23 boyutu, iki mertebe fazlasi - ya da hic.

**Kim uretti:** K11 (`:505/511/520/528`) bu satirlari **yaratmadi** - iki supurmede de
`videoK=0` sayisi 51 ve 51'inin notu `BudgetBelowCeilingTwoPass` (yani `:545`, tur 1/2).
Kusur devralinmis; tur 3 onu olcmek yerine "kabul edilen odun" diye muhurlemis. Kayitli
kusur sinifinin ucuncu gorunumu.

### Borc bulgulari (tur 3)

1. **K11'in dali gercek kodlamayla hic dogrulanmadi** (`ses-tabani.md:379-386`). Uc
   kosumun ucu de `TargetEnforcedTwoPass` (`:545`) notunu tasiyor. Ciddiyeti sinirli -
   `:545` dali 48-alti videoK'lari (21->18, 9->7) gercek kodlamayla dogruladi. Ama ikinci
   bir sorun aciyor: supurme 30dk/10MB'yi `:528` daline atiyor, gercek kosum ayni orana
   `:545`ten giriyor. **Sentetik supurmenin dal atfi guvenilir degil**; K11'in "bu satirlar
   tam olarak `:528`in dali" gerekcesi (`:194`) bu yuzden zayif.
2. `ses-tabani.md:191` ve `:341` **"30-120dk" yanlis, gercek 20-120dk** (11 cakili satirin
   biri 20dk/10MB). Tablo dogru, ozet cumle yanlis - imza kusurunun yeni ornegi.
3. `ses-tabani.md:345` "tek notu `BudgetBelowCeilingTwoPass`"; TSV'de not sutunu
   `BudgetBelowCeilingTwoPass,PredictedQualityEstimated` - **iki** kod. Ayni sinif.
4. `ses-tabani.md:371-373` K12 tablosunun `notlar:` sutunu tam liste degil. 30dk satiri
   iki not yaziyor, ham ciktida **sekiz** not var. K8 tablosu "notlar iceriyor:" diyerek
   dogru isaretlemisti, K12 bu ihtiyati dusurmus.
5. `ses-tabani.md:145` ve `:288` **hala 122 diyor**, guncel 123. Tur 3 bolumunde kol
   sayisini bildiren tablo yok; 123 yalniz `:403`te mutasyon ciktisinin icinde geciyor.
6. `SesTabaniTests.cs:75` test adi `K11_BuceTavanaCakiliKalanDalHedefiAsmaz` - "Butce"
   olacakti. Ayrica ad "tavana cakili kalan dal" diyor, olctugu sey tam tersi: floor'a
   **cakilmadigini** dogruluyor.
7. `ManualOverrideTests.cs:62-67` yorumu ve `K1_VarsayilanT165OncesiMotorlaBirebirAyni`
   adi hala yaniltici. Rapor borc yazmis (`:423-429`), duzeltmemis - dosya `owns` icinde,
   duzeltilebilirdi.
8. `PlanCalculator.cs:868` `QualityFloorTargetMb` = `SizeMb(MinVideoBitrateK, 0, sure)` -
   48k video + **0 kbps ses** varsayiyor. Ses artik hicbir zaman 0 olmadigina gore
   kalite-hedefli modun taban MB'si gercek tabanin altinda.

## Tur 4 - kapsam (T0)

**K15 - `videoK=0` plani uretilmesin.** Bugun bant genis (51 satir, 20dk/3MB'den
120dk/20MB'ye) ve iki gundelik senaryoyu (60dk/10MB, 120dk/20MB) iceriyor. Cozum
`PlanCalculator.cs` icinde, `owns` icinde. Iki yoldan **birini** sec ve gerekcesini yaz:

- (a) `videoK`'yi kodlayicinin gercekten calistirabilecegi bir alt sinira yukselt ve
  hedefin tutulamayacagini planda bir notla (`TargetBelowCodecFloor` gibi) bildir; ya da
- (b) hedef `SizeMb(0, audioK, sure)`'nin altindaysa plani hic uretme ve kullaniciya
  "bu sure icin ses tabaniyla en kucuk dosya X MB" de - `EncodeRunner`'a girmeden,
  alti ffmpeg pasi harcamadan.

Hangi yolu secersen sec: `Correct()` yeniden-deneme kolundaki **0 sabit noktasi**
(`:988`) da kapanmali. Kullanicinin sozu degismedi - "ses her zaman olmak zorunda" -
ama teslim edilen sey **calisan bir dosya ya da durust bir cumle** olmali; ham ffmpeg
istisnasi ikisi de degil.
CHECK: supurmeyi yeniden kos, `videoK=0` satir sayisi once/sonra (bugun 51), ham TSV.

**K16 - Bandin icinden gercek kodlama.** `videoK=0` bandindan **en az iki** kombinasyon
(en az biri 60dk/10MB veya 120dk/20MB oraninda) gercek `EncodeRunner` ile kosulacak.
Kisa kesit serbest, **sure/hedef oranini koru**. Kabul edilebilir iki sonuc var:
(1) dosya var, sesli, hedefin altinda; ya da (2) dosya yok ama kullaniciya giden mesaj
**durust bir cumle** (ham ffmpeg hatasi degil). Hangisi ciktiysa onu yaz.
CHECK: ikisinin ham ciktisi, `ffprobe` satirlari, ve dosya yoksa kullaniciya giden tam metin.

**K17 - `videoK=0` pimlensin.** `videoK == 0` uretmeyi geri getiren bir mutasyon en az bir
olcuyu dusurmeli. `SesTabaniTests.cs`e ekle; mutasyonu kos, kirilan olcunun adini ham
ciktiyla goster. Mevcut `K11_...` olcusunun bunu kapsamadigini dogrula.
CHECK: mutasyon izgarasi, kirilan olcu adi, ham cikti.

**K18 - Tur 3'un sekiz borcunu kapat.** 1'den 8'e kadar hepsi yukarida `dosya:satir`
capasiyla yazili. 2, 3, 4, 5 belge duzeltmesi; 6 ve 7 test adi/yorumu (`owns` icinde,
duzelt); 1 K11'in dal atfini zayiflatmak (supurmenin dal atfina dayanan cumleleri
yumusat ya da olcuyle destekle); 8 `:868` tutarsizligi - **kapsam disi kalabilir ama
raporda acikca borc olarak dursun**, sessizce gecme.
CHECK: her duzeltilen sayinin yaninda uretildigi ham dosyanin yolu.

**Siniri hatirlat:** `EncodeRunner.cs`, `ConversionArguments.cs`, `FfmpegArguments.cs`
okunacak, yazilmayacak. Cozum onlari degistirmeyi gerektiriyorsa **yazma, bildir.**
